BJR PATCH EVIDENCE BUNDLE — PLAYBACK DASHBOARD CLOSE-OUT
PWD: /Users/brendan/Projects/Angelfish-Records/BJR/web
Sonar source: sonarqube-export-20260810-132041/sonarqube-assistant-bundle.json
Sonar analysis date: 2026-08-10T01:06:42+0000


===== FRESH SONAR FINDINGS — EXACT TARGET SCOPE =====

app/admin/playback/PlaybackTelemetryDashboardClient.tsx:30  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/playback/dashboard/AudienceSplitCard.tsx:143  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/playback/dashboard/AudienceSplitCard.tsx:206  typescript:S6819  severity=MAJOR  impact=MEDIUM
  Use <img alt=...>, or <img alt=...> instead of the "img" role to ensure accessibility across all devices.

app/admin/playback/dashboard/AudienceSplitCard.tsx:229  typescript:S3358  severity=MAJOR  impact=MEDIUM
  Extract this nested ternary operation into an independent statement.

app/admin/playback/dashboard/AudienceSplitCard.tsx:327  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/playback/dashboard/DedupeTable.tsx:28  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx:16  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx:77  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx:92  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx:130  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx:157  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:69  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:218  typescript:S6819  severity=MAJOR  impact=MEDIUM
  Use <img alt=...>, or <img alt=...> instead of the "img" role to ensure accessibility across all devices.

app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:287  typescript:S7755  severity=MINOR  impact=LOW
  Prefer `.at(…)` over `[….length - index]`.

app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:290  typescript:S6479  severity=MAJOR  impact=MEDIUM
  Do not use Array index in keys

app/admin/playback/dashboard/ShareLinkActivityTable.tsx:29  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/playback/dashboard/TrackTable.tsx:21  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/playback/dashboard/TrackTable.tsx:61  typescript:S3358  severity=MAJOR  impact=MEDIUM
  Extract this nested ternary operation into an independent statement.

app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts:72  typescript:S3776  severity=CRITICAL  impact=HIGH
  Refactor this function to reduce its Cognitive Complexity from 18 to the 15 allowed.

app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts:78  typescript:S7755  severity=MINOR  impact=LOW
  Prefer `.at(…)` over `[….length - index]`.

app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts:104  typescript:S3358  severity=MAJOR  impact=MEDIUM
  Extract this nested ternary operation into an independent statement.

app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts:106  typescript:S3358  severity=MAJOR  impact=MEDIUM
  Extract this nested ternary operation into an independent statement.

app/admin/playback/page.tsx:7  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/home/usePlaybackReconciliation.ts:91  typescript:S4624  severity=MAJOR  impact=MEDIUM
  Refactor this code to not use nested template literals.

TOTAL TARGET FINDINGS: 24

===== CURRENT SHA-256 — PATCH TARGETS =====

1378afbd3bd536d4411c2c1e6c2af163e781426d1785a0f558e2e2cbeb68a1dd  app/admin/playback/PlaybackTelemetryDashboardClient.tsx
db8b20f475a0dcbb932f5067d288104bd3e4d2bf66d18b0c10b2273e4831b477  app/admin/playback/dashboard/AudienceSplitCard.tsx
332e5e915c891b43c8efe57cb8ba8406b5834abed59250becd69afdab0a363aa  app/admin/playback/dashboard/DedupeTable.tsx
2e41116e425ffca23893ef2717a04078a6da799aea05c4bf4d57b12a34e46a92  app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx
45e1bce08ea863f8909381a6f262f13a55c632eaf0da6cb3fc41d9d58bc379c6  app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx
efcb363087f44f7656a36bfe37adbfa62ba0c209bb52f4ddaa952a7dc7fd2204  app/admin/playback/dashboard/ShareLinkActivityTable.tsx
d69a513d90d617ff80977a512ec8a4e96b68118c2de1532c26db3c6b5fb6c79b  app/admin/playback/dashboard/TrackTable.tsx
d8adf0e11ea61217f2f796e266d7c3574610c14877076ab575213e86f356bec1  app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts
a48d804da737e050695a597112c62c41c94603465c1a0398c9b847bb44f90b80  app/admin/playback/page.tsx
f2ec25b604da3ebce96a3926b2644294101bb35f315e2c568e63ab958bba65fe  app/home/usePlaybackReconciliation.ts

===== COMPLETE CURRENT SOURCE — DASHBOARD TARGETS =====


----- app/admin/playback/PlaybackTelemetryDashboardClient.tsx -----
// web/app/admin/playback/PlaybackTelemetryDashboardClient.tsx
"use client";

import React from "react";
import { useRouter } from "next/navigation";
import type { PlaybackAdminSnapshot } from "@/lib/playbackAdmin";
import AdminPageFrame from "../AdminPageFrame";
import { AudienceSplitCard } from "./dashboard/AudienceSplitCard";
import { DedupeTable } from "./dashboard/DedupeTable";
import {
  MetricPill,
  SectionCard,
  TrendRangeToggle,
} from "./dashboard/PlaybackDashboardPrimitives";
import { QualifiedPlayTrendChart } from "./dashboard/QualifiedPlayTrendChart";
import { TrackTable } from "./dashboard/TrackTable";
import { ShareLinkActivityTable } from "./dashboard/ShareLinkActivityTable";
import {
  FONT_SIZE_UI,
  TEXT_MUTED,
  TEXT_PRIMARY,
} from "./dashboard/playbackTelemetryDashboardStyles";
import {
  formatAgo,
  formatNumber,
  fmtSnapshotStamp,
} from "./dashboard/playbackTelemetryDashboardFormatters";
import type { TrendRangeKey } from "./dashboard/types";

export default function PlaybackTelemetryDashboardClient(props: {
  embed: boolean;
  initialSnapshot: PlaybackAdminSnapshot;
}) {
  const router = useRouter();
  const [autoRefresh, setAutoRefresh] = React.useState(true);
  const [refreshing, setRefreshing] = React.useState(false);
  const [trendRange, setTrendRange] = React.useState<TrendRangeKey>("day");

  const snapshot = props.initialSnapshot;
  const selectedTrend = snapshot.qualifiedPlayTrends[trendRange];
  const recentPlayTotal = selectedTrend.buckets.reduce(
    (sum, row) => sum + row.sitePlayCount,
    0,
  );

  React.useEffect(() => {
    if (!autoRefresh) return;

    const id = window.setInterval(() => {
      setRefreshing(true);
      router.refresh();
    }, 20_000);

    return () => window.clearInterval(id);
  }, [autoRefresh, router]);

  React.useEffect(() => {
    setRefreshing(false);
  }, [snapshot.generatedAt]);

  const headerActions = (
    <>
      <button
        type="button"
        onClick={() => {
          setRefreshing(true);
          router.refresh();
        }}
        style={{
          height: 30,
          padding: "0 12px",
          borderRadius: 999,
          border: "1px solid rgba(255,255,255,0.14)",
          background: "rgba(255,255,255,0.04)",
          color: TEXT_PRIMARY,
          cursor: "pointer",
          fontSize: FONT_SIZE_UI,
          fontWeight: 700,
          opacity: refreshing ? 0.72 : 1,
        }}
      >
        {refreshing ? "Refreshing…" : "Refresh now"}
      </button>

      <button
        type="button"
        onClick={() => setAutoRefresh((value) => !value)}
        style={{
          height: 30,
          padding: "0 12px",
          borderRadius: 999,
          border: "1px solid rgba(255,255,255,0.14)",
          background: autoRefresh
            ? "rgba(255,255,255,0.10)"
            : "rgba(255,255,255,0.04)",
          color: TEXT_PRIMARY,
          cursor: "pointer",
          fontSize: FONT_SIZE_UI,
          fontWeight: 700,
          opacity: autoRefresh ? 1 : 0.82,
        }}
      >
        Auto-refresh: {autoRefresh ? "On" : "Off"}
      </button>
    </>
  );

  return (
    <AdminPageFrame
      embed={props.embed}
      maxWidth={1360}
      title="Playback telemetry"
      subtitle="Monitor site-wide qualified play trends, audience provenance, top tracks by listened time, and telemetry dedupe behaviour."
      headerActions={headerActions}
    >
      <div
        style={{
          display: "grid",
          gap: 16,
        }}
      >
        <div
          style={{
            display: "flex",
            gap: 10,
            alignItems: "center",
            justifyContent: "space-between",
            flexWrap: "wrap",
          }}
        >
          <div
            style={{
              display: "flex",
              gap: 10,
              flexWrap: "wrap",
            }}
          >
            <MetricPill
              label="Plays · all time"
              value={formatNumber(snapshot.siteTotals.playCount)}
            />
            <MetricPill
              label="Plays · 30d"
              value={formatNumber(snapshot.site30d.playCount)}
            />
            <MetricPill
              label={`Plays · ${selectedTrend.label}`}
              value={formatNumber(recentPlayTotal)}
            />
          </div>

          <div
            style={{
              fontSize: FONT_SIZE_UI,
              lineHeight: 1.5,
              color: TEXT_MUTED,
            }}
          >
            Generated {formatAgo(snapshot.generatedAt)} · snapshot{" "}
            {fmtSnapshotStamp(snapshot.generatedAt)}
          </div>
        </div>

        <SectionCard
          title="Qualified plays over time"
          subtitle=""
          headerRight={
            <TrendRangeToggle value={trendRange} onChange={setTrendRange} />
          }
        >
          <QualifiedPlayTrendChart
            rows={selectedTrend.buckets}
            range={trendRange}
          />
        </SectionCard>

        <div
          style={{
            display: "grid",
            gap: 16,
            gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
            alignItems: "stretch",
          }}
        >
          <AudienceSplitCard snapshot={snapshot} />

          <SectionCard title="Top tracks by listened time" subtitle="">
            <TrackTable rows={snapshot.topTracksByListenedMs} />
          </SectionCard>
        </div>

        <SectionCard
          title="Share-link activity"
          subtitle="Playback attributed to currently valid labelled share links. This identifies the link used, not a verified individual listener."
        >
          <ShareLinkActivityTable rows={snapshot.shareLinkActivity} />
        </SectionCard>

        <SectionCard title="Recent telemetry sessions" subtitle="">
          <DedupeTable rows={snapshot.recentDedupe} />
        </SectionCard>
      </div>
    </AdminPageFrame>
  );
}


----- app/admin/playback/dashboard/AudienceSplitCard.tsx -----
// web/app/admin/playback/dashboard/AudienceSplitCard.tsx
"use client";

import React from "react";
import type { PlaybackAdminSnapshot } from "@/lib/playbackAdmin";
import { SectionCard } from "./PlaybackDashboardPrimitives";
import {
  BG_ANON,
  BG_INSET,
  BG_MEMBER,
  TEXT_FAINT,
  TEXT_MUTED,
  TEXT_PRIMARY,
} from "./playbackTelemetryDashboardStyles";
import {
  formatNumber,
  percentage,
} from "./playbackTelemetryDashboardFormatters";

type SliceKey = "member" | "anonymous";

type AudienceDonutProps = {
  label: string;
  member: number;
  anonymous: number;
};

type SegmentArc = {
  key: SliceKey;
  label: string;
  value: number;
  pct: number;
  color: string;
  path: string;
  labelX: number;
  labelY: number;
};

function polarToCartesian(
  cx: number,
  cy: number,
  radius: number,
  angleInDegrees: number,
) {
  const radians = ((angleInDegrees - 90) * Math.PI) / 180;
  return {
    x: cx + radius * Math.cos(radians),
    y: cy + radius * Math.sin(radians),
  };
}

function describeDonutArc(
  cx: number,
  cy: number,
  outerRadius: number,
  innerRadius: number,
  startAngle: number,
  endAngle: number,
) {
  const startOuter = polarToCartesian(cx, cy, outerRadius, endAngle);
  const endOuter = polarToCartesian(cx, cy, outerRadius, startAngle);
  const startInner = polarToCartesian(cx, cy, innerRadius, endAngle);
  const endInner = polarToCartesian(cx, cy, innerRadius, startAngle);
  const angleDelta = endAngle - startAngle;
  const largeArcFlag: 0 | 1 = angleDelta > 180 ? 1 : 0;

  const path = [
    `M ${startOuter.x} ${startOuter.y}`,
    `A ${outerRadius} ${outerRadius} 0 ${largeArcFlag} 0 ${endOuter.x} ${endOuter.y}`,
    `L ${endInner.x} ${endInner.y}`,
    `A ${innerRadius} ${innerRadius} 0 ${largeArcFlag} 1 ${startInner.x} ${startInner.y}`,
    "Z",
  ].join(" ");

  return { path };
}

function buildSegments(
  member: number,
  anonymous: number,
  cx: number,
  cy: number,
  outerRadius: number,
  innerRadius: number,
): SegmentArc[] {
  const total = member + anonymous;

  const slices: Array<{
    key: SliceKey;
    label: string;
    value: number;
    pct: number;
    color: string;
  }> = [
    {
      key: "member",
      label: "Members",
      value: member,
      pct: percentage(member, total),
      color: BG_MEMBER,
    },
    {
      key: "anonymous",
      label: "Anonymous",
      value: anonymous,
      pct: percentage(anonymous, total),
      color: BG_ANON,
    },
  ];

  let currentAngle = 0;

  return slices
    .filter((slice) => slice.value > 0 && total > 0)
    .map((slice) => {
      const sweep = (slice.value / total) * 360;
      const startAngle = currentAngle;
      const endAngle = currentAngle + sweep;
      currentAngle = endAngle;

      const { path } = describeDonutArc(
        cx,
        cy,
        outerRadius,
        innerRadius,
        startAngle,
        endAngle,
      );

      const midAngle = startAngle + sweep / 2;
      const labelRadius = innerRadius + (outerRadius - innerRadius) * 0.58;
      const labelPosition = polarToCartesian(cx, cy, labelRadius, midAngle);

      return {
        ...slice,
        path,
        labelX: labelPosition.x,
        labelY: labelPosition.y,
      };
    });
}

function AudienceDonut(props: AudienceDonutProps) {
  const { label, member, anonymous } = props;
  const total = member + anonymous;
  const [hovered, setHovered] = React.useState<SliceKey | null>(null);

  const size = 272;
  const cx = size / 2;
  const cy = size / 2;
  const outerRadius = 110;
  const innerRadius = 58;

  const memberPct = percentage(member, total);
  const anonymousPct = percentage(anonymous, total);

  const segments = buildSegments(
    member,
    anonymous,
    cx,
    cy,
    outerRadius,
    innerRadius,
  );

  const hoveredSegment =
    hovered === null
      ? null
      : segments.find((segment) => segment.key === hovered) ?? null;

  const memberLabelVisible = member > 0 && memberPct >= 7;
  const anonymousLabelVisible = anonymous > 0 && anonymousPct >= 7;

  return (
    <div
      style={{
        padding: "6px 6px 2px",
        display: "grid",
        gap: 10,
      }}
    >
      <div
        style={{
          fontSize: 13,
          color: TEXT_PRIMARY,
          fontWeight: 700,
        }}
      >
        {label}
      </div>

      <div
        style={{
          display: "grid",
          placeItems: "center",
        }}
      >
        <div
          style={{
            position: "relative",
            width: "100%",
            maxWidth: size,
            aspectRatio: "1 / 1",
          }}
        >
          <svg
            viewBox={`0 0 ${size} ${size}`}
            style={{
              display: "block",
              width: "100%",
              height: "auto",
              overflow: "visible",
            }}
            aria-label={`${label} audience split`}
            role="img"
          >
            {segments.map((segment) => {
              const isHovered = hovered === segment.key;
              const shouldShowPct =
                segment.key === "member"
                  ? memberLabelVisible
                  : anonymousLabelVisible;

              return (
                <g key={segment.key}>
                  <path
                    d={segment.path}
                    fill={segment.color}
                    opacity={hovered === null ? 1 : isHovered ? 1 : 0.42}
                    style={{
                      cursor: "default",
                      transition: "opacity 160ms ease, filter 160ms ease",
                      filter: isHovered ? "brightness(1.05)" : "none",
                    }}
                    onMouseEnter={() => setHovered(segment.key)}
                    onMouseLeave={() => setHovered(null)}
                    onFocus={() => setHovered(segment.key)}
                    onBlur={() => setHovered(null)}
                    tabIndex={0}
                  >
                    <title>
                      {segment.label}: {formatNumber(segment.value)} plays (
                      {segment.pct.toFixed(1)}%)
                    </title>
                  </path>

                  {shouldShowPct ? (
                    <text
                      x={segment.labelX}
                      y={segment.labelY}
                      textAnchor="middle"
                      dominantBaseline="middle"
                      style={{
                        fill: "#ffffff",
                        fontSize: 14,
                        fontWeight: 800,
                        pointerEvents: "none",
                        userSelect: "none",
                      }}
                    >
                      {segment.pct.toFixed(0)}%
                    </text>
                  ) : null}
                </g>
              );
            })}

            <circle cx={cx} cy={cy} r={innerRadius - 1} fill={BG_INSET} />

            <text
              x={cx}
              y={cy - 22}
              textAnchor="middle"
              style={{
                fill: TEXT_FAINT,
                fontSize: 11,
                fontWeight: 700,
                letterSpacing: "0.04em",
                textTransform: "uppercase",
                userSelect: "none",
                pointerEvents: "none",
              }}
            >
              {hoveredSegment ? hoveredSegment.label : label}
            </text>

            <text
              x={cx}
              y={cy + 4}
              textAnchor="middle"
              style={{
                fill: TEXT_PRIMARY,
                fontSize: hoveredSegment ? 20 : 24,
                fontWeight: 800,
                userSelect: "none",
                pointerEvents: "none",
              }}
            >
              {hoveredSegment
                ? formatNumber(hoveredSegment.value)
                : formatNumber(total)}
            </text>

            <text
              x={cx}
              y={cy + 26}
              textAnchor="middle"
              style={{
                fill: TEXT_MUTED,
                fontSize: 11,
                fontWeight: 600,
                userSelect: "none",
                pointerEvents: "none",
              }}
            >
              {hoveredSegment
                ? `${hoveredSegment.pct.toFixed(1)}%`
                : "qualified plays"}
            </text>
          </svg>
        </div>
      </div>
    </div>
  );
}

export function AudienceSplitCard(props: { snapshot: PlaybackAdminSnapshot }) {
  const allTimeMember = props.snapshot.audienceSplit.allTimeMemberPlayCount;
  const allTimeAnonymous =
    props.snapshot.audienceSplit.allTimeAnonymousPlayCount;
  const recentMember = props.snapshot.audienceSplit.recent30dMemberPlayCount;
  const recentAnonymous =
    props.snapshot.audienceSplit.recent30dAnonymousPlayCount;

  return (
    <SectionCard
      title="Qualified play audience split"
      subtitle=""
    >
      <div
        style={{
          display: "grid",
          gap: 18,
          gridTemplateColumns: "repeat(auto-fit, minmax(280px, 1fr))",
          alignItems: "start",
        }}
      >
        <AudienceDonut
          label="All time"
          member={allTimeMember}
          anonymous={allTimeAnonymous}
        />

        <AudienceDonut
          label="Past 30 days"
          member={recentMember}
          anonymous={recentAnonymous}
        />
      </div>
    </SectionCard>
  );
}

----- app/admin/playback/dashboard/DedupeTable.tsx -----
// web/app/admin/playback/dashboard/DedupeTable.tsx
"use client";

import React from "react";
import type { PlaybackAdminSnapshot } from "@/lib/playbackAdmin";
import type { DedupeRow } from "./types";
import { TableShell } from "./PlaybackDashboardPrimitives";
import {
  BG_ACCENT,
  BG_ANON,
  BG_MEMBER,
  FONT_SIZE_DEDUPE,
  ROW_BORDER,
  TEXT_FAINT,
  TEXT_MUTED,
  TEXT_PRIMARY,
  TEXT_STRONG,
} from "./playbackTelemetryDashboardStyles";
import {
  ellipsisMiddle,
  formatAgo,
} from "./playbackTelemetryDashboardFormatters";
import {
  buildDedupeSessionRows,
  resolveDedupeIdentityLabel,
} from "./playbackTelemetryDashboardModel";

export function DedupeTable(props: {
  rows: PlaybackAdminSnapshot["recentDedupe"];
}) {
  const rawRows = props.rows as DedupeRow[];
  const rows = React.useMemo(() => buildDedupeSessionRows(rawRows), [rawRows]);

  return (
    <TableShell>
      <table
        style={{
          width: "100%",
          borderCollapse: "collapse",
          minWidth: 1160,
        }}
      >
        <thead>
          <tr>
            {[
              "When",
              "Track / Session",
              "Progress",
              "Status",
              "Share link",
              "Listener",
            ].map((label) => (
              <th
                key={label}
                style={{
                  textAlign: "left",
                  padding: "8px 10px",
                  fontSize: FONT_SIZE_DEDUPE,
                  fontWeight: 700,
                  color: TEXT_FAINT,
                  borderBottom: ROW_BORDER,
                  whiteSpace: "nowrap",
                }}
              >
                {label}
              </th>
            ))}
          </tr>
        </thead>

        <tbody>
          {rows.length === 0 ? (
            <tr>
              <td
                colSpan={6}
                style={{
                  padding: 10,
                  fontSize: FONT_SIZE_DEDUPE,
                  color: TEXT_MUTED,
                }}
              >
                No rows.
              </td>
            </tr>
          ) : null}

          {rows.map((row) => {
            const identityLabel = resolveDedupeIdentityLabel({
              audience: row.audience,
              memberEmail: row.memberEmail,
              memberId: row.memberId,
            });

            const monospaceIdentity =
              row.memberEmail == null && row.memberId != null;

            const shareTokenLabel =
              row.sourceRows.find(
                (sourceRow) =>
                  typeof sourceRow.shareTokenLabel === "string" &&
                  sourceRow.shareTokenLabel.trim().length > 0,
              )?.shareTokenLabel ?? null;

            return (
              <tr key={row.groupKey}>
                <td
                  style={{
                    padding: "10px 10px",
                    borderBottom: ROW_BORDER,
                    color: TEXT_MUTED,
                    fontSize: FONT_SIZE_DEDUPE,
                    whiteSpace: "nowrap",
                    verticalAlign: "top",
                  }}
                >
                  <div>{formatAgo(row.latestAt)}</div>
                </td>

                <td
                  style={{
                    padding: "10px 10px",
                    borderBottom: ROW_BORDER,
                    color: TEXT_STRONG,
                    fontSize: FONT_SIZE_DEDUPE,
                    verticalAlign: "top",
                    minWidth: 220,
                  }}
                >
                  <div
                    style={{
                      color: TEXT_PRIMARY,
                      fontWeight: 700,
                    }}
                  >
                    {row.recordingTitle ??
                      `session ${ellipsisMiddle(row.playbackId, 10)}`}
                  </div>
                </td>

                <td
                  style={{
                    padding: "10px 10px",
                    borderBottom: ROW_BORDER,
                    fontSize: FONT_SIZE_DEDUPE,
                    verticalAlign: "top",
                    minWidth: 300,
                  }}
                >
                  <div
                    style={{
                      height: 12,
                      width: "100%",
                      borderRadius: 999,
                      overflow: "hidden",
                      background: BG_ACCENT,
                      border: "1px solid rgba(255,255,255,0.08)",
                    }}
                  >
                    <div
                      style={{
                        height: "100%",
                        width: `${row.progressPct}%`,
                        borderRadius: 999,
                        background:
                          row.audience === "member" ? BG_MEMBER : BG_ANON,
                        transition: "width 180ms ease-out",
                      }}
                    />
                  </div>
                </td>

                <td
                  style={{
                    padding: "10px 10px",
                    borderBottom: ROW_BORDER,
                    color: TEXT_STRONG,
                    fontSize: FONT_SIZE_DEDUPE,
                    whiteSpace: "nowrap",
                    verticalAlign: "top",
                  }}
                >
                  <span
                    style={{
                      display: "inline-flex",
                      alignItems: "center",
                      height: 20,
                      padding: "0 8px",
                      borderRadius: 999,
                      border: "1px solid rgba(255,255,255,0.12)",
                      background: row.hasComplete
                        ? "rgba(255,255,255,0.12)"
                        : "rgba(255,255,255,0.06)",
                      color: TEXT_PRIMARY,
                      fontSize: 10,
                      fontWeight: 700,
                      letterSpacing: "0.02em",
                      textTransform: "uppercase",
                    }}
                  >
                    {row.statusLabel}
                  </span>
                </td>

                <td
                  style={{
                    padding: "10px 10px",
                    borderBottom: ROW_BORDER,
                    color: TEXT_STRONG,
                    fontSize: FONT_SIZE_DEDUPE,
                    maxWidth: 260,
                    verticalAlign: "top",
                  }}
                >
                  {shareTokenLabel ? (
                    <span
                      style={{
                        display: "inline-flex",
                        maxWidth: "100%",
                        padding: "3px 7px",
                        borderRadius: 999,
                        border: "1px solid rgba(255,255,255,0.14)",
                        background: "rgba(255,255,255,0.06)",
                        color: TEXT_PRIMARY,
                        fontSize: 11,
                        fontWeight: 650,
                        overflow: "hidden",
                        textOverflow: "ellipsis",
                        whiteSpace: "nowrap",
                      }}
                      title={shareTokenLabel}
                    >
                      {shareTokenLabel}
                    </span>
                  ) : (
                    <span style={{ color: TEXT_MUTED }}>—</span>
                  )}
                </td>
                <td
                  style={{
                    padding: "10px 10px",
                    borderBottom: ROW_BORDER,
                    color: TEXT_STRONG,
                    fontSize: FONT_SIZE_DEDUPE,
                    maxWidth: 280,
                    verticalAlign: "top",
                    fontFamily: monospaceIdentity
                      ? 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace'
                      : undefined,
                  }}
                >
                  {identityLabel}
                </td>
              </tr>
            );
          })}
        </tbody>
      </table>
    </TableShell>
  );
}


----- app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx -----
// web/app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx
"use client";

import React from "react";
import type { TrendRangeKey } from "./types";
import {
  BG_INSET,
  BG_PANEL,
  FONT_SIZE_UI,
  PANEL_BORDER,
  TEXT_FAINT,
  TEXT_MUTED,
  TEXT_PRIMARY,
} from "./playbackTelemetryDashboardStyles";

export function SectionCard(props: {
  title: string;
  subtitle?: string;
  children: React.ReactNode;
  headerRight?: React.ReactNode;
}) {
  return (
    <section
      style={{
        border: PANEL_BORDER,
        borderRadius: 14,
        padding: 14,
        background: BG_PANEL,
        display: "grid",
        gap: 12,
      }}
    >
      <div
        style={{
          display: "flex",
          alignItems: "flex-start",
          justifyContent: "space-between",
          gap: 12,
          flexWrap: "wrap",
        }}
      >
        <div>
          <div
            style={{
              fontSize: FONT_SIZE_UI,
              fontWeight: 800,
              color: TEXT_PRIMARY,
              lineHeight: 1.4,
            }}
          >
            {props.title}
          </div>

          {props.subtitle ? (
            <div
              style={{
                marginTop: 4,
                fontSize: FONT_SIZE_UI,
                lineHeight: 1.5,
                color: TEXT_MUTED,
                maxWidth: 780,
              }}
            >
              {props.subtitle}
            </div>
          ) : null}
        </div>

        {props.headerRight ? <div>{props.headerRight}</div> : null}
      </div>

      {props.children}
    </section>
  );
}

export function TableShell(props: { children: React.ReactNode }) {
  return (
    <div
      style={{
        overflowX: "auto",
        borderRadius: 12,
        border: PANEL_BORDER,
        background: BG_INSET,
      }}
    >
      {props.children}
    </div>
  );
}

export function MetricPill(props: { label: string; value: string }) {
  return (
    <div
      style={{
        minWidth: 120,
        padding: "10px 12px",
        borderRadius: 12,
        border: PANEL_BORDER,
        background: BG_INSET,
        display: "grid",
        gap: 4,
      }}
    >
      <div
        style={{
          fontSize: 10,
          color: TEXT_FAINT,
          fontWeight: 700,
          letterSpacing: "0.03em",
          textTransform: "uppercase",
        }}
      >
        {props.label}
      </div>
      <div
        style={{
          fontSize: 16,
          color: TEXT_PRIMARY,
          fontWeight: 800,
          lineHeight: 1.2,
        }}
      >
        {props.value}
      </div>
    </div>
  );
}

export function AudienceBadge(props: { audience: "member" | "anonymous" }) {
  const isMember = props.audience === "member";

  return (
    <span
      style={{
        display: "inline-flex",
        alignItems: "center",
        height: 20,
        padding: "0 8px",
        borderRadius: 999,
        border: "1px solid rgba(255,255,255,0.12)",
        background: isMember
          ? "rgba(255,255,255,0.10)"
          : "rgba(255,255,255,0.04)",
        color: isMember ? TEXT_PRIMARY : TEXT_MUTED,
        fontSize: 10,
        fontWeight: 700,
        letterSpacing: "0.02em",
        textTransform: "uppercase",
      }}
    >
      {isMember ? "Member" : "Anonymous"}
    </span>
  );
}

export function TrendRangeToggle(props: {
  value: TrendRangeKey;
  onChange: (value: TrendRangeKey) => void;
}) {
  const options: Array<{ key: TrendRangeKey; label: string }> = [
    { key: "hour", label: "Hour" },
    { key: "day", label: "Day" },
    { key: "week", label: "Week" },
    { key: "month", label: "Month" },
    { key: "year", label: "Year" },
    { key: "allTime", label: "All time" },
  ];

  return (
    <div
      style={{
        display: "inline-flex",
        gap: 6,
        flexWrap: "wrap",
      }}
    >
      {options.map((option) => {
        const selected = props.value === option.key;

        return (
          <button
            key={option.key}
            type="button"
            onClick={() => props.onChange(option.key)}
            style={{
              height: 30,
              padding: "0 12px",
              borderRadius: 999,
              border: "1px solid rgba(255,255,255,0.14)",
              background: selected
                ? "rgba(255,255,255,0.12)"
                : "rgba(255,255,255,0.04)",
              color: selected ? TEXT_PRIMARY : TEXT_MUTED,
              cursor: "pointer",
              fontSize: FONT_SIZE_UI,
              fontWeight: 700,
            }}
          >
            {option.label}
          </button>
        );
      })}
    </div>
  );
}

----- app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx -----
// web/app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx
"use client";

import React from "react";
import type { ChartPoint, TrendBucket, TrendRangeKey } from "./types";
import {
  BG_INSET,
  FONT_SIZE_UI,
  PANEL_BORDER,
  TEXT_FAINT,
  TEXT_MUTED,
  TEXT_PRIMARY,
} from "./playbackTelemetryDashboardStyles";
import {
  fmtTrendTick,
  formatNumber,
} from "./playbackTelemetryDashboardFormatters";

function buildLinearCommands(points: ChartPoint[]): string {
  return points
    .slice(1)
    .map((point) => ` L ${point.x} ${point.y}`)
    .join("");
}

function buildLinearLinePath(points: ChartPoint[]): string {
  if (points.length === 0) return "";
  return `M ${points[0].x} ${points[0].y}${buildLinearCommands(points)}`;
}

function buildLinearAreaPath(upper: ChartPoint[], lower: ChartPoint[]): string {
  if (upper.length === 0 || lower.length === 0) return "";

  const reversedLower = [...lower].reverse();

  return [
    `M ${upper[0].x} ${upper[0].y}`,
    buildLinearCommands(upper),
    ` L ${reversedLower[0].x} ${reversedLower[0].y}`,
    buildLinearCommands(reversedLower),
    " Z",
  ].join("");
}

type HoverZone = {
  index: number;
  x: number;
  left: number;
  width: number;
};

type LayerDescriptor = {
  key: string;
  label: string;
  fill: string;
  values: number[];
};

type StackedLayer = {
  key: string;
  label: string;
  fill: string;
  lowerPoints: ChartPoint[];
  upperPoints: ChartPoint[];
  areaPath: string;
  linePath: string;
};

export function QualifiedPlayTrendChart(props: {
  rows: TrendBucket[];
  range: TrendRangeKey;
}) {
  const rows = props.rows;
  const width = 920;
  const height = 240;
  const paddingTop = 12;
  const paddingRight = 14;
  const paddingBottom = 32;
  const paddingLeft = 46;
  const innerWidth = width - paddingLeft - paddingRight;
  const innerHeight = height - paddingTop - paddingBottom;

  const [activeIndex, setActiveIndex] = React.useState<number | null>(null);

  const series: LayerDescriptor[] = [
    {
      key: "anonymous",
      label: "Anonymous plays",
      fill: "rgba(255,255,255,0.18)",
      values: rows.map((row) => row.anonymousPlayCount),
    },
    {
      key: "member",
      label: "Member plays",
      fill: "rgba(255,255,255,0.56)",
      values: rows.map((row) => row.memberPlayCount),
    },
  ];

  const maxTotal = Math.max(1, ...rows.map((row) => row.sitePlayCount));
  const yTickCount = 4;
  const yTicks = Array.from({ length: yTickCount + 1 }, (_, index) => {
    const ratio = index / yTickCount;
    const value = Math.round(maxTotal * (1 - ratio));
    const y = paddingTop + innerHeight * ratio;
    return { value, y };
  });

  const xForIndex = (index: number): number => {
    if (rows.length <= 1) return paddingLeft + innerWidth / 2;
    return paddingLeft + (index / (rows.length - 1)) * innerWidth;
  };

  const yForValue = (value: number): number =>
    paddingTop + innerHeight - (value / maxTotal) * innerHeight;

  const stackedLayers: StackedLayer[] = series.map((layer, layerIndex) => {
    const lowerValues = rows.map((_, rowIndex) =>
      series
        .slice(0, layerIndex)
        .reduce((sum, candidate) => sum + candidate.values[rowIndex], 0),
    );

    const upperValues = lowerValues.map(
      (lower, rowIndex) => lower + layer.values[rowIndex],
    );

    const lowerPoints = lowerValues.map((value, rowIndex) => ({
      x: xForIndex(rowIndex),
      y: yForValue(value),
    }));

    const upperPoints = upperValues.map((value, rowIndex) => ({
      x: xForIndex(rowIndex),
      y: yForValue(value),
    }));

    return {
      key: layer.key,
      label: layer.label,
      fill: layer.fill,
      lowerPoints,
      upperPoints,
      areaPath: buildLinearAreaPath(upperPoints, lowerPoints),
      linePath: buildLinearLinePath(upperPoints),
    };
  });

  const tickIndexes = Array.from(
    new Set([
      0,
      Math.floor((rows.length - 1) * 0.33),
      Math.floor((rows.length - 1) * 0.66),
      rows.length - 1,
    ]),
  ).filter((index) => index >= 0 && index < rows.length);

  const hoverZones: HoverZone[] = rows.map((_, index) => {
    const x = xForIndex(index);
    const prevX = index > 0 ? xForIndex(index - 1) : x;
    const nextX = index < rows.length - 1 ? xForIndex(index + 1) : x;

    const left = index === 0 ? paddingLeft : x - (x - prevX) / 2;

    const right =
      index === rows.length - 1 ? width - paddingRight : x + (nextX - x) / 2;

    return {
      index,
      x,
      left,
      width: Math.max(1, right - left),
    };
  });

  const activeRow =
    activeIndex !== null && activeIndex >= 0 && activeIndex < rows.length
      ? rows[activeIndex]
      : null;

  const activeZone =
    activeIndex !== null && activeIndex >= 0 && activeIndex < hoverZones.length
      ? hoverZones[activeIndex]
      : null;

  const tooltipItems =
    activeRow === null
      ? []
      : [
          {
            key: "total",
            label: "Total plays",
            value: activeRow.sitePlayCount,
            swatch: "rgba(255,255,255,0.9)",
          },
          ...series.map((layer) => ({
            key: layer.key,
            label: layer.label,
            value: layer.values[activeIndex ?? 0] ?? 0,
            swatch: layer.fill,
          })),
        ];

  return (
    <div
      style={{
        display: "grid",
        gap: 10,
      }}
    >
      <div
        style={{
          position: "relative",
          background: BG_INSET,
          padding: "10px 12px 6px",
        }}
      >
        <svg
          viewBox={`0 0 ${width} ${height}`}
          style={{ width: "100%", height: "auto", display: "block" }}
          role="img"
          aria-label={`Qualified plays over the selected ${props.range} range`}
        >
          {yTicks.map((tick) => (
            <g key={`${tick.value}:${tick.y}`}>
              <line
                x1={paddingLeft}
                y1={tick.y}
                x2={width - paddingRight}
                y2={tick.y}
                stroke="rgba(255,255,255,0.08)"
                strokeWidth="1"
              />
              <text
                x={paddingLeft - 8}
                y={tick.y + 3}
                textAnchor="end"
                fill={TEXT_FAINT}
                fontSize="10"
              >
                {formatNumber(tick.value)}
              </text>
            </g>
          ))}

          {activeZone ? (
            <rect
              x={activeZone.left}
              y={paddingTop}
              width={activeZone.width}
              height={innerHeight}
              fill="rgba(255,255,255,0.035)"
            />
          ) : null}

          {stackedLayers.map((layer) => (
            <path
              key={`${layer.key}:fill`}
              d={layer.areaPath}
              fill={layer.fill}
              fillOpacity={activeIndex === null ? 0.72 : 0.56}
              stroke="none"
            />
          ))}

          {stackedLayers.map((layer, layerIndex) => {
            const isTopLayer = layerIndex === stackedLayers.length - 1;

            return (
              <path
                key={`${layer.key}:line`}
                d={layer.linePath}
                fill="none"
                stroke={
                  isTopLayer
                    ? "rgba(255,255,255,0.88)"
                    : "rgba(255,255,255,0.38)"
                }
                strokeWidth={isTopLayer ? "1.6" : "1"}
                strokeLinejoin="round"
                strokeLinecap="round"
                opacity={activeIndex === null ? 1 : 0.9}
              />
            );
          })}

          {stackedLayers[stackedLayers.length - 1]?.upperPoints.map(
            (point, index) => (
              <circle
                key={`point:${index}`}
                cx={point.x}
                cy={point.y}
                r="2"
                fill="rgba(255,255,255,0.72)"
                opacity={
                  activeIndex === null || activeIndex === index ? 1 : 0.32
                }
              />
            ),
          )}

          {activeZone ? (
            <line
              x1={activeZone.x}
              y1={paddingTop}
              x2={activeZone.x}
              y2={paddingTop + innerHeight}
              stroke="rgba(255,255,255,0.22)"
              strokeWidth="1"
              strokeDasharray="3 4"
            />
          ) : null}

          {activeIndex !== null
            ? stackedLayers.map((layer) => {
                const point = layer.upperPoints[activeIndex];
                if (!point) return null;

                return (
                  <g key={`${layer.key}:marker`}>
                    <circle
                      cx={point.x}
                      cy={point.y}
                      r="4"
                      fill="rgba(10,10,14,0.96)"
                    />
                    <circle
                      cx={point.x}
                      cy={point.y}
                      r="2.5"
                      fill="rgba(255,255,255,0.92)"
                    />
                  </g>
                );
              })
            : null}

          {tickIndexes.map((index) => (
            <text
              key={rows[index]?.bucketStart ?? index}
              x={xForIndex(index)}
              y={height - 8}
              textAnchor="middle"
              fill={TEXT_FAINT}
              fontSize="8"
            >
              {rows[index]
                ? fmtTrendTick(rows[index].bucketStart, props.range)
                : ""}
            </text>
          ))}

          {hoverZones.map((zone) => {
            const row = rows[zone.index];
            const label = row
              ? fmtTrendTick(row.bucketStart, props.range)
              : "Trend bucket";

            return (
              <rect
                key={`hover:${zone.index}`}
                x={zone.left}
                y={paddingTop}
                width={zone.width}
                height={innerHeight}
                fill="transparent"
                tabIndex={0}
                aria-label={`${label}. ${row ? formatNumber(row.sitePlayCount) : 0} total plays.`}
                onMouseEnter={() => setActiveIndex(zone.index)}
                onMouseLeave={() => setActiveIndex(null)}
                onFocus={() => setActiveIndex(zone.index)}
                onBlur={() => setActiveIndex(null)}
              />
            );
          })}
        </svg>

        {activeRow && activeZone ? (
          <div
            style={{
              position: "absolute",
              left: `${(activeZone.x / width) * 100}%`,
              top: 14,
              transform:
                activeZone.x > width * 0.72
                  ? "translateX(calc(-100% - 10px))"
                  : "translateX(10px)",
              pointerEvents: "none",
              zIndex: 2,
              minWidth: 170,
              maxWidth: 220,
              border: PANEL_BORDER,
              borderRadius: 12,
              background: "rgba(12,12,16,0.94)",
              boxShadow: "0 10px 26px rgba(0,0,0,0.28)",
              padding: "10px 12px",
              display: "grid",
              gap: 8,
              backdropFilter: "blur(10px)",
            }}
          >
            <div
              style={{
                fontSize: 11,
                fontWeight: 700,
                color: TEXT_PRIMARY,
                lineHeight: 1.3,
              }}
            >
              {fmtTrendTick(activeRow.bucketStart, props.range)}
            </div>

            <div
              style={{
                display: "grid",
                gap: 6,
              }}
            >
              {tooltipItems.map((item) => (
                <div
                  key={item.key}
                  style={{
                    display: "grid",
                    gridTemplateColumns: "auto 1fr auto",
                    alignItems: "center",
                    gap: 8,
                    fontSize: FONT_SIZE_UI,
                    color: TEXT_MUTED,
                  }}
                >
                  <span
                    style={{
                      width: 8,
                      height: 8,
                      borderRadius: 999,
                      background: item.swatch,
                      display: "inline-block",
                    }}
                  />
                  <span>{item.label}</span>
                  <span
                    style={{
                      color: TEXT_PRIMARY,
                      fontWeight: 700,
                      whiteSpace: "nowrap",
                    }}
                  >
                    {formatNumber(item.value)}
                  </span>
                </div>
              ))}
            </div>
          </div>
        ) : null}
      </div>

      <div
        style={{
          display: "grid",
          gridTemplateColumns: "repeat(3, minmax(0, max-content))",
          gap: 8,
          alignItems: "center",
        }}
      >
        {[
          {
            key: "total",
            label: "Total plays",
            fill: "rgba(255,255,255,0.9)",
          },
          ...stackedLayers.map((layer) => ({
            key: layer.key,
            label: layer.label,
            fill: layer.fill,
          })),
        ].map((layer) => (
          <div
            key={layer.key}
            style={{
              display: "inline-flex",
              alignItems: "center",
              gap: 6,
              color: TEXT_MUTED,
              fontSize: FONT_SIZE_UI,
            }}
          >
            <span
              style={{
                width: 10,
                height: 10,
                borderRadius: 999,
                background: layer.fill,
                display: "inline-block",
              }}
            />
            {layer.label}
          </div>
        ))}
      </div>
    </div>
  );
}


----- app/admin/playback/dashboard/ShareLinkActivityTable.tsx -----
"use client";

import React from "react";
import type { PlaybackAdminSnapshot } from "@/lib/playbackAdmin";
import { TableShell } from "./PlaybackDashboardPrimitives";
import {
  FONT_SIZE_DEDUPE,
  ROW_BORDER,
  TEXT_FAINT,
  TEXT_MUTED,
  TEXT_PRIMARY,
  TEXT_STRONG,
} from "./playbackTelemetryDashboardStyles";
import {
  formatAgo,
  formatNumber,
} from "./playbackTelemetryDashboardFormatters";

function formatListenedMs(ms: number): string {
  const totalSeconds = Math.max(0, Math.floor(ms / 1000));
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = totalSeconds % 60;

  if (minutes <= 0) return `${seconds}s`;

  return `${minutes}m ${String(seconds).padStart(2, "0")}s`;
}

export function ShareLinkActivityTable(props: {
  rows: PlaybackAdminSnapshot["shareLinkActivity"];
}) {
  const { rows } = props;

  return (
    <TableShell>
      <table
        style={{
          width: "100%",
          borderCollapse: "collapse",
          minWidth: 860,
        }}
      >
        <thead>
          <tr>
            {[
              "Share link",
              "Qualified plays",
              "Listened time",
              "Completions",
              "Top track",
              "Last activity",
            ].map((label) => (
              <th
                key={label}
                style={{
                  textAlign: "left",
                  padding: "8px 10px",
                  fontSize: FONT_SIZE_DEDUPE,
                  fontWeight: 700,
                  color: TEXT_FAINT,
                  borderBottom: ROW_BORDER,
                  whiteSpace: "nowrap",
                }}
              >
                {label}
              </th>
            ))}
          </tr>
        </thead>

        <tbody>
          {rows.length === 0 ? (
            <tr>
              <td
                colSpan={6}
                style={{
                  padding: 10,
                  fontSize: FONT_SIZE_DEDUPE,
                  color: TEXT_MUTED,
                }}
              >
                No labelled share-link playback yet.
              </td>
            </tr>
          ) : null}

          {rows.map((row) => (
            <tr key={row.telemetryLabel}>
              <td
                style={{
                  padding: "10px 10px",
                  borderBottom: ROW_BORDER,
                  color: TEXT_PRIMARY,
                  fontSize: FONT_SIZE_DEDUPE,
                  fontWeight: 700,
                  maxWidth: 300,
                  overflow: "hidden",
                  textOverflow: "ellipsis",
                  whiteSpace: "nowrap",
                }}
                title={row.telemetryLabel}
              >
                {row.telemetryLabel}
              </td>

              <td
                style={{
                  padding: "10px 10px",
                  borderBottom: ROW_BORDER,
                  color: TEXT_STRONG,
                  fontSize: FONT_SIZE_DEDUPE,
                  whiteSpace: "nowrap",
                }}
              >
                {formatNumber(row.qualifiedPlayCount)}
              </td>

              <td
                style={{
                  padding: "10px 10px",
                  borderBottom: ROW_BORDER,
                  color: TEXT_STRONG,
                  fontSize: FONT_SIZE_DEDUPE,
                  whiteSpace: "nowrap",
                }}
              >
                {formatListenedMs(row.listenedMs)}
              </td>

              <td
                style={{
                  padding: "10px 10px",
                  borderBottom: ROW_BORDER,
                  color: TEXT_STRONG,
                  fontSize: FONT_SIZE_DEDUPE,
                  whiteSpace: "nowrap",
                }}
              >
                {formatNumber(row.completedCount)}
              </td>

              <td
                style={{
                  padding: "10px 10px",
                  borderBottom: ROW_BORDER,
                  color: TEXT_MUTED,
                  fontSize: FONT_SIZE_DEDUPE,
                  maxWidth: 220,
                  overflow: "hidden",
                  textOverflow: "ellipsis",
                  whiteSpace: "nowrap",
                }}
              >
                {row.topTrackTitle ?? "—"}
              </td>

              <td
                style={{
                  padding: "10px 10px",
                  borderBottom: ROW_BORDER,
                  color: TEXT_MUTED,
                  fontSize: FONT_SIZE_DEDUPE,
                  whiteSpace: "nowrap",
                }}
              >
                {formatAgo(row.lastActivityAt)}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </TableShell>
  );
}

----- app/admin/playback/dashboard/TrackTable.tsx -----
// web/app/admin/playback/dashboard/TrackTable.tsx
"use client";

import React from "react";
import type { TrackRow } from "./types";
import {
  FONT_SIZE_UI,
  ROW_BORDER,
  TEXT_MUTED,
  TEXT_PRIMARY,
  TEXT_STRONG,
} from "./playbackTelemetryDashboardStyles";
import {
  formatAgo,
  formatNumber,
} from "./playbackTelemetryDashboardFormatters";

const POPULARITY_TRACK_BG = "rgba(255,255,255,0.08)";
const POPULARITY_TRACK_FILL = "rgba(255,255,255,0.78)";

export function TrackTable(props: { rows: TrackRow[]; emptyLabel?: string }) {
  const topListenedMs = props.rows.reduce(
    (max, row) => Math.max(max, row.listenedMs),
    0,
  );

  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        alignSelf: "start",
        overflowX: "auto",
      }}
    >
      <table
        style={{
          width: "100%",
          borderCollapse: "collapse",
          minWidth: 560,
          tableLayout: "auto",
        }}
      >
        <thead>
          <tr>
            {["Track", "Popularity", "Plays", "Completes", "Last heard"].map(
              (label) => (
                <th
                  key={label}
                  style={{
                    textAlign: "left",
                    padding: "6px 12px 10px",
                    fontSize: FONT_SIZE_UI,
                    fontWeight: 700,
                    color: TEXT_MUTED,
                    borderBottom: ROW_BORDER,
                    whiteSpace: "nowrap",
                    width:
                      label === "Track"
                        ? "44%"
                        : label === "Popularity"
                          ? "28%"
                          : "1%",
                  }}
                >
                  {label}
                </th>
              ),
            )}
          </tr>
        </thead>

        <tbody>
          {props.rows.length === 0 ? (
            <tr>
              <td
                colSpan={5}
                style={{
                  padding: "14px 12px 12px",
                  fontSize: FONT_SIZE_UI,
                  color: TEXT_MUTED,
                }}
              >
                {props.emptyLabel ?? "No rows."}
              </td>
            </tr>
          ) : null}

          {props.rows.map((row) => {
            const relativeWidthPct =
              topListenedMs > 0 ? (row.listenedMs / topListenedMs) * 100 : 0;

            return (
              <tr key={row.recordingId}>
                <td
                  style={{
                    padding: "12px 12px",
                    borderBottom: ROW_BORDER,
                    color: TEXT_PRIMARY,
                    fontSize: FONT_SIZE_UI,
                    fontWeight: 700,
                    verticalAlign: "top",
                    width: "1%",
                  }}
                >
                  <div
                    style={{
                      display: "grid",
                      gap: 4,
                      minWidth: 0,
                    }}
                  >
                    <div
                      title={row.title}
                      style={{
                        whiteSpace: "nowrap",
                        overflow: "hidden",
                        textOverflow: "ellipsis",
                      }}
                    >
                      {row.title}
                    </div>
                  </div>
                </td>

                <td
                  style={{
                    padding: "12px 12px",
                    borderBottom: ROW_BORDER,
                    verticalAlign: "middle",
                  }}
                >
                  <div
                    style={{
                      display: "grid",
                      gap: 6,
                    }}
                  >
                    <div
                      style={{
                        width: "100%",
                        maxWidth: 220,
                        height: 10,
                        borderRadius: 999,
                        background: POPULARITY_TRACK_BG,
                        overflow: "hidden",
                      }}
                    >
                      <div
                        style={{
                          width: `${relativeWidthPct}%`,
                          minWidth: row.listenedMs > 0 ? 6 : 0,
                          height: "100%",
                          borderRadius: 999,
                          background: POPULARITY_TRACK_FILL,
                          transition: "width 180ms ease",
                        }}
                      />
                    </div>
                  </div>
                </td>

                <td
                  style={{
                    padding: "12px 12px",
                    borderBottom: ROW_BORDER,
                    color: TEXT_STRONG,
                    fontSize: FONT_SIZE_UI,
                    whiteSpace: "nowrap",
                    verticalAlign: "middle",
                  }}
                >
                  {formatNumber(row.playCount)}
                </td>

                <td
                  style={{
                    padding: "12px 12px",
                    borderBottom: ROW_BORDER,
                    color: TEXT_STRONG,
                    fontSize: FONT_SIZE_UI,
                    whiteSpace: "nowrap",
                    verticalAlign: "middle",
                  }}
                >
                  {formatNumber(row.completedCount)}
                </td>

                <td
                  style={{
                    padding: "12px 12px",
                    borderBottom: ROW_BORDER,
                    color: TEXT_MUTED,
                    fontSize: FONT_SIZE_UI,
                    whiteSpace: "nowrap",
                    verticalAlign: "middle",
                  }}
                >
                  {formatAgo(row.lastListenedAt)}
                </td>
              </tr>
            );
          })}
        </tbody>
      </table>
    </div>
  );
}


----- app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts -----
// web/app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts
import type { DedupeRow, DedupeSessionRow } from "./types";
import { ellipsisMiddle, formatNumber } from "./playbackTelemetryDashboardFormatters";

export function resolveDedupeIdentityLabel(row: {
  audience: "member" | "anonymous";
  memberEmail?: string | null;
  memberId?: string | null;
}): string {
  if (row.audience === "anonymous") return "Anonymous";
  if (row.memberEmail) return row.memberEmail;
  if (row.memberId) return ellipsisMiddle(row.memberId, 8);
  return "Member";
}

export function parseMilestoneMs(milestoneKey: string): number | null {
  const normalized = milestoneKey.trim();

  if (!/^\d+$/.test(normalized)) return null;

  const milestoneMs = Number(normalized);
  if (!Number.isFinite(milestoneMs) || milestoneMs < 0) return null;

  return Math.floor(milestoneMs);
}

export function isProgressEvent(eventType: string): boolean {
  return eventType.toLowerCase().includes("progress");
}

export function isPlayEvent(eventType: string): boolean {
  const normalized = eventType.toLowerCase();
  return normalized.includes("play") && !normalized.includes("progress");
}

export function isCompleteEvent(eventType: string): boolean {
  return eventType.toLowerCase().includes("complete");
}

export function formatProgressLabel(
  progressMs: number,
  hasComplete: boolean,
): string {
  if (hasComplete) return "Complete";
  if (progressMs <= 0) return "Started";
  const wholeSeconds = Math.floor(progressMs / 1000);
  return `${formatNumber(wholeSeconds)}s credited`;
}

export function buildDedupeSessionRows(rows: DedupeRow[]): DedupeSessionRow[] {
  const groups = new Map<string, DedupeRow[]>();

  for (const row of rows) {
    const identityKey =
      row.audience === "anonymous"
        ? "anonymous"
        : (row.memberEmail ?? row.memberId ?? "member");
    const trackKey = row.recordingId ?? row.recordingTitle ?? "unknown-track";
    const groupKey = [row.audience, identityKey, trackKey, row.playbackId].join(
      "::",
    );

    const existing = groups.get(groupKey);
    if (existing) {
      existing.push(row);
    } else {
      groups.set(groupKey, [row]);
    }
  }

  return Array.from(groups.entries())
    .map(([groupKey, sourceRows]) => {
      const ordered = [...sourceRows].sort(
        (a, b) => Date.parse(a.createdAt) - Date.parse(b.createdAt),
      );

      const first = ordered[0];
      const last = ordered[ordered.length - 1];

      let maxProgressMs = 0;
      let creditedMilestoneCount = 0;
      let hasPlay = false;
      let hasComplete = false;

      for (const row of ordered) {
        if (isPlayEvent(row.eventType)) hasPlay = true;
        if (isCompleteEvent(row.eventType)) hasComplete = true;

        if (isProgressEvent(row.eventType)) {
          creditedMilestoneCount += 1;
          const milestoneMs = parseMilestoneMs(row.milestoneKey);
          if (milestoneMs != null) {
            maxProgressMs = Math.max(maxProgressMs, milestoneMs);
          }
        }
      }

      const progressPct = hasComplete
        ? 100
        : Math.max(0, Math.min(92, maxProgressMs / 1000));

      const statusLabel = hasComplete
        ? "Completed"
        : maxProgressMs > 0
          ? "In progress"
          : hasPlay
            ? "Started"
            : "Telemetry";

      return {
        groupKey,
        playbackId: first.playbackId,
        audience: first.audience,
        memberId: first.memberId ?? null,
        memberEmail: first.memberEmail ?? null,
        recordingId: first.recordingId ?? null,
        recordingTitle: first.recordingTitle ?? null,
        createdAt: first.createdAt,
        latestAt: last.createdAt,
        progressMs: hasComplete ? 100_000 : maxProgressMs,
        progressPct,
        creditedMilestoneCount,
        hasPlay,
        hasComplete,
        statusLabel,
        sourceRows: ordered,
      };
    })
    .sort((a, b) => Date.parse(b.latestAt) - Date.parse(a.latestAt));
}

----- app/admin/playback/page.tsx -----
// web/app/admin/playback/page.tsx
import "server-only";

import PlaybackTelemetryDashboardClient from "./PlaybackTelemetryDashboardClient";
import { getPlaybackAdminSnapshot } from "@/lib/playbackAdmin";

export default async function Page(props: {
  searchParams?: Promise<Record<string, string | string[] | undefined>>;
}) {
  const sp = (await props.searchParams) ?? {};
  const embed = typeof sp.embed === "string" && sp.embed === "1";

  const snapshot = await getPlaybackAdminSnapshot();

  return (
    <PlaybackTelemetryDashboardClient
      embed={embed}
      initialSnapshot={snapshot}
    />
  );
}

===== COMPLETE CURRENT SOURCE — RECONCILIATION TARGET =====

----- app/home/usePlaybackReconciliation.ts -----
// web/app/home/usePlaybackReconciliation.ts
"use client";

import React from "react";
import type { AlbumNavItem, AlbumPlayerBundle } from "@/lib/types";
import { usePlayer } from "@/app/home/player/PlayerState";
import { getAutoplayFlag } from "./urlState";

type PlayerApi = ReturnType<typeof usePlayer>;

export function usePlaybackReconciliation(props: {
  player: PlayerApi;
  bundle: AlbumPlayerBundle;
  albums: AlbumNavItem[];
  route: {
    albumSlug: string | null;
    displayId: string | null;
  };
  isPlayer: boolean;
  sp: URLSearchParams;
  patchQuery: (patch: Record<string, string | null | undefined>) => void;
  forceSurface: (
    surface: "player" | "portal",
    tabId?: string | null,
    mode?: "push" | "replace",
  ) => void;
  routerPush: (href: string) => void;
}) {
  const {
    player,
    bundle,
    route,
    isPlayer,
    sp,
    patchQuery,
    forceSurface,
    routerPush,
  } = props;

  const { setQueue, play, selectTrack } = player;

  const qAlbum = (isPlayer ? route.albumSlug : null) ?? null;
  const qDisplayId = (isPlayer ? route.displayId : null) ?? null;

  const qTrackRecordingId = React.useMemo(() => {
    if (!qDisplayId) return null;
    const hit = (bundle.tracks ?? []).find(
      (track) => track.displayId === qDisplayId,
    );
    return hit?.recordingId ?? null;
  }, [qDisplayId, bundle.tracks]);

  const qAutoplay = getAutoplayFlag(sp);
  const qShareToken = (sp.get("st") ?? sp.get("share") ?? "").trim() || null;
  const hasSt = Boolean(qShareToken);

  const currentAlbumSlug = bundle.albumSlug;
  const album = bundle.album;
  const tracks = bundle.tracks;

  const onSelectAlbum = React.useCallback(
    (slug: string) => {
      if (!slug) return;

      const out = new URLSearchParams();

      try {
        const cur = new URLSearchParams(window.location.search);

        const st = (cur.get("st") ?? "").trim();
        const share = (cur.get("share") ?? "").trim();
        const autoplay = (cur.get("autoplay") ?? "").trim();

        if (st) out.set("st", st);
        else if (share) out.set("share", share);

        if (autoplay) out.set("autoplay", autoplay);

        for (const [key, value] of cur.entries()) {
          if (key.startsWith("utm_") && value.trim()) {
            out.set(key, value.trim());
          }
        }
      } catch {
        // ignore
      }

      // Share-token context is URL-authoritative. A tokenless current URL
      // must remain tokenless rather than reviving a prior session value.
      const query = out.toString();
      routerPush(`/${encodeURIComponent(slug)}${query ? `?${query}` : ""}`);
    },
    [routerPush],
  );

  const forcedPlayerKeyRef = React.useRef<string | null>(null);
  React.useEffect(() => {
    const playbackIntent = Boolean(qDisplayId) || Boolean(qAutoplay);
    if (!playbackIntent) {
      forcedPlayerKeyRef.current = null;
      return;
    }

    const intentKey = `${qAlbum ?? currentAlbumSlug}:${qDisplayId ?? ""}:${qAutoplay ? "autoplay" : ""}`;
    if (forcedPlayerKeyRef.current === intentKey) return;

    if (isPlayer) {
      forcedPlayerKeyRef.current = intentKey;
      return;
    }

    forcedPlayerKeyRef.current = intentKey;
    forceSurface("player", null, "replace");
  }, [qAlbum, currentAlbumSlug, qDisplayId, qAutoplay, isPlayer, forceSurface]);

  React.useEffect(() => {
    if (!qTrackRecordingId) return;

    const selectedRecordingId = player.selectedRecordingId ?? null;
    const pendingRecordingId = player.pendingRecordingId ?? null;

    if (
      selectedRecordingId === qTrackRecordingId ||
      pendingRecordingId === qTrackRecordingId
    ) {
      return;
    }

    selectTrack(qTrackRecordingId);
  }, [
    qTrackRecordingId,
    player.selectedRecordingId,
    player.pendingRecordingId,
    selectTrack,
  ]);

  const primedAlbumKeyRef = React.useRef<string | null>(null);
  React.useEffect(() => {
    if (!album || tracks.length === 0) return;

    const primeKey = `${currentAlbumSlug}:${hasSt ? "shared" : "plain"}:${qDisplayId ?? ""}`;
    if (primedAlbumKeyRef.current === primeKey) return;

    if (player.current || player.queue.length > 0) {
      primedAlbumKeyRef.current = primeKey;
      return;
    }

    if (qDisplayId) {
      primedAlbumKeyRef.current = primeKey;
      return;
    }

    const first = tracks[0];
    if (!first?.recordingId) return;

    const ctxId = hasSt
      ? (album.catalogueId ?? undefined)
      : (album.catalogueId ?? album.id ?? undefined);

    const ctxSlug = qAlbum ?? currentAlbumSlug;

    player.setQueue(tracks, {
      contextId: ctxId,
      contextSlug: ctxSlug,
      contextTitle: album.title ?? undefined,
      contextArtist: album.artist ?? undefined,
      artworkUrl: album.artworkUrl ?? null,
    });

    const selectedRecordingId = player.selectedRecordingId ?? null;
    const pendingRecordingId = player.pendingRecordingId ?? null;

    const alreadySelected = selectedRecordingId === first.recordingId;
    const alreadyPending = pendingRecordingId === first.recordingId;

    if (!alreadySelected && !alreadyPending) {
      player.selectTrack(first.recordingId);
    }

    primedAlbumKeyRef.current = primeKey;
  }, [
    album,
    tracks,
    hasSt,
    qAlbum,
    currentAlbumSlug,
    qDisplayId,
    player.selectedRecordingId,
    player.pendingRecordingId,
    player,
  ]);

  const autoplayFiredRef = React.useRef<string | null>(null);
  React.useEffect(() => {
    if (!isPlayer) return;
    if (!qAutoplay) return;
    if (!qTrackRecordingId) return;

    if (!qShareToken) {
      patchQuery({ autoplay: null });
      return;
    }

    if (!album || tracks.length === 0) return;

    const key = `${qAlbum ?? ""}:${qTrackRecordingId}:${qShareToken}`;
    if (autoplayFiredRef.current === key) return;
    autoplayFiredRef.current = key;

    const ctxId = hasSt
      ? (album.catalogueId ?? undefined)
      : (album.catalogueId ?? album.id ?? undefined);

    const ctxSlug = qAlbum ?? currentAlbumSlug;

    setQueue(tracks, {
      contextId: ctxId,
      contextSlug: ctxSlug,
      contextTitle: album.title ?? undefined,
      contextArtist: album.artist ?? undefined,
      artworkUrl: album.artworkUrl ?? null,
    });

    const selectedTrack =
      tracks.find((track) => track.recordingId === qTrackRecordingId) ?? null;

    if (selectedTrack) {
      play(selectedTrack);
    }

    patchQuery({ autoplay: null });
  }, [
    isPlayer,
    qAutoplay,
    qTrackRecordingId,
    qAlbum,
    qShareToken,
    album,
    tracks,
    hasSt,
    currentAlbumSlug,
    play,
    setQueue,
    patchQuery,
  ]);

  React.useEffect(() => {
    const onOpen = (event: Event) => {
      const customEvent = event as CustomEvent<{ albumSlug?: string | null }>;
      const slug = customEvent.detail?.albumSlug ?? null;

      if (slug) {
        onSelectAlbum(slug);
        return;
      }

      forceSurface("player");
    };

    window.addEventListener("af:open-player", onOpen as EventListener);
    return () =>
      window.removeEventListener("af:open-player", onOpen as EventListener);
  }, [onSelectAlbum, forceSurface]);

  return {
    onSelectAlbum,
  };
}


===== COMPLETE CURRENT SOURCE — DIRECT LOCAL CONTEXT =====


----- app/admin/AdminPageFrame.tsx -----
import React from "react";

type Props = {
  title: string;
  subtitle?: string;
  embed?: boolean;
  maxWidth?: number;
  headerActions?: React.ReactNode;
  children: React.ReactNode;
};

export default function AdminPageFrame(props: Props) {
  const {
    title,
    subtitle,
    embed = false,
    maxWidth = 1120,
    headerActions,
    children,
  } = props;

  const outerStyle: React.CSSProperties = {
    padding: embed ? "22px 24px 26px" : "28px 28px 32px",
    maxWidth: embed ? undefined : maxWidth,
    margin: embed ? undefined : "0 auto",
  };

  return (
    <div style={outerStyle}>
      <div
        style={{
          display: "grid",
          gap: 20,
        }}
      >
        <div
          style={{
            display: "grid",
            gap: 14,
            padding: "2px 2px 0",
          }}
        >
          <div
            style={{
              display: "flex",
              alignItems: "flex-end",
              justifyContent: "space-between",
              gap: 18,
              flexWrap: "wrap",
            }}
          >
            <div style={{ minWidth: 0 }}>
              <div
                style={{
                  fontSize: 11,
                  letterSpacing: "0.16em",
                  textTransform: "uppercase",
                  opacity: 0.5,
                  userSelect: "none",
                }}
              >
                Admin
              </div>
              <h1
                style={{
                  margin: "7px 0 0",
                  fontSize: 24,
                  lineHeight: 1.08,
                  fontWeight: 700,
                  color: "rgba(255,255,255,0.97)",
                  textWrap: "balance",
                }}
              >
                {title}
              </h1>
              {subtitle ? (
                <div
                  style={{
                    marginTop: 10,
                    fontSize: 13,
                    lineHeight: 1.58,
                    opacity: 0.72,
                    maxWidth: 820,
                  }}
                >
                  {subtitle}
                </div>
              ) : null}
            </div>

            {headerActions ? (
              <div
                style={{
                  display: "flex",
                  gap: 8,
                  alignItems: "center",
                  flexWrap: "wrap",
                  justifyContent: "flex-start",
                }}
              >
                {headerActions}
              </div>
            ) : null}
          </div>

          <div
            style={{
              height: 1,
              background:
                "linear-gradient(90deg, rgba(255,224,166,0.24), rgba(255,255,255,0.08) 35%, rgba(255,255,255,0))",
              opacity: 0.75,
            }}
          />
        </div>

        <div
          style={{
            display: "grid",
            gap: 14,
            minHeight: 0,
          }}
        >
          {children}
        </div>
      </div>

      {embed ? (
        <style>{`
          html, body {
            background: transparent !important;
          }

          body {
            color: rgba(255,255,255,0.94);
          }

          ::selection {
            background: rgba(255, 222, 160, 0.22);
          }

          ::-webkit-scrollbar {
            width: 12px;
            height: 12px;
          }

          ::-webkit-scrollbar-track {
            background: rgba(255,255,255,0.03);
          }

          ::-webkit-scrollbar-thumb {
            background: rgba(255,255,255,0.14);
            border-radius: 999px;
            border: 2px solid rgba(10,10,14,0.92);
          }

          ::-webkit-scrollbar-thumb:hover {
            background: rgba(255,255,255,0.22);
          }
        `}</style>
      ) : null}
    </div>
  );
}

----- app/admin/playback/dashboard/playbackTelemetryDashboardFormatters.ts -----
// web/app/admin/playback/dashboard/playbackTelemetryDashboardFormatters.ts
import type { TrendRangeKey } from "./types";

export function formatNumber(value: number): string {
  return new Intl.NumberFormat("en-NZ").format(value);
}

export function formatHoursFromMs(value: number): string {
  const hours = value / 3_600_000;
  return hours >= 10 ? hours.toFixed(0) : hours.toFixed(1);
}

export function formatMinutesFromMs(value: number): string {
  return formatNumber(Math.floor(value / 60_000));
}

export function formatAgo(iso: string | null): string {
  if (!iso) return "—";

  const ms = Date.now() - Date.parse(iso);
  if (!Number.isFinite(ms) || ms < 0) return "—";

  const sec = Math.floor(ms / 1000);
  if (sec < 60) return `${sec}s ago`;

  const min = Math.floor(sec / 60);
  if (min < 60) return `${min}m ago`;

  const hr = Math.floor(min / 60);
  if (hr < 24) return `${hr}h ago`;

  const day = Math.floor(hr / 24);
  return `${day}d ago`;
}

export function ellipsisMiddle(value: string, keep = 8): string {
  if (value.length <= keep * 2 + 1) return value;
  return `${value.slice(0, keep)}…${value.slice(-keep)}`;
}

export function fmtSnapshotStamp(iso: string): string {
  try {
    return new Date(iso).toLocaleString("en-NZ", {
      year: "numeric",
      month: "short",
      day: "numeric",
      hour: "2-digit",
      minute: "2-digit",
      second: "2-digit",
    });
  } catch {
    return iso;
  }
}

export function fmtTrendTick(iso: string, range: TrendRangeKey): string {
  try {
    const date = new Date(iso);

    if (range === "hour") {
      return date.toLocaleTimeString("en-NZ", {
        hour: "numeric",
        minute: "2-digit",
      });
    }

    if (range === "day") {
      return date.toLocaleTimeString("en-NZ", {
        hour: "numeric",
      });
    }

    if (range === "week") {
      return date.toLocaleDateString("en-NZ", {
        weekday: "short",
      });
    }

    if (range === "month") {
      return date.toLocaleDateString("en-NZ", {
        day: "numeric",
        month: "short",
      });
    }

    if (range === "year") {
      return date.toLocaleDateString("en-NZ", {
        month: "short",
      });
    }

    return date.toLocaleDateString("en-NZ", {
      year: "numeric",
    });
  } catch {
    return iso;
  }
}

export function formatAggregateValue(
  label: string,
  value: number | null | undefined,
): string {
  if (value == null) return "—";
  if (label === "Hours listened") return formatHoursFromMs(value);
  if (label === "Minutes listened") return formatMinutesFromMs(value);
  return formatNumber(value);
}

export function percentage(part: number, total: number): number {
  if (total <= 0) return 0;
  return Math.max(0, Math.min(100, (part / total) * 100));
}

----- app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts -----
// web/app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts
export const PANEL_BORDER = "1px solid rgba(255,255,255,0.12)";
export const ROW_BORDER = "1px solid rgba(255,255,255,0.08)";
export const TEXT_PRIMARY = "rgba(255,255,255,0.92)";
export const TEXT_STRONG = "rgba(255,255,255,0.86)";
export const TEXT_MUTED = "rgba(255,255,255,0.68)";
export const TEXT_FAINT = "rgba(255,255,255,0.58)";
export const BG_PANEL = "rgba(255,255,255,0.04)";
export const BG_INSET = "rgba(255,255,255,0.02)";
export const BG_ACCENT = "rgba(255,255,255,0.08)";
export const BG_MEMBER = "rgba(255,255,255,0.78)";
export const BG_ANON = "rgba(255,255,255,0.22)";
export const FONT_SIZE_UI = 12;
export const FONT_SIZE_DEDUPE = 11;

----- app/admin/playback/dashboard/types.ts -----
// web/app/admin/playback/dashboard/types.ts
import type { PlaybackAdminSnapshot } from "@/lib/playbackAdmin";

export type TrackRow = PlaybackAdminSnapshot["topTracksByListenedMs"][number];
export type DedupeRowBase = PlaybackAdminSnapshot["recentDedupe"][number];
export type TrendRangeKey = keyof PlaybackAdminSnapshot["qualifiedPlayTrends"];
export type TrendBucket =
  PlaybackAdminSnapshot["qualifiedPlayTrends"][TrendRangeKey]["buckets"][number];

export type DedupeRow = DedupeRowBase & {
  recordingTitle?: string | null;
  trackTitle?: string | null;
  memberEmail?: string | null;
  shareTokenLabel?: string | null;
};

export type DedupeSessionRow = {
  groupKey: string;
  playbackId: string;
  audience: "member" | "anonymous";
  memberId: string | null;
  memberEmail: string | null;
  recordingId: string | null;
  recordingTitle: string | null;
  createdAt: string;
  latestAt: string;
  progressMs: number;
  progressPct: number;
  creditedMilestoneCount: number;
  hasPlay: boolean;
  hasComplete: boolean;
  statusLabel: string;
  sourceRows: DedupeRow[];
};

export type ChartPoint = {
  x: number;
  y: number;
};


----- app/home/player/PlayerState.tsx -----
// web/app/home/player/PlayerState.tsx
"use client";

import React from "react";
import { useAuth } from "@clerk/nextjs";
import type { PlayerTrack } from "@/lib/types";
import { ensureLyricsForTrack } from "@/app/home/player/lyrics/ensureLyricsForTrack";
import {
  fetchPlaybackAccessDecision,
  readPlaybackShareTokenFromLocation,
  type PlaybackAccessDecision,
} from "./playbackAccessClient";

type PlayerStatus = "idle" | "loading" | "playing" | "paused";
type RepeatMode = "off" | "one" | "all";
type Intent = "play" | "pause" | null;
type LoadingReason = "token" | "attach" | "buffering";

export type QueueContext = {
  contextId?: string;
  contextSlug?: string;
  contextTitle?: string;
  contextArtist?: string;
  artworkUrl?: string | null;
  sharePlaybackContext?: string | null;
  sharePlaybackScopeId?: string | null;

  // Lets a deep link prepare an album queue around a specific track without
  // beginning playback or rearranging the album’s natural track order.
  initialCurrentRecordingId?: string;
};

export type PlayerState = {
  status: PlayerStatus;
  current?: PlayerTrack;
  queue: PlayerTrack[];
  lastError?: string;
  lastPlayAttemptAtMs?: number;

  // True only while a user-initiated play request is awaiting the client-side
  // access decision. This is visual state: it must not itself arm protected
  // media attachment.
  playRequestPending: boolean;

  queueContextId?: string;
  queueContextSlug?: string;
  queueContextTitle?: string;
  queueContextArtist?: string;
  queueContextArtworkUrl?: string | null;
  queueSharePlaybackContext?: string | null;
  queueSharePlaybackScopeId?: string | null;

  intent: Intent;
  intentAtMs?: number;
  selectedRecordingId?: string;
  pendingRecordingId?: string;

  pendingSeekMs?: number;
  seeking: boolean;

  loadingReason?: LoadingReason;
  reloadNonce: number;

  positionMs: number;
  seekNonce: number;

  volume: number;
  muted: boolean;
  repeat: RepeatMode;

  // Catalogue duration supplied by Sanity. This remains stable metadata and a
  // pre-load fallback, but is not assumed to be the decoded asset's exact end.
  durationByRecordingId: Record<string, number>;

  // Runtime duration reported by the browser for the actual delivered Mux
  // rendition. Keyed by playback ID so a future asset replacement cannot reuse
  // a stale duration merely because it shares a recording ID.
  assetDurationByPlaybackId: Record<string, number>;
};

type PlayerActions = {
  play: (track?: PlayerTrack) => void;
  pause: () => void;
  next: () => void;
  advanceFromEngine: () => void;
  prev: () => void;

  setQueue: (tracks: PlayerTrack[], opts?: QueueContext) => void;
  enqueue: (track: PlayerTrack) => void;
  clearQueue: () => void;

  setPositionMs: (ms: number) => void;
  setDurationMs: (ms: number) => void;
  setAssetDurationMs: (playbackId: string, ms: number) => void;

  setStatusExternal: (s: PlayerStatus) => void;
  setLoadingReasonExternal: (r?: LoadingReason) => void;

  setIntent: (i: Intent) => void;
  clearIntent: () => void;
  selectTrack: (id?: string) => void;
  setPendingRecordingId: (id?: string) => void;
  resolvePendingTrack: (id: string) => void;

  seek: (ms: number) => void;
  clearPendingSeek: () => void;

  setVolume: (v: number) => void;
  toggleMute: () => void;

  cycleRepeat: () => void;
  tick: (deltaMs: number) => void;

  clearError: () => void;
  bumpReload: () => void;
};

type PlayerVisualState = {
  status: PlayerStatus;
  intent: Intent;
  playRequestPending: boolean;
  current?: PlayerTrack;
  firstQueuedTrack?: PlayerTrack;
};

const PlayerCtx = React.createContext<(PlayerState & PlayerActions) | null>(
  null,
);
const PlayerActionsCtx = React.createContext<PlayerActions | null>(null);
const PlayerVisualCtx = React.createContext<PlayerVisualState | null>(null);

function clamp(n: number, lo: number, hi: number) {
  return Math.max(lo, Math.min(hi, n));
}

function nextRepeat(r: RepeatMode): RepeatMode {
  if (r === "off") return "all";
  if (r === "all") return "one";
  return "off";
}

function positiveDurationMs(value: number | undefined): number {
  return typeof value === "number" && Number.isFinite(value) && value > 0
    ? Math.round(value)
    : 0;
}

function getAssetDurationMs(
  assetDurationByPlaybackId: Record<string, number>,
  track: PlayerTrack | undefined,
): number {
  const playbackId = (track?.muxPlaybackId ?? "").trim();

  return playbackId
    ? positiveDurationMs(assetDurationByPlaybackId[playbackId])
    : 0;
}

function getEffectiveDurationMs(
  state: Pick<
    PlayerState,
    "durationByRecordingId" | "assetDurationByPlaybackId"
  >,
  track: PlayerTrack | undefined,
): number {
  const assetDurationMs = getAssetDurationMs(
    state.assetDurationByPlaybackId,
    track,
  );

  if (assetDurationMs > 0) return assetDurationMs;

  const recordingId = (track?.recordingId ?? "").trim();
  const catalogueDurationMs = recordingId
    ? positiveDurationMs(state.durationByRecordingId[recordingId])
    : 0;

  return catalogueDurationMs || positiveDurationMs(track?.durationMs);
}

function retainAssetDurationsByPlaybackId(
  previous: Record<string, number>,
  tracks: PlayerTrack[],
): Record<string, number> {
  const activePlaybackIds = new Set<string>();

  for (const track of tracks) {
    const playbackId = (track.muxPlaybackId ?? "").trim();
    if (playbackId) activePlaybackIds.add(playbackId);
  }

  let next = previous;

  for (const playbackId of Object.keys(previous)) {
    if (activePlaybackIds.has(playbackId)) continue;

    if (next === previous) next = { ...previous };
    delete next[playbackId];
  }

  return next;
}

function hydrateTrack(
  t: PlayerTrack,
  durationByRecordingId: Record<string, number>,
): PlayerTrack {
  const cached = durationByRecordingId[t.recordingId];
  if (!cached || cached <= 0) return t;
  if (t.durationMs === cached) return t;
  return { ...t, durationMs: cached };
}

function hydrateTracks(
  ts: PlayerTrack[],
  durationByRecordingId: Record<string, number>,
) {
  let changed = false;
  const next = ts.map((t) => {
    const ht = hydrateTrack(t, durationByRecordingId);
    if (ht !== t) changed = true;
    return ht;
  });
  return changed ? next : ts;
}

function blockedPlaybackMessage(access: PlaybackAccessDecision): string {
  if (access.embargoed) {
    return "Playback is disabled pre-release.";
  }

  if (access.reason) return access.reason;

  return "Playback is not available for this release.";
}

type TargetedPlayIntentDetail = {
  track: PlayerTrack;
  albumId: string | null;
};

function dispatchTargetedPlayIntent(
  track: PlayerTrack,
  state: Pick<PlayerState, "queueContextId">,
): void {
  if (typeof window === "undefined") return;

  const albumId = (state.queueContextId ?? "").trim() || null;

  window.dispatchEvent(
    new CustomEvent<TargetedPlayIntentDetail>("af:play-target-intent", {
      detail: {
        track,
        albumId,
      },
    }),
  );
}

function getNextTransportTrack(state: PlayerState): PlayerTrack | undefined {
  const current = state.current;

  if (!current || state.queue.length === 0) return undefined;
  if (state.repeat === "one") return current;

  const index = state.queue.findIndex(
    (track) => track.recordingId === current.recordingId,
  );
  const currentIndex = Math.max(index, 0);

  if (currentIndex + 1 < state.queue.length) {
    return state.queue[currentIndex + 1];
  }

  return state.repeat === "all" ? state.queue[0] : undefined;
}

function getPreviousTransportTrack(
  state: PlayerState,
): PlayerTrack | undefined {
  const current = state.current;

  if (!current || state.queue.length === 0) return undefined;

  if (state.status === "playing" && state.positionMs > 3000) {
    return current;
  }

  const index = state.queue.findIndex(
    (track) => track.recordingId === current.recordingId,
  );
  const currentIndex = Math.max(index, 0);

  if (currentIndex > 0) {
    return state.queue[currentIndex - 1];
  }

  if (state.repeat === "all") {
    return state.queue.at(-1);
  }

  return current;
}

function primeDurationByRecordingId(
  prev: Record<string, number>,
  tracks: PlayerTrack[],
): Record<string, number> {
  let next = prev;
  for (const t of tracks) {
    if (!t?.recordingId) continue;
    const ms = t.durationMs;
    if (typeof ms !== "number" || !Number.isFinite(ms) || ms <= 0) continue;
    if (typeof next[t.recordingId] === "number" && next[t.recordingId] > 0)
      continue;
    if (next === prev) next = { ...prev };
    next[t.recordingId] = ms;
  }
  return next;
}

function resolvePlaybackAccessIdentityKey(
  params: Readonly<{
    clerkAuthLoaded: boolean;
    isSignedIn: boolean | undefined;
    userId: string | null | undefined;
    sessionId: string | null | undefined;
  }>,
): string {
  if (!params.clerkAuthLoaded) return "clerk:loading";
  if (!params.isSignedIn) return "clerk:anonymous";

  return `clerk:user:${params.userId ?? ""}:session:${params.sessionId ?? ""}`;
}

function markCurrentTrackLoading(
  state: PlayerState,
  loadingReason: "attach",
): PlayerState {
  const current = state.current;
  if (!current) return state;

  return {
    ...state,
    status: "loading",
    loadingReason,
    positionMs: 0,
    intent: "play",
    intentAtMs: Date.now(),
    pendingRecordingId: current.recordingId,
    selectedRecordingId: current.recordingId,
  };
}

function markNextTrackLoading(
  state: PlayerState,
  track: PlayerTrack,
  loadingReason: "token" | "attach",
): PlayerState {
  const hydratedTrack = hydrateTrack(track, state.durationByRecordingId);

  return {
    ...state,
    current: hydratedTrack,
    status: "loading",
    loadingReason,
    positionMs: 0,
    intent: "play",
    intentAtMs: Date.now(),
    pendingRecordingId: hydratedTrack.recordingId,
    selectedRecordingId: hydratedTrack.recordingId,
  };
}

function transitionForwardState(
  state: PlayerState,
  nextTrackLoadingReason: "token" | "attach",
  terminalIntent: "pause" | null,
): PlayerState {
  const current = state.current;
  if (!current || state.queue.length === 0) return state;

  if (state.repeat === "one") {
    return markCurrentTrackLoading(state, "attach");
  }

  const index = state.queue.findIndex(
    (track) => track.recordingId === current.recordingId,
  );
  const currentIndex = Math.max(index, 0);
  const nextTrack = state.queue[currentIndex + 1];

  if (nextTrack) {
    return markNextTrackLoading(state, nextTrack, nextTrackLoadingReason);
  }

  if (state.repeat === "all") {
    const firstTrack = state.queue[0];
    if (firstTrack) {
      return markNextTrackLoading(state, firstTrack, nextTrackLoadingReason);
    }
  }

  return {
    ...state,
    status: "paused",
    intent: terminalIntent,
    intentAtMs: terminalIntent === "pause" ? Date.now() : undefined,
    positionMs: 0,
    pendingSeekMs: 0,
    seeking: true,
    seekNonce: state.seekNonce + 1,
    loadingReason: undefined,
    pendingRecordingId: undefined,
  };
}

function transitionPreviousState(state: PlayerState): PlayerState {
  const current = state.current;
  if (!current || state.queue.length === 0) return state;

  if (state.status === "playing" && state.positionMs > 3000) {
    return markCurrentTrackLoading(state, "attach");
  }

  const index = state.queue.findIndex(
    (track) => track.recordingId === current.recordingId,
  );
  const currentIndex = Math.max(index, 0);
  const previousTrack = state.queue[currentIndex - 1];

  if (previousTrack) {
    return markNextTrackLoading(state, previousTrack, "token");
  }

  if (state.repeat === "all") {
    const lastTrack = state.queue.at(-1);
    if (lastTrack) {
      return markNextTrackLoading(state, lastTrack, "token");
    }
  }

  return markCurrentTrackLoading(state, "attach");
}

type QueueContextStatePatch = Partial<
  Pick<
    PlayerState,
    | "queueContextId"
    | "queueContextSlug"
    | "queueContextTitle"
    | "queueContextArtist"
    | "queueContextArtworkUrl"
    | "queueSharePlaybackContext"
    | "queueSharePlaybackScopeId"
  >
>;

function normalizedQueueShareValue(
  value: string | null | undefined,
): string | null {
  if (typeof value !== "string") return null;
  return value.trim() || null;
}

function buildQueueContextStatePatch(
  state: PlayerState,
  opts: QueueContext | undefined,
): QueueContextStatePatch {
  const patch: QueueContextStatePatch = {};
  const contextId = typeof opts?.contextId === "string" ? opts.contextId : "";
  const contextSlug =
    typeof opts?.contextSlug === "string" ? opts.contextSlug.trim() : "";
  const contextTitle =
    typeof opts?.contextTitle === "string" ? opts.contextTitle.trim() : "";
  const contextArtist =
    typeof opts?.contextArtist === "string" ? opts.contextArtist.trim() : "";

  if (contextId.length > 0) patch.queueContextId = contextId;
  if (contextSlug) patch.queueContextSlug = contextSlug;
  if (contextTitle) patch.queueContextTitle = contextTitle;
  if (contextArtist) patch.queueContextArtist = contextArtist;

  patch.queueContextArtworkUrl =
    opts?.artworkUrl !== undefined
      ? (opts.artworkUrl ?? null)
      : (state.queueContextArtworkUrl ?? null);

  const optionBag = opts ?? {};

  if (Object.hasOwn(optionBag, "sharePlaybackContext")) {
    patch.queueSharePlaybackContext = normalizedQueueShareValue(
      opts?.sharePlaybackContext,
    );
  }

  if (Object.hasOwn(optionBag, "sharePlaybackScopeId")) {
    patch.queueSharePlaybackScopeId = normalizedQueueShareValue(
      opts?.sharePlaybackScopeId,
    );
  }

  return patch;
}

function findRequestedInitialCurrent(
  queue: readonly PlayerTrack[],
  opts: QueueContext | undefined,
): PlayerTrack | null {
  const recordingId =
    typeof opts?.initialCurrentRecordingId === "string"
      ? opts.initialCurrentRecordingId.trim()
      : "";

  if (!recordingId) return null;

  return (
    queue.find((track) => track.recordingId === recordingId) ?? null
  );
}

type QueueSelectionState = Pick<
  PlayerState,
  "positionMs" | "selectedRecordingId" | "pendingRecordingId"
>;

function resolveQueueSelectionState(
  state: PlayerState,
  nextCurrent: PlayerTrack | undefined,
  requestedInitialCurrent: PlayerTrack | null,
): QueueSelectionState {
  if (requestedInitialCurrent) {
    return {
      positionMs: 0,
      selectedRecordingId: requestedInitialCurrent.recordingId,
      pendingRecordingId: undefined,
    };
  }

  return {
    positionMs: state.current ? state.positionMs : 0,
    selectedRecordingId:
      state.selectedRecordingId ?? nextCurrent?.recordingId,
    pendingRecordingId: state.pendingRecordingId,
  };
}

function applyQueueState(
  state: PlayerState,
  tracks: PlayerTrack[],
  opts: QueueContext | undefined,
): PlayerState {
  const nextDurationByRecordingId = primeDurationByRecordingId(
    state.durationByRecordingId,
    tracks,
  );
  const nextAssetDurationByPlaybackId = retainAssetDurationsByPlaybackId(
    state.assetDurationByPlaybackId,
    tracks,
  );
  const hydratedQueue = hydrateTracks(tracks, nextDurationByRecordingId);
  const requestedInitialCurrent = findRequestedInitialCurrent(
    hydratedQueue,
    opts,
  );
  const nextCurrentRaw =
    requestedInitialCurrent ?? state.current ?? hydratedQueue[0];
  const nextCurrent = nextCurrentRaw
    ? hydrateTrack(nextCurrentRaw, nextDurationByRecordingId)
    : undefined;

  return {
    ...state,
    ...buildQueueContextStatePatch(state, opts),
    durationByRecordingId: nextDurationByRecordingId,
    assetDurationByPlaybackId: nextAssetDurationByPlaybackId,
    queue: hydratedQueue,
    current: nextCurrent,
    ...resolveQueueSelectionState(
      state,
      nextCurrent,
      requestedInitialCurrent,
    ),
  };
}

export function PlayerStateProvider(
  props: Readonly<{ children: React.ReactNode }>,
) {
  const {
    isLoaded: clerkAuthLoaded,
    isSignedIn,
    userId,
    sessionId,
  } = useAuth();

  const playbackAccessIdentityKey = resolvePlaybackAccessIdentityKey({
    clerkAuthLoaded,
    isSignedIn,
    userId,
    sessionId,
  });

  const [state, setState] = React.useState<PlayerState>({
    status: "idle",
    current: undefined,
    queue: [],
    lastError: undefined,
    lastPlayAttemptAtMs: undefined,
    playRequestPending: false,

    queueContextId: undefined,
    queueContextSlug: undefined,
    queueContextTitle: undefined,
    queueContextArtist: undefined,
    queueContextArtworkUrl: null,
    queueSharePlaybackContext: null,
    queueSharePlaybackScopeId: null,

    intent: null,
    intentAtMs: undefined,
    selectedRecordingId: undefined,
    pendingRecordingId: undefined,

    pendingSeekMs: undefined,
    seeking: false,

    loadingReason: undefined,
    reloadNonce: 0,

    positionMs: 0,
    seekNonce: 0,

    volume: 0.9,
    muted: false,
    repeat: "off",

    durationByRecordingId: {},
    assetDurationByPlaybackId: {},
  });

  const stateRef = React.useRef(state);
  const playRequestSequenceRef = React.useRef(0);

  React.useEffect(() => {
    stateRef.current = state;
  }, [state]);

  const blockPlayback = React.useCallback((message: string) => {
    setState((s) => ({
      ...s,
      status: s.status === "loading" ? "idle" : s.status,
      intent: "pause",
      intentAtMs: Date.now(),
      playRequestPending: false,
      loadingReason: undefined,
      pendingRecordingId: undefined,
      lastError: message,
    }));
  }, []);

  const guardPlayback = React.useCallback(
    async (onAllowed: () => void): Promise<void> => {
      const catalogueId = (stateRef.current.queueContextId ?? "").trim();

      if (!catalogueId) {
        onAllowed();
        return;
      }

      try {
        const access = await fetchPlaybackAccessDecision({
          catalogueId,
          shareToken: readPlaybackShareTokenFromLocation(),
          accessIdentityKey: playbackAccessIdentityKey,
        });

        if (!access.allowed) {
          blockPlayback(blockedPlaybackMessage(access));
          return;
        }

        const expectedSharePlaybackScopeId = `alb:${catalogueId.replace(
          /^alb:/i,
          "",
        )}`;

        if (
          access.sharePlaybackContext &&
          access.sharePlaybackScopeId === expectedSharePlaybackScopeId
        ) {
          setState((current) => ({
            ...current,
            queueSharePlaybackContext: access.sharePlaybackContext,
            queueSharePlaybackScopeId: access.sharePlaybackScopeId,
          }));
        }

        onAllowed();
      } catch {
        blockPlayback("Playback access could not be verified.");
      }
    },
    [blockPlayback, playbackAccessIdentityKey],
  );
  const actions: PlayerActions = React.useMemo(() => {
    return {
      setIntent: (i: Intent) =>
        setState((s) => ({
          ...s,
          intent: i,
          intentAtMs: i ? Date.now() : undefined,
        })),

      clearIntent: () =>
        setState((s) =>
          s.intent ? { ...s, intent: null, intentAtMs: undefined } : s,
        ),

      selectTrack: (id?: string) =>
        setState((s) => {
          if (s.selectedRecordingId === id) return s;
          return {
            ...s,
            selectedRecordingId: id,
          };
        }),

      setPendingRecordingId: (id?: string) =>
        setState((s) => {
          if (s.pendingRecordingId === id) return s;
          return {
            ...s,
            pendingRecordingId: id,
          };
        }),

      resolvePendingTrack: (id: string) =>
        setState((s) => {
          if (s.pendingRecordingId !== id) return s;
          return { ...s, pendingRecordingId: undefined };
        }),

      play: (track?: PlayerTrack) => {
        const requestSequence = ++playRequestSequenceRef.current;
        const requestStartedAtMs = Date.now();
        const snapshot = stateRef.current;
        const requestedTrack = track ?? snapshot.current ?? snapshot.queue[0];

        if (requestedTrack) {
          dispatchTargetedPlayIntent(
            hydrateTrack(requestedTrack, snapshot.durationByRecordingId),
            snapshot,
          );
        }

        setState((s) => ({
          ...s,
          playRequestPending: true,
          lastPlayAttemptAtMs: requestStartedAtMs,
          lastError: undefined,
        }));

        void guardPlayback(() => {
          if (requestSequence !== playRequestSequenceRef.current) {
            return;
          }

          setState((s) => {
            if (requestSequence !== playRequestSequenceRef.current) {
              return s;
            }

            const now = Date.now();
            const rawNext = track ?? s.current ?? s.queue[0];

            if (!rawNext) {
              return {
                ...s,
                status: "idle",
                current: undefined,
                positionMs: 0,
                intent: "play",
                intentAtMs: now,
                lastPlayAttemptAtMs: now,
                playRequestPending: false,
                lastError: undefined,
                loadingReason: undefined,
                pendingRecordingId: undefined,
              };
            }

            const nextTrack = hydrateTrack(rawNext, s.durationByRecordingId);
            const sameTrack =
              s.current?.recordingId === nextTrack.recordingId;

            const base = {
              ...s,
              intent: "play" as const,
              intentAtMs: now,
              lastPlayAttemptAtMs: now,
              playRequestPending: false,
              lastError: undefined,
              selectedRecordingId: nextTrack.recordingId,
              pendingRecordingId: nextTrack.recordingId,
            };

            if (sameTrack && s.status === "paused") {
              return {
                ...base,
                current: hydrateTrack(s.current!, s.durationByRecordingId),
                status: "paused",
                loadingReason: undefined,
              };
            }

            if (
              sameTrack &&
              (s.status === "playing" || s.status === "loading")
            ) {
              return { ...base, loadingReason: s.loadingReason };
            }

            return {
              ...base,
              current: nextTrack,
              status: "loading",
              loadingReason: "token",
              positionMs: 0,
            };
          });
        });
      },

      pause: () => {
        playRequestSequenceRef.current += 1;

        setState((s) => ({
          ...s,
          intent: "pause",
          intentAtMs: Date.now(),
          playRequestPending: false,
        }));
      },

      next: () => {
        const snapshot = stateRef.current;
        const targetTrack = getNextTransportTrack(snapshot);

        if (targetTrack) {
          dispatchTargetedPlayIntent(
            hydrateTrack(targetTrack, snapshot.durationByRecordingId),
            snapshot,
          );
        }

        void guardPlayback(() => {
          setState((s) => transitionForwardState(s, "token", "pause"));
        });
      },

      advanceFromEngine: () => {
        setState((s) => transitionForwardState(s, "attach", null));
      },

      prev: () => {
        const snapshot = stateRef.current;
        const targetTrack = getPreviousTransportTrack(snapshot);

        if (targetTrack) {
          dispatchTargetedPlayIntent(
            hydrateTrack(targetTrack, snapshot.durationByRecordingId),
            snapshot,
          );
        }

        void guardPlayback(() => {
          setState(transitionPreviousState);
        });
      },

      setQueue: (tracks: PlayerTrack[], opts?: QueueContext) =>
        setState((s) => applyQueueState(s, tracks, opts)),

      enqueue: (track: PlayerTrack) =>
        setState((s) => {
          const nextDurationByRecordingId = primeDurationByRecordingId(
            s.durationByRecordingId,
            [track],
          );
          const t = hydrateTrack(track, nextDurationByRecordingId);
          return {
            ...s,
            durationByRecordingId: nextDurationByRecordingId,
            queue: [...s.queue, t],
            current: s.current ?? t,
            selectedRecordingId: s.selectedRecordingId ?? t.recordingId,
          };
        }),

      clearQueue: () =>
        setState((s) => ({
          ...s,
          queue: [],
          current: undefined,
          selectedRecordingId: undefined,
          pendingRecordingId: undefined,

          positionMs: 0,
          pendingSeekMs: undefined,
          seeking: false,
          seekNonce: s.seekNonce + 1,
          loadingReason: undefined,
          queueSharePlaybackContext: null,
          queueSharePlaybackScopeId: null,
          assetDurationByPlaybackId: {},
        })),

      setPositionMs: (ms: number) =>
        setState((s) => ({
          ...s,
          positionMs: Math.max(0, ms),
        })),

      setDurationMs: (ms: number) =>
        setState((s) => {
          const cur = s.current;
          if (!cur) return s;
          if (!Number.isFinite(ms) || ms <= 0) return s;

          const alreadyCached =
            typeof s.durationByRecordingId[cur.recordingId] === "number" &&
            s.durationByRecordingId[cur.recordingId] > 0;
          const alreadyOnTrack =
            typeof cur.durationMs === "number" && cur.durationMs > 0;
          if (alreadyCached || alreadyOnTrack) return s;

          const nextDurationByRecordingId = {
            ...s.durationByRecordingId,
            [cur.recordingId]: ms,
          };

          const nextCurrent = { ...cur, durationMs: ms };
          let changed = false;
          const nextQueue = s.queue.map((t) => {
            if (t.recordingId !== cur.recordingId) return t;
            if (typeof t.durationMs === "number" && t.durationMs > 0) return t;
            changed = true;
            return { ...t, durationMs: ms };
          });

          return {
            ...s,
            durationByRecordingId: nextDurationByRecordingId,
            current: nextCurrent,
            queue: changed ? nextQueue : s.queue,
          };
        }),

      setAssetDurationMs: (playbackId: string, ms: number) => {
        const normalizedPlaybackId = playbackId.trim();
        const normalizedDurationMs = positiveDurationMs(ms);

        if (!normalizedPlaybackId || normalizedDurationMs <= 0) return;

        setState((s) => {
          if (
            s.assetDurationByPlaybackId[normalizedPlaybackId] ===
            normalizedDurationMs
          ) {
            return s;
          }

          return {
            ...s,
            assetDurationByPlaybackId: {
              ...s.assetDurationByPlaybackId,
              [normalizedPlaybackId]: normalizedDurationMs,
            },
          };
        });
      },

      setStatusExternal: (st: PlayerStatus) =>
        setState((s) => (s.status === st ? s : { ...s, status: st })),

      setLoadingReasonExternal: (r?: LoadingReason) =>
        setState((s) =>
          s.loadingReason === r ? s : { ...s, loadingReason: r },
        ),

      seek: (ms: number) => {
        setState((s) => {
          const dur = getEffectiveDurationMs(s, s.current);
          const next = dur > 0 ? clamp(ms, 0, dur) : Math.max(0, ms);
          return {
            ...s,
            positionMs: next,
            pendingSeekMs: next,
            seeking: true,
            seekNonce: s.seekNonce + 1,
          };
        });
      },

      clearPendingSeek: () =>
        setState((s) => {
          if (!s.seeking && s.pendingSeekMs == null) return s;
          return { ...s, seeking: false, pendingSeekMs: undefined };
        }),

      setVolume: (v: number) =>
        setState((s) => ({ ...s, volume: clamp(v, 0, 1) })),
      toggleMute: () => setState((s) => ({ ...s, muted: !s.muted })),

      cycleRepeat: () =>
        setState((s) => ({ ...s, repeat: nextRepeat(s.repeat) })),

      tick: (deltaMs: number) => {
        setState((s) => {
          if (s.status !== "playing") return s;

          const dur = getEffectiveDurationMs(s, s.current);
          const nextPos = Math.max(0, s.positionMs + Math.max(0, deltaMs));

          if (dur <= 0) return { ...s, positionMs: nextPos };
          if (nextPos < dur) return { ...s, positionMs: nextPos };

          if (s.repeat === "one") return { ...s, positionMs: 0 };

          const cur = s.current;
          const idx = cur
            ? s.queue.findIndex((t) => t.recordingId === cur.recordingId)
            : -1;
          const at = Math.max(idx, 0);
          const nextIdx = at + 1;

          if (nextIdx < s.queue.length) {
            const t = hydrateTrack(s.queue[nextIdx], s.durationByRecordingId);
            return {
              ...s,
              current: t,
              positionMs: 0,
              selectedRecordingId: t.recordingId,
              pendingRecordingId: t.recordingId,
            };
          }

          if (s.repeat === "all" && s.queue.length > 0) {
            const t = hydrateTrack(s.queue[0], s.durationByRecordingId);
            return {
              ...s,
              current: t,
              positionMs: 0,
              selectedRecordingId: t.recordingId,
              pendingRecordingId: t.recordingId,
            };
          }

          return { ...s, status: "paused", positionMs: dur };
        });
      },

      clearError: () =>
        setState((s) => (s.lastError ? { ...s, lastError: undefined } : s)),

      bumpReload: () => {
        void guardPlayback(() => {
          setState((s) => {
            if (!s.current?.muxPlaybackId) {
              return { ...s, reloadNonce: s.reloadNonce + 1 };
            }

            return {
              ...s,
              reloadNonce: s.reloadNonce + 1,
              status: "loading",
              loadingReason: "token",
              lastError: undefined,
              intent: "play",
              intentAtMs: Date.now(),
              pendingRecordingId: s.current.recordingId,
              selectedRecordingId: s.current.recordingId,
            };
          });
        });
      },
    };
  }, [guardPlayback]);

  const api: PlayerState & PlayerActions = React.useMemo(() => {
    return {
      ...state,
      ...actions,
    };
  }, [state, actions]);

  const {
    status: visualStatus,
    intent: visualIntent,
    playRequestPending: visualPlayRequestPending,
    current: visualCurrent,
    queue: visualQueue,
  } = state;

  const firstQueuedTrack = visualQueue[0];

  const visualState: PlayerVisualState = React.useMemo(
    () => ({
      status: visualStatus,
      intent: visualIntent,
      playRequestPending: visualPlayRequestPending,
      current: visualCurrent,
      firstQueuedTrack,
    }),
    [
      visualStatus,
      visualIntent,
      visualPlayRequestPending,
      visualCurrent,
      firstQueuedTrack,
    ],
  );

  const currentId = state.current?.recordingId ?? null;
  const pendingId = state.pendingRecordingId ?? null;

  React.useEffect(() => {
    const id = (currentId ?? "").trim();
    if (!id) return;
    void ensureLyricsForTrack(id);
  }, [currentId]);

  React.useEffect(() => {
    const id = (pendingId ?? "").trim();
    if (!id) return;
    void ensureLyricsForTrack(id);
  }, [pendingId]);

  return (
    <PlayerActionsCtx.Provider value={actions}>
      <PlayerVisualCtx.Provider value={visualState}>
        <PlayerCtx.Provider value={api}>{props.children}</PlayerCtx.Provider>
      </PlayerVisualCtx.Provider>
    </PlayerActionsCtx.Provider>
  );
}

export function usePlayer() {
  const ctx = React.useContext(PlayerCtx);
  if (!ctx)
    throw new Error("usePlayer must be used within PlayerStateProvider");
  return ctx;
}

export function usePlayerActions() {
  const ctx = React.useContext(PlayerActionsCtx);
  if (!ctx)
    throw new Error("usePlayerActions must be used within PlayerStateProvider");
  return ctx;
}

export function usePlayerVisual() {
  const ctx = React.useContext(PlayerVisualCtx);
  if (!ctx)
    throw new Error("usePlayerVisual must be used within PlayerStateProvider");
  return ctx;
}

----- app/home/urlState.ts -----
// web/app/home/urlState.ts
"use client";

import * as React from "react";

const QS_EVENT = "af:qs-change";

// Secondary query policy (strict allowlist).
// Surfaces are path-based now; query is for secondary concerns only.
const ALLOW_KEYS = new Set([
  "st",
  "share",
  "autoplay",
  "gift",
  "checkout",
  "purchase",
  "purchaseAlbum",
  "post",
  "pt",
]);

const ALLOW_PREFIXES = ["utm_"];

// Secondary query keys that should survive internal navigation.
// `st` is durable access context for share-token playback rehydration.
const PERSIST_KEYS = new Set(["st"]);

// Explicitly forbidden legacy/state keys (must never persist).
const FORBIDDEN_KEYS = new Set(["p", "album", "track", "t"]);

function isJournalPath(pathname: string): boolean {
  return pathname === "/journal" || pathname.startsWith("/journal/");
}

function safeGetPathname(): string {
  if (typeof window === "undefined") return "";
  return window.location.pathname || "";
}

function stripPathScopedKeys(params: URLSearchParams, pathname: string): void {
  if (!isJournalPath(pathname)) params.delete("post");
}

function safeGetSearch(): string {
  if (typeof window === "undefined") return "";
  return window.location.search || "";
}

function isAllowedKey(k: string): boolean {
  if (ALLOW_KEYS.has(k)) return true;
  return ALLOW_PREFIXES.some((p) => k.startsWith(p));
}

/** Mutates params to enforce the secondary-query policy. */
export function sanitizeSecondaryQuery(params: URLSearchParams): void {
  sanitizeSecondaryQueryForPath(params, safeGetPathname());
}

/** Mutates params to enforce the secondary-query policy for a destination path. */
export function sanitizeSecondaryQueryForPath(
  params: URLSearchParams,
  pathname: string,
): void {
  // 1) kill forbidden keys always
  for (const k of Array.from(params.keys())) {
    if (FORBIDDEN_KEYS.has(k)) params.delete(k);
  }

  // 2) strict allowlist for everything else
  for (const k of Array.from(params.keys())) {
    if (!isAllowedKey(k)) params.delete(k);
  }

  // 3) normalize share -> st when present (prefer explicit st)
  const st = (params.get("st") ?? "").trim();
  const share = (params.get("share") ?? "").trim();
  if (!st && share) params.set("st", share);
  if (params.has("share")) params.delete("share");

  // 4) remove query keys that only make sense on specific routes
  stripPathScopedKeys(params, pathname);

  // 5) trim empties
  for (const [k, v] of Array.from(params.entries())) {
    if (!String(v ?? "").trim()) params.delete(k);
  }
}

/** Read current location query and return a policy-sanitized copy. */
export function pickSecondaryQueryFromLocation(): URLSearchParams {
  const params = new URLSearchParams(safeGetSearch().replace(/^\?/, ""));
  sanitizeSecondaryQuery(params);
  return params;
}

function pickPersistentSecondaryQueryFromParams(
  params: URLSearchParams,
): URLSearchParams {
  const clean = new URLSearchParams(params);
  sanitizeSecondaryQuery(clean);

  for (const k of Array.from(clean.keys())) {
    if (!PERSIST_KEYS.has(k)) clean.delete(k);
  }

  return clean;
}

/** Read current location query and return only durable query state. */
export function pickPersistentSecondaryQueryFromLocation(): URLSearchParams {
  const params = new URLSearchParams(safeGetSearch().replace(/^\?/, ""));
  return pickPersistentSecondaryQueryFromParams(params);
}

/**
 * Appends durable secondary query state to same-origin internal hrefs.
 *
 * Preserves hash fragments, so:
 *   /exegesis/foo#l=bar
 * becomes:
 *   /exegesis/foo?st=TOKEN#l=bar
 */
export function appendPersistentSecondaryQueryToHref(href: string): string {
  if (typeof window === "undefined") return href;

  const persistent = pickPersistentSecondaryQueryFromLocation();
  if (Array.from(persistent.keys()).length === 0) return href;

  const url = new URL(href, window.location.href);
  if (url.origin !== window.location.origin) return href;

  const params = new URLSearchParams(url.search);
  sanitizeSecondaryQueryForPath(params, url.pathname);

  for (const [k, v] of persistent.entries()) {
    if (!params.has(k)) params.set(k, v);
  }

  sanitizeSecondaryQueryForPath(params, url.pathname);

  const next = params.toString();
  url.search = next ? `?${next}` : "";

  return `${url.pathname}${url.search}${url.hash}`;
}

export function useClientSearchParams(): URLSearchParams {
  const [qs, setQs] = React.useState<string>(() => safeGetSearch());

  React.useEffect(() => {
    const onPop = () => setQs(safeGetSearch());
    const onCustom = () => setQs(safeGetSearch());

    window.addEventListener("popstate", onPop);
    window.addEventListener(QS_EVENT, onCustom as EventListener);
    return () => {
      window.removeEventListener("popstate", onPop);
      window.removeEventListener(QS_EVENT, onCustom as EventListener);
    };
  }, []);

  return React.useMemo(() => {
    const params = new URLSearchParams((qs || "").replace(/^\?/, ""));
    sanitizeSecondaryQuery(params);
    return params;
  }, [qs]);
}

export function getAutoplayFlag(sp: URLSearchParams): boolean {
  const v = (sp.get("autoplay") ?? "").trim().toLowerCase();
  return v === "1" || v === "true" || v === "yes";
}

export function usePersistentSecondaryHref(href: string): string {
  const sp = useClientSearchParams();
  const qs = React.useMemo(() => sp.toString(), [sp]);

  return React.useMemo(() => {
    void qs;
    return appendPersistentSecondaryQueryToHref(href);
  }, [href, qs]);
}

/**
 * Patch semantics:
 * - null/undefined/'' => delete
 * - otherwise => set
 *
 * IMPORTANT: This must NOT implement any portal/player surface logic.
 * Surfaces are path-based now. Query is secondary only.
 */
export function replaceQuery(patch: Record<string, string | null | undefined>) {
  if (typeof window === "undefined") return;

  const url = new URL(window.location.href);
  const params = new URLSearchParams(url.search);

  // Build a sanitized patch object WITHOUT mutating input.
  const cleanPatch: Record<string, string | null> = {};
  for (const [k, v] of Object.entries(patch)) {
    if (FORBIDDEN_KEYS.has(k)) continue;
    const sv = v == null ? "" : String(v);
    cleanPatch[k] = sv.trim() ? sv : null;
  }

  // Apply patch
  for (const [k, v] of Object.entries(cleanPatch)) {
    if (v == null) params.delete(k);
    else params.set(k, v);
  }

  // Enforce policy after patch
  sanitizeSecondaryQueryForPath(params, url.pathname);

  const next = params.toString();
  const cur = url.searchParams.toString();
  if (next === cur) return;

  url.search = next ? `?${next}` : "";
  window.history.replaceState({}, "", url.toString());
  window.dispatchEvent(new Event(QS_EVENT));
}

/**
 * Convenience: read a query param once, then clear it.
 */
export function useReadOnceParam(key: string): string | null {
  const sp = useClientSearchParams();
  const v = (sp.get(key) ?? "").trim() || null;
  const shownRef = React.useRef(false);

  React.useEffect(() => {
    if (!v) return;
    if (shownRef.current) return;
    shownRef.current = true;
    replaceQuery({ [key]: null });
  }, [v, key]);

  return v;
}


----- lib/playbackAdmin.ts -----
// web/lib/playbackAdmin.ts
import "server-only";

import { sql } from "@vercel/postgres";
import { getRecordingSummaryByRecordingId } from "@/lib/albums";
import { EVENT_TYPES } from "@/lib/vocab";

const RECENT_WINDOW_DAYS = 30;
const TELEMETRY_PROGRESS_STEP_MS = 15_000;
const RECENT_DEDUPE_SESSION_LIMIT = 24;

type AggregateRow = {
  listened_ms: string | number;
  credited_progress_count: string | number;
  play_count: string | number;
  completed_count: string | number;
  active_count: number | null;
};

type RecordingAggregateRow = {
  recording_id: string;
  listened_ms: string | number;
  credited_progress_count: string | number;
  play_count: string | number;
  completed_count: string | number;
  last_listened_at: string | null;
};

type DedupeRow = {
  member_id: string | null;
  member_email: string | null;
  playback_id: string;
  recording_id: string | null;
  event_type: string;
  milestone_key: string;
  created_at: string;
  audience: "member" | "anonymous";
  share_token_label: string | null;
};

type TrendRow = {
  bucket_start_iso: string;
  member_play_count: string | number;
  anonymous_play_count: string | number;
  site_play_count: string | number;
};

type ShareLinkActivityQueryRow = {
  telemetry_label: string;
  qualified_play_count: string | number;
  listened_ms: string | number;
  completed_count: string | number;
  top_recording_id: string | null;
  last_activity_at: string;
};

function asSafeInt(value: string | number | null | undefined): number {
  if (typeof value === "number") {
    return Number.isFinite(value) ? Math.floor(value) : 0;
  }

  if (typeof value === "string") {
    const n = Number(value);
    return Number.isFinite(n) ? Math.floor(n) : 0;
  }

  return 0;
}

function formatIso(value: string | null): string | null {
  if (!value) return null;
  const t = Date.parse(value);
  if (!Number.isFinite(t)) return null;
  return new Date(t).toISOString();
}

export type PlaybackAdminAggregate = {
  listenedMs: number;
  creditedProgressCount: number;
  playCount: number;
  completedCount: number;
  activeCount: number | null;
};

export type PlaybackAdminTrackRow = {
  recordingId: string;
  title: string;
  artist: string | null;
  listenedMs: number;
  creditedProgressCount: number;
  playCount: number;
  completedCount: number;
  lastListenedAt: string | null;
};

export type PlaybackAdminDedupeRow = {
  memberId: string | null;
  memberEmail: string | null;
  playbackId: string;
  recordingId: string | null;
  recordingTitle: string | null;
  eventType: string;
  milestoneKey: string;
  createdAt: string;
  audience: "member" | "anonymous";
  shareTokenLabel: string | null;
};

export type PlaybackAdminTrendRangeKey =
  | "hour"
  | "day"
  | "week"
  | "month"
  | "year"
  | "allTime";

export type PlaybackAdminTrendBucket = {
  bucketStart: string;
  memberPlayCount: number;
  anonymousPlayCount: number;
  sitePlayCount: number;
};

export type PlaybackAdminTrendRange = {
  key: PlaybackAdminTrendRangeKey;
  label: string;
  buckets: PlaybackAdminTrendBucket[];
};

export type PlaybackAdminAudienceSplit = {
  allTimeMemberPlayCount: number;
  allTimeAnonymousPlayCount: number;
  recent30dMemberPlayCount: number;
  recent30dAnonymousPlayCount: number;
};

export type PlaybackAdminShareLinkActivityRow = {
  telemetryLabel: string;
  qualifiedPlayCount: number;
  listenedMs: number;
  completedCount: number;
  topTrackTitle: string | null;
  lastActivityAt: string;
};

export type PlaybackAdminSnapshot = {
  generatedAt: string;
  memberTotals: PlaybackAdminAggregate;
  member30d: PlaybackAdminAggregate;
  siteTotals: PlaybackAdminAggregate;
  site30d: PlaybackAdminAggregate;
  qualifiedPlayTrends: Record<
    PlaybackAdminTrendRangeKey,
    PlaybackAdminTrendRange
  >;
  audienceSplit: PlaybackAdminAudienceSplit;
  topTracksByListenedMs: PlaybackAdminTrackRow[];
  recentTracks: PlaybackAdminTrackRow[];
  recentDedupe: PlaybackAdminDedupeRow[];
  shareLinkActivity: PlaybackAdminShareLinkActivityRow[];
};

async function getMemberTotals(): Promise<PlaybackAdminAggregate> {
  const res = await sql<AggregateRow>`
    select
      coalesce(sum(listened_ms), 0) as listened_ms,
      coalesce(sum(credited_progress_count), 0) as credited_progress_count,
      coalesce(sum(play_count), 0) as play_count,
      coalesce(sum(completed_count), 0) as completed_count,
      count(*)::int as active_count
    from member_listen_totals
    where
      listened_ms > 0
      or credited_progress_count > 0
      or play_count > 0
      or completed_count > 0
  `;

  const row = res.rows[0];

  return {
    listenedMs: asSafeInt(row?.listened_ms),
    creditedProgressCount: asSafeInt(row?.credited_progress_count),
    playCount: asSafeInt(row?.play_count),
    completedCount: asSafeInt(row?.completed_count),
    activeCount: row?.active_count ?? 0,
  };
}

async function getSiteTotals(): Promise<PlaybackAdminAggregate> {
  const res = await sql<AggregateRow>`
    select
      coalesce(sum(listened_ms), 0) as listened_ms,
      coalesce(sum(credited_progress_count), 0) as credited_progress_count,
      coalesce(sum(play_count), 0) as play_count,
      coalesce(sum(completed_count), 0) as completed_count,
      count(*)::int as active_count
    from recording_listen_totals
    where
      listened_ms > 0
      or credited_progress_count > 0
      or play_count > 0
      or completed_count > 0
  `;

  const row = res.rows[0];

  return {
    listenedMs: asSafeInt(row?.listened_ms),
    creditedProgressCount: asSafeInt(row?.credited_progress_count),
    playCount: asSafeInt(row?.play_count),
    completedCount: asSafeInt(row?.completed_count),
    activeCount: row?.active_count ?? 0,
  };
}

async function getMember30d(): Promise<PlaybackAdminAggregate> {
  const res = await sql<AggregateRow>`
    select
      coalesce(
        count(*) filter (
          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PROGRESS}
        ),
        0
      ) * ${TELEMETRY_PROGRESS_STEP_MS} as listened_ms,
      coalesce(
        count(*) filter (
          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PROGRESS}
        ),
        0
      ) as credited_progress_count,
      coalesce(
        count(*) filter (
          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        ),
        0
      ) as play_count,
      coalesce(
        count(*) filter (
          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_COMPLETE}
        ),
        0
      ) as completed_count,
      count(distinct member_id)::int as active_count
    from member_playback_telemetry_dedupe
    where created_at >= now() - (${RECENT_WINDOW_DAYS} * interval '1 day')
  `;

  const row = res.rows[0];

  return {
    listenedMs: asSafeInt(row?.listened_ms),
    creditedProgressCount: asSafeInt(row?.credited_progress_count),
    playCount: asSafeInt(row?.play_count),
    completedCount: asSafeInt(row?.completed_count),
    activeCount: row?.active_count ?? 0,
  };
}

async function getSite30d(): Promise<PlaybackAdminAggregate> {
  const res = await sql<AggregateRow>`
    with recent_events as (
      select event_type
      from member_playback_telemetry_dedupe
      where created_at >= now() - (${RECENT_WINDOW_DAYS} * interval '1 day')

      union all

      select event_type
      from anonymous_playback_telemetry_dedupe
      where created_at >= now() - (${RECENT_WINDOW_DAYS} * interval '1 day')
    )
    select
      coalesce(
        count(*) filter (
          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PROGRESS}
        ),
        0
      ) * ${TELEMETRY_PROGRESS_STEP_MS} as listened_ms,
      coalesce(
        count(*) filter (
          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PROGRESS}
        ),
        0
      ) as credited_progress_count,
      coalesce(
        count(*) filter (
          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        ),
        0
      ) as play_count,
      coalesce(
        count(*) filter (
          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_COMPLETE}
        ),
        0
      ) as completed_count,
      null::int as active_count
    from recent_events
  `;

  const row = res.rows[0];

  return {
    listenedMs: asSafeInt(row?.listened_ms),
    creditedProgressCount: asSafeInt(row?.credited_progress_count),
    playCount: asSafeInt(row?.play_count),
    completedCount: asSafeInt(row?.completed_count),
    activeCount: row?.active_count ?? null,
  };
}

async function hydrateTrackRows(
  rows: RecordingAggregateRow[],
): Promise<PlaybackAdminTrackRow[]> {
  const hydrated = await Promise.all(
    rows.map(async (row): Promise<PlaybackAdminTrackRow> => {
      const recording = await getRecordingSummaryByRecordingId(
        row.recording_id,
      );

      return {
        recordingId: row.recording_id,
        title: recording?.title ?? row.recording_id,
        artist: recording?.artist ?? null,
        listenedMs: asSafeInt(row.listened_ms),
        creditedProgressCount: asSafeInt(row.credited_progress_count),
        playCount: asSafeInt(row.play_count),
        completedCount: asSafeInt(row.completed_count),
        lastListenedAt: formatIso(row.last_listened_at),
      };
    }),
  );

  return hydrated;
}

async function getTopTracksByListenedMs(): Promise<PlaybackAdminTrackRow[]> {
  const res = await sql<RecordingAggregateRow>`
    select
      recording_id,
      listened_ms,
      credited_progress_count,
      play_count,
      completed_count,
      last_listened_at
    from recording_listen_totals
    where
      listened_ms > 0
      or credited_progress_count > 0
      or play_count > 0
      or completed_count > 0
    order by
      listened_ms desc,
      play_count desc,
      completed_count desc,
      last_listened_at desc nulls last
    limit 12
  `;

  return hydrateTrackRows(res.rows);
}

async function getRecentTracks(): Promise<PlaybackAdminTrackRow[]> {
  const res = await sql<RecordingAggregateRow>`
    select
      recording_id,
      listened_ms,
      credited_progress_count,
      play_count,
      completed_count,
      last_listened_at
    from recording_listen_totals
    where last_listened_at is not null
    order by last_listened_at desc
    limit 12
  `;

  return hydrateTrackRows(res.rows);
}

function mapTrendRows(rows: TrendRow[]): PlaybackAdminTrendBucket[] {
  return rows.map((row) => ({
    bucketStart: row.bucket_start_iso,
    memberPlayCount: asSafeInt(row.member_play_count),
    anonymousPlayCount: asSafeInt(row.anonymous_play_count),
    sitePlayCount: asSafeInt(row.site_play_count),
  }));
}

async function getQualifiedPlayTrendHour(): Promise<PlaybackAdminTrendRange> {
  const res = await sql<TrendRow>`
    with buckets as (
      select generate_series(
        date_trunc('minute', now()) - (59 * interval '1 minute'),
        date_trunc('minute', now()),
        interval '1 minute'
      ) as bucket_start
    ),
    member_counts as (
      select
        date_trunc('minute', created_at) as bucket_start,
        count(*)::int as n
      from member_playback_telemetry_dedupe
      where
        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        and created_at >= date_trunc('minute', now()) - (59 * interval '1 minute')
      group by 1
    ),
    anonymous_counts as (
      select
        date_trunc('minute', created_at) as bucket_start,
        count(*)::int as n
      from anonymous_playback_telemetry_dedupe
      where
        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        and created_at >= date_trunc('minute', now()) - (59 * interval '1 minute')
      group by 1
    )
    select
      buckets.bucket_start::text as bucket_start_iso,
      coalesce(member_counts.n, 0) as member_play_count,
      coalesce(anonymous_counts.n, 0) as anonymous_play_count,
      coalesce(member_counts.n, 0) + coalesce(anonymous_counts.n, 0) as site_play_count
    from buckets
    left join member_counts
      on member_counts.bucket_start = buckets.bucket_start
    left join anonymous_counts
      on anonymous_counts.bucket_start = buckets.bucket_start
    order by buckets.bucket_start asc
  `;

  return {
    key: "hour",
    label: "60m",
    buckets: mapTrendRows(res.rows),
  };
}

async function getQualifiedPlayTrendDay(): Promise<PlaybackAdminTrendRange> {
  const res = await sql<TrendRow>`
    with buckets as (
      select generate_series(
        date_trunc('hour', now()) - (23 * interval '1 hour'),
        date_trunc('hour', now()),
        interval '1 hour'
      ) as bucket_start
    ),
    member_counts as (
      select
        date_trunc('hour', created_at) as bucket_start,
        count(*)::int as n
      from member_playback_telemetry_dedupe
      where
        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        and created_at >= date_trunc('hour', now()) - (23 * interval '1 hour')
      group by 1
    ),
    anonymous_counts as (
      select
        date_trunc('hour', created_at) as bucket_start,
        count(*)::int as n
      from anonymous_playback_telemetry_dedupe
      where
        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        and created_at >= date_trunc('hour', now()) - (23 * interval '1 hour')
      group by 1
    )
    select
      buckets.bucket_start::text as bucket_start_iso,
      coalesce(member_counts.n, 0) as member_play_count,
      coalesce(anonymous_counts.n, 0) as anonymous_play_count,
      coalesce(member_counts.n, 0) + coalesce(anonymous_counts.n, 0) as site_play_count
    from buckets
    left join member_counts
      on member_counts.bucket_start = buckets.bucket_start
    left join anonymous_counts
      on anonymous_counts.bucket_start = buckets.bucket_start
    order by buckets.bucket_start asc
  `;

  return {
    key: "day",
    label: "24h",
    buckets: mapTrendRows(res.rows),
  };
}

async function getQualifiedPlayTrendWeek(): Promise<PlaybackAdminTrendRange> {
  const res = await sql<TrendRow>`
    with buckets as (
      select generate_series(
        date_trunc('day', now()) - (6 * interval '1 day'),
        date_trunc('day', now()),
        interval '1 day'
      ) as bucket_start
    ),
    member_counts as (
      select
        date_trunc('day', created_at) as bucket_start,
        count(*)::int as n
      from member_playback_telemetry_dedupe
      where
        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        and created_at >= date_trunc('day', now()) - (6 * interval '1 day')
      group by 1
    ),
    anonymous_counts as (
      select
        date_trunc('day', created_at) as bucket_start,
        count(*)::int as n
      from anonymous_playback_telemetry_dedupe
      where
        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        and created_at >= date_trunc('day', now()) - (6 * interval '1 day')
      group by 1
    )
    select
      buckets.bucket_start::text as bucket_start_iso,
      coalesce(member_counts.n, 0) as member_play_count,
      coalesce(anonymous_counts.n, 0) as anonymous_play_count,
      coalesce(member_counts.n, 0) + coalesce(anonymous_counts.n, 0) as site_play_count
    from buckets
    left join member_counts
      on member_counts.bucket_start = buckets.bucket_start
    left join anonymous_counts
      on anonymous_counts.bucket_start = buckets.bucket_start
    order by buckets.bucket_start asc
  `;

  return {
    key: "week",
    label: "7d",
    buckets: mapTrendRows(res.rows),
  };
}

async function getQualifiedPlayTrendMonth(): Promise<PlaybackAdminTrendRange> {
  const res = await sql<TrendRow>`
    with buckets as (
      select generate_series(
        date_trunc('day', now()) - (29 * interval '1 day'),
        date_trunc('day', now()),
        interval '1 day'
      ) as bucket_start
    ),
    member_counts as (
      select
        date_trunc('day', created_at) as bucket_start,
        count(*)::int as n
      from member_playback_telemetry_dedupe
      where
        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        and created_at >= date_trunc('day', now()) - (29 * interval '1 day')
      group by 1
    ),
    anonymous_counts as (
      select
        date_trunc('day', created_at) as bucket_start,
        count(*)::int as n
      from anonymous_playback_telemetry_dedupe
      where
        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        and created_at >= date_trunc('day', now()) - (29 * interval '1 day')
      group by 1
    )
    select
      buckets.bucket_start::text as bucket_start_iso,
      coalesce(member_counts.n, 0) as member_play_count,
      coalesce(anonymous_counts.n, 0) as anonymous_play_count,
      coalesce(member_counts.n, 0) + coalesce(anonymous_counts.n, 0) as site_play_count
    from buckets
    left join member_counts
      on member_counts.bucket_start = buckets.bucket_start
    left join anonymous_counts
      on anonymous_counts.bucket_start = buckets.bucket_start
    order by buckets.bucket_start asc
  `;

  return {
    key: "month",
    label: "30d",
    buckets: mapTrendRows(res.rows),
  };
}

async function getQualifiedPlayTrendYear(): Promise<PlaybackAdminTrendRange> {
  const res = await sql<TrendRow>`
    with buckets as (
      select generate_series(
        date_trunc('month', now()) - (11 * interval '1 month'),
        date_trunc('month', now()),
        interval '1 month'
      ) as bucket_start
    ),
    member_counts as (
      select
        date_trunc('month', created_at) as bucket_start,
        count(*)::int as n
      from member_playback_telemetry_dedupe
      where
        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        and created_at >= date_trunc('month', now()) - (11 * interval '1 month')
      group by 1
    ),
    anonymous_counts as (
      select
        date_trunc('month', created_at) as bucket_start,
        count(*)::int as n
      from anonymous_playback_telemetry_dedupe
      where
        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        and created_at >= date_trunc('month', now()) - (11 * interval '1 month')
      group by 1
    )
    select
      buckets.bucket_start::text as bucket_start_iso,
      coalesce(member_counts.n, 0) as member_play_count,
      coalesce(anonymous_counts.n, 0) as anonymous_play_count,
      coalesce(member_counts.n, 0) + coalesce(anonymous_counts.n, 0) as site_play_count
    from buckets
    left join member_counts
      on member_counts.bucket_start = buckets.bucket_start
    left join anonymous_counts
      on anonymous_counts.bucket_start = buckets.bucket_start
    order by buckets.bucket_start asc
  `;

  return {
    key: "year",
    label: "12m",
    buckets: mapTrendRows(res.rows),
  };
}

async function getQualifiedPlayTrendAllTime(): Promise<PlaybackAdminTrendRange> {
  const res = await sql<TrendRow>`
    with bounds as (
      select
        least(
          coalesce(
            (
              select date_trunc('year', min(created_at))
              from member_playback_telemetry_dedupe
              where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
            ),
            date_trunc('year', now())
          ),
          coalesce(
            (
              select date_trunc('year', min(created_at))
              from anonymous_playback_telemetry_dedupe
              where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
            ),
            date_trunc('year', now())
          )
        ) as first_year_start,
        date_trunc('year', now()) as current_year_start
    ),
    buckets as (
      select generate_series(
        (select first_year_start from bounds),
        (select current_year_start from bounds),
        interval '1 year'
      ) as bucket_start
    ),
    member_counts as (
      select
        date_trunc('year', created_at) as bucket_start,
        count(*)::int as n
      from member_playback_telemetry_dedupe
      where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
      group by 1
    ),
    anonymous_counts as (
      select
        date_trunc('year', created_at) as bucket_start,
        count(*)::int as n
      from anonymous_playback_telemetry_dedupe
      where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
      group by 1
    )
    select
      buckets.bucket_start::text as bucket_start_iso,
      coalesce(member_counts.n, 0) as member_play_count,
      coalesce(anonymous_counts.n, 0) as anonymous_play_count,
      coalesce(member_counts.n, 0) + coalesce(anonymous_counts.n, 0) as site_play_count
    from buckets
    left join member_counts
      on member_counts.bucket_start = buckets.bucket_start
    left join anonymous_counts
      on anonymous_counts.bucket_start = buckets.bucket_start
    order by buckets.bucket_start asc
  `;

  return {
    key: "allTime",
    label: "All time",
    buckets: mapTrendRows(res.rows),
  };
}

async function getQualifiedPlayTrends(): Promise<
  Record<PlaybackAdminTrendRangeKey, PlaybackAdminTrendRange>
> {
  const [hour, day, week, month, year, allTime] = await Promise.all([
    getQualifiedPlayTrendHour(),
    getQualifiedPlayTrendDay(),
    getQualifiedPlayTrendWeek(),
    getQualifiedPlayTrendMonth(),
    getQualifiedPlayTrendYear(),
    getQualifiedPlayTrendAllTime(),
  ]);

  return {
    hour,
    day,
    week,
    month,
    year,
    allTime,
  };
}

async function getShareLinkActivity(): Promise<
  PlaybackAdminShareLinkActivityRow[]
> {
  const result = await sql<ShareLinkActivityQueryRow>`
    with label_totals as (
      select
        telemetry_label,
        count(*) filter (
          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
        ) as qualified_play_count,
        coalesce(
          sum(listened_ms) filter (
            where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PROGRESS}
          ),
          0
        ) as listened_ms,
        count(*) filter (
          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_COMPLETE}
        ) as completed_count,
        max(occurred_at) as last_activity_at
      from share_token_playback_events
      where char_length(btrim(telemetry_label)) > 0
      group by telemetry_label
    ),
    ranked_tracks as (
      select
        telemetry_label,
        recording_id,
        row_number() over (
          partition by telemetry_label
          order by
            coalesce(
              sum(listened_ms) filter (
                where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PROGRESS}
              ),
              0
            ) desc,
            count(*) filter (
              where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
            ) desc,
            max(occurred_at) desc
        ) as rank
      from share_token_playback_events
      where char_length(btrim(telemetry_label)) > 0
      group by telemetry_label, recording_id
    )
    select
      totals.telemetry_label,
      totals.qualified_play_count,
      totals.listened_ms,
      totals.completed_count,
      tracks.recording_id as top_recording_id,
      totals.last_activity_at::text as last_activity_at
    from label_totals totals
    left join ranked_tracks tracks
      on tracks.telemetry_label = totals.telemetry_label
      and tracks.rank = 1
    order by
      totals.last_activity_at desc,
      totals.listened_ms desc,
      totals.qualified_play_count desc
    limit 24
  `;

  return Promise.all(
    result.rows.map(async (row): Promise<PlaybackAdminShareLinkActivityRow> => {
      const topTrack = row.top_recording_id
        ? await getRecordingSummaryByRecordingId(row.top_recording_id)
        : null;

      return {
        telemetryLabel: row.telemetry_label,
        qualifiedPlayCount: asSafeInt(row.qualified_play_count),
        listenedMs: asSafeInt(row.listened_ms),
        completedCount: asSafeInt(row.completed_count),
        topTrackTitle: topTrack?.title ?? row.top_recording_id,
        lastActivityAt: new Date(row.last_activity_at).toISOString(),
      };
    }),
  );
}

async function getRecentDedupe(): Promise<PlaybackAdminDedupeRow[]> {
  const res = await sql<DedupeRow>`
        with recent_member_dedupe as (
      select
        d.member_id,
        m.email::text as member_email,
        d.playback_id,
        evt.recording_id,
        d.event_type,
        d.milestone_key,
        d.created_at::text as created_at,
        'member'::text as audience,
        share_event.telemetry_label as share_token_label
      from member_playback_telemetry_dedupe d
      left join members m
        on m.id = d.member_id
      left join lateral (
        select
          me.payload->>'recording_id' as recording_id
        from member_events me
        where
          me.member_id = d.member_id
          and me.event_type = d.event_type
          and me.payload->>'playback_id' = d.playback_id
        order by me.occurred_at desc
        limit 1
      ) evt on true
      left join lateral (
        select e.telemetry_label
        from share_token_playback_events e
        where
          e.audience = 'member'
          and e.member_id = d.member_id
          and e.playback_id = d.playback_id
          and e.event_type = d.event_type
          and e.milestone_key = d.milestone_key
        order by e.occurred_at desc
        limit 1
      ) share_event on true
    ),
        recent_anonymous_dedupe as (
      select
        null::uuid as member_id,
        null::text as member_email,
        d.playback_id,
        d.recording_id,
        d.event_type,
        d.milestone_key,
        d.created_at::text as created_at,
        'anonymous'::text as audience,
        share_event.telemetry_label as share_token_label
      from anonymous_playback_telemetry_dedupe d
      left join lateral (
        select e.telemetry_label
        from share_token_playback_events e
        where
          e.audience = 'anonymous'
          and e.playback_id = d.playback_id
          and e.recording_id = d.recording_id
          and e.event_type = d.event_type
          and e.milestone_key = d.milestone_key
        order by e.occurred_at desc
        limit 1
      ) share_event on true
    ),
    all_dedupe_rows as (
      select * from recent_member_dedupe
      union all
      select * from recent_anonymous_dedupe
    ),
    recent_sessions as (
      select
        audience,
        coalesce(member_id::text, 'anonymous') as identity_key,
        coalesce(recording_id, '__unknown_recording__') as recording_key,
        playback_id,
        max(created_at::timestamptz) as latest_at
      from all_dedupe_rows
      group by
        audience,
        coalesce(member_id::text, 'anonymous'),
        coalesce(recording_id, '__unknown_recording__'),
        playback_id
      order by latest_at desc
      limit ${RECENT_DEDUPE_SESSION_LIMIT}
    )
    select
      t.member_id::text as member_id,
      t.member_email,
      t.playback_id,
      t.recording_id,
      t.event_type,
      t.milestone_key,
      t.created_at,
      t.audience,
      t.share_token_label
    from all_dedupe_rows t
    inner join recent_sessions s
      on s.audience = t.audience
      and s.identity_key = coalesce(t.member_id::text, 'anonymous')
      and s.recording_key = coalesce(t.recording_id, '__unknown_recording__')
      and s.playback_id = t.playback_id
    order by t.created_at desc
  `;

  const titleByRecordingId = new Map<string, string | null>();
  const distinctRecordingIds = Array.from(
    new Set(
      res.rows
        .map((row) => row.recording_id)
        .filter(
          (value): value is string =>
            typeof value === "string" && value.length > 0,
        ),
    ),
  );

  await Promise.all(
    distinctRecordingIds.map(async (recordingId) => {
      const recording = await getRecordingSummaryByRecordingId(recordingId);
      titleByRecordingId.set(recordingId, recording?.title ?? recordingId);
    }),
  );

  return res.rows.map((row) => ({
    memberId: row.member_id,
    memberEmail: row.member_email,
    playbackId: row.playback_id,
    recordingId: row.recording_id,
    recordingTitle: row.recording_id
      ? (titleByRecordingId.get(row.recording_id) ?? row.recording_id)
      : null,
    eventType: row.event_type,
    milestoneKey: row.milestone_key,
    createdAt: new Date(row.created_at).toISOString(),
    audience: row.audience,
    shareTokenLabel: row.share_token_label,
  }));
}

function buildAudienceSplit(params: {
  memberTotals: PlaybackAdminAggregate;
  siteTotals: PlaybackAdminAggregate;
  member30d: PlaybackAdminAggregate;
  site30d: PlaybackAdminAggregate;
}): PlaybackAdminAudienceSplit {
  const allTimeMemberPlayCount = Math.max(0, params.memberTotals.playCount);
  const allTimeAnonymousPlayCount = Math.max(
    0,
    params.siteTotals.playCount - params.memberTotals.playCount,
  );
  const recent30dMemberPlayCount = Math.max(0, params.member30d.playCount);
  const recent30dAnonymousPlayCount = Math.max(
    0,
    params.site30d.playCount - params.member30d.playCount,
  );

  return {
    allTimeMemberPlayCount,
    allTimeAnonymousPlayCount,
    recent30dMemberPlayCount,
    recent30dAnonymousPlayCount,
  };
}

export async function getPlaybackAdminSnapshot(): Promise<PlaybackAdminSnapshot> {
  const [
    memberTotals,
    member30d,
    siteTotals,
    site30d,
    qualifiedPlayTrends,
    topTracksByListenedMs,
    recentTracks,
    recentDedupe,
    shareLinkActivity,
  ] = await Promise.all([
    getMemberTotals(),
    getMember30d(),
    getSiteTotals(),
    getSite30d(),
    getQualifiedPlayTrends(),
    getTopTracksByListenedMs(),
    getRecentTracks(),
    getRecentDedupe(),
    getShareLinkActivity(),
  ]);

  return {
    generatedAt: new Date().toISOString(),
    memberTotals,
    member30d,
    siteTotals,
    site30d,
    qualifiedPlayTrends,
    audienceSplit: buildAudienceSplit({
      memberTotals,
      siteTotals,
      member30d,
      site30d,
    }),
    topTracksByListenedMs,
    recentTracks,
    recentDedupe,
    shareLinkActivity,
  };
}


----- lib/types.ts -----
// web/lib/types.ts

export type TierName = "friend" | "patron" | "partner";
export type Tier = "none" | TierName;

export type AlbumEmbargoInfo = {
  embargoed: boolean;
  releaseAt: string | null;
  note?: string | null;
};

export type AlbumInfo = {
  id: string;
  catalogueId?: string;
  title: string;
  artist?: string;
  year?: number;
  description?: string;
  artworkUrl?: string | null;
  policy?: AlbumPolicy;
  embargo?: AlbumEmbargoInfo;
  platformLinks?: { platform: string; url: string }[];
};

export type AlbumPolicy = {
  publicPageVisible: boolean;
  releaseAt?: string | null;
  earlyAccessEnabled?: boolean;
  earlyAccessTiers?: TierName[];
  minTierToLoad?: TierName | null;
};

export type AlbumPlayerBundle = {
  albumSlug: string;
  album: AlbumInfo | null;
  tracks: PlayerTrack[];
  albumLyrics: AlbumLyricsBundle | null;
};

// Optional helper (keeps call-sites tidy)
export function makeAlbumPlayerBundle(args: {
  albumSlug: string;
  album: AlbumInfo | null;
  tracks: PlayerTrack[];
  albumLyrics?: AlbumLyricsBundle | null;
}): AlbumPlayerBundle {
  return {
    albumSlug: args.albumSlug,
    album: args.album,
    tracks: Array.isArray(args.tracks) ? args.tracks : [],
    albumLyrics: args.albumLyrics ?? null,
  };
}

export type AlbumNavItem = {
  id: string;
  slug: string;
  title: string;
  artist?: string;
  year?: number;
  coverUrl?: string | null;

  // browse-click gating (load gate)
  policy?: {
    publicPageVisible: boolean;
    minTierToLoad: TierName | null;
  };
};

export type PlayerTrack = {
  recordingId: string;
  displayId: string;
  title?: string;
  artist?: string;
  durationMs?: number;
  muxPlaybackId?: string;
  visualTheme?: string;
  explicit?: boolean;
  playCount?: number;
};

export type AlbumLyricsBundle = {
  cuesByRecordingId: Record<string, LyricCue[]>;
  offsetByRecordingId: Record<string, number>;
};

export type LyricGroupMapEntry = {
  canonicalGroupKey: string;
  updatedAt: string; // ISO string from DB
};

export type LyricGroupMap = Record<string, LyricGroupMapEntry>; // keyed by lineKey

export type LyricCue = {
  lineKey: string;
  tMs: number;
  text: string;
  endMs?: number;

  /**
   * Optional enrichment: if this cue's lineKey is mapped into a canonical exegesis group.
   * Present when known; omitted otherwise.
   */
  canonicalGroupKey?: string;
};

export type TrackLyricsApiOk = {
  ok: true;
  recordingId: string;
  offsetMs: number;
  cues: LyricCue[];

  // present in API today, but optional for consumers
  version?: string;
  geniusUrl?: string | null;
  groupMap?: LyricGroupMap;

  // meta fields the API returns (nullable)
  trackTitle?: string | null;
  trackArtist?: string | null;
  trackCatalogueId?: string | null;
  albumTitle?: string | null;
  albumSlug?: string | null;
  albumCatalogueId?: string | null;
};

export function normalizeLyricCues(input: unknown): LyricCue[] {
  if (!Array.isArray(input)) return [];
  const out: LyricCue[] = [];

  for (const c of input) {
    if (!c || typeof c !== "object") continue;

    const lineKey = (c as { lineKey?: unknown }).lineKey;
    const tMs = (c as { tMs?: unknown }).tMs;
    const text = (c as { text?: unknown }).text;
    const endMs = (c as { endMs?: unknown }).endMs;
    const canonicalGroupKey = (c as { canonicalGroupKey?: unknown })
      .canonicalGroupKey;

    if (typeof lineKey !== "string" || lineKey.trim().length === 0) continue;
    if (typeof tMs !== "number" || !Number.isFinite(tMs) || tMs < 0) continue;
    if (typeof text !== "string" || text.trim().length === 0) continue;

    const cue: LyricCue = {
      lineKey: lineKey.trim(),
      tMs: Math.floor(tMs),
      text: text.trim(),
    };

    if (typeof endMs === "number" && Number.isFinite(endMs) && endMs >= 0) {
      cue.endMs = Math.floor(endMs);
    }

    // optional enrichment
    if (typeof canonicalGroupKey === "string" && canonicalGroupKey.trim()) {
      cue.canonicalGroupKey = canonicalGroupKey.trim();
    }

    out.push(cue);
  }

  out.sort((a, b) => a.tMs - b.tMs);
  return out;
}

export type SanityLyricCue = {
  _key?: string;
  tMs?: number;
  text?: string;
  endMs?: number;
};

// Adapts Sanity cue shape into canonical cue normalization.
// (Sanity uses `_key` for the line key.)
export function normalizeLyricCuesFromSanity(
  input: SanityLyricCue[] | undefined | null,
): LyricCue[] {
  if (!Array.isArray(input)) return [];
  return normalizeLyricCues(
    input.map((c) => ({
      lineKey: c?._key,
      tMs: c?.tMs,
      text: c?.text,
      endMs: c?.endMs,
      // canonicalGroupKey not present in Sanity lyrics docs
    })),
  );
}

export function parseTrackLyricsApiOk(raw: unknown): TrackLyricsApiOk | null {
  if (!raw || typeof raw !== "object") return null;
  const o = raw as Record<string, unknown>;
  if (o.ok !== true) return null;

  if (typeof o.recordingId !== "string" || !o.recordingId.trim()) return null;
  if (typeof o.offsetMs !== "number" || !Number.isFinite(o.offsetMs))
    return null;

  const cues = normalizeLyricCues(o.cues);

  // groupMap is optional; keep it only if it looks like the right shape
  const groupMap =
    o.groupMap && typeof o.groupMap === "object" && !Array.isArray(o.groupMap)
      ? (o.groupMap as LyricGroupMap)
      : undefined;

  const out: TrackLyricsApiOk = {
    ok: true,
    recordingId: o.recordingId.trim(),
    offsetMs: Math.floor(o.offsetMs),
    cues,
  };

  // Optional fields (keep them if present, but don’t over-validate here)
  if (typeof o.version === "string" && o.version.trim())
    out.version = o.version.trim();
  if (typeof o.geniusUrl === "string" || o.geniusUrl === null)
    out.geniusUrl = o.geniusUrl as string | null;
  if (groupMap) out.groupMap = groupMap;

  // Nullable meta fields (type-safe, no `any`)
  const nullableMetaKeys = [
    "trackTitle",
    "trackArtist",
    "trackCatalogueId",
    "albumTitle",
    "albumSlug",
    "albumCatalogueId",
  ] as const;

  type NullableMetaKey = (typeof nullableMetaKeys)[number];

  for (const k of nullableMetaKeys) {
    const v = o[k];
    if (typeof v === "string") {
      out[k as NullableMetaKey] = v.trim() || null;
    } else if (v === null) {
      out[k as NullableMetaKey] = null;
    }
  }

  return out;
}


===== DASHBOARD OWNERSHIP / DATA FLOW =====

app/admin/playback/page.tsx-1-// web/app/admin/playback/page.tsx
app/admin/playback/page.tsx-2-import "server-only";
app/admin/playback/page.tsx-3-
app/admin/playback/page.tsx:4:import PlaybackTelemetryDashboardClient from "./PlaybackTelemetryDashboardClient";
app/admin/playback/page.tsx-5-import { getPlaybackAdminSnapshot } from "@/lib/playbackAdmin";
app/admin/playback/page.tsx-6-
app/admin/playback/page.tsx-7-export default async function Page(props: {
app/admin/playback/page.tsx-8-  searchParams?: Promise<Record<string, string | string[] | undefined>>;
app/admin/playback/page.tsx-9-}) {
app/admin/playback/page.tsx-10-  const sp = (await props.searchParams) ?? {};
app/admin/playback/page.tsx-11-  const embed = typeof sp.embed === "string" && sp.embed === "1";
app/admin/playback/page.tsx-12-
app/admin/playback/page.tsx-13-  const snapshot = await getPlaybackAdminSnapshot();
app/admin/playback/page.tsx-14-
app/admin/playback/page.tsx-15-  return (
app/admin/playback/page.tsx:16:    <PlaybackTelemetryDashboardClient
app/admin/playback/page.tsx-17-      embed={embed}
app/admin/playback/page.tsx-18-      initialSnapshot={snapshot}
app/admin/playback/page.tsx-19-    />
app/admin/playback/page.tsx-20-  );
app/admin/playback/page.tsx-21-}
--
lib/badgeAdmin.ts-498-          where d.event_type = ${PLAYBACK_TELEMETRY_EVENT_TYPES.PLAY}
lib/badgeAdmin.ts-499-        )::int as play_count,
lib/badgeAdmin.ts-500-        count(*) filter (
lib/badgeAdmin.ts-501-          where d.event_type = ${PLAYBACK_TELEMETRY_EVENT_TYPES.PROGRESS}
lib/badgeAdmin.ts-502-        )::int as listened_ms_steps,
lib/badgeAdmin.ts-503-        count(*) filter (
lib/badgeAdmin.ts-504-          where d.event_type = ${PLAYBACK_TELEMETRY_EVENT_TYPES.COMPLETE}
lib/badgeAdmin.ts-505-        )::int as completed_count
lib/badgeAdmin.ts:506:      from member_playback_telemetry_dedupe d
lib/badgeAdmin.ts-507-      where
lib/badgeAdmin.ts-508-        d.created_at >= ${input.activeOnOrAfter}::timestamptz
lib/badgeAdmin.ts-509-        and (
lib/badgeAdmin.ts-510-          ${activeBefore}::timestamptz is null
lib/badgeAdmin.ts-511-          or d.created_at < ${activeBefore}::timestamptz
lib/badgeAdmin.ts-512-        )
lib/badgeAdmin.ts-513-      group by d.member_id
lib/badgeAdmin.ts-514-    )
--
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-22-  const seconds = totalSeconds % 60;
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-23-
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-24-  if (minutes <= 0) return `${seconds}s`;
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-25-
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-26-  return `${minutes}m ${String(seconds).padStart(2, "0")}s`;
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-27-}
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-28-
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-29-export function ShareLinkActivityTable(props: {
app/admin/playback/dashboard/ShareLinkActivityTable.tsx:30:  rows: PlaybackAdminSnapshot["shareLinkActivity"];
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-31-}) {
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-32-  const { rows } = props;
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-33-
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-34-  return (
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-35-    <TableShell>
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-36-      <table
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-37-        style={{
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-38-          width: "100%",
--
lib/playbackLive.ts-38-export async function getSiteActiveListenerCount(
lib/playbackLive.ts-39-  windowSeconds = DEFAULT_ACTIVE_LISTENER_WINDOW_SECONDS,
lib/playbackLive.ts-40-): Promise<number> {
lib/playbackLive.ts-41-  const safeWindowSeconds = clampActiveListenerWindowSeconds(windowSeconds);
lib/playbackLive.ts-42-
lib/playbackLive.ts-43-  const res = await sql<ActiveListenerCountRow>`
lib/playbackLive.ts-44-    with recent_events as (
lib/playbackLive.ts-45-      select playback_id
lib/playbackLive.ts:46:      from member_playback_telemetry_dedupe
lib/playbackLive.ts-47-      where created_at >= now() - (${safeWindowSeconds} * interval '1 second')
lib/playbackLive.ts-48-
lib/playbackLive.ts-49-      union all
lib/playbackLive.ts-50-
lib/playbackLive.ts-51-      select playback_id
lib/playbackLive.ts:52:      from anonymous_playback_telemetry_dedupe
lib/playbackLive.ts-53-      where created_at >= now() - (${safeWindowSeconds} * interval '1 second')
lib/playbackLive.ts-54-    )
lib/playbackLive.ts-55-    select count(distinct playback_id)::int as active_count
lib/playbackLive.ts-56-    from recent_events
lib/playbackLive.ts-57-  `;
lib/playbackLive.ts-58-
lib/playbackLive.ts-59-  return asSafeInt(res.rows[0]?.active_count);
lib/playbackLive.ts-60-}
--
app/admin/playback/dashboard/AudienceSplitCard.tsx:1:// web/app/admin/playback/dashboard/AudienceSplitCard.tsx
app/admin/playback/dashboard/AudienceSplitCard.tsx-2-"use client";
app/admin/playback/dashboard/AudienceSplitCard.tsx-3-
app/admin/playback/dashboard/AudienceSplitCard.tsx-4-import React from "react";
app/admin/playback/dashboard/AudienceSplitCard.tsx-5-import type { PlaybackAdminSnapshot } from "@/lib/playbackAdmin";
app/admin/playback/dashboard/AudienceSplitCard.tsx-6-import { SectionCard } from "./PlaybackDashboardPrimitives";
app/admin/playback/dashboard/AudienceSplitCard.tsx-7-import {
app/admin/playback/dashboard/AudienceSplitCard.tsx-8-  BG_ANON,
app/admin/playback/dashboard/AudienceSplitCard.tsx-9-  BG_INSET,
--
app/admin/playback/dashboard/AudienceSplitCard.tsx-319-            </text>
app/admin/playback/dashboard/AudienceSplitCard.tsx-320-          </svg>
app/admin/playback/dashboard/AudienceSplitCard.tsx-321-        </div>
app/admin/playback/dashboard/AudienceSplitCard.tsx-322-      </div>
app/admin/playback/dashboard/AudienceSplitCard.tsx-323-    </div>
app/admin/playback/dashboard/AudienceSplitCard.tsx-324-  );
app/admin/playback/dashboard/AudienceSplitCard.tsx-325-}
app/admin/playback/dashboard/AudienceSplitCard.tsx-326-
app/admin/playback/dashboard/AudienceSplitCard.tsx:327:export function AudienceSplitCard(props: { snapshot: PlaybackAdminSnapshot }) {
app/admin/playback/dashboard/AudienceSplitCard.tsx:328:  const allTimeMember = props.snapshot.audienceSplit.allTimeMemberPlayCount;
app/admin/playback/dashboard/AudienceSplitCard.tsx-329-  const allTimeAnonymous =
app/admin/playback/dashboard/AudienceSplitCard.tsx:330:    props.snapshot.audienceSplit.allTimeAnonymousPlayCount;
app/admin/playback/dashboard/AudienceSplitCard.tsx:331:  const recentMember = props.snapshot.audienceSplit.recent30dMemberPlayCount;
app/admin/playback/dashboard/AudienceSplitCard.tsx-332-  const recentAnonymous =
app/admin/playback/dashboard/AudienceSplitCard.tsx:333:    props.snapshot.audienceSplit.recent30dAnonymousPlayCount;
app/admin/playback/dashboard/AudienceSplitCard.tsx-334-
app/admin/playback/dashboard/AudienceSplitCard.tsx-335-  return (
app/admin/playback/dashboard/AudienceSplitCard.tsx-336-    <SectionCard
app/admin/playback/dashboard/AudienceSplitCard.tsx-337-      title="Qualified play audience split"
app/admin/playback/dashboard/AudienceSplitCard.tsx-338-      subtitle=""
app/admin/playback/dashboard/AudienceSplitCard.tsx-339-    >
app/admin/playback/dashboard/AudienceSplitCard.tsx-340-      <div
app/admin/playback/dashboard/AudienceSplitCard.tsx-341-        style={{
--
app/admin/playback/dashboard/TrackTable.tsx:1:// web/app/admin/playback/dashboard/TrackTable.tsx
app/admin/playback/dashboard/TrackTable.tsx-2-"use client";
app/admin/playback/dashboard/TrackTable.tsx-3-
app/admin/playback/dashboard/TrackTable.tsx-4-import React from "react";
app/admin/playback/dashboard/TrackTable.tsx-5-import type { TrackRow } from "./types";
app/admin/playback/dashboard/TrackTable.tsx-6-import {
app/admin/playback/dashboard/TrackTable.tsx-7-  FONT_SIZE_UI,
app/admin/playback/dashboard/TrackTable.tsx-8-  ROW_BORDER,
app/admin/playback/dashboard/TrackTable.tsx-9-  TEXT_MUTED,
--
app/admin/playback/dashboard/TrackTable.tsx-13-import {
app/admin/playback/dashboard/TrackTable.tsx-14-  formatAgo,
app/admin/playback/dashboard/TrackTable.tsx-15-  formatNumber,
app/admin/playback/dashboard/TrackTable.tsx-16-} from "./playbackTelemetryDashboardFormatters";
app/admin/playback/dashboard/TrackTable.tsx-17-
app/admin/playback/dashboard/TrackTable.tsx-18-const POPULARITY_TRACK_BG = "rgba(255,255,255,0.08)";
app/admin/playback/dashboard/TrackTable.tsx-19-const POPULARITY_TRACK_FILL = "rgba(255,255,255,0.78)";
app/admin/playback/dashboard/TrackTable.tsx-20-
app/admin/playback/dashboard/TrackTable.tsx:21:export function TrackTable(props: { rows: TrackRow[]; emptyLabel?: string }) {
app/admin/playback/dashboard/TrackTable.tsx-22-  const topListenedMs = props.rows.reduce(
app/admin/playback/dashboard/TrackTable.tsx-23-    (max, row) => Math.max(max, row.listenedMs),
app/admin/playback/dashboard/TrackTable.tsx-24-    0,
app/admin/playback/dashboard/TrackTable.tsx-25-  );
app/admin/playback/dashboard/TrackTable.tsx-26-
app/admin/playback/dashboard/TrackTable.tsx-27-  return (
app/admin/playback/dashboard/TrackTable.tsx-28-    <div
app/admin/playback/dashboard/TrackTable.tsx-29-      style={{
--
app/admin/playback/dashboard/types.ts-1-// web/app/admin/playback/dashboard/types.ts
app/admin/playback/dashboard/types.ts-2-import type { PlaybackAdminSnapshot } from "@/lib/playbackAdmin";
app/admin/playback/dashboard/types.ts-3-
app/admin/playback/dashboard/types.ts-4-export type TrackRow = PlaybackAdminSnapshot["topTracksByListenedMs"][number];
app/admin/playback/dashboard/types.ts-5-export type DedupeRowBase = PlaybackAdminSnapshot["recentDedupe"][number];
app/admin/playback/dashboard/types.ts:6:export type TrendRangeKey = keyof PlaybackAdminSnapshot["qualifiedPlayTrends"];
app/admin/playback/dashboard/types.ts-7-export type TrendBucket =
app/admin/playback/dashboard/types.ts:8:  PlaybackAdminSnapshot["qualifiedPlayTrends"][TrendRangeKey]["buckets"][number];
app/admin/playback/dashboard/types.ts-9-
app/admin/playback/dashboard/types.ts-10-export type DedupeRow = DedupeRowBase & {
app/admin/playback/dashboard/types.ts-11-  recordingTitle?: string | null;
app/admin/playback/dashboard/types.ts-12-  trackTitle?: string | null;
app/admin/playback/dashboard/types.ts-13-  memberEmail?: string | null;
app/admin/playback/dashboard/types.ts-14-  shareTokenLabel?: string | null;
app/admin/playback/dashboard/types.ts-15-};
app/admin/playback/dashboard/types.ts-16-
--
lib/events.ts-101-
lib/events.ts-102-  const sinceIso = new Date(
lib/events.ts-103-    Date.now() - sinceDays * 24 * 60 * 60 * 1000,
lib/events.ts-104-  ).toISOString();
lib/events.ts-105-
lib/events.ts-106-  const r = await sql<{ n: number }>`
lib/events.ts-107-    with completed_recordings as (
lib/events.ts-108-      select nullif(recording_id, '') as recording_id
lib/events.ts:109:      from anonymous_playback_telemetry_dedupe
lib/events.ts-110-      where anon_id = ${anonId}
lib/events.ts-111-        and event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_COMPLETE}
lib/events.ts-112-        and created_at >= ${sinceIso}::timestamptz
lib/events.ts-113-
lib/events.ts-114-      union
lib/events.ts-115-
lib/events.ts-116-      select coalesce(
lib/events.ts-117-        nullif(payload->>'recording_id', ''),
--
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts:1:// web/app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-2-import type { DedupeRow, DedupeSessionRow } from "./types";
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-3-import { ellipsisMiddle, formatNumber } from "./playbackTelemetryDashboardFormatters";
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-4-
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-5-export function resolveDedupeIdentityLabel(row: {
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-6-  audience: "member" | "anonymous";
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-7-  memberEmail?: string | null;
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-8-  memberId?: string | null;
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-9-}): string {
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:1:// web/app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-2-"use client";
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-3-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-4-import React from "react";
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-5-import type { ChartPoint, TrendBucket, TrendRangeKey } from "./types";
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-6-import {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-7-  BG_INSET,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-8-  FONT_SIZE_UI,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-9-  PANEL_BORDER,
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-61-  label: string;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-62-  fill: string;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-63-  lowerPoints: ChartPoint[];
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-64-  upperPoints: ChartPoint[];
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-65-  areaPath: string;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-66-  linePath: string;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-67-};
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-68-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:69:export function QualifiedPlayTrendChart(props: {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-70-  rows: TrendBucket[];
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-71-  range: TrendRangeKey;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-72-}) {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-73-  const rows = props.rows;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-74-  const width = 920;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-75-  const height = 240;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-76-  const paddingTop = 12;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-77-  const paddingRight = 14;
--
app/api/playback/telemetry/route.ts-69-  memberId: string;
app/api/playback/telemetry/route.ts-70-  playbackId: string;
app/api/playback/telemetry/route.ts-71-  eventType: string;
app/api/playback/telemetry/route.ts-72-  milestoneKey: string;
app/api/playback/telemetry/route.ts-73-}): Promise<boolean> {
app/api/playback/telemetry/route.ts-74-  const { memberId, playbackId, eventType, milestoneKey } = params;
app/api/playback/telemetry/route.ts-75-
app/api/playback/telemetry/route.ts-76-  const res = await sql<{ inserted: boolean }>`
app/api/playback/telemetry/route.ts:77:    insert into member_playback_telemetry_dedupe (
app/api/playback/telemetry/route.ts-78-      member_id,
app/api/playback/telemetry/route.ts-79-      playback_id,
app/api/playback/telemetry/route.ts-80-      event_type,
app/api/playback/telemetry/route.ts-81-      milestone_key
app/api/playback/telemetry/route.ts-82-    )
app/api/playback/telemetry/route.ts-83-    values (
app/api/playback/telemetry/route.ts-84-      ${memberId}::uuid,
app/api/playback/telemetry/route.ts-85-      ${playbackId},
--
app/api/playback/telemetry/route.ts-98-  playbackId: string;
app/api/playback/telemetry/route.ts-99-  recordingId: string;
app/api/playback/telemetry/route.ts-100-  eventType: string;
app/api/playback/telemetry/route.ts-101-  milestoneKey: string;
app/api/playback/telemetry/route.ts-102-}): Promise<boolean> {
app/api/playback/telemetry/route.ts-103-  const { anonId, playbackId, recordingId, eventType, milestoneKey } = params;
app/api/playback/telemetry/route.ts-104-
app/api/playback/telemetry/route.ts-105-  const res = await sql<{ inserted: boolean }>`
app/api/playback/telemetry/route.ts:106:    insert into anonymous_playback_telemetry_dedupe (
app/api/playback/telemetry/route.ts-107-      anon_id,
app/api/playback/telemetry/route.ts-108-      playback_id,
app/api/playback/telemetry/route.ts-109-      recording_id,
app/api/playback/telemetry/route.ts-110-      event_type,
app/api/playback/telemetry/route.ts-111-      milestone_key
app/api/playback/telemetry/route.ts-112-    )
app/api/playback/telemetry/route.ts-113-    values (
app/api/playback/telemetry/route.ts-114-      ${anonId},
--
app/api/playback/telemetry/route.ts-892-    recordingId: telemetry.recordingId,
app/api/playback/telemetry/route.ts-893-    playbackId: telemetry.playbackId,
app/api/playback/telemetry/route.ts-894-    eventType,
app/api/playback/telemetry/route.ts-895-    milestoneKey: telemetry.milestoneKey,
app/api/playback/telemetry/route.ts-896-  });
app/api/playback/telemetry/route.ts-897-
app/api/playback/telemetry/route.ts-898-  if (!inserted) {
app/api/playback/telemetry/route.ts-899-    return telemetryResponseWithAnon(
app/api/playback/telemetry/route.ts:900:      { ok: true, deduped: true },
app/api/playback/telemetry/route.ts-901-      200,
app/api/playback/telemetry/route.ts-902-      correlationId,
app/api/playback/telemetry/route.ts-903-      anonId,
app/api/playback/telemetry/route.ts-904-      isNewAnonId,
app/api/playback/telemetry/route.ts-905-    );
app/api/playback/telemetry/route.ts-906-  }
app/api/playback/telemetry/route.ts-907-
app/api/playback/telemetry/route.ts-908-  const newlyAwardedBadges = await processTelemetryEvent({
--
app/admin/playback/dashboard/DedupeTable.tsx-18-} from "./playbackTelemetryDashboardStyles";
app/admin/playback/dashboard/DedupeTable.tsx-19-import {
app/admin/playback/dashboard/DedupeTable.tsx-20-  ellipsisMiddle,
app/admin/playback/dashboard/DedupeTable.tsx-21-  formatAgo,
app/admin/playback/dashboard/DedupeTable.tsx-22-} from "./playbackTelemetryDashboardFormatters";
app/admin/playback/dashboard/DedupeTable.tsx-23-import {
app/admin/playback/dashboard/DedupeTable.tsx-24-  buildDedupeSessionRows,
app/admin/playback/dashboard/DedupeTable.tsx-25-  resolveDedupeIdentityLabel,
app/admin/playback/dashboard/DedupeTable.tsx:26:} from "./playbackTelemetryDashboardModel";
app/admin/playback/dashboard/DedupeTable.tsx-27-
app/admin/playback/dashboard/DedupeTable.tsx-28-export function DedupeTable(props: {
app/admin/playback/dashboard/DedupeTable.tsx-29-  rows: PlaybackAdminSnapshot["recentDedupe"];
app/admin/playback/dashboard/DedupeTable.tsx-30-}) {
app/admin/playback/dashboard/DedupeTable.tsx-31-  const rawRows = props.rows as DedupeRow[];
app/admin/playback/dashboard/DedupeTable.tsx-32-  const rows = React.useMemo(() => buildDedupeSessionRows(rawRows), [rawRows]);
app/admin/playback/dashboard/DedupeTable.tsx-33-
app/admin/playback/dashboard/DedupeTable.tsx-34-  return (
--
app/admin/playback/PlaybackTelemetryDashboardClient.tsx:1:// web/app/admin/playback/PlaybackTelemetryDashboardClient.tsx
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-2-"use client";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-3-
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-4-import React from "react";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-5-import { useRouter } from "next/navigation";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-6-import type { PlaybackAdminSnapshot } from "@/lib/playbackAdmin";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-7-import AdminPageFrame from "../AdminPageFrame";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx:8:import { AudienceSplitCard } from "./dashboard/AudienceSplitCard";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-9-import { DedupeTable } from "./dashboard/DedupeTable";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-10-import {
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-11-  MetricPill,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-12-  SectionCard,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-13-  TrendRangeToggle,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-14-} from "./dashboard/PlaybackDashboardPrimitives";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx:15:import { QualifiedPlayTrendChart } from "./dashboard/QualifiedPlayTrendChart";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx:16:import { TrackTable } from "./dashboard/TrackTable";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-17-import { ShareLinkActivityTable } from "./dashboard/ShareLinkActivityTable";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-18-import {
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-19-  FONT_SIZE_UI,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-20-  TEXT_MUTED,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-21-  TEXT_PRIMARY,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-22-} from "./dashboard/playbackTelemetryDashboardStyles";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-23-import {
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-24-  formatAgo,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-25-  formatNumber,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-26-  fmtSnapshotStamp,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-27-} from "./dashboard/playbackTelemetryDashboardFormatters";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-28-import type { TrendRangeKey } from "./dashboard/types";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-29-
app/admin/playback/PlaybackTelemetryDashboardClient.tsx:30:export default function PlaybackTelemetryDashboardClient(props: {
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-31-  embed: boolean;
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-32-  initialSnapshot: PlaybackAdminSnapshot;
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-33-}) {
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-34-  const router = useRouter();
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-35-  const [autoRefresh, setAutoRefresh] = React.useState(true);
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-36-  const [refreshing, setRefreshing] = React.useState(false);
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-37-  const [trendRange, setTrendRange] = React.useState<TrendRangeKey>("day");
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-38-
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-39-  const snapshot = props.initialSnapshot;
app/admin/playback/PlaybackTelemetryDashboardClient.tsx:40:  const selectedTrend = snapshot.qualifiedPlayTrends[trendRange];
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-41-  const recentPlayTotal = selectedTrend.buckets.reduce(
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-42-    (sum, row) => sum + row.sitePlayCount,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-43-    0,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-44-  );
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-45-
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-46-  React.useEffect(() => {
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-47-    if (!autoRefresh) return;
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-48-
--
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-105-    </>
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-106-  );
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-107-
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-108-  return (
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-109-    <AdminPageFrame
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-110-      embed={props.embed}
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-111-      maxWidth={1360}
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-112-      title="Playback telemetry"
app/admin/playback/PlaybackTelemetryDashboardClient.tsx:113:      subtitle="Monitor site-wide qualified play trends, audience provenance, top tracks by listened time, and telemetry dedupe behaviour."
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-114-      headerActions={headerActions}
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-115-    >
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-116-      <div
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-117-        style={{
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-118-          display: "grid",
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-119-          gap: 16,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-120-        }}
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-121-      >
--
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-163-
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-164-        <SectionCard
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-165-          title="Qualified plays over time"
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-166-          subtitle=""
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-167-          headerRight={
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-168-            <TrendRangeToggle value={trendRange} onChange={setTrendRange} />
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-169-          }
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-170-        >
app/admin/playback/PlaybackTelemetryDashboardClient.tsx:171:          <QualifiedPlayTrendChart
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-172-            rows={selectedTrend.buckets}
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-173-            range={trendRange}
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-174-          />
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-175-        </SectionCard>
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-176-
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-177-        <div
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-178-          style={{
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-179-            display: "grid",
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-180-            gap: 16,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-181-            gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-182-            alignItems: "stretch",
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-183-          }}
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-184-        >
app/admin/playback/PlaybackTelemetryDashboardClient.tsx:185:          <AudienceSplitCard snapshot={snapshot} />
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-186-
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-187-          <SectionCard title="Top tracks by listened time" subtitle="">
app/admin/playback/PlaybackTelemetryDashboardClient.tsx:188:            <TrackTable rows={snapshot.topTracksByListenedMs} />
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-189-          </SectionCard>
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-190-        </div>
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-191-
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-192-        <SectionCard
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-193-          title="Share-link activity"
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-194-          subtitle="Playback attributed to currently valid labelled share links. This identifies the link used, not a verified individual listener."
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-195-        >
app/admin/playback/PlaybackTelemetryDashboardClient.tsx:196:          <ShareLinkActivityTable rows={snapshot.shareLinkActivity} />
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-197-        </SectionCard>
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-198-
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-199-        <SectionCard title="Recent telemetry sessions" subtitle="">
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-200-          <DedupeTable rows={snapshot.recentDedupe} />
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-201-        </SectionCard>
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-202-      </div>
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-203-    </AdminPageFrame>
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-204-  );
--
lib/playbackAdmin.ts-144-};
lib/playbackAdmin.ts-145-
lib/playbackAdmin.ts-146-export type PlaybackAdminSnapshot = {
lib/playbackAdmin.ts-147-  generatedAt: string;
lib/playbackAdmin.ts-148-  memberTotals: PlaybackAdminAggregate;
lib/playbackAdmin.ts-149-  member30d: PlaybackAdminAggregate;
lib/playbackAdmin.ts-150-  siteTotals: PlaybackAdminAggregate;
lib/playbackAdmin.ts-151-  site30d: PlaybackAdminAggregate;
lib/playbackAdmin.ts:152:  qualifiedPlayTrends: Record<
lib/playbackAdmin.ts-153-    PlaybackAdminTrendRangeKey,
lib/playbackAdmin.ts-154-    PlaybackAdminTrendRange
lib/playbackAdmin.ts-155-  >;
lib/playbackAdmin.ts:156:  audienceSplit: PlaybackAdminAudienceSplit;
lib/playbackAdmin.ts-157-  topTracksByListenedMs: PlaybackAdminTrackRow[];
lib/playbackAdmin.ts-158-  recentTracks: PlaybackAdminTrackRow[];
lib/playbackAdmin.ts-159-  recentDedupe: PlaybackAdminDedupeRow[];
lib/playbackAdmin.ts:160:  shareLinkActivity: PlaybackAdminShareLinkActivityRow[];
lib/playbackAdmin.ts-161-};
lib/playbackAdmin.ts-162-
lib/playbackAdmin.ts-163-async function getMemberTotals(): Promise<PlaybackAdminAggregate> {
lib/playbackAdmin.ts-164-  const res = await sql<AggregateRow>`
lib/playbackAdmin.ts-165-    select
lib/playbackAdmin.ts-166-      coalesce(sum(listened_ms), 0) as listened_ms,
lib/playbackAdmin.ts-167-      coalesce(sum(credited_progress_count), 0) as credited_progress_count,
lib/playbackAdmin.ts-168-      coalesce(sum(play_count), 0) as play_count,
--
lib/playbackAdmin.ts-237-      ) as play_count,
lib/playbackAdmin.ts-238-      coalesce(
lib/playbackAdmin.ts-239-        count(*) filter (
lib/playbackAdmin.ts-240-          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_COMPLETE}
lib/playbackAdmin.ts-241-        ),
lib/playbackAdmin.ts-242-        0
lib/playbackAdmin.ts-243-      ) as completed_count,
lib/playbackAdmin.ts-244-      count(distinct member_id)::int as active_count
lib/playbackAdmin.ts:245:    from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-246-    where created_at >= now() - (${RECENT_WINDOW_DAYS} * interval '1 day')
lib/playbackAdmin.ts-247-  `;
lib/playbackAdmin.ts-248-
lib/playbackAdmin.ts-249-  const row = res.rows[0];
lib/playbackAdmin.ts-250-
lib/playbackAdmin.ts-251-  return {
lib/playbackAdmin.ts-252-    listenedMs: asSafeInt(row?.listened_ms),
lib/playbackAdmin.ts-253-    creditedProgressCount: asSafeInt(row?.credited_progress_count),
--
lib/playbackAdmin.ts-256-    activeCount: row?.active_count ?? 0,
lib/playbackAdmin.ts-257-  };
lib/playbackAdmin.ts-258-}
lib/playbackAdmin.ts-259-
lib/playbackAdmin.ts-260-async function getSite30d(): Promise<PlaybackAdminAggregate> {
lib/playbackAdmin.ts-261-  const res = await sql<AggregateRow>`
lib/playbackAdmin.ts-262-    with recent_events as (
lib/playbackAdmin.ts-263-      select event_type
lib/playbackAdmin.ts:264:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-265-      where created_at >= now() - (${RECENT_WINDOW_DAYS} * interval '1 day')
lib/playbackAdmin.ts-266-
lib/playbackAdmin.ts-267-      union all
lib/playbackAdmin.ts-268-
lib/playbackAdmin.ts-269-      select event_type
lib/playbackAdmin.ts:270:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-271-      where created_at >= now() - (${RECENT_WINDOW_DAYS} * interval '1 day')
lib/playbackAdmin.ts-272-    )
lib/playbackAdmin.ts-273-    select
lib/playbackAdmin.ts-274-      coalesce(
lib/playbackAdmin.ts-275-        count(*) filter (
lib/playbackAdmin.ts-276-          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PROGRESS}
lib/playbackAdmin.ts-277-        ),
lib/playbackAdmin.ts-278-        0
--
lib/playbackAdmin.ts-396-        date_trunc('minute', now()),
lib/playbackAdmin.ts-397-        interval '1 minute'
lib/playbackAdmin.ts-398-      ) as bucket_start
lib/playbackAdmin.ts-399-    ),
lib/playbackAdmin.ts-400-    member_counts as (
lib/playbackAdmin.ts-401-      select
lib/playbackAdmin.ts-402-        date_trunc('minute', created_at) as bucket_start,
lib/playbackAdmin.ts-403-        count(*)::int as n
lib/playbackAdmin.ts:404:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-405-      where
lib/playbackAdmin.ts-406-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-407-        and created_at >= date_trunc('minute', now()) - (59 * interval '1 minute')
lib/playbackAdmin.ts-408-      group by 1
lib/playbackAdmin.ts-409-    ),
lib/playbackAdmin.ts-410-    anonymous_counts as (
lib/playbackAdmin.ts-411-      select
lib/playbackAdmin.ts-412-        date_trunc('minute', created_at) as bucket_start,
lib/playbackAdmin.ts-413-        count(*)::int as n
lib/playbackAdmin.ts:414:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-415-      where
lib/playbackAdmin.ts-416-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-417-        and created_at >= date_trunc('minute', now()) - (59 * interval '1 minute')
lib/playbackAdmin.ts-418-      group by 1
lib/playbackAdmin.ts-419-    )
lib/playbackAdmin.ts-420-    select
lib/playbackAdmin.ts-421-      buckets.bucket_start::text as bucket_start_iso,
lib/playbackAdmin.ts-422-      coalesce(member_counts.n, 0) as member_play_count,
--
lib/playbackAdmin.ts-445-        date_trunc('hour', now()),
lib/playbackAdmin.ts-446-        interval '1 hour'
lib/playbackAdmin.ts-447-      ) as bucket_start
lib/playbackAdmin.ts-448-    ),
lib/playbackAdmin.ts-449-    member_counts as (
lib/playbackAdmin.ts-450-      select
lib/playbackAdmin.ts-451-        date_trunc('hour', created_at) as bucket_start,
lib/playbackAdmin.ts-452-        count(*)::int as n
lib/playbackAdmin.ts:453:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-454-      where
lib/playbackAdmin.ts-455-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-456-        and created_at >= date_trunc('hour', now()) - (23 * interval '1 hour')
lib/playbackAdmin.ts-457-      group by 1
lib/playbackAdmin.ts-458-    ),
lib/playbackAdmin.ts-459-    anonymous_counts as (
lib/playbackAdmin.ts-460-      select
lib/playbackAdmin.ts-461-        date_trunc('hour', created_at) as bucket_start,
lib/playbackAdmin.ts-462-        count(*)::int as n
lib/playbackAdmin.ts:463:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-464-      where
lib/playbackAdmin.ts-465-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-466-        and created_at >= date_trunc('hour', now()) - (23 * interval '1 hour')
lib/playbackAdmin.ts-467-      group by 1
lib/playbackAdmin.ts-468-    )
lib/playbackAdmin.ts-469-    select
lib/playbackAdmin.ts-470-      buckets.bucket_start::text as bucket_start_iso,
lib/playbackAdmin.ts-471-      coalesce(member_counts.n, 0) as member_play_count,
--
lib/playbackAdmin.ts-494-        date_trunc('day', now()),
lib/playbackAdmin.ts-495-        interval '1 day'
lib/playbackAdmin.ts-496-      ) as bucket_start
lib/playbackAdmin.ts-497-    ),
lib/playbackAdmin.ts-498-    member_counts as (
lib/playbackAdmin.ts-499-      select
lib/playbackAdmin.ts-500-        date_trunc('day', created_at) as bucket_start,
lib/playbackAdmin.ts-501-        count(*)::int as n
lib/playbackAdmin.ts:502:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-503-      where
lib/playbackAdmin.ts-504-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-505-        and created_at >= date_trunc('day', now()) - (6 * interval '1 day')
lib/playbackAdmin.ts-506-      group by 1
lib/playbackAdmin.ts-507-    ),
lib/playbackAdmin.ts-508-    anonymous_counts as (
lib/playbackAdmin.ts-509-      select
lib/playbackAdmin.ts-510-        date_trunc('day', created_at) as bucket_start,
lib/playbackAdmin.ts-511-        count(*)::int as n
lib/playbackAdmin.ts:512:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-513-      where
lib/playbackAdmin.ts-514-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-515-        and created_at >= date_trunc('day', now()) - (6 * interval '1 day')
lib/playbackAdmin.ts-516-      group by 1
lib/playbackAdmin.ts-517-    )
lib/playbackAdmin.ts-518-    select
lib/playbackAdmin.ts-519-      buckets.bucket_start::text as bucket_start_iso,
lib/playbackAdmin.ts-520-      coalesce(member_counts.n, 0) as member_play_count,
--
lib/playbackAdmin.ts-543-        date_trunc('day', now()),
lib/playbackAdmin.ts-544-        interval '1 day'
lib/playbackAdmin.ts-545-      ) as bucket_start
lib/playbackAdmin.ts-546-    ),
lib/playbackAdmin.ts-547-    member_counts as (
lib/playbackAdmin.ts-548-      select
lib/playbackAdmin.ts-549-        date_trunc('day', created_at) as bucket_start,
lib/playbackAdmin.ts-550-        count(*)::int as n
lib/playbackAdmin.ts:551:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-552-      where
lib/playbackAdmin.ts-553-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-554-        and created_at >= date_trunc('day', now()) - (29 * interval '1 day')
lib/playbackAdmin.ts-555-      group by 1
lib/playbackAdmin.ts-556-    ),
lib/playbackAdmin.ts-557-    anonymous_counts as (
lib/playbackAdmin.ts-558-      select
lib/playbackAdmin.ts-559-        date_trunc('day', created_at) as bucket_start,
lib/playbackAdmin.ts-560-        count(*)::int as n
lib/playbackAdmin.ts:561:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-562-      where
lib/playbackAdmin.ts-563-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-564-        and created_at >= date_trunc('day', now()) - (29 * interval '1 day')
lib/playbackAdmin.ts-565-      group by 1
lib/playbackAdmin.ts-566-    )
lib/playbackAdmin.ts-567-    select
lib/playbackAdmin.ts-568-      buckets.bucket_start::text as bucket_start_iso,
lib/playbackAdmin.ts-569-      coalesce(member_counts.n, 0) as member_play_count,
--
lib/playbackAdmin.ts-592-        date_trunc('month', now()),
lib/playbackAdmin.ts-593-        interval '1 month'
lib/playbackAdmin.ts-594-      ) as bucket_start
lib/playbackAdmin.ts-595-    ),
lib/playbackAdmin.ts-596-    member_counts as (
lib/playbackAdmin.ts-597-      select
lib/playbackAdmin.ts-598-        date_trunc('month', created_at) as bucket_start,
lib/playbackAdmin.ts-599-        count(*)::int as n
lib/playbackAdmin.ts:600:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-601-      where
lib/playbackAdmin.ts-602-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-603-        and created_at >= date_trunc('month', now()) - (11 * interval '1 month')
lib/playbackAdmin.ts-604-      group by 1
lib/playbackAdmin.ts-605-    ),
lib/playbackAdmin.ts-606-    anonymous_counts as (
lib/playbackAdmin.ts-607-      select
lib/playbackAdmin.ts-608-        date_trunc('month', created_at) as bucket_start,
lib/playbackAdmin.ts-609-        count(*)::int as n
lib/playbackAdmin.ts:610:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-611-      where
lib/playbackAdmin.ts-612-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-613-        and created_at >= date_trunc('month', now()) - (11 * interval '1 month')
lib/playbackAdmin.ts-614-      group by 1
lib/playbackAdmin.ts-615-    )
lib/playbackAdmin.ts-616-    select
lib/playbackAdmin.ts-617-      buckets.bucket_start::text as bucket_start_iso,
lib/playbackAdmin.ts-618-      coalesce(member_counts.n, 0) as member_play_count,
--
lib/playbackAdmin.ts-636-async function getQualifiedPlayTrendAllTime(): Promise<PlaybackAdminTrendRange> {
lib/playbackAdmin.ts-637-  const res = await sql<TrendRow>`
lib/playbackAdmin.ts-638-    with bounds as (
lib/playbackAdmin.ts-639-      select
lib/playbackAdmin.ts-640-        least(
lib/playbackAdmin.ts-641-          coalesce(
lib/playbackAdmin.ts-642-            (
lib/playbackAdmin.ts-643-              select date_trunc('year', min(created_at))
lib/playbackAdmin.ts:644:              from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-645-              where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-646-            ),
lib/playbackAdmin.ts-647-            date_trunc('year', now())
lib/playbackAdmin.ts-648-          ),
lib/playbackAdmin.ts-649-          coalesce(
lib/playbackAdmin.ts-650-            (
lib/playbackAdmin.ts-651-              select date_trunc('year', min(created_at))
lib/playbackAdmin.ts:652:              from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-653-              where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-654-            ),
lib/playbackAdmin.ts-655-            date_trunc('year', now())
lib/playbackAdmin.ts-656-          )
lib/playbackAdmin.ts-657-        ) as first_year_start,
lib/playbackAdmin.ts-658-        date_trunc('year', now()) as current_year_start
lib/playbackAdmin.ts-659-    ),
lib/playbackAdmin.ts-660-    buckets as (
--
lib/playbackAdmin.ts-663-        (select current_year_start from bounds),
lib/playbackAdmin.ts-664-        interval '1 year'
lib/playbackAdmin.ts-665-      ) as bucket_start
lib/playbackAdmin.ts-666-    ),
lib/playbackAdmin.ts-667-    member_counts as (
lib/playbackAdmin.ts-668-      select
lib/playbackAdmin.ts-669-        date_trunc('year', created_at) as bucket_start,
lib/playbackAdmin.ts-670-        count(*)::int as n
lib/playbackAdmin.ts:671:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-672-      where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-673-      group by 1
lib/playbackAdmin.ts-674-    ),
lib/playbackAdmin.ts-675-    anonymous_counts as (
lib/playbackAdmin.ts-676-      select
lib/playbackAdmin.ts-677-        date_trunc('year', created_at) as bucket_start,
lib/playbackAdmin.ts-678-        count(*)::int as n
lib/playbackAdmin.ts:679:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-680-      where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-681-      group by 1
lib/playbackAdmin.ts-682-    )
lib/playbackAdmin.ts-683-    select
lib/playbackAdmin.ts-684-      buckets.bucket_start::text as bucket_start_iso,
lib/playbackAdmin.ts-685-      coalesce(member_counts.n, 0) as member_play_count,
lib/playbackAdmin.ts-686-      coalesce(anonymous_counts.n, 0) as anonymous_play_count,
lib/playbackAdmin.ts-687-      coalesce(member_counts.n, 0) + coalesce(anonymous_counts.n, 0) as site_play_count
--
lib/playbackAdmin.ts-801-        lastActivityAt: new Date(row.last_activity_at).toISOString(),
lib/playbackAdmin.ts-802-      };
lib/playbackAdmin.ts-803-    }),
lib/playbackAdmin.ts-804-  );
lib/playbackAdmin.ts-805-}
lib/playbackAdmin.ts-806-
lib/playbackAdmin.ts-807-async function getRecentDedupe(): Promise<PlaybackAdminDedupeRow[]> {
lib/playbackAdmin.ts-808-  const res = await sql<DedupeRow>`
lib/playbackAdmin.ts:809:        with recent_member_dedupe as (
lib/playbackAdmin.ts-810-      select
lib/playbackAdmin.ts-811-        d.member_id,
lib/playbackAdmin.ts-812-        m.email::text as member_email,
lib/playbackAdmin.ts-813-        d.playback_id,
lib/playbackAdmin.ts-814-        evt.recording_id,
lib/playbackAdmin.ts-815-        d.event_type,
lib/playbackAdmin.ts-816-        d.milestone_key,
lib/playbackAdmin.ts-817-        d.created_at::text as created_at,
lib/playbackAdmin.ts-818-        'member'::text as audience,
lib/playbackAdmin.ts-819-        share_event.telemetry_label as share_token_label
lib/playbackAdmin.ts:820:      from member_playback_telemetry_dedupe d
lib/playbackAdmin.ts-821-      left join members m
lib/playbackAdmin.ts-822-        on m.id = d.member_id
lib/playbackAdmin.ts-823-      left join lateral (
lib/playbackAdmin.ts-824-        select
lib/playbackAdmin.ts-825-          me.payload->>'recording_id' as recording_id
lib/playbackAdmin.ts-826-        from member_events me
lib/playbackAdmin.ts-827-        where
lib/playbackAdmin.ts-828-          me.member_id = d.member_id
--
lib/playbackAdmin.ts-839-          and e.member_id = d.member_id
lib/playbackAdmin.ts-840-          and e.playback_id = d.playback_id
lib/playbackAdmin.ts-841-          and e.event_type = d.event_type
lib/playbackAdmin.ts-842-          and e.milestone_key = d.milestone_key
lib/playbackAdmin.ts-843-        order by e.occurred_at desc
lib/playbackAdmin.ts-844-        limit 1
lib/playbackAdmin.ts-845-      ) share_event on true
lib/playbackAdmin.ts-846-    ),
lib/playbackAdmin.ts:847:        recent_anonymous_dedupe as (
lib/playbackAdmin.ts-848-      select
lib/playbackAdmin.ts-849-        null::uuid as member_id,
lib/playbackAdmin.ts-850-        null::text as member_email,
lib/playbackAdmin.ts-851-        d.playback_id,
lib/playbackAdmin.ts-852-        d.recording_id,
lib/playbackAdmin.ts-853-        d.event_type,
lib/playbackAdmin.ts-854-        d.milestone_key,
lib/playbackAdmin.ts-855-        d.created_at::text as created_at,
lib/playbackAdmin.ts-856-        'anonymous'::text as audience,
lib/playbackAdmin.ts-857-        share_event.telemetry_label as share_token_label
lib/playbackAdmin.ts:858:      from anonymous_playback_telemetry_dedupe d
lib/playbackAdmin.ts-859-      left join lateral (
lib/playbackAdmin.ts-860-        select e.telemetry_label
lib/playbackAdmin.ts-861-        from share_token_playback_events e
lib/playbackAdmin.ts-862-        where
lib/playbackAdmin.ts-863-          e.audience = 'anonymous'
lib/playbackAdmin.ts-864-          and e.playback_id = d.playback_id
lib/playbackAdmin.ts-865-          and e.recording_id = d.recording_id
lib/playbackAdmin.ts-866-          and e.event_type = d.event_type
lib/playbackAdmin.ts-867-          and e.milestone_key = d.milestone_key
lib/playbackAdmin.ts-868-        order by e.occurred_at desc
lib/playbackAdmin.ts-869-        limit 1
lib/playbackAdmin.ts-870-      ) share_event on true
lib/playbackAdmin.ts-871-    ),
lib/playbackAdmin.ts:872:    all_dedupe_rows as (
lib/playbackAdmin.ts:873:      select * from recent_member_dedupe
lib/playbackAdmin.ts-874-      union all
lib/playbackAdmin.ts:875:      select * from recent_anonymous_dedupe
lib/playbackAdmin.ts-876-    ),
lib/playbackAdmin.ts-877-    recent_sessions as (
lib/playbackAdmin.ts-878-      select
lib/playbackAdmin.ts-879-        audience,
lib/playbackAdmin.ts-880-        coalesce(member_id::text, 'anonymous') as identity_key,
lib/playbackAdmin.ts-881-        coalesce(recording_id, '__unknown_recording__') as recording_key,
lib/playbackAdmin.ts-882-        playback_id,
lib/playbackAdmin.ts-883-        max(created_at::timestamptz) as latest_at
lib/playbackAdmin.ts:884:      from all_dedupe_rows
lib/playbackAdmin.ts-885-      group by
lib/playbackAdmin.ts-886-        audience,
lib/playbackAdmin.ts-887-        coalesce(member_id::text, 'anonymous'),
lib/playbackAdmin.ts-888-        coalesce(recording_id, '__unknown_recording__'),
lib/playbackAdmin.ts-889-        playback_id
lib/playbackAdmin.ts-890-      order by latest_at desc
lib/playbackAdmin.ts-891-      limit ${RECENT_DEDUPE_SESSION_LIMIT}
lib/playbackAdmin.ts-892-    )
--
lib/playbackAdmin.ts-895-      t.member_email,
lib/playbackAdmin.ts-896-      t.playback_id,
lib/playbackAdmin.ts-897-      t.recording_id,
lib/playbackAdmin.ts-898-      t.event_type,
lib/playbackAdmin.ts-899-      t.milestone_key,
lib/playbackAdmin.ts-900-      t.created_at,
lib/playbackAdmin.ts-901-      t.audience,
lib/playbackAdmin.ts-902-      t.share_token_label
lib/playbackAdmin.ts:903:    from all_dedupe_rows t
lib/playbackAdmin.ts-904-    inner join recent_sessions s
lib/playbackAdmin.ts-905-      on s.audience = t.audience
lib/playbackAdmin.ts-906-      and s.identity_key = coalesce(t.member_id::text, 'anonymous')
lib/playbackAdmin.ts-907-      and s.recording_key = coalesce(t.recording_id, '__unknown_recording__')
lib/playbackAdmin.ts-908-      and s.playback_id = t.playback_id
lib/playbackAdmin.ts-909-    order by t.created_at desc
lib/playbackAdmin.ts-910-  `;
lib/playbackAdmin.ts-911-
--
lib/playbackAdmin.ts-970-}
lib/playbackAdmin.ts-971-
lib/playbackAdmin.ts-972-export async function getPlaybackAdminSnapshot(): Promise<PlaybackAdminSnapshot> {
lib/playbackAdmin.ts-973-  const [
lib/playbackAdmin.ts-974-    memberTotals,
lib/playbackAdmin.ts-975-    member30d,
lib/playbackAdmin.ts-976-    siteTotals,
lib/playbackAdmin.ts-977-    site30d,
lib/playbackAdmin.ts:978:    qualifiedPlayTrends,
lib/playbackAdmin.ts-979-    topTracksByListenedMs,
lib/playbackAdmin.ts-980-    recentTracks,
lib/playbackAdmin.ts-981-    recentDedupe,
lib/playbackAdmin.ts:982:    shareLinkActivity,
lib/playbackAdmin.ts-983-  ] = await Promise.all([
lib/playbackAdmin.ts-984-    getMemberTotals(),
lib/playbackAdmin.ts-985-    getMember30d(),
lib/playbackAdmin.ts-986-    getSiteTotals(),
lib/playbackAdmin.ts-987-    getSite30d(),
lib/playbackAdmin.ts-988-    getQualifiedPlayTrends(),
lib/playbackAdmin.ts-989-    getTopTracksByListenedMs(),
lib/playbackAdmin.ts-990-    getRecentTracks(),
--
lib/playbackAdmin.ts-993-  ]);
lib/playbackAdmin.ts-994-
lib/playbackAdmin.ts-995-  return {
lib/playbackAdmin.ts-996-    generatedAt: new Date().toISOString(),
lib/playbackAdmin.ts-997-    memberTotals,
lib/playbackAdmin.ts-998-    member30d,
lib/playbackAdmin.ts-999-    siteTotals,
lib/playbackAdmin.ts-1000-    site30d,
lib/playbackAdmin.ts:1001:    qualifiedPlayTrends,
lib/playbackAdmin.ts:1002:    audienceSplit: buildAudienceSplit({
lib/playbackAdmin.ts-1003-      memberTotals,
lib/playbackAdmin.ts-1004-      siteTotals,
lib/playbackAdmin.ts-1005-      member30d,
lib/playbackAdmin.ts-1006-      site30d,
lib/playbackAdmin.ts-1007-    }),
lib/playbackAdmin.ts-1008-    topTracksByListenedMs,
lib/playbackAdmin.ts-1009-    recentTracks,
lib/playbackAdmin.ts-1010-    recentDedupe,
lib/playbackAdmin.ts:1011:    shareLinkActivity,
lib/playbackAdmin.ts-1012-  };
lib/playbackAdmin.ts-1013-}
--
app/api/webhooks/stripe/route.ts-429-      id,
app/api/webhooks/stripe/route.ts-430-      status,
app/api/webhooks/stripe/route.ts-431-      album_slug,
app/api/webhooks/stripe/route.ts-432-      entitlement_key,
app/api/webhooks/stripe/route.ts-433-      recipient_email,
app/api/webhooks/stripe/route.ts-434-      recipient_member_id,
app/api/webhooks/stripe/route.ts-435-      message,
app/api/webhooks/stripe/route.ts-436-      sender_email,
app/api/webhooks/stripe/route.ts:437:      gift_email_dedupe_hash
app/api/webhooks/stripe/route.ts-438-    from gifts
app/api/webhooks/stripe/route.ts-439-    where id = ${resolvedGiftId}::uuid
app/api/webhooks/stripe/route.ts-440-    limit 1
app/api/webhooks/stripe/route.ts-441-  `;
app/api/webhooks/stripe/route.ts-442-  const giftRow = giftRowRes.rows[0] as
app/api/webhooks/stripe/route.ts-443-    | {
app/api/webhooks/stripe/route.ts-444-        id: string;
app/api/webhooks/stripe/route.ts-445-        status: string;
app/api/webhooks/stripe/route.ts-446-        album_slug: string;
app/api/webhooks/stripe/route.ts-447-        entitlement_key: string;
app/api/webhooks/stripe/route.ts-448-        recipient_email: string;
app/api/webhooks/stripe/route.ts-449-        recipient_member_id: string | null;
app/api/webhooks/stripe/route.ts-450-        message: string | null;
app/api/webhooks/stripe/route.ts-451-        sender_email: string | null;
app/api/webhooks/stripe/route.ts:452:        gift_email_dedupe_hash: string | null;
app/api/webhooks/stripe/route.ts-453-      }
app/api/webhooks/stripe/route.ts-454-    | undefined;
app/api/webhooks/stripe/route.ts-455-  if (!giftRow) return;
app/api/webhooks/stripe/route.ts-456-
app/api/webhooks/stripe/route.ts-457-  const recipientEmail =
app/api/webhooks/stripe/route.ts-458-    normalizeEmail((giftRow.recipient_email ?? "").trim()) ||
app/api/webhooks/stripe/route.ts-459-    normalizeEmail((mergedMd.recipientEmail ?? "").trim());
app/api/webhooks/stripe/route.ts-460-
--
app/api/webhooks/stripe/route.ts-511-    grantReason: `gift_paid:${albumSlug || "unknown"}`,
app/api/webhooks/stripe/route.ts-512-    grantSource: "stripe_gift",
app/api/webhooks/stripe/route.ts-513-    grantSourceRef: session.id,
app/api/webhooks/stripe/route.ts-514-    expiresAt: null,
app/api/webhooks/stripe/route.ts-515-    correlationId: session.id,
app/api/webhooks/stripe/route.ts-516-    eventSource: EVENT_SOURCES.STRIPE,
app/api/webhooks/stripe/route.ts-517-  });
app/api/webhooks/stripe/route.ts-518-
app/api/webhooks/stripe/route.ts:519:  // Email dedupe gate: send once
app/api/webhooks/stripe/route.ts:520:  const dedupeCode = crypto.randomBytes(32).toString("base64url");
app/api/webhooks/stripe/route.ts:521:  const dedupeHash = sha256Hex(dedupeCode);
app/api/webhooks/stripe/route.ts-522-
app/api/webhooks/stripe/route.ts-523-  const gate = await sql`
app/api/webhooks/stripe/route.ts-524-    update gifts
app/api/webhooks/stripe/route.ts:525:    set gift_email_dedupe_hash = ${dedupeHash},
app/api/webhooks/stripe/route.ts-526-        gift_email_created_at = coalesce(gift_email_created_at, now())
app/api/webhooks/stripe/route.ts-527-    where id = ${resolvedGiftId}::uuid
app/api/webhooks/stripe/route.ts:528:      and gift_email_dedupe_hash is null
app/api/webhooks/stripe/route.ts-529-    returning id
app/api/webhooks/stripe/route.ts-530-  `;
app/api/webhooks/stripe/route.ts-531-  if (gate.rowCount === 0) return;
app/api/webhooks/stripe/route.ts-532-
app/api/webhooks/stripe/route.ts-533-  // Suppression check
app/api/webhooks/stripe/route.ts-534-  const sup = await sql`
app/api/webhooks/stripe/route.ts-535-    select 1
app/api/webhooks/stripe/route.ts-536-    from email_suppressions
--
app/api/webhooks/stripe/route.ts-607-
app/api/webhooks/stripe/route.ts-608-  const claim = await claimStripeWebhookEvent({
app/api/webhooks/stripe/route.ts-609-    eventId: event.id,
app/api/webhooks/stripe/route.ts-610-    eventType: event.type,
app/api/webhooks/stripe/route.ts-611-    refs,
app/api/webhooks/stripe/route.ts-612-  });
app/api/webhooks/stripe/route.ts-613-
app/api/webhooks/stripe/route.ts-614-  if (claim === "handled") {
app/api/webhooks/stripe/route.ts:615:    return NextResponse.json({ ok: true, deduped: true });
app/api/webhooks/stripe/route.ts-616-  }
app/api/webhooks/stripe/route.ts-617-
app/api/webhooks/stripe/route.ts-618-  if (claim === "processing") {
app/api/webhooks/stripe/route.ts-619-    return NextResponse.json(
app/api/webhooks/stripe/route.ts-620-      { ok: false, error: "Webhook event is already processing" },
app/api/webhooks/stripe/route.ts-621-      { status: 503 },
app/api/webhooks/stripe/route.ts-622-    );
app/api/webhooks/stripe/route.ts-623-  }

===== MODEL COMPLEXITY / GROUPING / SORTING CONTEXT =====

1-// web/app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts
2-import type { DedupeRow, DedupeSessionRow } from "./types";
3-import { ellipsisMiddle, formatNumber } from "./playbackTelemetryDashboardFormatters";
4-
5:export function resolveDedupeIdentityLabel(row: {
6-  audience: "member" | "anonymous";
7:  memberEmail?: string | null;
8:  memberId?: string | null;
9-}): string {
10:  if (row.audience === "anonymous") return "Anonymous";
11:  if (row.memberEmail) return row.memberEmail;
12:  if (row.memberId) return ellipsisMiddle(row.memberId, 8);
13-  return "Member";
14-}
15-
16:export function parseMilestoneMs(milestoneKey: string): number | null {
17-  const normalized = milestoneKey.trim();
18-
19:  if (!/^\d+$/.test(normalized)) return null;
20-
21-  const milestoneMs = Number(normalized);
22:  if (!Number.isFinite(milestoneMs) || milestoneMs < 0) return null;
23-
24-  return Math.floor(milestoneMs);
25-}
26-
27:export function isProgressEvent(eventType: string): boolean {
28-  return eventType.toLowerCase().includes("progress");
29-}
30-
31:export function isPlayEvent(eventType: string): boolean {
32-  const normalized = eventType.toLowerCase();
33-  return normalized.includes("play") && !normalized.includes("progress");
34-}
35-
36:export function isCompleteEvent(eventType: string): boolean {
37-  return eventType.toLowerCase().includes("complete");
38-}
39-
40:export function formatProgressLabel(
41-  progressMs: number,
42-  hasComplete: boolean,
43-): string {
44:  if (hasComplete) return "Complete";
45:  if (progressMs <= 0) return "Started";
46-  const wholeSeconds = Math.floor(progressMs / 1000);
47-  return `${formatNumber(wholeSeconds)}s credited`;
48-}
49-
50:export function buildDedupeSessionRows(rows: DedupeRow[]): DedupeSessionRow[] {
51:  const groups = new Map<string, DedupeRow[]>();
52-
53-  for (const row of rows) {
54-    const identityKey =
55-      row.audience === "anonymous"
56-        ? "anonymous"
57-        : (row.memberEmail ?? row.memberId ?? "member");
58-    const trackKey = row.recordingId ?? row.recordingTitle ?? "unknown-track";
59:    const groupKey = [row.audience, identityKey, trackKey, row.playbackId].join(
60-      "::",
61-    );
62-
63:    const existing = groups.get(groupKey);
64:    if (existing) {
65-      existing.push(row);
66-    } else {
67:      groups.set(groupKey, [row]);
68-    }
69-  }
70-
71:  return Array.from(groups.entries())
72:    .map(([groupKey, sourceRows]) => {
73:      const ordered = [...sourceRows].sort(
74:        (a, b) => Date.parse(a.createdAt) - Date.parse(b.createdAt),
75-      );
76-
77:      const first = ordered[0];
78:      const last = ordered[ordered.length - 1];
79-
80-      let maxProgressMs = 0;
81-      let creditedMilestoneCount = 0;
82-      let hasPlay = false;
83-      let hasComplete = false;
84-
85-      for (const row of ordered) {
86:        if (isPlayEvent(row.eventType)) hasPlay = true;
87:        if (isCompleteEvent(row.eventType)) hasComplete = true;
88-
89:        if (isProgressEvent(row.eventType)) {
90-          creditedMilestoneCount += 1;
91-          const milestoneMs = parseMilestoneMs(row.milestoneKey);
92:          if (milestoneMs != null) {
93-            maxProgressMs = Math.max(maxProgressMs, milestoneMs);
94-          }
95-        }
96-      }
97-
98-      const progressPct = hasComplete
99-        ? 100
100-        : Math.max(0, Math.min(92, maxProgressMs / 1000));
101-
102-      const statusLabel = hasComplete
103-        ? "Completed"
104-        : maxProgressMs > 0
105-          ? "In progress"
106-          : hasPlay
107-            ? "Started"
108-            : "Telemetry";
109-
110-      return {
111:        groupKey,
112:        playbackId: first.playbackId,
113:        audience: first.audience,
114:        memberId: first.memberId ?? null,
115:        memberEmail: first.memberEmail ?? null,
116:        recordingId: first.recordingId ?? null,
117:        recordingTitle: first.recordingTitle ?? null,
118:        createdAt: first.createdAt,
119:        latestAt: last.createdAt,
120:        progressMs: hasComplete ? 100_000 : maxProgressMs,
121-        progressPct,
122-        creditedMilestoneCount,
123-        hasPlay,
124-        hasComplete,
125-        statusLabel,
126-        sourceRows: ordered,
127-      };
128-    })
129:    .sort((a, b) => Date.parse(b.latestAt) - Date.parse(a.latestAt));
130-}

===== DASHBOARD JSX / ACCESSIBILITY / LIST KEYS =====

app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-64-    if (existing) {
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-65-      existing.push(row);
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-66-    } else {
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-67-      groups.set(groupKey, [row]);
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-68-    }
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-69-  }
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-70-
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-71-  return Array.from(groups.entries())
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts:72:    .map(([groupKey, sourceRows]) => {
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-73-      const ordered = [...sourceRows].sort(
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-74-        (a, b) => Date.parse(a.createdAt) - Date.parse(b.createdAt),
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-75-      );
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-76-
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-77-      const first = ordered[0];
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts:78:      const last = ordered[ordered.length - 1];
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-79-
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-80-      let maxProgressMs = 0;
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-81-      let creditedMilestoneCount = 0;
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-82-      let hasPlay = false;
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-83-      let hasComplete = false;
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-84-
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-85-      for (const row of ordered) {
app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts-86-        if (isPlayEvent(row.eventType)) hasPlay = true;
--
app/admin/playback/dashboard/DedupeTable.tsx-44-          <tr>
app/admin/playback/dashboard/DedupeTable.tsx-45-            {[
app/admin/playback/dashboard/DedupeTable.tsx-46-              "When",
app/admin/playback/dashboard/DedupeTable.tsx-47-              "Track / Session",
app/admin/playback/dashboard/DedupeTable.tsx-48-              "Progress",
app/admin/playback/dashboard/DedupeTable.tsx-49-              "Status",
app/admin/playback/dashboard/DedupeTable.tsx-50-              "Share link",
app/admin/playback/dashboard/DedupeTable.tsx-51-              "Listener",
app/admin/playback/dashboard/DedupeTable.tsx:52:            ].map((label) => (
app/admin/playback/dashboard/DedupeTable.tsx-53-              <th
app/admin/playback/dashboard/DedupeTable.tsx:54:                key={label}
app/admin/playback/dashboard/DedupeTable.tsx-55-                style={{
app/admin/playback/dashboard/DedupeTable.tsx-56-                  textAlign: "left",
app/admin/playback/dashboard/DedupeTable.tsx-57-                  padding: "8px 10px",
app/admin/playback/dashboard/DedupeTable.tsx-58-                  fontSize: FONT_SIZE_DEDUPE,
app/admin/playback/dashboard/DedupeTable.tsx-59-                  fontWeight: 700,
app/admin/playback/dashboard/DedupeTable.tsx-60-                  color: TEXT_FAINT,
app/admin/playback/dashboard/DedupeTable.tsx-61-                  borderBottom: ROW_BORDER,
app/admin/playback/dashboard/DedupeTable.tsx-62-                  whiteSpace: "nowrap",
--
app/admin/playback/dashboard/DedupeTable.tsx-79-                  color: TEXT_MUTED,
app/admin/playback/dashboard/DedupeTable.tsx-80-                }}
app/admin/playback/dashboard/DedupeTable.tsx-81-              >
app/admin/playback/dashboard/DedupeTable.tsx-82-                No rows.
app/admin/playback/dashboard/DedupeTable.tsx-83-              </td>
app/admin/playback/dashboard/DedupeTable.tsx-84-            </tr>
app/admin/playback/dashboard/DedupeTable.tsx-85-          ) : null}
app/admin/playback/dashboard/DedupeTable.tsx-86-
app/admin/playback/dashboard/DedupeTable.tsx:87:          {rows.map((row) => {
app/admin/playback/dashboard/DedupeTable.tsx-88-            const identityLabel = resolveDedupeIdentityLabel({
app/admin/playback/dashboard/DedupeTable.tsx-89-              audience: row.audience,
app/admin/playback/dashboard/DedupeTable.tsx-90-              memberEmail: row.memberEmail,
app/admin/playback/dashboard/DedupeTable.tsx-91-              memberId: row.memberId,
app/admin/playback/dashboard/DedupeTable.tsx-92-            });
app/admin/playback/dashboard/DedupeTable.tsx-93-
app/admin/playback/dashboard/DedupeTable.tsx-94-            const monospaceIdentity =
app/admin/playback/dashboard/DedupeTable.tsx-95-              row.memberEmail == null && row.memberId != null;
--
app/admin/playback/dashboard/DedupeTable.tsx-97-            const shareTokenLabel =
app/admin/playback/dashboard/DedupeTable.tsx-98-              row.sourceRows.find(
app/admin/playback/dashboard/DedupeTable.tsx-99-                (sourceRow) =>
app/admin/playback/dashboard/DedupeTable.tsx-100-                  typeof sourceRow.shareTokenLabel === "string" &&
app/admin/playback/dashboard/DedupeTable.tsx-101-                  sourceRow.shareTokenLabel.trim().length > 0,
app/admin/playback/dashboard/DedupeTable.tsx-102-              )?.shareTokenLabel ?? null;
app/admin/playback/dashboard/DedupeTable.tsx-103-
app/admin/playback/dashboard/DedupeTable.tsx-104-            return (
app/admin/playback/dashboard/DedupeTable.tsx:105:              <tr key={row.groupKey}>
app/admin/playback/dashboard/DedupeTable.tsx-106-                <td
app/admin/playback/dashboard/DedupeTable.tsx-107-                  style={{
app/admin/playback/dashboard/DedupeTable.tsx-108-                    padding: "10px 10px",
app/admin/playback/dashboard/DedupeTable.tsx-109-                    borderBottom: ROW_BORDER,
app/admin/playback/dashboard/DedupeTable.tsx-110-                    color: TEXT_MUTED,
app/admin/playback/dashboard/DedupeTable.tsx-111-                    fontSize: FONT_SIZE_DEDUPE,
app/admin/playback/dashboard/DedupeTable.tsx-112-                    whiteSpace: "nowrap",
app/admin/playback/dashboard/DedupeTable.tsx-113-                    verticalAlign: "top",
--
app/admin/playback/dashboard/TrackTable.tsx-38-          width: "100%",
app/admin/playback/dashboard/TrackTable.tsx-39-          borderCollapse: "collapse",
app/admin/playback/dashboard/TrackTable.tsx-40-          minWidth: 560,
app/admin/playback/dashboard/TrackTable.tsx-41-          tableLayout: "auto",
app/admin/playback/dashboard/TrackTable.tsx-42-        }}
app/admin/playback/dashboard/TrackTable.tsx-43-      >
app/admin/playback/dashboard/TrackTable.tsx-44-        <thead>
app/admin/playback/dashboard/TrackTable.tsx-45-          <tr>
app/admin/playback/dashboard/TrackTable.tsx:46:            {["Track", "Popularity", "Plays", "Completes", "Last heard"].map(
app/admin/playback/dashboard/TrackTable.tsx-47-              (label) => (
app/admin/playback/dashboard/TrackTable.tsx-48-                <th
app/admin/playback/dashboard/TrackTable.tsx:49:                  key={label}
app/admin/playback/dashboard/TrackTable.tsx-50-                  style={{
app/admin/playback/dashboard/TrackTable.tsx-51-                    textAlign: "left",
app/admin/playback/dashboard/TrackTable.tsx-52-                    padding: "6px 12px 10px",
app/admin/playback/dashboard/TrackTable.tsx-53-                    fontSize: FONT_SIZE_UI,
app/admin/playback/dashboard/TrackTable.tsx-54-                    fontWeight: 700,
app/admin/playback/dashboard/TrackTable.tsx-55-                    color: TEXT_MUTED,
app/admin/playback/dashboard/TrackTable.tsx-56-                    borderBottom: ROW_BORDER,
app/admin/playback/dashboard/TrackTable.tsx-57-                    whiteSpace: "nowrap",
--
app/admin/playback/dashboard/TrackTable.tsx-81-                  color: TEXT_MUTED,
app/admin/playback/dashboard/TrackTable.tsx-82-                }}
app/admin/playback/dashboard/TrackTable.tsx-83-              >
app/admin/playback/dashboard/TrackTable.tsx-84-                {props.emptyLabel ?? "No rows."}
app/admin/playback/dashboard/TrackTable.tsx-85-              </td>
app/admin/playback/dashboard/TrackTable.tsx-86-            </tr>
app/admin/playback/dashboard/TrackTable.tsx-87-          ) : null}
app/admin/playback/dashboard/TrackTable.tsx-88-
app/admin/playback/dashboard/TrackTable.tsx:89:          {props.rows.map((row) => {
app/admin/playback/dashboard/TrackTable.tsx-90-            const relativeWidthPct =
app/admin/playback/dashboard/TrackTable.tsx-91-              topListenedMs > 0 ? (row.listenedMs / topListenedMs) * 100 : 0;
app/admin/playback/dashboard/TrackTable.tsx-92-
app/admin/playback/dashboard/TrackTable.tsx-93-            return (
app/admin/playback/dashboard/TrackTable.tsx:94:              <tr key={row.recordingId}>
app/admin/playback/dashboard/TrackTable.tsx-95-                <td
app/admin/playback/dashboard/TrackTable.tsx-96-                  style={{
app/admin/playback/dashboard/TrackTable.tsx-97-                    padding: "12px 12px",
app/admin/playback/dashboard/TrackTable.tsx-98-                    borderBottom: ROW_BORDER,
app/admin/playback/dashboard/TrackTable.tsx-99-                    color: TEXT_PRIMARY,
app/admin/playback/dashboard/TrackTable.tsx-100-                    fontSize: FONT_SIZE_UI,
app/admin/playback/dashboard/TrackTable.tsx-101-                    fontWeight: 700,
app/admin/playback/dashboard/TrackTable.tsx-102-                    verticalAlign: "top",
--
app/admin/playback/dashboard/AudienceSplitCard.tsx-107-      color: BG_ANON,
app/admin/playback/dashboard/AudienceSplitCard.tsx-108-    },
app/admin/playback/dashboard/AudienceSplitCard.tsx-109-  ];
app/admin/playback/dashboard/AudienceSplitCard.tsx-110-
app/admin/playback/dashboard/AudienceSplitCard.tsx-111-  let currentAngle = 0;
app/admin/playback/dashboard/AudienceSplitCard.tsx-112-
app/admin/playback/dashboard/AudienceSplitCard.tsx-113-  return slices
app/admin/playback/dashboard/AudienceSplitCard.tsx-114-    .filter((slice) => slice.value > 0 && total > 0)
app/admin/playback/dashboard/AudienceSplitCard.tsx:115:    .map((slice) => {
app/admin/playback/dashboard/AudienceSplitCard.tsx-116-      const sweep = (slice.value / total) * 360;
app/admin/playback/dashboard/AudienceSplitCard.tsx-117-      const startAngle = currentAngle;
app/admin/playback/dashboard/AudienceSplitCard.tsx-118-      const endAngle = currentAngle + sweep;
app/admin/playback/dashboard/AudienceSplitCard.tsx-119-      currentAngle = endAngle;
app/admin/playback/dashboard/AudienceSplitCard.tsx-120-
app/admin/playback/dashboard/AudienceSplitCard.tsx-121-      const { path } = describeDonutArc(
app/admin/playback/dashboard/AudienceSplitCard.tsx-122-        cx,
app/admin/playback/dashboard/AudienceSplitCard.tsx-123-        cy,
--
app/admin/playback/dashboard/AudienceSplitCard.tsx-198-        <div
app/admin/playback/dashboard/AudienceSplitCard.tsx-199-          style={{
app/admin/playback/dashboard/AudienceSplitCard.tsx-200-            position: "relative",
app/admin/playback/dashboard/AudienceSplitCard.tsx-201-            width: "100%",
app/admin/playback/dashboard/AudienceSplitCard.tsx-202-            maxWidth: size,
app/admin/playback/dashboard/AudienceSplitCard.tsx-203-            aspectRatio: "1 / 1",
app/admin/playback/dashboard/AudienceSplitCard.tsx-204-          }}
app/admin/playback/dashboard/AudienceSplitCard.tsx-205-        >
app/admin/playback/dashboard/AudienceSplitCard.tsx:206:          <svg
app/admin/playback/dashboard/AudienceSplitCard.tsx-207-            viewBox={`0 0 ${size} ${size}`}
app/admin/playback/dashboard/AudienceSplitCard.tsx-208-            style={{
app/admin/playback/dashboard/AudienceSplitCard.tsx-209-              display: "block",
app/admin/playback/dashboard/AudienceSplitCard.tsx-210-              width: "100%",
app/admin/playback/dashboard/AudienceSplitCard.tsx-211-              height: "auto",
app/admin/playback/dashboard/AudienceSplitCard.tsx-212-              overflow: "visible",
app/admin/playback/dashboard/AudienceSplitCard.tsx-213-            }}
app/admin/playback/dashboard/AudienceSplitCard.tsx:214:            aria-label={`${label} audience split`}
app/admin/playback/dashboard/AudienceSplitCard.tsx:215:            role="img"
app/admin/playback/dashboard/AudienceSplitCard.tsx-216-          >
app/admin/playback/dashboard/AudienceSplitCard.tsx:217:            {segments.map((segment) => {
app/admin/playback/dashboard/AudienceSplitCard.tsx-218-              const isHovered = hovered === segment.key;
app/admin/playback/dashboard/AudienceSplitCard.tsx-219-              const shouldShowPct =
app/admin/playback/dashboard/AudienceSplitCard.tsx-220-                segment.key === "member"
app/admin/playback/dashboard/AudienceSplitCard.tsx-221-                  ? memberLabelVisible
app/admin/playback/dashboard/AudienceSplitCard.tsx-222-                  : anonymousLabelVisible;
app/admin/playback/dashboard/AudienceSplitCard.tsx-223-
app/admin/playback/dashboard/AudienceSplitCard.tsx-224-              return (
app/admin/playback/dashboard/AudienceSplitCard.tsx:225:                <g key={segment.key}>
app/admin/playback/dashboard/AudienceSplitCard.tsx-226-                  <path
app/admin/playback/dashboard/AudienceSplitCard.tsx-227-                    d={segment.path}
app/admin/playback/dashboard/AudienceSplitCard.tsx-228-                    fill={segment.color}
app/admin/playback/dashboard/AudienceSplitCard.tsx-229-                    opacity={hovered === null ? 1 : isHovered ? 1 : 0.42}
app/admin/playback/dashboard/AudienceSplitCard.tsx-230-                    style={{
app/admin/playback/dashboard/AudienceSplitCard.tsx-231-                      cursor: "default",
app/admin/playback/dashboard/AudienceSplitCard.tsx-232-                      transition: "opacity 160ms ease, filter 160ms ease",
app/admin/playback/dashboard/AudienceSplitCard.tsx-233-                      filter: isHovered ? "brightness(1.05)" : "none",
--
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-44-          <tr>
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-45-            {[
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-46-              "Share link",
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-47-              "Qualified plays",
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-48-              "Listened time",
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-49-              "Completions",
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-50-              "Top track",
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-51-              "Last activity",
app/admin/playback/dashboard/ShareLinkActivityTable.tsx:52:            ].map((label) => (
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-53-              <th
app/admin/playback/dashboard/ShareLinkActivityTable.tsx:54:                key={label}
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-55-                style={{
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-56-                  textAlign: "left",
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-57-                  padding: "8px 10px",
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-58-                  fontSize: FONT_SIZE_DEDUPE,
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-59-                  fontWeight: 700,
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-60-                  color: TEXT_FAINT,
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-61-                  borderBottom: ROW_BORDER,
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-62-                  whiteSpace: "nowrap",
--
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-79-                  color: TEXT_MUTED,
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-80-                }}
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-81-              >
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-82-                No labelled share-link playback yet.
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-83-              </td>
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-84-            </tr>
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-85-          ) : null}
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-86-
app/admin/playback/dashboard/ShareLinkActivityTable.tsx:87:          {rows.map((row) => (
app/admin/playback/dashboard/ShareLinkActivityTable.tsx:88:            <tr key={row.telemetryLabel}>
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-89-              <td
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-90-                style={{
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-91-                  padding: "10px 10px",
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-92-                  borderBottom: ROW_BORDER,
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-93-                  color: TEXT_PRIMARY,
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-94-                  fontSize: FONT_SIZE_DEDUPE,
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-95-                  fontWeight: 700,
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-96-                  maxWidth: 300,
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-14-import {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-15-  fmtTrendTick,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-16-  formatNumber,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-17-} from "./playbackTelemetryDashboardFormatters";
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-18-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-19-function buildLinearCommands(points: ChartPoint[]): string {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-20-  return points
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-21-    .slice(1)
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:22:    .map((point) => ` L ${point.x} ${point.y}`)
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-23-    .join("");
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-24-}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-25-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-26-function buildLinearLinePath(points: ChartPoint[]): string {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-27-  if (points.length === 0) return "";
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-28-  return `M ${points[0].x} ${points[0].y}${buildLinearCommands(points)}`;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-29-}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-30-
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-82-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-83-  const [activeIndex, setActiveIndex] = React.useState<number | null>(null);
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-84-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-85-  const series: LayerDescriptor[] = [
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-86-    {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-87-      key: "anonymous",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-88-      label: "Anonymous plays",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-89-      fill: "rgba(255,255,255,0.18)",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:90:      values: rows.map((row) => row.anonymousPlayCount),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-91-    },
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-92-    {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-93-      key: "member",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-94-      label: "Member plays",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-95-      fill: "rgba(255,255,255,0.56)",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:96:      values: rows.map((row) => row.memberPlayCount),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-97-    },
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-98-  ];
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-99-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:100:  const maxTotal = Math.max(1, ...rows.map((row) => row.sitePlayCount));
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-101-  const yTickCount = 4;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-102-  const yTicks = Array.from({ length: yTickCount + 1 }, (_, index) => {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-103-    const ratio = index / yTickCount;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-104-    const value = Math.round(maxTotal * (1 - ratio));
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-105-    const y = paddingTop + innerHeight * ratio;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-106-    return { value, y };
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-107-  });
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-108-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-109-  const xForIndex = (index: number): number => {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-110-    if (rows.length <= 1) return paddingLeft + innerWidth / 2;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:111:    return paddingLeft + (index / (rows.length - 1)) * innerWidth;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-112-  };
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-113-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-114-  const yForValue = (value: number): number =>
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-115-    paddingTop + innerHeight - (value / maxTotal) * innerHeight;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-116-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:117:  const stackedLayers: StackedLayer[] = series.map((layer, layerIndex) => {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:118:    const lowerValues = rows.map((_, rowIndex) =>
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-119-      series
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-120-        .slice(0, layerIndex)
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-121-        .reduce((sum, candidate) => sum + candidate.values[rowIndex], 0),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-122-    );
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-123-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:124:    const upperValues = lowerValues.map(
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-125-      (lower, rowIndex) => lower + layer.values[rowIndex],
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-126-    );
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-127-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:128:    const lowerPoints = lowerValues.map((value, rowIndex) => ({
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-129-      x: xForIndex(rowIndex),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-130-      y: yForValue(value),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-131-    }));
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-132-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:133:    const upperPoints = upperValues.map((value, rowIndex) => ({
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-134-      x: xForIndex(rowIndex),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-135-      y: yForValue(value),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-136-    }));
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-137-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-138-    return {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-139-      key: layer.key,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-140-      label: layer.label,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-141-      fill: layer.fill,
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-144-      areaPath: buildLinearAreaPath(upperPoints, lowerPoints),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-145-      linePath: buildLinearLinePath(upperPoints),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-146-    };
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-147-  });
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-148-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-149-  const tickIndexes = Array.from(
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-150-    new Set([
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-151-      0,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:152:      Math.floor((rows.length - 1) * 0.33),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:153:      Math.floor((rows.length - 1) * 0.66),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:154:      rows.length - 1,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-155-    ]),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-156-  ).filter((index) => index >= 0 && index < rows.length);
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-157-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:158:  const hoverZones: HoverZone[] = rows.map((_, index) => {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-159-    const x = xForIndex(index);
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-160-    const prevX = index > 0 ? xForIndex(index - 1) : x;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:161:    const nextX = index < rows.length - 1 ? xForIndex(index + 1) : x;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-162-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-163-    const left = index === 0 ? paddingLeft : x - (x - prevX) / 2;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-164-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-165-    const right =
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:166:      index === rows.length - 1 ? width - paddingRight : x + (nextX - x) / 2;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-167-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-168-    return {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-169-      index,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-170-      x,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-171-      left,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-172-      width: Math.max(1, right - left),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-173-    };
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-174-  });
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-188-      ? []
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-189-      : [
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-190-          {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-191-            key: "total",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-192-            label: "Total plays",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-193-            value: activeRow.sitePlayCount,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-194-            swatch: "rgba(255,255,255,0.9)",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-195-          },
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:196:          ...series.map((layer) => ({
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-197-            key: layer.key,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-198-            label: layer.label,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-199-            value: layer.values[activeIndex ?? 0] ?? 0,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-200-            swatch: layer.fill,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-201-          })),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-202-        ];
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-203-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-204-  return (
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-210-    >
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-211-      <div
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-212-        style={{
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-213-          position: "relative",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-214-          background: BG_INSET,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-215-          padding: "10px 12px 6px",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-216-        }}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-217-      >
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:218:        <svg
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-219-          viewBox={`0 0 ${width} ${height}`}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-220-          style={{ width: "100%", height: "auto", display: "block" }}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:221:          role="img"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:222:          aria-label={`Qualified plays over the selected ${props.range} range`}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-223-        >
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:224:          {yTicks.map((tick) => (
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:225:            <g key={`${tick.value}:${tick.y}`}>
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-226-              <line
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-227-                x1={paddingLeft}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-228-                y1={tick.y}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-229-                x2={width - paddingRight}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-230-                y2={tick.y}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-231-                stroke="rgba(255,255,255,0.08)"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-232-                strokeWidth="1"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-233-              />
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-248-              x={activeZone.left}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-249-              y={paddingTop}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-250-              width={activeZone.width}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-251-              height={innerHeight}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-252-              fill="rgba(255,255,255,0.035)"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-253-            />
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-254-          ) : null}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-255-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:256:          {stackedLayers.map((layer) => (
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-257-            <path
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:258:              key={`${layer.key}:fill`}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-259-              d={layer.areaPath}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-260-              fill={layer.fill}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-261-              fillOpacity={activeIndex === null ? 0.72 : 0.56}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-262-              stroke="none"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-263-            />
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-264-          ))}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-265-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:266:          {stackedLayers.map((layer, layerIndex) => {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:267:            const isTopLayer = layerIndex === stackedLayers.length - 1;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-268-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-269-            return (
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-270-              <path
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:271:                key={`${layer.key}:line`}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-272-                d={layer.linePath}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-273-                fill="none"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-274-                stroke={
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-275-                  isTopLayer
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-276-                    ? "rgba(255,255,255,0.88)"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-277-                    : "rgba(255,255,255,0.38)"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-278-                }
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-279-                strokeWidth={isTopLayer ? "1.6" : "1"}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-280-                strokeLinejoin="round"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-281-                strokeLinecap="round"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-282-                opacity={activeIndex === null ? 1 : 0.9}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-283-              />
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-284-            );
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-285-          })}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-286-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:287:          {stackedLayers[stackedLayers.length - 1]?.upperPoints.map(
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-288-            (point, index) => (
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-289-              <circle
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:290:                key={`point:${index}`}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-291-                cx={point.x}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-292-                cy={point.y}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-293-                r="2"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-294-                fill="rgba(255,255,255,0.72)"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-295-                opacity={
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-296-                  activeIndex === null || activeIndex === index ? 1 : 0.32
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-297-                }
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-298-              />
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-307-              y2={paddingTop + innerHeight}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-308-              stroke="rgba(255,255,255,0.22)"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-309-              strokeWidth="1"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-310-              strokeDasharray="3 4"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-311-            />
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-312-          ) : null}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-313-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-314-          {activeIndex !== null
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:315:            ? stackedLayers.map((layer) => {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-316-                const point = layer.upperPoints[activeIndex];
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-317-                if (!point) return null;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-318-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-319-                return (
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:320:                  <g key={`${layer.key}:marker`}>
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-321-                    <circle
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-322-                      cx={point.x}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-323-                      cy={point.y}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-324-                      r="4"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-325-                      fill="rgba(10,10,14,0.96)"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-326-                    />
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-327-                    <circle
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-328-                      cx={point.x}
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-330-                      r="2.5"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-331-                      fill="rgba(255,255,255,0.92)"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-332-                    />
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-333-                  </g>
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-334-                );
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-335-              })
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-336-            : null}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-337-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:338:          {tickIndexes.map((index) => (
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-339-            <text
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:340:              key={rows[index]?.bucketStart ?? index}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-341-              x={xForIndex(index)}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-342-              y={height - 8}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-343-              textAnchor="middle"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-344-              fill={TEXT_FAINT}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-345-              fontSize="8"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-346-            >
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-347-              {rows[index]
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-348-                ? fmtTrendTick(rows[index].bucketStart, props.range)
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-349-                : ""}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-350-            </text>
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-351-          ))}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-352-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:353:          {hoverZones.map((zone) => {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-354-            const row = rows[zone.index];
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-355-            const label = row
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-356-              ? fmtTrendTick(row.bucketStart, props.range)
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-357-              : "Trend bucket";
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-358-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-359-            return (
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-360-              <rect
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:361:                key={`hover:${zone.index}`}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-362-                x={zone.left}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-363-                y={paddingTop}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-364-                width={zone.width}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-365-                height={innerHeight}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-366-                fill="transparent"
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-367-                tabIndex={0}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:368:                aria-label={`${label}. ${row ? formatNumber(row.sitePlayCount) : 0} total plays.`}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-369-                onMouseEnter={() => setActiveIndex(zone.index)}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-370-                onMouseLeave={() => setActiveIndex(null)}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-371-                onFocus={() => setActiveIndex(zone.index)}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-372-                onBlur={() => setActiveIndex(null)}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-373-              />
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-374-            );
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-375-          })}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-376-        </svg>
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-411-            </div>
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-412-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-413-            <div
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-414-              style={{
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-415-                display: "grid",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-416-                gap: 6,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-417-              }}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-418-            >
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:419:              {tooltipItems.map((item) => (
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-420-                <div
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:421:                  key={item.key}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-422-                  style={{
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-423-                    display: "grid",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-424-                    gridTemplateColumns: "auto 1fr auto",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-425-                    alignItems: "center",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-426-                    gap: 8,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-427-                    fontSize: FONT_SIZE_UI,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-428-                    color: TEXT_MUTED,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-429-                  }}
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-463-        }}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-464-      >
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-465-        {[
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-466-          {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-467-            key: "total",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-468-            label: "Total plays",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-469-            fill: "rgba(255,255,255,0.9)",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-470-          },
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:471:          ...stackedLayers.map((layer) => ({
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-472-            key: layer.key,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-473-            label: layer.label,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-474-            fill: layer.fill,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-475-          })),
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:476:        ].map((layer) => (
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-477-          <div
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:478:            key={layer.key}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-479-            style={{
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-480-              display: "inline-flex",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-481-              alignItems: "center",
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-482-              gap: 6,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-483-              color: TEXT_MUTED,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-484-              fontSize: FONT_SIZE_UI,
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-485-            }}
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-486-          >
--
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-170-  return (
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-171-    <div
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-172-      style={{
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-173-        display: "inline-flex",
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-174-        gap: 6,
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-175-        flexWrap: "wrap",
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-176-      }}
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-177-    >
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx:178:      {options.map((option) => {
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-179-        const selected = props.value === option.key;
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-180-
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-181-        return (
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-182-          <button
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx:183:            key={option.key}
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-184-            type="button"
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-185-            onClick={() => props.onChange(option.key)}
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-186-            style={{
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-187-              height: 30,
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-188-              padding: "0 12px",
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-189-              borderRadius: 999,
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-190-              border: "1px solid rgba(255,255,255,0.14)",
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-191-              background: selected

===== DASHBOARD PROPS CONTRACTS =====

app/admin/playback/page.tsx-2-import "server-only";
app/admin/playback/page.tsx-3-
app/admin/playback/page.tsx-4-import PlaybackTelemetryDashboardClient from "./PlaybackTelemetryDashboardClient";
app/admin/playback/page.tsx-5-import { getPlaybackAdminSnapshot } from "@/lib/playbackAdmin";
app/admin/playback/page.tsx-6-
app/admin/playback/page.tsx:7:export default async function Page(props: {
app/admin/playback/page.tsx-8-  searchParams?: Promise<Record<string, string | string[] | undefined>>;
app/admin/playback/page.tsx-9-}) {
app/admin/playback/page.tsx-10-  const sp = (await props.searchParams) ?? {};
app/admin/playback/page.tsx-11-  const embed = typeof sp.embed === "string" && sp.embed === "1";
app/admin/playback/page.tsx-12-
--
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-24-  if (minutes <= 0) return `${seconds}s`;
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-25-
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-26-  return `${minutes}m ${String(seconds).padStart(2, "0")}s`;
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-27-}
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-28-
app/admin/playback/dashboard/ShareLinkActivityTable.tsx:29:export function ShareLinkActivityTable(props: {
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-30-  rows: PlaybackAdminSnapshot["shareLinkActivity"];
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-31-}) {
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-32-  const { rows } = props;
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-33-
app/admin/playback/dashboard/ShareLinkActivityTable.tsx-34-  return (
--
app/admin/playback/dashboard/AudienceSplitCard.tsx-138-        labelY: labelPosition.y,
app/admin/playback/dashboard/AudienceSplitCard.tsx-139-      };
app/admin/playback/dashboard/AudienceSplitCard.tsx-140-    });
app/admin/playback/dashboard/AudienceSplitCard.tsx-141-}
app/admin/playback/dashboard/AudienceSplitCard.tsx-142-
app/admin/playback/dashboard/AudienceSplitCard.tsx:143:function AudienceDonut(props: AudienceDonutProps) {
app/admin/playback/dashboard/AudienceSplitCard.tsx-144-  const { label, member, anonymous } = props;
app/admin/playback/dashboard/AudienceSplitCard.tsx-145-  const total = member + anonymous;
app/admin/playback/dashboard/AudienceSplitCard.tsx-146-  const [hovered, setHovered] = React.useState<SliceKey | null>(null);
app/admin/playback/dashboard/AudienceSplitCard.tsx-147-
app/admin/playback/dashboard/AudienceSplitCard.tsx-148-  const size = 272;
--
app/admin/playback/dashboard/AudienceSplitCard.tsx-322-      </div>
app/admin/playback/dashboard/AudienceSplitCard.tsx-323-    </div>
app/admin/playback/dashboard/AudienceSplitCard.tsx-324-  );
app/admin/playback/dashboard/AudienceSplitCard.tsx-325-}
app/admin/playback/dashboard/AudienceSplitCard.tsx-326-
app/admin/playback/dashboard/AudienceSplitCard.tsx:327:export function AudienceSplitCard(props: { snapshot: PlaybackAdminSnapshot }) {
app/admin/playback/dashboard/AudienceSplitCard.tsx-328-  const allTimeMember = props.snapshot.audienceSplit.allTimeMemberPlayCount;
app/admin/playback/dashboard/AudienceSplitCard.tsx-329-  const allTimeAnonymous =
app/admin/playback/dashboard/AudienceSplitCard.tsx-330-    props.snapshot.audienceSplit.allTimeAnonymousPlayCount;
app/admin/playback/dashboard/AudienceSplitCard.tsx-331-  const recentMember = props.snapshot.audienceSplit.recent30dMemberPlayCount;
app/admin/playback/dashboard/AudienceSplitCard.tsx-332-  const recentAnonymous =
--
app/admin/playback/dashboard/TrackTable.tsx-16-} from "./playbackTelemetryDashboardFormatters";
app/admin/playback/dashboard/TrackTable.tsx-17-
app/admin/playback/dashboard/TrackTable.tsx-18-const POPULARITY_TRACK_BG = "rgba(255,255,255,0.08)";
app/admin/playback/dashboard/TrackTable.tsx-19-const POPULARITY_TRACK_FILL = "rgba(255,255,255,0.78)";
app/admin/playback/dashboard/TrackTable.tsx-20-
app/admin/playback/dashboard/TrackTable.tsx:21:export function TrackTable(props: { rows: TrackRow[]; emptyLabel?: string }) {
app/admin/playback/dashboard/TrackTable.tsx-22-  const topListenedMs = props.rows.reduce(
app/admin/playback/dashboard/TrackTable.tsx-23-    (max, row) => Math.max(max, row.listenedMs),
app/admin/playback/dashboard/TrackTable.tsx-24-    0,
app/admin/playback/dashboard/TrackTable.tsx-25-  );
app/admin/playback/dashboard/TrackTable.tsx-26-
--
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-64-  upperPoints: ChartPoint[];
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-65-  areaPath: string;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-66-  linePath: string;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-67-};
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-68-
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx:69:export function QualifiedPlayTrendChart(props: {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-70-  rows: TrendBucket[];
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-71-  range: TrendRangeKey;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-72-}) {
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-73-  const rows = props.rows;
app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx-74-  const width = 920;
--
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-11-  TEXT_FAINT,
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-12-  TEXT_MUTED,
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-13-  TEXT_PRIMARY,
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-14-} from "./playbackTelemetryDashboardStyles";
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-15-
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx:16:export function SectionCard(props: {
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-17-  title: string;
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-18-  subtitle?: string;
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-19-  children: React.ReactNode;
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-20-  headerRight?: React.ReactNode;
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-21-}) {
--
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-72-      {props.children}
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-73-    </section>
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-74-  );
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-75-}
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-76-
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx:77:export function TableShell(props: { children: React.ReactNode }) {
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-78-  return (
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-79-    <div
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-80-      style={{
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-81-        overflowX: "auto",
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-82-        borderRadius: 12,
--
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-87-      {props.children}
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-88-    </div>
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-89-  );
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-90-}
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-91-
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx:92:export function MetricPill(props: { label: string; value: string }) {
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-93-  return (
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-94-    <div
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-95-      style={{
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-96-        minWidth: 120,
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-97-        padding: "10px 12px",
--
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-125-      </div>
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-126-    </div>
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-127-  );
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-128-}
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-129-
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx:130:export function AudienceBadge(props: { audience: "member" | "anonymous" }) {
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-131-  const isMember = props.audience === "member";
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-132-
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-133-  return (
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-134-    <span
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-135-      style={{
--
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-152-      {isMember ? "Member" : "Anonymous"}
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-153-    </span>
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-154-  );
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-155-}
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-156-
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx:157:export function TrendRangeToggle(props: {
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-158-  value: TrendRangeKey;
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-159-  onChange: (value: TrendRangeKey) => void;
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-160-}) {
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-161-  const options: Array<{ key: TrendRangeKey; label: string }> = [
app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx-162-    { key: "hour", label: "Hour" },
--
app/admin/playback/dashboard/DedupeTable.tsx-23-import {
app/admin/playback/dashboard/DedupeTable.tsx-24-  buildDedupeSessionRows,
app/admin/playback/dashboard/DedupeTable.tsx-25-  resolveDedupeIdentityLabel,
app/admin/playback/dashboard/DedupeTable.tsx-26-} from "./playbackTelemetryDashboardModel";
app/admin/playback/dashboard/DedupeTable.tsx-27-
app/admin/playback/dashboard/DedupeTable.tsx:28:export function DedupeTable(props: {
app/admin/playback/dashboard/DedupeTable.tsx-29-  rows: PlaybackAdminSnapshot["recentDedupe"];
app/admin/playback/dashboard/DedupeTable.tsx-30-}) {
app/admin/playback/dashboard/DedupeTable.tsx-31-  const rawRows = props.rows as DedupeRow[];
app/admin/playback/dashboard/DedupeTable.tsx-32-  const rows = React.useMemo(() => buildDedupeSessionRows(rawRows), [rawRows]);
app/admin/playback/dashboard/DedupeTable.tsx-33-
--
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-25-  formatNumber,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-26-  fmtSnapshotStamp,
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-27-} from "./dashboard/playbackTelemetryDashboardFormatters";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-28-import type { TrendRangeKey } from "./dashboard/types";
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-29-
app/admin/playback/PlaybackTelemetryDashboardClient.tsx:30:export default function PlaybackTelemetryDashboardClient(props: {
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-31-  embed: boolean;
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-32-  initialSnapshot: PlaybackAdminSnapshot;
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-33-}) {
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-34-  const router = useRouter();
app/admin/playback/PlaybackTelemetryDashboardClient.tsx-35-  const [autoRefresh, setAutoRefresh] = React.useState(true);

===== DASHBOARD QUERY / TELEMETRY STORAGE REFERENCES =====

lib/playbackLive.ts-39-  windowSeconds = DEFAULT_ACTIVE_LISTENER_WINDOW_SECONDS,
lib/playbackLive.ts-40-): Promise<number> {
lib/playbackLive.ts-41-  const safeWindowSeconds = clampActiveListenerWindowSeconds(windowSeconds);
lib/playbackLive.ts-42-
lib/playbackLive.ts-43-  const res = await sql<ActiveListenerCountRow>`
lib/playbackLive.ts-44-    with recent_events as (
lib/playbackLive.ts-45-      select playback_id
lib/playbackLive.ts:46:      from member_playback_telemetry_dedupe
lib/playbackLive.ts-47-      where created_at >= now() - (${safeWindowSeconds} * interval '1 second')
lib/playbackLive.ts-48-
lib/playbackLive.ts-49-      union all
lib/playbackLive.ts-50-
lib/playbackLive.ts-51-      select playback_id
lib/playbackLive.ts:52:      from anonymous_playback_telemetry_dedupe
lib/playbackLive.ts-53-      where created_at >= now() - (${safeWindowSeconds} * interval '1 second')
lib/playbackLive.ts-54-    )
lib/playbackLive.ts-55-    select count(distinct playback_id)::int as active_count
lib/playbackLive.ts-56-    from recent_events
lib/playbackLive.ts-57-  `;
lib/playbackLive.ts-58-
lib/playbackLive.ts-59-  return asSafeInt(res.rows[0]?.active_count);
--
lib/badgeAdmin.ts-384-      mlt.member_id,
lib/badgeAdmin.ts-385-      m.email,
lib/badgeAdmin.ts-386-      
lib/badgeAdmin.ts-387-      m.created_at,
lib/badgeAdmin.ts-388-      mlt.listened_ms,
lib/badgeAdmin.ts-389-      mlt.play_count,
lib/badgeAdmin.ts-390-      mlt.completed_count
lib/badgeAdmin.ts:391:    from member_listen_totals mlt
lib/badgeAdmin.ts-392-    join members m on m.id = mlt.member_id
lib/badgeAdmin.ts-393-    where mlt.listened_ms >= ${minListenedMs}
lib/badgeAdmin.ts-394-    order by
lib/badgeAdmin.ts-395-      mlt.listened_ms desc,
lib/badgeAdmin.ts-396-      mlt.play_count desc,
lib/badgeAdmin.ts-397-      m.created_at asc nulls last
lib/badgeAdmin.ts-398-    limit ${limit}
--
lib/badgeAdmin.ts-412-      mlt.member_id,
lib/badgeAdmin.ts-413-      m.email,
lib/badgeAdmin.ts-414-      
lib/badgeAdmin.ts-415-      m.created_at,
lib/badgeAdmin.ts-416-      mlt.listened_ms,
lib/badgeAdmin.ts-417-      mlt.play_count,
lib/badgeAdmin.ts-418-      mlt.completed_count
lib/badgeAdmin.ts:419:    from member_listen_totals mlt
lib/badgeAdmin.ts-420-    join members m on m.id = mlt.member_id
lib/badgeAdmin.ts-421-    where mlt.play_count >= ${minPlayCount}
lib/badgeAdmin.ts-422-    order by
lib/badgeAdmin.ts-423-      mlt.play_count desc,
lib/badgeAdmin.ts-424-      mlt.listened_ms desc,
lib/badgeAdmin.ts-425-      m.created_at asc nulls last
lib/badgeAdmin.ts-426-    limit ${limit}
--
lib/badgeAdmin.ts-440-      mlt.member_id,
lib/badgeAdmin.ts-441-      m.email,
lib/badgeAdmin.ts-442-      
lib/badgeAdmin.ts-443-      m.created_at,
lib/badgeAdmin.ts-444-      mlt.listened_ms,
lib/badgeAdmin.ts-445-      mlt.play_count,
lib/badgeAdmin.ts-446-      mlt.completed_count
lib/badgeAdmin.ts:447:    from member_listen_totals mlt
lib/badgeAdmin.ts-448-    join members m on m.id = mlt.member_id
lib/badgeAdmin.ts-449-    where mlt.completed_count >= ${minCompletedCount}
lib/badgeAdmin.ts-450-    order by
lib/badgeAdmin.ts-451-      mlt.completed_count desc,
lib/badgeAdmin.ts-452-      mlt.listened_ms desc,
lib/badgeAdmin.ts-453-      m.created_at asc nulls last
lib/badgeAdmin.ts-454-    limit ${limit}
--
lib/badgeAdmin.ts-499-        )::int as play_count,
lib/badgeAdmin.ts-500-        count(*) filter (
lib/badgeAdmin.ts-501-          where d.event_type = ${PLAYBACK_TELEMETRY_EVENT_TYPES.PROGRESS}
lib/badgeAdmin.ts-502-        )::int as listened_ms_steps,
lib/badgeAdmin.ts-503-        count(*) filter (
lib/badgeAdmin.ts-504-          where d.event_type = ${PLAYBACK_TELEMETRY_EVENT_TYPES.COMPLETE}
lib/badgeAdmin.ts-505-        )::int as completed_count
lib/badgeAdmin.ts:506:      from member_playback_telemetry_dedupe d
lib/badgeAdmin.ts-507-      where
lib/badgeAdmin.ts-508-        d.created_at >= ${input.activeOnOrAfter}::timestamptz
lib/badgeAdmin.ts-509-        and (
lib/badgeAdmin.ts-510-          ${activeBefore}::timestamptz is null
lib/badgeAdmin.ts-511-          or d.created_at < ${activeBefore}::timestamptz
lib/badgeAdmin.ts-512-        )
lib/badgeAdmin.ts-513-      group by d.member_id
--
lib/badgeAdmin.ts-554-      m.email,
lib/badgeAdmin.ts-555-      
lib/badgeAdmin.ts-556-      m.created_at,
lib/badgeAdmin.ts-557-      mts.listened_ms,
lib/badgeAdmin.ts-558-      mts.play_count,
lib/badgeAdmin.ts-559-      mts.completed_count,
lib/badgeAdmin.ts-560-      mts.recording_id
lib/badgeAdmin.ts:561:    from member_track_listen_stats mts
lib/badgeAdmin.ts-562-    join members m on m.id = mts.member_id
lib/badgeAdmin.ts-563-    where mts.recording_id = ${input.recordingId}
lib/badgeAdmin.ts-564-      and mts.listened_ms >= ${minListenedMs}
lib/badgeAdmin.ts-565-    order by
lib/badgeAdmin.ts-566-      mts.listened_ms desc,
lib/badgeAdmin.ts-567-      mts.play_count desc,
lib/badgeAdmin.ts-568-      m.created_at asc nulls last
--
lib/badgeAdmin.ts-584-      m.email,
lib/badgeAdmin.ts-585-      
lib/badgeAdmin.ts-586-      m.created_at,
lib/badgeAdmin.ts-587-      mts.listened_ms,
lib/badgeAdmin.ts-588-      mts.play_count,
lib/badgeAdmin.ts-589-      mts.completed_count,
lib/badgeAdmin.ts-590-      mts.recording_id
lib/badgeAdmin.ts:591:    from member_track_listen_stats mts
lib/badgeAdmin.ts-592-    join members m on m.id = mts.member_id
lib/badgeAdmin.ts-593-    where mts.recording_id = ${input.recordingId}
lib/badgeAdmin.ts-594-      and mts.play_count >= ${minPlayCount}
lib/badgeAdmin.ts-595-    order by
lib/badgeAdmin.ts-596-      mts.play_count desc,
lib/badgeAdmin.ts-597-      mts.listened_ms desc,
lib/badgeAdmin.ts-598-      m.created_at asc nulls last
--
lib/badgeAdmin.ts-614-      m.email,
lib/badgeAdmin.ts-615-      
lib/badgeAdmin.ts-616-      m.created_at,
lib/badgeAdmin.ts-617-      mts.listened_ms,
lib/badgeAdmin.ts-618-      mts.play_count,
lib/badgeAdmin.ts-619-      mts.completed_count,
lib/badgeAdmin.ts-620-      mts.recording_id
lib/badgeAdmin.ts:621:    from member_track_listen_stats mts
lib/badgeAdmin.ts-622-    join members m on m.id = mts.member_id
lib/badgeAdmin.ts-623-    where mts.recording_id = ${input.recordingId}
lib/badgeAdmin.ts-624-      and mts.completed_count >= ${minCompletedCount}
lib/badgeAdmin.ts-625-    order by
lib/badgeAdmin.ts-626-      mts.completed_count desc,
lib/badgeAdmin.ts-627-      mts.listened_ms desc,
lib/badgeAdmin.ts-628-      m.created_at asc nulls last
--
lib/recordingListenTotals.ts-49-  const placeholders = ids.map((_, index) => `$${index + 1}`).join(", ");
lib/recordingListenTotals.ts-50-
lib/recordingListenTotals.ts-51-  const res = await sql.query<RecordingPlayCountRow>(
lib/recordingListenTotals.ts-52-    `
lib/recordingListenTotals.ts-53-      select
lib/recordingListenTotals.ts-54-        recording_id,
lib/recordingListenTotals.ts-55-        play_count
lib/recordingListenTotals.ts:56:      from recording_listen_totals
lib/recordingListenTotals.ts-57-      where recording_id in (${placeholders})
lib/recordingListenTotals.ts-58-    `,
lib/recordingListenTotals.ts-59-    ids,
lib/recordingListenTotals.ts-60-  );
lib/recordingListenTotals.ts-61-
lib/recordingListenTotals.ts-62-  const out: Record<string, number> = {};
lib/recordingListenTotals.ts-63-
--
lib/badgeAutoAward.ts-173-  recordingId: string;
lib/badgeAutoAward.ts-174-}): Promise<PlaybackAggregateSnapshot> {
lib/badgeAutoAward.ts-175-  const result = await sql<PlaybackAggregateSnapshotRow>`
lib/badgeAutoAward.ts-176-    select
lib/badgeAutoAward.ts-177-      coalesce(
lib/badgeAutoAward.ts-178-        (
lib/badgeAutoAward.ts-179-          select listened_ms
lib/badgeAutoAward.ts:180:          from member_listen_totals
lib/badgeAutoAward.ts-181-          where member_id = ${params.memberId}::uuid
lib/badgeAutoAward.ts-182-          limit 1
lib/badgeAutoAward.ts-183-        ),
lib/badgeAutoAward.ts-184-        0
lib/badgeAutoAward.ts-185-      ) as member_listened_ms,
lib/badgeAutoAward.ts-186-      coalesce(
lib/badgeAutoAward.ts-187-        (
lib/badgeAutoAward.ts-188-          select play_count
lib/badgeAutoAward.ts:189:          from member_listen_totals
lib/badgeAutoAward.ts-190-          where member_id = ${params.memberId}::uuid
lib/badgeAutoAward.ts-191-          limit 1
lib/badgeAutoAward.ts-192-        ),
lib/badgeAutoAward.ts-193-        0
lib/badgeAutoAward.ts-194-      ) as member_play_count,
lib/badgeAutoAward.ts-195-      coalesce(
lib/badgeAutoAward.ts-196-        (
lib/badgeAutoAward.ts-197-          select completed_count
lib/badgeAutoAward.ts:198:          from member_listen_totals
lib/badgeAutoAward.ts-199-          where member_id = ${params.memberId}::uuid
lib/badgeAutoAward.ts-200-          limit 1
lib/badgeAutoAward.ts-201-        ),
lib/badgeAutoAward.ts-202-        0
lib/badgeAutoAward.ts-203-      ) as member_completed_count,
lib/badgeAutoAward.ts-204-      coalesce(
lib/badgeAutoAward.ts-205-        (
lib/badgeAutoAward.ts-206-          select listened_ms
lib/badgeAutoAward.ts:207:          from member_track_listen_stats
lib/badgeAutoAward.ts-208-          where member_id = ${params.memberId}::uuid
lib/badgeAutoAward.ts-209-            and recording_id = ${params.recordingId}
lib/badgeAutoAward.ts-210-          limit 1
lib/badgeAutoAward.ts-211-        ),
lib/badgeAutoAward.ts-212-        0
lib/badgeAutoAward.ts-213-      ) as recording_listened_ms,
lib/badgeAutoAward.ts-214-      coalesce(
lib/badgeAutoAward.ts-215-        (
lib/badgeAutoAward.ts-216-          select play_count
lib/badgeAutoAward.ts:217:          from member_track_listen_stats
lib/badgeAutoAward.ts-218-          where member_id = ${params.memberId}::uuid
lib/badgeAutoAward.ts-219-            and recording_id = ${params.recordingId}
lib/badgeAutoAward.ts-220-          limit 1
lib/badgeAutoAward.ts-221-        ),
lib/badgeAutoAward.ts-222-        0
lib/badgeAutoAward.ts-223-      ) as recording_play_count,
lib/badgeAutoAward.ts-224-      coalesce(
lib/badgeAutoAward.ts-225-        (
lib/badgeAutoAward.ts-226-          select completed_count
lib/badgeAutoAward.ts:227:          from member_track_listen_stats
lib/badgeAutoAward.ts-228-          where member_id = ${params.memberId}::uuid
lib/badgeAutoAward.ts-229-            and recording_id = ${params.recordingId}
lib/badgeAutoAward.ts-230-          limit 1
lib/badgeAutoAward.ts-231-        ),
lib/badgeAutoAward.ts-232-        0
lib/badgeAutoAward.ts-233-      ) as recording_completed_count
lib/badgeAutoAward.ts-234-  `;
--
lib/badgeAutoAward.ts-367-  switch (mode) {
lib/badgeAutoAward.ts-368-    case "minutes_streamed": {
lib/badgeAutoAward.ts-369-      const minMinutes = Math.max(0, Math.floor(autoAward.minMinutes ?? 0));
lib/badgeAutoAward.ts-370-      const minListenedMs = minMinutes * 60_000;
lib/badgeAutoAward.ts-371-
lib/badgeAutoAward.ts-372-      const res = await sql<{ count: string | number }>`
lib/badgeAutoAward.ts-373-        select count(*) as count
lib/badgeAutoAward.ts:374:        from member_listen_totals
lib/badgeAutoAward.ts-375-        where member_id = ${memberId}::uuid
lib/badgeAutoAward.ts-376-          and listened_ms >= ${minListenedMs}
lib/badgeAutoAward.ts-377-      `;
lib/badgeAutoAward.ts-378-
lib/badgeAutoAward.ts-379-      return asNumber(res.rows[0]?.count) > 0;
lib/badgeAutoAward.ts-380-    }
lib/badgeAutoAward.ts-381-
lib/badgeAutoAward.ts-382-    case "play_count": {
lib/badgeAutoAward.ts-383-      const minPlayCount = Math.max(0, Math.floor(autoAward.minPlayCount ?? 0));
lib/badgeAutoAward.ts-384-
lib/badgeAutoAward.ts-385-      const res = await sql<{ count: string | number }>`
lib/badgeAutoAward.ts-386-        select count(*) as count
lib/badgeAutoAward.ts:387:        from member_listen_totals
lib/badgeAutoAward.ts-388-        where member_id = ${memberId}::uuid
lib/badgeAutoAward.ts-389-          and play_count >= ${minPlayCount}
lib/badgeAutoAward.ts-390-      `;
lib/badgeAutoAward.ts-391-
lib/badgeAutoAward.ts-392-      return asNumber(res.rows[0]?.count) > 0;
lib/badgeAutoAward.ts-393-    }
lib/badgeAutoAward.ts-394-
--
lib/badgeAutoAward.ts-396-      const minCompletedCount = Math.max(
lib/badgeAutoAward.ts-397-        0,
lib/badgeAutoAward.ts-398-        Math.floor(autoAward.minCompletedCount ?? 0),
lib/badgeAutoAward.ts-399-      );
lib/badgeAutoAward.ts-400-
lib/badgeAutoAward.ts-401-      const res = await sql<{ count: string | number }>`
lib/badgeAutoAward.ts-402-        select count(*) as count
lib/badgeAutoAward.ts:403:        from member_listen_totals
lib/badgeAutoAward.ts-404-        where member_id = ${memberId}::uuid
lib/badgeAutoAward.ts-405-          and completed_count >= ${minCompletedCount}
lib/badgeAutoAward.ts-406-      `;
lib/badgeAutoAward.ts-407-
lib/badgeAutoAward.ts-408-      return asNumber(res.rows[0]?.count) > 0;
lib/badgeAutoAward.ts-409-    }
lib/badgeAutoAward.ts-410-
--
lib/badgeAutoAward.ts-413-      const minMinutes = Math.max(0, Math.floor(autoAward.minMinutes ?? 0));
lib/badgeAutoAward.ts-414-      const minListenedMs = minMinutes * 60_000;
lib/badgeAutoAward.ts-415-
lib/badgeAutoAward.ts-416-      if (!recordingId) return false;
lib/badgeAutoAward.ts-417-
lib/badgeAutoAward.ts-418-      const res = await sql<{ count: string | number }>`
lib/badgeAutoAward.ts-419-        select count(*) as count
lib/badgeAutoAward.ts:420:        from member_track_listen_stats
lib/badgeAutoAward.ts-421-        where member_id = ${memberId}::uuid
lib/badgeAutoAward.ts-422-          and recording_id = ${recordingId}
lib/badgeAutoAward.ts-423-          and listened_ms >= ${minListenedMs}
lib/badgeAutoAward.ts-424-      `;
lib/badgeAutoAward.ts-425-
lib/badgeAutoAward.ts-426-      return asNumber(res.rows[0]?.count) > 0;
lib/badgeAutoAward.ts-427-    }
--
lib/badgeAutoAward.ts-430-      const recordingId = autoAward.recordingId?.trim() ?? "";
lib/badgeAutoAward.ts-431-      const minPlayCount = Math.max(0, Math.floor(autoAward.minPlayCount ?? 0));
lib/badgeAutoAward.ts-432-
lib/badgeAutoAward.ts-433-      if (!recordingId) return false;
lib/badgeAutoAward.ts-434-
lib/badgeAutoAward.ts-435-      const res = await sql<{ count: string | number }>`
lib/badgeAutoAward.ts-436-        select count(*) as count
lib/badgeAutoAward.ts:437:        from member_track_listen_stats
lib/badgeAutoAward.ts-438-        where member_id = ${memberId}::uuid
lib/badgeAutoAward.ts-439-          and recording_id = ${recordingId}
lib/badgeAutoAward.ts-440-          and play_count >= ${minPlayCount}
lib/badgeAutoAward.ts-441-      `;
lib/badgeAutoAward.ts-442-
lib/badgeAutoAward.ts-443-      return asNumber(res.rows[0]?.count) > 0;
lib/badgeAutoAward.ts-444-    }
--
lib/badgeAutoAward.ts-450-        Math.floor(autoAward.minCompletedCount ?? 0),
lib/badgeAutoAward.ts-451-      );
lib/badgeAutoAward.ts-452-
lib/badgeAutoAward.ts-453-      if (!recordingId) return false;
lib/badgeAutoAward.ts-454-
lib/badgeAutoAward.ts-455-      const res = await sql<{ count: string | number }>`
lib/badgeAutoAward.ts-456-        select count(*) as count
lib/badgeAutoAward.ts:457:        from member_track_listen_stats
lib/badgeAutoAward.ts-458-        where member_id = ${memberId}::uuid
lib/badgeAutoAward.ts-459-          and recording_id = ${recordingId}
lib/badgeAutoAward.ts-460-          and completed_count >= ${minCompletedCount}
lib/badgeAutoAward.ts-461-      `;
lib/badgeAutoAward.ts-462-
lib/badgeAutoAward.ts-463-      return asNumber(res.rows[0]?.count) > 0;
lib/badgeAutoAward.ts-464-    }
--
lib/shareTokenPlaybackContext.ts-322-  listenedMs: number;
lib/shareTokenPlaybackContext.ts-323-  progressMs: number;
lib/shareTokenPlaybackContext.ts-324-  durationMs: number | null;
lib/shareTokenPlaybackContext.ts-325-  occurredAtIso: string;
lib/shareTokenPlaybackContext.ts-326-}): Promise<void> {
lib/shareTokenPlaybackContext.ts-327-  try {
lib/shareTokenPlaybackContext.ts-328-    await sql`
lib/shareTokenPlaybackContext.ts:329:      insert into share_token_playback_events (
lib/shareTokenPlaybackContext.ts-330-        share_token_id,
lib/shareTokenPlaybackContext.ts-331-        telemetry_label,
lib/shareTokenPlaybackContext.ts-332-        scope_id,
lib/shareTokenPlaybackContext.ts-333-        audience,
lib/shareTokenPlaybackContext.ts-334-        member_id,
lib/shareTokenPlaybackContext.ts-335-        recording_id,
lib/shareTokenPlaybackContext.ts-336-        playback_id,
--
lib/events.ts-102-  const sinceIso = new Date(
lib/events.ts-103-    Date.now() - sinceDays * 24 * 60 * 60 * 1000,
lib/events.ts-104-  ).toISOString();
lib/events.ts-105-
lib/events.ts-106-  const r = await sql<{ n: number }>`
lib/events.ts-107-    with completed_recordings as (
lib/events.ts-108-      select nullif(recording_id, '') as recording_id
lib/events.ts:109:      from anonymous_playback_telemetry_dedupe
lib/events.ts-110-      where anon_id = ${anonId}
lib/events.ts-111-        and event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_COMPLETE}
lib/events.ts-112-        and created_at >= ${sinceIso}::timestamptz
lib/events.ts-113-
lib/events.ts-114-      union
lib/events.ts-115-
lib/events.ts-116-      select coalesce(
--
lib/memberDashboardServer.ts-38-}
lib/memberDashboardServer.ts-39-
lib/memberDashboardServer.ts-40-async function getDashboardMinutesStreamed(
lib/memberDashboardServer.ts-41-  memberId: string,
lib/memberDashboardServer.ts-42-): Promise<number | null> {
lib/memberDashboardServer.ts-43-  const res = await sql<MemberListenTotalsRow>`
lib/memberDashboardServer.ts-44-    select listened_ms
lib/memberDashboardServer.ts:45:    from member_listen_totals
lib/memberDashboardServer.ts-46-    where member_id = ${memberId}::uuid
lib/memberDashboardServer.ts-47-    limit 1
lib/memberDashboardServer.ts-48-  `;
lib/memberDashboardServer.ts-49-
lib/memberDashboardServer.ts-50-  const listenedMsRaw = res.rows[0]?.listened_ms;
lib/memberDashboardServer.ts-51-  const listenedMs =
lib/memberDashboardServer.ts-52-    typeof listenedMsRaw === "string"
--
lib/memberDashboardServer.ts-62-): Promise<PortalMemberFavouriteTrack | null> {
lib/memberDashboardServer.ts-63-  const statsRes = await sql<MemberFavouriteTrackRow>`
lib/memberDashboardServer.ts-64-    select
lib/memberDashboardServer.ts-65-      recording_id,
lib/memberDashboardServer.ts-66-      listened_ms,
lib/memberDashboardServer.ts-67-      completed_count,
lib/memberDashboardServer.ts-68-      last_listened_at
lib/memberDashboardServer.ts:69:    from member_track_listen_stats
lib/memberDashboardServer.ts-70-    where member_id = ${memberId}::uuid
lib/memberDashboardServer.ts-71-    order by
lib/memberDashboardServer.ts-72-      listened_ms desc,
lib/memberDashboardServer.ts-73-      completed_count desc,
lib/memberDashboardServer.ts-74-      last_listened_at desc nulls last
lib/memberDashboardServer.ts-75-    limit 1
lib/memberDashboardServer.ts-76-  `;
--
lib/playbackAdmin.ts-164-  const res = await sql<AggregateRow>`
lib/playbackAdmin.ts-165-    select
lib/playbackAdmin.ts-166-      coalesce(sum(listened_ms), 0) as listened_ms,
lib/playbackAdmin.ts-167-      coalesce(sum(credited_progress_count), 0) as credited_progress_count,
lib/playbackAdmin.ts-168-      coalesce(sum(play_count), 0) as play_count,
lib/playbackAdmin.ts-169-      coalesce(sum(completed_count), 0) as completed_count,
lib/playbackAdmin.ts-170-      count(*)::int as active_count
lib/playbackAdmin.ts:171:    from member_listen_totals
lib/playbackAdmin.ts-172-    where
lib/playbackAdmin.ts-173-      listened_ms > 0
lib/playbackAdmin.ts-174-      or credited_progress_count > 0
lib/playbackAdmin.ts-175-      or play_count > 0
lib/playbackAdmin.ts-176-      or completed_count > 0
lib/playbackAdmin.ts-177-  `;
lib/playbackAdmin.ts-178-
--
lib/playbackAdmin.ts-191-  const res = await sql<AggregateRow>`
lib/playbackAdmin.ts-192-    select
lib/playbackAdmin.ts-193-      coalesce(sum(listened_ms), 0) as listened_ms,
lib/playbackAdmin.ts-194-      coalesce(sum(credited_progress_count), 0) as credited_progress_count,
lib/playbackAdmin.ts-195-      coalesce(sum(play_count), 0) as play_count,
lib/playbackAdmin.ts-196-      coalesce(sum(completed_count), 0) as completed_count,
lib/playbackAdmin.ts-197-      count(*)::int as active_count
lib/playbackAdmin.ts:198:    from recording_listen_totals
lib/playbackAdmin.ts-199-    where
lib/playbackAdmin.ts-200-      listened_ms > 0
lib/playbackAdmin.ts-201-      or credited_progress_count > 0
lib/playbackAdmin.ts-202-      or play_count > 0
lib/playbackAdmin.ts-203-      or completed_count > 0
lib/playbackAdmin.ts-204-  `;
lib/playbackAdmin.ts-205-
--
lib/playbackAdmin.ts-238-      coalesce(
lib/playbackAdmin.ts-239-        count(*) filter (
lib/playbackAdmin.ts-240-          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_COMPLETE}
lib/playbackAdmin.ts-241-        ),
lib/playbackAdmin.ts-242-        0
lib/playbackAdmin.ts-243-      ) as completed_count,
lib/playbackAdmin.ts-244-      count(distinct member_id)::int as active_count
lib/playbackAdmin.ts:245:    from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-246-    where created_at >= now() - (${RECENT_WINDOW_DAYS} * interval '1 day')
lib/playbackAdmin.ts-247-  `;
lib/playbackAdmin.ts-248-
lib/playbackAdmin.ts-249-  const row = res.rows[0];
lib/playbackAdmin.ts-250-
lib/playbackAdmin.ts-251-  return {
lib/playbackAdmin.ts-252-    listenedMs: asSafeInt(row?.listened_ms),
--
lib/playbackAdmin.ts-257-  };
lib/playbackAdmin.ts-258-}
lib/playbackAdmin.ts-259-
lib/playbackAdmin.ts-260-async function getSite30d(): Promise<PlaybackAdminAggregate> {
lib/playbackAdmin.ts-261-  const res = await sql<AggregateRow>`
lib/playbackAdmin.ts-262-    with recent_events as (
lib/playbackAdmin.ts-263-      select event_type
lib/playbackAdmin.ts:264:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-265-      where created_at >= now() - (${RECENT_WINDOW_DAYS} * interval '1 day')
lib/playbackAdmin.ts-266-
lib/playbackAdmin.ts-267-      union all
lib/playbackAdmin.ts-268-
lib/playbackAdmin.ts-269-      select event_type
lib/playbackAdmin.ts:270:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-271-      where created_at >= now() - (${RECENT_WINDOW_DAYS} * interval '1 day')
lib/playbackAdmin.ts-272-    )
lib/playbackAdmin.ts-273-    select
lib/playbackAdmin.ts-274-      coalesce(
lib/playbackAdmin.ts-275-        count(*) filter (
lib/playbackAdmin.ts-276-          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PROGRESS}
lib/playbackAdmin.ts-277-        ),
--
lib/playbackAdmin.ts-340-    select
lib/playbackAdmin.ts-341-      recording_id,
lib/playbackAdmin.ts-342-      listened_ms,
lib/playbackAdmin.ts-343-      credited_progress_count,
lib/playbackAdmin.ts-344-      play_count,
lib/playbackAdmin.ts-345-      completed_count,
lib/playbackAdmin.ts-346-      last_listened_at
lib/playbackAdmin.ts:347:    from recording_listen_totals
lib/playbackAdmin.ts-348-    where
lib/playbackAdmin.ts-349-      listened_ms > 0
lib/playbackAdmin.ts-350-      or credited_progress_count > 0
lib/playbackAdmin.ts-351-      or play_count > 0
lib/playbackAdmin.ts-352-      or completed_count > 0
lib/playbackAdmin.ts-353-    order by
lib/playbackAdmin.ts-354-      listened_ms desc,
--
lib/playbackAdmin.ts-366-    select
lib/playbackAdmin.ts-367-      recording_id,
lib/playbackAdmin.ts-368-      listened_ms,
lib/playbackAdmin.ts-369-      credited_progress_count,
lib/playbackAdmin.ts-370-      play_count,
lib/playbackAdmin.ts-371-      completed_count,
lib/playbackAdmin.ts-372-      last_listened_at
lib/playbackAdmin.ts:373:    from recording_listen_totals
lib/playbackAdmin.ts-374-    where last_listened_at is not null
lib/playbackAdmin.ts-375-    order by last_listened_at desc
lib/playbackAdmin.ts-376-    limit 12
lib/playbackAdmin.ts-377-  `;
lib/playbackAdmin.ts-378-
lib/playbackAdmin.ts-379-  return hydrateTrackRows(res.rows);
lib/playbackAdmin.ts-380-}
--
lib/playbackAdmin.ts-397-        interval '1 minute'
lib/playbackAdmin.ts-398-      ) as bucket_start
lib/playbackAdmin.ts-399-    ),
lib/playbackAdmin.ts-400-    member_counts as (
lib/playbackAdmin.ts-401-      select
lib/playbackAdmin.ts-402-        date_trunc('minute', created_at) as bucket_start,
lib/playbackAdmin.ts-403-        count(*)::int as n
lib/playbackAdmin.ts:404:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-405-      where
lib/playbackAdmin.ts-406-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-407-        and created_at >= date_trunc('minute', now()) - (59 * interval '1 minute')
lib/playbackAdmin.ts-408-      group by 1
lib/playbackAdmin.ts-409-    ),
lib/playbackAdmin.ts-410-    anonymous_counts as (
lib/playbackAdmin.ts-411-      select
lib/playbackAdmin.ts-412-        date_trunc('minute', created_at) as bucket_start,
lib/playbackAdmin.ts-413-        count(*)::int as n
lib/playbackAdmin.ts:414:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-415-      where
lib/playbackAdmin.ts-416-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-417-        and created_at >= date_trunc('minute', now()) - (59 * interval '1 minute')
lib/playbackAdmin.ts-418-      group by 1
lib/playbackAdmin.ts-419-    )
lib/playbackAdmin.ts-420-    select
lib/playbackAdmin.ts-421-      buckets.bucket_start::text as bucket_start_iso,
--
lib/playbackAdmin.ts-446-        interval '1 hour'
lib/playbackAdmin.ts-447-      ) as bucket_start
lib/playbackAdmin.ts-448-    ),
lib/playbackAdmin.ts-449-    member_counts as (
lib/playbackAdmin.ts-450-      select
lib/playbackAdmin.ts-451-        date_trunc('hour', created_at) as bucket_start,
lib/playbackAdmin.ts-452-        count(*)::int as n
lib/playbackAdmin.ts:453:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-454-      where
lib/playbackAdmin.ts-455-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-456-        and created_at >= date_trunc('hour', now()) - (23 * interval '1 hour')
lib/playbackAdmin.ts-457-      group by 1
lib/playbackAdmin.ts-458-    ),
lib/playbackAdmin.ts-459-    anonymous_counts as (
lib/playbackAdmin.ts-460-      select
lib/playbackAdmin.ts-461-        date_trunc('hour', created_at) as bucket_start,
lib/playbackAdmin.ts-462-        count(*)::int as n
lib/playbackAdmin.ts:463:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-464-      where
lib/playbackAdmin.ts-465-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-466-        and created_at >= date_trunc('hour', now()) - (23 * interval '1 hour')
lib/playbackAdmin.ts-467-      group by 1
lib/playbackAdmin.ts-468-    )
lib/playbackAdmin.ts-469-    select
lib/playbackAdmin.ts-470-      buckets.bucket_start::text as bucket_start_iso,
--
lib/playbackAdmin.ts-495-        interval '1 day'
lib/playbackAdmin.ts-496-      ) as bucket_start
lib/playbackAdmin.ts-497-    ),
lib/playbackAdmin.ts-498-    member_counts as (
lib/playbackAdmin.ts-499-      select
lib/playbackAdmin.ts-500-        date_trunc('day', created_at) as bucket_start,
lib/playbackAdmin.ts-501-        count(*)::int as n
lib/playbackAdmin.ts:502:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-503-      where
lib/playbackAdmin.ts-504-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-505-        and created_at >= date_trunc('day', now()) - (6 * interval '1 day')
lib/playbackAdmin.ts-506-      group by 1
lib/playbackAdmin.ts-507-    ),
lib/playbackAdmin.ts-508-    anonymous_counts as (
lib/playbackAdmin.ts-509-      select
lib/playbackAdmin.ts-510-        date_trunc('day', created_at) as bucket_start,
lib/playbackAdmin.ts-511-        count(*)::int as n
lib/playbackAdmin.ts:512:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-513-      where
lib/playbackAdmin.ts-514-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-515-        and created_at >= date_trunc('day', now()) - (6 * interval '1 day')
lib/playbackAdmin.ts-516-      group by 1
lib/playbackAdmin.ts-517-    )
lib/playbackAdmin.ts-518-    select
lib/playbackAdmin.ts-519-      buckets.bucket_start::text as bucket_start_iso,
--
lib/playbackAdmin.ts-544-        interval '1 day'
lib/playbackAdmin.ts-545-      ) as bucket_start
lib/playbackAdmin.ts-546-    ),
lib/playbackAdmin.ts-547-    member_counts as (
lib/playbackAdmin.ts-548-      select
lib/playbackAdmin.ts-549-        date_trunc('day', created_at) as bucket_start,
lib/playbackAdmin.ts-550-        count(*)::int as n
lib/playbackAdmin.ts:551:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-552-      where
lib/playbackAdmin.ts-553-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-554-        and created_at >= date_trunc('day', now()) - (29 * interval '1 day')
lib/playbackAdmin.ts-555-      group by 1
lib/playbackAdmin.ts-556-    ),
lib/playbackAdmin.ts-557-    anonymous_counts as (
lib/playbackAdmin.ts-558-      select
lib/playbackAdmin.ts-559-        date_trunc('day', created_at) as bucket_start,
lib/playbackAdmin.ts-560-        count(*)::int as n
lib/playbackAdmin.ts:561:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-562-      where
lib/playbackAdmin.ts-563-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-564-        and created_at >= date_trunc('day', now()) - (29 * interval '1 day')
lib/playbackAdmin.ts-565-      group by 1
lib/playbackAdmin.ts-566-    )
lib/playbackAdmin.ts-567-    select
lib/playbackAdmin.ts-568-      buckets.bucket_start::text as bucket_start_iso,
--
lib/playbackAdmin.ts-593-        interval '1 month'
lib/playbackAdmin.ts-594-      ) as bucket_start
lib/playbackAdmin.ts-595-    ),
lib/playbackAdmin.ts-596-    member_counts as (
lib/playbackAdmin.ts-597-      select
lib/playbackAdmin.ts-598-        date_trunc('month', created_at) as bucket_start,
lib/playbackAdmin.ts-599-        count(*)::int as n
lib/playbackAdmin.ts:600:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-601-      where
lib/playbackAdmin.ts-602-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-603-        and created_at >= date_trunc('month', now()) - (11 * interval '1 month')
lib/playbackAdmin.ts-604-      group by 1
lib/playbackAdmin.ts-605-    ),
lib/playbackAdmin.ts-606-    anonymous_counts as (
lib/playbackAdmin.ts-607-      select
lib/playbackAdmin.ts-608-        date_trunc('month', created_at) as bucket_start,
lib/playbackAdmin.ts-609-        count(*)::int as n
lib/playbackAdmin.ts:610:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-611-      where
lib/playbackAdmin.ts-612-        event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-613-        and created_at >= date_trunc('month', now()) - (11 * interval '1 month')
lib/playbackAdmin.ts-614-      group by 1
lib/playbackAdmin.ts-615-    )
lib/playbackAdmin.ts-616-    select
lib/playbackAdmin.ts-617-      buckets.bucket_start::text as bucket_start_iso,
--
lib/playbackAdmin.ts-637-  const res = await sql<TrendRow>`
lib/playbackAdmin.ts-638-    with bounds as (
lib/playbackAdmin.ts-639-      select
lib/playbackAdmin.ts-640-        least(
lib/playbackAdmin.ts-641-          coalesce(
lib/playbackAdmin.ts-642-            (
lib/playbackAdmin.ts-643-              select date_trunc('year', min(created_at))
lib/playbackAdmin.ts:644:              from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-645-              where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-646-            ),
lib/playbackAdmin.ts-647-            date_trunc('year', now())
lib/playbackAdmin.ts-648-          ),
lib/playbackAdmin.ts-649-          coalesce(
lib/playbackAdmin.ts-650-            (
lib/playbackAdmin.ts-651-              select date_trunc('year', min(created_at))
lib/playbackAdmin.ts:652:              from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-653-              where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-654-            ),
lib/playbackAdmin.ts-655-            date_trunc('year', now())
lib/playbackAdmin.ts-656-          )
lib/playbackAdmin.ts-657-        ) as first_year_start,
lib/playbackAdmin.ts-658-        date_trunc('year', now()) as current_year_start
lib/playbackAdmin.ts-659-    ),
--
lib/playbackAdmin.ts-664-        interval '1 year'
lib/playbackAdmin.ts-665-      ) as bucket_start
lib/playbackAdmin.ts-666-    ),
lib/playbackAdmin.ts-667-    member_counts as (
lib/playbackAdmin.ts-668-      select
lib/playbackAdmin.ts-669-        date_trunc('year', created_at) as bucket_start,
lib/playbackAdmin.ts-670-        count(*)::int as n
lib/playbackAdmin.ts:671:      from member_playback_telemetry_dedupe
lib/playbackAdmin.ts-672-      where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-673-      group by 1
lib/playbackAdmin.ts-674-    ),
lib/playbackAdmin.ts-675-    anonymous_counts as (
lib/playbackAdmin.ts-676-      select
lib/playbackAdmin.ts-677-        date_trunc('year', created_at) as bucket_start,
lib/playbackAdmin.ts-678-        count(*)::int as n
lib/playbackAdmin.ts:679:      from anonymous_playback_telemetry_dedupe
lib/playbackAdmin.ts-680-      where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-681-      group by 1
lib/playbackAdmin.ts-682-    )
lib/playbackAdmin.ts-683-    select
lib/playbackAdmin.ts-684-      buckets.bucket_start::text as bucket_start_iso,
lib/playbackAdmin.ts-685-      coalesce(member_counts.n, 0) as member_play_count,
lib/playbackAdmin.ts-686-      coalesce(anonymous_counts.n, 0) as anonymous_play_count,
--
lib/playbackAdmin.ts-738-          ),
lib/playbackAdmin.ts-739-          0
lib/playbackAdmin.ts-740-        ) as listened_ms,
lib/playbackAdmin.ts-741-        count(*) filter (
lib/playbackAdmin.ts-742-          where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_COMPLETE}
lib/playbackAdmin.ts-743-        ) as completed_count,
lib/playbackAdmin.ts-744-        max(occurred_at) as last_activity_at
lib/playbackAdmin.ts:745:      from share_token_playback_events
lib/playbackAdmin.ts-746-      where char_length(btrim(telemetry_label)) > 0
lib/playbackAdmin.ts-747-      group by telemetry_label
lib/playbackAdmin.ts-748-    ),
lib/playbackAdmin.ts-749-    ranked_tracks as (
lib/playbackAdmin.ts-750-      select
lib/playbackAdmin.ts-751-        telemetry_label,
lib/playbackAdmin.ts-752-        recording_id,
--
lib/playbackAdmin.ts-760-              0
lib/playbackAdmin.ts-761-            ) desc,
lib/playbackAdmin.ts-762-            count(*) filter (
lib/playbackAdmin.ts-763-              where event_type = ${EVENT_TYPES.PLAYBACK_TELEMETRY_PLAY}
lib/playbackAdmin.ts-764-            ) desc,
lib/playbackAdmin.ts-765-            max(occurred_at) desc
lib/playbackAdmin.ts-766-        ) as rank
lib/playbackAdmin.ts:767:      from share_token_playback_events
lib/playbackAdmin.ts-768-      where char_length(btrim(telemetry_label)) > 0
lib/playbackAdmin.ts-769-      group by telemetry_label, recording_id
lib/playbackAdmin.ts-770-    )
lib/playbackAdmin.ts-771-    select
lib/playbackAdmin.ts-772-      totals.telemetry_label,
lib/playbackAdmin.ts-773-      totals.qualified_play_count,
lib/playbackAdmin.ts-774-      totals.listened_ms,
--
lib/playbackAdmin.ts-813-        d.playback_id,
lib/playbackAdmin.ts-814-        evt.recording_id,
lib/playbackAdmin.ts-815-        d.event_type,
lib/playbackAdmin.ts-816-        d.milestone_key,
lib/playbackAdmin.ts-817-        d.created_at::text as created_at,
lib/playbackAdmin.ts-818-        'member'::text as audience,
lib/playbackAdmin.ts-819-        share_event.telemetry_label as share_token_label
lib/playbackAdmin.ts:820:      from member_playback_telemetry_dedupe d
lib/playbackAdmin.ts-821-      left join members m
lib/playbackAdmin.ts-822-        on m.id = d.member_id
lib/playbackAdmin.ts-823-      left join lateral (
lib/playbackAdmin.ts-824-        select
lib/playbackAdmin.ts-825-          me.payload->>'recording_id' as recording_id
lib/playbackAdmin.ts-826-        from member_events me
lib/playbackAdmin.ts-827-        where
--
lib/playbackAdmin.ts-829-          and me.event_type = d.event_type
lib/playbackAdmin.ts-830-          and me.payload->>'playback_id' = d.playback_id
lib/playbackAdmin.ts-831-        order by me.occurred_at desc
lib/playbackAdmin.ts-832-        limit 1
lib/playbackAdmin.ts-833-      ) evt on true
lib/playbackAdmin.ts-834-      left join lateral (
lib/playbackAdmin.ts-835-        select e.telemetry_label
lib/playbackAdmin.ts:836:        from share_token_playback_events e
lib/playbackAdmin.ts-837-        where
lib/playbackAdmin.ts-838-          e.audience = 'member'
lib/playbackAdmin.ts-839-          and e.member_id = d.member_id
lib/playbackAdmin.ts-840-          and e.playback_id = d.playback_id
lib/playbackAdmin.ts-841-          and e.event_type = d.event_type
lib/playbackAdmin.ts-842-          and e.milestone_key = d.milestone_key
lib/playbackAdmin.ts-843-        order by e.occurred_at desc
--
lib/playbackAdmin.ts-851-        d.playback_id,
lib/playbackAdmin.ts-852-        d.recording_id,
lib/playbackAdmin.ts-853-        d.event_type,
lib/playbackAdmin.ts-854-        d.milestone_key,
lib/playbackAdmin.ts-855-        d.created_at::text as created_at,
lib/playbackAdmin.ts-856-        'anonymous'::text as audience,
lib/playbackAdmin.ts-857-        share_event.telemetry_label as share_token_label
lib/playbackAdmin.ts:858:      from anonymous_playback_telemetry_dedupe d
lib/playbackAdmin.ts-859-      left join lateral (
lib/playbackAdmin.ts-860-        select e.telemetry_label
lib/playbackAdmin.ts:861:        from share_token_playback_events e
lib/playbackAdmin.ts-862-        where
lib/playbackAdmin.ts-863-          e.audience = 'anonymous'
lib/playbackAdmin.ts-864-          and e.playback_id = d.playback_id
lib/playbackAdmin.ts-865-          and e.recording_id = d.recording_id
lib/playbackAdmin.ts-866-          and e.event_type = d.event_type
lib/playbackAdmin.ts-867-          and e.milestone_key = d.milestone_key
lib/playbackAdmin.ts-868-        order by e.occurred_at desc
--
app/api/playback/telemetry/route.ts-70-  playbackId: string;
app/api/playback/telemetry/route.ts-71-  eventType: string;
app/api/playback/telemetry/route.ts-72-  milestoneKey: string;
app/api/playback/telemetry/route.ts-73-}): Promise<boolean> {
app/api/playback/telemetry/route.ts-74-  const { memberId, playbackId, eventType, milestoneKey } = params;
app/api/playback/telemetry/route.ts-75-
app/api/playback/telemetry/route.ts-76-  const res = await sql<{ inserted: boolean }>`
app/api/playback/telemetry/route.ts:77:    insert into member_playback_telemetry_dedupe (
app/api/playback/telemetry/route.ts-78-      member_id,
app/api/playback/telemetry/route.ts-79-      playback_id,
app/api/playback/telemetry/route.ts-80-      event_type,
app/api/playback/telemetry/route.ts-81-      milestone_key
app/api/playback/telemetry/route.ts-82-    )
app/api/playback/telemetry/route.ts-83-    values (
app/api/playback/telemetry/route.ts-84-      ${memberId}::uuid,
--
app/api/playback/telemetry/route.ts-99-  recordingId: string;
app/api/playback/telemetry/route.ts-100-  eventType: string;
app/api/playback/telemetry/route.ts-101-  milestoneKey: string;
app/api/playback/telemetry/route.ts-102-}): Promise<boolean> {
app/api/playback/telemetry/route.ts-103-  const { anonId, playbackId, recordingId, eventType, milestoneKey } = params;
app/api/playback/telemetry/route.ts-104-
app/api/playback/telemetry/route.ts-105-  const res = await sql<{ inserted: boolean }>`
app/api/playback/telemetry/route.ts:106:    insert into anonymous_playback_telemetry_dedupe (
app/api/playback/telemetry/route.ts-107-      anon_id,
app/api/playback/telemetry/route.ts-108-      playback_id,
app/api/playback/telemetry/route.ts-109-      recording_id,
app/api/playback/telemetry/route.ts-110-      event_type,
app/api/playback/telemetry/route.ts-111-      milestone_key
app/api/playback/telemetry/route.ts-112-    )
app/api/playback/telemetry/route.ts-113-    values (
--
app/api/playback/telemetry/route.ts-128-  memberId: string;
app/api/playback/telemetry/route.ts-129-  recordingId: string;
app/api/playback/telemetry/route.ts-130-  occurredAtIso: string;
app/api/playback/telemetry/route.ts-131-}): Promise<void> {
app/api/playback/telemetry/route.ts-132-  const { memberId, recordingId, occurredAtIso } = params;
app/api/playback/telemetry/route.ts-133-
app/api/playback/telemetry/route.ts-134-  await sql`
app/api/playback/telemetry/route.ts:135:    insert into member_track_listen_stats (
app/api/playback/telemetry/route.ts-136-      member_id,
app/api/playback/telemetry/route.ts-137-      recording_id,
app/api/playback/telemetry/route.ts-138-      listened_ms,
app/api/playback/telemetry/route.ts-139-      credited_progress_count,
app/api/playback/telemetry/route.ts-140-      play_count,
app/api/playback/telemetry/route.ts-141-      completed_count,
app/api/playback/telemetry/route.ts-142-      first_listened_at,
--
app/api/playback/telemetry/route.ts-154-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-155-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-156-      now(),
app/api/playback/telemetry/route.ts-157-      now()
app/api/playback/telemetry/route.ts-158-    )
app/api/playback/telemetry/route.ts-159-    on conflict (member_id, recording_id)
app/api/playback/telemetry/route.ts-160-    do update set
app/api/playback/telemetry/route.ts:161:      play_count = member_track_listen_stats.play_count + 1,
app/api/playback/telemetry/route.ts-162-      first_listened_at = coalesce(
app/api/playback/telemetry/route.ts:163:        member_track_listen_stats.first_listened_at,
app/api/playback/telemetry/route.ts-164-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-165-      ),
app/api/playback/telemetry/route.ts-166-      last_listened_at = greatest(
app/api/playback/telemetry/route.ts:167:        coalesce(member_track_listen_stats.last_listened_at, ${occurredAtIso}::timestamptz),
app/api/playback/telemetry/route.ts-168-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-169-      ),
app/api/playback/telemetry/route.ts-170-      updated_at = now()
app/api/playback/telemetry/route.ts-171-  `;
app/api/playback/telemetry/route.ts-172-
app/api/playback/telemetry/route.ts-173-  await sql`
app/api/playback/telemetry/route.ts:174:    insert into member_listen_totals (
app/api/playback/telemetry/route.ts-175-      member_id,
app/api/playback/telemetry/route.ts-176-      listened_ms,
app/api/playback/telemetry/route.ts-177-      credited_progress_count,
app/api/playback/telemetry/route.ts-178-      play_count,
app/api/playback/telemetry/route.ts-179-      completed_count,
app/api/playback/telemetry/route.ts-180-      first_listened_at,
app/api/playback/telemetry/route.ts-181-      last_listened_at,
--
app/api/playback/telemetry/route.ts-191-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-192-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-193-      now(),
app/api/playback/telemetry/route.ts-194-      now()
app/api/playback/telemetry/route.ts-195-    )
app/api/playback/telemetry/route.ts-196-    on conflict (member_id)
app/api/playback/telemetry/route.ts-197-    do update set
app/api/playback/telemetry/route.ts:198:      play_count = member_listen_totals.play_count + 1,
app/api/playback/telemetry/route.ts-199-      first_listened_at = coalesce(
app/api/playback/telemetry/route.ts:200:        member_listen_totals.first_listened_at,
app/api/playback/telemetry/route.ts-201-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-202-      ),
app/api/playback/telemetry/route.ts-203-      last_listened_at = greatest(
app/api/playback/telemetry/route.ts:204:        coalesce(member_listen_totals.last_listened_at, ${occurredAtIso}::timestamptz),
app/api/playback/telemetry/route.ts-205-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-206-      ),
app/api/playback/telemetry/route.ts-207-      updated_at = now()
app/api/playback/telemetry/route.ts-208-  `;
app/api/playback/telemetry/route.ts-209-}
app/api/playback/telemetry/route.ts-210-
app/api/playback/telemetry/route.ts-211-async function upsertPlaybackProgress(params: {
--
app/api/playback/telemetry/route.ts-213-  recordingId: string;
app/api/playback/telemetry/route.ts-214-  listenedMs: number;
app/api/playback/telemetry/route.ts-215-  occurredAtIso: string;
app/api/playback/telemetry/route.ts-216-}): Promise<void> {
app/api/playback/telemetry/route.ts-217-  const { memberId, recordingId, listenedMs, occurredAtIso } = params;
app/api/playback/telemetry/route.ts-218-
app/api/playback/telemetry/route.ts-219-  await sql`
app/api/playback/telemetry/route.ts:220:    insert into member_track_listen_stats (
app/api/playback/telemetry/route.ts-221-      member_id,
app/api/playback/telemetry/route.ts-222-      recording_id,
app/api/playback/telemetry/route.ts-223-      listened_ms,
app/api/playback/telemetry/route.ts-224-      credited_progress_count,
app/api/playback/telemetry/route.ts-225-      play_count,
app/api/playback/telemetry/route.ts-226-      completed_count,
app/api/playback/telemetry/route.ts-227-      first_listened_at,
--
app/api/playback/telemetry/route.ts-239-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-240-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-241-      now(),
app/api/playback/telemetry/route.ts-242-      now()
app/api/playback/telemetry/route.ts-243-    )
app/api/playback/telemetry/route.ts-244-    on conflict (member_id, recording_id)
app/api/playback/telemetry/route.ts-245-    do update set
app/api/playback/telemetry/route.ts:246:      listened_ms = member_track_listen_stats.listened_ms + ${listenedMs},
app/api/playback/telemetry/route.ts:247:      credited_progress_count = member_track_listen_stats.credited_progress_count + 1,
app/api/playback/telemetry/route.ts-248-      first_listened_at = coalesce(
app/api/playback/telemetry/route.ts:249:        member_track_listen_stats.first_listened_at,
app/api/playback/telemetry/route.ts-250-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-251-      ),
app/api/playback/telemetry/route.ts-252-      last_listened_at = greatest(
app/api/playback/telemetry/route.ts:253:        coalesce(member_track_listen_stats.last_listened_at, ${occurredAtIso}::timestamptz),
app/api/playback/telemetry/route.ts-254-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-255-      ),
app/api/playback/telemetry/route.ts-256-      updated_at = now()
app/api/playback/telemetry/route.ts-257-  `;
app/api/playback/telemetry/route.ts-258-
app/api/playback/telemetry/route.ts-259-  await sql`
app/api/playback/telemetry/route.ts:260:    insert into member_listen_totals (
app/api/playback/telemetry/route.ts-261-      member_id,
app/api/playback/telemetry/route.ts-262-      listened_ms,
app/api/playback/telemetry/route.ts-263-      credited_progress_count,
app/api/playback/telemetry/route.ts-264-      play_count,
app/api/playback/telemetry/route.ts-265-      completed_count,
app/api/playback/telemetry/route.ts-266-      first_listened_at,
app/api/playback/telemetry/route.ts-267-      last_listened_at,
--
app/api/playback/telemetry/route.ts-277-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-278-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-279-      now(),
app/api/playback/telemetry/route.ts-280-      now()
app/api/playback/telemetry/route.ts-281-    )
app/api/playback/telemetry/route.ts-282-    on conflict (member_id)
app/api/playback/telemetry/route.ts-283-    do update set
app/api/playback/telemetry/route.ts:284:      listened_ms = member_listen_totals.listened_ms + ${listenedMs},
app/api/playback/telemetry/route.ts:285:      credited_progress_count = member_listen_totals.credited_progress_count + 1,
app/api/playback/telemetry/route.ts-286-      first_listened_at = coalesce(
app/api/playback/telemetry/route.ts:287:        member_listen_totals.first_listened_at,
app/api/playback/telemetry/route.ts-288-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-289-      ),
app/api/playback/telemetry/route.ts-290-      last_listened_at = greatest(
app/api/playback/telemetry/route.ts:291:        coalesce(member_listen_totals.last_listened_at, ${occurredAtIso}::timestamptz),
app/api/playback/telemetry/route.ts-292-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-293-      ),
app/api/playback/telemetry/route.ts-294-      updated_at = now()
app/api/playback/telemetry/route.ts-295-  `;
app/api/playback/telemetry/route.ts-296-}
app/api/playback/telemetry/route.ts-297-
app/api/playback/telemetry/route.ts-298-async function upsertRecordingPlaybackPlay(params: {
app/api/playback/telemetry/route.ts-299-  recordingId: string;
app/api/playback/telemetry/route.ts-300-  occurredAtIso: string;
app/api/playback/telemetry/route.ts-301-}): Promise<void> {
app/api/playback/telemetry/route.ts-302-  const { recordingId, occurredAtIso } = params;
app/api/playback/telemetry/route.ts-303-
app/api/playback/telemetry/route.ts-304-  await sql`
app/api/playback/telemetry/route.ts:305:    insert into recording_listen_totals (
app/api/playback/telemetry/route.ts-306-      recording_id,
app/api/playback/telemetry/route.ts-307-      listened_ms,
app/api/playback/telemetry/route.ts-308-      credited_progress_count,
app/api/playback/telemetry/route.ts-309-      play_count,
app/api/playback/telemetry/route.ts-310-      completed_count,
app/api/playback/telemetry/route.ts-311-      first_listened_at,
app/api/playback/telemetry/route.ts-312-      last_listened_at,
--
app/api/playback/telemetry/route.ts-322-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-323-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-324-      now(),
app/api/playback/telemetry/route.ts-325-      now()
app/api/playback/telemetry/route.ts-326-    )
app/api/playback/telemetry/route.ts-327-    on conflict (recording_id)
app/api/playback/telemetry/route.ts-328-    do update set
app/api/playback/telemetry/route.ts:329:      play_count = recording_listen_totals.play_count + 1,
app/api/playback/telemetry/route.ts-330-      first_listened_at = coalesce(
app/api/playback/telemetry/route.ts:331:        recording_listen_totals.first_listened_at,
app/api/playback/telemetry/route.ts-332-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-333-      ),
app/api/playback/telemetry/route.ts-334-      last_listened_at = greatest(
app/api/playback/telemetry/route.ts:335:        coalesce(recording_listen_totals.last_listened_at, ${occurredAtIso}::timestamptz),
app/api/playback/telemetry/route.ts-336-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-337-      ),
app/api/playback/telemetry/route.ts-338-      updated_at = now()
app/api/playback/telemetry/route.ts-339-  `;
app/api/playback/telemetry/route.ts-340-}
app/api/playback/telemetry/route.ts-341-
app/api/playback/telemetry/route.ts-342-async function upsertRecordingPlaybackProgress(params: {
app/api/playback/telemetry/route.ts-343-  recordingId: string;
app/api/playback/telemetry/route.ts-344-  listenedMs: number;
app/api/playback/telemetry/route.ts-345-  occurredAtIso: string;
app/api/playback/telemetry/route.ts-346-}): Promise<void> {
app/api/playback/telemetry/route.ts-347-  const { recordingId, listenedMs, occurredAtIso } = params;
app/api/playback/telemetry/route.ts-348-
app/api/playback/telemetry/route.ts-349-  await sql`
app/api/playback/telemetry/route.ts:350:    insert into recording_listen_totals (
app/api/playback/telemetry/route.ts-351-      recording_id,
app/api/playback/telemetry/route.ts-352-      listened_ms,
app/api/playback/telemetry/route.ts-353-      credited_progress_count,
app/api/playback/telemetry/route.ts-354-      play_count,
app/api/playback/telemetry/route.ts-355-      completed_count,
app/api/playback/telemetry/route.ts-356-      first_listened_at,
app/api/playback/telemetry/route.ts-357-      last_listened_at,
--
app/api/playback/telemetry/route.ts-367-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-368-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-369-      now(),
app/api/playback/telemetry/route.ts-370-      now()
app/api/playback/telemetry/route.ts-371-    )
app/api/playback/telemetry/route.ts-372-    on conflict (recording_id)
app/api/playback/telemetry/route.ts-373-    do update set
app/api/playback/telemetry/route.ts:374:      listened_ms = recording_listen_totals.listened_ms + ${listenedMs},
app/api/playback/telemetry/route.ts:375:      credited_progress_count = recording_listen_totals.credited_progress_count + 1,
app/api/playback/telemetry/route.ts-376-      first_listened_at = coalesce(
app/api/playback/telemetry/route.ts:377:        recording_listen_totals.first_listened_at,
app/api/playback/telemetry/route.ts-378-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-379-      ),
app/api/playback/telemetry/route.ts-380-      last_listened_at = greatest(
app/api/playback/telemetry/route.ts:381:        coalesce(recording_listen_totals.last_listened_at, ${occurredAtIso}::timestamptz),
app/api/playback/telemetry/route.ts-382-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-383-      ),
app/api/playback/telemetry/route.ts-384-      updated_at = now()
app/api/playback/telemetry/route.ts-385-  `;
app/api/playback/telemetry/route.ts-386-}
app/api/playback/telemetry/route.ts-387-
app/api/playback/telemetry/route.ts-388-async function upsertRecordingPlaybackComplete(params: {
app/api/playback/telemetry/route.ts-389-  recordingId: string;
app/api/playback/telemetry/route.ts-390-  occurredAtIso: string;
app/api/playback/telemetry/route.ts-391-}): Promise<void> {
app/api/playback/telemetry/route.ts-392-  const { recordingId, occurredAtIso } = params;
app/api/playback/telemetry/route.ts-393-
app/api/playback/telemetry/route.ts-394-  await sql`
app/api/playback/telemetry/route.ts:395:    insert into recording_listen_totals (
app/api/playback/telemetry/route.ts-396-      recording_id,
app/api/playback/telemetry/route.ts-397-      listened_ms,
app/api/playback/telemetry/route.ts-398-      credited_progress_count,
app/api/playback/telemetry/route.ts-399-      play_count,
app/api/playback/telemetry/route.ts-400-      completed_count,
app/api/playback/telemetry/route.ts-401-      first_listened_at,
app/api/playback/telemetry/route.ts-402-      last_listened_at,
--
app/api/playback/telemetry/route.ts-412-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-413-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-414-      now(),
app/api/playback/telemetry/route.ts-415-      now()
app/api/playback/telemetry/route.ts-416-    )
app/api/playback/telemetry/route.ts-417-    on conflict (recording_id)
app/api/playback/telemetry/route.ts-418-    do update set
app/api/playback/telemetry/route.ts:419:      completed_count = recording_listen_totals.completed_count + 1,
app/api/playback/telemetry/route.ts-420-      first_listened_at = coalesce(
app/api/playback/telemetry/route.ts:421:        recording_listen_totals.first_listened_at,
app/api/playback/telemetry/route.ts-422-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-423-      ),
app/api/playback/telemetry/route.ts-424-      last_listened_at = greatest(
app/api/playback/telemetry/route.ts:425:        coalesce(recording_listen_totals.last_listened_at, ${occurredAtIso}::timestamptz),
app/api/playback/telemetry/route.ts-426-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-427-      ),
app/api/playback/telemetry/route.ts-428-      updated_at = now()
app/api/playback/telemetry/route.ts-429-  `;
app/api/playback/telemetry/route.ts-430-}
app/api/playback/telemetry/route.ts-431-
app/api/playback/telemetry/route.ts-432-async function upsertPlaybackComplete(params: {
app/api/playback/telemetry/route.ts-433-  memberId: string;
app/api/playback/telemetry/route.ts-434-  recordingId: string;
app/api/playback/telemetry/route.ts-435-  occurredAtIso: string;
app/api/playback/telemetry/route.ts-436-}): Promise<void> {
app/api/playback/telemetry/route.ts-437-  const { memberId, recordingId, occurredAtIso } = params;
app/api/playback/telemetry/route.ts-438-
app/api/playback/telemetry/route.ts-439-  await sql`
app/api/playback/telemetry/route.ts:440:    insert into member_track_listen_stats (
app/api/playback/telemetry/route.ts-441-      member_id,
app/api/playback/telemetry/route.ts-442-      recording_id,
app/api/playback/telemetry/route.ts-443-      listened_ms,
app/api/playback/telemetry/route.ts-444-      credited_progress_count,
app/api/playback/telemetry/route.ts-445-      play_count,
app/api/playback/telemetry/route.ts-446-      completed_count,
app/api/playback/telemetry/route.ts-447-      first_listened_at,
--
app/api/playback/telemetry/route.ts-459-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-460-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-461-      now(),
app/api/playback/telemetry/route.ts-462-      now()
app/api/playback/telemetry/route.ts-463-    )
app/api/playback/telemetry/route.ts-464-    on conflict (member_id, recording_id)
app/api/playback/telemetry/route.ts-465-    do update set
app/api/playback/telemetry/route.ts:466:      completed_count = member_track_listen_stats.completed_count + 1,
app/api/playback/telemetry/route.ts-467-      first_listened_at = coalesce(
app/api/playback/telemetry/route.ts:468:        member_track_listen_stats.first_listened_at,
app/api/playback/telemetry/route.ts-469-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-470-      ),
app/api/playback/telemetry/route.ts-471-      last_listened_at = greatest(
app/api/playback/telemetry/route.ts:472:        coalesce(member_track_listen_stats.last_listened_at, ${occurredAtIso}::timestamptz),
app/api/playback/telemetry/route.ts-473-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-474-      ),
app/api/playback/telemetry/route.ts-475-      updated_at = now()
app/api/playback/telemetry/route.ts-476-  `;
app/api/playback/telemetry/route.ts-477-
app/api/playback/telemetry/route.ts-478-  await sql`
app/api/playback/telemetry/route.ts:479:    insert into member_listen_totals (
app/api/playback/telemetry/route.ts-480-      member_id,
app/api/playback/telemetry/route.ts-481-      listened_ms,
app/api/playback/telemetry/route.ts-482-      credited_progress_count,
app/api/playback/telemetry/route.ts-483-      play_count,
app/api/playback/telemetry/route.ts-484-      completed_count,
app/api/playback/telemetry/route.ts-485-      first_listened_at,
app/api/playback/telemetry/route.ts-486-      last_listened_at,
--
app/api/playback/telemetry/route.ts-496-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-497-      ${occurredAtIso}::timestamptz,
app/api/playback/telemetry/route.ts-498-      now(),
app/api/playback/telemetry/route.ts-499-      now()
app/api/playback/telemetry/route.ts-500-    )
app/api/playback/telemetry/route.ts-501-    on conflict (member_id)
app/api/playback/telemetry/route.ts-502-    do update set
app/api/playback/telemetry/route.ts:503:      completed_count = member_listen_totals.completed_count + 1,
app/api/playback/telemetry/route.ts-504-      first_listened_at = coalesce(
app/api/playback/telemetry/route.ts:505:        member_listen_totals.first_listened_at,
app/api/playback/telemetry/route.ts-506-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-507-      ),
app/api/playback/telemetry/route.ts-508-      last_listened_at = greatest(
app/api/playback/telemetry/route.ts:509:        coalesce(member_listen_totals.last_listened_at, ${occurredAtIso}::timestamptz),
app/api/playback/telemetry/route.ts-510-        ${occurredAtIso}::timestamptz
app/api/playback/telemetry/route.ts-511-      ),
app/api/playback/telemetry/route.ts-512-      updated_at = now()
app/api/playback/telemetry/route.ts-513-  `;
app/api/playback/telemetry/route.ts-514-}
app/api/playback/telemetry/route.ts-515-
app/api/playback/telemetry/route.ts-516-type NormalizedPlaybackTelemetry = {

===== PLAYBACK RECONCILIATION EXACT ISSUE / CALLERS =====

app/home/player/AudioEngine.tsx-551-  while (s.startsWith("alb:")) s = s.slice(4);
app/home/player/AudioEngine.tsx-552-  return s.trim();
app/home/player/AudioEngine.tsx-553-}
app/home/player/AudioEngine.tsx-554-
app/home/player/AudioEngine.tsx-555-type SharePlaybackAttribution = {
app/home/player/AudioEngine.tsx-556-  context: string;
app/home/player/AudioEngine.tsx-557-  scopeId: string;
app/home/player/AudioEngine.tsx-558-};
app/home/player/AudioEngine.tsx-559-
app/home/player/AudioEngine.tsx-560-function readQueueSharePlaybackAttribution(queue: {
app/home/player/AudioEngine.tsx:561:  queueSharePlaybackContext?: string | null;
app/home/player/AudioEngine.tsx:562:  queueSharePlaybackScopeId?: string | null;
app/home/player/AudioEngine.tsx-563-}): SharePlaybackAttribution | null {
app/home/player/AudioEngine.tsx:564:  const context = queue.queueSharePlaybackContext?.trim() ?? "";
app/home/player/AudioEngine.tsx:565:  const scopeId = queue.queueSharePlaybackScopeId?.trim() ?? "";
app/home/player/AudioEngine.tsx-566-
app/home/player/AudioEngine.tsx-567-  if (!context.startsWith("stpc1.") || context.length > 900) {
app/home/player/AudioEngine.tsx-568-    return null;
app/home/player/AudioEngine.tsx-569-  }
app/home/player/AudioEngine.tsx-570-
app/home/player/AudioEngine.tsx-571-  if (!/^alb:[^\s]+$/i.test(scopeId)) {
app/home/player/AudioEngine.tsx-572-    return null;
app/home/player/AudioEngine.tsx-573-  }
app/home/player/AudioEngine.tsx-574-
app/home/player/AudioEngine.tsx-575-  return { context, scopeId };
--
app/home/player/AudioEngine.tsx-2837-      const shareAttribution = telemetryShareAttributionRef.current;
app/home/player/AudioEngine.tsx-2838-
app/home/player/AudioEngine.tsx-2839-      return fetch("/api/playback/telemetry", {
app/home/player/AudioEngine.tsx-2840-        method: "POST",
app/home/player/AudioEngine.tsx-2841-        headers: { "Content-Type": "application/json" },
app/home/player/AudioEngine.tsx-2842-        body: JSON.stringify({
app/home/player/AudioEngine.tsx-2843-          ...payload,
app/home/player/AudioEngine.tsx-2844-          ...(shareAttribution
app/home/player/AudioEngine.tsx-2845-            ? {
app/home/player/AudioEngine.tsx-2846-                albumScopeId: shareAttribution.scopeId,
app/home/player/AudioEngine.tsx:2847:                sharePlaybackContext: shareAttribution.context,
app/home/player/AudioEngine.tsx-2848-              }
app/home/player/AudioEngine.tsx-2849-            : {}),
app/home/player/AudioEngine.tsx-2850-        }),
app/home/player/AudioEngine.tsx-2851-        keepalive: true,
app/home/player/AudioEngine.tsx-2852-      })
app/home/player/AudioEngine.tsx-2853-        .then(async (response) => {
app/home/player/AudioEngine.tsx-2854-          let json: unknown = null;
app/home/player/AudioEngine.tsx-2855-
app/home/player/AudioEngine.tsx-2856-          try {
app/home/player/AudioEngine.tsx-2857-            json = await response.json();
--
app/home/badges/BadgeAwardOverlayProvider.tsx-87-
app/home/badges/BadgeAwardOverlayProvider.tsx-88-    clearActiveTimeoutRef.current = window.setTimeout(() => {
app/home/badges/BadgeAwardOverlayProvider.tsx-89-      setActiveBadge(null);
app/home/badges/BadgeAwardOverlayProvider.tsx-90-      clearActiveTimeoutRef.current = null;
app/home/badges/BadgeAwardOverlayProvider.tsx-91-    }, EXIT_MS);
app/home/badges/BadgeAwardOverlayProvider.tsx-92-  }, [clearTimers]);
app/home/badges/BadgeAwardOverlayProvider.tsx-93-
app/home/badges/BadgeAwardOverlayProvider.tsx-94-  const announceBadges = React.useCallback((badges: BadgeAwardNotice[]) => {
app/home/badges/BadgeAwardOverlayProvider.tsx-95-    if (!Array.isArray(badges) || badges.length === 0) return;
app/home/badges/BadgeAwardOverlayProvider.tsx-96-
app/home/badges/BadgeAwardOverlayProvider.tsx:97:    setQueue((current) => {
app/home/badges/BadgeAwardOverlayProvider.tsx-98-      const next = dedupeIncomingBadges(badges, recentSeenRef);
app/home/badges/BadgeAwardOverlayProvider.tsx-99-      if (next.length === 0) return current;
app/home/badges/BadgeAwardOverlayProvider.tsx-100-      return current.concat(next);
app/home/badges/BadgeAwardOverlayProvider.tsx-101-    });
app/home/badges/BadgeAwardOverlayProvider.tsx-102-  }, []);
app/home/badges/BadgeAwardOverlayProvider.tsx-103-
app/home/badges/BadgeAwardOverlayProvider.tsx-104-  const announceBadge = React.useCallback(
app/home/badges/BadgeAwardOverlayProvider.tsx-105-    (badge: BadgeAwardNotice) => {
app/home/badges/BadgeAwardOverlayProvider.tsx-106-      announceBadges([badge]);
app/home/badges/BadgeAwardOverlayProvider.tsx-107-    },
app/home/badges/BadgeAwardOverlayProvider.tsx-108-    [announceBadges],
app/home/badges/BadgeAwardOverlayProvider.tsx-109-  );
app/home/badges/BadgeAwardOverlayProvider.tsx-110-
app/home/badges/BadgeAwardOverlayProvider.tsx-111-  const resetOverlayDebugState = React.useCallback(() => {
app/home/badges/BadgeAwardOverlayProvider.tsx-112-    clearTimers();
app/home/badges/BadgeAwardOverlayProvider.tsx-113-    recentSeenRef.current.clear();
app/home/badges/BadgeAwardOverlayProvider.tsx:114:    setQueue([]);
app/home/badges/BadgeAwardOverlayProvider.tsx-115-    setVisible(false);
app/home/badges/BadgeAwardOverlayProvider.tsx-116-    setActiveBadge(null);
app/home/badges/BadgeAwardOverlayProvider.tsx-117-  }, [clearTimers]);
app/home/badges/BadgeAwardOverlayProvider.tsx-118-
app/home/badges/BadgeAwardOverlayProvider.tsx-119-  React.useEffect(() => {
app/home/badges/BadgeAwardOverlayProvider.tsx-120-    if (activeBadge !== null) return;
app/home/badges/BadgeAwardOverlayProvider.tsx-121-    if (queue.length === 0) return;
app/home/badges/BadgeAwardOverlayProvider.tsx-122-
app/home/badges/BadgeAwardOverlayProvider.tsx-123-    const [nextBadge, ...rest] = queue;
app/home/badges/BadgeAwardOverlayProvider.tsx:124:    setQueue(rest);
app/home/badges/BadgeAwardOverlayProvider.tsx-125-    setActiveBadge(nextBadge);
app/home/badges/BadgeAwardOverlayProvider.tsx-126-    setVisible(true);
app/home/badges/BadgeAwardOverlayProvider.tsx-127-
app/home/badges/BadgeAwardOverlayProvider.tsx-128-    recentSeenRef.current.set(
app/home/badges/BadgeAwardOverlayProvider.tsx-129-      nextBadge.entitlementKey,
app/home/badges/BadgeAwardOverlayProvider.tsx-130-      Date.now() + RECENT_DEDUPE_WINDOW_MS,
app/home/badges/BadgeAwardOverlayProvider.tsx-131-    );
app/home/badges/BadgeAwardOverlayProvider.tsx-132-
app/home/badges/BadgeAwardOverlayProvider.tsx-133-    dismissTimeoutRef.current = window.setTimeout(() => {
app/home/badges/BadgeAwardOverlayProvider.tsx-134-      setVisible(false);
--
app/home/useSessionSurfaceController.ts-206-      router.prefetch(hrefToPlayer);
app/home/useSessionSurfaceController.ts-207-    } catch {}
app/home/useSessionSurfaceController.ts-208-  }, [router, hrefToPlayer]);
app/home/useSessionSurfaceController.ts-209-
app/home/useSessionSurfaceController.ts-210-  const prefetchPortal = React.useCallback(() => {
app/home/useSessionSurfaceController.ts-211-    try {
app/home/useSessionSurfaceController.ts-212-      router.prefetch(hrefToPortal);
app/home/useSessionSurfaceController.ts-213-    } catch {}
app/home/useSessionSurfaceController.ts-214-  }, [router, hrefToPortal]);
app/home/useSessionSurfaceController.ts-215-
app/home/useSessionSurfaceController.ts:216:  const patchQuery = React.useCallback(
app/home/useSessionSurfaceController.ts-217-    (patch: Record<string, string | null | undefined>) => {
app/home/useSessionSurfaceController.ts-218-      const filtered: Record<string, string | null | undefined> = {};
app/home/useSessionSurfaceController.ts-219-
app/home/useSessionSurfaceController.ts-220-      for (const [key, value] of Object.entries(patch)) {
app/home/useSessionSurfaceController.ts-221-        if (
app/home/useSessionSurfaceController.ts-222-          key === "st" ||
app/home/useSessionSurfaceController.ts-223-          key === "share" ||
app/home/useSessionSurfaceController.ts-224-          key === "autoplay" ||
app/home/useSessionSurfaceController.ts-225-          key === "gift" ||
app/home/useSessionSurfaceController.ts-226-          key === "checkout" ||
--
app/home/useSessionSurfaceController.ts-358-    [forceSurface],
app/home/useSessionSurfaceController.ts-359-  );
app/home/useSessionSurfaceController.ts-360-
app/home/useSessionSurfaceController.ts-361-  return {
app/home/useSessionSurfaceController.ts-362-    sp,
app/home/useSessionSurfaceController.ts-363-    route,
app/home/useSessionSurfaceController.ts-364-    isPlayer,
app/home/useSessionSurfaceController.ts-365-    portalTabId,
app/home/useSessionSurfaceController.ts-366-    effectiveIsPlayer,
app/home/useSessionSurfaceController.ts-367-    playerAlbumSlug,
app/home/useSessionSurfaceController.ts:368:    patchQuery,
app/home/useSessionSurfaceController.ts-369-    forceSurface,
app/home/useSessionSurfaceController.ts-370-    prefetchPlayer,
app/home/useSessionSurfaceController.ts-371-    prefetchPortal,
app/home/useSessionSurfaceController.ts-372-    dismissBanner,
app/home/useSessionSurfaceController.ts-373-    bannerKind,
app/home/useSessionSurfaceController.ts-374-    bannerCode,
app/home/useSessionSurfaceController.ts-375-    openPlayer,
app/home/useSessionSurfaceController.ts-376-    openPortal,
app/home/useSessionSurfaceController.ts-377-  };
app/home/useSessionSurfaceController.ts-378-}
--
app/home/player/PlayerState.tsx-15-type RepeatMode = "off" | "one" | "all";
app/home/player/PlayerState.tsx-16-type Intent = "play" | "pause" | null;
app/home/player/PlayerState.tsx-17-type LoadingReason = "token" | "attach" | "buffering";
app/home/player/PlayerState.tsx-18-
app/home/player/PlayerState.tsx-19-export type QueueContext = {
app/home/player/PlayerState.tsx-20-  contextId?: string;
app/home/player/PlayerState.tsx-21-  contextSlug?: string;
app/home/player/PlayerState.tsx-22-  contextTitle?: string;
app/home/player/PlayerState.tsx-23-  contextArtist?: string;
app/home/player/PlayerState.tsx-24-  artworkUrl?: string | null;
app/home/player/PlayerState.tsx:25:  sharePlaybackContext?: string | null;
app/home/player/PlayerState.tsx:26:  sharePlaybackScopeId?: string | null;
app/home/player/PlayerState.tsx-27-
app/home/player/PlayerState.tsx-28-  // Lets a deep link prepare an album queue around a specific track without
app/home/player/PlayerState.tsx-29-  // beginning playback or rearranging the album’s natural track order.
app/home/player/PlayerState.tsx-30-  initialCurrentRecordingId?: string;
app/home/player/PlayerState.tsx-31-};
app/home/player/PlayerState.tsx-32-
app/home/player/PlayerState.tsx-33-export type PlayerState = {
app/home/player/PlayerState.tsx-34-  status: PlayerStatus;
app/home/player/PlayerState.tsx-35-  current?: PlayerTrack;
app/home/player/PlayerState.tsx-36-  queue: PlayerTrack[];
--
app/home/player/PlayerState.tsx-40-  // True only while a user-initiated play request is awaiting the client-side
app/home/player/PlayerState.tsx-41-  // access decision. This is visual state: it must not itself arm protected
app/home/player/PlayerState.tsx-42-  // media attachment.
app/home/player/PlayerState.tsx-43-  playRequestPending: boolean;
app/home/player/PlayerState.tsx-44-
app/home/player/PlayerState.tsx-45-  queueContextId?: string;
app/home/player/PlayerState.tsx-46-  queueContextSlug?: string;
app/home/player/PlayerState.tsx-47-  queueContextTitle?: string;
app/home/player/PlayerState.tsx-48-  queueContextArtist?: string;
app/home/player/PlayerState.tsx-49-  queueContextArtworkUrl?: string | null;
app/home/player/PlayerState.tsx:50:  queueSharePlaybackContext?: string | null;
app/home/player/PlayerState.tsx:51:  queueSharePlaybackScopeId?: string | null;
app/home/player/PlayerState.tsx-52-
app/home/player/PlayerState.tsx-53-  intent: Intent;
app/home/player/PlayerState.tsx-54-  intentAtMs?: number;
app/home/player/PlayerState.tsx-55-  selectedRecordingId?: string;
app/home/player/PlayerState.tsx-56-  pendingRecordingId?: string;
app/home/player/PlayerState.tsx-57-
app/home/player/PlayerState.tsx-58-  pendingSeekMs?: number;
app/home/player/PlayerState.tsx-59-  seeking: boolean;
app/home/player/PlayerState.tsx-60-
app/home/player/PlayerState.tsx-61-  loadingReason?: LoadingReason;
--
app/home/player/PlayerState.tsx-444-}
app/home/player/PlayerState.tsx-445-
app/home/player/PlayerState.tsx-446-type QueueContextStatePatch = Partial<
app/home/player/PlayerState.tsx-447-  Pick<
app/home/player/PlayerState.tsx-448-    PlayerState,
app/home/player/PlayerState.tsx-449-    | "queueContextId"
app/home/player/PlayerState.tsx-450-    | "queueContextSlug"
app/home/player/PlayerState.tsx-451-    | "queueContextTitle"
app/home/player/PlayerState.tsx-452-    | "queueContextArtist"
app/home/player/PlayerState.tsx-453-    | "queueContextArtworkUrl"
app/home/player/PlayerState.tsx:454:    | "queueSharePlaybackContext"
app/home/player/PlayerState.tsx:455:    | "queueSharePlaybackScopeId"
app/home/player/PlayerState.tsx-456-  >
app/home/player/PlayerState.tsx-457->;
app/home/player/PlayerState.tsx-458-
app/home/player/PlayerState.tsx-459-function normalizedQueueShareValue(
app/home/player/PlayerState.tsx-460-  value: string | null | undefined,
app/home/player/PlayerState.tsx-461-): string | null {
app/home/player/PlayerState.tsx-462-  if (typeof value !== "string") return null;
app/home/player/PlayerState.tsx-463-  return value.trim() || null;
app/home/player/PlayerState.tsx-464-}
app/home/player/PlayerState.tsx-465-
--
app/home/player/PlayerState.tsx-481-  if (contextTitle) patch.queueContextTitle = contextTitle;
app/home/player/PlayerState.tsx-482-  if (contextArtist) patch.queueContextArtist = contextArtist;
app/home/player/PlayerState.tsx-483-
app/home/player/PlayerState.tsx-484-  patch.queueContextArtworkUrl =
app/home/player/PlayerState.tsx-485-    opts?.artworkUrl !== undefined
app/home/player/PlayerState.tsx-486-      ? (opts.artworkUrl ?? null)
app/home/player/PlayerState.tsx-487-      : (state.queueContextArtworkUrl ?? null);
app/home/player/PlayerState.tsx-488-
app/home/player/PlayerState.tsx-489-  const optionBag = opts ?? {};
app/home/player/PlayerState.tsx-490-
app/home/player/PlayerState.tsx:491:  if (Object.hasOwn(optionBag, "sharePlaybackContext")) {
app/home/player/PlayerState.tsx:492:    patch.queueSharePlaybackContext = normalizedQueueShareValue(
app/home/player/PlayerState.tsx:493:      opts?.sharePlaybackContext,
app/home/player/PlayerState.tsx-494-    );
app/home/player/PlayerState.tsx-495-  }
app/home/player/PlayerState.tsx-496-
app/home/player/PlayerState.tsx:497:  if (Object.hasOwn(optionBag, "sharePlaybackScopeId")) {
app/home/player/PlayerState.tsx:498:    patch.queueSharePlaybackScopeId = normalizedQueueShareValue(
app/home/player/PlayerState.tsx:499:      opts?.sharePlaybackScopeId,
app/home/player/PlayerState.tsx-500-    );
app/home/player/PlayerState.tsx-501-  }
app/home/player/PlayerState.tsx-502-
app/home/player/PlayerState.tsx-503-  return patch;
app/home/player/PlayerState.tsx-504-}
app/home/player/PlayerState.tsx-505-
app/home/player/PlayerState.tsx-506-function findRequestedInitialCurrent(
app/home/player/PlayerState.tsx-507-  queue: readonly PlayerTrack[],
app/home/player/PlayerState.tsx-508-  opts: QueueContext | undefined,
app/home/player/PlayerState.tsx-509-): PlayerTrack | null {
--
app/home/player/PlayerState.tsx-607-    queue: [],
app/home/player/PlayerState.tsx-608-    lastError: undefined,
app/home/player/PlayerState.tsx-609-    lastPlayAttemptAtMs: undefined,
app/home/player/PlayerState.tsx-610-    playRequestPending: false,
app/home/player/PlayerState.tsx-611-
app/home/player/PlayerState.tsx-612-    queueContextId: undefined,
app/home/player/PlayerState.tsx-613-    queueContextSlug: undefined,
app/home/player/PlayerState.tsx-614-    queueContextTitle: undefined,
app/home/player/PlayerState.tsx-615-    queueContextArtist: undefined,
app/home/player/PlayerState.tsx-616-    queueContextArtworkUrl: null,
app/home/player/PlayerState.tsx:617:    queueSharePlaybackContext: null,
app/home/player/PlayerState.tsx:618:    queueSharePlaybackScopeId: null,
app/home/player/PlayerState.tsx-619-
app/home/player/PlayerState.tsx-620-    intent: null,
app/home/player/PlayerState.tsx-621-    intentAtMs: undefined,
app/home/player/PlayerState.tsx-622-    selectedRecordingId: undefined,
app/home/player/PlayerState.tsx-623-    pendingRecordingId: undefined,
app/home/player/PlayerState.tsx-624-
app/home/player/PlayerState.tsx-625-    pendingSeekMs: undefined,
app/home/player/PlayerState.tsx-626-    seeking: false,
app/home/player/PlayerState.tsx-627-
app/home/player/PlayerState.tsx-628-    loadingReason: undefined,
--
app/home/player/PlayerState.tsx-665-
app/home/player/PlayerState.tsx-666-      if (!catalogueId) {
app/home/player/PlayerState.tsx-667-        onAllowed();
app/home/player/PlayerState.tsx-668-        return;
app/home/player/PlayerState.tsx-669-      }
app/home/player/PlayerState.tsx-670-
app/home/player/PlayerState.tsx-671-      try {
app/home/player/PlayerState.tsx-672-        const access = await fetchPlaybackAccessDecision({
app/home/player/PlayerState.tsx-673-          catalogueId,
app/home/player/PlayerState.tsx-674-          shareToken: readPlaybackShareTokenFromLocation(),
app/home/player/PlayerState.tsx:675:          accessIdentityKey: playbackAccessIdentityKey,
app/home/player/PlayerState.tsx-676-        });
app/home/player/PlayerState.tsx-677-
app/home/player/PlayerState.tsx-678-        if (!access.allowed) {
app/home/player/PlayerState.tsx-679-          blockPlayback(blockedPlaybackMessage(access));
app/home/player/PlayerState.tsx-680-          return;
app/home/player/PlayerState.tsx-681-        }
app/home/player/PlayerState.tsx-682-
app/home/player/PlayerState.tsx-683-        const expectedSharePlaybackScopeId = `alb:${catalogueId.replace(
app/home/player/PlayerState.tsx-684-          /^alb:/i,
app/home/player/PlayerState.tsx-685-          "",
app/home/player/PlayerState.tsx-686-        )}`;
app/home/player/PlayerState.tsx-687-
app/home/player/PlayerState.tsx-688-        if (
app/home/player/PlayerState.tsx:689:          access.sharePlaybackContext &&
app/home/player/PlayerState.tsx:690:          access.sharePlaybackScopeId === expectedSharePlaybackScopeId
app/home/player/PlayerState.tsx-691-        ) {
app/home/player/PlayerState.tsx-692-          setState((current) => ({
app/home/player/PlayerState.tsx-693-            ...current,
app/home/player/PlayerState.tsx:694:            queueSharePlaybackContext: access.sharePlaybackContext,
app/home/player/PlayerState.tsx:695:            queueSharePlaybackScopeId: access.sharePlaybackScopeId,
app/home/player/PlayerState.tsx-696-          }));
app/home/player/PlayerState.tsx-697-        }
app/home/player/PlayerState.tsx-698-
app/home/player/PlayerState.tsx-699-        onAllowed();
app/home/player/PlayerState.tsx-700-      } catch {
app/home/player/PlayerState.tsx-701-        blockPlayback("Playback access could not be verified.");
app/home/player/PlayerState.tsx-702-      }
app/home/player/PlayerState.tsx-703-    },
app/home/player/PlayerState.tsx-704-    [blockPlayback, playbackAccessIdentityKey],
app/home/player/PlayerState.tsx-705-  );
--
app/home/player/PlayerState.tsx-904-          queue: [],
app/home/player/PlayerState.tsx-905-          current: undefined,
app/home/player/PlayerState.tsx-906-          selectedRecordingId: undefined,
app/home/player/PlayerState.tsx-907-          pendingRecordingId: undefined,
app/home/player/PlayerState.tsx-908-
app/home/player/PlayerState.tsx-909-          positionMs: 0,
app/home/player/PlayerState.tsx-910-          pendingSeekMs: undefined,
app/home/player/PlayerState.tsx-911-          seeking: false,
app/home/player/PlayerState.tsx-912-          seekNonce: s.seekNonce + 1,
app/home/player/PlayerState.tsx-913-          loadingReason: undefined,
app/home/player/PlayerState.tsx:914:          queueSharePlaybackContext: null,
app/home/player/PlayerState.tsx:915:          queueSharePlaybackScopeId: null,
app/home/player/PlayerState.tsx-916-          assetDurationByPlaybackId: {},
app/home/player/PlayerState.tsx-917-        })),
app/home/player/PlayerState.tsx-918-
app/home/player/PlayerState.tsx-919-      setPositionMs: (ms: number) =>
app/home/player/PlayerState.tsx-920-        setState((s) => ({
app/home/player/PlayerState.tsx-921-          ...s,
app/home/player/PlayerState.tsx-922-          positionMs: Math.max(0, ms),
app/home/player/PlayerState.tsx-923-        })),
app/home/player/PlayerState.tsx-924-
app/home/player/PlayerState.tsx-925-      setDurationMs: (ms: number) =>
--
app/home/PortalArea.tsx-12-import type { PortalMemberSummary } from "@/lib/memberDashboard";
app/home/PortalArea.tsx-13-import PlayerController from "./player/PlayerController";
app/home/PortalArea.tsx-14-import ActivationGate from "@/app/home/ActivationGate";
app/home/PortalArea.tsx-15-import { PortalViewerProvider } from "@/app/home/PortalViewerProvider";
app/home/PortalArea.tsx-16-import { useGateBroker } from "@/app/home/gating/GateBroker";
app/home/PortalArea.tsx-17-import GateSpotlightOverlay from "@/app/home/gating/GateSpotlightOverlay";
app/home/PortalArea.tsx-18-import MiniPlayerHost from "./MiniPlayerHost";
app/home/PortalArea.tsx-19-import SessionChrome from "./SessionChrome";
app/home/PortalArea.tsx-20-import PortalSurface from "./PortalSurface";
app/home/PortalArea.tsx-21-import { useSessionSurfaceController } from "./useSessionSurfaceController";
app/home/PortalArea.tsx:22:import { usePlaybackReconciliation } from "./usePlaybackReconciliation";
app/home/PortalArea.tsx-23-
app/home/PortalArea.tsx-24-export type PortalAreaProps = {
app/home/PortalArea.tsx-25-  portalModules: PortalModule[];
app/home/PortalArea.tsx-26-  memberId: string | null;
app/home/PortalArea.tsx-27-  entitlementKeys: string[];
app/home/PortalArea.tsx-28-  memberSummary: PortalMemberSummary | null;
app/home/PortalArea.tsx-29-  topLogoUrl: string | null;
app/home/PortalArea.tsx-30-  topLogoHeight: number | null;
app/home/PortalArea.tsx-31-  featuredAlbumSlug: string;
app/home/PortalArea.tsx-32-  initialPortalTabId: string | null;
--
app/home/PortalArea.tsx-59-  const isSignedIn = Boolean(isSignedInRaw);
app/home/PortalArea.tsx-60-
app/home/PortalArea.tsx-61-  const router = useRouter();
app/home/PortalArea.tsx-62-
app/home/PortalArea.tsx-63-  const {
app/home/PortalArea.tsx-64-    sp,
app/home/PortalArea.tsx-65-    route,
app/home/PortalArea.tsx-66-    isPlayer,
app/home/PortalArea.tsx-67-    portalTabId,
app/home/PortalArea.tsx-68-    effectiveIsPlayer,
app/home/PortalArea.tsx:69:    patchQuery,
app/home/PortalArea.tsx-70-    forceSurface,
app/home/PortalArea.tsx-71-    prefetchPlayer,
app/home/PortalArea.tsx-72-    prefetchPortal,
app/home/PortalArea.tsx-73-    dismissBanner,
app/home/PortalArea.tsx-74-    bannerKind,
app/home/PortalArea.tsx-75-    bannerCode,
app/home/PortalArea.tsx-76-    openPlayer,
app/home/PortalArea.tsx-77-    openPortal,
app/home/PortalArea.tsx-78-  } = useSessionSurfaceController({
app/home/PortalArea.tsx-79-    defaultAlbumSlug: featuredAlbumSlug,
--
app/home/PortalArea.tsx-98-    bannerCode === "success" &&
app/home/PortalArea.tsx-99-    checkoutPurchase === "album";
app/home/PortalArea.tsx-100-
app/home/PortalArea.tsx-101-  React.useEffect(() => {
app/home/PortalArea.tsx-102-    if (!isSignedIn || !albumPurchaseSuccessBanner) return;
app/home/PortalArea.tsx-103-    dismissBanner();
app/home/PortalArea.tsx-104-  }, [albumPurchaseSuccessBanner, dismissBanner, isSignedIn]);
app/home/PortalArea.tsx-105-
app/home/PortalArea.tsx-106-  const isBrowsingAlbum = false;
app/home/PortalArea.tsx-107-
app/home/PortalArea.tsx:108:  const { onSelectAlbum } = usePlaybackReconciliation({
app/home/PortalArea.tsx-109-    player: p,
app/home/PortalArea.tsx-110-    bundle,
app/home/PortalArea.tsx-111-    albums,
app/home/PortalArea.tsx-112-    route,
app/home/PortalArea.tsx-113-    isPlayer,
app/home/PortalArea.tsx-114-    sp,
app/home/PortalArea.tsx:115:    patchQuery,
app/home/PortalArea.tsx-116-    forceSurface,
app/home/PortalArea.tsx-117-    routerPush: (href) => {
app/home/PortalArea.tsx-118-      router.push(href, { scroll: false });
app/home/PortalArea.tsx-119-    },
app/home/PortalArea.tsx-120-  });
app/home/PortalArea.tsx-121-
app/home/PortalArea.tsx-122-  const panels = React.useMemo<PortalPanelSpec[]>(
app/home/PortalArea.tsx-123-    () => [
app/home/PortalArea.tsx-124-      {
app/home/PortalArea.tsx-125-        id: "player",
--
app/home/player/FullPlayer.tsx-562-function accessFailureFallback(catalogueId: string): AccessState {
app/home/player/FullPlayer.tsx-563-  return {
app/home/player/FullPlayer.tsx-564-    forCatalogueId: catalogueId,
app/home/player/FullPlayer.tsx-565-    allowed: true,
app/home/player/FullPlayer.tsx-566-    embargoed: false,
app/home/player/FullPlayer.tsx-567-    releaseAt: null,
app/home/player/FullPlayer.tsx-568-    code: "ACCESS_CHECK_ERROR",
app/home/player/FullPlayer.tsx-569-    reason: "Access check failed (client).",
app/home/player/FullPlayer.tsx-570-    corr: null,
app/home/player/FullPlayer.tsx-571-    shareTokenAccess: null,
app/home/player/FullPlayer.tsx:572:    sharePlaybackContext: null,
app/home/player/FullPlayer.tsx:573:    sharePlaybackScopeId: null,
app/home/player/FullPlayer.tsx-574-  };
app/home/player/FullPlayer.tsx-575-}
app/home/player/FullPlayer.tsx-576-
app/home/player/FullPlayer.tsx-577-function resolveAccessForAlbum(
app/home/player/FullPlayer.tsx-578-  catalogueId: string | null,
app/home/player/FullPlayer.tsx-579-  access: AccessState | null,
app/home/player/FullPlayer.tsx-580-): AccessState | null {
app/home/player/FullPlayer.tsx-581-  if (!catalogueId) return null;
app/home/player/FullPlayer.tsx-582-  if (access?.forCatalogueId !== catalogueId) return null;
app/home/player/FullPlayer.tsx-583-  return access;
--
app/home/player/FullPlayer.tsx-1232-  const { album, tracks, albumLyrics } = bundle;
app/home/player/FullPlayer.tsx-1233-  const p = usePlayer();
app/home/player/FullPlayer.tsx-1234-  const { reportGate } = useGateBroker();
app/home/player/FullPlayer.tsx-1235-  const {
app/home/player/FullPlayer.tsx-1236-    isLoaded: clerkAuthLoaded,
app/home/player/FullPlayer.tsx-1237-    isSignedIn,
app/home/player/FullPlayer.tsx-1238-    userId,
app/home/player/FullPlayer.tsx-1239-    sessionId,
app/home/player/FullPlayer.tsx-1240-  } = useAuth();
app/home/player/FullPlayer.tsx-1241-
app/home/player/FullPlayer.tsx:1242:  const accessIdentityKey = resolveAccessIdentityKey({
app/home/player/FullPlayer.tsx-1243-    clerkAuthLoaded,
app/home/player/FullPlayer.tsx-1244-    isSignedIn,
app/home/player/FullPlayer.tsx-1245-    userId,
app/home/player/FullPlayer.tsx-1246-    sessionId,
app/home/player/FullPlayer.tsx-1247-  });
app/home/player/FullPlayer.tsx-1248-
app/home/player/FullPlayer.tsx-1249-  // near your other hooks
app/home/player/FullPlayer.tsx-1250-  const searchParams = useSearchParams();
app/home/player/FullPlayer.tsx-1251-  const stParam =
app/home/player/FullPlayer.tsx-1252-    (searchParams?.get("st") ?? searchParams?.get("share") ?? "").trim() ||
--
app/home/player/FullPlayer.tsx-1521-  // ✅ album-scoped view (prevents stale flash)
app/home/player/FullPlayer.tsx-1522-  const accessForAlbum = resolveAccessForAlbum(catalogueId, access);
app/home/player/FullPlayer.tsx-1523-
app/home/player/FullPlayer.tsx-1524-  React.useEffect(() => {
app/home/player/FullPlayer.tsx-1525-    if (!catalogueId || !clerkAuthLoaded) return;
app/home/player/FullPlayer.tsx-1526-
app/home/player/FullPlayer.tsx-1527-    let alive = true;
app/home/player/FullPlayer.tsx-1528-    const accessRequest = {
app/home/player/FullPlayer.tsx-1529-      catalogueId,
app/home/player/FullPlayer.tsx-1530-      shareToken: stParam,
app/home/player/FullPlayer.tsx:1531:      accessIdentityKey,
app/home/player/FullPlayer.tsx-1532-    };
app/home/player/FullPlayer.tsx-1533-
app/home/player/FullPlayer.tsx-1534-    // Do not leave an anonymous denial visibly attached to the newly
app/home/player/FullPlayer.tsx-1535-    // authenticated session while its fresh access request is in flight.
app/home/player/FullPlayer.tsx-1536-    setAccess(null);
app/home/player/FullPlayer.tsx-1537-
app/home/player/FullPlayer.tsx-1538-    const cached = getCachedPlaybackAccessDecision(accessRequest);
app/home/player/FullPlayer.tsx-1539-    setAccess((previous) => mergeAccessState(previous, cached));
app/home/player/FullPlayer.tsx-1540-
app/home/player/FullPlayer.tsx-1541-    void fetchPlaybackAccessDecision(accessRequest)
--
app/home/player/FullPlayer.tsx-1559-        // PlayerState must still fail closed if real play authorisation fails.
app/home/player/FullPlayer.tsx-1560-        setAccess(accessFailureFallback(catalogueId));
app/home/player/FullPlayer.tsx-1561-
app/home/player/FullPlayer.tsx-1562-        const player = pRef.current;
app/home/player/FullPlayer.tsx-1563-        if (player.lastError) player.clearError();
app/home/player/FullPlayer.tsx-1564-      });
app/home/player/FullPlayer.tsx-1565-
app/home/player/FullPlayer.tsx-1566-    return () => {
app/home/player/FullPlayer.tsx-1567-      alive = false;
app/home/player/FullPlayer.tsx-1568-    };
app/home/player/FullPlayer.tsx:1569:  }, [catalogueId, albumKey, stParam, accessIdentityKey, clerkAuthLoaded]);
app/home/player/FullPlayer.tsx-1570-
app/home/player/FullPlayer.tsx-1571-  // Unknown access still disables the transport until the access check resolves.
app/home/player/FullPlayer.tsx-1572-  // A resolved auth/cap block remains clickable so the user can summon the gate.
app/home/player/FullPlayer.tsx-1573-  const emb = effAlbum?.embargo;
app/home/player/FullPlayer.tsx-1574-  const { accessKnown, isPlaybackGate, canPlay, canAttemptPlay } =
app/home/player/FullPlayer.tsx-1575-    derivePlaybackAccessUiState(effTracks, accessForAlbum);
app/home/player/FullPlayer.tsx-1576-  const { releaseAtMs, showEmbargo, showShareTokenEmbargoAccess } =
app/home/player/FullPlayer.tsx-1577-    deriveEmbargoUiState({
app/home/player/FullPlayer.tsx-1578-      embargo: emb,
app/home/player/FullPlayer.tsx-1579-      accessForAlbum,
--
app/home/player/FullPlayer.tsx-1582-  const isThisAlbumActive = isAlbumPlaybackActive(albumKey, p.queueContextId);
app/home/player/FullPlayer.tsx-1583-  const playingThisAlbum = playingish && isThisAlbumActive;
app/home/player/FullPlayer.tsx-1584-
app/home/player/FullPlayer.tsx-1585-  const shareTokenAccess = accessForAlbum?.shareTokenAccess ?? null;
app/home/player/FullPlayer.tsx-1586-  const shareTokenExpiryLabel = formatShareTokenExpiry(
app/home/player/FullPlayer.tsx-1587-    shareTokenAccess?.expiresAt ?? null,
app/home/player/FullPlayer.tsx-1588-  );
app/home/player/FullPlayer.tsx-1589-  const shareTokenRedemptionLimit = formatRedemptionLimit(
app/home/player/FullPlayer.tsx-1590-    shareTokenAccess?.maxRedemptions ?? null,
app/home/player/FullPlayer.tsx-1591-  );
app/home/player/FullPlayer.tsx:1592:  const queueSharePlaybackContext =
app/home/player/FullPlayer.tsx:1593:    accessForAlbum?.sharePlaybackContext ?? null;
app/home/player/FullPlayer.tsx:1594:  const queueSharePlaybackScopeId =
app/home/player/FullPlayer.tsx:1595:    accessForAlbum?.sharePlaybackScopeId ?? null;
app/home/player/FullPlayer.tsx-1596-
app/home/player/FullPlayer.tsx-1597-  const [playLock, setPlayLock] = React.useState(false);
app/home/player/FullPlayer.tsx-1598-  const lockPlayFor = (ms: number) => {
app/home/player/FullPlayer.tsx-1599-    setPlayLock(true);
app/home/player/FullPlayer.tsx-1600-    window.setTimeout(() => setPlayLock(false), ms);
app/home/player/FullPlayer.tsx-1601-  };
app/home/player/FullPlayer.tsx-1602-
app/home/player/FullPlayer.tsx-1603-  const [transportLock, setTransportLock] = React.useState(false);
app/home/player/FullPlayer.tsx-1604-  const lockTransportFor = (ms: number) => {
app/home/player/FullPlayer.tsx-1605-    setTransportLock(true);
--
app/home/player/FullPlayer.tsx-1757-  const curId = p.current?.recordingId ?? "";
app/home/player/FullPlayer.tsx-1758-  const { albumIdx, prevDisabled, nextDisabled } = deriveAlbumTransportState({
app/home/player/FullPlayer.tsx-1759-    currentRecordingId: curId,
app/home/player/FullPlayer.tsx-1760-    tracks: effTracks,
app/home/player/FullPlayer.tsx-1761-    transportLock,
app/home/player/FullPlayer.tsx-1762-  });
app/home/player/FullPlayer.tsx-1763-
app/home/player/FullPlayer.tsx-1764-  function ensureAlbumQueue() {
app/home/player/FullPlayer.tsx-1765-    // Explicit nulls prevent an earlier private-link attribution context from
app/home/player/FullPlayer.tsx-1766-    // surviving when ordinary album playback replaces the queue.
app/home/player/FullPlayer.tsx:1767:    p.setQueue(effTracks, {
app/home/player/FullPlayer.tsx-1768-      contextId: albumKey ?? undefined,
app/home/player/FullPlayer.tsx-1769-      artworkUrl: effAlbum?.artworkUrl ?? null,
app/home/player/FullPlayer.tsx-1770-      contextSlug: effAlbumSlug,
app/home/player/FullPlayer.tsx-1771-      contextTitle: effAlbum?.title ?? undefined,
app/home/player/FullPlayer.tsx-1772-      contextArtist: effAlbum?.artist ?? undefined,
app/home/player/FullPlayer.tsx:1773:      sharePlaybackContext: queueSharePlaybackContext,
app/home/player/FullPlayer.tsx:1774:      sharePlaybackScopeId: queueSharePlaybackScopeId,
app/home/player/FullPlayer.tsx-1775-    });
app/home/player/FullPlayer.tsx-1776-  }
app/home/player/FullPlayer.tsx-1777-
app/home/player/FullPlayer.tsx-1778-  const preparedInitialQueueRef = React.useRef<string | null>(null);
app/home/player/FullPlayer.tsx-1779-
app/home/player/FullPlayer.tsx-1780-  React.useEffect(() => {
app/home/player/FullPlayer.tsx-1781-    // Wait for the access check so a private-link queue receives its resolved
app/home/player/FullPlayer.tsx-1782-    // attribution context before any listener can press play.
app/home/player/FullPlayer.tsx-1783-    if (!accessKnown) return;
app/home/player/FullPlayer.tsx-1784-
--
app/home/player/FullPlayer.tsx-1802-    );
app/home/player/FullPlayer.tsx-1803-
app/home/player/FullPlayer.tsx-1804-    if (hasExistingPlayerSession) return;
app/home/player/FullPlayer.tsx-1805-
app/home/player/FullPlayer.tsx-1806-    const queueKey = `${albumKey}:${initialTrack.recordingId}`;
app/home/player/FullPlayer.tsx-1807-
app/home/player/FullPlayer.tsx-1808-    if (preparedInitialQueueRef.current === queueKey) return;
app/home/player/FullPlayer.tsx-1809-
app/home/player/FullPlayer.tsx-1810-    preparedInitialQueueRef.current = queueKey;
app/home/player/FullPlayer.tsx-1811-
app/home/player/FullPlayer.tsx:1812:    p.setQueue(effTracks, {
app/home/player/FullPlayer.tsx-1813-      contextId: albumKey,
app/home/player/FullPlayer.tsx-1814-      artworkUrl: effAlbum?.artworkUrl ?? null,
app/home/player/FullPlayer.tsx-1815-      contextSlug: effAlbumSlug,
app/home/player/FullPlayer.tsx-1816-      contextTitle: effAlbum?.title ?? undefined,
app/home/player/FullPlayer.tsx-1817-      contextArtist: effAlbum?.artist ?? undefined,
app/home/player/FullPlayer.tsx:1818:      sharePlaybackContext: queueSharePlaybackContext,
app/home/player/FullPlayer.tsx:1819:      sharePlaybackScopeId: queueSharePlaybackScopeId,
app/home/player/FullPlayer.tsx-1820-      initialCurrentRecordingId: initialTrack.recordingId,
app/home/player/FullPlayer.tsx-1821-    });
app/home/player/FullPlayer.tsx-1822-  }, [
app/home/player/FullPlayer.tsx-1823-    accessKnown,
app/home/player/FullPlayer.tsx-1824-    albumKey,
app/home/player/FullPlayer.tsx-1825-    effAlbum?.artworkUrl,
app/home/player/FullPlayer.tsx-1826-    effAlbum?.artist,
app/home/player/FullPlayer.tsx-1827-    effAlbum?.title,
app/home/player/FullPlayer.tsx-1828-    effAlbumSlug,
app/home/player/FullPlayer.tsx-1829-    effTracks,
app/home/player/FullPlayer.tsx-1830-    isPublicAlbumRoute,
app/home/player/FullPlayer.tsx-1831-    p,
app/home/player/FullPlayer.tsx:1832:    queueSharePlaybackContext,
app/home/player/FullPlayer.tsx:1833:    queueSharePlaybackScopeId,
app/home/player/FullPlayer.tsx-1834-    route.albumSlug,
app/home/player/FullPlayer.tsx-1835-    route.displayId,
app/home/player/FullPlayer.tsx-1836-  ]);
app/home/player/FullPlayer.tsx-1837-
app/home/player/FullPlayer.tsx-1838-  function playAlbumIndex(i: number) {
app/home/player/FullPlayer.tsx-1839-    const t = effTracks[i];
app/home/player/FullPlayer.tsx-1840-    if (!t) return;
app/home/player/FullPlayer.tsx-1841-
app/home/player/FullPlayer.tsx-1842-    prefetchAlbumSession(t);
app/home/player/FullPlayer.tsx-1843-    ensureAlbumQueue();
--
app/home/usePlaybackReconciliation.ts:1:// web/app/home/usePlaybackReconciliation.ts
app/home/usePlaybackReconciliation.ts-2-"use client";
app/home/usePlaybackReconciliation.ts-3-
app/home/usePlaybackReconciliation.ts-4-import React from "react";
app/home/usePlaybackReconciliation.ts-5-import type { AlbumNavItem, AlbumPlayerBundle } from "@/lib/types";
app/home/usePlaybackReconciliation.ts-6-import { usePlayer } from "@/app/home/player/PlayerState";
app/home/usePlaybackReconciliation.ts-7-import { getAutoplayFlag } from "./urlState";
app/home/usePlaybackReconciliation.ts-8-
app/home/usePlaybackReconciliation.ts-9-type PlayerApi = ReturnType<typeof usePlayer>;
app/home/usePlaybackReconciliation.ts-10-
app/home/usePlaybackReconciliation.ts:11:export function usePlaybackReconciliation(props: {
app/home/usePlaybackReconciliation.ts-12-  player: PlayerApi;
app/home/usePlaybackReconciliation.ts-13-  bundle: AlbumPlayerBundle;
app/home/usePlaybackReconciliation.ts-14-  albums: AlbumNavItem[];
app/home/usePlaybackReconciliation.ts-15-  route: {
app/home/usePlaybackReconciliation.ts-16-    albumSlug: string | null;
app/home/usePlaybackReconciliation.ts-17-    displayId: string | null;
app/home/usePlaybackReconciliation.ts-18-  };
app/home/usePlaybackReconciliation.ts-19-  isPlayer: boolean;
app/home/usePlaybackReconciliation.ts-20-  sp: URLSearchParams;
app/home/usePlaybackReconciliation.ts:21:  patchQuery: (patch: Record<string, string | null | undefined>) => void;
app/home/usePlaybackReconciliation.ts-22-  forceSurface: (
app/home/usePlaybackReconciliation.ts-23-    surface: "player" | "portal",
app/home/usePlaybackReconciliation.ts-24-    tabId?: string | null,
app/home/usePlaybackReconciliation.ts-25-    mode?: "push" | "replace",
app/home/usePlaybackReconciliation.ts-26-  ) => void;
app/home/usePlaybackReconciliation.ts-27-  routerPush: (href: string) => void;
app/home/usePlaybackReconciliation.ts-28-}) {
app/home/usePlaybackReconciliation.ts-29-  const {
app/home/usePlaybackReconciliation.ts-30-    player,
app/home/usePlaybackReconciliation.ts-31-    bundle,
app/home/usePlaybackReconciliation.ts-32-    route,
app/home/usePlaybackReconciliation.ts-33-    isPlayer,
app/home/usePlaybackReconciliation.ts-34-    sp,
app/home/usePlaybackReconciliation.ts:35:    patchQuery,
app/home/usePlaybackReconciliation.ts-36-    forceSurface,
app/home/usePlaybackReconciliation.ts-37-    routerPush,
app/home/usePlaybackReconciliation.ts-38-  } = props;
app/home/usePlaybackReconciliation.ts-39-
app/home/usePlaybackReconciliation.ts-40-  const { setQueue, play, selectTrack } = player;
app/home/usePlaybackReconciliation.ts-41-
app/home/usePlaybackReconciliation.ts-42-  const qAlbum = (isPlayer ? route.albumSlug : null) ?? null;
app/home/usePlaybackReconciliation.ts-43-  const qDisplayId = (isPlayer ? route.displayId : null) ?? null;
app/home/usePlaybackReconciliation.ts-44-
app/home/usePlaybackReconciliation.ts-45-  const qTrackRecordingId = React.useMemo(() => {
--
app/home/usePlaybackReconciliation.ts-119-    const selectedRecordingId = player.selectedRecordingId ?? null;
app/home/usePlaybackReconciliation.ts-120-    const pendingRecordingId = player.pendingRecordingId ?? null;
app/home/usePlaybackReconciliation.ts-121-
app/home/usePlaybackReconciliation.ts-122-    if (
app/home/usePlaybackReconciliation.ts-123-      selectedRecordingId === qTrackRecordingId ||
app/home/usePlaybackReconciliation.ts-124-      pendingRecordingId === qTrackRecordingId
app/home/usePlaybackReconciliation.ts-125-    ) {
app/home/usePlaybackReconciliation.ts-126-      return;
app/home/usePlaybackReconciliation.ts-127-    }
app/home/usePlaybackReconciliation.ts-128-
app/home/usePlaybackReconciliation.ts:129:    selectTrack(qTrackRecordingId);
app/home/usePlaybackReconciliation.ts-130-  }, [
app/home/usePlaybackReconciliation.ts-131-    qTrackRecordingId,
app/home/usePlaybackReconciliation.ts-132-    player.selectedRecordingId,
app/home/usePlaybackReconciliation.ts-133-    player.pendingRecordingId,
app/home/usePlaybackReconciliation.ts-134-    selectTrack,
app/home/usePlaybackReconciliation.ts-135-  ]);
app/home/usePlaybackReconciliation.ts-136-
app/home/usePlaybackReconciliation.ts-137-  const primedAlbumKeyRef = React.useRef<string | null>(null);
app/home/usePlaybackReconciliation.ts-138-  React.useEffect(() => {
app/home/usePlaybackReconciliation.ts-139-    if (!album || tracks.length === 0) return;
--
app/home/usePlaybackReconciliation.ts-153-
app/home/usePlaybackReconciliation.ts-154-    const first = tracks[0];
app/home/usePlaybackReconciliation.ts-155-    if (!first?.recordingId) return;
app/home/usePlaybackReconciliation.ts-156-
app/home/usePlaybackReconciliation.ts-157-    const ctxId = hasSt
app/home/usePlaybackReconciliation.ts-158-      ? (album.catalogueId ?? undefined)
app/home/usePlaybackReconciliation.ts-159-      : (album.catalogueId ?? album.id ?? undefined);
app/home/usePlaybackReconciliation.ts-160-
app/home/usePlaybackReconciliation.ts-161-    const ctxSlug = qAlbum ?? currentAlbumSlug;
app/home/usePlaybackReconciliation.ts-162-
app/home/usePlaybackReconciliation.ts:163:    player.setQueue(tracks, {
app/home/usePlaybackReconciliation.ts-164-      contextId: ctxId,
app/home/usePlaybackReconciliation.ts-165-      contextSlug: ctxSlug,
app/home/usePlaybackReconciliation.ts-166-      contextTitle: album.title ?? undefined,
app/home/usePlaybackReconciliation.ts-167-      contextArtist: album.artist ?? undefined,
app/home/usePlaybackReconciliation.ts-168-      artworkUrl: album.artworkUrl ?? null,
app/home/usePlaybackReconciliation.ts-169-    });
app/home/usePlaybackReconciliation.ts-170-
app/home/usePlaybackReconciliation.ts-171-    const selectedRecordingId = player.selectedRecordingId ?? null;
app/home/usePlaybackReconciliation.ts-172-    const pendingRecordingId = player.pendingRecordingId ?? null;
app/home/usePlaybackReconciliation.ts-173-
app/home/usePlaybackReconciliation.ts-174-    const alreadySelected = selectedRecordingId === first.recordingId;
app/home/usePlaybackReconciliation.ts-175-    const alreadyPending = pendingRecordingId === first.recordingId;
app/home/usePlaybackReconciliation.ts-176-
app/home/usePlaybackReconciliation.ts-177-    if (!alreadySelected && !alreadyPending) {
app/home/usePlaybackReconciliation.ts:178:      player.selectTrack(first.recordingId);
app/home/usePlaybackReconciliation.ts-179-    }
app/home/usePlaybackReconciliation.ts-180-
app/home/usePlaybackReconciliation.ts-181-    primedAlbumKeyRef.current = primeKey;
app/home/usePlaybackReconciliation.ts-182-  }, [
app/home/usePlaybackReconciliation.ts-183-    album,
app/home/usePlaybackReconciliation.ts-184-    tracks,
app/home/usePlaybackReconciliation.ts-185-    hasSt,
app/home/usePlaybackReconciliation.ts-186-    qAlbum,
app/home/usePlaybackReconciliation.ts-187-    currentAlbumSlug,
app/home/usePlaybackReconciliation.ts-188-    qDisplayId,
--
app/home/usePlaybackReconciliation.ts-191-    player,
app/home/usePlaybackReconciliation.ts-192-  ]);
app/home/usePlaybackReconciliation.ts-193-
app/home/usePlaybackReconciliation.ts-194-  const autoplayFiredRef = React.useRef<string | null>(null);
app/home/usePlaybackReconciliation.ts-195-  React.useEffect(() => {
app/home/usePlaybackReconciliation.ts-196-    if (!isPlayer) return;
app/home/usePlaybackReconciliation.ts-197-    if (!qAutoplay) return;
app/home/usePlaybackReconciliation.ts-198-    if (!qTrackRecordingId) return;
app/home/usePlaybackReconciliation.ts-199-
app/home/usePlaybackReconciliation.ts-200-    if (!qShareToken) {
app/home/usePlaybackReconciliation.ts:201:      patchQuery({ autoplay: null });
app/home/usePlaybackReconciliation.ts-202-      return;
app/home/usePlaybackReconciliation.ts-203-    }
app/home/usePlaybackReconciliation.ts-204-
app/home/usePlaybackReconciliation.ts-205-    if (!album || tracks.length === 0) return;
app/home/usePlaybackReconciliation.ts-206-
app/home/usePlaybackReconciliation.ts-207-    const key = `${qAlbum ?? ""}:${qTrackRecordingId}:${qShareToken}`;
app/home/usePlaybackReconciliation.ts-208-    if (autoplayFiredRef.current === key) return;
app/home/usePlaybackReconciliation.ts-209-    autoplayFiredRef.current = key;
app/home/usePlaybackReconciliation.ts-210-
app/home/usePlaybackReconciliation.ts-211-    const ctxId = hasSt
app/home/usePlaybackReconciliation.ts-212-      ? (album.catalogueId ?? undefined)
app/home/usePlaybackReconciliation.ts-213-      : (album.catalogueId ?? album.id ?? undefined);
app/home/usePlaybackReconciliation.ts-214-
app/home/usePlaybackReconciliation.ts-215-    const ctxSlug = qAlbum ?? currentAlbumSlug;
app/home/usePlaybackReconciliation.ts-216-
app/home/usePlaybackReconciliation.ts:217:    setQueue(tracks, {
app/home/usePlaybackReconciliation.ts-218-      contextId: ctxId,
app/home/usePlaybackReconciliation.ts-219-      contextSlug: ctxSlug,
app/home/usePlaybackReconciliation.ts-220-      contextTitle: album.title ?? undefined,
app/home/usePlaybackReconciliation.ts-221-      contextArtist: album.artist ?? undefined,
app/home/usePlaybackReconciliation.ts-222-      artworkUrl: album.artworkUrl ?? null,
app/home/usePlaybackReconciliation.ts-223-    });
app/home/usePlaybackReconciliation.ts-224-
app/home/usePlaybackReconciliation.ts-225-    const selectedTrack =
app/home/usePlaybackReconciliation.ts-226-      tracks.find((track) => track.recordingId === qTrackRecordingId) ?? null;
app/home/usePlaybackReconciliation.ts-227-
app/home/usePlaybackReconciliation.ts-228-    if (selectedTrack) {
app/home/usePlaybackReconciliation.ts-229-      play(selectedTrack);
app/home/usePlaybackReconciliation.ts-230-    }
app/home/usePlaybackReconciliation.ts-231-
app/home/usePlaybackReconciliation.ts:232:    patchQuery({ autoplay: null });
app/home/usePlaybackReconciliation.ts-233-  }, [
app/home/usePlaybackReconciliation.ts-234-    isPlayer,
app/home/usePlaybackReconciliation.ts-235-    qAutoplay,
app/home/usePlaybackReconciliation.ts-236-    qTrackRecordingId,
app/home/usePlaybackReconciliation.ts-237-    qAlbum,
app/home/usePlaybackReconciliation.ts-238-    qShareToken,
app/home/usePlaybackReconciliation.ts-239-    album,
app/home/usePlaybackReconciliation.ts-240-    tracks,
app/home/usePlaybackReconciliation.ts-241-    hasSt,
app/home/usePlaybackReconciliation.ts-242-    currentAlbumSlug,
app/home/usePlaybackReconciliation.ts-243-    play,
app/home/usePlaybackReconciliation.ts-244-    setQueue,
app/home/usePlaybackReconciliation.ts:245:    patchQuery,
app/home/usePlaybackReconciliation.ts-246-  ]);
app/home/usePlaybackReconciliation.ts-247-
app/home/usePlaybackReconciliation.ts-248-  React.useEffect(() => {
app/home/usePlaybackReconciliation.ts-249-    const onOpen = (event: Event) => {
app/home/usePlaybackReconciliation.ts-250-      const customEvent = event as CustomEvent<{ albumSlug?: string | null }>;
app/home/usePlaybackReconciliation.ts-251-      const slug = customEvent.detail?.albumSlug ?? null;
app/home/usePlaybackReconciliation.ts-252-
app/home/usePlaybackReconciliation.ts-253-      if (slug) {
app/home/usePlaybackReconciliation.ts-254-        onSelectAlbum(slug);
app/home/usePlaybackReconciliation.ts-255-        return;
app/home/usePlaybackReconciliation.ts-256-      }
app/home/usePlaybackReconciliation.ts-257-
app/home/usePlaybackReconciliation.ts-258-      forceSurface("player");
app/home/usePlaybackReconciliation.ts-259-    };
app/home/usePlaybackReconciliation.ts-260-
app/home/usePlaybackReconciliation.ts:261:    window.addEventListener("af:open-player", onOpen as EventListener);
app/home/usePlaybackReconciliation.ts-262-    return () =>
app/home/usePlaybackReconciliation.ts:263:      window.removeEventListener("af:open-player", onOpen as EventListener);
app/home/usePlaybackReconciliation.ts-264-  }, [onSelectAlbum, forceSurface]);
app/home/usePlaybackReconciliation.ts-265-
app/home/usePlaybackReconciliation.ts-266-  return {
app/home/usePlaybackReconciliation.ts-267-    onSelectAlbum,
app/home/usePlaybackReconciliation.ts-268-  };
app/home/usePlaybackReconciliation.ts-269-}
--
app/home/player/playbackAccessClient.ts-18-export type PlaybackAccessDecision = {
app/home/player/playbackAccessClient.ts-19-  forCatalogueId: string;
app/home/player/playbackAccessClient.ts-20-  allowed: boolean;
app/home/player/playbackAccessClient.ts-21-  embargoed: boolean;
app/home/player/playbackAccessClient.ts-22-  releaseAt: string | null;
app/home/player/playbackAccessClient.ts-23-  code?: string;
app/home/player/playbackAccessClient.ts-24-  action?: PlaybackAccessAction;
app/home/player/playbackAccessClient.ts-25-  reason?: string;
app/home/player/playbackAccessClient.ts-26-  corr: string | null;
app/home/player/playbackAccessClient.ts-27-  shareTokenAccess: PlaybackShareTokenAccess | null;
app/home/player/playbackAccessClient.ts:28:  sharePlaybackContext: string | null;
app/home/player/playbackAccessClient.ts:29:  sharePlaybackScopeId: string | null;
app/home/player/playbackAccessClient.ts-30-};
app/home/player/playbackAccessClient.ts-31-
app/home/player/playbackAccessClient.ts-32-export type PlaybackAccessRequest = {
app/home/player/playbackAccessClient.ts-33-  catalogueId: string;
app/home/player/playbackAccessClient.ts-34-  shareToken: string | null;
app/home/player/playbackAccessClient.ts:35:  accessIdentityKey: string;
app/home/player/playbackAccessClient.ts-36-};
app/home/player/playbackAccessClient.ts-37-
app/home/player/playbackAccessClient.ts-38-type PlaybackAccessResponse = {
app/home/player/playbackAccessClient.ts-39-  allowed?: boolean;
app/home/player/playbackAccessClient.ts-40-  embargoed?: boolean;
app/home/player/playbackAccessClient.ts-41-  releaseAt?: string | null;
app/home/player/playbackAccessClient.ts-42-  code?: string | null;
app/home/player/playbackAccessClient.ts-43-  action?: string | null;
app/home/player/playbackAccessClient.ts-44-  reason?: string | null;
app/home/player/playbackAccessClient.ts-45-  shareTokenAccess?: unknown;
app/home/player/playbackAccessClient.ts:46:  sharePlaybackContext?: unknown;
app/home/player/playbackAccessClient.ts:47:  sharePlaybackScopeId?: unknown;
app/home/player/playbackAccessClient.ts-48-};
app/home/player/playbackAccessClient.ts-49-
app/home/player/playbackAccessClient.ts-50-type CachedPlaybackAccessDecision = {
app/home/player/playbackAccessClient.ts-51-  decision: PlaybackAccessDecision;
app/home/player/playbackAccessClient.ts-52-  expiresAtMs: number;
app/home/player/playbackAccessClient.ts-53-};
app/home/player/playbackAccessClient.ts-54-
app/home/player/playbackAccessClient.ts-55-const PLAYBACK_ACCESS_CACHE_TTL_MS = 60_000;
app/home/player/playbackAccessClient.ts-56-
app/home/player/playbackAccessClient.ts-57-const decisionsByKey = new Map<string, CachedPlaybackAccessDecision>();
--
app/home/player/playbackAccessClient.ts-79-function normalizeShareToken(value: string | null): string | null {
app/home/player/playbackAccessClient.ts-80-  const token = (value ?? "").trim();
app/home/player/playbackAccessClient.ts-81-  return token || null;
app/home/player/playbackAccessClient.ts-82-}
app/home/player/playbackAccessClient.ts-83-
app/home/player/playbackAccessClient.ts-84-function playbackAccessKey(request: PlaybackAccessRequest): string {
app/home/player/playbackAccessClient.ts-85-  const catalogueId = normalizeCatalogueId(request.catalogueId);
app/home/player/playbackAccessClient.ts-86-  const shareToken = normalizeShareToken(request.shareToken);
app/home/player/playbackAccessClient.ts-87-
app/home/player/playbackAccessClient.ts-88-  return `${catalogueId}::st=${shareToken ?? ""}::identity=${
app/home/player/playbackAccessClient.ts:89:    request.accessIdentityKey
app/home/player/playbackAccessClient.ts-90-  }`;
app/home/player/playbackAccessClient.ts-91-}
app/home/player/playbackAccessClient.ts-92-
app/home/player/playbackAccessClient.ts-93-function parseShareTokenAccess(
app/home/player/playbackAccessClient.ts-94-  value: unknown,
app/home/player/playbackAccessClient.ts-95-): PlaybackShareTokenAccess | null {
app/home/player/playbackAccessClient.ts-96-  if (!value || typeof value !== "object") return null;
app/home/player/playbackAccessClient.ts-97-
app/home/player/playbackAccessClient.ts-98-  const raw = value as Record<string, unknown>;
app/home/player/playbackAccessClient.ts-99-  const hasExpiresAt = Object.hasOwn(raw, "expiresAt");
--
app/home/player/playbackAccessClient.ts-180-  const catalogueId = normalizeCatalogueId(request.catalogueId);
app/home/player/playbackAccessClient.ts-181-
app/home/player/playbackAccessClient.ts-182-  if (!catalogueId) {
app/home/player/playbackAccessClient.ts-183-    throw new Error("Playback access requires a catalogue ID.");
app/home/player/playbackAccessClient.ts-184-  }
app/home/player/playbackAccessClient.ts-185-
app/home/player/playbackAccessClient.ts-186-  const shareToken = normalizeShareToken(request.shareToken);
app/home/player/playbackAccessClient.ts-187-  const key = playbackAccessKey({
app/home/player/playbackAccessClient.ts-188-    catalogueId,
app/home/player/playbackAccessClient.ts-189-    shareToken,
app/home/player/playbackAccessClient.ts:190:    accessIdentityKey: request.accessIdentityKey,
app/home/player/playbackAccessClient.ts-191-  });
app/home/player/playbackAccessClient.ts-192-
app/home/player/playbackAccessClient.ts-193-  const cached = getFreshCachedPlaybackAccessDecision(key);
app/home/player/playbackAccessClient.ts-194-  if (cached) return cached;
app/home/player/playbackAccessClient.ts-195-
app/home/player/playbackAccessClient.ts-196-  const existing = inFlightByKey.get(key);
app/home/player/playbackAccessClient.ts-197-  if (existing) return existing;
app/home/player/playbackAccessClient.ts-198-
app/home/player/playbackAccessClient.ts-199-  const promise = (async () => {
app/home/player/playbackAccessClient.ts-200-    const url = new URL("/api/access/check", window.location.origin);
--
app/home/player/playbackAccessClient.ts-220-        typeof body.code === "string" && body.code.trim()
app/home/player/playbackAccessClient.ts-221-          ? body.code
app/home/player/playbackAccessClient.ts-222-          : undefined,
app/home/player/playbackAccessClient.ts-223-      action: isPlaybackAccessAction(body.action) ? body.action : undefined,
app/home/player/playbackAccessClient.ts-224-      reason:
app/home/player/playbackAccessClient.ts-225-        typeof body.reason === "string" && body.reason.trim()
app/home/player/playbackAccessClient.ts-226-          ? body.reason
app/home/player/playbackAccessClient.ts-227-          : undefined,
app/home/player/playbackAccessClient.ts-228-      corr: response.headers.get("x-correlation-id") ?? null,
app/home/player/playbackAccessClient.ts-229-      shareTokenAccess: parseShareTokenAccess(body.shareTokenAccess),
app/home/player/playbackAccessClient.ts:230:      sharePlaybackContext: parseSharePlaybackContext(
app/home/player/playbackAccessClient.ts:231:        body.sharePlaybackContext,
app/home/player/playbackAccessClient.ts-232-      ),
app/home/player/playbackAccessClient.ts:233:      sharePlaybackScopeId: parseSharePlaybackScopeId(
app/home/player/playbackAccessClient.ts:234:        body.sharePlaybackScopeId,
app/home/player/playbackAccessClient.ts-235-      ),
app/home/player/playbackAccessClient.ts-236-    };
app/home/player/playbackAccessClient.ts-237-
app/home/player/playbackAccessClient.ts-238-    decisionsByKey.set(key, {
app/home/player/playbackAccessClient.ts-239-      decision,
app/home/player/playbackAccessClient.ts-240-      expiresAtMs: Date.now() + PLAYBACK_ACCESS_CACHE_TTL_MS,
app/home/player/playbackAccessClient.ts-241-    });
app/home/player/playbackAccessClient.ts-242-
app/home/player/playbackAccessClient.ts-243-    return decision;
app/home/player/playbackAccessClient.ts-244-  })().finally(() => {

===== RELEVANT TEST REFERENCES =====



===== TYPESCRIPT / PACKAGE / ESLINT CONFIG =====


----- tsconfig.json -----
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules"]
}


----- package.json -----
{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.962.0",
    "@aws-sdk/s3-request-presigner": "^3.962.0",
    "@clerk/nextjs": "^6.36.5",
    "@mux/mux-node": "^12.8.1",
    "@portabletext/react": "^6.0.2",
    "@portabletext/types": "^4.0.1",
    "@react-email/components": "^1.0.6",
    "@react-email/render": "^2.0.4",
    "@sanity/client": "^7.13.2",
    "@sanity/image-url": "^1.2.0",
    "@sanity/vision": "^4.22.0",
    "@tiptap/extension-link": "^3.20.0",
    "@tiptap/extension-placeholder": "^3.20.0",
    "@tiptap/react": "^3.20.0",
    "@tiptap/starter-kit": "^3.20.0",
    "@vercel/postgres": "^0.10.0",
    "hls.js": "^1.6.15",
    "jose": "^6.1.3",
    "next": "16.1.1",
    "next-sanity": "^11.6.12",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "react-email": "^5.2.5",
    "resend": "^6.8.0",
    "sanity": "^4.22.0",
    "stripe": "^20.1.0",
    "styled-components": "^6.1.19",
    "svix": "^1.84.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.1",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}


----- eslint.config.mjs -----
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;


===== CURRENT GIT STATUS =====

?? sonarqube-export-20260810-132041/

===== CURRENT GIT DIFF — DASHBOARD =====



===== STAGED GIT DIFF — DASHBOARD =====



===== CURRENT GIT DIFF — RECONCILIATION =====



===== STAGED GIT DIFF — RECONCILIATION =====



===== DIFF CHECK — TARGET SCOPE =====


