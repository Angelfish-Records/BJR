BJR PATCH EVIDENCE BUNDLE — ENSURE LYRICS FOR TRACK
PWD: /Users/brendan/Projects/Angelfish-Records/BJR/web
Sonar source: sonarqube-export-20260810-132041/sonarqube-assistant-bundle.json


===== FRESH SONAR FINDINGS — EXACT TARGET =====

app/home/player/lyrics/ensureLyricsForTrack.ts:12  typescript:S6653  severity=MINOR  impact=LOW
  Use 'Object.hasOwn()' instead of 'Object.prototype.hasOwnProperty.call()'.

app/home/player/lyrics/ensureLyricsForTrack.ts:21  typescript:S3776  severity=CRITICAL  impact=HIGH
  Refactor this function to reduce its Cognitive Complexity from 18 to the 15 allowed.
  flow: line=24 +1
  flow: line=35 +1
  flow: line=37 +2 (incl. 1 for nesting)
  flow: line=39 +2 (incl. 1 for nesting)
  flow: line=41 +2 (incl. 1 for nesting)
  flow: line=45 +1
  flow: line=47 +2 (incl. 1 for nesting)
  flow: line=49 +2 (incl. 1 for nesting)
  flow: line=53 +2 (incl. 1 for nesting)
  flow: line=52 +1
  flow: line=60 +1
  flow: line=60 +1

app/home/player/lyrics/ensureLyricsForTrack.ts:117  typescript:S3358  severity=MAJOR  impact=MEDIUM
  Extract this nested ternary operation into an independent statement.

TOTAL TARGET FINDINGS: 3

===== CURRENT SHA-256 — PATCH TARGET =====

49cf523167968d2dfb2dc8e4810232bd1ee535793316cc0ea48864eff5871cdc  app/home/player/lyrics/ensureLyricsForTrack.ts

===== COMPLETE CURRENT SOURCE — PATCH TARGET =====

----- app/home/player/lyrics/ensureLyricsForTrack.ts -----
// web/app/home/player/lyrics/ensureLyricsForTrack.ts
"use client";

import { lyricsSurface } from "./lyricsSurface";
import { fetchLyricsByrecordingId } from "./fetchLyricsByRecordingId";
import type { AlbumLyricsBundle } from "@/lib/types";

// Module-scope in-flight map so *any* caller (any surface) dedupes fetches.
const inFlight = new Map<string, AbortController>();

function hasOwn(obj: object, key: string): boolean {
  return Object.prototype.hasOwnProperty.call(obj, key);
}

/**
 * Prime lyricsSurface from an album-provided bundle.
 * - Merges without clobbering existing per-track entries (unless missing).
 * - Ensures we still preserve "known no lyrics" as [] if already present.
 * - No network; safe to call repeatedly (idempotent-ish).
 */
export function primeLyricsFromAlbumBundle(
  bundle: AlbumLyricsBundle | null | undefined,
) {
  if (!bundle) return;

  const snap = lyricsSurface.getSnapshot();

  const cuesIn = bundle.cuesByRecordingId ?? {};
  const offIn = bundle.offsetByRecordingId ?? {};

  // Build merged maps but only add keys that are currently unknown.
  let cuesOut = snap.cuesByRecordingId;
  let offOut = snap.offsetByRecordingId;

  for (const [recordingIdRaw, cuesRaw] of Object.entries(cuesIn)) {
    const id = (recordingIdRaw ?? "").trim();
    if (!id) continue;

    if (hasOwn(cuesOut, id)) continue; // don't overwrite existing (including [] known-no-lyrics)

    const cues = Array.isArray(cuesRaw) ? cuesRaw : [];
    cuesOut = { ...cuesOut, [id]: cues };
  }

  for (const [recordingIdRaw, offRaw] of Object.entries(offIn)) {
    const id = (recordingIdRaw ?? "").trim();
    if (!id) continue;

    if (hasOwn(offOut, id)) continue; // don't overwrite existing

    const n =
      typeof offRaw === "number" && Number.isFinite(offRaw)
        ? Math.floor(offRaw)
        : 0;

    offOut = { ...offOut, [id]: n };
  }

  // Only write if something changed (cheap reference check).
  if (cuesOut === snap.cuesByRecordingId && offOut === snap.offsetByRecordingId) return;

  lyricsSurface.setMaps({
    cuesByrecordingId: cuesOut,
    offsetByrecordingId: offOut,
    globalOffsetMs: snap.globalOffsetMs,
  });
}

/**
 * Ensure lyrics (cues + offset) exist in lyricsSurface for recordingId.
 * - Dedupes concurrent calls (module-level inFlight).
 * - Caches "no lyrics" as [] to avoid refetch loops.
 * - Safe to call repeatedly from any surface.
 */
export async function ensureLyricsForTrack(
  recordingId: string,
  opts?: { signal?: AbortSignal },
): Promise<void> {
  const id = (recordingId ?? "").trim();
  if (!id) return;

  const snap = lyricsSurface.getSnapshot();
  const existing = snap.cuesByRecordingId[id];

  const knownKey = hasOwn(snap.cuesByRecordingId, id);
  const hasCues = Array.isArray(existing) && existing.length > 0;
  const knownNoLyrics =
    knownKey && Array.isArray(existing) && existing.length === 0;

  if (hasCues || knownNoLyrics) return;

  // Already fetching?
  if (inFlight.has(id)) return;

  const ac = new AbortController();
  inFlight.set(id, ac);

  // Caller abort should NOT abort the shared in-flight request.
  // It only means "this caller no longer cares". The module-scope fetch may be
  // servicing other callers (PlayerState, StageInline, etc).
  const outerSignal = opts?.signal;
  if (outerSignal?.aborted) {
    inFlight.delete(id);
    return;
  }

  try {
    const r = await fetchLyricsByrecordingId(id, ac.signal);
    if (!r) return;
    if (r.recordingId !== id) return;

    const prev = lyricsSurface.getSnapshot();

    const nextCuesByrecordingId =
      Array.isArray(r.cues) && r.cues.length
        ? { ...prev.cuesByRecordingId, [id]: r.cues }
        : hasOwn(prev.cuesByRecordingId, id)
          ? prev.cuesByRecordingId
          : { ...prev.cuesByRecordingId, [id]: [] };

    const nextOffsetByrecordingId =
      typeof r.offsetMs === "number" && Number.isFinite(r.offsetMs)
        ? { ...prev.offsetByRecordingId, [id]: r.offsetMs }
        : prev.offsetByRecordingId;

    lyricsSurface.setMaps({
      cuesByrecordingId: nextCuesByrecordingId,
      offsetByrecordingId: nextOffsetByrecordingId,
      // preserve whatever globalOffsetMs currently is (lyricsSurface handles defaulting)
      globalOffsetMs: prev.globalOffsetMs,
    });
  } finally {
    inFlight.delete(id);
  }
}


===== DIRECT CALLERS DISCOVERED =====

app/home/player/FullPlayer.tsx
app/home/player/PlayerState.tsx
app/home/player/StageInline.tsx
app/home/player/lyrics/ensureLyricsForTrack.ts

===== COMPLETE CURRENT SOURCE — CALLERS / DEPENDENCIES =====


----- app/home/player/FullPlayer.tsx -----
// web/app/home/player/FullPlayer.tsx
"use client";

import React from "react";
import { useAuth } from "@clerk/nextjs";
import { usePathname, useRouter, useSearchParams } from "next/navigation";
import { usePlayer } from "./PlayerState";
import {
  fetchPlaybackAccessDecision,
  getCachedPlaybackAccessDecision,
  isPlaybackAccessAction,
  type PlaybackAccessDecision as AccessState,
} from "./playbackAccessClient";
import { useGateBroker } from "@/app/home/gating/GateBroker";
import type { GateAction, GateCodeRaw } from "@/app/home/gating/gateTypes";
import type {
  AlbumPlayerBundle,
  AlbumNavItem,
  Tier,
  TierName,
  PlayerTrack,
} from "@/lib/types";
import { primeLyricsFromAlbumBundle } from "./lyrics/ensureLyricsForTrack";
import { deriveShareContext, shareAlbum, shareTrack } from "./share";
import { PatternRingGlow } from "./VisualizerPattern";

const LIVE_LISTENER_COUNT_ENABLED =
  process.env.NEXT_PUBLIC_LIVE_LISTENER_COUNT_ENABLED === "true";

function fmtTime(ms: number) {
  const s = Math.max(0, Math.floor(ms / 1000));
  const m = Math.floor(s / 60);
  const r = s % 60;
  return `${m}:${String(r).padStart(2, "0")}`;
}

function fmtActiveListeners(value: number): string {
  const n = Number.isFinite(value) && value > 0 ? Math.floor(value) : 0;
  return n === 1 ? "1 listening now" : `${n} listening now`;
}

function tierRank(t: Tier): number {
  if (t === "partner") return 3;
  if (t === "patron") return 2;
  if (t === "friend") return 1;
  return 0;
}

function tierLabel(t: TierName): string {
  if (t === "friend") return "Friend+";
  if (t === "patron") return "Patron+";
  return "Partner+";
}

function IconCircleBtn(
  props: Readonly<{
    label: string;
    onClick?: () => void;
    disabled?: boolean;
    size?: number;
    children: React.ReactNode;
  }>,
) {
  const { label, onClick, disabled, size = 44, children } = props;
  return (
    <button
      type="button"
      aria-label={label}
      title={label}
      disabled={disabled}
      onClick={disabled ? undefined : onClick}
      style={{
        width: size,
        height: size,
        borderRadius: 999,
        border: "1px solid rgba(255,255,255,0.14)",
        background: "rgba(255,255,255,0.05)",
        color: "rgba(255,255,255,0.92)",
        display: "grid",
        placeItems: "center",
        cursor: disabled ? "default" : "pointer",
        opacity: disabled ? 0.45 : 0.9,
        userSelect: "none",
        transform: "translateZ(0)",
      }}
    >
      {children}
    </button>
  );
}

function PlayPauseBig({ playing }: Readonly<{ playing: boolean }>) {
  return playing ? (
    <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor">
      <rect x="6.6" y="5" width="4.2" height="14" rx="1.3" />
      <rect x="13.2" y="5" width="4.2" height="14" rx="1.3" />
    </svg>
  ) : (
    <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor">
      <polygon points="9,7 19,12 9,17" />
    </svg>
  );
}

function DownloadIcon() {
  return (
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
      <path
        d="M12 3v10"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
      />
      <path
        d="M8 11l4 4 4-4"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
      <path
        d="M5 20h14"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
      />
    </svg>
  );
}

function PrevIcon() {
  return (
    <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
      <rect x="6" y="6" width="2" height="12" />
      <polygon points="18,7 10,12 18,17" />
    </svg>
  );
}

function NextIcon() {
  return (
    <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
      <rect x="16" y="6" width="2" height="12" />
      <polygon points="6,7 14,12 6,17" />
    </svg>
  );
}

function ShareIcon() {
  return (
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
      <path
        d="M16 8a3 3 0 1 0-2.9-3.7A3 3 0 0 0 16 8Z"
        stroke="currentColor"
        strokeWidth="2"
      />
      <path
        d="M6 14a3 3 0 1 0-2.9-3.7A3 3 0 0 0 6 14Z"
        stroke="currentColor"
        strokeWidth="2"
      />
      <path
        d="M16 22a3 3 0 1 0-2.9-3.7A3 3 0 0 0 16 22Z"
        stroke="currentColor"
        strokeWidth="2"
      />
      <path
        d="M8.7 11.2l5-3"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
      />
      <path
        d="M8.7 12.8l5 3"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
      />
    </svg>
  );
}

function NowPlayingPip() {
  return (
    <span className="afEq" aria-hidden="true">
      <i />
      <i />
      <i />
    </span>
  );
}

function TrackInlinePlayIcon() {
  return (
    <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
      <polygon points="7.5,5.5 19,12 7.5,18.5" />
    </svg>
  );
}

const PLAYBACK_GATE_CODES = [
  "AUTH_REQUIRED",
  "PLAYBACK_CAP_REACHED",
  "ANON_CAP_REACHED",
  "CAP_REACHED",
] as const;

function isPlaybackGateCode(v: unknown): v is GateCodeRaw {
  return (
    typeof v === "string" &&
    (PLAYBACK_GATE_CODES as readonly string[]).includes(v)
  );
}

function toGateAction(v: unknown): GateAction {
  return isPlaybackAccessAction(v) ? v : "login";
}

function playbackGateMessage(reason: string | null | undefined): string {
  const trimmed = reason?.trim();
  return trimmed || "Enter your email address to continue listening.";
}

function formatShareTokenExpiry(iso: string | null): string | null {
  if (!iso) return null;

  const ms = Date.parse(iso);
  if (!Number.isFinite(ms)) return null;

  return new Intl.DateTimeFormat(undefined, {
    day: "numeric",
    month: "long",
    year: "numeric",
    hour: "numeric",
    minute: "2-digit",
  }).format(new Date(ms));
}

function formatRedemptionLimit(maxRedemptions: number | null): string | null {
  if (maxRedemptions == null) return null;

  return `The link will work ${maxRedemptions} time${
    maxRedemptions === 1 ? "" : "s"
  }.`;
}

type StableView = AlbumPlayerBundle;

type StreamingPlatform =
  | "spotify"
  | "appleMusic"
  | "youtubeMusic"
  | "amazonMusic"
  | "tidal"
  | "deezer";

type PlatformLink = { platform: StreamingPlatform; url: string };

function isStreamingPlatform(v: unknown): v is StreamingPlatform {
  return (
    v === "spotify" ||
    v === "appleMusic" ||
    v === "youtubeMusic" ||
    v === "amazonMusic" ||
    v === "tidal" ||
    v === "deezer"
  );
}

function isPlatformLink(v: unknown): v is PlatformLink {
  if (typeof v !== "object" || v === null) return false;

  const maybe = v as Record<string, unknown>;

  return (
    isStreamingPlatform(maybe.platform) &&
    typeof maybe.url === "string" &&
    /^https:\/\//i.test(maybe.url)
  );
}

function normalizePlatformLinks(
  links:
    | NonNullable<AlbumPlayerBundle["album"]>["platformLinks"]
    | null
    | undefined,
): PlatformLink[] {
  if (!Array.isArray(links)) return [];
  return links.filter(isPlatformLink);
}
function PlatformIcon({ platform }: Readonly<{ platform: StreamingPlatform }>) {
  const common = { width: 18, height: 18, "aria-hidden": true as const };

  switch (platform) {
    case "spotify":
      return (
        <svg {...common} viewBox="0 0 24 24">
          <path
            fill="currentColor"
            d="M12 0C5.4 0 0 5.4 0 12s5.4 12 12 12 12-5.4 12-12S18.66 0 12 0zm5.521 17.34c-.24.359-.66.48-1.021.24-2.82-1.74-6.36-2.101-10.561-1.141-.418.122-.779-.179-.899-.539-.12-.421.18-.78.54-.9 4.56-1.021 8.52-.6 11.64 1.32.42.18.479.659.301 1.02zm1.44-3.3c-.301.42-.841.6-1.262.3-3.239-1.98-8.159-2.58-11.939-1.38-.479.12-1.02-.12-1.14-.6-.12-.48.12-1.021.6-1.141C9.6 9.9 15 10.561 18.72 12.84c.361.181.54.78.241 1.2zm.12-3.36C15.24 8.4 8.82 8.16 5.16 9.301c-.6.179-1.2-.181-1.38-.721-.18-.601.18-1.2.72-1.381 4.26-1.26 11.28-1.02 15.721 1.621.539.3.719 1.02.419 1.56-.299.421-1.02.599-1.559.3z"
          />
        </svg>
      );

    case "appleMusic":
      return (
        <svg {...common} viewBox="0 0 24 24">
          <path
            fill="currentColor"
            d="M23.994 6.124a9.23 9.23 0 00-.24-2.19c-.317-1.31-1.062-2.31-2.18-3.043a5.022 5.022 0 00-1.877-.726 10.496 10.496 0 00-1.564-.15c-.04-.003-.083-.01-.124-.013H5.986c-.152.01-.303.017-.455.026-.747.043-1.49.123-2.193.4-1.336.53-2.3 1.452-2.865 2.78-.192.448-.292.925-.363 1.408-.056.392-.088.785-.1 1.18 0 .032-.007.062-.01.093v12.223c.01.14.017.283.027.424.05.815.154 1.624.497 2.373.65 1.42 1.738 2.353 3.234 2.801.42.127.856.187 1.293.228.555.053 1.11.06 1.667.06h11.03a12.5 12.5 0 001.57-.1c.822-.106 1.596-.35 2.295-.81a5.046 5.046 0 001.88-2.207c.186-.42.293-.87.37-1.324.113-.675.138-1.358.137-2.04-.002-3.8 0-7.595-.003-11.393zm-6.423 3.99v5.712c0 .417-.058.827-.244 1.206-.29.59-.76.962-1.388 1.14-.35.1-.706.157-1.07.173-.95.045-1.773-.6-1.943-1.536a1.88 1.88 0 011.038-2.022c.323-.16.67-.25 1.018-.324.378-.082.758-.153 1.134-.24.274-.063.457-.23.51-.516a.904.904 0 00.02-.193c0-1.815 0-3.63-.002-5.443a.725.725 0 00-.026-.185c-.04-.15-.15-.243-.304-.234-.16.01-.318.035-.475.066-.76.15-1.52.303-2.28.456l-2.325.47-1.374.278c-.016.003-.032.01-.048.013-.277.077-.377.203-.39.49-.002.042 0 .086 0 .13-.002 2.602 0 5.204-.003 7.805 0 .42-.047.836-.215 1.227-.278.64-.77 1.04-1.434 1.233-.35.1-.71.16-1.075.172-.96.036-1.755-.6-1.92-1.544-.14-.812.23-1.685 1.154-2.075.357-.15.73-.232 1.108-.31.287-.06.575-.116.86-.177.383-.083.583-.323.6-.714v-.15c0-2.96 0-5.922.002-8.882 0-.123.013-.25.042-.37.07-.285.273-.448.546-.518.255-.066.515-.112.774-.165.733-.15 1.466-.296 2.2-.444l2.27-.46c.67-.134 1.34-.27 2.01-.403.22-.043.442-.088.663-.106.31-.025.523.17.554.482.008.073.012.148.012.223.002 1.91.002 3.822 0 5.732z"
          />
        </svg>
      );

    case "youtubeMusic":
      return (
        <svg {...common} viewBox="0 0 24 24">
          <path
            fill="currentColor"
            d="M12 0C5.376 0 0 5.376 0 12s5.376 12 12 12 12-5.376 12-12S18.624 0 12 0zm0 19.104c-3.924 0-7.104-3.18-7.104-7.104S8.076 4.896 12 4.896s7.104 3.18 7.104 7.104-3.18 7.104-7.104 7.104zm0-13.332c-3.432 0-6.228 2.796-6.228 6.228S8.568 18.228 12 18.228s6.228-2.796 6.228-6.228S15.432 5.772 12 5.772zM9.684 15.54V8.46L15.816 12l-6.132 3.54z"
          />
        </svg>
      );

    case "tidal":
      return (
        <svg {...common} viewBox="0 0 24 24">
          <path
            fill="currentColor"
            d="M12.012 3.992L8.008 7.996 4.004 3.992 0 7.996 4.004 12l4.004-4.004L12.012 12l-4.004 4.004 4.004 4.004 4.004-4.004L12.012 12l4.004-4.004-4.004-4.004zM16.042 7.996l3.979-3.979L24 7.996l-3.979 3.979z"
          />
        </svg>
      );

    case "deezer":
      return (
        <svg {...common} viewBox="0 0 24 24">
          <path
            fill="currentColor"
            d="M.693 10.024c.381 0 .693-1.256.693-2.807 0-1.55-.312-2.807-.693-2.807C.312 4.41 0 5.666 0 7.217s.312 2.808.693 2.808ZM21.038 1.56c-.364 0-.684.805-.91 2.096C19.765 1.446 19.184 0 18.526 0c-.78 0-1.464 2.036-1.784 5-.312-2.158-.788-3.536-1.325-3.536-.745 0-1.386 2.704-1.62 6.472-.442-1.932-1.083-3.145-1.793-3.145s-1.35 1.213-1.793 3.145c-.242-3.76-.874-6.463-1.628-6.463-.537 0-1.013 1.378-1.325 3.535C6.938 2.036 6.262 0 5.474 0c-.658 0-1.247 1.447-1.602 3.665-.217-1.291-.546-2.105-.91-2.105-.675 0-1.221 2.807-1.221 6.272 0 3.466.546 6.273 1.221 6.273.277 0 .537-.476.736-1.273.32 2.928.996 4.938 1.776 4.938.606 0 1.143-1.204 1.507-3.11.251 3.622.875 6.195 1.602 6.195.46 0 .875-1.023 1.187-2.677C10.142 21.6 11 24 12.004 24c1.005 0 1.863-2.4 2.235-5.822.312 1.654.727 2.677 1.186 2.677.728 0 1.352-2.573 1.603-6.195.364 1.906.9 3.11 1.507 3.11.78 0 1.455-2.01 1.775-4.938.208.797.46 1.273.737 1.273.675 0 1.22-2.807 1.22-6.273-.008-3.457-.553-6.272-1.23-6.272ZM23.307 10.024c.381 0 .693-1.256.693-2.807 0-1.55-.312-2.807-.693-2.807-.381 0-.693 1.256-.693 2.807s.312 2.808.693 2.808Z"
          />
        </svg>
      );

    case "amazonMusic":
      return (
        <svg {...common} viewBox="0 0 32 32">
          <g transform="translate(16 16) scale(1.12) translate(-16 -16)">
            <path
              fill="currentColor"
              d="M16 2C8.27812 2 2 8.27812 2 16C2 23.7219 8.27812 30 16 30C23.7219 30 30 23.7219 30 16C30 8.27812 23.7219 2 16 2Z"
              opacity="0.18"
            />
            <path
              fill="currentColor"
              fillRule="evenodd"
              clipRule="evenodd"
              d="M20.8099 18.038C20.7688 17.979 20.7274 17.9213 20.6862 17.8639C20.3513 17.3971 20.0322 16.9521 20.0322 16.0603V12.7356C20.0322 12.62 20.0328 12.5052 20.0335 12.3913C20.0408 11.1166 20.0474 9.95337 19.1281 9.06351C18.3397 8.27737 17.0301 8 16.0286 8C14.0698 8 11.8846 8.75761 11.4264 11.2681C11.3779 11.5348 11.5646 11.6743 11.734 11.7136L13.7285 11.9374C13.9153 11.9276 14.0502 11.7372 14.0863 11.5455C14.2574 10.6804 14.956 10.2648 15.741 10.2648C16.1643 10.2648 16.6451 10.4253 16.8956 10.8191C17.154 11.2117 17.1499 11.7359 17.1462 12.2047C17.1458 12.2593 17.1454 12.3131 17.1454 12.3659V12.6446C16.9603 12.6661 16.7664 12.6864 16.5667 12.7073C15.4789 12.8214 14.2185 12.9535 13.2746 13.3826C11.9859 13.9614 11.0809 15.1391 11.0809 16.8701C11.0809 19.0885 12.4275 20.1975 14.1613 20.1975C15.6244 20.1975 16.4251 19.8394 17.5539 18.6462C17.6103 18.7309 17.6609 18.809 17.7087 18.8826C17.9779 19.2978 18.1537 19.5687 18.7337 20.0691C18.8868 20.1538 19.0894 20.1506 19.2251 20.0231C19.6361 19.6441 20.3824 18.9717 20.8031 18.6061C20.9712 18.4629 20.9414 18.2315 20.8099 18.038ZM16.7604 17.0792C16.4337 17.6812 15.9133 18.0495 15.3351 18.0495C14.5467 18.0495 14.0838 17.4253 14.0838 16.5018C14.0838 14.6838 15.6572 14.3538 17.145 14.3538C17.145 14.4626 17.146 14.572 17.1469 14.6816C17.1542 15.5046 17.1617 16.3439 16.7604 17.0792Z"
            />
            <path
              fill="currentColor"
              d="M23.2699 21.6659C21.2997 23.1894 18.4425 24 15.9818 24C12.5345 24 9.42926 22.6645 7.07958 20.4412C6.89528 20.2664 7.05978 20.0277 7.28121 20.1634C9.81666 21.7097 12.9516 22.6405 16.1893 22.6405C18.3738 22.6405 20.7746 22.1658 22.9836 21.1832C23.3168 21.0355 23.5961 21.4139 23.2699 21.6659Z"
            />
            <path
              fill="currentColor"
              d="M21.7891 20.6047C22.4234 20.5244 23.8379 20.3467 24.0897 20.6845C24.3419 21.022 23.8123 22.4099 23.5736 23.0355L23.5719 23.04C23.5001 23.2287 23.6545 23.304 23.8172 23.161C24.8745 22.2346 25.1477 20.2928 24.931 20.0114C24.7161 19.7339 22.8677 19.4936 21.7398 20.3239C21.5664 20.4527 21.5961 20.6282 21.7891 20.6047Z"
            />
          </g>
        </svg>
      );

    default:
      return null;
  }
}

function AvailableOnRibbon({
  links,
}: Readonly<{ links: readonly PlatformLink[] }>) {
  if (!links.length) return null;

  const order: StreamingPlatform[] = [
    "spotify",
    "appleMusic",
    "youtubeMusic",
    "amazonMusic",
    "tidal",
    "deezer",
  ];

  const byPlatform = new Map<StreamingPlatform, string>();
  for (const l of links) byPlatform.set(l.platform, l.url);

  const ordered = order
    .map((p) => ({ platform: p, url: byPlatform.get(p) }))
    .filter((x): x is { platform: StreamingPlatform; url: string } =>
      Boolean(x.url),
    );

  if (!ordered.length) return null;

  return (
    <div className="afAvailWrap" aria-label="Available on">
      <div className="afAvailLine" aria-hidden="true" />
      <div className="afAvailLabel">AVAILABLE ON</div>

      <div className="afAvailIcons">
        {ordered.map(({ platform, url }) => (
          <a
            key={platform}
            href={url}
            target="_blank"
            rel="noopener noreferrer external nofollow"
            className="afAvailIcon"
            aria-label={`Open on ${platform}`}
            title={`Open on ${platform}`}
          >
            <PlatformIcon platform={platform} />
          </a>
        ))}
      </div>
    </div>
  );
}

type PlayerApi = ReturnType<typeof usePlayer>;

type FullPlayerProps = Readonly<{
  bundle: AlbumPlayerBundle;
  albums: AlbumNavItem[];
  onSelectAlbum?: (slug: string) => void;
  isBrowsingAlbum?: boolean;
  tier?: Tier;
}>;

type EffectivePlayerView = Readonly<{
  albumSlug: string;
  album: AlbumPlayerBundle["album"];
  tracks: PlayerTrack[];
}>;

function resolveAccessIdentityKey(
  params: Readonly<{
    clerkAuthLoaded: boolean;
    isSignedIn: boolean | undefined;
    userId: string | null | undefined;
    sessionId: string | null | undefined;
  }>,
): string {
  if (!params.clerkAuthLoaded) return "clerk:loading";
  if (!params.isSignedIn) return "clerk:anonymous";

  return `clerk:user:${params.userId ?? ""}:session:${params.sessionId ?? ""}`;
}

function resolveEffectivePlayerView(
  params: Readonly<{
    bundle: AlbumPlayerBundle;
    stableView: StableView | null;
    isBrowsingAlbum: boolean;
  }>,
): EffectivePlayerView {
  const { bundle, stableView, isBrowsingAlbum } = params;
  const shouldUseStableView = Boolean(
    isBrowsingAlbum &&
    stableView?.album &&
    (!bundle.album || bundle.tracks.length === 0),
  );

  if (shouldUseStableView && stableView) {
    return {
      albumSlug: stableView.albumSlug,
      album: stableView.album,
      tracks: stableView.tracks,
    };
  }

  return {
    albumSlug: bundle.albumSlug,
    album: bundle.album,
    tracks: bundle.tracks,
  };
}

function accessStatesEqual(
  left: AccessState | null,
  right: AccessState | null,
): boolean {
  if (!left || !right) return left === right;
  return JSON.stringify(left) === JSON.stringify(right);
}

function mergeAccessState(
  previous: AccessState | null,
  next: AccessState | null,
): AccessState | null {
  return accessStatesEqual(previous, next) ? previous : next;
}

function isAuthOrLegacyCapCode(code: string): boolean {
  return (
    code === "AUTH_REQUIRED" ||
    code === "CAP_REACHED" ||
    code === "ANON_CAP_REACHED"
  );
}

function hasPlaybackIntentForAlbum(
  player: PlayerApi,
  pendingInAlbum: boolean,
): boolean {
  return (
    player.playRequestPending ||
    player.intent === "play" ||
    player.status === "loading" ||
    player.status === "playing" ||
    player.status === "paused" ||
    pendingInAlbum
  );
}

function reconcilePlayerWithAccessDecision(
  params: Readonly<{
    player: PlayerApi;
    decision: AccessState;
    recordingIds: ReadonlySet<string>;
    albumKey: string | null;
  }>,
): void {
  const { player, decision, recordingIds, albumKey } = params;

  if (decision.allowed) {
    if (player.lastError) player.clearError();
    return;
  }

  const currentInAlbum = Boolean(
    player.current?.recordingId && recordingIds.has(player.current.recordingId),
  );
  const pendingInAlbum = Boolean(
    player.pendingRecordingId && recordingIds.has(player.pendingRecordingId),
  );
  const queueIsAlbum = Boolean(albumKey && player.queueContextId === albumKey);
  const hasIntent = hasPlaybackIntentForAlbum(player, pendingInAlbum);
  const shouldBlockAlbum =
    currentInAlbum || pendingInAlbum || (queueIsAlbum && hasIntent);

  const code = typeof decision.code === "string" ? decision.code.trim() : "";
  if (!shouldBlockAlbum || (isAuthOrLegacyCapCode(code) && !hasIntent)) return;

  try {
    player.pause();
  } catch {
    // ignore
  }

  if (player.status === "loading") {
    player.setStatusExternal("idle");
  }
}

function accessFailureFallback(catalogueId: string): AccessState {
  return {
    forCatalogueId: catalogueId,
    allowed: true,
    embargoed: false,
    releaseAt: null,
    code: "ACCESS_CHECK_ERROR",
    reason: "Access check failed (client).",
    corr: null,
    shareTokenAccess: null,
    sharePlaybackContext: null,
    sharePlaybackScopeId: null,
  };
}

function resolveAccessForAlbum(
  catalogueId: string | null,
  access: AccessState | null,
): AccessState | null {
  if (!catalogueId) return null;
  if (access?.forCatalogueId !== catalogueId) return null;
  return access;
}

function isPlayerPlayingish(player: PlayerApi): boolean {
  return (
    player.status === "playing" ||
    player.status === "loading" ||
    player.playRequestPending
  );
}

type PlaybackAccessUiState = Readonly<{
  accessKnown: boolean;
  isPlaybackGate: boolean;
  canPlay: boolean;
  canAttemptPlay: boolean;
}>;

function derivePlaybackAccessUiState(
  tracks: readonly PlayerTrack[],
  accessForAlbum: AccessState | null,
): PlaybackAccessUiState {
  const accessKnown = accessForAlbum !== null;
  const hasPlayableTracks = tracks.length > 0;
  const isPlaybackGate = Boolean(
    hasPlayableTracks &&
    accessKnown &&
    accessForAlbum?.allowed === false &&
    isPlaybackGateCode(accessForAlbum.code),
  );
  const canPlay = Boolean(
    hasPlayableTracks && accessKnown && accessForAlbum?.allowed !== false,
  );

  return {
    accessKnown,
    isPlaybackGate,
    canPlay,
    canAttemptPlay: canPlay || isPlaybackGate,
  };
}

type EmbargoUiState = Readonly<{
  releaseAtMs: number;
  showEmbargo: boolean;
  showShareTokenEmbargoAccess: boolean;
}>;

function deriveEmbargoUiState(
  params: Readonly<{
    embargo: NonNullable<AlbumPlayerBundle["album"]>["embargo"];
    accessForAlbum: AccessState | null;
    shareToken: string | null;
  }>,
): EmbargoUiState {
  const releaseAtMs = params.embargo?.releaseAt
    ? Date.parse(params.embargo.releaseAt)
    : Number.NaN;
  const accessResolved = params.accessForAlbum !== null;
  const accessAllowsEmbargoBypass = params.accessForAlbum?.allowed === true;
  const showEmbargo = Boolean(
    params.embargo?.embargoed &&
    Number.isFinite(releaseAtMs) &&
    !accessAllowsEmbargoBypass &&
    (!params.shareToken || accessResolved),
  );
  const showShareTokenEmbargoAccess = Boolean(
    params.embargo?.embargoed &&
    params.accessForAlbum?.allowed === true &&
    params.accessForAlbum.shareTokenAccess,
  );

  return { releaseAtMs, showEmbargo, showShareTokenEmbargoAccess };
}

function isAlbumPlaybackActive(
  albumKey: string | null,
  queueContextId: string | undefined,
): boolean {
  return Boolean(albumKey && queueContextId === albumKey);
}

type AlbumTransportState = Readonly<{
  albumIdx: number;
  prevDisabled: boolean;
  nextDisabled: boolean;
}>;

function deriveAlbumTransportState(
  params: Readonly<{
    currentRecordingId: string;
    tracks: readonly PlayerTrack[];
    transportLock: boolean;
  }>,
): AlbumTransportState {
  const albumIdx = params.currentRecordingId
    ? params.tracks.findIndex(
        (track) => track.recordingId === params.currentRecordingId,
      )
    : -1;

  if (albumIdx < 0) {
    return { albumIdx, prevDisabled: true, nextDisabled: true };
  }

  return {
    albumIdx,
    prevDisabled: params.transportLock || albumIdx === 0,
    nextDisabled: params.transportLock || albumIdx === params.tracks.length - 1,
  };
}

function isCoarsePointerDevice(): boolean {
  if (typeof window === "undefined") return false;

  try {
    return window.matchMedia?.("(pointer: coarse)").matches ?? false;
  } catch {
    return "ontouchstart" in window;
  }
}

function trackRowColors(
  canPlay: boolean,
  isCurrent: boolean,
): Readonly<{ titleColor: string; subColor: string }> {
  if (!canPlay) {
    return {
      titleColor: "rgba(255,255,255,0.38)",
      subColor: "rgba(255,255,255,0.32)",
    };
  }

  if (isCurrent) {
    return {
      titleColor: "var(--accent)",
      subColor: "color-mix(in srgb, var(--accent) 70%, rgba(255,255,255,0.70))",
    };
  }

  return {
    titleColor: "rgba(255,255,255,0.92)",
    subColor: "rgba(255,255,255,0.70)",
  };
}

function trackRowRadius(index: number, trackCount: number): string {
  if (index === 0) return "14px 14px 0 0";
  if (index === trackCount - 1) return "0 0 14px 14px";
  return "0";
}

type TrackRowView = Readonly<{
  isCurrent: boolean;
  shimmerTitle: boolean;
  isNowPlaying: boolean;
  titleColor: string;
  subColor: string;
  restingBackground: string;
  rowRadius: string;
  dataReason: string;
  cursor: "pointer" | "default";
  opacity: number;
}>;

function deriveTrackRowView(
  params: Readonly<{
    track: PlayerTrack;
    index: number;
    trackCount: number;
    currentRecordingId: string | undefined;
    pendingRecordingId: string | undefined;
    playerStatus: PlayerApi["status"];
    playerIntent: PlayerApi["intent"];
    loadingReason: PlayerApi["loadingReason"];
    canPlay: boolean;
    canAttemptPlay: boolean;
    isSelected: boolean;
  }>,
): TrackRowView {
  const isCurrent = params.currentRecordingId === params.track.recordingId;
  const isPending = params.pendingRecordingId === params.track.recordingId;
  const shimmerTitle =
    params.playerStatus === "loading" && isPending && !isCurrent;
  const isNowPlaying = Boolean(
    isCurrent &&
    (params.playerStatus === "playing" ||
      params.playerStatus === "loading" ||
      params.playerIntent === "play"),
  );
  const { titleColor, subColor } = trackRowColors(params.canPlay, isCurrent);
  const baseBackground = params.isSelected
    ? "rgba(255,255,255,0.14)"
    : "transparent";
  const restingBackground =
    isCurrent && !params.isSelected ? "transparent" : baseBackground;
  const dataReason =
    isCurrent && params.playerStatus === "loading"
      ? (params.loadingReason ?? "")
      : "";

  return {
    isCurrent,
    shimmerTitle,
    isNowPlaying,
    titleColor,
    subColor,
    restingBackground,
    rowRadius: trackRowRadius(params.index, params.trackCount),
    dataReason,
    cursor: params.canAttemptPlay ? "pointer" : "default",
    opacity: params.canPlay ? 1 : 0.75,
  };
}

function trackPlayLabel(track: PlayerTrack, index: number): string {
  const title = track.title?.trim();
  const subject = title || `track ${index + 1}`;
  return `Play ${subject}`;
}

function trackRowActionLabel(
  track: PlayerTrack,
  index: number,
  isCoarsePointer: boolean,
): string {
  const title = track.title?.trim();
  const subject = title || `track ${index + 1}`;
  const verb = isCoarsePointer ? "Play" : "Select";
  return `${verb} ${subject}`;
}

function albumArtworkBackground(artworkUrl: string | null | undefined): string {
  if (artworkUrl) return `url(${artworkUrl}) center/cover no-repeat`;
  return "radial-gradient(120px 120px at 30% 20%, rgba(255,255,255,0.14), rgba(255,255,255,0.02))";
}

function AlbumHero(
  props: Readonly<{
    artworkUrl: string | null | undefined;
    activeListeners: number;
  }>,
) {
  return (
    <div
      style={{
        position: "relative",
        width: "min(334px, 86vw)",
        height: "min(334px, 86vw)",
        borderRadius: 18,
        border: "1px solid rgba(255,255,255,0.14)",
        background: albumArtworkBackground(props.artworkUrl),
        boxShadow: "0 22px 60px rgba(0,0,0,0.35)",
        overflow: "hidden",
      }}
    >
      {props.activeListeners > 0 ? (
        <div
          className="afArtworkLiveBadge"
          aria-live="polite"
          aria-label={fmtActiveListeners(props.activeListeners)}
          title={fmtActiveListeners(props.activeListeners)}
        >
          <span className="afLiveBadgeDot" aria-hidden="true" />
          <span>{fmtActiveListeners(props.activeListeners)}</span>
        </div>
      ) : null}
    </div>
  );
}

function EmbargoNotices(
  props: Readonly<{
    showEmbargo: boolean;
    releaseAtMs: number;
    embargoNote: string | null | undefined;
    showShareTokenEmbargoAccess: boolean;
    shareTokenExpiryLabel: string | null;
    shareTokenRedemptionLimit: string | null;
  }>,
) {
  const note =
    props.embargoNote?.trim() ||
    "Playback disabled pre-release. Patrons have instant early access.";
  const hasShareTokenDetails = Boolean(
    props.shareTokenExpiryLabel || props.shareTokenRedemptionLimit,
  );

  return (
    <>
      {props.showEmbargo ? (
        <div
          role="note"
          style={{
            marginTop: 10,
            padding: "10px 12px",
            borderRadius: 14,
            border: "1px solid rgba(255,255,255,0.14)",
            background: "rgba(0,0,0,0.22)",
            boxShadow: "0 10px 34px rgba(0,0,0,0.28)",
            maxWidth: 560,
            textAlign: "center",
          }}
        >
          <div
            style={{
              fontSize: 12,
              fontWeight: 650,
              letterSpacing: 0.2,
              opacity: 0.95,
            }}
          >
            Releases on{" "}
            {new Date(props.releaseAtMs).toLocaleDateString(undefined, {
              day: "numeric",
              month: "long",
              year: "numeric",
            })}
          </div>

          <div
            style={{
              marginTop: 4,
              fontSize: 12,
              opacity: 0.78,
              lineHeight: 1.35,
            }}
          >
            {note}
          </div>
        </div>
      ) : null}

      {props.showShareTokenEmbargoAccess ? (
        <div
          role="note"
          style={{
            marginTop: 10,
            padding: "10px 12px",
            borderRadius: 14,
            border: "1px solid rgba(255,255,255,0.16)",
            background:
              "color-mix(in srgb, var(--accent) 8%, rgba(0,0,0,0.24))",
            boxShadow: "0 10px 34px rgba(0,0,0,0.28)",
            maxWidth: 560,
            textAlign: "center",
          }}
        >
          <div
            style={{
              fontSize: 12,
              fontWeight: 700,
              letterSpacing: 0.2,
              opacity: 0.96,
            }}
          >
            Embargoed access
          </div>

          <div
            style={{
              marginTop: 4,
              fontSize: 12,
              opacity: 0.78,
              lineHeight: 1.35,
            }}
          >
            This private link enables playback during embargo.
          </div>

          {hasShareTokenDetails ? (
            <div
              style={{
                display: "flex",
                flexWrap: "wrap",
                justifyContent: "center",
                gap: "4px 10px",
                marginTop: 8,
                fontSize: 11,
                fontWeight: 650,
                opacity: 0.76,
                lineHeight: 1.35,
              }}
            >
              {props.shareTokenExpiryLabel ? (
                <span>Expires {props.shareTokenExpiryLabel} local time.</span>
              ) : null}

              {props.shareTokenRedemptionLimit ? (
                <span>{props.shareTokenRedemptionLimit}</span>
              ) : null}
            </div>
          ) : null}
        </div>
      ) : null}
    </>
  );
}

function TransportControls(
  props: Readonly<{
    prevDisabled: boolean;
    nextDisabled: boolean;
    canAttemptPlay: boolean;
    canPlay: boolean;
    playLock: boolean;
    playingThisAlbum: boolean;
    onDownload: () => void;
    onPrevious: () => void;
    onTogglePlay: () => void;
    onNext: () => void;
    onShare: () => void;
  }>,
) {
  const playLabel = props.playingThisAlbum ? "Pause" : "Play";
  const playClick =
    props.canAttemptPlay && !props.playLock ? props.onTogglePlay : undefined;
  const playCursor = props.canAttemptPlay ? "pointer" : "default";
  const playOpacity = props.canAttemptPlay ? 1 : 0.55;
  const playShadow = props.playingThisAlbum
    ? "0 18px 50px rgba(0,0,0,0.35)"
    : "0 18px 50px rgba(0,0,0,0.30)";

  return (
    <div className="afTransportRow">
      <div className="afTransportBadgeSlot" aria-hidden="true" />

      <div className="afTransportControls">
        <IconCircleBtn label="Download" onClick={props.onDownload}>
          <DownloadIcon />
        </IconCircleBtn>

        <IconCircleBtn
          label="Previous"
          disabled={props.prevDisabled}
          onClick={props.onPrevious}
        >
          <PrevIcon />
        </IconCircleBtn>

        <div style={{ position: "relative", width: 64, height: 64 }}>
          <button
            type="button"
            onClick={playClick}
            disabled={!props.canAttemptPlay || props.playLock}
            aria-label={playLabel}
            title={playLabel}
            style={{
              width: 64,
              height: 64,
              borderRadius: 999,
              border: "1px solid rgba(255,255,255,0.12)",
              background: "rgba(245,245,245,0.95)",
              color: "rgba(0,0,0,0.92)",
              display: "grid",
              placeItems: "center",
              cursor: playCursor,
              opacity: playOpacity,
              boxShadow: playShadow,
              transform: "translateZ(0)",
              position: "relative",
              zIndex: 2,
            }}
          >
            <PlayPauseBig playing={props.playingThisAlbum} />
          </button>

          {props.canPlay ? (
            <div
              style={{
                position: "absolute",
                inset: -5,
                zIndex: 1,
                pointerEvents: "none",
                overflow: "visible",
                isolation: "isolate",
                transform: "translateZ(0)",
                display: "grid",
                placeItems: "center",
              }}
            >
              <PatternRingGlow
                size={64}
                ringPx={1}
                glowPx={2}
                blurPx={4}
                opacity={0.45}
                seed={913}
              />
            </div>
          ) : null}
        </div>

        <IconCircleBtn
          label="Next"
          disabled={props.nextDisabled}
          onClick={props.onNext}
        >
          <NextIcon />
        </IconCircleBtn>

        <IconCircleBtn label="Share" onClick={props.onShare}>
          <ShareIcon />
        </IconCircleBtn>
      </div>

      <div aria-hidden="true" />
    </div>
  );
}

function TrackIndexCell(
  props: Readonly<{
    index: number;
    track: PlayerTrack;
    isNowPlaying: boolean;
    canAttemptPlay: boolean;
    isCurrent: boolean;
    subColor: string;
    onInlinePlay: (track: PlayerTrack) => void;
  }>,
) {
  let content: React.ReactNode = (
    <span className="afTrackIndexNumber">{props.index + 1}</span>
  );

  if (props.isNowPlaying) {
    content = <NowPlayingPip />;
  } else if (props.canAttemptPlay && !props.isCurrent) {
    content = (
      <span className="afTrackIndexSwap">
        <span className="afTrackIndexNumber">{props.index + 1}</span>
        <button
          type="button"
          className="afTrackInlinePlay"
          aria-label={trackPlayLabel(props.track, props.index)}
          title="Play"
          onClick={(event) => {
            event.preventDefault();
            event.stopPropagation();
            props.onInlinePlay(props.track);
          }}
        >
          <TrackInlinePlayIcon />
        </button>
      </span>
    );
  }

  return (
    <div
      style={{
        fontSize: 12,
        opacity: 0.9,
        display: "flex",
        alignItems: "center",
        gap: 6,
        color: props.subColor,
        paddingLeft: 12,
        justifyContent: "flex-start",
        position: "relative",
        zIndex: 1,
        pointerEvents: "none",
      }}
    >
      {content}
    </div>
  );
}

function parsePublicAlbumPath(pathname: string | null): {
  albumSlug: string | null;
  displayId: string | null;
} {
  const p = (pathname ?? "").trim();

  // canonical music surfaces:
  // /:slug
  // /:slug/:displayId
  //
  // exclude obvious non-music reserved roots
  const reserved = new Set([
    "",
    "player",
    "journal",
    "portal",
    "posts",
    "extras",
    "download",
    "gift",
    "unsubscribe",
    "studio",
    "admin",
    "api",
  ]);

  const parts = p
    .split("?")[0]
    .split("#")[0]
    .split("/")
    .filter(Boolean)
    .map((x) => decodeURIComponent(x).trim())
    .filter(Boolean);

  if (parts.length === 0) return { albumSlug: null, displayId: null };
  if (parts.length > 2) return { albumSlug: null, displayId: null };

  const albumSlug = parts[0] ?? null;
  if (!albumSlug || reserved.has(albumSlug)) {
    return { albumSlug: null, displayId: null };
  }

  const displayId = parts[1] ?? null;
  return { albumSlug, displayId };
}

function canonicalCarryQuery(): string {
  // Only carry the params that are allowed to exist on canonical public routes.
  if (typeof window === "undefined") return "";
  const sp = new URLSearchParams(window.location.search);
  const out = new URLSearchParams();

  const st = (sp.get("st") ?? "").trim();
  const share = (sp.get("share") ?? "").trim();
  const autoplay = (sp.get("autoplay") ?? "").trim();

  if (st) out.set("st", st);
  else if (share) out.set("share", share);

  if (autoplay) out.set("autoplay", autoplay);

  // keep utm_* if present
  for (const [k, v] of sp.entries()) {
    if (k.startsWith("utm_") && v.trim()) out.set(k, v.trim());
  }

  const q = out.toString();
  return q ? `?${q}` : "";
}

export default function FullPlayer(props: FullPlayerProps) {
  const {
    bundle,
    albums,
    onSelectAlbum,
    isBrowsingAlbum = false,
    tier = "none",
  } = props;

  const { album, tracks, albumLyrics } = bundle;
  const p = usePlayer();
  const { reportGate } = useGateBroker();
  const {
    isLoaded: clerkAuthLoaded,
    isSignedIn,
    userId,
    sessionId,
  } = useAuth();

  const accessIdentityKey = resolveAccessIdentityKey({
    clerkAuthLoaded,
    isSignedIn,
    userId,
    sessionId,
  });

  // near your other hooks
  const searchParams = useSearchParams();
  const stParam =
    (searchParams?.get("st") ?? searchParams?.get("share") ?? "").trim() ||
    null;

  const pRef = React.useRef(p);
  React.useEffect(() => {
    pRef.current = p;
  }, [p]);

  // ---- optimistic UI cache (state, not ref) to satisfy react-hooks/refs lint ----
  const [stableView, setStableView] = React.useState<StableView | null>(null);

  React.useEffect(() => {
    if (album && tracks?.length) {
      setStableView(bundle);
    }
    primeLyricsFromAlbumBundle(albumLyrics ?? null);
  }, [bundle, album, tracks, albumLyrics]);

  const {
    albumSlug: effAlbumSlug,
    album: effAlbum,
    tracks: effTracks,
  } = resolveEffectivePlayerView({
    bundle,
    stableView,
    isBrowsingAlbum,
  });
  // Track membership ref (lets access-check logic test membership without re-triggering fetch)
  const effRecordingIdSetRef = React.useRef<Set<string>>(new Set());

  React.useEffect(() => {
    effRecordingIdSetRef.current = new Set(effTracks.map((t) => t.recordingId));
  }, [effTracks]);

  const router = useRouter();
  const pathname = usePathname();

  const route = React.useMemo(() => parsePublicAlbumPath(pathname), [pathname]);
  const isPublicAlbumRoute = Boolean(route.albumSlug);

  const goCanonicalTrack = React.useCallback(
    (displayId: string | null, mode: "push" | "replace" = "push") => {
      if (!isPublicAlbumRoute) return;

      const qs = canonicalCarryQuery();
      const base = `/${encodeURIComponent(effAlbumSlug)}`;
      const href = displayId
        ? `${base}/${encodeURIComponent(displayId)}${qs}`
        : `${base}${qs}`;

      if (mode === "replace") router.replace(href, { scroll: false });
      else router.push(href, { scroll: false });
    },
    [isPublicAlbumRoute, router, effAlbumSlug],
  );

  const [pendingAlbumSlug, setPendingAlbumSlug] = React.useState<string | null>(
    null,
  );
  const fullPlayerTopRef = React.useRef<HTMLDivElement | null>(null);
  const mobileScrollTargetAlbumSlugRef = React.useRef<string | null>(null);

  const scrollMobilePlayerToTop = React.useCallback(() => {
    if (typeof window === "undefined") return;
    if (!window.matchMedia("(max-width: 640px)").matches) return;

    const target = fullPlayerTopRef.current;
    if (!target) return;

    let ancestor: HTMLElement | null = target.parentElement;

    while (ancestor) {
      const overflowY = window.getComputedStyle(ancestor).overflowY;
      const canScroll =
        (overflowY === "auto" ||
          overflowY === "scroll" ||
          overflowY === "overlay") &&
        ancestor.scrollHeight > ancestor.clientHeight;

      if (canScroll) {
        const ancestorTop = ancestor.getBoundingClientRect().top;
        const targetTop = target.getBoundingClientRect().top;
        const nextTop = Math.max(
          0,
          ancestor.scrollTop + targetTop - ancestorTop,
        );

        ancestor.scrollTo({
          top: nextTop,
          behavior: "auto",
        });

        return;
      }

      ancestor = ancestor.parentElement;
    }

    const targetTop = window.scrollY + target.getBoundingClientRect().top;

    window.scrollTo({
      top: Math.max(0, targetTop),
      behavior: "auto",
    });
  }, []);

  React.useLayoutEffect(() => {
    const targetAlbumSlug = mobileScrollTargetAlbumSlugRef.current;

    const hasResolvedSelectedAlbum =
      targetAlbumSlug === effAlbumSlug &&
      Boolean(effAlbum) &&
      effTracks.length > 0;

    if (!hasResolvedSelectedAlbum) return;

    // This fires after React has committed the incoming album's DOM, but before
    // the browser paints it. The user therefore sees the new album and the
    // restored top position as a single visual change.
    scrollMobilePlayerToTop();

    mobileScrollTargetAlbumSlugRef.current = null;
  }, [effAlbum, effAlbumSlug, effTracks.length, scrollMobilePlayerToTop]);

  // Clear when the pending selection has actually become the effective album view.
  React.useEffect(() => {
    if (!pendingAlbumSlug) return;

    const resolved =
      effAlbumSlug === pendingAlbumSlug &&
      Boolean(effAlbum) &&
      effTracks.length > 0;

    if (resolved) setPendingAlbumSlug(null);
  }, [pendingAlbumSlug, effAlbumSlug, effAlbum, effTracks.length]);

  // Safety: auto-clear after 8s so it can't stick forever if something goes sideways.
  React.useEffect(() => {
    if (!pendingAlbumSlug) return;
    const t = window.setTimeout(() => setPendingAlbumSlug(null), 8000);
    return () => window.clearTimeout(t);
  }, [pendingAlbumSlug]);

  const albumTitle = effAlbum?.title ?? "—";
  const albumDesc =
    effAlbum?.description ??
    "This is placeholder copy. Soon: pull album description from Sanity.";

  // ---- browser tab title (client-side) ----
  // We set this here because in portal surfaces we may not actually navigate routes
  // (and therefore server metadata won't rerun).
  React.useEffect(() => {
    if (typeof document === "undefined") return;

    const prev = document.title;

    const cur = p.current;
    const curInThisAlbum = Boolean(
      cur?.recordingId &&
      effTracks.some((t) => t.recordingId === cur.recordingId),
    );
    const trackTitle = curInThisAlbum
      ? cur?.title?.trim() || cur?.recordingId || ""
      : "";

    const aTitle = (effAlbum?.title?.trim() || "").trim();
    const artist = (effAlbum?.artist?.trim() || "").trim();

    // Format: "Track · Album · Artist" (falls back gracefully)
    const next = trackTitle
      ? [trackTitle, aTitle, artist].filter(Boolean).join(" · ")
      : [aTitle, artist].filter(Boolean).join(" · ");

    if (next) document.title = next;

    return () => {
      // Don't leave a stale player title behind if the component unmounts
      document.title = prev;
    };
    // Intentionally depend on the minimal fields that affect the title.
  }, [
    p.current?.recordingId,
    p.current?.title,
    effAlbum?.title,
    effAlbum?.artist,
    effTracks,
    p,
  ]);

  const platformLinks = React.useMemo(
    () => normalizePlatformLinks(effAlbum?.platformLinks),
    [effAlbum?.platformLinks],
  );

  const browseAlbums = albums.filter((a) => a.id !== effAlbum?.id);

  const playingish = isPlayerPlayingish(p);

  const [activeListeners, setActiveListeners] = React.useState<number>(0);

  React.useEffect(() => {
    if (!LIVE_LISTENER_COUNT_ENABLED || !playingish) {
      setActiveListeners(0);
      return;
    }

    let cancelled = false;

    const isVisible = () => document.visibilityState === "visible";

    async function refreshActiveListeners(): Promise<void> {
      if (!isVisible()) return;

      try {
        const res = await fetch("/api/playback/live?windowSeconds=30", {
          method: "GET",
        });

        if (!res.ok) return;

        const body = (await res.json()) as {
          ok?: boolean;
          activeListeners?: number;
        };

        if (cancelled) return;

        const next =
          typeof body.activeListeners === "number" &&
          Number.isFinite(body.activeListeners) &&
          body.activeListeners >= 0
            ? Math.floor(body.activeListeners)
            : 0;

        setActiveListeners(next);
      } catch {
        // A live-count failure must never affect playback.
      }
    }

    const onVisibilityChange = () => {
      if (isVisible()) {
        void refreshActiveListeners();
      }
    };

    void refreshActiveListeners();

    const id = window.setInterval(() => {
      void refreshActiveListeners();
    }, 30_000);

    document.addEventListener("visibilitychange", onVisibilityChange);

    return () => {
      cancelled = true;
      window.clearInterval(id);
      document.removeEventListener("visibilitychange", onVisibilityChange);
    };
  }, [playingish]);

  const [access, setAccess] = React.useState<AccessState | null>(null);

  // Canonical album key used in queue context + gating
  const albumKey = effAlbum?.catalogueId ?? effAlbum?.id ?? null;

  // ✅ use a single scalar for deps + narrowing
  const catalogueId = albumKey;

  // ✅ album-scoped view (prevents stale flash)
  const accessForAlbum = resolveAccessForAlbum(catalogueId, access);

  React.useEffect(() => {
    if (!catalogueId || !clerkAuthLoaded) return;

    let alive = true;
    const accessRequest = {
      catalogueId,
      shareToken: stParam,
      accessIdentityKey,
    };

    // Do not leave an anonymous denial visibly attached to the newly
    // authenticated session while its fresh access request is in flight.
    setAccess(null);

    const cached = getCachedPlaybackAccessDecision(accessRequest);
    setAccess((previous) => mergeAccessState(previous, cached));

    void fetchPlaybackAccessDecision(accessRequest)
      .then((next) => {
        if (!alive) return;

        setAccess((previous) => mergeAccessState(previous, next));
        reconcilePlayerWithAccessDecision({
          player: pRef.current,
          decision: next,
          recordingIds: effRecordingIdSetRef.current,
          albumKey,
        });
      })
      .catch((error: unknown) => {
        if (!alive) return;
        console.error("FullPlayer access check failed", error);

        // Presentation may remain available after a transient request failure,
        // but do not cache this fallback in the shared decision store.
        // PlayerState must still fail closed if real play authorisation fails.
        setAccess(accessFailureFallback(catalogueId));

        const player = pRef.current;
        if (player.lastError) player.clearError();
      });

    return () => {
      alive = false;
    };
  }, [catalogueId, albumKey, stParam, accessIdentityKey, clerkAuthLoaded]);

  // Unknown access still disables the transport until the access check resolves.
  // A resolved auth/cap block remains clickable so the user can summon the gate.
  const emb = effAlbum?.embargo;
  const { accessKnown, isPlaybackGate, canPlay, canAttemptPlay } =
    derivePlaybackAccessUiState(effTracks, accessForAlbum);
  const { releaseAtMs, showEmbargo, showShareTokenEmbargoAccess } =
    deriveEmbargoUiState({
      embargo: emb,
      accessForAlbum,
      shareToken: stParam,
    });
  const isThisAlbumActive = isAlbumPlaybackActive(albumKey, p.queueContextId);
  const playingThisAlbum = playingish && isThisAlbumActive;

  const shareTokenAccess = accessForAlbum?.shareTokenAccess ?? null;
  const shareTokenExpiryLabel = formatShareTokenExpiry(
    shareTokenAccess?.expiresAt ?? null,
  );
  const shareTokenRedemptionLimit = formatRedemptionLimit(
    shareTokenAccess?.maxRedemptions ?? null,
  );
  const queueSharePlaybackContext =
    accessForAlbum?.sharePlaybackContext ?? null;
  const queueSharePlaybackScopeId =
    accessForAlbum?.sharePlaybackScopeId ?? null;

  const [playLock, setPlayLock] = React.useState(false);
  const lockPlayFor = (ms: number) => {
    setPlayLock(true);
    window.setTimeout(() => setPlayLock(false), ms);
  };

  const [transportLock, setTransportLock] = React.useState(false);
  const lockTransportFor = (ms: number) => {
    setTransportLock(true);
    window.setTimeout(() => setTransportLock(false), ms);
  };

  const prefetchAlbumSession = React.useCallback(
    (track?: PlayerTrack) => {
      if (!albumKey) return;

      const startPlaybackId = track?.muxPlaybackId?.trim() || null;

      window.dispatchEvent(
        new CustomEvent("af:prefetch-album-session", {
          detail: {
            albumId: albumKey,
            ...(stParam ? { st: stParam } : {}),
            ...(startPlaybackId ? { startPlaybackId } : {}),
          },
        }),
      );
    },
    [albumKey, stParam],
  );

  const prefetchAlbumArt = (url?: string | null) => {
    if (!url) return;
    try {
      const img = new Image();
      img.src = url;
    } catch {}
  };

  function openPlaybackGate(resumeTrack?: PlayerTrack) {
    if (resumeTrack) {
      window.dispatchEvent(
        new CustomEvent("af:playback-auth-resume-target", {
          detail: {
            track: resumeTrack,
          },
        }),
      );
    }

    reportGate({
      code: isPlaybackGateCode(accessForAlbum?.code)
        ? accessForAlbum.code
        : "PLAYBACK_CAP_REACHED",
      action: toGateAction(accessForAlbum?.action),
      domain: "playback",
      correlationId: accessForAlbum?.corr ?? null,
      message: playbackGateMessage(accessForAlbum?.reason),
      uiMode: "spotlight",
    });
  }

  const playTrackWithIntent = (
    track: PlayerTrack,
    options: Readonly<{
      ensureQueue: boolean;
      navigationMode: "push" | "replace";
    }>,
  ) => {
    prefetchAlbumSession(track);
    if (options.ensureQueue) ensureAlbumQueue();
    goCanonicalTrack(track.displayId, options.navigationMode);
    p.play(track);
    window.dispatchEvent(new Event("af:play-intent"));
  };

  const onTogglePlay = () => {
    lockPlayFor(120);

    if (isPlaybackGate) {
      openPlaybackGate();
      return;
    }
    if (!canPlay) return;

    const currentRecordingId = p.current?.recordingId ?? "";
    const currentTrackInThisAlbum = isThisAlbumActive
      ? (effTracks.find((track) => track.recordingId === currentRecordingId) ??
        null)
      : null;
    const routeTargetsThisAlbum =
      isPublicAlbumRoute && route.albumSlug === effAlbumSlug;
    const directRouteTrack = routeTargetsThisAlbum
      ? (effTracks.find((track) => track.displayId === route.displayId) ?? null)
      : null;
    const directRouteTargetsAnotherTrack = Boolean(
      directRouteTrack &&
      directRouteTrack.recordingId !== currentTrackInThisAlbum?.recordingId,
    );

    // A generic album page controls its existing same-album transport natively.
    // A direct track URL is more specific: it deliberately selects that track.
    if (playingThisAlbum && !directRouteTargetsAnotherTrack) {
      window.dispatchEvent(new Event("af:pause-intent"));
      p.pause();
      return;
    }

    if (directRouteTrack && directRouteTargetsAnotherTrack) {
      playTrackWithIntent(directRouteTrack, {
        ensureQueue: true,
        navigationMode: "replace",
      });
      return;
    }

    // Same active album, no differently targeted direct-track URL:
    // retain position and resume rather than resetting to track one.
    if (currentTrackInThisAlbum) {
      playTrackWithIntent(currentTrackInThisAlbum, {
        ensureQueue: false,
        navigationMode: "replace",
      });
      return;
    }

    // A different album, or an album with no recoverable current track:
    // intentionally establish its queue and begin at its first track.
    const firstTrack = effTracks[0];
    if (!firstTrack) return;

    playTrackWithIntent(firstTrack, {
      ensureQueue: true,
      navigationMode: "replace",
    });
  };

  const getDurMs = (t: PlayerTrack) =>
    p.durationByRecordingId?.[t.recordingId] ?? t.durationMs;
  const renderDur = (t: PlayerTrack) => {
    const ms = getDurMs(t) ?? 0;
    return ms > 0 ? fmtTime(ms) : "—";
  };

  const shareCtx = deriveShareContext({
    albumSlug: effAlbumSlug,
    album: effAlbum,
    queueArtist: p.queueContextArtist,
    albumId: albumKey ?? undefined,
  });

  const [selectedRecordingId, setSelectedRecordingId] = React.useState<
    string | null
  >(null);

  const isCoarsePointer = isCoarsePointerDevice();

  // --- Album-local transport (FullPlayer) ---
  // Only operate within effTracks. Never call p.prev()/p.next() here.

  const curId = p.current?.recordingId ?? "";
  const { albumIdx, prevDisabled, nextDisabled } = deriveAlbumTransportState({
    currentRecordingId: curId,
    tracks: effTracks,
    transportLock,
  });

  function ensureAlbumQueue() {
    // Explicit nulls prevent an earlier private-link attribution context from
    // surviving when ordinary album playback replaces the queue.
    p.setQueue(effTracks, {
      contextId: albumKey ?? undefined,
      artworkUrl: effAlbum?.artworkUrl ?? null,
      contextSlug: effAlbumSlug,
      contextTitle: effAlbum?.title ?? undefined,
      contextArtist: effAlbum?.artist ?? undefined,
      sharePlaybackContext: queueSharePlaybackContext,
      sharePlaybackScopeId: queueSharePlaybackScopeId,
    });
  }

  const preparedInitialQueueRef = React.useRef<string | null>(null);

  React.useEffect(() => {
    // Wait for the access check so a private-link queue receives its resolved
    // attribution context before any listener can press play.
    if (!accessKnown) return;

    if (!isPublicAlbumRoute || route.albumSlug !== effAlbumSlug || !albumKey) {
      return;
    }

    const directTrack = route.displayId
      ? (effTracks.find((track) => track.displayId === route.displayId) ?? null)
      : null;

    const initialTrack = directTrack ?? effTracks[0] ?? null;

    if (!initialTrack) return;

    // Prepare a fresh player around the route's target track—or track one on a
    // generic album route—without beginning playback. Never overwrite an
    // existing session, including a paused or playing different album.
    const hasExistingPlayerSession = Boolean(
      p.current || p.queue.length > 0 || p.pendingRecordingId,
    );

    if (hasExistingPlayerSession) return;

    const queueKey = `${albumKey}:${initialTrack.recordingId}`;

    if (preparedInitialQueueRef.current === queueKey) return;

    preparedInitialQueueRef.current = queueKey;

    p.setQueue(effTracks, {
      contextId: albumKey,
      artworkUrl: effAlbum?.artworkUrl ?? null,
      contextSlug: effAlbumSlug,
      contextTitle: effAlbum?.title ?? undefined,
      contextArtist: effAlbum?.artist ?? undefined,
      sharePlaybackContext: queueSharePlaybackContext,
      sharePlaybackScopeId: queueSharePlaybackScopeId,
      initialCurrentRecordingId: initialTrack.recordingId,
    });
  }, [
    accessKnown,
    albumKey,
    effAlbum?.artworkUrl,
    effAlbum?.artist,
    effAlbum?.title,
    effAlbumSlug,
    effTracks,
    isPublicAlbumRoute,
    p,
    queueSharePlaybackContext,
    queueSharePlaybackScopeId,
    route.albumSlug,
    route.displayId,
  ]);

  function playAlbumIndex(i: number) {
    const t = effTracks[i];
    if (!t) return;

    prefetchAlbumSession(t);
    ensureAlbumQueue();
    if (isPublicAlbumRoute) {
      // User-initiated navigation; preserve history.
      goCanonicalTrack(t.displayId, "push");
    }

    p.play(t);
    window.dispatchEvent(new Event("af:play-intent"));
  }

  const gotoDownload = () => {
    router.push("/download");
  };

  const activateTrackRow = (track: PlayerTrack) => {
    if (isPlaybackGate) {
      openPlaybackGate(track);
      return;
    }
    if (!canPlay) return;

    ensureAlbumQueue();
    goCanonicalTrack(track.displayId, "push");

    if (isCoarsePointer) {
      prefetchAlbumSession(track);
      p.play(track);
      window.dispatchEvent(new Event("af:play-intent"));
      return;
    }

    setSelectedRecordingId(track.recordingId);
  };

  const doubleActivateTrackRow = (track: PlayerTrack) => {
    if (isCoarsePointer) return;
    if (isPlaybackGate) {
      openPlaybackGate(track);
      return;
    }
    if (!canPlay) return;

    goCanonicalTrack(track.displayId, "push");
    prefetchAlbumSession(track);
    p.play(track);
    window.dispatchEvent(new Event("af:play-intent"));
  };

  const inlinePlayTrack = (track: PlayerTrack) => {
    if (isPlaybackGate) {
      openPlaybackGate(track);
      return;
    }

    ensureAlbumQueue();
    goCanonicalTrack(track.displayId, "push");
    prefetchAlbumSession(track);
    p.play(track);
    window.dispatchEvent(new Event("af:play-intent"));
  };

  return (
    <div
      ref={fullPlayerTopRef}
      style={{
        minWidth: 0,
        width: "100%",
        maxWidth: 760,
        margin: "0 auto",
        boxSizing: "border-box",
      }}
    >
      <div
        style={{
          display: "grid",
          justifyItems: "center",
          textAlign: "center",
          gap: 10,
          padding: "18px 0", // no horizontal inset (fixes mobile “drift”)
        }}
      >
        <AlbumHero
          artworkUrl={effAlbum?.artworkUrl}
          activeListeners={activeListeners}
        />

        <div
          style={{
            fontSize: "clamp(28px, 4vw, 40px)",
            fontWeight: 800,
            lineHeight: 1.45,
            letterSpacing: "-0.3px",
            color: "#fff",
            marginBottom: 2,
          }}
        >
          {albumTitle}
        </div>
        <div
          style={{
            maxWidth: 540,
            fontSize: 12,
            opacity: 0.62,
            lineHeight: 1.45,
          }}
        >
          {albumDesc}
        </div>

        <EmbargoNotices
          showEmbargo={showEmbargo}
          releaseAtMs={releaseAtMs}
          embargoNote={effAlbum?.embargo?.note}
          showShareTokenEmbargoAccess={showShareTokenEmbargoAccess}
          shareTokenExpiryLabel={shareTokenExpiryLabel}
          shareTokenRedemptionLimit={shareTokenRedemptionLimit}
        />

        <TransportControls
          prevDisabled={prevDisabled}
          nextDisabled={nextDisabled}
          canAttemptPlay={canAttemptPlay}
          canPlay={canPlay}
          playLock={playLock}
          playingThisAlbum={playingThisAlbum}
          onDownload={gotoDownload}
          onPrevious={() => {
            lockTransportFor(350);
            if (prevDisabled) return;
            playAlbumIndex(albumIdx - 1);
          }}
          onTogglePlay={onTogglePlay}
          onNext={() => {
            lockTransportFor(350);
            if (nextDisabled) return;
            playAlbumIndex(albumIdx + 1);
          }}
          onShare={() => {
            void shareAlbum(shareCtx);
          }}
        />
      </div>

      <div style={{ marginTop: 18, padding: "0 0 18px" }}>
        <div
          style={{
            borderTop: "1px solid rgba(255,255,255,0.10)",
            paddingTop: 14,
          }}
        >
          {effTracks.map((t, i) => {
            const isSelected = selectedRecordingId === t.recordingId;
            const view = deriveTrackRowView({
              track: t,
              index: i,
              trackCount: effTracks.length,
              currentRecordingId: p.current?.recordingId,
              pendingRecordingId: p.pendingRecordingId,
              playerStatus: p.status,
              playerIntent: p.intent,
              loadingReason: p.loadingReason,
              canPlay,
              canAttemptPlay,
              isSelected,
            });

            return (
              <div
                key={t.recordingId}
                className="afTrackRow"
                onMouseEnter={(event) => {
                  if (!canAttemptPlay) return;
                  if (!isCoarsePointer && !isSelected) {
                    event.currentTarget.style.background =
                      "rgba(255,255,255,0.08)";
                  }
                }}
                onMouseLeave={(event) => {
                  event.currentTarget.style.background = view.restingBackground;
                }}
                onContextMenu={(event) => {
                  event.preventDefault();
                  void shareTrack(shareCtx, t);
                }}
                style={{
                  width: "100%",
                  display: "grid",
                  gridTemplateColumns: "44px minmax(0, 1fr) auto",
                  alignItems: "center",
                  gap: 12,
                  textAlign: "left",
                  padding: "6px 6px",
                  borderRadius: view.rowRadius,
                  border: "1px solid rgba(255,255,255,0.00)",
                  background: view.restingBackground,
                  cursor: view.cursor,
                  transform: "translateZ(0)",
                  transition: "background 120ms ease",
                  opacity: view.opacity,
                  outline: "none",
                  position: "relative",
                }}
              >
                <button
                  type="button"
                  className="afTrackRowAction"
                  disabled={!canAttemptPlay}
                  aria-label={trackRowActionLabel(t, i, isCoarsePointer)}
                  onClick={() => activateTrackRow(t)}
                  onDoubleClick={() => doubleActivateTrackRow(t)}
                  style={{
                    position: "absolute",
                    inset: 0,
                    width: "100%",
                    height: "100%",
                    border: 0,
                    borderRadius: view.rowRadius,
                    background: "transparent",
                    padding: 0,
                    cursor: view.cursor,
                    zIndex: 0,
                  }}
                />

                <TrackIndexCell
                  index={i}
                  track={t}
                  isNowPlaying={view.isNowPlaying}
                  canAttemptPlay={canAttemptPlay}
                  isCurrent={view.isCurrent}
                  subColor={view.subColor}
                  onInlinePlay={inlinePlayTrack}
                />

                <div
                  className="afRowMid"
                  style={{
                    minWidth: 0,
                    position: "relative",
                    zIndex: 1,
                    pointerEvents: "none",
                  }}
                >
                  <div
                    style={{
                      display: "flex",
                      alignItems: "center",
                      gap: 8,
                      minWidth: 0,
                      fontSize: 13,
                      opacity: 1,
                      color: view.titleColor,
                      whiteSpace: "nowrap",
                      overflow: "hidden",
                      transition: "opacity 160ms ease, color 160ms ease",
                    }}
                    data-reason={view.dataReason}
                  >
                    <span
                      className={
                        view.shimmerTitle ? "afShimmerText" : undefined
                      }
                      style={{
                        minWidth: 0,
                        overflow: "hidden",
                        textOverflow: "ellipsis",
                      }}
                    >
                      {t.title ?? t.recordingId}
                    </span>

                    {t.explicit ? (
                      <span
                        className="afExplicitBadge"
                        aria-label="Explicit"
                        title="Explicit"
                      >
                        E
                      </span>
                    ) : null}
                  </div>

                  <div className="afRowMetaUnder" aria-hidden="true">
                    {/** ENABLE THIS WHEN TOTALS ARE RESPECTABLE <span>{renderPlayCount(t)}</span>
                    <span className="afRowMetaDot">•</span> */}
                    <span>{renderDur(t)}</span>
                  </div>
                </div>

                <div
                  style={{
                    justifySelf: "end",
                    display: "flex",
                    alignItems: "center",
                    gap: 14,
                    color: view.subColor,
                    position: "relative",
                    zIndex: 1,
                    pointerEvents: "none",
                  }}
                >
                  <button
                    type="button"
                    className="afRowShare"
                    aria-label="Share track"
                    title="Share"
                    onClick={(event) => {
                      event.preventDefault();
                      event.stopPropagation();
                      void shareTrack(shareCtx, t);
                    }}
                    style={{
                      border: 0,
                      background: "transparent",
                      padding: 6,
                      borderRadius: 999,
                      color: "rgba(255,255,255,0.80)",
                      display: "grid",
                      placeItems: "center",
                      cursor: "pointer",
                      lineHeight: 0,
                    }}
                  >
                    <ShareIcon />
                  </button>

                  {/** ENABLE THIS WHEN TOTALS ARE RESPECTABLE 
                    <div
                    className="afRowPlaysRight"
                    style={{ fontSize: 12, opacity: 0.38, color: view.subColor }}
                  >
                    {renderPlayCount(t)}
                  </div> 
                  */}
                  <div
                    className="afRowDurRight"
                    style={{
                      fontSize: 12,
                      opacity: 0.85,
                      color: view.subColor,
                    }}
                  >
                    {renderDur(t)}
                  </div>
                </div>
              </div>
            );
          })}
        </div>

        <AvailableOnRibbon links={platformLinks} />

        {browseAlbums.length ? (
          <div style={{ marginTop: 18 }}>
            <div
              style={{
                display: "flex",
                justifyContent: "space-between",
                alignItems: "baseline",
                gap: 12,
              }}
            >
              <div
                style={{
                  fontSize: "clamp(20px, 3vw, 28px)",
                  fontWeight: 600,
                  lineHeight: 1.05,
                  letterSpacing: "-0.3px",
                  color: "#fff",
                  marginBottom: 10,
                }}
              >
                More releases
              </div>
            </div>

            <div
              style={{
                display: "grid",
                gridTemplateColumns: "repeat(3, minmax(0, 1fr))",
                gap: 12,
              }}
            >
              {browseAlbums.map((a) => {
                const isActive = effAlbum?.id === a.id;
                const min = a.policy?.minTierToLoad ?? null;
                const canLoadByTier = !min || tierRank(tier) >= tierRank(min);
                const disabled =
                  !onSelectAlbum ||
                  isBrowsingAlbum ||
                  isActive ||
                  !canLoadByTier;
                const isPendingPick =
                  pendingAlbumSlug === a.slug &&
                  (effAlbumSlug !== a.slug ||
                    !effAlbum ||
                    effTracks.length === 0);

                return (
                  <button
                    key={a.id}
                    type="button"
                    disabled={disabled}
                    onMouseEnter={(e) => {
                      prefetchAlbumArt(a.coverUrl);
                      if (disabled) return;
                      e.currentTarget.style.transform =
                        "translateZ(0) translateY(-1px)";
                      e.currentTarget.style.boxShadow =
                        "0 16px 38px rgba(0,0,0,0.22)";
                    }}
                    onMouseLeave={(e) => {
                      e.currentTarget.style.transform = "translateZ(0)";
                      e.currentTarget.style.boxShadow = disabled
                        ? "none"
                        : "0 14px 34px rgba(0,0,0,0.18)";
                    }}
                    onFocus={() => prefetchAlbumArt(a.coverUrl)}
                    onClick={() => {
                      mobileScrollTargetAlbumSlugRef.current = a.slug;
                      setPendingAlbumSlug(a.slug);
                      onSelectAlbum?.(a.slug);
                    }}
                    style={{
                      display: "grid",
                      gridTemplateRows: "auto auto",
                      gap: 10,
                      padding: 12,
                      borderRadius: 16,
                      border: "none",
                      background: isActive
                        ? "color-mix(in srgb, var(--accent) 10%, rgba(255,255,255,0.05))"
                        : "rgba(255,255,255,0.03)",
                      color: "rgba(255,255,255,0.92)",
                      cursor: disabled ? "default" : "pointer",
                      opacity: disabled ? 0.72 : 1,
                      textAlign: "center",
                      boxShadow: disabled
                        ? "none"
                        : "0 14px 34px rgba(0,0,0,0.18)",
                      transform: "translateZ(0)",
                      transition:
                        "transform 140ms ease, border-color 140ms ease, background 140ms ease, box-shadow 140ms ease",
                      position: "relative",
                      overflow: "hidden",
                    }}
                  >
                    <div
                      style={{
                        position: "relative",
                        width: "100%",
                        aspectRatio: "1 / 1",
                        borderRadius: 14,
                        border: "1px solid rgba(255,255,255,0.14)",
                        background: a.coverUrl
                          ? `url(${a.coverUrl}) center/cover no-repeat`
                          : "radial-gradient(60px 60px at 30% 20%, rgba(255,255,255,0.14), rgba(255,255,255,0.02))",
                        boxShadow: "0 18px 40px rgba(0,0,0,0.22)",
                        overflow: "hidden",
                      }}
                    >
                      {isPendingPick ? (
                        <div
                          aria-hidden="true"
                          className="afShimmerBlock"
                          style={{
                            position: "absolute",
                            inset: 0,
                            borderRadius: 14,
                            pointerEvents: "none",
                            opacity: 0.95,
                          }}
                        />
                      ) : null}
                    </div>

                    <div style={{ minWidth: 0 }}>
                      <div
                        style={{
                          fontSize: 13,
                          fontWeight: 650,
                          whiteSpace: "nowrap",
                          overflow: "hidden",
                          textOverflow: "ellipsis",
                        }}
                      >
                        {a.title}
                      </div>
                      <div
                        style={{
                          marginTop: 4,
                          fontSize: 12,
                          opacity: 0.68,
                          whiteSpace: "nowrap",
                          overflow: "hidden",
                          textOverflow: "ellipsis",
                        }}
                      >
                        {a.artist ?? ""}
                      </div>

                      {!canLoadByTier && min ? (
                        <div
                          style={{ marginTop: 6, fontSize: 11, opacity: 0.6 }}
                        >
                          Requires {tierLabel(min)}
                        </div>
                      ) : null}
                    </div>
                  </button>
                );
              })}
            </div>
          </div>
        ) : null}

        {p.lastError ? (
          <div
            style={{
              marginTop: 12,
              borderRadius: 14,
              border: "1px solid rgba(255,255,255,0.14)",
              background: "rgba(0,0,0,0.22)",
              padding: "10px 12px",
              fontSize: 12,
              opacity: 0.85,
              lineHeight: 1.45,
            }}
          >
            {p.lastError}
          </div>
        ) : null}
      </div>

      <style>{`
        @keyframes afShimmer {
          0% { background-position: 200% 0; }
          100% { background-position: -200% 0; }
        }
        .afShimmerText {
          background: linear-gradient(
            90deg,
            rgba(255,255,255,0.55) 0%,
            rgba(255,255,255,0.95) 45%,
            rgba(255,255,255,0.55) 100%
          );
          background-size: 200% 100%;
          -webkit-background-clip: text;
          background-clip: text;
          color: transparent;
          animation: afShimmer 1.1s linear infinite;
        }
        @media (prefers-reduced-motion: reduce) {
          .afShimmerText {
            animation: none;
            color: rgba(255,255,255,0.92);
            background: none;
          }
        }

        .afShimmerBlock{
          background: linear-gradient(
            90deg,
            rgba(255,255,255,0.06) 0%,
            rgba(255,255,255,0.16) 45%,
            rgba(255,255,255,0.06) 100%
          );
          background-size: 200% 100%;
          animation: afShimmer 1.05s linear infinite;
          mix-blend-mode: screen;
        }
        @media (prefers-reduced-motion: reduce) {
          .afShimmerBlock { animation: none; }
        }

        .afEq{
          width: 16px;
          height: 16px;
          display: inline-flex;
          align-items: flex-end;
          gap: 2px;
          color: color-mix(in srgb, var(--accent) 72%, rgba(255,255,255,0.92));
          transform: translateY(-2px);
        }
        .afEq i{
  display: block;
  width: 3px;
  height: 6px;
  background: currentColor;
  border-radius: 2px;
  transform-origin: 50% 100%;
  opacity: 0.9;
  will-change: transform;
  animation: afEq1 920ms linear infinite;
}

.afEq i:nth-child(2){
  height: 10px;
  animation: afEq2 780ms linear infinite;
}

.afEq i:nth-child(3){
  height: 8px;
  animation: afEq3 1040ms linear infinite;
}

@keyframes afEq1{
  0%{transform:scaleY(.40)}
  18%{transform:scaleY(1.10)}
  43%{transform:scaleY(.55)}
  62%{transform:scaleY(1.35)}
  100%{transform:scaleY(.45)}
}
@keyframes afEq2{
  0%{transform:scaleY(.55)}
  22%{transform:scaleY(1.35)}
  50%{transform:scaleY(.45)}
  74%{transform:scaleY(1.05)}
  100%{transform:scaleY(.60)}
}
@keyframes afEq3{
  0%{transform:scaleY(.35)}
  28%{transform:scaleY(1.25)}
  46%{transform:scaleY(.50)}
  68%{transform:scaleY(1.10)}
  100%{transform:scaleY(.40)}
}

.afExplicitBadge{
  flex: 0 0 auto;
  width: 12px;
  height: 12px;
  display: inline-grid;
  place-items: center;
  border-radius: 4px;
  border: none;
  background: rgba(255, 255, 255, 0.31);
  color: rgba(0,0,0,0.92);
  font-size: 7px;
  font-weight: 750;
  line-height: 1;
  letter-spacing: 0.2px;
  transform: translateY(-1px);
  user-select: none;
}

        @media (prefers-reduced-motion: reduce){
          .afEq i{ animation: none; }
        }

                           .afTransportRow{
          width: 100%;
          display: grid;
          grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
          align-items: center;
          gap: 12px;
          margin-top: 8px;
        }

                .afTransportBadgeSlot{
          min-height: 0;
          min-width: 0;
        }

        .afTransportControls{
          display: flex;
          align-items: center;
          gap: 10px;
        }

                  .afArtworkLiveBadge{
          position: absolute;
          top: 12px;
          left: 12px;
          z-index: 2;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          padding: 5px 8px;
          border-radius: 999px;
          background: rgba(0,0,0,0.34);
          border: 1px solid rgba(255,255,255,0.10);
          backdrop-filter: blur(8px);
          -webkit-backdrop-filter: blur(8px);
          color: rgba(255,255,255,0.82);
          font-size: 11px;
          font-weight: 420;
          letter-spacing: 0.01em;
          line-height: 1;
          white-space: nowrap;
          box-shadow: 0 8px 24px rgba(0,0,0,0.20);
          opacity: 0.72;
          animation: afArtworkLiveBadgeIn 160ms ease-out both;
        }

        @keyframes afArtworkLiveBadgeIn{
          0%{
            opacity: 0;
            transform: translateY(-2px);
          }
          100%{
            opacity: 0.82;
            transform: translateY(0);
          }
        }

        @media (prefers-reduced-motion: reduce){
          .afArtworkLiveBadge{ animation: none; }
        }

        .afLiveBadgeDot{
          width: 7px;
          height: 7px;
          border-radius: 999px;
          background: rgba(92, 214, 123, 0.92);
          box-shadow:
            0 0 0 0 rgba(92, 214, 123, 0.22),
            0 0 8px rgba(92, 214, 123, 0.16);
          animation: afLivePulse 2.4s ease-out infinite;
          flex: 0 0 auto;
          transform-origin: 50% 50%;
        }

        @keyframes afLivePulse{
          0%{
            transform: scale(0.94);
            opacity: 0.82;
            box-shadow:
              0 0 0 0 rgba(92, 214, 123, 0.22),
              0 0 8px rgba(92, 214, 123, 0.14);
          }
          45%{
            transform: scale(1.08);
            opacity: 0.96;
            box-shadow:
              0 0 0 4px rgba(92, 214, 123, 0.10),
              0 0 10px rgba(92, 214, 123, 0.18);
          }
          100%{
            transform: scale(0.94);
            opacity: 0.82;
            box-shadow:
              0 0 0 8px rgba(92, 214, 123, 0),
              0 0 8px rgba(92, 214, 123, 0.12);
          }
        }

        @media (prefers-reduced-motion: reduce){
          .afLiveBadgeDot{ animation: none; }
        }

                  @media (max-width: 640px){
          .afArtworkLiveBadge{
            top: 10px;
            left: 10px;
            padding: 6px 9px;
            font-size: 11px;
          }
        }

        .afTrackRow .afRowShare{
          opacity: 0;
          pointer-events: none;
          transform: translateX(2px);
          transition: opacity 120ms ease, transform 120ms ease;
        }

        .afTrackRow:hover .afRowShare,
        .afTrackRow:focus-within .afRowShare{
          opacity: 0.95;
          pointer-events: auto;
          transform: translateX(0);
        }

        .afTrackRowAction{
          appearance: none;
          -webkit-appearance: none;
        }

        .afTrackRowAction:focus-visible{
          outline: 1px solid rgba(255,255,255,0.22);
          outline-offset: -1px;
        }

        .afTrackIndexNumber{
  width: 16px;
  display: inline-grid;
  place-items: center;
  font-variant-numeric: tabular-nums;
}

.afTrackIndexSwap{
  position: relative;
  width: 16px;
  height: 16px;
  display: inline-grid;
  place-items: center;
}

.afTrackIndexSwap .afTrackIndexNumber{
  transition: opacity 120ms ease, transform 120ms ease;
}

.afTrackInlinePlay{
  position: absolute;
  inset: -6px;                 /* slightly larger hit area */
  border: 0;
  pointer-events: auto;
  background: transparent;
  color: currentColor;
  display: grid;
  place-items: center;
  cursor: pointer;
  opacity: 0;
  transform: scale(0.8);
  transition: opacity 120ms ease, transform 120ms ease;
  padding: 0;
  line-height: 0;
}

.afTrackRow:hover .afTrackIndexSwap .afTrackIndexNumber,
.afTrackRow:focus-within .afTrackIndexSwap .afTrackIndexNumber{
  opacity: 0;
  transform: scale(0.88);
}

.afTrackRow:hover .afTrackInlinePlay,
.afTrackRow:focus-within .afTrackInlinePlay{
  opacity: 1;
  transform: scale(1);
}

.afTrackInlinePlay:focus-visible{
  outline: 1px solid rgba(255,255,255,0.28);
  outline-offset: 2px;
}

@media (pointer: coarse){
  .afTrackInlinePlay{
    display: none;
  }

  .afTrackRow:hover .afTrackIndexSwap .afTrackIndexNumber,
  .afTrackRow:focus-within .afTrackIndexSwap .afTrackIndexNumber{
    opacity: 1;
    transform: none;
  }
}

                .afRowMetaUnder{
          display: none;
          margin-top: 4px;
          font-size: 12px;
          opacity: 0.65;
          color: rgba(255,255,255,0.70);
          line-height: 1.1;
          align-items: center;
          gap: 6px;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }

        .afRowMetaDot{
          opacity: 0.5;
          flex: 0 0 auto;
        }

        @media (max-width: 520px){
          .afRowMetaUnder{ display: flex; }
          .afRowPlaysRight{ display: none; }
          .afRowDurRight{ display: none; }
          .afTrackRow .afRowShare{
            opacity: 0.95;
            pointer-events: auto;
            transform: none;
          }
        }

       .afAvailWrap{
  margin: 18px 0px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8px;
  color: rgba(255,255,255,0.62);
  user-select: none;
  position: relative;
}

/* horizontal rule ABOVE the label */
.afAvailWrap::before{
  content: "";
  width: fit-content;
  min-width: 120px;                 /* safety floor */
  height: 1px;
  background: rgba(255,255,255,0.14);
  border-radius: 999px;
  opacity: 0.85;
  margin-bottom: 3px;               /* gap between line and label */
}

/* make rule auto-match icon width */
.afAvailWrap{
  width: fit-content;
  margin-left: auto;
  margin-right: auto;
}

.afAvailLabel{
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.28px;
  text-transform: uppercase;
  opacity: 0.65;
  text-align: center;
}

.afAvailIcons{
  display: inline-flex;
  align-items: center;
  gap: 10px;
}

.afAvailIcon{
  display: grid;
  place-items: center;
  width: 22px;          /* tighter since we’re not boxing */
  height: 22px;
  padding: 0;
  border: 0;
  background: transparent;
  border-radius: 0;
  color: rgba(255,255,255,0.72);
  opacity: 0.65;
  transition: transform 120ms ease, color 120ms ease, opacity 120ms ease;
}

.afAvailIcon:hover{
  transform: translateY(-1px);
  background: transparent;
  border-color: transparent;
  color: rgba(255,255,255,0.90);
  opacity: 0.90;
}

.afAvailIcon:focus-visible{
  outline: none;
  box-shadow: 0 0 0 3px rgba(255,255,255,0.12); /* keep accessible focus */
  border-radius: 10px; /* focus ring shape only */
}




      `}</style>
    </div>
  );
}


----- app/home/player/PlayerState.tsx -----
// web/app/home/player/PlayerState.tsx
"use client";

import React from "react";
import { useAuth } from "@clerk/nextjs";
import type { PlayerTrack } from "@/lib/types";
import { ensureLyricsForTrack } from "@/app/home/player/lyrics/ensureLyricsForTrack";
import {
  fetchPlaybackAccessDecision,
  readPlaybackShareTokenFromLocation,
  type PlaybackAccessDecision,
} from "./playbackAccessClient";

type PlayerStatus = "idle" | "loading" | "playing" | "paused";
type RepeatMode = "off" | "one" | "all";
type Intent = "play" | "pause" | null;
type LoadingReason = "token" | "attach" | "buffering";

export type QueueContext = {
  contextId?: string;
  contextSlug?: string;
  contextTitle?: string;
  contextArtist?: string;
  artworkUrl?: string | null;
  sharePlaybackContext?: string | null;
  sharePlaybackScopeId?: string | null;

  // Lets a deep link prepare an album queue around a specific track without
  // beginning playback or rearranging the album’s natural track order.
  initialCurrentRecordingId?: string;
};

export type PlayerState = {
  status: PlayerStatus;
  current?: PlayerTrack;
  queue: PlayerTrack[];
  lastError?: string;
  lastPlayAttemptAtMs?: number;

  // True only while a user-initiated play request is awaiting the client-side
  // access decision. This is visual state: it must not itself arm protected
  // media attachment.
  playRequestPending: boolean;

  queueContextId?: string;
  queueContextSlug?: string;
  queueContextTitle?: string;
  queueContextArtist?: string;
  queueContextArtworkUrl?: string | null;
  queueSharePlaybackContext?: string | null;
  queueSharePlaybackScopeId?: string | null;

  intent: Intent;
  intentAtMs?: number;
  selectedRecordingId?: string;
  pendingRecordingId?: string;

  pendingSeekMs?: number;
  seeking: boolean;

  loadingReason?: LoadingReason;
  reloadNonce: number;

  positionMs: number;
  seekNonce: number;

  volume: number;
  muted: boolean;
  repeat: RepeatMode;

  // Catalogue duration supplied by Sanity. This remains stable metadata and a
  // pre-load fallback, but is not assumed to be the decoded asset's exact end.
  durationByRecordingId: Record<string, number>;

  // Runtime duration reported by the browser for the actual delivered Mux
  // rendition. Keyed by playback ID so a future asset replacement cannot reuse
  // a stale duration merely because it shares a recording ID.
  assetDurationByPlaybackId: Record<string, number>;
};

type PlayerActions = {
  play: (track?: PlayerTrack) => void;
  pause: () => void;
  next: () => void;
  advanceFromEngine: () => void;
  prev: () => void;

  setQueue: (tracks: PlayerTrack[], opts?: QueueContext) => void;
  enqueue: (track: PlayerTrack) => void;
  clearQueue: () => void;

  setPositionMs: (ms: number) => void;
  setDurationMs: (ms: number) => void;
  setAssetDurationMs: (playbackId: string, ms: number) => void;

  setStatusExternal: (s: PlayerStatus) => void;
  setLoadingReasonExternal: (r?: LoadingReason) => void;

  setIntent: (i: Intent) => void;
  clearIntent: () => void;
  selectTrack: (id?: string) => void;
  setPendingRecordingId: (id?: string) => void;
  resolvePendingTrack: (id: string) => void;

  seek: (ms: number) => void;
  clearPendingSeek: () => void;

  setVolume: (v: number) => void;
  toggleMute: () => void;

  cycleRepeat: () => void;
  tick: (deltaMs: number) => void;

  clearError: () => void;
  bumpReload: () => void;
};

type PlayerVisualState = {
  status: PlayerStatus;
  intent: Intent;
  playRequestPending: boolean;
  current?: PlayerTrack;
  firstQueuedTrack?: PlayerTrack;
};

const PlayerCtx = React.createContext<(PlayerState & PlayerActions) | null>(
  null,
);
const PlayerActionsCtx = React.createContext<PlayerActions | null>(null);
const PlayerVisualCtx = React.createContext<PlayerVisualState | null>(null);

function clamp(n: number, lo: number, hi: number) {
  return Math.max(lo, Math.min(hi, n));
}

function nextRepeat(r: RepeatMode): RepeatMode {
  if (r === "off") return "all";
  if (r === "all") return "one";
  return "off";
}

function positiveDurationMs(value: number | undefined): number {
  return typeof value === "number" && Number.isFinite(value) && value > 0
    ? Math.round(value)
    : 0;
}

function getAssetDurationMs(
  assetDurationByPlaybackId: Record<string, number>,
  track: PlayerTrack | undefined,
): number {
  const playbackId = (track?.muxPlaybackId ?? "").trim();

  return playbackId
    ? positiveDurationMs(assetDurationByPlaybackId[playbackId])
    : 0;
}

function getEffectiveDurationMs(
  state: Pick<
    PlayerState,
    "durationByRecordingId" | "assetDurationByPlaybackId"
  >,
  track: PlayerTrack | undefined,
): number {
  const assetDurationMs = getAssetDurationMs(
    state.assetDurationByPlaybackId,
    track,
  );

  if (assetDurationMs > 0) return assetDurationMs;

  const recordingId = (track?.recordingId ?? "").trim();
  const catalogueDurationMs = recordingId
    ? positiveDurationMs(state.durationByRecordingId[recordingId])
    : 0;

  return catalogueDurationMs || positiveDurationMs(track?.durationMs);
}

function retainAssetDurationsByPlaybackId(
  previous: Record<string, number>,
  tracks: PlayerTrack[],
): Record<string, number> {
  const activePlaybackIds = new Set<string>();

  for (const track of tracks) {
    const playbackId = (track.muxPlaybackId ?? "").trim();
    if (playbackId) activePlaybackIds.add(playbackId);
  }

  let next = previous;

  for (const playbackId of Object.keys(previous)) {
    if (activePlaybackIds.has(playbackId)) continue;

    if (next === previous) next = { ...previous };
    delete next[playbackId];
  }

  return next;
}

function hydrateTrack(
  t: PlayerTrack,
  durationByRecordingId: Record<string, number>,
): PlayerTrack {
  const cached = durationByRecordingId[t.recordingId];
  if (!cached || cached <= 0) return t;
  if (t.durationMs === cached) return t;
  return { ...t, durationMs: cached };
}

function hydrateTracks(
  ts: PlayerTrack[],
  durationByRecordingId: Record<string, number>,
) {
  let changed = false;
  const next = ts.map((t) => {
    const ht = hydrateTrack(t, durationByRecordingId);
    if (ht !== t) changed = true;
    return ht;
  });
  return changed ? next : ts;
}

function blockedPlaybackMessage(access: PlaybackAccessDecision): string {
  if (access.embargoed) {
    return "Playback is disabled pre-release.";
  }

  if (access.reason) return access.reason;

  return "Playback is not available for this release.";
}

type TargetedPlayIntentDetail = {
  track: PlayerTrack;
  albumId: string | null;
};

function dispatchTargetedPlayIntent(
  track: PlayerTrack,
  state: Pick<PlayerState, "queueContextId">,
): void {
  if (typeof window === "undefined") return;

  const albumId = (state.queueContextId ?? "").trim() || null;

  window.dispatchEvent(
    new CustomEvent<TargetedPlayIntentDetail>("af:play-target-intent", {
      detail: {
        track,
        albumId,
      },
    }),
  );
}

function getNextTransportTrack(state: PlayerState): PlayerTrack | undefined {
  const current = state.current;

  if (!current || state.queue.length === 0) return undefined;
  if (state.repeat === "one") return current;

  const index = state.queue.findIndex(
    (track) => track.recordingId === current.recordingId,
  );
  const currentIndex = Math.max(index, 0);

  if (currentIndex + 1 < state.queue.length) {
    return state.queue[currentIndex + 1];
  }

  return state.repeat === "all" ? state.queue[0] : undefined;
}

function getPreviousTransportTrack(
  state: PlayerState,
): PlayerTrack | undefined {
  const current = state.current;

  if (!current || state.queue.length === 0) return undefined;

  if (state.status === "playing" && state.positionMs > 3000) {
    return current;
  }

  const index = state.queue.findIndex(
    (track) => track.recordingId === current.recordingId,
  );
  const currentIndex = Math.max(index, 0);

  if (currentIndex > 0) {
    return state.queue[currentIndex - 1];
  }

  if (state.repeat === "all") {
    return state.queue.at(-1);
  }

  return current;
}

function primeDurationByRecordingId(
  prev: Record<string, number>,
  tracks: PlayerTrack[],
): Record<string, number> {
  let next = prev;
  for (const t of tracks) {
    if (!t?.recordingId) continue;
    const ms = t.durationMs;
    if (typeof ms !== "number" || !Number.isFinite(ms) || ms <= 0) continue;
    if (typeof next[t.recordingId] === "number" && next[t.recordingId] > 0)
      continue;
    if (next === prev) next = { ...prev };
    next[t.recordingId] = ms;
  }
  return next;
}

function resolvePlaybackAccessIdentityKey(
  params: Readonly<{
    clerkAuthLoaded: boolean;
    isSignedIn: boolean | undefined;
    userId: string | null | undefined;
    sessionId: string | null | undefined;
  }>,
): string {
  if (!params.clerkAuthLoaded) return "clerk:loading";
  if (!params.isSignedIn) return "clerk:anonymous";

  return `clerk:user:${params.userId ?? ""}:session:${params.sessionId ?? ""}`;
}

function markCurrentTrackLoading(
  state: PlayerState,
  loadingReason: "attach",
): PlayerState {
  const current = state.current;
  if (!current) return state;

  return {
    ...state,
    status: "loading",
    loadingReason,
    positionMs: 0,
    intent: "play",
    intentAtMs: Date.now(),
    pendingRecordingId: current.recordingId,
    selectedRecordingId: current.recordingId,
  };
}

function markNextTrackLoading(
  state: PlayerState,
  track: PlayerTrack,
  loadingReason: "token" | "attach",
): PlayerState {
  const hydratedTrack = hydrateTrack(track, state.durationByRecordingId);

  return {
    ...state,
    current: hydratedTrack,
    status: "loading",
    loadingReason,
    positionMs: 0,
    intent: "play",
    intentAtMs: Date.now(),
    pendingRecordingId: hydratedTrack.recordingId,
    selectedRecordingId: hydratedTrack.recordingId,
  };
}

function transitionForwardState(
  state: PlayerState,
  nextTrackLoadingReason: "token" | "attach",
  terminalIntent: "pause" | null,
): PlayerState {
  const current = state.current;
  if (!current || state.queue.length === 0) return state;

  if (state.repeat === "one") {
    return markCurrentTrackLoading(state, "attach");
  }

  const index = state.queue.findIndex(
    (track) => track.recordingId === current.recordingId,
  );
  const currentIndex = Math.max(index, 0);
  const nextTrack = state.queue[currentIndex + 1];

  if (nextTrack) {
    return markNextTrackLoading(state, nextTrack, nextTrackLoadingReason);
  }

  if (state.repeat === "all") {
    const firstTrack = state.queue[0];
    if (firstTrack) {
      return markNextTrackLoading(state, firstTrack, nextTrackLoadingReason);
    }
  }

  return {
    ...state,
    status: "paused",
    intent: terminalIntent,
    intentAtMs: terminalIntent === "pause" ? Date.now() : undefined,
    positionMs: 0,
    pendingSeekMs: 0,
    seeking: true,
    seekNonce: state.seekNonce + 1,
    loadingReason: undefined,
    pendingRecordingId: undefined,
  };
}

function transitionPreviousState(state: PlayerState): PlayerState {
  const current = state.current;
  if (!current || state.queue.length === 0) return state;

  if (state.status === "playing" && state.positionMs > 3000) {
    return markCurrentTrackLoading(state, "attach");
  }

  const index = state.queue.findIndex(
    (track) => track.recordingId === current.recordingId,
  );
  const currentIndex = Math.max(index, 0);
  const previousTrack = state.queue[currentIndex - 1];

  if (previousTrack) {
    return markNextTrackLoading(state, previousTrack, "token");
  }

  if (state.repeat === "all") {
    const lastTrack = state.queue.at(-1);
    if (lastTrack) {
      return markNextTrackLoading(state, lastTrack, "token");
    }
  }

  return markCurrentTrackLoading(state, "attach");
}

type QueueContextStatePatch = Partial<
  Pick<
    PlayerState,
    | "queueContextId"
    | "queueContextSlug"
    | "queueContextTitle"
    | "queueContextArtist"
    | "queueContextArtworkUrl"
    | "queueSharePlaybackContext"
    | "queueSharePlaybackScopeId"
  >
>;

function normalizedQueueShareValue(
  value: string | null | undefined,
): string | null {
  if (typeof value !== "string") return null;
  return value.trim() || null;
}

function buildQueueContextStatePatch(
  state: PlayerState,
  opts: QueueContext | undefined,
): QueueContextStatePatch {
  const patch: QueueContextStatePatch = {};
  const contextId = typeof opts?.contextId === "string" ? opts.contextId : "";
  const contextSlug =
    typeof opts?.contextSlug === "string" ? opts.contextSlug.trim() : "";
  const contextTitle =
    typeof opts?.contextTitle === "string" ? opts.contextTitle.trim() : "";
  const contextArtist =
    typeof opts?.contextArtist === "string" ? opts.contextArtist.trim() : "";

  if (contextId.length > 0) patch.queueContextId = contextId;
  if (contextSlug) patch.queueContextSlug = contextSlug;
  if (contextTitle) patch.queueContextTitle = contextTitle;
  if (contextArtist) patch.queueContextArtist = contextArtist;

  patch.queueContextArtworkUrl =
    opts?.artworkUrl !== undefined
      ? (opts.artworkUrl ?? null)
      : (state.queueContextArtworkUrl ?? null);

  const optionBag = opts ?? {};

  if (Object.hasOwn(optionBag, "sharePlaybackContext")) {
    patch.queueSharePlaybackContext = normalizedQueueShareValue(
      opts?.sharePlaybackContext,
    );
  }

  if (Object.hasOwn(optionBag, "sharePlaybackScopeId")) {
    patch.queueSharePlaybackScopeId = normalizedQueueShareValue(
      opts?.sharePlaybackScopeId,
    );
  }

  return patch;
}

function findRequestedInitialCurrent(
  queue: readonly PlayerTrack[],
  opts: QueueContext | undefined,
): PlayerTrack | null {
  const recordingId =
    typeof opts?.initialCurrentRecordingId === "string"
      ? opts.initialCurrentRecordingId.trim()
      : "";

  if (!recordingId) return null;

  return (
    queue.find((track) => track.recordingId === recordingId) ?? null
  );
}

type QueueSelectionState = Pick<
  PlayerState,
  "positionMs" | "selectedRecordingId" | "pendingRecordingId"
>;

function resolveQueueSelectionState(
  state: PlayerState,
  nextCurrent: PlayerTrack | undefined,
  requestedInitialCurrent: PlayerTrack | null,
): QueueSelectionState {
  if (requestedInitialCurrent) {
    return {
      positionMs: 0,
      selectedRecordingId: requestedInitialCurrent.recordingId,
      pendingRecordingId: undefined,
    };
  }

  return {
    positionMs: state.current ? state.positionMs : 0,
    selectedRecordingId:
      state.selectedRecordingId ?? nextCurrent?.recordingId,
    pendingRecordingId: state.pendingRecordingId,
  };
}

function applyQueueState(
  state: PlayerState,
  tracks: PlayerTrack[],
  opts: QueueContext | undefined,
): PlayerState {
  const nextDurationByRecordingId = primeDurationByRecordingId(
    state.durationByRecordingId,
    tracks,
  );
  const nextAssetDurationByPlaybackId = retainAssetDurationsByPlaybackId(
    state.assetDurationByPlaybackId,
    tracks,
  );
  const hydratedQueue = hydrateTracks(tracks, nextDurationByRecordingId);
  const requestedInitialCurrent = findRequestedInitialCurrent(
    hydratedQueue,
    opts,
  );
  const nextCurrentRaw =
    requestedInitialCurrent ?? state.current ?? hydratedQueue[0];
  const nextCurrent = nextCurrentRaw
    ? hydrateTrack(nextCurrentRaw, nextDurationByRecordingId)
    : undefined;

  return {
    ...state,
    ...buildQueueContextStatePatch(state, opts),
    durationByRecordingId: nextDurationByRecordingId,
    assetDurationByPlaybackId: nextAssetDurationByPlaybackId,
    queue: hydratedQueue,
    current: nextCurrent,
    ...resolveQueueSelectionState(
      state,
      nextCurrent,
      requestedInitialCurrent,
    ),
  };
}

export function PlayerStateProvider(
  props: Readonly<{ children: React.ReactNode }>,
) {
  const {
    isLoaded: clerkAuthLoaded,
    isSignedIn,
    userId,
    sessionId,
  } = useAuth();

  const playbackAccessIdentityKey = resolvePlaybackAccessIdentityKey({
    clerkAuthLoaded,
    isSignedIn,
    userId,
    sessionId,
  });

  const [state, setState] = React.useState<PlayerState>({
    status: "idle",
    current: undefined,
    queue: [],
    lastError: undefined,
    lastPlayAttemptAtMs: undefined,
    playRequestPending: false,

    queueContextId: undefined,
    queueContextSlug: undefined,
    queueContextTitle: undefined,
    queueContextArtist: undefined,
    queueContextArtworkUrl: null,
    queueSharePlaybackContext: null,
    queueSharePlaybackScopeId: null,

    intent: null,
    intentAtMs: undefined,
    selectedRecordingId: undefined,
    pendingRecordingId: undefined,

    pendingSeekMs: undefined,
    seeking: false,

    loadingReason: undefined,
    reloadNonce: 0,

    positionMs: 0,
    seekNonce: 0,

    volume: 0.9,
    muted: false,
    repeat: "off",

    durationByRecordingId: {},
    assetDurationByPlaybackId: {},
  });

  const stateRef = React.useRef(state);
  const playRequestSequenceRef = React.useRef(0);

  React.useEffect(() => {
    stateRef.current = state;
  }, [state]);

  const blockPlayback = React.useCallback((message: string) => {
    setState((s) => ({
      ...s,
      status: s.status === "loading" ? "idle" : s.status,
      intent: "pause",
      intentAtMs: Date.now(),
      playRequestPending: false,
      loadingReason: undefined,
      pendingRecordingId: undefined,
      lastError: message,
    }));
  }, []);

  const guardPlayback = React.useCallback(
    async (onAllowed: () => void): Promise<void> => {
      const catalogueId = (stateRef.current.queueContextId ?? "").trim();

      if (!catalogueId) {
        onAllowed();
        return;
      }

      try {
        const access = await fetchPlaybackAccessDecision({
          catalogueId,
          shareToken: readPlaybackShareTokenFromLocation(),
          accessIdentityKey: playbackAccessIdentityKey,
        });

        if (!access.allowed) {
          blockPlayback(blockedPlaybackMessage(access));
          return;
        }

        const expectedSharePlaybackScopeId = `alb:${catalogueId.replace(
          /^alb:/i,
          "",
        )}`;

        if (
          access.sharePlaybackContext &&
          access.sharePlaybackScopeId === expectedSharePlaybackScopeId
        ) {
          setState((current) => ({
            ...current,
            queueSharePlaybackContext: access.sharePlaybackContext,
            queueSharePlaybackScopeId: access.sharePlaybackScopeId,
          }));
        }

        onAllowed();
      } catch {
        blockPlayback("Playback access could not be verified.");
      }
    },
    [blockPlayback, playbackAccessIdentityKey],
  );
  const actions: PlayerActions = React.useMemo(() => {
    return {
      setIntent: (i: Intent) =>
        setState((s) => ({
          ...s,
          intent: i,
          intentAtMs: i ? Date.now() : undefined,
        })),

      clearIntent: () =>
        setState((s) =>
          s.intent ? { ...s, intent: null, intentAtMs: undefined } : s,
        ),

      selectTrack: (id?: string) =>
        setState((s) => {
          if (s.selectedRecordingId === id) return s;
          return {
            ...s,
            selectedRecordingId: id,
          };
        }),

      setPendingRecordingId: (id?: string) =>
        setState((s) => {
          if (s.pendingRecordingId === id) return s;
          return {
            ...s,
            pendingRecordingId: id,
          };
        }),

      resolvePendingTrack: (id: string) =>
        setState((s) => {
          if (s.pendingRecordingId !== id) return s;
          return { ...s, pendingRecordingId: undefined };
        }),

      play: (track?: PlayerTrack) => {
        const requestSequence = ++playRequestSequenceRef.current;
        const requestStartedAtMs = Date.now();
        const snapshot = stateRef.current;
        const requestedTrack = track ?? snapshot.current ?? snapshot.queue[0];

        if (requestedTrack) {
          dispatchTargetedPlayIntent(
            hydrateTrack(requestedTrack, snapshot.durationByRecordingId),
            snapshot,
          );
        }

        setState((s) => ({
          ...s,
          playRequestPending: true,
          lastPlayAttemptAtMs: requestStartedAtMs,
          lastError: undefined,
        }));

        void guardPlayback(() => {
          if (requestSequence !== playRequestSequenceRef.current) {
            return;
          }

          setState((s) => {
            if (requestSequence !== playRequestSequenceRef.current) {
              return s;
            }

            const now = Date.now();
            const rawNext = track ?? s.current ?? s.queue[0];

            if (!rawNext) {
              return {
                ...s,
                status: "idle",
                current: undefined,
                positionMs: 0,
                intent: "play",
                intentAtMs: now,
                lastPlayAttemptAtMs: now,
                playRequestPending: false,
                lastError: undefined,
                loadingReason: undefined,
                pendingRecordingId: undefined,
              };
            }

            const nextTrack = hydrateTrack(rawNext, s.durationByRecordingId);
            const sameTrack =
              s.current?.recordingId === nextTrack.recordingId;

            const base = {
              ...s,
              intent: "play" as const,
              intentAtMs: now,
              lastPlayAttemptAtMs: now,
              playRequestPending: false,
              lastError: undefined,
              selectedRecordingId: nextTrack.recordingId,
              pendingRecordingId: nextTrack.recordingId,
            };

            if (sameTrack && s.status === "paused") {
              return {
                ...base,
                current: hydrateTrack(s.current!, s.durationByRecordingId),
                status: "paused",
                loadingReason: undefined,
              };
            }

            if (
              sameTrack &&
              (s.status === "playing" || s.status === "loading")
            ) {
              return { ...base, loadingReason: s.loadingReason };
            }

            return {
              ...base,
              current: nextTrack,
              status: "loading",
              loadingReason: "token",
              positionMs: 0,
            };
          });
        });
      },

      pause: () => {
        playRequestSequenceRef.current += 1;

        setState((s) => ({
          ...s,
          intent: "pause",
          intentAtMs: Date.now(),
          playRequestPending: false,
        }));
      },

      next: () => {
        const snapshot = stateRef.current;
        const targetTrack = getNextTransportTrack(snapshot);

        if (targetTrack) {
          dispatchTargetedPlayIntent(
            hydrateTrack(targetTrack, snapshot.durationByRecordingId),
            snapshot,
          );
        }

        void guardPlayback(() => {
          setState((s) => transitionForwardState(s, "token", "pause"));
        });
      },

      advanceFromEngine: () => {
        setState((s) => transitionForwardState(s, "attach", null));
      },

      prev: () => {
        const snapshot = stateRef.current;
        const targetTrack = getPreviousTransportTrack(snapshot);

        if (targetTrack) {
          dispatchTargetedPlayIntent(
            hydrateTrack(targetTrack, snapshot.durationByRecordingId),
            snapshot,
          );
        }

        void guardPlayback(() => {
          setState(transitionPreviousState);
        });
      },

      setQueue: (tracks: PlayerTrack[], opts?: QueueContext) =>
        setState((s) => applyQueueState(s, tracks, opts)),

      enqueue: (track: PlayerTrack) =>
        setState((s) => {
          const nextDurationByRecordingId = primeDurationByRecordingId(
            s.durationByRecordingId,
            [track],
          );
          const t = hydrateTrack(track, nextDurationByRecordingId);
          return {
            ...s,
            durationByRecordingId: nextDurationByRecordingId,
            queue: [...s.queue, t],
            current: s.current ?? t,
            selectedRecordingId: s.selectedRecordingId ?? t.recordingId,
          };
        }),

      clearQueue: () =>
        setState((s) => ({
          ...s,
          queue: [],
          current: undefined,
          selectedRecordingId: undefined,
          pendingRecordingId: undefined,

          positionMs: 0,
          pendingSeekMs: undefined,
          seeking: false,
          seekNonce: s.seekNonce + 1,
          loadingReason: undefined,
          queueSharePlaybackContext: null,
          queueSharePlaybackScopeId: null,
          assetDurationByPlaybackId: {},
        })),

      setPositionMs: (ms: number) =>
        setState((s) => ({
          ...s,
          positionMs: Math.max(0, ms),
        })),

      setDurationMs: (ms: number) =>
        setState((s) => {
          const cur = s.current;
          if (!cur) return s;
          if (!Number.isFinite(ms) || ms <= 0) return s;

          const alreadyCached =
            typeof s.durationByRecordingId[cur.recordingId] === "number" &&
            s.durationByRecordingId[cur.recordingId] > 0;
          const alreadyOnTrack =
            typeof cur.durationMs === "number" && cur.durationMs > 0;
          if (alreadyCached || alreadyOnTrack) return s;

          const nextDurationByRecordingId = {
            ...s.durationByRecordingId,
            [cur.recordingId]: ms,
          };

          const nextCurrent = { ...cur, durationMs: ms };
          let changed = false;
          const nextQueue = s.queue.map((t) => {
            if (t.recordingId !== cur.recordingId) return t;
            if (typeof t.durationMs === "number" && t.durationMs > 0) return t;
            changed = true;
            return { ...t, durationMs: ms };
          });

          return {
            ...s,
            durationByRecordingId: nextDurationByRecordingId,
            current: nextCurrent,
            queue: changed ? nextQueue : s.queue,
          };
        }),

      setAssetDurationMs: (playbackId: string, ms: number) => {
        const normalizedPlaybackId = playbackId.trim();
        const normalizedDurationMs = positiveDurationMs(ms);

        if (!normalizedPlaybackId || normalizedDurationMs <= 0) return;

        setState((s) => {
          if (
            s.assetDurationByPlaybackId[normalizedPlaybackId] ===
            normalizedDurationMs
          ) {
            return s;
          }

          return {
            ...s,
            assetDurationByPlaybackId: {
              ...s.assetDurationByPlaybackId,
              [normalizedPlaybackId]: normalizedDurationMs,
            },
          };
        });
      },

      setStatusExternal: (st: PlayerStatus) =>
        setState((s) => (s.status === st ? s : { ...s, status: st })),

      setLoadingReasonExternal: (r?: LoadingReason) =>
        setState((s) =>
          s.loadingReason === r ? s : { ...s, loadingReason: r },
        ),

      seek: (ms: number) => {
        setState((s) => {
          const dur = getEffectiveDurationMs(s, s.current);
          const next = dur > 0 ? clamp(ms, 0, dur) : Math.max(0, ms);
          return {
            ...s,
            positionMs: next,
            pendingSeekMs: next,
            seeking: true,
            seekNonce: s.seekNonce + 1,
          };
        });
      },

      clearPendingSeek: () =>
        setState((s) => {
          if (!s.seeking && s.pendingSeekMs == null) return s;
          return { ...s, seeking: false, pendingSeekMs: undefined };
        }),

      setVolume: (v: number) =>
        setState((s) => ({ ...s, volume: clamp(v, 0, 1) })),
      toggleMute: () => setState((s) => ({ ...s, muted: !s.muted })),

      cycleRepeat: () =>
        setState((s) => ({ ...s, repeat: nextRepeat(s.repeat) })),

      tick: (deltaMs: number) => {
        setState((s) => {
          if (s.status !== "playing") return s;

          const dur = getEffectiveDurationMs(s, s.current);
          const nextPos = Math.max(0, s.positionMs + Math.max(0, deltaMs));

          if (dur <= 0) return { ...s, positionMs: nextPos };
          if (nextPos < dur) return { ...s, positionMs: nextPos };

          if (s.repeat === "one") return { ...s, positionMs: 0 };

          const cur = s.current;
          const idx = cur
            ? s.queue.findIndex((t) => t.recordingId === cur.recordingId)
            : -1;
          const at = Math.max(idx, 0);
          const nextIdx = at + 1;

          if (nextIdx < s.queue.length) {
            const t = hydrateTrack(s.queue[nextIdx], s.durationByRecordingId);
            return {
              ...s,
              current: t,
              positionMs: 0,
              selectedRecordingId: t.recordingId,
              pendingRecordingId: t.recordingId,
            };
          }

          if (s.repeat === "all" && s.queue.length > 0) {
            const t = hydrateTrack(s.queue[0], s.durationByRecordingId);
            return {
              ...s,
              current: t,
              positionMs: 0,
              selectedRecordingId: t.recordingId,
              pendingRecordingId: t.recordingId,
            };
          }

          return { ...s, status: "paused", positionMs: dur };
        });
      },

      clearError: () =>
        setState((s) => (s.lastError ? { ...s, lastError: undefined } : s)),

      bumpReload: () => {
        void guardPlayback(() => {
          setState((s) => {
            if (!s.current?.muxPlaybackId) {
              return { ...s, reloadNonce: s.reloadNonce + 1 };
            }

            return {
              ...s,
              reloadNonce: s.reloadNonce + 1,
              status: "loading",
              loadingReason: "token",
              lastError: undefined,
              intent: "play",
              intentAtMs: Date.now(),
              pendingRecordingId: s.current.recordingId,
              selectedRecordingId: s.current.recordingId,
            };
          });
        });
      },
    };
  }, [guardPlayback]);

  const api: PlayerState & PlayerActions = React.useMemo(() => {
    return {
      ...state,
      ...actions,
    };
  }, [state, actions]);

  const {
    status: visualStatus,
    intent: visualIntent,
    playRequestPending: visualPlayRequestPending,
    current: visualCurrent,
    queue: visualQueue,
  } = state;

  const firstQueuedTrack = visualQueue[0];

  const visualState: PlayerVisualState = React.useMemo(
    () => ({
      status: visualStatus,
      intent: visualIntent,
      playRequestPending: visualPlayRequestPending,
      current: visualCurrent,
      firstQueuedTrack,
    }),
    [
      visualStatus,
      visualIntent,
      visualPlayRequestPending,
      visualCurrent,
      firstQueuedTrack,
    ],
  );

  const currentId = state.current?.recordingId ?? null;
  const pendingId = state.pendingRecordingId ?? null;

  React.useEffect(() => {
    const id = (currentId ?? "").trim();
    if (!id) return;
    void ensureLyricsForTrack(id);
  }, [currentId]);

  React.useEffect(() => {
    const id = (pendingId ?? "").trim();
    if (!id) return;
    void ensureLyricsForTrack(id);
  }, [pendingId]);

  return (
    <PlayerActionsCtx.Provider value={actions}>
      <PlayerVisualCtx.Provider value={visualState}>
        <PlayerCtx.Provider value={api}>{props.children}</PlayerCtx.Provider>
      </PlayerVisualCtx.Provider>
    </PlayerActionsCtx.Provider>
  );
}

export function usePlayer() {
  const ctx = React.useContext(PlayerCtx);
  if (!ctx)
    throw new Error("usePlayer must be used within PlayerStateProvider");
  return ctx;
}

export function usePlayerActions() {
  const ctx = React.useContext(PlayerActionsCtx);
  if (!ctx)
    throw new Error("usePlayerActions must be used within PlayerStateProvider");
  return ctx;
}

export function usePlayerVisual() {
  const ctx = React.useContext(PlayerVisualCtx);
  if (!ctx)
    throw new Error("usePlayerVisual must be used within PlayerStateProvider");
  return ctx;
}

----- app/home/player/StageCore.tsx -----
// web/app/home/player/StageCore.tsx
"use client";

import React from "react";
import { usePlayerActions, usePlayerVisual } from "./PlayerState";
import VisualizerCanvas from "./VisualizerCanvas";
import LyricsOverlay from "./stage/LyricsOverlay";
import type { LyricCue } from "@/lib/types";
import { STAGE_TRANSPORT_FOOTER_PX } from "./StageTransportBar";
import { mediaSurface } from "./mediaSurface";
import { useLyricsSnapshot } from "./lyrics/useLyricsSurface";

type CuesByRecordingId = Record<string, LyricCue[]>;
type OffsetByRecordingId = Record<string, number>;

function pickKeyWithCues(
  cuesByRecordingId: CuesByRecordingId | undefined,
  keys: Array<string | null | undefined>,
) {
  if (!cuesByRecordingId) return (keys.find(Boolean) as string | null) ?? null;
  for (const k of keys) {
    if (!k) continue;
    const xs = cuesByRecordingId[k];
    if (Array.isArray(xs) && xs.length) return k;
  }
  return (keys.find(Boolean) as string | null) ?? null;
}

function StageCore(props: {
  variant: "inline" | "fullscreen";
  cuesByRecordingId?: CuesByRecordingId;
  offsetByRecordingId?: OffsetByRecordingId;
  offsetMs?: number;
  autoResumeOnSeek?: boolean;
  lyricsMode?: "embedded" | "none";
}) {
  const {
    variant,
    cuesByRecordingId: cuesByRecordingIdProp,
    offsetByRecordingId: offsetByRecordingIdProp,
    offsetMs: globalOffsetMs = 0,
    autoResumeOnSeek = false,
    lyricsMode = "embedded",
  } = props;

  const player = usePlayerVisual();
  const actions = usePlayerActions();
  const snap = useLyricsSnapshot();

  // ✅ prefer props when provided; otherwise use lyricsSurface
  const cuesByRecordingId = cuesByRecordingIdProp ?? snap.cuesByRecordingId;
  const offsetByRecordingId =
    offsetByRecordingIdProp ?? snap.offsetByRecordingId;

  // Register stage presence; fullscreen wins if it exists.
  React.useEffect(() => mediaSurface.registerStage(variant), [variant]);

  const [surfaceRecordingId, setSurfaceRecordingId] = React.useState<
    string | null
  >(() => mediaSurface.getRecordingId());

  React.useEffect(() => {
    const unsub = mediaSurface.subscribe((e) => {
      if (e.type === "track") setSurfaceRecordingId(e.id);
    });
    return unsub;
  }, []);

  const playerRecordingId = player.current?.recordingId ?? null;
  const playerDisplayId = player.current?.displayId ?? null;
  const playerMuxId = player.current?.muxPlaybackId ?? null;

  const recordingId = pickKeyWithCues(cuesByRecordingId, [
    playerRecordingId,
    surfaceRecordingId,
    playerMuxId,
  ]);

  const displayId = playerDisplayId;

  const cues: LyricCue[] | null = recordingId
    ? Array.isArray(cuesByRecordingId?.[recordingId]) &&
      cuesByRecordingId[recordingId].length
      ? cuesByRecordingId[recordingId]
      : null
    : null;

  const trackOffsetMs =
    recordingId && typeof offsetByRecordingId?.[recordingId] === "number"
      ? Number.isFinite(offsetByRecordingId[recordingId])
        ? offsetByRecordingId[recordingId]
        : 0
      : 0;

  const effectiveOffsetMs = trackOffsetMs + globalOffsetMs;
  const resumeTrack = player.current ?? player.firstQueuedTrack;

  const onSeek = React.useCallback(
    (tMs: number) => {
      const ms = Math.max(0, Math.floor(tMs));
      actions.seek(ms);

      if (autoResumeOnSeek) {
        if (!resumeTrack) return;
        actions.setIntent("play");
        actions.play(resumeTrack);
        window.dispatchEvent(new Event("af:play-intent"));
      }
    },
    [actions, autoResumeOnSeek, resumeTrack],
  );

  const lyricsVariant = variant === "inline" ? "inline" : "stage";
  const reservedBottomPx = variant === "inline" ? 0 : STAGE_TRANSPORT_FOOTER_PX;

  return (
    <div
      style={{
        position: "absolute",
        inset: 0,
        borderRadius: 18,
        overflow: "hidden",
        background: "rgba(0,0,0,0.35)",
        isolation: "isolate",
      }}
    >
      <div style={{ position: "absolute", inset: 0, zIndex: 0 }}>
        <VisualizerCanvas variant={variant} />
      </div>

      <div
        aria-hidden="true"
        style={{
          position: "absolute",
          inset: 0,
          zIndex: 1,
          background:
            variant === "inline"
              ? "radial-gradient(70% 55% at 50% 40%, rgba(0,0,0,0.00), rgba(0,0,0,0.22) 72%), linear-gradient(180deg, rgba(0,0,0,0.14), rgba(0,0,0,0.06) 45%, rgba(0,0,0,0.16))"
              : "radial-gradient(70% 55% at 50% 40%, rgba(0,0,0,0.00), rgba(0,0,0,0.28) 72%), linear-gradient(180deg, rgba(0,0,0,0.22), rgba(0,0,0,0.10) 45%, rgba(0,0,0,0.24))",
          pointerEvents: "none",
        }}
      />

      <div style={{ position: "absolute", inset: 0, zIndex: 2 }}>
        {lyricsMode === "embedded" ? (
          <LyricsOverlay
            recordingId={recordingId}
            displayId={displayId}
            cues={cues}
            offsetMs={effectiveOffsetMs}
            onSeek={onSeek}
            variant={lyricsVariant}
            reservedBottomPx={reservedBottomPx}
          />
        ) : null}
      </div>
    </div>
  );
}

export default React.memo(StageCore);


----- app/home/player/StageInline.tsx -----
// web/app/home/player/StageInline.tsx
"use client";

import React from "react";
import { createPortal } from "react-dom";
import { usePlayer } from "./PlayerState";
import StageCore from "./StageCore";
import StageTransportBar from "./StageTransportBar";
import StageNowPlayingBadge from "./stage/StageNowPlayingBadge";
import StagePerfHud from "./stage/StagePerfHud";
import { ensureLyricsForTrack } from "./lyrics/ensureLyricsForTrack";

function lockBodyScroll(lock: boolean) {
  if (typeof document === "undefined") return;
  const el = document.documentElement;
  const body = document.body;
  if (lock) {
    el.style.overflow = "hidden";
    body.style.overflow = "hidden";
    body.style.touchAction = "none";
  } else {
    el.style.overflow = "";
    body.style.overflow = "";
    body.style.touchAction = "";
  }
}

function useIsMobile(breakpointPx = 640) {
  const [isMobile, setIsMobile] = React.useState(false);

  React.useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${breakpointPx}px)`);
    const apply = () => setIsMobile(mq.matches);
    apply();
    mq.addEventListener?.("change", apply);
    return () => mq.removeEventListener?.("change", apply);
  }, [breakpointPx]);

  return isMobile;
}

function useIsAdminBodyFlag() {
  const [isAdmin, setIsAdmin] = React.useState(false);

  React.useEffect(() => {
    if (typeof document === "undefined") return;

    const body = document.body;
    const apply = () => setIsAdmin(body.dataset.afIsAdmin === "1");

    apply();

    const mo = new MutationObserver(() => apply());
    mo.observe(body, {
      attributes: true,
      attributeFilter: ["data-af-is-admin"],
    });

    return () => mo.disconnect();
  }, []);

  return isAdmin;
}

function useIdleCursor(active: boolean, timeoutMs: number) {
  const [hidden, setHidden] = React.useState(false);

  React.useEffect(() => {
    if (!active) {
      setHidden(false);
      return;
    }

    let timer: number | null = null;

    const reset = () => {
      setHidden(false);
      if (timer != null) window.clearTimeout(timer);
      timer = window.setTimeout(() => {
        setHidden(true);
      }, timeoutMs);
    };

    reset();

    const onMove = () => reset();
    const onDown = () => reset();

    window.addEventListener("mousemove", onMove, { passive: true });
    window.addEventListener("mousedown", onDown, { passive: true });

    return () => {
      if (timer != null) window.clearTimeout(timer);
      window.removeEventListener("mousemove", onMove);
      window.removeEventListener("mousedown", onDown);
    };
  }, [active, timeoutMs]);

  return hidden;
}

function IconFullscreen(props: { size?: number }) {
  const { size = 18 } = props;
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      aria-hidden="true"
    >
      <path
        d="M8 4H5a1 1 0 0 0-1 1v3m0 8v3a1 1 0 0 0 1 1h3m8-16h3a1 1 0 0 1 1 1v3m0 8v3a1 1 0 0 1-1 1h-3"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
      />
    </svg>
  );
}

function IconClose(props: { size?: number }) {
  const { size = 18 } = props;
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      aria-hidden="true"
    >
      <path
        d="M6 6l12 12M18 6L6 18"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
      />
    </svg>
  );
}

const ROUND_ICON_BUTTON_STYLE: React.CSSProperties = {
  width: 40,
  height: 40,
  borderRadius: 999,
  border: "1px solid rgba(255,255,255,0.14)",
  background: "rgba(0,0,0,0.28)",
  color: "rgba(255,255,255,0.92)",
  display: "grid",
  placeItems: "center",
  boxShadow: "0 14px 30px rgba(0,0,0,0.22)",
};

const RoundIconButton = React.memo(function RoundIconButton(props: {
  label: string;
  title?: string;
  onClick: () => void;
  disabled?: boolean;
  children: React.ReactNode;
}) {
  const { label, title, onClick, disabled, children } = props;
  return (
    <button
      type="button"
      aria-label={label}
      title={title ?? label}
      onClick={disabled ? undefined : onClick}
      disabled={disabled}
      style={{
        ...ROUND_ICON_BUTTON_STYLE,
        cursor: disabled ? "not-allowed" : "pointer",
        opacity: disabled ? 0.6 : 1,
      }}
    >
      {children}
    </button>
  );
});

const FullscreenStageOverlay = React.memo(
  function FullscreenStageOverlay(props: {
    cursorHidden: boolean;
    isMobile: boolean;
    showPerfHud: boolean;
    onBackdropMouseDown: (e: React.MouseEvent<HTMLDivElement>) => void;
    onRequestFullscreen: () => void;
    onClose: () => void;
  }) {
    const {
      cursorHidden,
      isMobile,
      showPerfHud,
      onBackdropMouseDown,
      onRequestFullscreen,
      onClose,
    } = props;

    return (
      <div
        id="af-stage-overlay"
        role="dialog"
        aria-modal="true"
        aria-label="Stage"
        onMouseDown={onBackdropMouseDown}
        style={{
          position: "fixed",
          inset: 0,
          width: "100%",
          height: "100dvh",
          zIndex: 200000,
          cursor: cursorHidden ? "none" : "default",
          background: "rgba(0,0,0,0.80)",
          backdropFilter: "blur(10px)",
          WebkitBackdropFilter: "blur(10px)",
          padding: 0,
          display: "grid",
        }}
      >
        <div
          style={{
            position: "relative",
            width: "100%",
            height: "100%",
            minHeight: 0,
          }}
        >
          <StageCore variant="fullscreen" lyricsMode="embedded" />

          {!isMobile && (
            <div
              aria-hidden="true"
              style={{
                position: "absolute",
                inset: 0,
                zIndex: 9,
                pointerEvents: "none",
              }}
            >
              <StageNowPlayingBadge />
            </div>
          )}

          {showPerfHud ? <StagePerfHud /> : null}

          <div
            aria-hidden
            style={{
              position: "absolute",
              left: 0,
              right: 0,
              top: 0,
              height: 64,
              background:
                "linear-gradient(180deg, rgba(0,0,0,0.55), rgba(0,0,0,0.22) 55%, rgba(0,0,0,0.00))",
              pointerEvents: "none",
            }}
          />

          <div
            style={{
              position: "absolute",
              top: `calc(10px + env(safe-area-inset-top, 0px))`,
              right: `calc(10px + env(safe-area-inset-right, 0px))`,
              display: "flex",
              alignItems: "center",
              gap: 10,
              pointerEvents: "auto",
              zIndex: 50,
            }}
          >
            <RoundIconButton
              label="Fullscreen"
              title="Fullscreen"
              onClick={onRequestFullscreen}
            >
              <IconFullscreen />
            </RoundIconButton>

            <RoundIconButton label="Close" title="Close" onClick={onClose}>
              <IconClose />
            </RoundIconButton>
          </div>

          <StageTransportBar />
        </div>
      </div>
    );
  },
);

export default function StageInline(props: { height?: number }) {
  const { height = 300 } = props;
  const p = usePlayer();

  // Lazy-load lyrics for the currently playing track when missing.
  const currentRecordingId = p.current?.recordingId ?? null;

  React.useEffect(() => {
    if (!currentRecordingId) return;

    const recordingId = currentRecordingId; // stable capture
    const ac = new AbortController();

    void ensureLyricsForTrack(recordingId, { signal: ac.signal });

    return () => ac.abort();
  }, [currentRecordingId]);

  const isMobile = useIsMobile(640);
  const isAdmin = useIsAdminBodyFlag();
  const inlineHeight = isMobile
    ? Math.max(140, Math.round(height * 0.5))
    : height;

  const [mounted, setMounted] = React.useState(false);
  React.useEffect(() => setMounted(true), []);

  const [open, setOpen] = React.useState(false);
  const enableIdleCursor = open && !isMobile;
  const cursorHidden = useIdleCursor(enableIdleCursor, 3000);

  React.useEffect(() => {
    lockBodyScroll(open);
    return () => lockBodyScroll(false);
  }, [open]);

  React.useEffect(() => {
    if (!open) return;
    if (typeof document === "undefined") return;

    const exitFsIfNeeded = async () => {
      try {
        if (
          document.fullscreenElement &&
          typeof document.exitFullscreen === "function"
        ) {
          await document.exitFullscreen();
        }
      } catch {
        // ignore
      }
    };

    const onKey = (e: KeyboardEvent) => {
      if (e.key !== "Escape") return;
      e.preventDefault();
      void exitFsIfNeeded().finally(() => setOpen(false));
    };

    const onFsChange = () => {
      if (!document.fullscreenElement) setOpen(false);
    };

    window.addEventListener("keydown", onKey);
    document.addEventListener("fullscreenchange", onFsChange);
    return () => {
      window.removeEventListener("keydown", onKey);
      document.removeEventListener("fullscreenchange", onFsChange);
    };
  }, [open]);

  const tryRequestFullscreen = React.useCallback(async () => {
    if (typeof document === "undefined") return;
    const el = document.getElementById("af-stage-overlay");
    if (!el) return;
    const requestFullscreen = (
      el as Element & { requestFullscreen?: () => Promise<void> }
    ).requestFullscreen;
    if (typeof requestFullscreen !== "function") return;
    try {
      await requestFullscreen.call(el);
    } catch {
      // ignore
    }
  }, []);

  const nothingPlaying = p.queue.length === 0 && !p.current?.recordingId;

  const handleOverlayBackdropMouseDown = React.useCallback(
    (e: React.MouseEvent<HTMLDivElement>) => {
      if (e.target === e.currentTarget) setOpen(false);
    },
    [],
  );

  const handleOverlayClose = React.useCallback(() => {
    setOpen(false);
  }, []);

  const handleOverlayRequestFullscreen = React.useCallback(() => {
    void tryRequestFullscreen();
  }, [tryRequestFullscreen]);

  const overlay =
    mounted && open
      ? createPortal(
          <FullscreenStageOverlay
            cursorHidden={cursorHidden}
            isMobile={isMobile}
            showPerfHud={isAdmin}
            onBackdropMouseDown={handleOverlayBackdropMouseDown}
            onRequestFullscreen={handleOverlayRequestFullscreen}
            onClose={handleOverlayClose}
          />,
          document.body,
        )
      : null;

  return (
    <>
      <div
        style={{
          borderRadius: 18,
          border: "1px solid rgba(255,255,255,0.12)",
          background: "rgba(255,255,255,0.05)",
          overflow: "hidden",
          height: inlineHeight,
          position: "relative",
        }}
      >
        {!open ? (
          <div style={{ position: "absolute", inset: 0 }}>
            <StageCore variant="inline" lyricsMode="embedded" />
          </div>
        ) : null}

        <div
          aria-hidden
          style={{
            position: "absolute",
            left: 0,
            right: 0,
            top: 0,
            height: 56,
            background:
              "linear-gradient(180deg, rgba(0,0,0,0.45), rgba(0,0,0,0.18) 55%, rgba(0,0,0,0.00))",
            pointerEvents: "none",
          }}
        />

        <div
          style={{
            position: "absolute",
            top: 10,
            right: 10,
            pointerEvents: "auto",
            zIndex: 50,
          }}
        >
          <RoundIconButton
            label="Open stage fullscreen"
            title={nothingPlaying ? "Nothing playing" : "Open fullscreen stage"}
            disabled={nothingPlaying}
            onClick={() => setOpen(true)}
          >
            <IconFullscreen />
          </RoundIconButton>
        </div>
      </div>

      {overlay}
    </>
  );
}


----- app/home/player/StageInlineHost.tsx -----
// web/app/home/player/StageInlineHost.tsx
"use client";

import React from "react";
import { createPortal } from "react-dom";
import StageInline from "@/app/home/player/StageInline";

type SlotConfig = {
  height: number;
};

function safeParseHeight(v: string | null | undefined, fallback: number) {
  const n = v ? Number(v) : NaN;
  return Number.isFinite(n) && n > 0 ? n : fallback;
}

function ensureOffscreenParking(): HTMLElement {
  const existing = document.getElementById("af-stage-inline-offscreen");
  if (existing) return existing;

  const el = document.createElement("div");
  el.id = "af-stage-inline-offscreen";
  el.style.position = "fixed";
  el.style.left = "-100000px";
  el.style.top = "0";

  // IMPORTANT: do NOT shrink to 1px.
  // Keep a stable, “real” box so WebGL/canvas code doesn’t treat it as effectively unmounted.
  el.style.width = "720px";
  el.style.height = "560px";

  el.style.overflow = "hidden";
  el.style.pointerEvents = "none";
  el.style.opacity = "0";
  el.style.contain = "layout paint size";
  document.body.appendChild(el);
  return el;
}

/**
 * A single stable host element that React portals into forever.
 * We physically move this element into the current slot (if present),
 * otherwise park it offscreen. Moving the DOM node does NOT remount React.
 */
function ensureStableHostEl(): HTMLElement {
  const existing = document.getElementById("af-stage-inline-host");
  if (existing) return existing;

  const el = document.createElement("div");
  el.id = "af-stage-inline-host";
  el.style.width = "100%";
  el.style.height = "100%";
  // Important: do not position here; it inherits context from the slot container.
  return el;
}

function readSlotConfig(
  slot: HTMLElement | null,
  fallback: SlotConfig,
): SlotConfig {
  if (!slot) return fallback;

  const height = safeParseHeight(
    slot.getAttribute("data-height"),
    fallback.height,
  );
  return { height };
}

function dbgEnabled(): boolean {
  try {
    return window.sessionStorage.getItem("af_dbg_stage_host") === "1";
  } catch {
    return false;
  }
}

function dbg(...args: unknown[]) {
  if (!dbgEnabled()) return;
  console.log("[StageInlineHost]", ...args);
}

export default function StageInlineHost(props: {
  /** Optional defaults; layouts can override via slot data-* attrs */
  height?: number;
  /** Slot id to attach the host into when present */
  slotId?: string;
}) {
  const slotId = props.slotId ?? "af-stage-inline-slot";

  const fallback = React.useMemo<SlotConfig>(
    () => ({ height: props.height ?? 560 }),
    [props.height],
  );

  // Important: first client render must match the server render.
  // So we start with null and only create the host after mount.
  const [hostEl, setHostEl] = React.useState<HTMLElement | null>(null);

  React.useEffect(() => {
    setHostEl(ensureStableHostEl());
  }, []);

  // Config is stateful (allowed to change), but portal container is NOT.
  const [cfg, setCfg] = React.useState<SlotConfig>(fallback);

  // Every session layout renders the inline-stage slot. Attach during hydration
  // and once again on the next paint, rather than permanently observing every
  // mutation beneath document.body for a slot that does not disappear.
  React.useEffect(() => {
    if (!hostEl) return;

    const parking = ensureOffscreenParking();
    let cancelled = false;
    let retryFrame: number | null = null;

    const attach = () => {
      if (cancelled) return;

      const slot = document.getElementById(slotId) as HTMLElement | null;
      const targetParent = slot ?? parking;

      // Move hostEl only if its parent actually changed.
      if (hostEl.parentElement !== targetParent) {
        try {
          targetParent.appendChild(hostEl);
          dbg(
            "moved hostEl into",
            slot ? `#${slotId}` : "#af-stage-inline-offscreen",
          );
        } catch (error) {
          dbg("appendChild failed", error);
        }
      }

      const nextCfg = readSlotConfig(slot, fallback);

      // Keep the fallback parking box sized correctly even though ordinary
      // session layouts should keep the host in the visible stage slot.
      parking.style.height = `${Math.max(1, Math.floor(nextCfg.height))}px`;

      setCfg((previous) =>
        previous.height === nextCfg.height ? previous : nextCfg,
      );
    };

    // The microtask covers ordinary hydration; the next-frame retry covers
    // any late sibling commit without keeping a permanent global observer.
    queueMicrotask(attach);
    retryFrame = window.requestAnimationFrame(attach);

    return () => {
      cancelled = true;

      if (retryFrame != null) {
        window.cancelAnimationFrame(retryFrame);
      }

      // Do NOT remove hostEl; preserving it retains stage and WebGL state.
    };
  }, [hostEl, slotId, fallback]);

  if (!hostEl) return null;

  // StageInline now owns its own data source (lyricsSurface + fetch), host only provides stable mount + height.
  return createPortal(<StageInline height={cfg.height} />, hostEl);
}


----- app/home/player/lyrics/fetchLyricsByRecordingId.ts -----
//web/app/home/player/lyrics/fetchLyricsByrecordingId.ts
"use client";

import { parseTrackLyricsApiOk } from "@/lib/types";
import type { LyricCue } from "@/lib/types";

export async function fetchLyricsByrecordingId(
  recordingId: string,
  signal?: AbortSignal,
): Promise<{ recordingId: string; cues: LyricCue[]; offsetMs: number } | null> {
  const res = await fetch(
    `/api/lyrics/by-track?recordingId=${encodeURIComponent(recordingId)}`,
    { signal, cache: "no-store" },
  );
  if (!res.ok) return null;

  const raw: unknown = await res.json();
  const parsed = parseTrackLyricsApiOk(raw);
  if (!parsed) return null;

  return {
    recordingId: parsed.recordingId,
    cues: parsed.cues,
    offsetMs: parsed.offsetMs,
  };
}

----- app/home/player/lyrics/lyricsSurface.ts -----
// web/app/home/player/lyrics/lyricsSurface.ts
"use client";

import type { LyricCue } from "@/lib/types";

export type CuesByrecordingId = Record<string, LyricCue[]>;
export type OffsetByrecordingId = Record<string, number>;

type Snapshot = {
  cuesByRecordingId: CuesByrecordingId;
  offsetByRecordingId: OffsetByrecordingId;
  globalOffsetMs: number;
};

type Listener = () => void;

let snap: Snapshot = {
  cuesByRecordingId: {},
  offsetByRecordingId: {},
  globalOffsetMs: 0,
};

const listeners = new Set<Listener>();

function emit() {
  for (const fn of listeners) fn();
}

export const lyricsSurface = {
  getSnapshot(): Snapshot {
    return snap;
  },

  subscribe(fn: Listener): () => void {
    listeners.add(fn);
    return () => listeners.delete(fn);
  },

  setMaps(next: {
    cuesByrecordingId?: CuesByrecordingId | null;
    offsetByrecordingId?: OffsetByrecordingId | null;
    globalOffsetMs?: number | null;
  }) {
    const cuesByRecordingId = next.cuesByrecordingId ?? {};
    const offsetByRecordingId = next.offsetByrecordingId ?? {};
    const globalOffsetMs =
      typeof next.globalOffsetMs === "number" && Number.isFinite(next.globalOffsetMs)
        ? next.globalOffsetMs
        : 0;

    // cheap identity guard: only emit if something actually changes by reference/value
    const changed =
      snap.cuesByRecordingId !== cuesByRecordingId ||
      snap.offsetByRecordingId !== offsetByRecordingId ||
      snap.globalOffsetMs !== globalOffsetMs;

    if (!changed) return;

    snap = { cuesByRecordingId, offsetByRecordingId, globalOffsetMs };
    emit();
  },
};

----- app/home/player/lyrics/useLyricsSurface.ts -----
//web/app/home/player/lyrics/useLyricsSurface.ts
"use client";

import React from "react";
import { lyricsSurface } from "./lyricsSurface";

export function useLyricsSnapshot() {
  return React.useSyncExternalStore(
    lyricsSurface.subscribe,
    lyricsSurface.getSnapshot,
    lyricsSurface.getSnapshot,
  );
}

----- lib/albums.ts -----
// web/lib/albums.ts
import { unstable_cache } from "next/cache";
import { client } from "@/sanity/lib/client";
import { urlFor } from "@/sanity/lib/image";
import type {
  AlbumInfo,
  PlayerTrack,
  TierName,
  LyricCue,
  AlbumPlayerBundle,
} from "@/lib/types";
import {
  makeAlbumPlayerBundle,
  normalizeLyricCuesFromSanity,
} from "@/lib/types";
import { getRecordingPlayCountsByRecordingIds } from "@/lib/recordingListenTotals";

export type AlbumDocTrack = {
  recordingId: string;
  displayId?: string;
  title?: string;
  artist?: string;
  durationMs?: number;
  muxPlaybackId?: string;
  visualTheme?: string;
  explicit?: boolean;
};

type AlbumDoc = {
  _id?: string;
  catalogueId?: string | null;
  slug?: string | null;
  title?: string;
  displayTitle?: string | null;
  artist?: string;
  year?: number;
  description?: string;
  artwork?: unknown;
  visualTheme?: string;
  publicPageVisible?: boolean;
  releaseAt?: string;
  embargoNote?: string;
  earlyAccessEnabled?: boolean;
  earlyAccessTiers?: string[];
  minTierToLoad?: string;
  platformLinks?: Array<{
    platform?: string;
    url?: string;
  }>;
  tracks?: AlbumDocTrack[];
};

type TrackLyricsDoc = {
  recordingId?: string;
  offsetMs?: number;
  cues?: Array<{ _key?: string; tMs?: number; text?: string; endMs?: number }>;
};

export async function getFeaturedAlbumSlugFromSanity(): Promise<{
  slug: string | null;
  fallbackSlug: string | null;
}> {
  const res = await getCachedFeaturedAlbumSlugDoc();

  return {
    slug:
      typeof res?.slug === "string" && res.slug.trim() ? res.slug.trim() : null,
    fallbackSlug:
      typeof res?.fallbackSlug === "string" && res.fallbackSlug.trim()
        ? res.fallbackSlug.trim()
        : null,
  };
}

export type AlbumBrowseItem = {
  id: string;
  slug: string;
  catalogueId?: string | null;
  title: string;
  artist?: string;
  year?: number;
  artwork?: unknown;
  artworkUrl?: string | null;

  // raw fields from GROQ
  publicPageVisible?: boolean;
  minTierToLoad?: string;

  policy?: {
    publicPageVisible: boolean;
    minTierToLoad?: string | null;
  };
};

function normStr(v: unknown): string | undefined {
  if (typeof v !== "string") return undefined;
  const s = v.trim();
  return s.length ? s : undefined;
}

function normTheme(v: unknown): string | undefined {
  const s = normStr(v);
  return s && s !== "" ? s : undefined;
}

function parseTierName(v: unknown): TierName | null {
  const s = normStr(v);
  if (!s) return null;
  if (s === "friend" || s === "patron" || s === "partner") return s;
  return null;
}

function parseTierNameArray(v: unknown): TierName[] {
  if (!Array.isArray(v)) return [];
  return v.map(parseTierName).filter((x): x is TierName => x !== null);
}

function slugifySeg(v: string): string {
  // safe for URLs and stable-ish: lowercase, hyphens, strip junk
  return v
    .toLowerCase()
    .trim()
    .replace(/['"]/g, "")
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/-+/g, "-")
    .replace(/^-|-$/g, "");
}

function fallbackDisplayId(args: {
  title?: string;
  trackNo: number; // 1-based
}): string {
  const base = slugifySeg(args.title ?? "");
  // always produce something
  return base
    ? `${String(args.trackNo).padStart(2, "0")}-${base}`
    : `track-${String(args.trackNo).padStart(2, "0")}`;
}

function uniqifyDisplayId(desired: string, used: Set<string>): string {
  let d = desired;
  let i = 2;
  while (used.has(d)) {
    d = `${desired}-${i}`;
    i++;
  }
  used.add(d);
  return d;
}

export type NormalizedAlbumTrack = {
  recordingId: string;
  displayId: string;
  title?: string;
  artist?: string;
  muxPlaybackId?: string;
  durationMs?: number;
  visualTheme?: string;
  explicit?: boolean;
  trackNo: number;
};

export function normalizeAlbumTracks(
  tracks: AlbumDocTrack[] | null | undefined,
  args?: { albumTheme?: string | undefined },
): NormalizedAlbumTrack[] {
  const albumTheme = normTheme(args?.albumTheme);
  if (!Array.isArray(tracks)) return [];

  const used = new Set<string>();

  return tracks
    .filter((t) => typeof t?.recordingId === "string" && t.recordingId.trim())
    .map((t, idx) => {
      const rawDur = t.durationMs;
      const dur =
        typeof rawDur === "number" && Number.isFinite(rawDur)
          ? rawDur
          : undefined;

      const trackNo = idx + 1;
      const recordingId = t.recordingId.trim();

      const wanted =
        normStr(t.displayId) ?? fallbackDisplayId({ title: t.title, trackNo });

      const displayId = uniqifyDisplayId(wanted, used);
      const trackTheme = normTheme(t.visualTheme);

      return {
        recordingId,
        displayId,
        title: normStr(t.title),
        artist: normStr(t.artist),
        muxPlaybackId: normStr(t.muxPlaybackId),
        durationMs: typeof dur === "number" && dur > 0 ? dur : undefined,
        visualTheme: trackTheme ?? albumTheme,
        explicit: t.explicit === true,
        trackNo,
      };
    });
}

type FeaturedAlbumSlugDoc = {
  slug?: string | null;
  fallbackSlug?: string | null;
};

const STATIC_CATALOGUE_REVALIDATE_SECONDS = 60 * 60;

const featuredAlbumSlugQuery = `
  *[_type == "siteFlags"]
    | order(_updatedAt desc)[0]{
      "slug": featuredAlbum->slug.current,
      "fallbackSlug": featuredAlbumFallbackSlug
    }
`;

const albumBySlugQuery = `
  *[_type == "album" && slug.current == $slug][0]{
    _id,
    catalogueId,
    "slug": slug.current,
    title,
    displayTitle,
    artist,
    year,
    description,
    artwork,
    visualTheme,
    publicPageVisible,
    releaseAt,
    embargoNote,
    earlyAccessEnabled,
    earlyAccessTiers,
    minTierToLoad,
    platformLinks[]{
      platform,
      url
    },
    "tracks": tracks[]{
      recordingId,
      displayId,
      title,
      artist,
      durationMs,
      muxPlaybackId,
      visualTheme,
      explicit
    }
  }
`;

const lyricsByRecordingIdsQuery = `
  *[_type == "lyrics" && recordingId in $recordingIds]{
    recordingId,
    offsetMs,
    cues[]{ _key, tMs, text, endMs }
  }
`;

const albumBrowseQuery = `
  *[_type=="album"]|order(year desc, _createdAt desc){
    "id": _id,
    "catalogueId": catalogueId,
    "slug": slug.current,
    title,
    artist,
    year,
    artwork,
    publicPageVisible,
    minTierToLoad
  }
`;

const getCachedFeaturedAlbumSlugDoc = unstable_cache(
  async (): Promise<FeaturedAlbumSlugDoc | null> => {
    return client.fetch<FeaturedAlbumSlugDoc | null>(featuredAlbumSlugQuery);
  },
  ["featured-album-slug-v1"],
  {
    revalidate: STATIC_CATALOGUE_REVALIDATE_SECONDS,
    tags: ["siteFlags"],
  },
);

const getCachedAlbumDocumentBySlug = unstable_cache(
  async (slug: string): Promise<AlbumDoc | null> => {
    return client.fetch<AlbumDoc | null>(albumBySlugQuery, { slug });
  },
  ["album-document-by-slug-v1"],
  {
    revalidate: STATIC_CATALOGUE_REVALIDATE_SECONDS,
    tags: ["albums"],
  },
);

const getCachedLyricsByRecordingIds = unstable_cache(
  async (recordingIds: string[]): Promise<TrackLyricsDoc[]> => {
    if (recordingIds.length === 0) return [];

    const docs = await client.fetch<TrackLyricsDoc[]>(
      lyricsByRecordingIdsQuery,
      { recordingIds },
    );

    return Array.isArray(docs) ? docs : [];
  },
  ["album-lyrics-by-recording-ids-v1"],
  {
    revalidate: STATIC_CATALOGUE_REVALIDATE_SECONDS,
    tags: ["lyrics"],
  },
);

const getCachedAlbumBrowseDocs = unstable_cache(
  async (): Promise<AlbumBrowseItem[]> => {
    const docs = await client.fetch<AlbumBrowseItem[]>(albumBrowseQuery);
    return Array.isArray(docs) ? docs : [];
  },
  ["album-browse-list-v1"],
  {
    revalidate: STATIC_CATALOGUE_REVALIDATE_SECONDS,
    tags: ["albums"],
  },
);

export type AlbumPlaybackAssetVerification = {
  ok: boolean;
  albumId: string;
  playbackId: string;
  trackId: string | null;
  matchedRecordingId: string | null;
};

export type AlbumPlaybackSessionAsset = {
  recordingId: string;
  displayId: string;
  playbackId: string;
  durationMs?: number;
};

function normalizeAlbumScopeId(raw: string): string {
  let s = (raw ?? "").trim();
  while (s.startsWith("alb:")) s = s.slice(4);
  return s.trim();
}

export async function getAlbumPlaybackAssetsForSession(params: {
  albumId: string;
}): Promise<{
  ok: boolean;
  albumId: string;
  albumScopeId: string;
  tracks: AlbumPlaybackSessionAsset[];
}> {
  const albumId = normalizeAlbumScopeId(params.albumId);

  if (!albumId) {
    return {
      ok: false,
      albumId,
      albumScopeId: "",
      tracks: [],
    };
  }

  const q = `
    *[
      _type == "album" &&
      (
        _id == $albumId ||
        catalogueId == $albumId
      )
    ][0]{
      _id,
      catalogueId,
      "tracks": tracks[
        defined(recordingId) &&
        defined(displayId) &&
        defined(muxPlaybackId)
      ]{
        recordingId,
        displayId,
        muxPlaybackId,
        durationMs
      }
    }
  `;

  const doc = await client.fetch<{
    _id?: string;
    catalogueId?: string | null;
    tracks?: Array<{
      recordingId?: string;
      displayId?: string;
      muxPlaybackId?: string;
      durationMs?: number;
    }>;
  } | null>(q, { albumId }, { next: { tags: ["albums"] } });

  const resolvedAlbumId = normStr(doc?.catalogueId) ?? normStr(doc?._id) ?? "";
  const albumScopeId = resolvedAlbumId ? `alb:${resolvedAlbumId}` : "";

  const tracks: AlbumPlaybackSessionAsset[] = [];

  if (Array.isArray(doc?.tracks)) {
    for (const t of doc.tracks) {
      const recordingId = normStr(t.recordingId);
      const displayId = normStr(t.displayId);
      const playbackId = normStr(t.muxPlaybackId);

      if (!recordingId || !displayId || !playbackId) continue;

      const durationMs =
        typeof t.durationMs === "number" &&
        Number.isFinite(t.durationMs) &&
        t.durationMs > 0
          ? Math.floor(t.durationMs)
          : undefined;

      tracks.push({
        recordingId,
        displayId,
        playbackId,
        ...(typeof durationMs === "number" ? { durationMs } : {}),
      });
    }
  }

  return {
    ok: Boolean(resolvedAlbumId && tracks.length > 0),
    albumId: resolvedAlbumId,
    albumScopeId,
    tracks,
  };
}

export async function verifyAlbumPlaybackAsset(params: {
  albumId: string;
  playbackId: string;
  trackId?: string | null;
}): Promise<AlbumPlaybackAssetVerification> {
  const albumId = normalizeAlbumScopeId(params.albumId);
  const playbackId = normStr(params.playbackId) ?? "";
  const trackId = normStr(params.trackId) ?? null;

  if (!albumId || !playbackId) {
    return {
      ok: false,
      albumId,
      playbackId,
      trackId,
      matchedRecordingId: null,
    };
  }

  const q = `
    *[
      _type == "album" &&
      (
        _id == $albumId ||
        catalogueId == $albumId
      )
    ][0]{
      "matches": tracks[
        defined(muxPlaybackId) &&
        muxPlaybackId == $playbackId
      ]{
        recordingId,
        displayId,
        muxPlaybackId
      }
    }
  `;

  const doc = await client.fetch<{
    matches?: Array<{
      recordingId?: string;
      displayId?: string;
      muxPlaybackId?: string;
    }>;
  } | null>(q, { albumId, playbackId }, { next: { tags: ["albums"] } });

  const matches = Array.isArray(doc?.matches) ? doc.matches : [];

  const matched = matches.find((t) => {
    const recordingId = normStr(t.recordingId);
    const displayId = normStr(t.displayId);
    const muxPlaybackId = normStr(t.muxPlaybackId);

    if (muxPlaybackId !== playbackId) return false;
    if (!trackId) return true;

    return recordingId === trackId || displayId === trackId;
  });

  return {
    ok: Boolean(matched),
    albumId,
    playbackId,
    trackId,
    matchedRecordingId: normStr(matched?.recordingId) ?? null,
  };
}

export async function getAlbumBySlug(slug: string): Promise<AlbumPlayerBundle> {
  const albumSlug = (slug ?? "").trim().toLowerCase();

  if (!albumSlug) {
    return makeAlbumPlayerBundle({
      albumSlug: "",
      album: null,
      tracks: [],
      albumLyrics: null,
    });
  }

  const doc = await getCachedAlbumDocumentBySlug(albumSlug);

  if (!doc?._id) {
    return makeAlbumPlayerBundle({
      albumSlug,
      album: null,
      tracks: [],
      albumLyrics: null,
    });
  }

  const albumCatalogueId = normStr(doc.catalogueId) ?? undefined;
  const albumTheme = normTheme(doc.visualTheme);

  // This remains outside the long-lived catalogue cache. An album must become
  // non-embargoed as its release timestamp passes, without a fresh publish.
  const releaseAt = doc.releaseAt ?? null;
  const releaseAtMs = releaseAt ? Date.parse(releaseAt) : NaN;
  const isEmbargoedByDate = Boolean(
    releaseAt && Number.isFinite(releaseAtMs) && releaseAtMs > Date.now(),
  );

  const embargoNote = normStr(doc.embargoNote) ?? null;

  const album: AlbumInfo = {
    id: doc._id,
    catalogueId: albumCatalogueId,
    title: doc.title ?? "Untitled",
    artist: normStr(doc.artist),
    year:
      typeof doc.year === "number" && Number.isFinite(doc.year)
        ? doc.year
        : undefined,
    description: normStr(doc.description),
    artworkUrl: doc.artwork
      ? urlFor(doc.artwork).width(900).height(900).quality(85).url()
      : null,
    platformLinks: Array.isArray(doc.platformLinks)
      ? doc.platformLinks
          .filter(
            (p): p is { platform: string; url: string } =>
              typeof p?.platform === "string" && typeof p?.url === "string",
          )
          .map((p) => ({
            platform: p.platform,
            url: p.url,
          }))
      : [],
    policy: {
      publicPageVisible: doc.publicPageVisible !== false,
      releaseAt: doc.releaseAt ?? null,
      earlyAccessEnabled: !!doc.earlyAccessEnabled,
      earlyAccessTiers: parseTierNameArray(doc.earlyAccessTiers),
      minTierToLoad: parseTierName(doc.minTierToLoad),
    },
    embargo: {
      embargoed: isEmbargoedByDate,
      releaseAt,
      note: embargoNote,
    },
  };

  const tracks: PlayerTrack[] = normalizeAlbumTracks(doc.tracks, {
    albumTheme,
  }).map((t) => ({
    recordingId: t.recordingId,
    displayId: t.displayId,
    title: t.title,
    artist: t.artist,
    muxPlaybackId: t.muxPlaybackId,
    durationMs: t.durationMs,
    visualTheme: t.visualTheme,
    explicit: t.explicit,
  }));

  const recordingIds = tracks
    .map((t) => t.recordingId)
    .filter((recordingId): recordingId is string => recordingId.length > 0);

  const lyricRecordingIds = Array.from(new Set(recordingIds)).sort((a, b) =>
    a.localeCompare(b),
  );

  const [playCountsByRecordingId, lyricDocs] = await Promise.all([
    getRecordingPlayCountsByRecordingIds(recordingIds),
    getCachedLyricsByRecordingIds(lyricRecordingIds),
  ]);

  const tracksWithPlayCounts: PlayerTrack[] = tracks.map((t) => ({
    ...t,
    playCount: playCountsByRecordingId[t.recordingId] ?? 0,
  }));

  const cuesByRecordingId: Record<string, LyricCue[]> = {};
  const offsetByRecordingId: Record<string, number> = {};

  for (const d of lyricDocs) {
    const id = (d.recordingId ?? "").trim();
    if (!id) continue;

    cuesByRecordingId[id] = normalizeLyricCuesFromSanity(d.cues);
    offsetByRecordingId[id] =
      typeof d.offsetMs === "number" && Number.isFinite(d.offsetMs)
        ? Math.floor(d.offsetMs)
        : 0;
  }

  return makeAlbumPlayerBundle({
    albumSlug: normStr(doc.slug) ?? albumSlug,
    album,
    tracks: tracksWithPlayCounts,
    albumLyrics: { cuesByRecordingId, offsetByRecordingId },
  });
}

export type AlbumCanonicalMetadata = {
  title: string | null;
  displayTitle: string | null;
  slug: string | null;
};

export type TrackCanonicalMetadata = {
  albumTitle: string | null;
  albumDisplayTitle: string | null;
  albumSlug: string | null;
  trackTitle: string | null;
  displayId: string | null;
};

export async function getAlbumCanonicalMetadataBySlug(
  slug: string,
): Promise<AlbumCanonicalMetadata | null> {
  const albumSlug = (slug ?? "").trim().toLowerCase();
  if (!albumSlug) return null;

  const doc = await getCachedAlbumDocumentBySlug(albumSlug);
  if (!doc?._id) return null;

  return {
    title: normStr(doc.title) ?? null,
    displayTitle: normStr(doc.displayTitle) ?? null,
    slug: normStr(doc.slug) ?? null,
  };
}

export async function getTrackCanonicalMetadataBySlugAndDisplayId(params: {
  slug: string;
  displayId: string;
}): Promise<TrackCanonicalMetadata | null> {
  const albumSlug = (params.slug ?? "").trim().toLowerCase();
  const requestedDisplayId = (params.displayId ?? "").trim();

  if (!albumSlug || !requestedDisplayId) return null;

  const doc = await getCachedAlbumDocumentBySlug(albumSlug);
  if (!doc?._id) return null;

  const matchingTrack =
    doc.tracks?.find(
      (track) => normStr(track.displayId) === requestedDisplayId,
    ) ?? null;

  return {
    albumTitle: normStr(doc.title) ?? null,
    albumDisplayTitle: normStr(doc.displayTitle) ?? null,
    albumSlug: normStr(doc.slug) ?? null,
    trackTitle: normStr(matchingTrack?.title) ?? null,
    displayId: normStr(matchingTrack?.displayId) ?? null,
  };
}

export async function listAlbumsForBrowse(): Promise<AlbumBrowseItem[]> {
  const items = await getCachedAlbumBrowseDocs();
  return items.map((a) => ({
    ...a,
    catalogueId: normStr(a.catalogueId) ?? null,
    artist: normStr(a.artist),
    title: a.title ?? "Untitled",
    artworkUrl: a.artwork
      ? urlFor(a.artwork).width(600).height(600).quality(80).url()
      : null,
    policy: {
      publicPageVisible: a.publicPageVisible !== false,
      minTierToLoad: parseTierName(a.minTierToLoad),
    },
  }));
}

export type RecordingSearchResult = {
  recordingId: string;
  title: string;
  artist?: string | null;
  albumSlug?: string | null;
  albumTitle?: string | null;
};

export async function searchRecordingsForAdmin(args: {
  query: string;
  limit?: number;
}): Promise<RecordingSearchResult[]> {
  const rawQuery = normStr(args.query);
  if (!rawQuery) return [];

  const limit = Math.max(1, Math.min(25, Math.floor(args.limit ?? 12)));

  const wildcard = `*${rawQuery}*`;
  const exactId = rawQuery;

  const q = `
    *[
      _type == "album" &&
      count(
        tracks[
          defined(recordingId) && (
            recordingId == $exactId ||
            recordingId match $wildcard ||
            title match $wildcard ||
            artist match $wildcard
          )
        ]
      ) > 0
    ] | order(title asc) {
      "albumSlug": slug.current,
      "albumTitle": title,
      "albumArtist": artist,
      "matches": tracks[
        defined(recordingId) && (
          recordingId == $exactId ||
          recordingId match $wildcard ||
          title match $wildcard ||
          artist match $wildcard
        )
      ]{
        recordingId,
        title,
        artist
      }
    }
  `;

  const docs = await client.fetch<
    Array<{
      albumSlug?: string;
      albumTitle?: string;
      albumArtist?: string;
      matches?: Array<{
        recordingId?: string;
        title?: string;
        artist?: string;
      }>;
    }>
  >(q, {
    exactId,
    wildcard,
  });

  const flattened: RecordingSearchResult[] = [];
  const seen = new Set<string>();

  for (const doc of Array.isArray(docs) ? docs : []) {
    const albumSlug = normStr(doc?.albumSlug) ?? null;
    const albumTitle = normStr(doc?.albumTitle) ?? null;
    const albumArtist = normStr(doc?.albumArtist) ?? null;

    for (const match of Array.isArray(doc?.matches) ? doc.matches : []) {
      const recordingId = normStr(match?.recordingId);
      const title = normStr(match?.title);

      if (!recordingId || !title || seen.has(recordingId)) continue;
      seen.add(recordingId);

      flattened.push({
        recordingId,
        title,
        artist: normStr(match?.artist) ?? albumArtist,
        albumSlug,
        albumTitle,
      });
    }
  }

  const normalizedQuery = rawQuery.toLowerCase();

  flattened.sort((a, b) => {
    const aId = a.recordingId.toLowerCase();
    const bId = b.recordingId.toLowerCase();
    const aTitle = a.title.toLowerCase();
    const bTitle = b.title.toLowerCase();

    const aExact = aId === normalizedQuery ? 1 : 0;
    const bExact = bId === normalizedQuery ? 1 : 0;
    if (aExact !== bExact) return bExact - aExact;

    const aTitleStarts = aTitle.startsWith(normalizedQuery) ? 1 : 0;
    const bTitleStarts = bTitle.startsWith(normalizedQuery) ? 1 : 0;
    if (aTitleStarts !== bTitleStarts) return bTitleStarts - aTitleStarts;

    const aTitleIncludes = aTitle.includes(normalizedQuery) ? 1 : 0;
    const bTitleIncludes = bTitle.includes(normalizedQuery) ? 1 : 0;
    if (aTitleIncludes !== bTitleIncludes) {
      return bTitleIncludes - aTitleIncludes;
    }

    return a.title.localeCompare(b.title);
  });

  return flattened.slice(0, limit);
}

export async function getRecordingSummaryByRecordingId(
  recordingId: string,
): Promise<RecordingSummary | null> {
  const id = normStr(recordingId);
  if (!id) return null;

  const q = `
        *[_type == "album" && count(tracks[recordingId == $recordingId]) > 0][0]{
      "albumCatalogueId": catalogueId,
      "albumSlug": slug.current,
      "albumTitle": title,
      "albumArtist": artist,
      "track": tracks[recordingId == $recordingId][0]{
        recordingId,
        title,
        artist
      }
    }
  `;

  const doc = await client.fetch<{
    albumCatalogueId?: string;
    albumSlug?: string;
    albumTitle?: string;
    albumArtist?: string;
    track?: {
      recordingId?: string;
      title?: string;
      artist?: string;
    };
  } | null>(q, { recordingId: id });

  const trackRecordingId = normStr(doc?.track?.recordingId);
  const trackTitle = normStr(doc?.track?.title);
  if (!trackRecordingId || !trackTitle) return null;

  return {
    recordingId: trackRecordingId,
    title: trackTitle,
    artist: normStr(doc?.track?.artist) ?? normStr(doc?.albumArtist) ?? null,
    albumCatalogueId: normStr(doc?.albumCatalogueId) ?? null,
    albumSlug: normStr(doc?.albumSlug) ?? null,
    albumTitle: normStr(doc?.albumTitle) ?? null,
  };
}

export type RecordingSummary = {
  recordingId: string;
  title: string;
  artist?: string | null;
  albumCatalogueId?: string | null;
  albumSlug?: string | null;
  albumTitle?: string | null;
};

export type AlbumEmailMeta = {
  slug: string;
  title: string;
  artist?: string;
  artworkUrl?: string | null;
};

export async function getAlbumEmailMetaBySlug(
  slug: string,
): Promise<AlbumEmailMeta | null> {
  const s = (slug ?? "").trim().toLowerCase();
  if (!s) return null;

  const q = `
    *[_type == "album" && slug.current == $slug][0]{
      "slug": slug.current,
      title,
      artist,
      artwork
    }
  `;

  const doc = await client.fetch<{
    slug?: string;
    title?: string;
    artist?: string;
    artwork?: unknown;
  } | null>(q, { slug: s });
  if (!doc?.slug) return null;

  return {
    slug: doc.slug,
    title: doc.title ?? "Untitled",
    artist: normStr(doc.artist),
    artworkUrl: doc.artwork
      ? urlFor(doc.artwork).width(900).height(900).quality(85).url()
      : null,
  };
}


----- lib/types.ts -----
// web/lib/types.ts

export type TierName = "friend" | "patron" | "partner";
export type Tier = "none" | TierName;

export type AlbumEmbargoInfo = {
  embargoed: boolean;
  releaseAt: string | null;
  note?: string | null;
};

export type AlbumInfo = {
  id: string;
  catalogueId?: string;
  title: string;
  artist?: string;
  year?: number;
  description?: string;
  artworkUrl?: string | null;
  policy?: AlbumPolicy;
  embargo?: AlbumEmbargoInfo;
  platformLinks?: { platform: string; url: string }[];
};

export type AlbumPolicy = {
  publicPageVisible: boolean;
  releaseAt?: string | null;
  earlyAccessEnabled?: boolean;
  earlyAccessTiers?: TierName[];
  minTierToLoad?: TierName | null;
};

export type AlbumPlayerBundle = {
  albumSlug: string;
  album: AlbumInfo | null;
  tracks: PlayerTrack[];
  albumLyrics: AlbumLyricsBundle | null;
};

// Optional helper (keeps call-sites tidy)
export function makeAlbumPlayerBundle(args: {
  albumSlug: string;
  album: AlbumInfo | null;
  tracks: PlayerTrack[];
  albumLyrics?: AlbumLyricsBundle | null;
}): AlbumPlayerBundle {
  return {
    albumSlug: args.albumSlug,
    album: args.album,
    tracks: Array.isArray(args.tracks) ? args.tracks : [],
    albumLyrics: args.albumLyrics ?? null,
  };
}

export type AlbumNavItem = {
  id: string;
  slug: string;
  title: string;
  artist?: string;
  year?: number;
  coverUrl?: string | null;

  // browse-click gating (load gate)
  policy?: {
    publicPageVisible: boolean;
    minTierToLoad: TierName | null;
  };
};

export type PlayerTrack = {
  recordingId: string;
  displayId: string;
  title?: string;
  artist?: string;
  durationMs?: number;
  muxPlaybackId?: string;
  visualTheme?: string;
  explicit?: boolean;
  playCount?: number;
};

export type AlbumLyricsBundle = {
  cuesByRecordingId: Record<string, LyricCue[]>;
  offsetByRecordingId: Record<string, number>;
};

export type LyricGroupMapEntry = {
  canonicalGroupKey: string;
  updatedAt: string; // ISO string from DB
};

export type LyricGroupMap = Record<string, LyricGroupMapEntry>; // keyed by lineKey

export type LyricCue = {
  lineKey: string;
  tMs: number;
  text: string;
  endMs?: number;

  /**
   * Optional enrichment: if this cue's lineKey is mapped into a canonical exegesis group.
   * Present when known; omitted otherwise.
   */
  canonicalGroupKey?: string;
};

export type TrackLyricsApiOk = {
  ok: true;
  recordingId: string;
  offsetMs: number;
  cues: LyricCue[];

  // present in API today, but optional for consumers
  version?: string;
  geniusUrl?: string | null;
  groupMap?: LyricGroupMap;

  // meta fields the API returns (nullable)
  trackTitle?: string | null;
  trackArtist?: string | null;
  trackCatalogueId?: string | null;
  albumTitle?: string | null;
  albumSlug?: string | null;
  albumCatalogueId?: string | null;
};

export function normalizeLyricCues(input: unknown): LyricCue[] {
  if (!Array.isArray(input)) return [];
  const out: LyricCue[] = [];

  for (const c of input) {
    if (!c || typeof c !== "object") continue;

    const lineKey = (c as { lineKey?: unknown }).lineKey;
    const tMs = (c as { tMs?: unknown }).tMs;
    const text = (c as { text?: unknown }).text;
    const endMs = (c as { endMs?: unknown }).endMs;
    const canonicalGroupKey = (c as { canonicalGroupKey?: unknown })
      .canonicalGroupKey;

    if (typeof lineKey !== "string" || lineKey.trim().length === 0) continue;
    if (typeof tMs !== "number" || !Number.isFinite(tMs) || tMs < 0) continue;
    if (typeof text !== "string" || text.trim().length === 0) continue;

    const cue: LyricCue = {
      lineKey: lineKey.trim(),
      tMs: Math.floor(tMs),
      text: text.trim(),
    };

    if (typeof endMs === "number" && Number.isFinite(endMs) && endMs >= 0) {
      cue.endMs = Math.floor(endMs);
    }

    // optional enrichment
    if (typeof canonicalGroupKey === "string" && canonicalGroupKey.trim()) {
      cue.canonicalGroupKey = canonicalGroupKey.trim();
    }

    out.push(cue);
  }

  out.sort((a, b) => a.tMs - b.tMs);
  return out;
}

export type SanityLyricCue = {
  _key?: string;
  tMs?: number;
  text?: string;
  endMs?: number;
};

// Adapts Sanity cue shape into canonical cue normalization.
// (Sanity uses `_key` for the line key.)
export function normalizeLyricCuesFromSanity(
  input: SanityLyricCue[] | undefined | null,
): LyricCue[] {
  if (!Array.isArray(input)) return [];
  return normalizeLyricCues(
    input.map((c) => ({
      lineKey: c?._key,
      tMs: c?.tMs,
      text: c?.text,
      endMs: c?.endMs,
      // canonicalGroupKey not present in Sanity lyrics docs
    })),
  );
}

export function parseTrackLyricsApiOk(raw: unknown): TrackLyricsApiOk | null {
  if (!raw || typeof raw !== "object") return null;
  const o = raw as Record<string, unknown>;
  if (o.ok !== true) return null;

  if (typeof o.recordingId !== "string" || !o.recordingId.trim()) return null;
  if (typeof o.offsetMs !== "number" || !Number.isFinite(o.offsetMs))
    return null;

  const cues = normalizeLyricCues(o.cues);

  // groupMap is optional; keep it only if it looks like the right shape
  const groupMap =
    o.groupMap && typeof o.groupMap === "object" && !Array.isArray(o.groupMap)
      ? (o.groupMap as LyricGroupMap)
      : undefined;

  const out: TrackLyricsApiOk = {
    ok: true,
    recordingId: o.recordingId.trim(),
    offsetMs: Math.floor(o.offsetMs),
    cues,
  };

  // Optional fields (keep them if present, but don’t over-validate here)
  if (typeof o.version === "string" && o.version.trim())
    out.version = o.version.trim();
  if (typeof o.geniusUrl === "string" || o.geniusUrl === null)
    out.geniusUrl = o.geniusUrl as string | null;
  if (groupMap) out.groupMap = groupMap;

  // Nullable meta fields (type-safe, no `any`)
  const nullableMetaKeys = [
    "trackTitle",
    "trackArtist",
    "trackCatalogueId",
    "albumTitle",
    "albumSlug",
    "albumCatalogueId",
  ] as const;

  type NullableMetaKey = (typeof nullableMetaKeys)[number];

  for (const k of nullableMetaKeys) {
    const v = o[k];
    if (typeof v === "string") {
      out[k as NullableMetaKey] = v.trim() || null;
    } else if (v === null) {
      out[k as NullableMetaKey] = null;
    }
  }

  return out;
}


===== IN-FLIGHT / CONCURRENCY / ABORT CONTRACT =====

app/home/player/PlayerState.tsx-1-// web/app/home/player/PlayerState.tsx
app/home/player/PlayerState.tsx-2-"use client";
app/home/player/PlayerState.tsx-3-
app/home/player/PlayerState.tsx-4-import React from "react";
app/home/player/PlayerState.tsx-5-import { useAuth } from "@clerk/nextjs";
app/home/player/PlayerState.tsx-6-import type { PlayerTrack } from "@/lib/types";
app/home/player/PlayerState.tsx:7:import { ensureLyricsForTrack } from "@/app/home/player/lyrics/ensureLyricsForTrack";
app/home/player/PlayerState.tsx-8-import {
app/home/player/PlayerState.tsx-9-  fetchPlaybackAccessDecision,
app/home/player/PlayerState.tsx-10-  readPlaybackShareTokenFromLocation,
app/home/player/PlayerState.tsx-11-  type PlaybackAccessDecision,
app/home/player/PlayerState.tsx-12-} from "./playbackAccessClient";
app/home/player/PlayerState.tsx-13-
app/home/player/PlayerState.tsx-14-type PlayerStatus = "idle" | "loading" | "playing" | "paused";
app/home/player/PlayerState.tsx-15-type RepeatMode = "off" | "one" | "all";
app/home/player/PlayerState.tsx-16-type Intent = "play" | "pause" | null;
app/home/player/PlayerState.tsx-17-type LoadingReason = "token" | "attach" | "buffering";
app/home/player/PlayerState.tsx-18-
app/home/player/PlayerState.tsx-19-export type QueueContext = {
app/home/player/PlayerState.tsx-20-  contextId?: string;
app/home/player/PlayerState.tsx-21-  contextSlug?: string;
--
app/home/player/PlayerState.tsx-1117-      visualStatus,
app/home/player/PlayerState.tsx-1118-      visualIntent,
app/home/player/PlayerState.tsx-1119-      visualPlayRequestPending,
app/home/player/PlayerState.tsx-1120-      visualCurrent,
app/home/player/PlayerState.tsx-1121-      firstQueuedTrack,
app/home/player/PlayerState.tsx-1122-    ],
app/home/player/PlayerState.tsx-1123-  );
app/home/player/PlayerState.tsx-1124-
app/home/player/PlayerState.tsx-1125-  const currentId = state.current?.recordingId ?? null;
app/home/player/PlayerState.tsx-1126-  const pendingId = state.pendingRecordingId ?? null;
app/home/player/PlayerState.tsx-1127-
app/home/player/PlayerState.tsx-1128-  React.useEffect(() => {
app/home/player/PlayerState.tsx-1129-    const id = (currentId ?? "").trim();
app/home/player/PlayerState.tsx-1130-    if (!id) return;
app/home/player/PlayerState.tsx:1131:    void ensureLyricsForTrack(id);
app/home/player/PlayerState.tsx-1132-  }, [currentId]);
app/home/player/PlayerState.tsx-1133-
app/home/player/PlayerState.tsx-1134-  React.useEffect(() => {
app/home/player/PlayerState.tsx-1135-    const id = (pendingId ?? "").trim();
app/home/player/PlayerState.tsx-1136-    if (!id) return;
app/home/player/PlayerState.tsx:1137:    void ensureLyricsForTrack(id);
app/home/player/PlayerState.tsx-1138-  }, [pendingId]);
app/home/player/PlayerState.tsx-1139-
app/home/player/PlayerState.tsx-1140-  return (
app/home/player/PlayerState.tsx-1141-    <PlayerActionsCtx.Provider value={actions}>
app/home/player/PlayerState.tsx-1142-      <PlayerVisualCtx.Provider value={visualState}>
app/home/player/PlayerState.tsx-1143-        <PlayerCtx.Provider value={api}>{props.children}</PlayerCtx.Provider>
app/home/player/PlayerState.tsx-1144-      </PlayerVisualCtx.Provider>
app/home/player/PlayerState.tsx-1145-    </PlayerActionsCtx.Provider>
app/home/player/PlayerState.tsx-1146-  );
app/home/player/PlayerState.tsx-1147-}
app/home/player/PlayerState.tsx-1148-
app/home/player/PlayerState.tsx-1149-export function usePlayer() {
app/home/player/PlayerState.tsx-1150-  const ctx = React.useContext(PlayerCtx);
app/home/player/PlayerState.tsx-1151-  if (!ctx)
--
app/home/player/lyrics/ensureLyricsForTrack.ts:1:// web/app/home/player/lyrics/ensureLyricsForTrack.ts
app/home/player/lyrics/ensureLyricsForTrack.ts-2-"use client";
app/home/player/lyrics/ensureLyricsForTrack.ts-3-
app/home/player/lyrics/ensureLyricsForTrack.ts-4-import { lyricsSurface } from "./lyricsSurface";
app/home/player/lyrics/ensureLyricsForTrack.ts-5-import { fetchLyricsByrecordingId } from "./fetchLyricsByRecordingId";
app/home/player/lyrics/ensureLyricsForTrack.ts-6-import type { AlbumLyricsBundle } from "@/lib/types";
app/home/player/lyrics/ensureLyricsForTrack.ts-7-
app/home/player/lyrics/ensureLyricsForTrack.ts-8-// Module-scope in-flight map so *any* caller (any surface) dedupes fetches.
app/home/player/lyrics/ensureLyricsForTrack.ts:9:const inFlight = new Map<string, AbortController>();
app/home/player/lyrics/ensureLyricsForTrack.ts-10-
app/home/player/lyrics/ensureLyricsForTrack.ts-11-function hasOwn(obj: object, key: string): boolean {
app/home/player/lyrics/ensureLyricsForTrack.ts-12-  return Object.prototype.hasOwnProperty.call(obj, key);
app/home/player/lyrics/ensureLyricsForTrack.ts-13-}
app/home/player/lyrics/ensureLyricsForTrack.ts-14-
app/home/player/lyrics/ensureLyricsForTrack.ts-15-/**
app/home/player/lyrics/ensureLyricsForTrack.ts-16- * Prime lyricsSurface from an album-provided bundle.
app/home/player/lyrics/ensureLyricsForTrack.ts-17- * - Merges without clobbering existing per-track entries (unless missing).
app/home/player/lyrics/ensureLyricsForTrack.ts-18- * - Ensures we still preserve "known no lyrics" as [] if already present.
app/home/player/lyrics/ensureLyricsForTrack.ts-19- * - No network; safe to call repeatedly (idempotent-ish).
app/home/player/lyrics/ensureLyricsForTrack.ts-20- */
app/home/player/lyrics/ensureLyricsForTrack.ts-21-export function primeLyricsFromAlbumBundle(
app/home/player/lyrics/ensureLyricsForTrack.ts-22-  bundle: AlbumLyricsBundle | null | undefined,
app/home/player/lyrics/ensureLyricsForTrack.ts-23-) {
--
app/home/player/lyrics/ensureLyricsForTrack.ts-57-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-58-
app/home/player/lyrics/ensureLyricsForTrack.ts-59-  // Only write if something changed (cheap reference check).
app/home/player/lyrics/ensureLyricsForTrack.ts-60-  if (cuesOut === snap.cuesByRecordingId && offOut === snap.offsetByRecordingId) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-61-
app/home/player/lyrics/ensureLyricsForTrack.ts-62-  lyricsSurface.setMaps({
app/home/player/lyrics/ensureLyricsForTrack.ts-63-    cuesByrecordingId: cuesOut,
app/home/player/lyrics/ensureLyricsForTrack.ts-64-    offsetByrecordingId: offOut,
app/home/player/lyrics/ensureLyricsForTrack.ts-65-    globalOffsetMs: snap.globalOffsetMs,
app/home/player/lyrics/ensureLyricsForTrack.ts-66-  });
app/home/player/lyrics/ensureLyricsForTrack.ts-67-}
app/home/player/lyrics/ensureLyricsForTrack.ts-68-
app/home/player/lyrics/ensureLyricsForTrack.ts-69-/**
app/home/player/lyrics/ensureLyricsForTrack.ts-70- * Ensure lyrics (cues + offset) exist in lyricsSurface for recordingId.
app/home/player/lyrics/ensureLyricsForTrack.ts:71: * - Dedupes concurrent calls (module-level inFlight).
app/home/player/lyrics/ensureLyricsForTrack.ts-72- * - Caches "no lyrics" as [] to avoid refetch loops.
app/home/player/lyrics/ensureLyricsForTrack.ts-73- * - Safe to call repeatedly from any surface.
app/home/player/lyrics/ensureLyricsForTrack.ts-74- */
app/home/player/lyrics/ensureLyricsForTrack.ts:75:export async function ensureLyricsForTrack(
app/home/player/lyrics/ensureLyricsForTrack.ts-76-  recordingId: string,
app/home/player/lyrics/ensureLyricsForTrack.ts:77:  opts?: { signal?: AbortSignal },
app/home/player/lyrics/ensureLyricsForTrack.ts-78-): Promise<void> {
app/home/player/lyrics/ensureLyricsForTrack.ts-79-  const id = (recordingId ?? "").trim();
app/home/player/lyrics/ensureLyricsForTrack.ts-80-  if (!id) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-81-
app/home/player/lyrics/ensureLyricsForTrack.ts-82-  const snap = lyricsSurface.getSnapshot();
app/home/player/lyrics/ensureLyricsForTrack.ts-83-  const existing = snap.cuesByRecordingId[id];
app/home/player/lyrics/ensureLyricsForTrack.ts-84-
app/home/player/lyrics/ensureLyricsForTrack.ts-85-  const knownKey = hasOwn(snap.cuesByRecordingId, id);
app/home/player/lyrics/ensureLyricsForTrack.ts-86-  const hasCues = Array.isArray(existing) && existing.length > 0;
app/home/player/lyrics/ensureLyricsForTrack.ts-87-  const knownNoLyrics =
app/home/player/lyrics/ensureLyricsForTrack.ts-88-    knownKey && Array.isArray(existing) && existing.length === 0;
app/home/player/lyrics/ensureLyricsForTrack.ts-89-
app/home/player/lyrics/ensureLyricsForTrack.ts-90-  if (hasCues || knownNoLyrics) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-91-
app/home/player/lyrics/ensureLyricsForTrack.ts-92-  // Already fetching?
app/home/player/lyrics/ensureLyricsForTrack.ts:93:  if (inFlight.has(id)) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-94-
app/home/player/lyrics/ensureLyricsForTrack.ts:95:  const ac = new AbortController();
app/home/player/lyrics/ensureLyricsForTrack.ts:96:  inFlight.set(id, ac);
app/home/player/lyrics/ensureLyricsForTrack.ts-97-
app/home/player/lyrics/ensureLyricsForTrack.ts-98-  // Caller abort should NOT abort the shared in-flight request.
app/home/player/lyrics/ensureLyricsForTrack.ts-99-  // It only means "this caller no longer cares". The module-scope fetch may be
app/home/player/lyrics/ensureLyricsForTrack.ts-100-  // servicing other callers (PlayerState, StageInline, etc).
app/home/player/lyrics/ensureLyricsForTrack.ts-101-  const outerSignal = opts?.signal;
app/home/player/lyrics/ensureLyricsForTrack.ts:102:  if (outerSignal?.aborted) {
app/home/player/lyrics/ensureLyricsForTrack.ts:103:    inFlight.delete(id);
app/home/player/lyrics/ensureLyricsForTrack.ts-104-    return;
app/home/player/lyrics/ensureLyricsForTrack.ts-105-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-106-
app/home/player/lyrics/ensureLyricsForTrack.ts-107-  try {
app/home/player/lyrics/ensureLyricsForTrack.ts-108-    const r = await fetchLyricsByrecordingId(id, ac.signal);
app/home/player/lyrics/ensureLyricsForTrack.ts-109-    if (!r) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-110-    if (r.recordingId !== id) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-111-
app/home/player/lyrics/ensureLyricsForTrack.ts-112-    const prev = lyricsSurface.getSnapshot();
app/home/player/lyrics/ensureLyricsForTrack.ts-113-
app/home/player/lyrics/ensureLyricsForTrack.ts-114-    const nextCuesByrecordingId =
app/home/player/lyrics/ensureLyricsForTrack.ts-115-      Array.isArray(r.cues) && r.cues.length
app/home/player/lyrics/ensureLyricsForTrack.ts-116-        ? { ...prev.cuesByRecordingId, [id]: r.cues }
app/home/player/lyrics/ensureLyricsForTrack.ts-117-        : hasOwn(prev.cuesByRecordingId, id)
--
app/home/player/lyrics/ensureLyricsForTrack.ts-119-          : { ...prev.cuesByRecordingId, [id]: [] };
app/home/player/lyrics/ensureLyricsForTrack.ts-120-
app/home/player/lyrics/ensureLyricsForTrack.ts-121-    const nextOffsetByrecordingId =
app/home/player/lyrics/ensureLyricsForTrack.ts-122-      typeof r.offsetMs === "number" && Number.isFinite(r.offsetMs)
app/home/player/lyrics/ensureLyricsForTrack.ts-123-        ? { ...prev.offsetByRecordingId, [id]: r.offsetMs }
app/home/player/lyrics/ensureLyricsForTrack.ts-124-        : prev.offsetByRecordingId;
app/home/player/lyrics/ensureLyricsForTrack.ts-125-
app/home/player/lyrics/ensureLyricsForTrack.ts-126-    lyricsSurface.setMaps({
app/home/player/lyrics/ensureLyricsForTrack.ts-127-      cuesByrecordingId: nextCuesByrecordingId,
app/home/player/lyrics/ensureLyricsForTrack.ts-128-      offsetByrecordingId: nextOffsetByrecordingId,
app/home/player/lyrics/ensureLyricsForTrack.ts-129-      // preserve whatever globalOffsetMs currently is (lyricsSurface handles defaulting)
app/home/player/lyrics/ensureLyricsForTrack.ts-130-      globalOffsetMs: prev.globalOffsetMs,
app/home/player/lyrics/ensureLyricsForTrack.ts-131-    });
app/home/player/lyrics/ensureLyricsForTrack.ts-132-  } finally {
app/home/player/lyrics/ensureLyricsForTrack.ts:133:    inFlight.delete(id);
app/home/player/lyrics/ensureLyricsForTrack.ts-134-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-135-}
--
app/home/player/StageInline.tsx-1-// web/app/home/player/StageInline.tsx
app/home/player/StageInline.tsx-2-"use client";
app/home/player/StageInline.tsx-3-
app/home/player/StageInline.tsx-4-import React from "react";
app/home/player/StageInline.tsx-5-import { createPortal } from "react-dom";
app/home/player/StageInline.tsx-6-import { usePlayer } from "./PlayerState";
app/home/player/StageInline.tsx-7-import StageCore from "./StageCore";
app/home/player/StageInline.tsx-8-import StageTransportBar from "./StageTransportBar";
app/home/player/StageInline.tsx-9-import StageNowPlayingBadge from "./stage/StageNowPlayingBadge";
app/home/player/StageInline.tsx-10-import StagePerfHud from "./stage/StagePerfHud";
app/home/player/StageInline.tsx:11:import { ensureLyricsForTrack } from "./lyrics/ensureLyricsForTrack";
app/home/player/StageInline.tsx-12-
app/home/player/StageInline.tsx-13-function lockBodyScroll(lock: boolean) {
app/home/player/StageInline.tsx-14-  if (typeof document === "undefined") return;
app/home/player/StageInline.tsx-15-  const el = document.documentElement;
app/home/player/StageInline.tsx-16-  const body = document.body;
app/home/player/StageInline.tsx-17-  if (lock) {
app/home/player/StageInline.tsx-18-    el.style.overflow = "hidden";
app/home/player/StageInline.tsx-19-    body.style.overflow = "hidden";
app/home/player/StageInline.tsx-20-    body.style.touchAction = "none";
app/home/player/StageInline.tsx-21-  } else {
app/home/player/StageInline.tsx-22-    el.style.overflow = "";
app/home/player/StageInline.tsx-23-    body.style.overflow = "";
app/home/player/StageInline.tsx-24-    body.style.touchAction = "";
app/home/player/StageInline.tsx-25-  }
--
app/home/player/StageInline.tsx-288-  },
app/home/player/StageInline.tsx-289-);
app/home/player/StageInline.tsx-290-
app/home/player/StageInline.tsx-291-export default function StageInline(props: { height?: number }) {
app/home/player/StageInline.tsx-292-  const { height = 300 } = props;
app/home/player/StageInline.tsx-293-  const p = usePlayer();
app/home/player/StageInline.tsx-294-
app/home/player/StageInline.tsx-295-  // Lazy-load lyrics for the currently playing track when missing.
app/home/player/StageInline.tsx-296-  const currentRecordingId = p.current?.recordingId ?? null;
app/home/player/StageInline.tsx-297-
app/home/player/StageInline.tsx-298-  React.useEffect(() => {
app/home/player/StageInline.tsx-299-    if (!currentRecordingId) return;
app/home/player/StageInline.tsx-300-
app/home/player/StageInline.tsx-301-    const recordingId = currentRecordingId; // stable capture
app/home/player/StageInline.tsx:302:    const ac = new AbortController();
app/home/player/StageInline.tsx-303-
app/home/player/StageInline.tsx:304:    void ensureLyricsForTrack(recordingId, { signal: ac.signal });
app/home/player/StageInline.tsx-305-
app/home/player/StageInline.tsx:306:    return () => ac.abort();
app/home/player/StageInline.tsx-307-  }, [currentRecordingId]);
app/home/player/StageInline.tsx-308-
app/home/player/StageInline.tsx-309-  const isMobile = useIsMobile(640);
app/home/player/StageInline.tsx-310-  const isAdmin = useIsAdminBodyFlag();
app/home/player/StageInline.tsx-311-  const inlineHeight = isMobile
app/home/player/StageInline.tsx-312-    ? Math.max(140, Math.round(height * 0.5))
app/home/player/StageInline.tsx-313-    : height;
app/home/player/StageInline.tsx-314-
app/home/player/StageInline.tsx-315-  const [mounted, setMounted] = React.useState(false);
app/home/player/StageInline.tsx-316-  React.useEffect(() => setMounted(true), []);
app/home/player/StageInline.tsx-317-
app/home/player/StageInline.tsx-318-  const [open, setOpen] = React.useState(false);
app/home/player/StageInline.tsx-319-  const enableIdleCursor = open && !isMobile;
app/home/player/StageInline.tsx-320-  const cursorHidden = useIdleCursor(enableIdleCursor, 3000);
--
app/home/player/FullPlayer.tsx-9-  fetchPlaybackAccessDecision,
app/home/player/FullPlayer.tsx-10-  getCachedPlaybackAccessDecision,
app/home/player/FullPlayer.tsx-11-  isPlaybackAccessAction,
app/home/player/FullPlayer.tsx-12-  type PlaybackAccessDecision as AccessState,
app/home/player/FullPlayer.tsx-13-} from "./playbackAccessClient";
app/home/player/FullPlayer.tsx-14-import { useGateBroker } from "@/app/home/gating/GateBroker";
app/home/player/FullPlayer.tsx-15-import type { GateAction, GateCodeRaw } from "@/app/home/gating/gateTypes";
app/home/player/FullPlayer.tsx-16-import type {
app/home/player/FullPlayer.tsx-17-  AlbumPlayerBundle,
app/home/player/FullPlayer.tsx-18-  AlbumNavItem,
app/home/player/FullPlayer.tsx-19-  Tier,
app/home/player/FullPlayer.tsx-20-  TierName,
app/home/player/FullPlayer.tsx-21-  PlayerTrack,
app/home/player/FullPlayer.tsx-22-} from "@/lib/types";
app/home/player/FullPlayer.tsx:23:import { primeLyricsFromAlbumBundle } from "./lyrics/ensureLyricsForTrack";
app/home/player/FullPlayer.tsx-24-import { deriveShareContext, shareAlbum, shareTrack } from "./share";
app/home/player/FullPlayer.tsx-25-import { PatternRingGlow } from "./VisualizerPattern";
app/home/player/FullPlayer.tsx-26-
app/home/player/FullPlayer.tsx-27-const LIVE_LISTENER_COUNT_ENABLED =
app/home/player/FullPlayer.tsx-28-  process.env.NEXT_PUBLIC_LIVE_LISTENER_COUNT_ENABLED === "true";
app/home/player/FullPlayer.tsx-29-
app/home/player/FullPlayer.tsx-30-function fmtTime(ms: number) {
app/home/player/FullPlayer.tsx-31-  const s = Math.max(0, Math.floor(ms / 1000));
app/home/player/FullPlayer.tsx-32-  const m = Math.floor(s / 60);
app/home/player/FullPlayer.tsx-33-  const r = s % 60;
app/home/player/FullPlayer.tsx-34-  return `${m}:${String(r).padStart(2, "0")}`;
app/home/player/FullPlayer.tsx-35-}
app/home/player/FullPlayer.tsx-36-
app/home/player/FullPlayer.tsx-37-function fmtActiveListeners(value: number): string {
--
app/home/player/lyrics/ensureLyricsForTrack.ts:1:// web/app/home/player/lyrics/ensureLyricsForTrack.ts
app/home/player/lyrics/ensureLyricsForTrack.ts-2-"use client";
app/home/player/lyrics/ensureLyricsForTrack.ts-3-
app/home/player/lyrics/ensureLyricsForTrack.ts-4-import { lyricsSurface } from "./lyricsSurface";
app/home/player/lyrics/ensureLyricsForTrack.ts-5-import { fetchLyricsByrecordingId } from "./fetchLyricsByRecordingId";
app/home/player/lyrics/ensureLyricsForTrack.ts-6-import type { AlbumLyricsBundle } from "@/lib/types";
app/home/player/lyrics/ensureLyricsForTrack.ts-7-
app/home/player/lyrics/ensureLyricsForTrack.ts-8-// Module-scope in-flight map so *any* caller (any surface) dedupes fetches.
app/home/player/lyrics/ensureLyricsForTrack.ts:9:const inFlight = new Map<string, AbortController>();
app/home/player/lyrics/ensureLyricsForTrack.ts-10-
app/home/player/lyrics/ensureLyricsForTrack.ts-11-function hasOwn(obj: object, key: string): boolean {
app/home/player/lyrics/ensureLyricsForTrack.ts-12-  return Object.prototype.hasOwnProperty.call(obj, key);
app/home/player/lyrics/ensureLyricsForTrack.ts-13-}
app/home/player/lyrics/ensureLyricsForTrack.ts-14-
app/home/player/lyrics/ensureLyricsForTrack.ts-15-/**
app/home/player/lyrics/ensureLyricsForTrack.ts-16- * Prime lyricsSurface from an album-provided bundle.
app/home/player/lyrics/ensureLyricsForTrack.ts-17- * - Merges without clobbering existing per-track entries (unless missing).
app/home/player/lyrics/ensureLyricsForTrack.ts-18- * - Ensures we still preserve "known no lyrics" as [] if already present.
app/home/player/lyrics/ensureLyricsForTrack.ts-19- * - No network; safe to call repeatedly (idempotent-ish).
app/home/player/lyrics/ensureLyricsForTrack.ts-20- */
app/home/player/lyrics/ensureLyricsForTrack.ts-21-export function primeLyricsFromAlbumBundle(
app/home/player/lyrics/ensureLyricsForTrack.ts-22-  bundle: AlbumLyricsBundle | null | undefined,
app/home/player/lyrics/ensureLyricsForTrack.ts-23-) {
--
app/home/player/lyrics/ensureLyricsForTrack.ts-57-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-58-
app/home/player/lyrics/ensureLyricsForTrack.ts-59-  // Only write if something changed (cheap reference check).
app/home/player/lyrics/ensureLyricsForTrack.ts-60-  if (cuesOut === snap.cuesByRecordingId && offOut === snap.offsetByRecordingId) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-61-
app/home/player/lyrics/ensureLyricsForTrack.ts-62-  lyricsSurface.setMaps({
app/home/player/lyrics/ensureLyricsForTrack.ts-63-    cuesByrecordingId: cuesOut,
app/home/player/lyrics/ensureLyricsForTrack.ts-64-    offsetByrecordingId: offOut,
app/home/player/lyrics/ensureLyricsForTrack.ts-65-    globalOffsetMs: snap.globalOffsetMs,
app/home/player/lyrics/ensureLyricsForTrack.ts-66-  });
app/home/player/lyrics/ensureLyricsForTrack.ts-67-}
app/home/player/lyrics/ensureLyricsForTrack.ts-68-
app/home/player/lyrics/ensureLyricsForTrack.ts-69-/**
app/home/player/lyrics/ensureLyricsForTrack.ts-70- * Ensure lyrics (cues + offset) exist in lyricsSurface for recordingId.
app/home/player/lyrics/ensureLyricsForTrack.ts:71: * - Dedupes concurrent calls (module-level inFlight).
app/home/player/lyrics/ensureLyricsForTrack.ts-72- * - Caches "no lyrics" as [] to avoid refetch loops.
app/home/player/lyrics/ensureLyricsForTrack.ts-73- * - Safe to call repeatedly from any surface.
app/home/player/lyrics/ensureLyricsForTrack.ts-74- */
app/home/player/lyrics/ensureLyricsForTrack.ts:75:export async function ensureLyricsForTrack(
app/home/player/lyrics/ensureLyricsForTrack.ts-76-  recordingId: string,
app/home/player/lyrics/ensureLyricsForTrack.ts:77:  opts?: { signal?: AbortSignal },
app/home/player/lyrics/ensureLyricsForTrack.ts-78-): Promise<void> {
app/home/player/lyrics/ensureLyricsForTrack.ts-79-  const id = (recordingId ?? "").trim();
app/home/player/lyrics/ensureLyricsForTrack.ts-80-  if (!id) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-81-
app/home/player/lyrics/ensureLyricsForTrack.ts-82-  const snap = lyricsSurface.getSnapshot();
app/home/player/lyrics/ensureLyricsForTrack.ts-83-  const existing = snap.cuesByRecordingId[id];
app/home/player/lyrics/ensureLyricsForTrack.ts-84-
app/home/player/lyrics/ensureLyricsForTrack.ts-85-  const knownKey = hasOwn(snap.cuesByRecordingId, id);
app/home/player/lyrics/ensureLyricsForTrack.ts-86-  const hasCues = Array.isArray(existing) && existing.length > 0;
app/home/player/lyrics/ensureLyricsForTrack.ts-87-  const knownNoLyrics =
app/home/player/lyrics/ensureLyricsForTrack.ts-88-    knownKey && Array.isArray(existing) && existing.length === 0;
app/home/player/lyrics/ensureLyricsForTrack.ts-89-
app/home/player/lyrics/ensureLyricsForTrack.ts-90-  if (hasCues || knownNoLyrics) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-91-
app/home/player/lyrics/ensureLyricsForTrack.ts-92-  // Already fetching?
app/home/player/lyrics/ensureLyricsForTrack.ts:93:  if (inFlight.has(id)) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-94-
app/home/player/lyrics/ensureLyricsForTrack.ts:95:  const ac = new AbortController();
app/home/player/lyrics/ensureLyricsForTrack.ts:96:  inFlight.set(id, ac);
app/home/player/lyrics/ensureLyricsForTrack.ts-97-
app/home/player/lyrics/ensureLyricsForTrack.ts-98-  // Caller abort should NOT abort the shared in-flight request.
app/home/player/lyrics/ensureLyricsForTrack.ts-99-  // It only means "this caller no longer cares". The module-scope fetch may be
app/home/player/lyrics/ensureLyricsForTrack.ts-100-  // servicing other callers (PlayerState, StageInline, etc).
app/home/player/lyrics/ensureLyricsForTrack.ts-101-  const outerSignal = opts?.signal;
app/home/player/lyrics/ensureLyricsForTrack.ts:102:  if (outerSignal?.aborted) {
app/home/player/lyrics/ensureLyricsForTrack.ts:103:    inFlight.delete(id);
app/home/player/lyrics/ensureLyricsForTrack.ts-104-    return;
app/home/player/lyrics/ensureLyricsForTrack.ts-105-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-106-
app/home/player/lyrics/ensureLyricsForTrack.ts-107-  try {
app/home/player/lyrics/ensureLyricsForTrack.ts-108-    const r = await fetchLyricsByrecordingId(id, ac.signal);
app/home/player/lyrics/ensureLyricsForTrack.ts-109-    if (!r) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-110-    if (r.recordingId !== id) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-111-
app/home/player/lyrics/ensureLyricsForTrack.ts-112-    const prev = lyricsSurface.getSnapshot();
app/home/player/lyrics/ensureLyricsForTrack.ts-113-
app/home/player/lyrics/ensureLyricsForTrack.ts-114-    const nextCuesByrecordingId =
app/home/player/lyrics/ensureLyricsForTrack.ts-115-      Array.isArray(r.cues) && r.cues.length
app/home/player/lyrics/ensureLyricsForTrack.ts-116-        ? { ...prev.cuesByRecordingId, [id]: r.cues }
app/home/player/lyrics/ensureLyricsForTrack.ts-117-        : hasOwn(prev.cuesByRecordingId, id)
--
app/home/player/lyrics/ensureLyricsForTrack.ts-119-          : { ...prev.cuesByRecordingId, [id]: [] };
app/home/player/lyrics/ensureLyricsForTrack.ts-120-
app/home/player/lyrics/ensureLyricsForTrack.ts-121-    const nextOffsetByrecordingId =
app/home/player/lyrics/ensureLyricsForTrack.ts-122-      typeof r.offsetMs === "number" && Number.isFinite(r.offsetMs)
app/home/player/lyrics/ensureLyricsForTrack.ts-123-        ? { ...prev.offsetByRecordingId, [id]: r.offsetMs }
app/home/player/lyrics/ensureLyricsForTrack.ts-124-        : prev.offsetByRecordingId;
app/home/player/lyrics/ensureLyricsForTrack.ts-125-
app/home/player/lyrics/ensureLyricsForTrack.ts-126-    lyricsSurface.setMaps({
app/home/player/lyrics/ensureLyricsForTrack.ts-127-      cuesByrecordingId: nextCuesByrecordingId,
app/home/player/lyrics/ensureLyricsForTrack.ts-128-      offsetByrecordingId: nextOffsetByrecordingId,
app/home/player/lyrics/ensureLyricsForTrack.ts-129-      // preserve whatever globalOffsetMs currently is (lyricsSurface handles defaulting)
app/home/player/lyrics/ensureLyricsForTrack.ts-130-      globalOffsetMs: prev.globalOffsetMs,
app/home/player/lyrics/ensureLyricsForTrack.ts-131-    });
app/home/player/lyrics/ensureLyricsForTrack.ts-132-  } finally {
app/home/player/lyrics/ensureLyricsForTrack.ts:133:    inFlight.delete(id);
app/home/player/lyrics/ensureLyricsForTrack.ts-134-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-135-}

===== LYRICS FETCH / RECORDING ID CONTRACT =====

app/home/player/lyrics/ensureLyricsForTrack.ts-1-// web/app/home/player/lyrics/ensureLyricsForTrack.ts
app/home/player/lyrics/ensureLyricsForTrack.ts-2-"use client";
app/home/player/lyrics/ensureLyricsForTrack.ts-3-
app/home/player/lyrics/ensureLyricsForTrack.ts-4-import { lyricsSurface } from "./lyricsSurface";
app/home/player/lyrics/ensureLyricsForTrack.ts:5:import { fetchLyricsByrecordingId } from "./fetchLyricsByRecordingId";
app/home/player/lyrics/ensureLyricsForTrack.ts-6-import type { AlbumLyricsBundle } from "@/lib/types";
app/home/player/lyrics/ensureLyricsForTrack.ts-7-
app/home/player/lyrics/ensureLyricsForTrack.ts-8-// Module-scope in-flight map so *any* caller (any surface) dedupes fetches.
app/home/player/lyrics/ensureLyricsForTrack.ts-9-const inFlight = new Map<string, AbortController>();
app/home/player/lyrics/ensureLyricsForTrack.ts-10-
app/home/player/lyrics/ensureLyricsForTrack.ts-11-function hasOwn(obj: object, key: string): boolean {
app/home/player/lyrics/ensureLyricsForTrack.ts-12-  return Object.prototype.hasOwnProperty.call(obj, key);
app/home/player/lyrics/ensureLyricsForTrack.ts-13-}
app/home/player/lyrics/ensureLyricsForTrack.ts-14-
app/home/player/lyrics/ensureLyricsForTrack.ts-15-/**
app/home/player/lyrics/ensureLyricsForTrack.ts-16- * Prime lyricsSurface from an album-provided bundle.
app/home/player/lyrics/ensureLyricsForTrack.ts-17- * - Merges without clobbering existing per-track entries (unless missing).
app/home/player/lyrics/ensureLyricsForTrack.ts-18- * - Ensures we still preserve "known no lyrics" as [] if already present.
app/home/player/lyrics/ensureLyricsForTrack.ts-19- * - No network; safe to call repeatedly (idempotent-ish).
app/home/player/lyrics/ensureLyricsForTrack.ts-20- */
app/home/player/lyrics/ensureLyricsForTrack.ts-21-export function primeLyricsFromAlbumBundle(
app/home/player/lyrics/ensureLyricsForTrack.ts-22-  bundle: AlbumLyricsBundle | null | undefined,
app/home/player/lyrics/ensureLyricsForTrack.ts-23-) {
app/home/player/lyrics/ensureLyricsForTrack.ts-24-  if (!bundle) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-25-
app/home/player/lyrics/ensureLyricsForTrack.ts-26-  const snap = lyricsSurface.getSnapshot();
app/home/player/lyrics/ensureLyricsForTrack.ts-27-
app/home/player/lyrics/ensureLyricsForTrack.ts:28:  const cuesIn = bundle.cuesByRecordingId ?? {};
app/home/player/lyrics/ensureLyricsForTrack.ts-29-  const offIn = bundle.offsetByRecordingId ?? {};
app/home/player/lyrics/ensureLyricsForTrack.ts-30-
app/home/player/lyrics/ensureLyricsForTrack.ts-31-  // Build merged maps but only add keys that are currently unknown.
app/home/player/lyrics/ensureLyricsForTrack.ts:32:  let cuesOut = snap.cuesByRecordingId;
app/home/player/lyrics/ensureLyricsForTrack.ts-33-  let offOut = snap.offsetByRecordingId;
app/home/player/lyrics/ensureLyricsForTrack.ts-34-
app/home/player/lyrics/ensureLyricsForTrack.ts:35:  for (const [recordingIdRaw, cuesRaw] of Object.entries(cuesIn)) {
app/home/player/lyrics/ensureLyricsForTrack.ts:36:    const id = (recordingIdRaw ?? "").trim();
app/home/player/lyrics/ensureLyricsForTrack.ts-37-    if (!id) continue;
app/home/player/lyrics/ensureLyricsForTrack.ts-38-
app/home/player/lyrics/ensureLyricsForTrack.ts:39:    if (hasOwn(cuesOut, id)) continue; // don't overwrite existing (including [] known-no-lyrics)
app/home/player/lyrics/ensureLyricsForTrack.ts-40-
app/home/player/lyrics/ensureLyricsForTrack.ts:41:    const cues = Array.isArray(cuesRaw) ? cuesRaw : [];
app/home/player/lyrics/ensureLyricsForTrack.ts:42:    cuesOut = { ...cuesOut, [id]: cues };
app/home/player/lyrics/ensureLyricsForTrack.ts-43-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-44-
app/home/player/lyrics/ensureLyricsForTrack.ts:45:  for (const [recordingIdRaw, offRaw] of Object.entries(offIn)) {
app/home/player/lyrics/ensureLyricsForTrack.ts:46:    const id = (recordingIdRaw ?? "").trim();
app/home/player/lyrics/ensureLyricsForTrack.ts-47-    if (!id) continue;
app/home/player/lyrics/ensureLyricsForTrack.ts-48-
app/home/player/lyrics/ensureLyricsForTrack.ts-49-    if (hasOwn(offOut, id)) continue; // don't overwrite existing
app/home/player/lyrics/ensureLyricsForTrack.ts-50-
app/home/player/lyrics/ensureLyricsForTrack.ts-51-    const n =
app/home/player/lyrics/ensureLyricsForTrack.ts-52-      typeof offRaw === "number" && Number.isFinite(offRaw)
app/home/player/lyrics/ensureLyricsForTrack.ts-53-        ? Math.floor(offRaw)
app/home/player/lyrics/ensureLyricsForTrack.ts-54-        : 0;
app/home/player/lyrics/ensureLyricsForTrack.ts-55-
app/home/player/lyrics/ensureLyricsForTrack.ts-56-    offOut = { ...offOut, [id]: n };
app/home/player/lyrics/ensureLyricsForTrack.ts-57-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-58-
app/home/player/lyrics/ensureLyricsForTrack.ts-59-  // Only write if something changed (cheap reference check).
app/home/player/lyrics/ensureLyricsForTrack.ts:60:  if (cuesOut === snap.cuesByRecordingId && offOut === snap.offsetByRecordingId) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-61-
app/home/player/lyrics/ensureLyricsForTrack.ts-62-  lyricsSurface.setMaps({
app/home/player/lyrics/ensureLyricsForTrack.ts:63:    cuesByrecordingId: cuesOut,
app/home/player/lyrics/ensureLyricsForTrack.ts:64:    offsetByrecordingId: offOut,
app/home/player/lyrics/ensureLyricsForTrack.ts-65-    globalOffsetMs: snap.globalOffsetMs,
app/home/player/lyrics/ensureLyricsForTrack.ts-66-  });
app/home/player/lyrics/ensureLyricsForTrack.ts-67-}
app/home/player/lyrics/ensureLyricsForTrack.ts-68-
app/home/player/lyrics/ensureLyricsForTrack.ts-69-/**
app/home/player/lyrics/ensureLyricsForTrack.ts:70: * Ensure lyrics (cues + offset) exist in lyricsSurface for recordingId.
app/home/player/lyrics/ensureLyricsForTrack.ts-71- * - Dedupes concurrent calls (module-level inFlight).
app/home/player/lyrics/ensureLyricsForTrack.ts-72- * - Caches "no lyrics" as [] to avoid refetch loops.
app/home/player/lyrics/ensureLyricsForTrack.ts-73- * - Safe to call repeatedly from any surface.
app/home/player/lyrics/ensureLyricsForTrack.ts-74- */
app/home/player/lyrics/ensureLyricsForTrack.ts-75-export async function ensureLyricsForTrack(
app/home/player/lyrics/ensureLyricsForTrack.ts:76:  recordingId: string,
app/home/player/lyrics/ensureLyricsForTrack.ts-77-  opts?: { signal?: AbortSignal },
app/home/player/lyrics/ensureLyricsForTrack.ts-78-): Promise<void> {
app/home/player/lyrics/ensureLyricsForTrack.ts:79:  const id = (recordingId ?? "").trim();
app/home/player/lyrics/ensureLyricsForTrack.ts-80-  if (!id) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-81-
app/home/player/lyrics/ensureLyricsForTrack.ts-82-  const snap = lyricsSurface.getSnapshot();
app/home/player/lyrics/ensureLyricsForTrack.ts:83:  const existing = snap.cuesByRecordingId[id];
app/home/player/lyrics/ensureLyricsForTrack.ts-84-
app/home/player/lyrics/ensureLyricsForTrack.ts:85:  const knownKey = hasOwn(snap.cuesByRecordingId, id);
app/home/player/lyrics/ensureLyricsForTrack.ts-86-  const hasCues = Array.isArray(existing) && existing.length > 0;
app/home/player/lyrics/ensureLyricsForTrack.ts-87-  const knownNoLyrics =
app/home/player/lyrics/ensureLyricsForTrack.ts-88-    knownKey && Array.isArray(existing) && existing.length === 0;
app/home/player/lyrics/ensureLyricsForTrack.ts-89-
app/home/player/lyrics/ensureLyricsForTrack.ts-90-  if (hasCues || knownNoLyrics) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-91-
app/home/player/lyrics/ensureLyricsForTrack.ts-92-  // Already fetching?
app/home/player/lyrics/ensureLyricsForTrack.ts-93-  if (inFlight.has(id)) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-94-
app/home/player/lyrics/ensureLyricsForTrack.ts-95-  const ac = new AbortController();
app/home/player/lyrics/ensureLyricsForTrack.ts-96-  inFlight.set(id, ac);
app/home/player/lyrics/ensureLyricsForTrack.ts-97-
app/home/player/lyrics/ensureLyricsForTrack.ts-98-  // Caller abort should NOT abort the shared in-flight request.
app/home/player/lyrics/ensureLyricsForTrack.ts-99-  // It only means "this caller no longer cares". The module-scope fetch may be
app/home/player/lyrics/ensureLyricsForTrack.ts-100-  // servicing other callers (PlayerState, StageInline, etc).
app/home/player/lyrics/ensureLyricsForTrack.ts-101-  const outerSignal = opts?.signal;
app/home/player/lyrics/ensureLyricsForTrack.ts-102-  if (outerSignal?.aborted) {
app/home/player/lyrics/ensureLyricsForTrack.ts-103-    inFlight.delete(id);
app/home/player/lyrics/ensureLyricsForTrack.ts-104-    return;
app/home/player/lyrics/ensureLyricsForTrack.ts-105-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-106-
app/home/player/lyrics/ensureLyricsForTrack.ts-107-  try {
app/home/player/lyrics/ensureLyricsForTrack.ts:108:    const r = await fetchLyricsByrecordingId(id, ac.signal);
app/home/player/lyrics/ensureLyricsForTrack.ts-109-    if (!r) return;
app/home/player/lyrics/ensureLyricsForTrack.ts:110:    if (r.recordingId !== id) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-111-
app/home/player/lyrics/ensureLyricsForTrack.ts-112-    const prev = lyricsSurface.getSnapshot();
app/home/player/lyrics/ensureLyricsForTrack.ts-113-
app/home/player/lyrics/ensureLyricsForTrack.ts:114:    const nextCuesByrecordingId =
app/home/player/lyrics/ensureLyricsForTrack.ts:115:      Array.isArray(r.cues) && r.cues.length
app/home/player/lyrics/ensureLyricsForTrack.ts:116:        ? { ...prev.cuesByRecordingId, [id]: r.cues }
app/home/player/lyrics/ensureLyricsForTrack.ts:117:        : hasOwn(prev.cuesByRecordingId, id)
app/home/player/lyrics/ensureLyricsForTrack.ts:118:          ? prev.cuesByRecordingId
app/home/player/lyrics/ensureLyricsForTrack.ts:119:          : { ...prev.cuesByRecordingId, [id]: [] };
app/home/player/lyrics/ensureLyricsForTrack.ts-120-
app/home/player/lyrics/ensureLyricsForTrack.ts:121:    const nextOffsetByrecordingId =
app/home/player/lyrics/ensureLyricsForTrack.ts:122:      typeof r.offsetMs === "number" && Number.isFinite(r.offsetMs)
app/home/player/lyrics/ensureLyricsForTrack.ts:123:        ? { ...prev.offsetByRecordingId, [id]: r.offsetMs }
app/home/player/lyrics/ensureLyricsForTrack.ts-124-        : prev.offsetByRecordingId;
app/home/player/lyrics/ensureLyricsForTrack.ts-125-
app/home/player/lyrics/ensureLyricsForTrack.ts-126-    lyricsSurface.setMaps({
app/home/player/lyrics/ensureLyricsForTrack.ts:127:      cuesByrecordingId: nextCuesByrecordingId,
app/home/player/lyrics/ensureLyricsForTrack.ts:128:      offsetByrecordingId: nextOffsetByrecordingId,
app/home/player/lyrics/ensureLyricsForTrack.ts-129-      // preserve whatever globalOffsetMs currently is (lyricsSurface handles defaulting)
app/home/player/lyrics/ensureLyricsForTrack.ts-130-      globalOffsetMs: prev.globalOffsetMs,
app/home/player/lyrics/ensureLyricsForTrack.ts-131-    });
app/home/player/lyrics/ensureLyricsForTrack.ts-132-  } finally {
app/home/player/lyrics/ensureLyricsForTrack.ts-133-    inFlight.delete(id);
app/home/player/lyrics/ensureLyricsForTrack.ts-134-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-135-}
--
app/home/player/StageInline.tsx-282-          </div>
app/home/player/StageInline.tsx-283-
app/home/player/StageInline.tsx-284-          <StageTransportBar />
app/home/player/StageInline.tsx-285-        </div>
app/home/player/StageInline.tsx-286-      </div>
app/home/player/StageInline.tsx-287-    );
app/home/player/StageInline.tsx-288-  },
app/home/player/StageInline.tsx-289-);
app/home/player/StageInline.tsx-290-
app/home/player/StageInline.tsx-291-export default function StageInline(props: { height?: number }) {
app/home/player/StageInline.tsx-292-  const { height = 300 } = props;
app/home/player/StageInline.tsx-293-  const p = usePlayer();
app/home/player/StageInline.tsx-294-
app/home/player/StageInline.tsx-295-  // Lazy-load lyrics for the currently playing track when missing.
app/home/player/StageInline.tsx:296:  const currentRecordingId = p.current?.recordingId ?? null;
app/home/player/StageInline.tsx-297-
app/home/player/StageInline.tsx-298-  React.useEffect(() => {
app/home/player/StageInline.tsx-299-    if (!currentRecordingId) return;
app/home/player/StageInline.tsx-300-
app/home/player/StageInline.tsx:301:    const recordingId = currentRecordingId; // stable capture
app/home/player/StageInline.tsx-302-    const ac = new AbortController();
app/home/player/StageInline.tsx-303-
app/home/player/StageInline.tsx:304:    void ensureLyricsForTrack(recordingId, { signal: ac.signal });
app/home/player/StageInline.tsx-305-
app/home/player/StageInline.tsx-306-    return () => ac.abort();
app/home/player/StageInline.tsx-307-  }, [currentRecordingId]);
app/home/player/StageInline.tsx-308-
app/home/player/StageInline.tsx-309-  const isMobile = useIsMobile(640);
app/home/player/StageInline.tsx-310-  const isAdmin = useIsAdminBodyFlag();
app/home/player/StageInline.tsx-311-  const inlineHeight = isMobile
app/home/player/StageInline.tsx-312-    ? Math.max(140, Math.round(height * 0.5))
app/home/player/StageInline.tsx-313-    : height;
app/home/player/StageInline.tsx-314-
app/home/player/StageInline.tsx-315-  const [mounted, setMounted] = React.useState(false);
app/home/player/StageInline.tsx-316-  React.useEffect(() => setMounted(true), []);
app/home/player/StageInline.tsx-317-
app/home/player/StageInline.tsx-318-  const [open, setOpen] = React.useState(false);
--
app/home/player/StageInline.tsx-363-    if (typeof document === "undefined") return;
app/home/player/StageInline.tsx-364-    const el = document.getElementById("af-stage-overlay");
app/home/player/StageInline.tsx-365-    if (!el) return;
app/home/player/StageInline.tsx-366-    const requestFullscreen = (
app/home/player/StageInline.tsx-367-      el as Element & { requestFullscreen?: () => Promise<void> }
app/home/player/StageInline.tsx-368-    ).requestFullscreen;
app/home/player/StageInline.tsx-369-    if (typeof requestFullscreen !== "function") return;
app/home/player/StageInline.tsx-370-    try {
app/home/player/StageInline.tsx-371-      await requestFullscreen.call(el);
app/home/player/StageInline.tsx-372-    } catch {
app/home/player/StageInline.tsx-373-      // ignore
app/home/player/StageInline.tsx-374-    }
app/home/player/StageInline.tsx-375-  }, []);
app/home/player/StageInline.tsx-376-
app/home/player/StageInline.tsx:377:  const nothingPlaying = p.queue.length === 0 && !p.current?.recordingId;
app/home/player/StageInline.tsx-378-
app/home/player/StageInline.tsx-379-  const handleOverlayBackdropMouseDown = React.useCallback(
app/home/player/StageInline.tsx-380-    (e: React.MouseEvent<HTMLDivElement>) => {
app/home/player/StageInline.tsx-381-      if (e.target === e.currentTarget) setOpen(false);
app/home/player/StageInline.tsx-382-    },
app/home/player/StageInline.tsx-383-    [],
app/home/player/StageInline.tsx-384-  );
app/home/player/StageInline.tsx-385-
app/home/player/StageInline.tsx-386-  const handleOverlayClose = React.useCallback(() => {
app/home/player/StageInline.tsx-387-    setOpen(false);
app/home/player/StageInline.tsx-388-  }, []);
app/home/player/StageInline.tsx-389-
app/home/player/StageInline.tsx-390-  const handleOverlayRequestFullscreen = React.useCallback(() => {
app/home/player/StageInline.tsx-391-    void tryRequestFullscreen();
--
app/home/player/StageCore.tsx-3-
app/home/player/StageCore.tsx-4-import React from "react";
app/home/player/StageCore.tsx-5-import { usePlayerActions, usePlayerVisual } from "./PlayerState";
app/home/player/StageCore.tsx-6-import VisualizerCanvas from "./VisualizerCanvas";
app/home/player/StageCore.tsx-7-import LyricsOverlay from "./stage/LyricsOverlay";
app/home/player/StageCore.tsx-8-import type { LyricCue } from "@/lib/types";
app/home/player/StageCore.tsx-9-import { STAGE_TRANSPORT_FOOTER_PX } from "./StageTransportBar";
app/home/player/StageCore.tsx-10-import { mediaSurface } from "./mediaSurface";
app/home/player/StageCore.tsx-11-import { useLyricsSnapshot } from "./lyrics/useLyricsSurface";
app/home/player/StageCore.tsx-12-
app/home/player/StageCore.tsx-13-type CuesByRecordingId = Record<string, LyricCue[]>;
app/home/player/StageCore.tsx-14-type OffsetByRecordingId = Record<string, number>;
app/home/player/StageCore.tsx-15-
app/home/player/StageCore.tsx-16-function pickKeyWithCues(
app/home/player/StageCore.tsx:17:  cuesByRecordingId: CuesByRecordingId | undefined,
app/home/player/StageCore.tsx-18-  keys: Array<string | null | undefined>,
app/home/player/StageCore.tsx-19-) {
app/home/player/StageCore.tsx:20:  if (!cuesByRecordingId) return (keys.find(Boolean) as string | null) ?? null;
app/home/player/StageCore.tsx-21-  for (const k of keys) {
app/home/player/StageCore.tsx-22-    if (!k) continue;
app/home/player/StageCore.tsx:23:    const xs = cuesByRecordingId[k];
app/home/player/StageCore.tsx-24-    if (Array.isArray(xs) && xs.length) return k;
app/home/player/StageCore.tsx-25-  }
app/home/player/StageCore.tsx-26-  return (keys.find(Boolean) as string | null) ?? null;
app/home/player/StageCore.tsx-27-}
app/home/player/StageCore.tsx-28-
app/home/player/StageCore.tsx-29-function StageCore(props: {
app/home/player/StageCore.tsx-30-  variant: "inline" | "fullscreen";
app/home/player/StageCore.tsx:31:  cuesByRecordingId?: CuesByRecordingId;
app/home/player/StageCore.tsx-32-  offsetByRecordingId?: OffsetByRecordingId;
app/home/player/StageCore.tsx:33:  offsetMs?: number;
app/home/player/StageCore.tsx-34-  autoResumeOnSeek?: boolean;
app/home/player/StageCore.tsx-35-  lyricsMode?: "embedded" | "none";
app/home/player/StageCore.tsx-36-}) {
app/home/player/StageCore.tsx-37-  const {
app/home/player/StageCore.tsx-38-    variant,
app/home/player/StageCore.tsx:39:    cuesByRecordingId: cuesByRecordingIdProp,
app/home/player/StageCore.tsx-40-    offsetByRecordingId: offsetByRecordingIdProp,
app/home/player/StageCore.tsx:41:    offsetMs: globalOffsetMs = 0,
app/home/player/StageCore.tsx-42-    autoResumeOnSeek = false,
app/home/player/StageCore.tsx-43-    lyricsMode = "embedded",
app/home/player/StageCore.tsx-44-  } = props;
app/home/player/StageCore.tsx-45-
app/home/player/StageCore.tsx-46-  const player = usePlayerVisual();
app/home/player/StageCore.tsx-47-  const actions = usePlayerActions();
app/home/player/StageCore.tsx-48-  const snap = useLyricsSnapshot();
app/home/player/StageCore.tsx-49-
app/home/player/StageCore.tsx-50-  // ✅ prefer props when provided; otherwise use lyricsSurface
app/home/player/StageCore.tsx:51:  const cuesByRecordingId = cuesByRecordingIdProp ?? snap.cuesByRecordingId;
app/home/player/StageCore.tsx-52-  const offsetByRecordingId =
app/home/player/StageCore.tsx-53-    offsetByRecordingIdProp ?? snap.offsetByRecordingId;
app/home/player/StageCore.tsx-54-
app/home/player/StageCore.tsx-55-  // Register stage presence; fullscreen wins if it exists.
app/home/player/StageCore.tsx-56-  React.useEffect(() => mediaSurface.registerStage(variant), [variant]);
app/home/player/StageCore.tsx-57-
app/home/player/StageCore.tsx-58-  const [surfaceRecordingId, setSurfaceRecordingId] = React.useState<
app/home/player/StageCore.tsx-59-    string | null
app/home/player/StageCore.tsx-60-  >(() => mediaSurface.getRecordingId());
app/home/player/StageCore.tsx-61-
app/home/player/StageCore.tsx-62-  React.useEffect(() => {
app/home/player/StageCore.tsx-63-    const unsub = mediaSurface.subscribe((e) => {
app/home/player/StageCore.tsx-64-      if (e.type === "track") setSurfaceRecordingId(e.id);
app/home/player/StageCore.tsx-65-    });
app/home/player/StageCore.tsx-66-    return unsub;
app/home/player/StageCore.tsx-67-  }, []);
app/home/player/StageCore.tsx-68-
app/home/player/StageCore.tsx:69:  const playerRecordingId = player.current?.recordingId ?? null;
app/home/player/StageCore.tsx-70-  const playerDisplayId = player.current?.displayId ?? null;
app/home/player/StageCore.tsx-71-  const playerMuxId = player.current?.muxPlaybackId ?? null;
app/home/player/StageCore.tsx-72-
app/home/player/StageCore.tsx:73:  const recordingId = pickKeyWithCues(cuesByRecordingId, [
app/home/player/StageCore.tsx-74-    playerRecordingId,
app/home/player/StageCore.tsx-75-    surfaceRecordingId,
app/home/player/StageCore.tsx-76-    playerMuxId,
app/home/player/StageCore.tsx-77-  ]);
app/home/player/StageCore.tsx-78-
app/home/player/StageCore.tsx-79-  const displayId = playerDisplayId;
app/home/player/StageCore.tsx-80-
app/home/player/StageCore.tsx:81:  const cues: LyricCue[] | null = recordingId
app/home/player/StageCore.tsx:82:    ? Array.isArray(cuesByRecordingId?.[recordingId]) &&
app/home/player/StageCore.tsx:83:      cuesByRecordingId[recordingId].length
app/home/player/StageCore.tsx:84:      ? cuesByRecordingId[recordingId]
app/home/player/StageCore.tsx-85-      : null
app/home/player/StageCore.tsx-86-    : null;
app/home/player/StageCore.tsx-87-
app/home/player/StageCore.tsx-88-  const trackOffsetMs =
app/home/player/StageCore.tsx:89:    recordingId && typeof offsetByRecordingId?.[recordingId] === "number"
app/home/player/StageCore.tsx:90:      ? Number.isFinite(offsetByRecordingId[recordingId])
app/home/player/StageCore.tsx:91:        ? offsetByRecordingId[recordingId]
app/home/player/StageCore.tsx-92-        : 0
app/home/player/StageCore.tsx-93-      : 0;
app/home/player/StageCore.tsx-94-
app/home/player/StageCore.tsx-95-  const effectiveOffsetMs = trackOffsetMs + globalOffsetMs;
app/home/player/StageCore.tsx-96-  const resumeTrack = player.current ?? player.firstQueuedTrack;
app/home/player/StageCore.tsx-97-
app/home/player/StageCore.tsx-98-  const onSeek = React.useCallback(
app/home/player/StageCore.tsx-99-    (tMs: number) => {
app/home/player/StageCore.tsx-100-      const ms = Math.max(0, Math.floor(tMs));
app/home/player/StageCore.tsx-101-      actions.seek(ms);
app/home/player/StageCore.tsx-102-
app/home/player/StageCore.tsx-103-      if (autoResumeOnSeek) {
app/home/player/StageCore.tsx-104-        if (!resumeTrack) return;
app/home/player/StageCore.tsx-105-        actions.setIntent("play");
--
app/home/player/StageCore.tsx-134-          position: "absolute",
app/home/player/StageCore.tsx-135-          inset: 0,
app/home/player/StageCore.tsx-136-          zIndex: 1,
app/home/player/StageCore.tsx-137-          background:
app/home/player/StageCore.tsx-138-            variant === "inline"
app/home/player/StageCore.tsx-139-              ? "radial-gradient(70% 55% at 50% 40%, rgba(0,0,0,0.00), rgba(0,0,0,0.22) 72%), linear-gradient(180deg, rgba(0,0,0,0.14), rgba(0,0,0,0.06) 45%, rgba(0,0,0,0.16))"
app/home/player/StageCore.tsx-140-              : "radial-gradient(70% 55% at 50% 40%, rgba(0,0,0,0.00), rgba(0,0,0,0.28) 72%), linear-gradient(180deg, rgba(0,0,0,0.22), rgba(0,0,0,0.10) 45%, rgba(0,0,0,0.24))",
app/home/player/StageCore.tsx-141-          pointerEvents: "none",
app/home/player/StageCore.tsx-142-        }}
app/home/player/StageCore.tsx-143-      />
app/home/player/StageCore.tsx-144-
app/home/player/StageCore.tsx-145-      <div style={{ position: "absolute", inset: 0, zIndex: 2 }}>
app/home/player/StageCore.tsx-146-        {lyricsMode === "embedded" ? (
app/home/player/StageCore.tsx-147-          <LyricsOverlay
app/home/player/StageCore.tsx:148:            recordingId={recordingId}
app/home/player/StageCore.tsx-149-            displayId={displayId}
app/home/player/StageCore.tsx:150:            cues={cues}
app/home/player/StageCore.tsx:151:            offsetMs={effectiveOffsetMs}
app/home/player/StageCore.tsx-152-            onSeek={onSeek}
app/home/player/StageCore.tsx-153-            variant={lyricsVariant}
app/home/player/StageCore.tsx-154-            reservedBottomPx={reservedBottomPx}
app/home/player/StageCore.tsx-155-          />
app/home/player/StageCore.tsx-156-        ) : null}
app/home/player/StageCore.tsx-157-      </div>
app/home/player/StageCore.tsx-158-    </div>
app/home/player/StageCore.tsx-159-  );
app/home/player/StageCore.tsx-160-}
app/home/player/StageCore.tsx-161-
app/home/player/StageCore.tsx-162-export default React.memo(StageCore);
--
app/home/player/FullPlayer.tsx-512-  return (
app/home/player/FullPlayer.tsx-513-    player.playRequestPending ||
app/home/player/FullPlayer.tsx-514-    player.intent === "play" ||
app/home/player/FullPlayer.tsx-515-    player.status === "loading" ||
app/home/player/FullPlayer.tsx-516-    player.status === "playing" ||
app/home/player/FullPlayer.tsx-517-    player.status === "paused" ||
app/home/player/FullPlayer.tsx-518-    pendingInAlbum
app/home/player/FullPlayer.tsx-519-  );
app/home/player/FullPlayer.tsx-520-}
app/home/player/FullPlayer.tsx-521-
app/home/player/FullPlayer.tsx-522-function reconcilePlayerWithAccessDecision(
app/home/player/FullPlayer.tsx-523-  params: Readonly<{
app/home/player/FullPlayer.tsx-524-    player: PlayerApi;
app/home/player/FullPlayer.tsx-525-    decision: AccessState;
app/home/player/FullPlayer.tsx:526:    recordingIds: ReadonlySet<string>;
app/home/player/FullPlayer.tsx-527-    albumKey: string | null;
app/home/player/FullPlayer.tsx-528-  }>,
app/home/player/FullPlayer.tsx-529-): void {
app/home/player/FullPlayer.tsx:530:  const { player, decision, recordingIds, albumKey } = params;
app/home/player/FullPlayer.tsx-531-
app/home/player/FullPlayer.tsx-532-  if (decision.allowed) {
app/home/player/FullPlayer.tsx-533-    if (player.lastError) player.clearError();
app/home/player/FullPlayer.tsx-534-    return;
app/home/player/FullPlayer.tsx-535-  }
app/home/player/FullPlayer.tsx-536-
app/home/player/FullPlayer.tsx-537-  const currentInAlbum = Boolean(
app/home/player/FullPlayer.tsx:538:    player.current?.recordingId && recordingIds.has(player.current.recordingId),
app/home/player/FullPlayer.tsx-539-  );
app/home/player/FullPlayer.tsx-540-  const pendingInAlbum = Boolean(
app/home/player/FullPlayer.tsx:541:    player.pendingRecordingId && recordingIds.has(player.pendingRecordingId),
app/home/player/FullPlayer.tsx-542-  );
app/home/player/FullPlayer.tsx-543-  const queueIsAlbum = Boolean(albumKey && player.queueContextId === albumKey);
app/home/player/FullPlayer.tsx-544-  const hasIntent = hasPlaybackIntentForAlbum(player, pendingInAlbum);
app/home/player/FullPlayer.tsx-545-  const shouldBlockAlbum =
app/home/player/FullPlayer.tsx-546-    currentInAlbum || pendingInAlbum || (queueIsAlbum && hasIntent);
app/home/player/FullPlayer.tsx-547-
app/home/player/FullPlayer.tsx-548-  const code = typeof decision.code === "string" ? decision.code.trim() : "";
app/home/player/FullPlayer.tsx-549-  if (!shouldBlockAlbum || (isAuthOrLegacyCapCode(code) && !hasIntent)) return;
app/home/player/FullPlayer.tsx-550-
app/home/player/FullPlayer.tsx-551-  try {
app/home/player/FullPlayer.tsx-552-    player.pause();
app/home/player/FullPlayer.tsx-553-  } catch {
app/home/player/FullPlayer.tsx-554-    // ignore
app/home/player/FullPlayer.tsx-555-  }
--
app/home/player/FullPlayer.tsx-666-  albumIdx: number;
app/home/player/FullPlayer.tsx-667-  prevDisabled: boolean;
app/home/player/FullPlayer.tsx-668-  nextDisabled: boolean;
app/home/player/FullPlayer.tsx-669-}>;
app/home/player/FullPlayer.tsx-670-
app/home/player/FullPlayer.tsx-671-function deriveAlbumTransportState(
app/home/player/FullPlayer.tsx-672-  params: Readonly<{
app/home/player/FullPlayer.tsx-673-    currentRecordingId: string;
app/home/player/FullPlayer.tsx-674-    tracks: readonly PlayerTrack[];
app/home/player/FullPlayer.tsx-675-    transportLock: boolean;
app/home/player/FullPlayer.tsx-676-  }>,
app/home/player/FullPlayer.tsx-677-): AlbumTransportState {
app/home/player/FullPlayer.tsx-678-  const albumIdx = params.currentRecordingId
app/home/player/FullPlayer.tsx-679-    ? params.tracks.findIndex(
app/home/player/FullPlayer.tsx:680:        (track) => track.recordingId === params.currentRecordingId,
app/home/player/FullPlayer.tsx-681-      )
app/home/player/FullPlayer.tsx-682-    : -1;
app/home/player/FullPlayer.tsx-683-
app/home/player/FullPlayer.tsx-684-  if (albumIdx < 0) {
app/home/player/FullPlayer.tsx-685-    return { albumIdx, prevDisabled: true, nextDisabled: true };
app/home/player/FullPlayer.tsx-686-  }
app/home/player/FullPlayer.tsx-687-
app/home/player/FullPlayer.tsx-688-  return {
app/home/player/FullPlayer.tsx-689-    albumIdx,
app/home/player/FullPlayer.tsx-690-    prevDisabled: params.transportLock || albumIdx === 0,
app/home/player/FullPlayer.tsx-691-    nextDisabled: params.transportLock || albumIdx === params.tracks.length - 1,
app/home/player/FullPlayer.tsx-692-  };
app/home/player/FullPlayer.tsx-693-}
app/home/player/FullPlayer.tsx-694-
--
app/home/player/FullPlayer.tsx-749-  params: Readonly<{
app/home/player/FullPlayer.tsx-750-    track: PlayerTrack;
app/home/player/FullPlayer.tsx-751-    index: number;
app/home/player/FullPlayer.tsx-752-    trackCount: number;
app/home/player/FullPlayer.tsx-753-    currentRecordingId: string | undefined;
app/home/player/FullPlayer.tsx-754-    pendingRecordingId: string | undefined;
app/home/player/FullPlayer.tsx-755-    playerStatus: PlayerApi["status"];
app/home/player/FullPlayer.tsx-756-    playerIntent: PlayerApi["intent"];
app/home/player/FullPlayer.tsx-757-    loadingReason: PlayerApi["loadingReason"];
app/home/player/FullPlayer.tsx-758-    canPlay: boolean;
app/home/player/FullPlayer.tsx-759-    canAttemptPlay: boolean;
app/home/player/FullPlayer.tsx-760-    isSelected: boolean;
app/home/player/FullPlayer.tsx-761-  }>,
app/home/player/FullPlayer.tsx-762-): TrackRowView {
app/home/player/FullPlayer.tsx:763:  const isCurrent = params.currentRecordingId === params.track.recordingId;
app/home/player/FullPlayer.tsx:764:  const isPending = params.pendingRecordingId === params.track.recordingId;
app/home/player/FullPlayer.tsx-765-  const shimmerTitle =
app/home/player/FullPlayer.tsx-766-    params.playerStatus === "loading" && isPending && !isCurrent;
app/home/player/FullPlayer.tsx-767-  const isNowPlaying = Boolean(
app/home/player/FullPlayer.tsx-768-    isCurrent &&
app/home/player/FullPlayer.tsx-769-    (params.playerStatus === "playing" ||
app/home/player/FullPlayer.tsx-770-      params.playerStatus === "loading" ||
app/home/player/FullPlayer.tsx-771-      params.playerIntent === "play"),
app/home/player/FullPlayer.tsx-772-  );
app/home/player/FullPlayer.tsx-773-  const { titleColor, subColor } = trackRowColors(params.canPlay, isCurrent);
app/home/player/FullPlayer.tsx-774-  const baseBackground = params.isSelected
app/home/player/FullPlayer.tsx-775-    ? "rgba(255,255,255,0.14)"
app/home/player/FullPlayer.tsx-776-    : "transparent";
app/home/player/FullPlayer.tsx-777-  const restingBackground =
app/home/player/FullPlayer.tsx-778-    isCurrent && !params.isSelected ? "transparent" : baseBackground;
--
app/home/player/FullPlayer.tsx-1269-
app/home/player/FullPlayer.tsx-1270-  const {
app/home/player/FullPlayer.tsx-1271-    albumSlug: effAlbumSlug,
app/home/player/FullPlayer.tsx-1272-    album: effAlbum,
app/home/player/FullPlayer.tsx-1273-    tracks: effTracks,
app/home/player/FullPlayer.tsx-1274-  } = resolveEffectivePlayerView({
app/home/player/FullPlayer.tsx-1275-    bundle,
app/home/player/FullPlayer.tsx-1276-    stableView,
app/home/player/FullPlayer.tsx-1277-    isBrowsingAlbum,
app/home/player/FullPlayer.tsx-1278-  });
app/home/player/FullPlayer.tsx-1279-  // Track membership ref (lets access-check logic test membership without re-triggering fetch)
app/home/player/FullPlayer.tsx-1280-  const effRecordingIdSetRef = React.useRef<Set<string>>(new Set());
app/home/player/FullPlayer.tsx-1281-
app/home/player/FullPlayer.tsx-1282-  React.useEffect(() => {
app/home/player/FullPlayer.tsx:1283:    effRecordingIdSetRef.current = new Set(effTracks.map((t) => t.recordingId));
app/home/player/FullPlayer.tsx-1284-  }, [effTracks]);
app/home/player/FullPlayer.tsx-1285-
app/home/player/FullPlayer.tsx-1286-  const router = useRouter();
app/home/player/FullPlayer.tsx-1287-  const pathname = usePathname();
app/home/player/FullPlayer.tsx-1288-
app/home/player/FullPlayer.tsx-1289-  const route = React.useMemo(() => parsePublicAlbumPath(pathname), [pathname]);
app/home/player/FullPlayer.tsx-1290-  const isPublicAlbumRoute = Boolean(route.albumSlug);
app/home/player/FullPlayer.tsx-1291-
app/home/player/FullPlayer.tsx-1292-  const goCanonicalTrack = React.useCallback(
app/home/player/FullPlayer.tsx-1293-    (displayId: string | null, mode: "push" | "replace" = "push") => {
app/home/player/FullPlayer.tsx-1294-      if (!isPublicAlbumRoute) return;
app/home/player/FullPlayer.tsx-1295-
app/home/player/FullPlayer.tsx-1296-      const qs = canonicalCarryQuery();
app/home/player/FullPlayer.tsx-1297-      const base = `/${encodeURIComponent(effAlbumSlug)}`;
--
app/home/player/FullPlayer.tsx-1396-  const albumDesc =
app/home/player/FullPlayer.tsx-1397-    effAlbum?.description ??
app/home/player/FullPlayer.tsx-1398-    "This is placeholder copy. Soon: pull album description from Sanity.";
app/home/player/FullPlayer.tsx-1399-
app/home/player/FullPlayer.tsx-1400-  // ---- browser tab title (client-side) ----
app/home/player/FullPlayer.tsx-1401-  // We set this here because in portal surfaces we may not actually navigate routes
app/home/player/FullPlayer.tsx-1402-  // (and therefore server metadata won't rerun).
app/home/player/FullPlayer.tsx-1403-  React.useEffect(() => {
app/home/player/FullPlayer.tsx-1404-    if (typeof document === "undefined") return;
app/home/player/FullPlayer.tsx-1405-
app/home/player/FullPlayer.tsx-1406-    const prev = document.title;
app/home/player/FullPlayer.tsx-1407-
app/home/player/FullPlayer.tsx-1408-    const cur = p.current;
app/home/player/FullPlayer.tsx-1409-    const curInThisAlbum = Boolean(
app/home/player/FullPlayer.tsx:1410:      cur?.recordingId &&
app/home/player/FullPlayer.tsx:1411:      effTracks.some((t) => t.recordingId === cur.recordingId),
app/home/player/FullPlayer.tsx-1412-    );
app/home/player/FullPlayer.tsx-1413-    const trackTitle = curInThisAlbum
app/home/player/FullPlayer.tsx:1414:      ? cur?.title?.trim() || cur?.recordingId || ""
app/home/player/FullPlayer.tsx-1415-      : "";
app/home/player/FullPlayer.tsx-1416-
app/home/player/FullPlayer.tsx-1417-    const aTitle = (effAlbum?.title?.trim() || "").trim();
app/home/player/FullPlayer.tsx-1418-    const artist = (effAlbum?.artist?.trim() || "").trim();
app/home/player/FullPlayer.tsx-1419-
app/home/player/FullPlayer.tsx-1420-    // Format: "Track · Album · Artist" (falls back gracefully)
app/home/player/FullPlayer.tsx-1421-    const next = trackTitle
app/home/player/FullPlayer.tsx-1422-      ? [trackTitle, aTitle, artist].filter(Boolean).join(" · ")
app/home/player/FullPlayer.tsx-1423-      : [aTitle, artist].filter(Boolean).join(" · ");
app/home/player/FullPlayer.tsx-1424-
app/home/player/FullPlayer.tsx-1425-    if (next) document.title = next;
app/home/player/FullPlayer.tsx-1426-
app/home/player/FullPlayer.tsx-1427-    return () => {
app/home/player/FullPlayer.tsx-1428-      // Don't leave a stale player title behind if the component unmounts
app/home/player/FullPlayer.tsx-1429-      document.title = prev;
app/home/player/FullPlayer.tsx-1430-    };
app/home/player/FullPlayer.tsx-1431-    // Intentionally depend on the minimal fields that affect the title.
app/home/player/FullPlayer.tsx-1432-  }, [
app/home/player/FullPlayer.tsx:1433:    p.current?.recordingId,
app/home/player/FullPlayer.tsx-1434-    p.current?.title,
app/home/player/FullPlayer.tsx-1435-    effAlbum?.title,
app/home/player/FullPlayer.tsx-1436-    effAlbum?.artist,
app/home/player/FullPlayer.tsx-1437-    effTracks,
app/home/player/FullPlayer.tsx-1438-    p,
app/home/player/FullPlayer.tsx-1439-  ]);
app/home/player/FullPlayer.tsx-1440-
app/home/player/FullPlayer.tsx-1441-  const platformLinks = React.useMemo(
app/home/player/FullPlayer.tsx-1442-    () => normalizePlatformLinks(effAlbum?.platformLinks),
app/home/player/FullPlayer.tsx-1443-    [effAlbum?.platformLinks],
app/home/player/FullPlayer.tsx-1444-  );
app/home/player/FullPlayer.tsx-1445-
app/home/player/FullPlayer.tsx-1446-  const browseAlbums = albums.filter((a) => a.id !== effAlbum?.id);
app/home/player/FullPlayer.tsx-1447-
--
app/home/player/FullPlayer.tsx-1535-    // authenticated session while its fresh access request is in flight.
app/home/player/FullPlayer.tsx-1536-    setAccess(null);
app/home/player/FullPlayer.tsx-1537-
app/home/player/FullPlayer.tsx-1538-    const cached = getCachedPlaybackAccessDecision(accessRequest);
app/home/player/FullPlayer.tsx-1539-    setAccess((previous) => mergeAccessState(previous, cached));
app/home/player/FullPlayer.tsx-1540-
app/home/player/FullPlayer.tsx-1541-    void fetchPlaybackAccessDecision(accessRequest)
app/home/player/FullPlayer.tsx-1542-      .then((next) => {
app/home/player/FullPlayer.tsx-1543-        if (!alive) return;
app/home/player/FullPlayer.tsx-1544-
app/home/player/FullPlayer.tsx-1545-        setAccess((previous) => mergeAccessState(previous, next));
app/home/player/FullPlayer.tsx-1546-        reconcilePlayerWithAccessDecision({
app/home/player/FullPlayer.tsx-1547-          player: pRef.current,
app/home/player/FullPlayer.tsx-1548-          decision: next,
app/home/player/FullPlayer.tsx:1549:          recordingIds: effRecordingIdSetRef.current,
app/home/player/FullPlayer.tsx-1550-          albumKey,
app/home/player/FullPlayer.tsx-1551-        });
app/home/player/FullPlayer.tsx-1552-      })
app/home/player/FullPlayer.tsx-1553-      .catch((error: unknown) => {
app/home/player/FullPlayer.tsx-1554-        if (!alive) return;
app/home/player/FullPlayer.tsx-1555-        console.error("FullPlayer access check failed", error);
app/home/player/FullPlayer.tsx-1556-
app/home/player/FullPlayer.tsx-1557-        // Presentation may remain available after a transient request failure,
app/home/player/FullPlayer.tsx-1558-        // but do not cache this fallback in the shared decision store.
app/home/player/FullPlayer.tsx-1559-        // PlayerState must still fail closed if real play authorisation fails.
app/home/player/FullPlayer.tsx-1560-        setAccess(accessFailureFallback(catalogueId));
app/home/player/FullPlayer.tsx-1561-
app/home/player/FullPlayer.tsx-1562-        const player = pRef.current;
app/home/player/FullPlayer.tsx-1563-        if (player.lastError) player.clearError();
--
app/home/player/FullPlayer.tsx-1668-    goCanonicalTrack(track.displayId, options.navigationMode);
app/home/player/FullPlayer.tsx-1669-    p.play(track);
app/home/player/FullPlayer.tsx-1670-    window.dispatchEvent(new Event("af:play-intent"));
app/home/player/FullPlayer.tsx-1671-  };
app/home/player/FullPlayer.tsx-1672-
app/home/player/FullPlayer.tsx-1673-  const onTogglePlay = () => {
app/home/player/FullPlayer.tsx-1674-    lockPlayFor(120);
app/home/player/FullPlayer.tsx-1675-
app/home/player/FullPlayer.tsx-1676-    if (isPlaybackGate) {
app/home/player/FullPlayer.tsx-1677-      openPlaybackGate();
app/home/player/FullPlayer.tsx-1678-      return;
app/home/player/FullPlayer.tsx-1679-    }
app/home/player/FullPlayer.tsx-1680-    if (!canPlay) return;
app/home/player/FullPlayer.tsx-1681-
app/home/player/FullPlayer.tsx:1682:    const currentRecordingId = p.current?.recordingId ?? "";
app/home/player/FullPlayer.tsx-1683-    const currentTrackInThisAlbum = isThisAlbumActive
app/home/player/FullPlayer.tsx:1684:      ? (effTracks.find((track) => track.recordingId === currentRecordingId) ??
app/home/player/FullPlayer.tsx-1685-        null)
app/home/player/FullPlayer.tsx-1686-      : null;
app/home/player/FullPlayer.tsx-1687-    const routeTargetsThisAlbum =
app/home/player/FullPlayer.tsx-1688-      isPublicAlbumRoute && route.albumSlug === effAlbumSlug;
app/home/player/FullPlayer.tsx-1689-    const directRouteTrack = routeTargetsThisAlbum
app/home/player/FullPlayer.tsx-1690-      ? (effTracks.find((track) => track.displayId === route.displayId) ?? null)
app/home/player/FullPlayer.tsx-1691-      : null;
app/home/player/FullPlayer.tsx-1692-    const directRouteTargetsAnotherTrack = Boolean(
app/home/player/FullPlayer.tsx-1693-      directRouteTrack &&
app/home/player/FullPlayer.tsx:1694:      directRouteTrack.recordingId !== currentTrackInThisAlbum?.recordingId,
app/home/player/FullPlayer.tsx-1695-    );
app/home/player/FullPlayer.tsx-1696-
app/home/player/FullPlayer.tsx-1697-    // A generic album page controls its existing same-album transport natively.
app/home/player/FullPlayer.tsx-1698-    // A direct track URL is more specific: it deliberately selects that track.
app/home/player/FullPlayer.tsx-1699-    if (playingThisAlbum && !directRouteTargetsAnotherTrack) {
app/home/player/FullPlayer.tsx-1700-      window.dispatchEvent(new Event("af:pause-intent"));
app/home/player/FullPlayer.tsx-1701-      p.pause();
app/home/player/FullPlayer.tsx-1702-      return;
app/home/player/FullPlayer.tsx-1703-    }
app/home/player/FullPlayer.tsx-1704-
app/home/player/FullPlayer.tsx-1705-    if (directRouteTrack && directRouteTargetsAnotherTrack) {
app/home/player/FullPlayer.tsx-1706-      playTrackWithIntent(directRouteTrack, {
app/home/player/FullPlayer.tsx-1707-        ensureQueue: true,
app/home/player/FullPlayer.tsx-1708-        navigationMode: "replace",
--
app/home/player/FullPlayer.tsx-1721-    }
app/home/player/FullPlayer.tsx-1722-
app/home/player/FullPlayer.tsx-1723-    // A different album, or an album with no recoverable current track:
app/home/player/FullPlayer.tsx-1724-    // intentionally establish its queue and begin at its first track.
app/home/player/FullPlayer.tsx-1725-    const firstTrack = effTracks[0];
app/home/player/FullPlayer.tsx-1726-    if (!firstTrack) return;
app/home/player/FullPlayer.tsx-1727-
app/home/player/FullPlayer.tsx-1728-    playTrackWithIntent(firstTrack, {
app/home/player/FullPlayer.tsx-1729-      ensureQueue: true,
app/home/player/FullPlayer.tsx-1730-      navigationMode: "replace",
app/home/player/FullPlayer.tsx-1731-    });
app/home/player/FullPlayer.tsx-1732-  };
app/home/player/FullPlayer.tsx-1733-
app/home/player/FullPlayer.tsx-1734-  const getDurMs = (t: PlayerTrack) =>
app/home/player/FullPlayer.tsx:1735:    p.durationByRecordingId?.[t.recordingId] ?? t.durationMs;
app/home/player/FullPlayer.tsx-1736-  const renderDur = (t: PlayerTrack) => {
app/home/player/FullPlayer.tsx-1737-    const ms = getDurMs(t) ?? 0;
app/home/player/FullPlayer.tsx-1738-    return ms > 0 ? fmtTime(ms) : "—";
app/home/player/FullPlayer.tsx-1739-  };
app/home/player/FullPlayer.tsx-1740-
app/home/player/FullPlayer.tsx-1741-  const shareCtx = deriveShareContext({
app/home/player/FullPlayer.tsx-1742-    albumSlug: effAlbumSlug,
app/home/player/FullPlayer.tsx-1743-    album: effAlbum,
app/home/player/FullPlayer.tsx-1744-    queueArtist: p.queueContextArtist,
app/home/player/FullPlayer.tsx-1745-    albumId: albumKey ?? undefined,
app/home/player/FullPlayer.tsx-1746-  });
app/home/player/FullPlayer.tsx-1747-
app/home/player/FullPlayer.tsx-1748-  const [selectedRecordingId, setSelectedRecordingId] = React.useState<
app/home/player/FullPlayer.tsx-1749-    string | null
app/home/player/FullPlayer.tsx-1750-  >(null);
app/home/player/FullPlayer.tsx-1751-
app/home/player/FullPlayer.tsx-1752-  const isCoarsePointer = isCoarsePointerDevice();
app/home/player/FullPlayer.tsx-1753-
app/home/player/FullPlayer.tsx-1754-  // --- Album-local transport (FullPlayer) ---
app/home/player/FullPlayer.tsx-1755-  // Only operate within effTracks. Never call p.prev()/p.next() here.
app/home/player/FullPlayer.tsx-1756-
app/home/player/FullPlayer.tsx:1757:  const curId = p.current?.recordingId ?? "";
app/home/player/FullPlayer.tsx-1758-  const { albumIdx, prevDisabled, nextDisabled } = deriveAlbumTransportState({
app/home/player/FullPlayer.tsx-1759-    currentRecordingId: curId,
app/home/player/FullPlayer.tsx-1760-    tracks: effTracks,
app/home/player/FullPlayer.tsx-1761-    transportLock,
app/home/player/FullPlayer.tsx-1762-  });
app/home/player/FullPlayer.tsx-1763-
app/home/player/FullPlayer.tsx-1764-  function ensureAlbumQueue() {
app/home/player/FullPlayer.tsx-1765-    // Explicit nulls prevent an earlier private-link attribution context from
app/home/player/FullPlayer.tsx-1766-    // surviving when ordinary album playback replaces the queue.
app/home/player/FullPlayer.tsx-1767-    p.setQueue(effTracks, {
app/home/player/FullPlayer.tsx-1768-      contextId: albumKey ?? undefined,
app/home/player/FullPlayer.tsx-1769-      artworkUrl: effAlbum?.artworkUrl ?? null,
app/home/player/FullPlayer.tsx-1770-      contextSlug: effAlbumSlug,
app/home/player/FullPlayer.tsx-1771-      contextTitle: effAlbum?.title ?? undefined,
--
app/home/player/FullPlayer.tsx-1792-
app/home/player/FullPlayer.tsx-1793-    const initialTrack = directTrack ?? effTracks[0] ?? null;
app/home/player/FullPlayer.tsx-1794-
app/home/player/FullPlayer.tsx-1795-    if (!initialTrack) return;
app/home/player/FullPlayer.tsx-1796-
app/home/player/FullPlayer.tsx-1797-    // Prepare a fresh player around the route's target track—or track one on a
app/home/player/FullPlayer.tsx-1798-    // generic album route—without beginning playback. Never overwrite an
app/home/player/FullPlayer.tsx-1799-    // existing session, including a paused or playing different album.
app/home/player/FullPlayer.tsx-1800-    const hasExistingPlayerSession = Boolean(
app/home/player/FullPlayer.tsx-1801-      p.current || p.queue.length > 0 || p.pendingRecordingId,
app/home/player/FullPlayer.tsx-1802-    );
app/home/player/FullPlayer.tsx-1803-
app/home/player/FullPlayer.tsx-1804-    if (hasExistingPlayerSession) return;
app/home/player/FullPlayer.tsx-1805-
app/home/player/FullPlayer.tsx:1806:    const queueKey = `${albumKey}:${initialTrack.recordingId}`;
app/home/player/FullPlayer.tsx-1807-
app/home/player/FullPlayer.tsx-1808-    if (preparedInitialQueueRef.current === queueKey) return;
app/home/player/FullPlayer.tsx-1809-
app/home/player/FullPlayer.tsx-1810-    preparedInitialQueueRef.current = queueKey;
app/home/player/FullPlayer.tsx-1811-
app/home/player/FullPlayer.tsx-1812-    p.setQueue(effTracks, {
app/home/player/FullPlayer.tsx-1813-      contextId: albumKey,
app/home/player/FullPlayer.tsx-1814-      artworkUrl: effAlbum?.artworkUrl ?? null,
app/home/player/FullPlayer.tsx-1815-      contextSlug: effAlbumSlug,
app/home/player/FullPlayer.tsx-1816-      contextTitle: effAlbum?.title ?? undefined,
app/home/player/FullPlayer.tsx-1817-      contextArtist: effAlbum?.artist ?? undefined,
app/home/player/FullPlayer.tsx-1818-      sharePlaybackContext: queueSharePlaybackContext,
app/home/player/FullPlayer.tsx-1819-      sharePlaybackScopeId: queueSharePlaybackScopeId,
app/home/player/FullPlayer.tsx:1820:      initialCurrentRecordingId: initialTrack.recordingId,
app/home/player/FullPlayer.tsx-1821-    });
app/home/player/FullPlayer.tsx-1822-  }, [
app/home/player/FullPlayer.tsx-1823-    accessKnown,
app/home/player/FullPlayer.tsx-1824-    albumKey,
app/home/player/FullPlayer.tsx-1825-    effAlbum?.artworkUrl,
app/home/player/FullPlayer.tsx-1826-    effAlbum?.artist,
app/home/player/FullPlayer.tsx-1827-    effAlbum?.title,
app/home/player/FullPlayer.tsx-1828-    effAlbumSlug,
app/home/player/FullPlayer.tsx-1829-    effTracks,
app/home/player/FullPlayer.tsx-1830-    isPublicAlbumRoute,
app/home/player/FullPlayer.tsx-1831-    p,
app/home/player/FullPlayer.tsx-1832-    queueSharePlaybackContext,
app/home/player/FullPlayer.tsx-1833-    queueSharePlaybackScopeId,
app/home/player/FullPlayer.tsx-1834-    route.albumSlug,
--
app/home/player/FullPlayer.tsx-1860-      return;
app/home/player/FullPlayer.tsx-1861-    }
app/home/player/FullPlayer.tsx-1862-    if (!canPlay) return;
app/home/player/FullPlayer.tsx-1863-
app/home/player/FullPlayer.tsx-1864-    ensureAlbumQueue();
app/home/player/FullPlayer.tsx-1865-    goCanonicalTrack(track.displayId, "push");
app/home/player/FullPlayer.tsx-1866-
app/home/player/FullPlayer.tsx-1867-    if (isCoarsePointer) {
app/home/player/FullPlayer.tsx-1868-      prefetchAlbumSession(track);
app/home/player/FullPlayer.tsx-1869-      p.play(track);
app/home/player/FullPlayer.tsx-1870-      window.dispatchEvent(new Event("af:play-intent"));
app/home/player/FullPlayer.tsx-1871-      return;
app/home/player/FullPlayer.tsx-1872-    }
app/home/player/FullPlayer.tsx-1873-
app/home/player/FullPlayer.tsx:1874:    setSelectedRecordingId(track.recordingId);
app/home/player/FullPlayer.tsx-1875-  };
app/home/player/FullPlayer.tsx-1876-
app/home/player/FullPlayer.tsx-1877-  const doubleActivateTrackRow = (track: PlayerTrack) => {
app/home/player/FullPlayer.tsx-1878-    if (isCoarsePointer) return;
app/home/player/FullPlayer.tsx-1879-    if (isPlaybackGate) {
app/home/player/FullPlayer.tsx-1880-      openPlaybackGate(track);
app/home/player/FullPlayer.tsx-1881-      return;
app/home/player/FullPlayer.tsx-1882-    }
app/home/player/FullPlayer.tsx-1883-    if (!canPlay) return;
app/home/player/FullPlayer.tsx-1884-
app/home/player/FullPlayer.tsx-1885-    goCanonicalTrack(track.displayId, "push");
app/home/player/FullPlayer.tsx-1886-    prefetchAlbumSession(track);
app/home/player/FullPlayer.tsx-1887-    p.play(track);
app/home/player/FullPlayer.tsx-1888-    window.dispatchEvent(new Event("af:play-intent"));
--
app/home/player/FullPlayer.tsx-1980-          onShare={() => {
app/home/player/FullPlayer.tsx-1981-            void shareAlbum(shareCtx);
app/home/player/FullPlayer.tsx-1982-          }}
app/home/player/FullPlayer.tsx-1983-        />
app/home/player/FullPlayer.tsx-1984-      </div>
app/home/player/FullPlayer.tsx-1985-
app/home/player/FullPlayer.tsx-1986-      <div style={{ marginTop: 18, padding: "0 0 18px" }}>
app/home/player/FullPlayer.tsx-1987-        <div
app/home/player/FullPlayer.tsx-1988-          style={{
app/home/player/FullPlayer.tsx-1989-            borderTop: "1px solid rgba(255,255,255,0.10)",
app/home/player/FullPlayer.tsx-1990-            paddingTop: 14,
app/home/player/FullPlayer.tsx-1991-          }}
app/home/player/FullPlayer.tsx-1992-        >
app/home/player/FullPlayer.tsx-1993-          {effTracks.map((t, i) => {
app/home/player/FullPlayer.tsx:1994:            const isSelected = selectedRecordingId === t.recordingId;
app/home/player/FullPlayer.tsx-1995-            const view = deriveTrackRowView({
app/home/player/FullPlayer.tsx-1996-              track: t,
app/home/player/FullPlayer.tsx-1997-              index: i,
app/home/player/FullPlayer.tsx-1998-              trackCount: effTracks.length,
app/home/player/FullPlayer.tsx:1999:              currentRecordingId: p.current?.recordingId,
app/home/player/FullPlayer.tsx-2000-              pendingRecordingId: p.pendingRecordingId,
app/home/player/FullPlayer.tsx-2001-              playerStatus: p.status,
app/home/player/FullPlayer.tsx-2002-              playerIntent: p.intent,
app/home/player/FullPlayer.tsx-2003-              loadingReason: p.loadingReason,
app/home/player/FullPlayer.tsx-2004-              canPlay,
app/home/player/FullPlayer.tsx-2005-              canAttemptPlay,
app/home/player/FullPlayer.tsx-2006-              isSelected,
app/home/player/FullPlayer.tsx-2007-            });
app/home/player/FullPlayer.tsx-2008-
app/home/player/FullPlayer.tsx-2009-            return (
app/home/player/FullPlayer.tsx-2010-              <div
app/home/player/FullPlayer.tsx:2011:                key={t.recordingId}
app/home/player/FullPlayer.tsx-2012-                className="afTrackRow"
app/home/player/FullPlayer.tsx-2013-                onMouseEnter={(event) => {
app/home/player/FullPlayer.tsx-2014-                  if (!canAttemptPlay) return;
app/home/player/FullPlayer.tsx-2015-                  if (!isCoarsePointer && !isSelected) {
app/home/player/FullPlayer.tsx-2016-                    event.currentTarget.style.background =
app/home/player/FullPlayer.tsx-2017-                      "rgba(255,255,255,0.08)";
app/home/player/FullPlayer.tsx-2018-                  }
app/home/player/FullPlayer.tsx-2019-                }}
app/home/player/FullPlayer.tsx-2020-                onMouseLeave={(event) => {
app/home/player/FullPlayer.tsx-2021-                  event.currentTarget.style.background = view.restingBackground;
app/home/player/FullPlayer.tsx-2022-                }}
app/home/player/FullPlayer.tsx-2023-                onContextMenu={(event) => {
app/home/player/FullPlayer.tsx-2024-                  event.preventDefault();
app/home/player/FullPlayer.tsx-2025-                  void shareTrack(shareCtx, t);
--
app/home/player/FullPlayer.tsx-2097-                      transition: "opacity 160ms ease, color 160ms ease",
app/home/player/FullPlayer.tsx-2098-                    }}
app/home/player/FullPlayer.tsx-2099-                    data-reason={view.dataReason}
app/home/player/FullPlayer.tsx-2100-                  >
app/home/player/FullPlayer.tsx-2101-                    <span
app/home/player/FullPlayer.tsx-2102-                      className={
app/home/player/FullPlayer.tsx-2103-                        view.shimmerTitle ? "afShimmerText" : undefined
app/home/player/FullPlayer.tsx-2104-                      }
app/home/player/FullPlayer.tsx-2105-                      style={{
app/home/player/FullPlayer.tsx-2106-                        minWidth: 0,
app/home/player/FullPlayer.tsx-2107-                        overflow: "hidden",
app/home/player/FullPlayer.tsx-2108-                        textOverflow: "ellipsis",
app/home/player/FullPlayer.tsx-2109-                      }}
app/home/player/FullPlayer.tsx-2110-                    >
app/home/player/FullPlayer.tsx:2111:                      {t.title ?? t.recordingId}
app/home/player/FullPlayer.tsx-2112-                    </span>
app/home/player/FullPlayer.tsx-2113-
app/home/player/FullPlayer.tsx-2114-                    {t.explicit ? (
app/home/player/FullPlayer.tsx-2115-                      <span
app/home/player/FullPlayer.tsx-2116-                        className="afExplicitBadge"
app/home/player/FullPlayer.tsx-2117-                        aria-label="Explicit"
app/home/player/FullPlayer.tsx-2118-                        title="Explicit"
app/home/player/FullPlayer.tsx-2119-                      >
app/home/player/FullPlayer.tsx-2120-                        E
app/home/player/FullPlayer.tsx-2121-                      </span>
app/home/player/FullPlayer.tsx-2122-                    ) : null}
app/home/player/FullPlayer.tsx-2123-                  </div>
app/home/player/FullPlayer.tsx-2124-
app/home/player/FullPlayer.tsx-2125-                  <div className="afRowMetaUnder" aria-hidden="true">
--
app/home/player/lyrics/lyricsSurface.ts-1-// web/app/home/player/lyrics/lyricsSurface.ts
app/home/player/lyrics/lyricsSurface.ts-2-"use client";
app/home/player/lyrics/lyricsSurface.ts-3-
app/home/player/lyrics/lyricsSurface.ts-4-import type { LyricCue } from "@/lib/types";
app/home/player/lyrics/lyricsSurface.ts-5-
app/home/player/lyrics/lyricsSurface.ts:6:export type CuesByrecordingId = Record<string, LyricCue[]>;
app/home/player/lyrics/lyricsSurface.ts:7:export type OffsetByrecordingId = Record<string, number>;
app/home/player/lyrics/lyricsSurface.ts-8-
app/home/player/lyrics/lyricsSurface.ts-9-type Snapshot = {
app/home/player/lyrics/lyricsSurface.ts:10:  cuesByRecordingId: CuesByrecordingId;
app/home/player/lyrics/lyricsSurface.ts:11:  offsetByRecordingId: OffsetByrecordingId;
app/home/player/lyrics/lyricsSurface.ts-12-  globalOffsetMs: number;
app/home/player/lyrics/lyricsSurface.ts-13-};
app/home/player/lyrics/lyricsSurface.ts-14-
app/home/player/lyrics/lyricsSurface.ts-15-type Listener = () => void;
app/home/player/lyrics/lyricsSurface.ts-16-
app/home/player/lyrics/lyricsSurface.ts-17-let snap: Snapshot = {
app/home/player/lyrics/lyricsSurface.ts:18:  cuesByRecordingId: {},
app/home/player/lyrics/lyricsSurface.ts-19-  offsetByRecordingId: {},
app/home/player/lyrics/lyricsSurface.ts-20-  globalOffsetMs: 0,
app/home/player/lyrics/lyricsSurface.ts-21-};
app/home/player/lyrics/lyricsSurface.ts-22-
app/home/player/lyrics/lyricsSurface.ts-23-const listeners = new Set<Listener>();
app/home/player/lyrics/lyricsSurface.ts-24-
app/home/player/lyrics/lyricsSurface.ts-25-function emit() {
app/home/player/lyrics/lyricsSurface.ts-26-  for (const fn of listeners) fn();
app/home/player/lyrics/lyricsSurface.ts-27-}
app/home/player/lyrics/lyricsSurface.ts-28-
app/home/player/lyrics/lyricsSurface.ts-29-export const lyricsSurface = {
app/home/player/lyrics/lyricsSurface.ts-30-  getSnapshot(): Snapshot {
app/home/player/lyrics/lyricsSurface.ts-31-    return snap;
app/home/player/lyrics/lyricsSurface.ts-32-  },
app/home/player/lyrics/lyricsSurface.ts-33-
app/home/player/lyrics/lyricsSurface.ts-34-  subscribe(fn: Listener): () => void {
app/home/player/lyrics/lyricsSurface.ts-35-    listeners.add(fn);
app/home/player/lyrics/lyricsSurface.ts-36-    return () => listeners.delete(fn);
app/home/player/lyrics/lyricsSurface.ts-37-  },
app/home/player/lyrics/lyricsSurface.ts-38-
app/home/player/lyrics/lyricsSurface.ts-39-  setMaps(next: {
app/home/player/lyrics/lyricsSurface.ts:40:    cuesByrecordingId?: CuesByrecordingId | null;
app/home/player/lyrics/lyricsSurface.ts:41:    offsetByrecordingId?: OffsetByrecordingId | null;
app/home/player/lyrics/lyricsSurface.ts-42-    globalOffsetMs?: number | null;
app/home/player/lyrics/lyricsSurface.ts-43-  }) {
app/home/player/lyrics/lyricsSurface.ts:44:    const cuesByRecordingId = next.cuesByrecordingId ?? {};
app/home/player/lyrics/lyricsSurface.ts:45:    const offsetByRecordingId = next.offsetByrecordingId ?? {};
app/home/player/lyrics/lyricsSurface.ts-46-    const globalOffsetMs =
app/home/player/lyrics/lyricsSurface.ts-47-      typeof next.globalOffsetMs === "number" && Number.isFinite(next.globalOffsetMs)
app/home/player/lyrics/lyricsSurface.ts-48-        ? next.globalOffsetMs
app/home/player/lyrics/lyricsSurface.ts-49-        : 0;
app/home/player/lyrics/lyricsSurface.ts-50-
app/home/player/lyrics/lyricsSurface.ts-51-    // cheap identity guard: only emit if something actually changes by reference/value
app/home/player/lyrics/lyricsSurface.ts-52-    const changed =
app/home/player/lyrics/lyricsSurface.ts:53:      snap.cuesByRecordingId !== cuesByRecordingId ||
app/home/player/lyrics/lyricsSurface.ts-54-      snap.offsetByRecordingId !== offsetByRecordingId ||
app/home/player/lyrics/lyricsSurface.ts-55-      snap.globalOffsetMs !== globalOffsetMs;
app/home/player/lyrics/lyricsSurface.ts-56-
app/home/player/lyrics/lyricsSurface.ts-57-    if (!changed) return;
app/home/player/lyrics/lyricsSurface.ts-58-
app/home/player/lyrics/lyricsSurface.ts:59:    snap = { cuesByRecordingId, offsetByRecordingId, globalOffsetMs };
app/home/player/lyrics/lyricsSurface.ts-60-    emit();
app/home/player/lyrics/lyricsSurface.ts-61-  },
app/home/player/lyrics/lyricsSurface.ts-62-};
--
app/home/player/lyrics/fetchLyricsByRecordingId.ts:1://web/app/home/player/lyrics/fetchLyricsByrecordingId.ts
app/home/player/lyrics/fetchLyricsByRecordingId.ts-2-"use client";
app/home/player/lyrics/fetchLyricsByRecordingId.ts-3-
app/home/player/lyrics/fetchLyricsByRecordingId.ts-4-import { parseTrackLyricsApiOk } from "@/lib/types";
app/home/player/lyrics/fetchLyricsByRecordingId.ts-5-import type { LyricCue } from "@/lib/types";
app/home/player/lyrics/fetchLyricsByRecordingId.ts-6-
app/home/player/lyrics/fetchLyricsByRecordingId.ts:7:export async function fetchLyricsByrecordingId(
app/home/player/lyrics/fetchLyricsByRecordingId.ts:8:  recordingId: string,
app/home/player/lyrics/fetchLyricsByRecordingId.ts-9-  signal?: AbortSignal,
app/home/player/lyrics/fetchLyricsByRecordingId.ts:10:): Promise<{ recordingId: string; cues: LyricCue[]; offsetMs: number } | null> {
app/home/player/lyrics/fetchLyricsByRecordingId.ts-11-  const res = await fetch(
app/home/player/lyrics/fetchLyricsByRecordingId.ts:12:    `/api/lyrics/by-track?recordingId=${encodeURIComponent(recordingId)}`,
app/home/player/lyrics/fetchLyricsByRecordingId.ts-13-    { signal, cache: "no-store" },
app/home/player/lyrics/fetchLyricsByRecordingId.ts-14-  );
app/home/player/lyrics/fetchLyricsByRecordingId.ts-15-  if (!res.ok) return null;
app/home/player/lyrics/fetchLyricsByRecordingId.ts-16-
app/home/player/lyrics/fetchLyricsByRecordingId.ts-17-  const raw: unknown = await res.json();
app/home/player/lyrics/fetchLyricsByRecordingId.ts-18-  const parsed = parseTrackLyricsApiOk(raw);
app/home/player/lyrics/fetchLyricsByRecordingId.ts-19-  if (!parsed) return null;
app/home/player/lyrics/fetchLyricsByRecordingId.ts-20-
app/home/player/lyrics/fetchLyricsByRecordingId.ts-21-  return {
app/home/player/lyrics/fetchLyricsByRecordingId.ts:22:    recordingId: parsed.recordingId,
app/home/player/lyrics/fetchLyricsByRecordingId.ts:23:    cues: parsed.cues,
app/home/player/lyrics/fetchLyricsByRecordingId.ts:24:    offsetMs: parsed.offsetMs,
app/home/player/lyrics/fetchLyricsByRecordingId.ts-25-  };
app/home/player/lyrics/fetchLyricsByRecordingId.ts-26-}
--
app/home/player/PlayerState.tsx-159-function getEffectiveDurationMs(
app/home/player/PlayerState.tsx-160-  state: Pick<
app/home/player/PlayerState.tsx-161-    PlayerState,
app/home/player/PlayerState.tsx-162-    "durationByRecordingId" | "assetDurationByPlaybackId"
app/home/player/PlayerState.tsx-163-  >,
app/home/player/PlayerState.tsx-164-  track: PlayerTrack | undefined,
app/home/player/PlayerState.tsx-165-): number {
app/home/player/PlayerState.tsx-166-  const assetDurationMs = getAssetDurationMs(
app/home/player/PlayerState.tsx-167-    state.assetDurationByPlaybackId,
app/home/player/PlayerState.tsx-168-    track,
app/home/player/PlayerState.tsx-169-  );
app/home/player/PlayerState.tsx-170-
app/home/player/PlayerState.tsx-171-  if (assetDurationMs > 0) return assetDurationMs;
app/home/player/PlayerState.tsx-172-
app/home/player/PlayerState.tsx:173:  const recordingId = (track?.recordingId ?? "").trim();
app/home/player/PlayerState.tsx:174:  const catalogueDurationMs = recordingId
app/home/player/PlayerState.tsx:175:    ? positiveDurationMs(state.durationByRecordingId[recordingId])
app/home/player/PlayerState.tsx-176-    : 0;
app/home/player/PlayerState.tsx-177-
app/home/player/PlayerState.tsx-178-  return catalogueDurationMs || positiveDurationMs(track?.durationMs);
app/home/player/PlayerState.tsx-179-}
app/home/player/PlayerState.tsx-180-
app/home/player/PlayerState.tsx-181-function retainAssetDurationsByPlaybackId(
app/home/player/PlayerState.tsx-182-  previous: Record<string, number>,
app/home/player/PlayerState.tsx-183-  tracks: PlayerTrack[],
app/home/player/PlayerState.tsx-184-): Record<string, number> {
app/home/player/PlayerState.tsx-185-  const activePlaybackIds = new Set<string>();
app/home/player/PlayerState.tsx-186-
app/home/player/PlayerState.tsx-187-  for (const track of tracks) {
app/home/player/PlayerState.tsx-188-    const playbackId = (track.muxPlaybackId ?? "").trim();
app/home/player/PlayerState.tsx-189-    if (playbackId) activePlaybackIds.add(playbackId);
--
app/home/player/PlayerState.tsx-194-  for (const playbackId of Object.keys(previous)) {
app/home/player/PlayerState.tsx-195-    if (activePlaybackIds.has(playbackId)) continue;
app/home/player/PlayerState.tsx-196-
app/home/player/PlayerState.tsx-197-    if (next === previous) next = { ...previous };
app/home/player/PlayerState.tsx-198-    delete next[playbackId];
app/home/player/PlayerState.tsx-199-  }
app/home/player/PlayerState.tsx-200-
app/home/player/PlayerState.tsx-201-  return next;
app/home/player/PlayerState.tsx-202-}
app/home/player/PlayerState.tsx-203-
app/home/player/PlayerState.tsx-204-function hydrateTrack(
app/home/player/PlayerState.tsx-205-  t: PlayerTrack,
app/home/player/PlayerState.tsx-206-  durationByRecordingId: Record<string, number>,
app/home/player/PlayerState.tsx-207-): PlayerTrack {
app/home/player/PlayerState.tsx:208:  const cached = durationByRecordingId[t.recordingId];
app/home/player/PlayerState.tsx-209-  if (!cached || cached <= 0) return t;
app/home/player/PlayerState.tsx-210-  if (t.durationMs === cached) return t;
app/home/player/PlayerState.tsx-211-  return { ...t, durationMs: cached };
app/home/player/PlayerState.tsx-212-}
app/home/player/PlayerState.tsx-213-
app/home/player/PlayerState.tsx-214-function hydrateTracks(
app/home/player/PlayerState.tsx-215-  ts: PlayerTrack[],
app/home/player/PlayerState.tsx-216-  durationByRecordingId: Record<string, number>,
app/home/player/PlayerState.tsx-217-) {
app/home/player/PlayerState.tsx-218-  let changed = false;
app/home/player/PlayerState.tsx-219-  const next = ts.map((t) => {
app/home/player/PlayerState.tsx-220-    const ht = hydrateTrack(t, durationByRecordingId);
app/home/player/PlayerState.tsx-221-    if (ht !== t) changed = true;
app/home/player/PlayerState.tsx-222-    return ht;
--
app/home/player/PlayerState.tsx-253-        track,
app/home/player/PlayerState.tsx-254-        albumId,
app/home/player/PlayerState.tsx-255-      },
app/home/player/PlayerState.tsx-256-    }),
app/home/player/PlayerState.tsx-257-  );
app/home/player/PlayerState.tsx-258-}
app/home/player/PlayerState.tsx-259-
app/home/player/PlayerState.tsx-260-function getNextTransportTrack(state: PlayerState): PlayerTrack | undefined {
app/home/player/PlayerState.tsx-261-  const current = state.current;
app/home/player/PlayerState.tsx-262-
app/home/player/PlayerState.tsx-263-  if (!current || state.queue.length === 0) return undefined;
app/home/player/PlayerState.tsx-264-  if (state.repeat === "one") return current;
app/home/player/PlayerState.tsx-265-
app/home/player/PlayerState.tsx-266-  const index = state.queue.findIndex(
app/home/player/PlayerState.tsx:267:    (track) => track.recordingId === current.recordingId,
app/home/player/PlayerState.tsx-268-  );
app/home/player/PlayerState.tsx-269-  const currentIndex = Math.max(index, 0);
app/home/player/PlayerState.tsx-270-
app/home/player/PlayerState.tsx-271-  if (currentIndex + 1 < state.queue.length) {
app/home/player/PlayerState.tsx-272-    return state.queue[currentIndex + 1];
app/home/player/PlayerState.tsx-273-  }
app/home/player/PlayerState.tsx-274-
app/home/player/PlayerState.tsx-275-  return state.repeat === "all" ? state.queue[0] : undefined;
app/home/player/PlayerState.tsx-276-}
app/home/player/PlayerState.tsx-277-
app/home/player/PlayerState.tsx-278-function getPreviousTransportTrack(
app/home/player/PlayerState.tsx-279-  state: PlayerState,
app/home/player/PlayerState.tsx-280-): PlayerTrack | undefined {
app/home/player/PlayerState.tsx-281-  const current = state.current;
app/home/player/PlayerState.tsx-282-
app/home/player/PlayerState.tsx-283-  if (!current || state.queue.length === 0) return undefined;
app/home/player/PlayerState.tsx-284-
app/home/player/PlayerState.tsx-285-  if (state.status === "playing" && state.positionMs > 3000) {
app/home/player/PlayerState.tsx-286-    return current;
app/home/player/PlayerState.tsx-287-  }
app/home/player/PlayerState.tsx-288-
app/home/player/PlayerState.tsx-289-  const index = state.queue.findIndex(
app/home/player/PlayerState.tsx:290:    (track) => track.recordingId === current.recordingId,
app/home/player/PlayerState.tsx-291-  );
app/home/player/PlayerState.tsx-292-  const currentIndex = Math.max(index, 0);
app/home/player/PlayerState.tsx-293-
app/home/player/PlayerState.tsx-294-  if (currentIndex > 0) {
app/home/player/PlayerState.tsx-295-    return state.queue[currentIndex - 1];
app/home/player/PlayerState.tsx-296-  }
app/home/player/PlayerState.tsx-297-
app/home/player/PlayerState.tsx-298-  if (state.repeat === "all") {
app/home/player/PlayerState.tsx-299-    return state.queue.at(-1);
app/home/player/PlayerState.tsx-300-  }
app/home/player/PlayerState.tsx-301-
app/home/player/PlayerState.tsx-302-  return current;
app/home/player/PlayerState.tsx-303-}
app/home/player/PlayerState.tsx-304-
app/home/player/PlayerState.tsx-305-function primeDurationByRecordingId(
app/home/player/PlayerState.tsx-306-  prev: Record<string, number>,
app/home/player/PlayerState.tsx-307-  tracks: PlayerTrack[],
app/home/player/PlayerState.tsx-308-): Record<string, number> {
app/home/player/PlayerState.tsx-309-  let next = prev;
app/home/player/PlayerState.tsx-310-  for (const t of tracks) {
app/home/player/PlayerState.tsx:311:    if (!t?.recordingId) continue;
app/home/player/PlayerState.tsx-312-    const ms = t.durationMs;
app/home/player/PlayerState.tsx-313-    if (typeof ms !== "number" || !Number.isFinite(ms) || ms <= 0) continue;
app/home/player/PlayerState.tsx:314:    if (typeof next[t.recordingId] === "number" && next[t.recordingId] > 0)
app/home/player/PlayerState.tsx-315-      continue;
app/home/player/PlayerState.tsx-316-    if (next === prev) next = { ...prev };
app/home/player/PlayerState.tsx:317:    next[t.recordingId] = ms;
app/home/player/PlayerState.tsx-318-  }
app/home/player/PlayerState.tsx-319-  return next;
app/home/player/PlayerState.tsx-320-}
app/home/player/PlayerState.tsx-321-
app/home/player/PlayerState.tsx-322-function resolvePlaybackAccessIdentityKey(
app/home/player/PlayerState.tsx-323-  params: Readonly<{
app/home/player/PlayerState.tsx-324-    clerkAuthLoaded: boolean;
app/home/player/PlayerState.tsx-325-    isSignedIn: boolean | undefined;
app/home/player/PlayerState.tsx-326-    userId: string | null | undefined;
app/home/player/PlayerState.tsx-327-    sessionId: string | null | undefined;
app/home/player/PlayerState.tsx-328-  }>,
app/home/player/PlayerState.tsx-329-): string {
app/home/player/PlayerState.tsx-330-  if (!params.clerkAuthLoaded) return "clerk:loading";
app/home/player/PlayerState.tsx-331-  if (!params.isSignedIn) return "clerk:anonymous";
--
app/home/player/PlayerState.tsx-336-function markCurrentTrackLoading(
app/home/player/PlayerState.tsx-337-  state: PlayerState,
app/home/player/PlayerState.tsx-338-  loadingReason: "attach",
app/home/player/PlayerState.tsx-339-): PlayerState {
app/home/player/PlayerState.tsx-340-  const current = state.current;
app/home/player/PlayerState.tsx-341-  if (!current) return state;
app/home/player/PlayerState.tsx-342-
app/home/player/PlayerState.tsx-343-  return {
app/home/player/PlayerState.tsx-344-    ...state,
app/home/player/PlayerState.tsx-345-    status: "loading",
app/home/player/PlayerState.tsx-346-    loadingReason,
app/home/player/PlayerState.tsx-347-    positionMs: 0,
app/home/player/PlayerState.tsx-348-    intent: "play",
app/home/player/PlayerState.tsx-349-    intentAtMs: Date.now(),
app/home/player/PlayerState.tsx:350:    pendingRecordingId: current.recordingId,
app/home/player/PlayerState.tsx:351:    selectedRecordingId: current.recordingId,
app/home/player/PlayerState.tsx-352-  };
app/home/player/PlayerState.tsx-353-}
app/home/player/PlayerState.tsx-354-
app/home/player/PlayerState.tsx-355-function markNextTrackLoading(
app/home/player/PlayerState.tsx-356-  state: PlayerState,
app/home/player/PlayerState.tsx-357-  track: PlayerTrack,
app/home/player/PlayerState.tsx-358-  loadingReason: "token" | "attach",
app/home/player/PlayerState.tsx-359-): PlayerState {
app/home/player/PlayerState.tsx-360-  const hydratedTrack = hydrateTrack(track, state.durationByRecordingId);
app/home/player/PlayerState.tsx-361-
app/home/player/PlayerState.tsx-362-  return {
app/home/player/PlayerState.tsx-363-    ...state,
app/home/player/PlayerState.tsx-364-    current: hydratedTrack,
app/home/player/PlayerState.tsx-365-    status: "loading",
app/home/player/PlayerState.tsx-366-    loadingReason,
app/home/player/PlayerState.tsx-367-    positionMs: 0,
app/home/player/PlayerState.tsx-368-    intent: "play",
app/home/player/PlayerState.tsx-369-    intentAtMs: Date.now(),
app/home/player/PlayerState.tsx:370:    pendingRecordingId: hydratedTrack.recordingId,
app/home/player/PlayerState.tsx:371:    selectedRecordingId: hydratedTrack.recordingId,
app/home/player/PlayerState.tsx-372-  };
app/home/player/PlayerState.tsx-373-}
app/home/player/PlayerState.tsx-374-
app/home/player/PlayerState.tsx-375-function transitionForwardState(
app/home/player/PlayerState.tsx-376-  state: PlayerState,
app/home/player/PlayerState.tsx-377-  nextTrackLoadingReason: "token" | "attach",
app/home/player/PlayerState.tsx-378-  terminalIntent: "pause" | null,
app/home/player/PlayerState.tsx-379-): PlayerState {
app/home/player/PlayerState.tsx-380-  const current = state.current;
app/home/player/PlayerState.tsx-381-  if (!current || state.queue.length === 0) return state;
app/home/player/PlayerState.tsx-382-
app/home/player/PlayerState.tsx-383-  if (state.repeat === "one") {
app/home/player/PlayerState.tsx-384-    return markCurrentTrackLoading(state, "attach");
app/home/player/PlayerState.tsx-385-  }
app/home/player/PlayerState.tsx-386-
app/home/player/PlayerState.tsx-387-  const index = state.queue.findIndex(
app/home/player/PlayerState.tsx:388:    (track) => track.recordingId === current.recordingId,
app/home/player/PlayerState.tsx-389-  );
app/home/player/PlayerState.tsx-390-  const currentIndex = Math.max(index, 0);
app/home/player/PlayerState.tsx-391-  const nextTrack = state.queue[currentIndex + 1];
app/home/player/PlayerState.tsx-392-
app/home/player/PlayerState.tsx-393-  if (nextTrack) {
app/home/player/PlayerState.tsx-394-    return markNextTrackLoading(state, nextTrack, nextTrackLoadingReason);
app/home/player/PlayerState.tsx-395-  }
app/home/player/PlayerState.tsx-396-
app/home/player/PlayerState.tsx-397-  if (state.repeat === "all") {
app/home/player/PlayerState.tsx-398-    const firstTrack = state.queue[0];
app/home/player/PlayerState.tsx-399-    if (firstTrack) {
app/home/player/PlayerState.tsx-400-      return markNextTrackLoading(state, firstTrack, nextTrackLoadingReason);
app/home/player/PlayerState.tsx-401-    }
app/home/player/PlayerState.tsx-402-  }
--
app/home/player/PlayerState.tsx-413-    loadingReason: undefined,
app/home/player/PlayerState.tsx-414-    pendingRecordingId: undefined,
app/home/player/PlayerState.tsx-415-  };
app/home/player/PlayerState.tsx-416-}
app/home/player/PlayerState.tsx-417-
app/home/player/PlayerState.tsx-418-function transitionPreviousState(state: PlayerState): PlayerState {
app/home/player/PlayerState.tsx-419-  const current = state.current;
app/home/player/PlayerState.tsx-420-  if (!current || state.queue.length === 0) return state;
app/home/player/PlayerState.tsx-421-
app/home/player/PlayerState.tsx-422-  if (state.status === "playing" && state.positionMs > 3000) {
app/home/player/PlayerState.tsx-423-    return markCurrentTrackLoading(state, "attach");
app/home/player/PlayerState.tsx-424-  }
app/home/player/PlayerState.tsx-425-
app/home/player/PlayerState.tsx-426-  const index = state.queue.findIndex(
app/home/player/PlayerState.tsx:427:    (track) => track.recordingId === current.recordingId,
app/home/player/PlayerState.tsx-428-  );
app/home/player/PlayerState.tsx-429-  const currentIndex = Math.max(index, 0);
app/home/player/PlayerState.tsx-430-  const previousTrack = state.queue[currentIndex - 1];
app/home/player/PlayerState.tsx-431-
app/home/player/PlayerState.tsx-432-  if (previousTrack) {
app/home/player/PlayerState.tsx-433-    return markNextTrackLoading(state, previousTrack, "token");
app/home/player/PlayerState.tsx-434-  }
app/home/player/PlayerState.tsx-435-
app/home/player/PlayerState.tsx-436-  if (state.repeat === "all") {
app/home/player/PlayerState.tsx-437-    const lastTrack = state.queue.at(-1);
app/home/player/PlayerState.tsx-438-    if (lastTrack) {
app/home/player/PlayerState.tsx-439-      return markNextTrackLoading(state, lastTrack, "token");
app/home/player/PlayerState.tsx-440-    }
app/home/player/PlayerState.tsx-441-  }
--
app/home/player/PlayerState.tsx-496-
app/home/player/PlayerState.tsx-497-  if (Object.hasOwn(optionBag, "sharePlaybackScopeId")) {
app/home/player/PlayerState.tsx-498-    patch.queueSharePlaybackScopeId = normalizedQueueShareValue(
app/home/player/PlayerState.tsx-499-      opts?.sharePlaybackScopeId,
app/home/player/PlayerState.tsx-500-    );
app/home/player/PlayerState.tsx-501-  }
app/home/player/PlayerState.tsx-502-
app/home/player/PlayerState.tsx-503-  return patch;
app/home/player/PlayerState.tsx-504-}
app/home/player/PlayerState.tsx-505-
app/home/player/PlayerState.tsx-506-function findRequestedInitialCurrent(
app/home/player/PlayerState.tsx-507-  queue: readonly PlayerTrack[],
app/home/player/PlayerState.tsx-508-  opts: QueueContext | undefined,
app/home/player/PlayerState.tsx-509-): PlayerTrack | null {
app/home/player/PlayerState.tsx:510:  const recordingId =
app/home/player/PlayerState.tsx-511-    typeof opts?.initialCurrentRecordingId === "string"
app/home/player/PlayerState.tsx-512-      ? opts.initialCurrentRecordingId.trim()
app/home/player/PlayerState.tsx-513-      : "";
app/home/player/PlayerState.tsx-514-
app/home/player/PlayerState.tsx:515:  if (!recordingId) return null;
app/home/player/PlayerState.tsx-516-
app/home/player/PlayerState.tsx-517-  return (
app/home/player/PlayerState.tsx:518:    queue.find((track) => track.recordingId === recordingId) ?? null
app/home/player/PlayerState.tsx-519-  );
app/home/player/PlayerState.tsx-520-}
app/home/player/PlayerState.tsx-521-
app/home/player/PlayerState.tsx-522-type QueueSelectionState = Pick<
app/home/player/PlayerState.tsx-523-  PlayerState,
app/home/player/PlayerState.tsx-524-  "positionMs" | "selectedRecordingId" | "pendingRecordingId"
app/home/player/PlayerState.tsx-525->;
app/home/player/PlayerState.tsx-526-
app/home/player/PlayerState.tsx-527-function resolveQueueSelectionState(
app/home/player/PlayerState.tsx-528-  state: PlayerState,
app/home/player/PlayerState.tsx-529-  nextCurrent: PlayerTrack | undefined,
app/home/player/PlayerState.tsx-530-  requestedInitialCurrent: PlayerTrack | null,
app/home/player/PlayerState.tsx-531-): QueueSelectionState {
app/home/player/PlayerState.tsx-532-  if (requestedInitialCurrent) {
app/home/player/PlayerState.tsx-533-    return {
app/home/player/PlayerState.tsx-534-      positionMs: 0,
app/home/player/PlayerState.tsx:535:      selectedRecordingId: requestedInitialCurrent.recordingId,
app/home/player/PlayerState.tsx-536-      pendingRecordingId: undefined,
app/home/player/PlayerState.tsx-537-    };
app/home/player/PlayerState.tsx-538-  }
app/home/player/PlayerState.tsx-539-
app/home/player/PlayerState.tsx-540-  return {
app/home/player/PlayerState.tsx-541-    positionMs: state.current ? state.positionMs : 0,
app/home/player/PlayerState.tsx-542-    selectedRecordingId:
app/home/player/PlayerState.tsx:543:      state.selectedRecordingId ?? nextCurrent?.recordingId,
app/home/player/PlayerState.tsx-544-    pendingRecordingId: state.pendingRecordingId,
app/home/player/PlayerState.tsx-545-  };
app/home/player/PlayerState.tsx-546-}
app/home/player/PlayerState.tsx-547-
app/home/player/PlayerState.tsx-548-function applyQueueState(
app/home/player/PlayerState.tsx-549-  state: PlayerState,
app/home/player/PlayerState.tsx-550-  tracks: PlayerTrack[],
app/home/player/PlayerState.tsx-551-  opts: QueueContext | undefined,
app/home/player/PlayerState.tsx-552-): PlayerState {
app/home/player/PlayerState.tsx-553-  const nextDurationByRecordingId = primeDurationByRecordingId(
app/home/player/PlayerState.tsx-554-    state.durationByRecordingId,
app/home/player/PlayerState.tsx-555-    tracks,
app/home/player/PlayerState.tsx-556-  );
app/home/player/PlayerState.tsx-557-  const nextAssetDurationByPlaybackId = retainAssetDurationsByPlaybackId(
--
app/home/player/PlayerState.tsx-781-                current: undefined,
app/home/player/PlayerState.tsx-782-                positionMs: 0,
app/home/player/PlayerState.tsx-783-                intent: "play",
app/home/player/PlayerState.tsx-784-                intentAtMs: now,
app/home/player/PlayerState.tsx-785-                lastPlayAttemptAtMs: now,
app/home/player/PlayerState.tsx-786-                playRequestPending: false,
app/home/player/PlayerState.tsx-787-                lastError: undefined,
app/home/player/PlayerState.tsx-788-                loadingReason: undefined,
app/home/player/PlayerState.tsx-789-                pendingRecordingId: undefined,
app/home/player/PlayerState.tsx-790-              };
app/home/player/PlayerState.tsx-791-            }
app/home/player/PlayerState.tsx-792-
app/home/player/PlayerState.tsx-793-            const nextTrack = hydrateTrack(rawNext, s.durationByRecordingId);
app/home/player/PlayerState.tsx-794-            const sameTrack =
app/home/player/PlayerState.tsx:795:              s.current?.recordingId === nextTrack.recordingId;
app/home/player/PlayerState.tsx-796-
app/home/player/PlayerState.tsx-797-            const base = {
app/home/player/PlayerState.tsx-798-              ...s,
app/home/player/PlayerState.tsx-799-              intent: "play" as const,
app/home/player/PlayerState.tsx-800-              intentAtMs: now,
app/home/player/PlayerState.tsx-801-              lastPlayAttemptAtMs: now,
app/home/player/PlayerState.tsx-802-              playRequestPending: false,
app/home/player/PlayerState.tsx-803-              lastError: undefined,
app/home/player/PlayerState.tsx:804:              selectedRecordingId: nextTrack.recordingId,
app/home/player/PlayerState.tsx:805:              pendingRecordingId: nextTrack.recordingId,
app/home/player/PlayerState.tsx-806-            };
app/home/player/PlayerState.tsx-807-
app/home/player/PlayerState.tsx-808-            if (sameTrack && s.status === "paused") {
app/home/player/PlayerState.tsx-809-              return {
app/home/player/PlayerState.tsx-810-                ...base,
app/home/player/PlayerState.tsx-811-                current: hydrateTrack(s.current!, s.durationByRecordingId),
app/home/player/PlayerState.tsx-812-                status: "paused",
app/home/player/PlayerState.tsx-813-                loadingReason: undefined,
app/home/player/PlayerState.tsx-814-              };
app/home/player/PlayerState.tsx-815-            }
app/home/player/PlayerState.tsx-816-
app/home/player/PlayerState.tsx-817-            if (
app/home/player/PlayerState.tsx-818-              sameTrack &&
app/home/player/PlayerState.tsx-819-              (s.status === "playing" || s.status === "loading")
--
app/home/player/PlayerState.tsx-883-        setState((s) => applyQueueState(s, tracks, opts)),
app/home/player/PlayerState.tsx-884-
app/home/player/PlayerState.tsx-885-      enqueue: (track: PlayerTrack) =>
app/home/player/PlayerState.tsx-886-        setState((s) => {
app/home/player/PlayerState.tsx-887-          const nextDurationByRecordingId = primeDurationByRecordingId(
app/home/player/PlayerState.tsx-888-            s.durationByRecordingId,
app/home/player/PlayerState.tsx-889-            [track],
app/home/player/PlayerState.tsx-890-          );
app/home/player/PlayerState.tsx-891-          const t = hydrateTrack(track, nextDurationByRecordingId);
app/home/player/PlayerState.tsx-892-          return {
app/home/player/PlayerState.tsx-893-            ...s,
app/home/player/PlayerState.tsx-894-            durationByRecordingId: nextDurationByRecordingId,
app/home/player/PlayerState.tsx-895-            queue: [...s.queue, t],
app/home/player/PlayerState.tsx-896-            current: s.current ?? t,
app/home/player/PlayerState.tsx:897:            selectedRecordingId: s.selectedRecordingId ?? t.recordingId,
app/home/player/PlayerState.tsx-898-          };
app/home/player/PlayerState.tsx-899-        }),
app/home/player/PlayerState.tsx-900-
app/home/player/PlayerState.tsx-901-      clearQueue: () =>
app/home/player/PlayerState.tsx-902-        setState((s) => ({
app/home/player/PlayerState.tsx-903-          ...s,
app/home/player/PlayerState.tsx-904-          queue: [],
app/home/player/PlayerState.tsx-905-          current: undefined,
app/home/player/PlayerState.tsx-906-          selectedRecordingId: undefined,
app/home/player/PlayerState.tsx-907-          pendingRecordingId: undefined,
app/home/player/PlayerState.tsx-908-
app/home/player/PlayerState.tsx-909-          positionMs: 0,
app/home/player/PlayerState.tsx-910-          pendingSeekMs: undefined,
app/home/player/PlayerState.tsx-911-          seeking: false,
--
app/home/player/PlayerState.tsx-918-
app/home/player/PlayerState.tsx-919-      setPositionMs: (ms: number) =>
app/home/player/PlayerState.tsx-920-        setState((s) => ({
app/home/player/PlayerState.tsx-921-          ...s,
app/home/player/PlayerState.tsx-922-          positionMs: Math.max(0, ms),
app/home/player/PlayerState.tsx-923-        })),
app/home/player/PlayerState.tsx-924-
app/home/player/PlayerState.tsx-925-      setDurationMs: (ms: number) =>
app/home/player/PlayerState.tsx-926-        setState((s) => {
app/home/player/PlayerState.tsx-927-          const cur = s.current;
app/home/player/PlayerState.tsx-928-          if (!cur) return s;
app/home/player/PlayerState.tsx-929-          if (!Number.isFinite(ms) || ms <= 0) return s;
app/home/player/PlayerState.tsx-930-
app/home/player/PlayerState.tsx-931-          const alreadyCached =
app/home/player/PlayerState.tsx:932:            typeof s.durationByRecordingId[cur.recordingId] === "number" &&
app/home/player/PlayerState.tsx:933:            s.durationByRecordingId[cur.recordingId] > 0;
app/home/player/PlayerState.tsx-934-          const alreadyOnTrack =
app/home/player/PlayerState.tsx-935-            typeof cur.durationMs === "number" && cur.durationMs > 0;
app/home/player/PlayerState.tsx-936-          if (alreadyCached || alreadyOnTrack) return s;
app/home/player/PlayerState.tsx-937-
app/home/player/PlayerState.tsx-938-          const nextDurationByRecordingId = {
app/home/player/PlayerState.tsx-939-            ...s.durationByRecordingId,
app/home/player/PlayerState.tsx:940:            [cur.recordingId]: ms,
app/home/player/PlayerState.tsx-941-          };
app/home/player/PlayerState.tsx-942-
app/home/player/PlayerState.tsx-943-          const nextCurrent = { ...cur, durationMs: ms };
app/home/player/PlayerState.tsx-944-          let changed = false;
app/home/player/PlayerState.tsx-945-          const nextQueue = s.queue.map((t) => {
app/home/player/PlayerState.tsx:946:            if (t.recordingId !== cur.recordingId) return t;
app/home/player/PlayerState.tsx-947-            if (typeof t.durationMs === "number" && t.durationMs > 0) return t;
app/home/player/PlayerState.tsx-948-            changed = true;
app/home/player/PlayerState.tsx-949-            return { ...t, durationMs: ms };
app/home/player/PlayerState.tsx-950-          });
app/home/player/PlayerState.tsx-951-
app/home/player/PlayerState.tsx-952-          return {
app/home/player/PlayerState.tsx-953-            ...s,
app/home/player/PlayerState.tsx-954-            durationByRecordingId: nextDurationByRecordingId,
app/home/player/PlayerState.tsx-955-            current: nextCurrent,
app/home/player/PlayerState.tsx-956-            queue: changed ? nextQueue : s.queue,
app/home/player/PlayerState.tsx-957-          };
app/home/player/PlayerState.tsx-958-        }),
app/home/player/PlayerState.tsx-959-
app/home/player/PlayerState.tsx-960-      setAssetDurationMs: (playbackId: string, ms: number) => {
--
app/home/player/PlayerState.tsx-1019-      tick: (deltaMs: number) => {
app/home/player/PlayerState.tsx-1020-        setState((s) => {
app/home/player/PlayerState.tsx-1021-          if (s.status !== "playing") return s;
app/home/player/PlayerState.tsx-1022-
app/home/player/PlayerState.tsx-1023-          const dur = getEffectiveDurationMs(s, s.current);
app/home/player/PlayerState.tsx-1024-          const nextPos = Math.max(0, s.positionMs + Math.max(0, deltaMs));
app/home/player/PlayerState.tsx-1025-
app/home/player/PlayerState.tsx-1026-          if (dur <= 0) return { ...s, positionMs: nextPos };
app/home/player/PlayerState.tsx-1027-          if (nextPos < dur) return { ...s, positionMs: nextPos };
app/home/player/PlayerState.tsx-1028-
app/home/player/PlayerState.tsx-1029-          if (s.repeat === "one") return { ...s, positionMs: 0 };
app/home/player/PlayerState.tsx-1030-
app/home/player/PlayerState.tsx-1031-          const cur = s.current;
app/home/player/PlayerState.tsx-1032-          const idx = cur
app/home/player/PlayerState.tsx:1033:            ? s.queue.findIndex((t) => t.recordingId === cur.recordingId)
app/home/player/PlayerState.tsx-1034-            : -1;
app/home/player/PlayerState.tsx-1035-          const at = Math.max(idx, 0);
app/home/player/PlayerState.tsx-1036-          const nextIdx = at + 1;
app/home/player/PlayerState.tsx-1037-
app/home/player/PlayerState.tsx-1038-          if (nextIdx < s.queue.length) {
app/home/player/PlayerState.tsx-1039-            const t = hydrateTrack(s.queue[nextIdx], s.durationByRecordingId);
app/home/player/PlayerState.tsx-1040-            return {
app/home/player/PlayerState.tsx-1041-              ...s,
app/home/player/PlayerState.tsx-1042-              current: t,
app/home/player/PlayerState.tsx-1043-              positionMs: 0,
app/home/player/PlayerState.tsx:1044:              selectedRecordingId: t.recordingId,
app/home/player/PlayerState.tsx:1045:              pendingRecordingId: t.recordingId,
app/home/player/PlayerState.tsx-1046-            };
app/home/player/PlayerState.tsx-1047-          }
app/home/player/PlayerState.tsx-1048-
app/home/player/PlayerState.tsx-1049-          if (s.repeat === "all" && s.queue.length > 0) {
app/home/player/PlayerState.tsx-1050-            const t = hydrateTrack(s.queue[0], s.durationByRecordingId);
app/home/player/PlayerState.tsx-1051-            return {
app/home/player/PlayerState.tsx-1052-              ...s,
app/home/player/PlayerState.tsx-1053-              current: t,
app/home/player/PlayerState.tsx-1054-              positionMs: 0,
app/home/player/PlayerState.tsx:1055:              selectedRecordingId: t.recordingId,
app/home/player/PlayerState.tsx:1056:              pendingRecordingId: t.recordingId,
app/home/player/PlayerState.tsx-1057-            };
app/home/player/PlayerState.tsx-1058-          }
app/home/player/PlayerState.tsx-1059-
app/home/player/PlayerState.tsx-1060-          return { ...s, status: "paused", positionMs: dur };
app/home/player/PlayerState.tsx-1061-        });
app/home/player/PlayerState.tsx-1062-      },
app/home/player/PlayerState.tsx-1063-
app/home/player/PlayerState.tsx-1064-      clearError: () =>
app/home/player/PlayerState.tsx-1065-        setState((s) => (s.lastError ? { ...s, lastError: undefined } : s)),
app/home/player/PlayerState.tsx-1066-
app/home/player/PlayerState.tsx-1067-      bumpReload: () => {
app/home/player/PlayerState.tsx-1068-        void guardPlayback(() => {
app/home/player/PlayerState.tsx-1069-          setState((s) => {
app/home/player/PlayerState.tsx-1070-            if (!s.current?.muxPlaybackId) {
app/home/player/PlayerState.tsx-1071-              return { ...s, reloadNonce: s.reloadNonce + 1 };
app/home/player/PlayerState.tsx-1072-            }
app/home/player/PlayerState.tsx-1073-
app/home/player/PlayerState.tsx-1074-            return {
app/home/player/PlayerState.tsx-1075-              ...s,
app/home/player/PlayerState.tsx-1076-              reloadNonce: s.reloadNonce + 1,
app/home/player/PlayerState.tsx-1077-              status: "loading",
app/home/player/PlayerState.tsx-1078-              loadingReason: "token",
app/home/player/PlayerState.tsx-1079-              lastError: undefined,
app/home/player/PlayerState.tsx-1080-              intent: "play",
app/home/player/PlayerState.tsx-1081-              intentAtMs: Date.now(),
app/home/player/PlayerState.tsx:1082:              pendingRecordingId: s.current.recordingId,
app/home/player/PlayerState.tsx:1083:              selectedRecordingId: s.current.recordingId,
app/home/player/PlayerState.tsx-1084-            };
app/home/player/PlayerState.tsx-1085-          });
app/home/player/PlayerState.tsx-1086-        });
app/home/player/PlayerState.tsx-1087-      },
app/home/player/PlayerState.tsx-1088-    };
app/home/player/PlayerState.tsx-1089-  }, [guardPlayback]);
app/home/player/PlayerState.tsx-1090-
app/home/player/PlayerState.tsx-1091-  const api: PlayerState & PlayerActions = React.useMemo(() => {
app/home/player/PlayerState.tsx-1092-    return {
app/home/player/PlayerState.tsx-1093-      ...state,
app/home/player/PlayerState.tsx-1094-      ...actions,
app/home/player/PlayerState.tsx-1095-    };
app/home/player/PlayerState.tsx-1096-  }, [state, actions]);
app/home/player/PlayerState.tsx-1097-
--
app/home/player/PlayerState.tsx-1111-      intent: visualIntent,
app/home/player/PlayerState.tsx-1112-      playRequestPending: visualPlayRequestPending,
app/home/player/PlayerState.tsx-1113-      current: visualCurrent,
app/home/player/PlayerState.tsx-1114-      firstQueuedTrack,
app/home/player/PlayerState.tsx-1115-    }),
app/home/player/PlayerState.tsx-1116-    [
app/home/player/PlayerState.tsx-1117-      visualStatus,
app/home/player/PlayerState.tsx-1118-      visualIntent,
app/home/player/PlayerState.tsx-1119-      visualPlayRequestPending,
app/home/player/PlayerState.tsx-1120-      visualCurrent,
app/home/player/PlayerState.tsx-1121-      firstQueuedTrack,
app/home/player/PlayerState.tsx-1122-    ],
app/home/player/PlayerState.tsx-1123-  );
app/home/player/PlayerState.tsx-1124-
app/home/player/PlayerState.tsx:1125:  const currentId = state.current?.recordingId ?? null;
app/home/player/PlayerState.tsx-1126-  const pendingId = state.pendingRecordingId ?? null;
app/home/player/PlayerState.tsx-1127-
app/home/player/PlayerState.tsx-1128-  React.useEffect(() => {
app/home/player/PlayerState.tsx-1129-    const id = (currentId ?? "").trim();
app/home/player/PlayerState.tsx-1130-    if (!id) return;
app/home/player/PlayerState.tsx-1131-    void ensureLyricsForTrack(id);
app/home/player/PlayerState.tsx-1132-  }, [currentId]);
app/home/player/PlayerState.tsx-1133-
app/home/player/PlayerState.tsx-1134-  React.useEffect(() => {
app/home/player/PlayerState.tsx-1135-    const id = (pendingId ?? "").trim();
app/home/player/PlayerState.tsx-1136-    if (!id) return;
app/home/player/PlayerState.tsx-1137-    void ensureLyricsForTrack(id);
app/home/player/PlayerState.tsx-1138-  }, [pendingId]);
app/home/player/PlayerState.tsx-1139-
--
lib/types.ts-57-  slug: string;
lib/types.ts-58-  title: string;
lib/types.ts-59-  artist?: string;
lib/types.ts-60-  year?: number;
lib/types.ts-61-  coverUrl?: string | null;
lib/types.ts-62-
lib/types.ts-63-  // browse-click gating (load gate)
lib/types.ts-64-  policy?: {
lib/types.ts-65-    publicPageVisible: boolean;
lib/types.ts-66-    minTierToLoad: TierName | null;
lib/types.ts-67-  };
lib/types.ts-68-};
lib/types.ts-69-
lib/types.ts-70-export type PlayerTrack = {
lib/types.ts:71:  recordingId: string;
lib/types.ts-72-  displayId: string;
lib/types.ts-73-  title?: string;
lib/types.ts-74-  artist?: string;
lib/types.ts-75-  durationMs?: number;
lib/types.ts-76-  muxPlaybackId?: string;
lib/types.ts-77-  visualTheme?: string;
lib/types.ts-78-  explicit?: boolean;
lib/types.ts-79-  playCount?: number;
lib/types.ts-80-};
lib/types.ts-81-
lib/types.ts-82-export type AlbumLyricsBundle = {
lib/types.ts:83:  cuesByRecordingId: Record<string, LyricCue[]>;
lib/types.ts-84-  offsetByRecordingId: Record<string, number>;
lib/types.ts-85-};
lib/types.ts-86-
lib/types.ts-87-export type LyricGroupMapEntry = {
lib/types.ts-88-  canonicalGroupKey: string;
lib/types.ts-89-  updatedAt: string; // ISO string from DB
lib/types.ts-90-};
lib/types.ts-91-
lib/types.ts-92-export type LyricGroupMap = Record<string, LyricGroupMapEntry>; // keyed by lineKey
lib/types.ts-93-
lib/types.ts-94-export type LyricCue = {
lib/types.ts-95-  lineKey: string;
lib/types.ts-96-  tMs: number;
lib/types.ts-97-  text: string;
lib/types.ts-98-  endMs?: number;
lib/types.ts-99-
lib/types.ts-100-  /**
lib/types.ts-101-   * Optional enrichment: if this cue's lineKey is mapped into a canonical exegesis group.
lib/types.ts-102-   * Present when known; omitted otherwise.
lib/types.ts-103-   */
lib/types.ts-104-  canonicalGroupKey?: string;
lib/types.ts-105-};
lib/types.ts-106-
lib/types.ts-107-export type TrackLyricsApiOk = {
lib/types.ts-108-  ok: true;
lib/types.ts:109:  recordingId: string;
lib/types.ts:110:  offsetMs: number;
lib/types.ts:111:  cues: LyricCue[];
lib/types.ts-112-
lib/types.ts-113-  // present in API today, but optional for consumers
lib/types.ts-114-  version?: string;
lib/types.ts-115-  geniusUrl?: string | null;
lib/types.ts-116-  groupMap?: LyricGroupMap;
lib/types.ts-117-
lib/types.ts-118-  // meta fields the API returns (nullable)
lib/types.ts-119-  trackTitle?: string | null;
lib/types.ts-120-  trackArtist?: string | null;
lib/types.ts-121-  trackCatalogueId?: string | null;
lib/types.ts-122-  albumTitle?: string | null;
lib/types.ts-123-  albumSlug?: string | null;
lib/types.ts-124-  albumCatalogueId?: string | null;
lib/types.ts-125-};
--
lib/types.ts-182-      lineKey: c?._key,
lib/types.ts-183-      tMs: c?.tMs,
lib/types.ts-184-      text: c?.text,
lib/types.ts-185-      endMs: c?.endMs,
lib/types.ts-186-      // canonicalGroupKey not present in Sanity lyrics docs
lib/types.ts-187-    })),
lib/types.ts-188-  );
lib/types.ts-189-}
lib/types.ts-190-
lib/types.ts-191-export function parseTrackLyricsApiOk(raw: unknown): TrackLyricsApiOk | null {
lib/types.ts-192-  if (!raw || typeof raw !== "object") return null;
lib/types.ts-193-  const o = raw as Record<string, unknown>;
lib/types.ts-194-  if (o.ok !== true) return null;
lib/types.ts-195-
lib/types.ts:196:  if (typeof o.recordingId !== "string" || !o.recordingId.trim()) return null;
lib/types.ts:197:  if (typeof o.offsetMs !== "number" || !Number.isFinite(o.offsetMs))
lib/types.ts-198-    return null;
lib/types.ts-199-
lib/types.ts:200:  const cues = normalizeLyricCues(o.cues);
lib/types.ts-201-
lib/types.ts-202-  // groupMap is optional; keep it only if it looks like the right shape
lib/types.ts-203-  const groupMap =
lib/types.ts-204-    o.groupMap && typeof o.groupMap === "object" && !Array.isArray(o.groupMap)
lib/types.ts-205-      ? (o.groupMap as LyricGroupMap)
lib/types.ts-206-      : undefined;
lib/types.ts-207-
lib/types.ts-208-  const out: TrackLyricsApiOk = {
lib/types.ts-209-    ok: true,
lib/types.ts:210:    recordingId: o.recordingId.trim(),
lib/types.ts:211:    offsetMs: Math.floor(o.offsetMs),
lib/types.ts:212:    cues,
lib/types.ts-213-  };
lib/types.ts-214-
lib/types.ts-215-  // Optional fields (keep them if present, but don’t over-validate here)
lib/types.ts-216-  if (typeof o.version === "string" && o.version.trim())
lib/types.ts-217-    out.version = o.version.trim();
lib/types.ts-218-  if (typeof o.geniusUrl === "string" || o.geniusUrl === null)
lib/types.ts-219-    out.geniusUrl = o.geniusUrl as string | null;
lib/types.ts-220-  if (groupMap) out.groupMap = groupMap;
lib/types.ts-221-
lib/types.ts-222-  // Nullable meta fields (type-safe, no `any`)
lib/types.ts-223-  const nullableMetaKeys = [
lib/types.ts-224-    "trackTitle",
lib/types.ts-225-    "trackArtist",
lib/types.ts-226-    "trackCatalogueId",
--
lib/albums.ts-5-import type {
lib/albums.ts-6-  AlbumInfo,
lib/albums.ts-7-  PlayerTrack,
lib/albums.ts-8-  TierName,
lib/albums.ts-9-  LyricCue,
lib/albums.ts-10-  AlbumPlayerBundle,
lib/albums.ts-11-} from "@/lib/types";
lib/albums.ts-12-import {
lib/albums.ts-13-  makeAlbumPlayerBundle,
lib/albums.ts-14-  normalizeLyricCuesFromSanity,
lib/albums.ts-15-} from "@/lib/types";
lib/albums.ts-16-import { getRecordingPlayCountsByRecordingIds } from "@/lib/recordingListenTotals";
lib/albums.ts-17-
lib/albums.ts-18-export type AlbumDocTrack = {
lib/albums.ts:19:  recordingId: string;
lib/albums.ts-20-  displayId?: string;
lib/albums.ts-21-  title?: string;
lib/albums.ts-22-  artist?: string;
lib/albums.ts-23-  durationMs?: number;
lib/albums.ts-24-  muxPlaybackId?: string;
lib/albums.ts-25-  visualTheme?: string;
lib/albums.ts-26-  explicit?: boolean;
lib/albums.ts-27-};
lib/albums.ts-28-
lib/albums.ts-29-type AlbumDoc = {
lib/albums.ts-30-  _id?: string;
lib/albums.ts-31-  catalogueId?: string | null;
lib/albums.ts-32-  slug?: string | null;
lib/albums.ts-33-  title?: string;
--
lib/albums.ts-40-  publicPageVisible?: boolean;
lib/albums.ts-41-  releaseAt?: string;
lib/albums.ts-42-  embargoNote?: string;
lib/albums.ts-43-  earlyAccessEnabled?: boolean;
lib/albums.ts-44-  earlyAccessTiers?: string[];
lib/albums.ts-45-  minTierToLoad?: string;
lib/albums.ts-46-  platformLinks?: Array<{
lib/albums.ts-47-    platform?: string;
lib/albums.ts-48-    url?: string;
lib/albums.ts-49-  }>;
lib/albums.ts-50-  tracks?: AlbumDocTrack[];
lib/albums.ts-51-};
lib/albums.ts-52-
lib/albums.ts-53-type TrackLyricsDoc = {
lib/albums.ts:54:  recordingId?: string;
lib/albums.ts:55:  offsetMs?: number;
lib/albums.ts:56:  cues?: Array<{ _key?: string; tMs?: number; text?: string; endMs?: number }>;
lib/albums.ts-57-};
lib/albums.ts-58-
lib/albums.ts-59-export async function getFeaturedAlbumSlugFromSanity(): Promise<{
lib/albums.ts-60-  slug: string | null;
lib/albums.ts-61-  fallbackSlug: string | null;
lib/albums.ts-62-}> {
lib/albums.ts-63-  const res = await getCachedFeaturedAlbumSlugDoc();
lib/albums.ts-64-
lib/albums.ts-65-  return {
lib/albums.ts-66-    slug:
lib/albums.ts-67-      typeof res?.slug === "string" && res.slug.trim() ? res.slug.trim() : null,
lib/albums.ts-68-    fallbackSlug:
lib/albums.ts-69-      typeof res?.fallbackSlug === "string" && res.fallbackSlug.trim()
lib/albums.ts-70-        ? res.fallbackSlug.trim()
--
lib/albums.ts-138-}
lib/albums.ts-139-
lib/albums.ts-140-function uniqifyDisplayId(desired: string, used: Set<string>): string {
lib/albums.ts-141-  let d = desired;
lib/albums.ts-142-  let i = 2;
lib/albums.ts-143-  while (used.has(d)) {
lib/albums.ts-144-    d = `${desired}-${i}`;
lib/albums.ts-145-    i++;
lib/albums.ts-146-  }
lib/albums.ts-147-  used.add(d);
lib/albums.ts-148-  return d;
lib/albums.ts-149-}
lib/albums.ts-150-
lib/albums.ts-151-export type NormalizedAlbumTrack = {
lib/albums.ts:152:  recordingId: string;
lib/albums.ts-153-  displayId: string;
lib/albums.ts-154-  title?: string;
lib/albums.ts-155-  artist?: string;
lib/albums.ts-156-  muxPlaybackId?: string;
lib/albums.ts-157-  durationMs?: number;
lib/albums.ts-158-  visualTheme?: string;
lib/albums.ts-159-  explicit?: boolean;
lib/albums.ts-160-  trackNo: number;
lib/albums.ts-161-};
lib/albums.ts-162-
lib/albums.ts-163-export function normalizeAlbumTracks(
lib/albums.ts-164-  tracks: AlbumDocTrack[] | null | undefined,
lib/albums.ts-165-  args?: { albumTheme?: string | undefined },
lib/albums.ts-166-): NormalizedAlbumTrack[] {
lib/albums.ts-167-  const albumTheme = normTheme(args?.albumTheme);
lib/albums.ts-168-  if (!Array.isArray(tracks)) return [];
lib/albums.ts-169-
lib/albums.ts-170-  const used = new Set<string>();
lib/albums.ts-171-
lib/albums.ts-172-  return tracks
lib/albums.ts:173:    .filter((t) => typeof t?.recordingId === "string" && t.recordingId.trim())
lib/albums.ts-174-    .map((t, idx) => {
lib/albums.ts-175-      const rawDur = t.durationMs;
lib/albums.ts-176-      const dur =
lib/albums.ts-177-        typeof rawDur === "number" && Number.isFinite(rawDur)
lib/albums.ts-178-          ? rawDur
lib/albums.ts-179-          : undefined;
lib/albums.ts-180-
lib/albums.ts-181-      const trackNo = idx + 1;
lib/albums.ts:182:      const recordingId = t.recordingId.trim();
lib/albums.ts-183-
lib/albums.ts-184-      const wanted =
lib/albums.ts-185-        normStr(t.displayId) ?? fallbackDisplayId({ title: t.title, trackNo });
lib/albums.ts-186-
lib/albums.ts-187-      const displayId = uniqifyDisplayId(wanted, used);
lib/albums.ts-188-      const trackTheme = normTheme(t.visualTheme);
lib/albums.ts-189-
lib/albums.ts-190-      return {
lib/albums.ts:191:        recordingId,
lib/albums.ts-192-        displayId,
lib/albums.ts-193-        title: normStr(t.title),
lib/albums.ts-194-        artist: normStr(t.artist),
lib/albums.ts-195-        muxPlaybackId: normStr(t.muxPlaybackId),
lib/albums.ts-196-        durationMs: typeof dur === "number" && dur > 0 ? dur : undefined,
lib/albums.ts-197-        visualTheme: trackTheme ?? albumTheme,
lib/albums.ts-198-        explicit: t.explicit === true,
lib/albums.ts-199-        trackNo,
lib/albums.ts-200-      };
lib/albums.ts-201-    });
lib/albums.ts-202-}
lib/albums.ts-203-
lib/albums.ts-204-type FeaturedAlbumSlugDoc = {
lib/albums.ts-205-  slug?: string | null;
--
lib/albums.ts-228-    description,
lib/albums.ts-229-    artwork,
lib/albums.ts-230-    visualTheme,
lib/albums.ts-231-    publicPageVisible,
lib/albums.ts-232-    releaseAt,
lib/albums.ts-233-    embargoNote,
lib/albums.ts-234-    earlyAccessEnabled,
lib/albums.ts-235-    earlyAccessTiers,
lib/albums.ts-236-    minTierToLoad,
lib/albums.ts-237-    platformLinks[]{
lib/albums.ts-238-      platform,
lib/albums.ts-239-      url
lib/albums.ts-240-    },
lib/albums.ts-241-    "tracks": tracks[]{
lib/albums.ts:242:      recordingId,
lib/albums.ts-243-      displayId,
lib/albums.ts-244-      title,
lib/albums.ts-245-      artist,
lib/albums.ts-246-      durationMs,
lib/albums.ts-247-      muxPlaybackId,
lib/albums.ts-248-      visualTheme,
lib/albums.ts-249-      explicit
lib/albums.ts-250-    }
lib/albums.ts-251-  }
lib/albums.ts-252-`;
lib/albums.ts-253-
lib/albums.ts-254-const lyricsByRecordingIdsQuery = `
lib/albums.ts:255:  *[_type == "lyrics" && recordingId in $recordingIds]{
lib/albums.ts:256:    recordingId,
lib/albums.ts:257:    offsetMs,
lib/albums.ts:258:    cues[]{ _key, tMs, text, endMs }
lib/albums.ts-259-  }
lib/albums.ts-260-`;
lib/albums.ts-261-
lib/albums.ts-262-const albumBrowseQuery = `
lib/albums.ts-263-  *[_type=="album"]|order(year desc, _createdAt desc){
lib/albums.ts-264-    "id": _id,
lib/albums.ts-265-    "catalogueId": catalogueId,
lib/albums.ts-266-    "slug": slug.current,
lib/albums.ts-267-    title,
lib/albums.ts-268-    artist,
lib/albums.ts-269-    year,
lib/albums.ts-270-    artwork,
lib/albums.ts-271-    publicPageVisible,
lib/albums.ts-272-    minTierToLoad
--
lib/albums.ts-285-);
lib/albums.ts-286-
lib/albums.ts-287-const getCachedAlbumDocumentBySlug = unstable_cache(
lib/albums.ts-288-  async (slug: string): Promise<AlbumDoc | null> => {
lib/albums.ts-289-    return client.fetch<AlbumDoc | null>(albumBySlugQuery, { slug });
lib/albums.ts-290-  },
lib/albums.ts-291-  ["album-document-by-slug-v1"],
lib/albums.ts-292-  {
lib/albums.ts-293-    revalidate: STATIC_CATALOGUE_REVALIDATE_SECONDS,
lib/albums.ts-294-    tags: ["albums"],
lib/albums.ts-295-  },
lib/albums.ts-296-);
lib/albums.ts-297-
lib/albums.ts-298-const getCachedLyricsByRecordingIds = unstable_cache(
lib/albums.ts:299:  async (recordingIds: string[]): Promise<TrackLyricsDoc[]> => {
lib/albums.ts:300:    if (recordingIds.length === 0) return [];
lib/albums.ts-301-
lib/albums.ts-302-    const docs = await client.fetch<TrackLyricsDoc[]>(
lib/albums.ts-303-      lyricsByRecordingIdsQuery,
lib/albums.ts:304:      { recordingIds },
lib/albums.ts-305-    );
lib/albums.ts-306-
lib/albums.ts-307-    return Array.isArray(docs) ? docs : [];
lib/albums.ts-308-  },
lib/albums.ts-309-  ["album-lyrics-by-recording-ids-v1"],
lib/albums.ts-310-  {
lib/albums.ts-311-    revalidate: STATIC_CATALOGUE_REVALIDATE_SECONDS,
lib/albums.ts-312-    tags: ["lyrics"],
lib/albums.ts-313-  },
lib/albums.ts-314-);
lib/albums.ts-315-
lib/albums.ts-316-const getCachedAlbumBrowseDocs = unstable_cache(
lib/albums.ts-317-  async (): Promise<AlbumBrowseItem[]> => {
lib/albums.ts-318-    const docs = await client.fetch<AlbumBrowseItem[]>(albumBrowseQuery);
--
lib/albums.ts-323-    revalidate: STATIC_CATALOGUE_REVALIDATE_SECONDS,
lib/albums.ts-324-    tags: ["albums"],
lib/albums.ts-325-  },
lib/albums.ts-326-);
lib/albums.ts-327-
lib/albums.ts-328-export type AlbumPlaybackAssetVerification = {
lib/albums.ts-329-  ok: boolean;
lib/albums.ts-330-  albumId: string;
lib/albums.ts-331-  playbackId: string;
lib/albums.ts-332-  trackId: string | null;
lib/albums.ts-333-  matchedRecordingId: string | null;
lib/albums.ts-334-};
lib/albums.ts-335-
lib/albums.ts-336-export type AlbumPlaybackSessionAsset = {
lib/albums.ts:337:  recordingId: string;
lib/albums.ts-338-  displayId: string;
lib/albums.ts-339-  playbackId: string;
lib/albums.ts-340-  durationMs?: number;
lib/albums.ts-341-};
lib/albums.ts-342-
lib/albums.ts-343-function normalizeAlbumScopeId(raw: string): string {
lib/albums.ts-344-  let s = (raw ?? "").trim();
lib/albums.ts-345-  while (s.startsWith("alb:")) s = s.slice(4);
lib/albums.ts-346-  return s.trim();
lib/albums.ts-347-}
lib/albums.ts-348-
lib/albums.ts-349-export async function getAlbumPlaybackAssetsForSession(params: {
lib/albums.ts-350-  albumId: string;
lib/albums.ts-351-}): Promise<{
--
lib/albums.ts-365-    };
lib/albums.ts-366-  }
lib/albums.ts-367-
lib/albums.ts-368-  const q = `
lib/albums.ts-369-    *[
lib/albums.ts-370-      _type == "album" &&
lib/albums.ts-371-      (
lib/albums.ts-372-        _id == $albumId ||
lib/albums.ts-373-        catalogueId == $albumId
lib/albums.ts-374-      )
lib/albums.ts-375-    ][0]{
lib/albums.ts-376-      _id,
lib/albums.ts-377-      catalogueId,
lib/albums.ts-378-      "tracks": tracks[
lib/albums.ts:379:        defined(recordingId) &&
lib/albums.ts-380-        defined(displayId) &&
lib/albums.ts-381-        defined(muxPlaybackId)
lib/albums.ts-382-      ]{
lib/albums.ts:383:        recordingId,
lib/albums.ts-384-        displayId,
lib/albums.ts-385-        muxPlaybackId,
lib/albums.ts-386-        durationMs
lib/albums.ts-387-      }
lib/albums.ts-388-    }
lib/albums.ts-389-  `;
lib/albums.ts-390-
lib/albums.ts-391-  const doc = await client.fetch<{
lib/albums.ts-392-    _id?: string;
lib/albums.ts-393-    catalogueId?: string | null;
lib/albums.ts-394-    tracks?: Array<{
lib/albums.ts:395:      recordingId?: string;
lib/albums.ts-396-      displayId?: string;
lib/albums.ts-397-      muxPlaybackId?: string;
lib/albums.ts-398-      durationMs?: number;
lib/albums.ts-399-    }>;
lib/albums.ts-400-  } | null>(q, { albumId }, { next: { tags: ["albums"] } });
lib/albums.ts-401-
lib/albums.ts-402-  const resolvedAlbumId = normStr(doc?.catalogueId) ?? normStr(doc?._id) ?? "";
lib/albums.ts-403-  const albumScopeId = resolvedAlbumId ? `alb:${resolvedAlbumId}` : "";
lib/albums.ts-404-
lib/albums.ts-405-  const tracks: AlbumPlaybackSessionAsset[] = [];
lib/albums.ts-406-
lib/albums.ts-407-  if (Array.isArray(doc?.tracks)) {
lib/albums.ts-408-    for (const t of doc.tracks) {
lib/albums.ts:409:      const recordingId = normStr(t.recordingId);
lib/albums.ts-410-      const displayId = normStr(t.displayId);
lib/albums.ts-411-      const playbackId = normStr(t.muxPlaybackId);
lib/albums.ts-412-
lib/albums.ts:413:      if (!recordingId || !displayId || !playbackId) continue;
lib/albums.ts-414-
lib/albums.ts-415-      const durationMs =
lib/albums.ts-416-        typeof t.durationMs === "number" &&
lib/albums.ts-417-        Number.isFinite(t.durationMs) &&
lib/albums.ts-418-        t.durationMs > 0
lib/albums.ts-419-          ? Math.floor(t.durationMs)
lib/albums.ts-420-          : undefined;
lib/albums.ts-421-
lib/albums.ts-422-      tracks.push({
lib/albums.ts:423:        recordingId,
lib/albums.ts-424-        displayId,
lib/albums.ts-425-        playbackId,
lib/albums.ts-426-        ...(typeof durationMs === "number" ? { durationMs } : {}),
lib/albums.ts-427-      });
lib/albums.ts-428-    }
lib/albums.ts-429-  }
lib/albums.ts-430-
lib/albums.ts-431-  return {
lib/albums.ts-432-    ok: Boolean(resolvedAlbumId && tracks.length > 0),
lib/albums.ts-433-    albumId: resolvedAlbumId,
lib/albums.ts-434-    albumScopeId,
lib/albums.ts-435-    tracks,
lib/albums.ts-436-  };
lib/albums.ts-437-}
--
lib/albums.ts-456-  }
lib/albums.ts-457-
lib/albums.ts-458-  const q = `
lib/albums.ts-459-    *[
lib/albums.ts-460-      _type == "album" &&
lib/albums.ts-461-      (
lib/albums.ts-462-        _id == $albumId ||
lib/albums.ts-463-        catalogueId == $albumId
lib/albums.ts-464-      )
lib/albums.ts-465-    ][0]{
lib/albums.ts-466-      "matches": tracks[
lib/albums.ts-467-        defined(muxPlaybackId) &&
lib/albums.ts-468-        muxPlaybackId == $playbackId
lib/albums.ts-469-      ]{
lib/albums.ts:470:        recordingId,
lib/albums.ts-471-        displayId,
lib/albums.ts-472-        muxPlaybackId
lib/albums.ts-473-      }
lib/albums.ts-474-    }
lib/albums.ts-475-  `;
lib/albums.ts-476-
lib/albums.ts-477-  const doc = await client.fetch<{
lib/albums.ts-478-    matches?: Array<{
lib/albums.ts:479:      recordingId?: string;
lib/albums.ts-480-      displayId?: string;
lib/albums.ts-481-      muxPlaybackId?: string;
lib/albums.ts-482-    }>;
lib/albums.ts-483-  } | null>(q, { albumId, playbackId }, { next: { tags: ["albums"] } });
lib/albums.ts-484-
lib/albums.ts-485-  const matches = Array.isArray(doc?.matches) ? doc.matches : [];
lib/albums.ts-486-
lib/albums.ts-487-  const matched = matches.find((t) => {
lib/albums.ts:488:    const recordingId = normStr(t.recordingId);
lib/albums.ts-489-    const displayId = normStr(t.displayId);
lib/albums.ts-490-    const muxPlaybackId = normStr(t.muxPlaybackId);
lib/albums.ts-491-
lib/albums.ts-492-    if (muxPlaybackId !== playbackId) return false;
lib/albums.ts-493-    if (!trackId) return true;
lib/albums.ts-494-
lib/albums.ts:495:    return recordingId === trackId || displayId === trackId;
lib/albums.ts-496-  });
lib/albums.ts-497-
lib/albums.ts-498-  return {
lib/albums.ts-499-    ok: Boolean(matched),
lib/albums.ts-500-    albumId,
lib/albums.ts-501-    playbackId,
lib/albums.ts-502-    trackId,
lib/albums.ts:503:    matchedRecordingId: normStr(matched?.recordingId) ?? null,
lib/albums.ts-504-  };
lib/albums.ts-505-}
lib/albums.ts-506-
lib/albums.ts-507-export async function getAlbumBySlug(slug: string): Promise<AlbumPlayerBundle> {
lib/albums.ts-508-  const albumSlug = (slug ?? "").trim().toLowerCase();
lib/albums.ts-509-
lib/albums.ts-510-  if (!albumSlug) {
lib/albums.ts-511-    return makeAlbumPlayerBundle({
lib/albums.ts-512-      albumSlug: "",
lib/albums.ts-513-      album: null,
lib/albums.ts-514-      tracks: [],
lib/albums.ts-515-      albumLyrics: null,
lib/albums.ts-516-    });
lib/albums.ts-517-  }
--
lib/albums.ts-570-      earlyAccessEnabled: !!doc.earlyAccessEnabled,
lib/albums.ts-571-      earlyAccessTiers: parseTierNameArray(doc.earlyAccessTiers),
lib/albums.ts-572-      minTierToLoad: parseTierName(doc.minTierToLoad),
lib/albums.ts-573-    },
lib/albums.ts-574-    embargo: {
lib/albums.ts-575-      embargoed: isEmbargoedByDate,
lib/albums.ts-576-      releaseAt,
lib/albums.ts-577-      note: embargoNote,
lib/albums.ts-578-    },
lib/albums.ts-579-  };
lib/albums.ts-580-
lib/albums.ts-581-  const tracks: PlayerTrack[] = normalizeAlbumTracks(doc.tracks, {
lib/albums.ts-582-    albumTheme,
lib/albums.ts-583-  }).map((t) => ({
lib/albums.ts:584:    recordingId: t.recordingId,
lib/albums.ts-585-    displayId: t.displayId,
lib/albums.ts-586-    title: t.title,
lib/albums.ts-587-    artist: t.artist,
lib/albums.ts-588-    muxPlaybackId: t.muxPlaybackId,
lib/albums.ts-589-    durationMs: t.durationMs,
lib/albums.ts-590-    visualTheme: t.visualTheme,
lib/albums.ts-591-    explicit: t.explicit,
lib/albums.ts-592-  }));
lib/albums.ts-593-
lib/albums.ts:594:  const recordingIds = tracks
lib/albums.ts:595:    .map((t) => t.recordingId)
lib/albums.ts:596:    .filter((recordingId): recordingId is string => recordingId.length > 0);
lib/albums.ts-597-
lib/albums.ts:598:  const lyricRecordingIds = Array.from(new Set(recordingIds)).sort((a, b) =>
lib/albums.ts-599-    a.localeCompare(b),
lib/albums.ts-600-  );
lib/albums.ts-601-
lib/albums.ts-602-  const [playCountsByRecordingId, lyricDocs] = await Promise.all([
lib/albums.ts:603:    getRecordingPlayCountsByRecordingIds(recordingIds),
lib/albums.ts-604-    getCachedLyricsByRecordingIds(lyricRecordingIds),
lib/albums.ts-605-  ]);
lib/albums.ts-606-
lib/albums.ts-607-  const tracksWithPlayCounts: PlayerTrack[] = tracks.map((t) => ({
lib/albums.ts-608-    ...t,
lib/albums.ts:609:    playCount: playCountsByRecordingId[t.recordingId] ?? 0,
lib/albums.ts-610-  }));
lib/albums.ts-611-
lib/albums.ts:612:  const cuesByRecordingId: Record<string, LyricCue[]> = {};
lib/albums.ts-613-  const offsetByRecordingId: Record<string, number> = {};
lib/albums.ts-614-
lib/albums.ts-615-  for (const d of lyricDocs) {
lib/albums.ts:616:    const id = (d.recordingId ?? "").trim();
lib/albums.ts-617-    if (!id) continue;
lib/albums.ts-618-
lib/albums.ts:619:    cuesByRecordingId[id] = normalizeLyricCuesFromSanity(d.cues);
lib/albums.ts-620-    offsetByRecordingId[id] =
lib/albums.ts:621:      typeof d.offsetMs === "number" && Number.isFinite(d.offsetMs)
lib/albums.ts:622:        ? Math.floor(d.offsetMs)
lib/albums.ts-623-        : 0;
lib/albums.ts-624-  }
lib/albums.ts-625-
lib/albums.ts-626-  return makeAlbumPlayerBundle({
lib/albums.ts-627-    albumSlug: normStr(doc.slug) ?? albumSlug,
lib/albums.ts-628-    album,
lib/albums.ts-629-    tracks: tracksWithPlayCounts,
lib/albums.ts:630:    albumLyrics: { cuesByRecordingId, offsetByRecordingId },
lib/albums.ts-631-  });
lib/albums.ts-632-}
lib/albums.ts-633-
lib/albums.ts-634-export type AlbumCanonicalMetadata = {
lib/albums.ts-635-  title: string | null;
lib/albums.ts-636-  displayTitle: string | null;
lib/albums.ts-637-  slug: string | null;
lib/albums.ts-638-};
lib/albums.ts-639-
lib/albums.ts-640-export type TrackCanonicalMetadata = {
lib/albums.ts-641-  albumTitle: string | null;
lib/albums.ts-642-  albumDisplayTitle: string | null;
lib/albums.ts-643-  albumSlug: string | null;
lib/albums.ts-644-  trackTitle: string | null;
--
lib/albums.ts-694-    catalogueId: normStr(a.catalogueId) ?? null,
lib/albums.ts-695-    artist: normStr(a.artist),
lib/albums.ts-696-    title: a.title ?? "Untitled",
lib/albums.ts-697-    artworkUrl: a.artwork
lib/albums.ts-698-      ? urlFor(a.artwork).width(600).height(600).quality(80).url()
lib/albums.ts-699-      : null,
lib/albums.ts-700-    policy: {
lib/albums.ts-701-      publicPageVisible: a.publicPageVisible !== false,
lib/albums.ts-702-      minTierToLoad: parseTierName(a.minTierToLoad),
lib/albums.ts-703-    },
lib/albums.ts-704-  }));
lib/albums.ts-705-}
lib/albums.ts-706-
lib/albums.ts-707-export type RecordingSearchResult = {
lib/albums.ts:708:  recordingId: string;
lib/albums.ts-709-  title: string;
lib/albums.ts-710-  artist?: string | null;
lib/albums.ts-711-  albumSlug?: string | null;
lib/albums.ts-712-  albumTitle?: string | null;
lib/albums.ts-713-};
lib/albums.ts-714-
lib/albums.ts-715-export async function searchRecordingsForAdmin(args: {
lib/albums.ts-716-  query: string;
lib/albums.ts-717-  limit?: number;
lib/albums.ts-718-}): Promise<RecordingSearchResult[]> {
lib/albums.ts-719-  const rawQuery = normStr(args.query);
lib/albums.ts-720-  if (!rawQuery) return [];
lib/albums.ts-721-
lib/albums.ts-722-  const limit = Math.max(1, Math.min(25, Math.floor(args.limit ?? 12)));
lib/albums.ts-723-
lib/albums.ts-724-  const wildcard = `*${rawQuery}*`;
lib/albums.ts-725-  const exactId = rawQuery;
lib/albums.ts-726-
lib/albums.ts-727-  const q = `
lib/albums.ts-728-    *[
lib/albums.ts-729-      _type == "album" &&
lib/albums.ts-730-      count(
lib/albums.ts-731-        tracks[
lib/albums.ts:732:          defined(recordingId) && (
lib/albums.ts:733:            recordingId == $exactId ||
lib/albums.ts:734:            recordingId match $wildcard ||
lib/albums.ts-735-            title match $wildcard ||
lib/albums.ts-736-            artist match $wildcard
lib/albums.ts-737-          )
lib/albums.ts-738-        ]
lib/albums.ts-739-      ) > 0
lib/albums.ts-740-    ] | order(title asc) {
lib/albums.ts-741-      "albumSlug": slug.current,
lib/albums.ts-742-      "albumTitle": title,
lib/albums.ts-743-      "albumArtist": artist,
lib/albums.ts-744-      "matches": tracks[
lib/albums.ts:745:        defined(recordingId) && (
lib/albums.ts:746:          recordingId == $exactId ||
lib/albums.ts:747:          recordingId match $wildcard ||
lib/albums.ts-748-          title match $wildcard ||
lib/albums.ts-749-          artist match $wildcard
lib/albums.ts-750-        )
lib/albums.ts-751-      ]{
lib/albums.ts:752:        recordingId,
lib/albums.ts-753-        title,
lib/albums.ts-754-        artist
lib/albums.ts-755-      }
lib/albums.ts-756-    }
lib/albums.ts-757-  `;
lib/albums.ts-758-
lib/albums.ts-759-  const docs = await client.fetch<
lib/albums.ts-760-    Array<{
lib/albums.ts-761-      albumSlug?: string;
lib/albums.ts-762-      albumTitle?: string;
lib/albums.ts-763-      albumArtist?: string;
lib/albums.ts-764-      matches?: Array<{
lib/albums.ts:765:        recordingId?: string;
lib/albums.ts-766-        title?: string;
lib/albums.ts-767-        artist?: string;
lib/albums.ts-768-      }>;
lib/albums.ts-769-    }>
lib/albums.ts-770-  >(q, {
lib/albums.ts-771-    exactId,
lib/albums.ts-772-    wildcard,
lib/albums.ts-773-  });
lib/albums.ts-774-
lib/albums.ts-775-  const flattened: RecordingSearchResult[] = [];
lib/albums.ts-776-  const seen = new Set<string>();
lib/albums.ts-777-
lib/albums.ts-778-  for (const doc of Array.isArray(docs) ? docs : []) {
lib/albums.ts-779-    const albumSlug = normStr(doc?.albumSlug) ?? null;
lib/albums.ts-780-    const albumTitle = normStr(doc?.albumTitle) ?? null;
lib/albums.ts-781-    const albumArtist = normStr(doc?.albumArtist) ?? null;
lib/albums.ts-782-
lib/albums.ts-783-    for (const match of Array.isArray(doc?.matches) ? doc.matches : []) {
lib/albums.ts:784:      const recordingId = normStr(match?.recordingId);
lib/albums.ts-785-      const title = normStr(match?.title);
lib/albums.ts-786-
lib/albums.ts:787:      if (!recordingId || !title || seen.has(recordingId)) continue;
lib/albums.ts:788:      seen.add(recordingId);
lib/albums.ts-789-
lib/albums.ts-790-      flattened.push({
lib/albums.ts:791:        recordingId,
lib/albums.ts-792-        title,
lib/albums.ts-793-        artist: normStr(match?.artist) ?? albumArtist,
lib/albums.ts-794-        albumSlug,
lib/albums.ts-795-        albumTitle,
lib/albums.ts-796-      });
lib/albums.ts-797-    }
lib/albums.ts-798-  }
lib/albums.ts-799-
lib/albums.ts-800-  const normalizedQuery = rawQuery.toLowerCase();
lib/albums.ts-801-
lib/albums.ts-802-  flattened.sort((a, b) => {
lib/albums.ts:803:    const aId = a.recordingId.toLowerCase();
lib/albums.ts:804:    const bId = b.recordingId.toLowerCase();
lib/albums.ts-805-    const aTitle = a.title.toLowerCase();
lib/albums.ts-806-    const bTitle = b.title.toLowerCase();
lib/albums.ts-807-
lib/albums.ts-808-    const aExact = aId === normalizedQuery ? 1 : 0;
lib/albums.ts-809-    const bExact = bId === normalizedQuery ? 1 : 0;
lib/albums.ts-810-    if (aExact !== bExact) return bExact - aExact;
lib/albums.ts-811-
lib/albums.ts-812-    const aTitleStarts = aTitle.startsWith(normalizedQuery) ? 1 : 0;
lib/albums.ts-813-    const bTitleStarts = bTitle.startsWith(normalizedQuery) ? 1 : 0;
lib/albums.ts-814-    if (aTitleStarts !== bTitleStarts) return bTitleStarts - aTitleStarts;
lib/albums.ts-815-
lib/albums.ts-816-    const aTitleIncludes = aTitle.includes(normalizedQuery) ? 1 : 0;
lib/albums.ts-817-    const bTitleIncludes = bTitle.includes(normalizedQuery) ? 1 : 0;
lib/albums.ts-818-    if (aTitleIncludes !== bTitleIncludes) {
lib/albums.ts-819-      return bTitleIncludes - aTitleIncludes;
lib/albums.ts-820-    }
lib/albums.ts-821-
lib/albums.ts-822-    return a.title.localeCompare(b.title);
lib/albums.ts-823-  });
lib/albums.ts-824-
lib/albums.ts-825-  return flattened.slice(0, limit);
lib/albums.ts-826-}
lib/albums.ts-827-
lib/albums.ts-828-export async function getRecordingSummaryByRecordingId(
lib/albums.ts:829:  recordingId: string,
lib/albums.ts-830-): Promise<RecordingSummary | null> {
lib/albums.ts:831:  const id = normStr(recordingId);
lib/albums.ts-832-  if (!id) return null;
lib/albums.ts-833-
lib/albums.ts-834-  const q = `
lib/albums.ts:835:        *[_type == "album" && count(tracks[recordingId == $recordingId]) > 0][0]{
lib/albums.ts-836-      "albumCatalogueId": catalogueId,
lib/albums.ts-837-      "albumSlug": slug.current,
lib/albums.ts-838-      "albumTitle": title,
lib/albums.ts-839-      "albumArtist": artist,
lib/albums.ts:840:      "track": tracks[recordingId == $recordingId][0]{
lib/albums.ts:841:        recordingId,
lib/albums.ts-842-        title,
lib/albums.ts-843-        artist
lib/albums.ts-844-      }
lib/albums.ts-845-    }
lib/albums.ts-846-  `;
lib/albums.ts-847-
lib/albums.ts-848-  const doc = await client.fetch<{
lib/albums.ts-849-    albumCatalogueId?: string;
lib/albums.ts-850-    albumSlug?: string;
lib/albums.ts-851-    albumTitle?: string;
lib/albums.ts-852-    albumArtist?: string;
lib/albums.ts-853-    track?: {
lib/albums.ts:854:      recordingId?: string;
lib/albums.ts-855-      title?: string;
lib/albums.ts-856-      artist?: string;
lib/albums.ts-857-    };
lib/albums.ts:858:  } | null>(q, { recordingId: id });
lib/albums.ts-859-
lib/albums.ts:860:  const trackRecordingId = normStr(doc?.track?.recordingId);
lib/albums.ts-861-  const trackTitle = normStr(doc?.track?.title);
lib/albums.ts-862-  if (!trackRecordingId || !trackTitle) return null;
lib/albums.ts-863-
lib/albums.ts-864-  return {
lib/albums.ts:865:    recordingId: trackRecordingId,
lib/albums.ts-866-    title: trackTitle,
lib/albums.ts-867-    artist: normStr(doc?.track?.artist) ?? normStr(doc?.albumArtist) ?? null,
lib/albums.ts-868-    albumCatalogueId: normStr(doc?.albumCatalogueId) ?? null,
lib/albums.ts-869-    albumSlug: normStr(doc?.albumSlug) ?? null,
lib/albums.ts-870-    albumTitle: normStr(doc?.albumTitle) ?? null,
lib/albums.ts-871-  };
lib/albums.ts-872-}
lib/albums.ts-873-
lib/albums.ts-874-export type RecordingSummary = {
lib/albums.ts:875:  recordingId: string;
lib/albums.ts-876-  title: string;
lib/albums.ts-877-  artist?: string | null;
lib/albums.ts-878-  albumCatalogueId?: string | null;
lib/albums.ts-879-  albumSlug?: string | null;
lib/albums.ts-880-  albumTitle?: string | null;
lib/albums.ts-881-};
lib/albums.ts-882-
lib/albums.ts-883-export type AlbumEmailMeta = {
lib/albums.ts-884-  slug: string;
lib/albums.ts-885-  title: string;
lib/albums.ts-886-  artist?: string;
lib/albums.ts-887-  artworkUrl?: string | null;
lib/albums.ts-888-};
lib/albums.ts-889-

===== LYRICS SURFACE STATE / HYDRATION CONTRACT =====

lib/types.ts-69-
lib/types.ts-70-export type PlayerTrack = {
lib/types.ts-71-  recordingId: string;
lib/types.ts-72-  displayId: string;
lib/types.ts-73-  title?: string;
lib/types.ts-74-  artist?: string;
lib/types.ts-75-  durationMs?: number;
lib/types.ts-76-  muxPlaybackId?: string;
lib/types.ts-77-  visualTheme?: string;
lib/types.ts-78-  explicit?: boolean;
lib/types.ts-79-  playCount?: number;
lib/types.ts-80-};
lib/types.ts-81-
lib/types.ts-82-export type AlbumLyricsBundle = {
lib/types.ts:83:  cuesByRecordingId: Record<string, LyricCue[]>;
lib/types.ts:84:  offsetByRecordingId: Record<string, number>;
lib/types.ts-85-};
lib/types.ts-86-
lib/types.ts-87-export type LyricGroupMapEntry = {
lib/types.ts-88-  canonicalGroupKey: string;
lib/types.ts-89-  updatedAt: string; // ISO string from DB
lib/types.ts-90-};
lib/types.ts-91-
lib/types.ts-92-export type LyricGroupMap = Record<string, LyricGroupMapEntry>; // keyed by lineKey
lib/types.ts-93-
lib/types.ts-94-export type LyricCue = {
lib/types.ts-95-  lineKey: string;
lib/types.ts-96-  tMs: number;
lib/types.ts-97-  text: string;
lib/types.ts-98-  endMs?: number;
--
lib/albums.ts-598-  const lyricRecordingIds = Array.from(new Set(recordingIds)).sort((a, b) =>
lib/albums.ts-599-    a.localeCompare(b),
lib/albums.ts-600-  );
lib/albums.ts-601-
lib/albums.ts-602-  const [playCountsByRecordingId, lyricDocs] = await Promise.all([
lib/albums.ts-603-    getRecordingPlayCountsByRecordingIds(recordingIds),
lib/albums.ts-604-    getCachedLyricsByRecordingIds(lyricRecordingIds),
lib/albums.ts-605-  ]);
lib/albums.ts-606-
lib/albums.ts-607-  const tracksWithPlayCounts: PlayerTrack[] = tracks.map((t) => ({
lib/albums.ts-608-    ...t,
lib/albums.ts-609-    playCount: playCountsByRecordingId[t.recordingId] ?? 0,
lib/albums.ts-610-  }));
lib/albums.ts-611-
lib/albums.ts:612:  const cuesByRecordingId: Record<string, LyricCue[]> = {};
lib/albums.ts:613:  const offsetByRecordingId: Record<string, number> = {};
lib/albums.ts-614-
lib/albums.ts-615-  for (const d of lyricDocs) {
lib/albums.ts-616-    const id = (d.recordingId ?? "").trim();
lib/albums.ts-617-    if (!id) continue;
lib/albums.ts-618-
lib/albums.ts:619:    cuesByRecordingId[id] = normalizeLyricCuesFromSanity(d.cues);
lib/albums.ts:620:    offsetByRecordingId[id] =
lib/albums.ts-621-      typeof d.offsetMs === "number" && Number.isFinite(d.offsetMs)
lib/albums.ts-622-        ? Math.floor(d.offsetMs)
lib/albums.ts-623-        : 0;
lib/albums.ts-624-  }
lib/albums.ts-625-
lib/albums.ts-626-  return makeAlbumPlayerBundle({
lib/albums.ts-627-    albumSlug: normStr(doc.slug) ?? albumSlug,
lib/albums.ts-628-    album,
lib/albums.ts-629-    tracks: tracksWithPlayCounts,
lib/albums.ts:630:    albumLyrics: { cuesByRecordingId, offsetByRecordingId },
lib/albums.ts-631-  });
lib/albums.ts-632-}
lib/albums.ts-633-
lib/albums.ts-634-export type AlbumCanonicalMetadata = {
lib/albums.ts-635-  title: string | null;
lib/albums.ts-636-  displayTitle: string | null;
lib/albums.ts-637-  slug: string | null;
lib/albums.ts-638-};
lib/albums.ts-639-
lib/albums.ts-640-export type TrackCanonicalMetadata = {
lib/albums.ts-641-  albumTitle: string | null;
lib/albums.ts-642-  albumDisplayTitle: string | null;
lib/albums.ts-643-  albumSlug: string | null;
lib/albums.ts-644-  trackTitle: string | null;
--
app/home/player/lyrics/useLyricsSurface.ts-1-//web/app/home/player/lyrics/useLyricsSurface.ts
app/home/player/lyrics/useLyricsSurface.ts-2-"use client";
app/home/player/lyrics/useLyricsSurface.ts-3-
app/home/player/lyrics/useLyricsSurface.ts-4-import React from "react";
app/home/player/lyrics/useLyricsSurface.ts-5-import { lyricsSurface } from "./lyricsSurface";
app/home/player/lyrics/useLyricsSurface.ts-6-
app/home/player/lyrics/useLyricsSurface.ts-7-export function useLyricsSnapshot() {
app/home/player/lyrics/useLyricsSurface.ts-8-  return React.useSyncExternalStore(
app/home/player/lyrics/useLyricsSurface.ts-9-    lyricsSurface.subscribe,
app/home/player/lyrics/useLyricsSurface.ts:10:    lyricsSurface.getSnapshot,
app/home/player/lyrics/useLyricsSurface.ts:11:    lyricsSurface.getSnapshot,
app/home/player/lyrics/useLyricsSurface.ts-12-  );
app/home/player/lyrics/useLyricsSurface.ts-13-}
--
app/home/player/lyrics/lyricsSurface.ts-1-// web/app/home/player/lyrics/lyricsSurface.ts
app/home/player/lyrics/lyricsSurface.ts-2-"use client";
app/home/player/lyrics/lyricsSurface.ts-3-
app/home/player/lyrics/lyricsSurface.ts-4-import type { LyricCue } from "@/lib/types";
app/home/player/lyrics/lyricsSurface.ts-5-
app/home/player/lyrics/lyricsSurface.ts-6-export type CuesByrecordingId = Record<string, LyricCue[]>;
app/home/player/lyrics/lyricsSurface.ts-7-export type OffsetByrecordingId = Record<string, number>;
app/home/player/lyrics/lyricsSurface.ts-8-
app/home/player/lyrics/lyricsSurface.ts-9-type Snapshot = {
app/home/player/lyrics/lyricsSurface.ts:10:  cuesByRecordingId: CuesByrecordingId;
app/home/player/lyrics/lyricsSurface.ts:11:  offsetByRecordingId: OffsetByrecordingId;
app/home/player/lyrics/lyricsSurface.ts:12:  globalOffsetMs: number;
app/home/player/lyrics/lyricsSurface.ts-13-};
app/home/player/lyrics/lyricsSurface.ts-14-
app/home/player/lyrics/lyricsSurface.ts-15-type Listener = () => void;
app/home/player/lyrics/lyricsSurface.ts-16-
app/home/player/lyrics/lyricsSurface.ts-17-let snap: Snapshot = {
app/home/player/lyrics/lyricsSurface.ts:18:  cuesByRecordingId: {},
app/home/player/lyrics/lyricsSurface.ts:19:  offsetByRecordingId: {},
app/home/player/lyrics/lyricsSurface.ts:20:  globalOffsetMs: 0,
app/home/player/lyrics/lyricsSurface.ts-21-};
app/home/player/lyrics/lyricsSurface.ts-22-
app/home/player/lyrics/lyricsSurface.ts-23-const listeners = new Set<Listener>();
app/home/player/lyrics/lyricsSurface.ts-24-
app/home/player/lyrics/lyricsSurface.ts-25-function emit() {
app/home/player/lyrics/lyricsSurface.ts-26-  for (const fn of listeners) fn();
app/home/player/lyrics/lyricsSurface.ts-27-}
app/home/player/lyrics/lyricsSurface.ts-28-
app/home/player/lyrics/lyricsSurface.ts-29-export const lyricsSurface = {
app/home/player/lyrics/lyricsSurface.ts-30-  getSnapshot(): Snapshot {
app/home/player/lyrics/lyricsSurface.ts-31-    return snap;
app/home/player/lyrics/lyricsSurface.ts-32-  },
app/home/player/lyrics/lyricsSurface.ts-33-
app/home/player/lyrics/lyricsSurface.ts-34-  subscribe(fn: Listener): () => void {
app/home/player/lyrics/lyricsSurface.ts-35-    listeners.add(fn);
app/home/player/lyrics/lyricsSurface.ts-36-    return () => listeners.delete(fn);
app/home/player/lyrics/lyricsSurface.ts-37-  },
app/home/player/lyrics/lyricsSurface.ts-38-
app/home/player/lyrics/lyricsSurface.ts-39-  setMaps(next: {
app/home/player/lyrics/lyricsSurface.ts:40:    cuesByrecordingId?: CuesByrecordingId | null;
app/home/player/lyrics/lyricsSurface.ts:41:    offsetByrecordingId?: OffsetByrecordingId | null;
app/home/player/lyrics/lyricsSurface.ts:42:    globalOffsetMs?: number | null;
app/home/player/lyrics/lyricsSurface.ts-43-  }) {
app/home/player/lyrics/lyricsSurface.ts:44:    const cuesByRecordingId = next.cuesByrecordingId ?? {};
app/home/player/lyrics/lyricsSurface.ts:45:    const offsetByRecordingId = next.offsetByrecordingId ?? {};
app/home/player/lyrics/lyricsSurface.ts:46:    const globalOffsetMs =
app/home/player/lyrics/lyricsSurface.ts:47:      typeof next.globalOffsetMs === "number" && Number.isFinite(next.globalOffsetMs)
app/home/player/lyrics/lyricsSurface.ts:48:        ? next.globalOffsetMs
app/home/player/lyrics/lyricsSurface.ts-49-        : 0;
app/home/player/lyrics/lyricsSurface.ts-50-
app/home/player/lyrics/lyricsSurface.ts-51-    // cheap identity guard: only emit if something actually changes by reference/value
app/home/player/lyrics/lyricsSurface.ts-52-    const changed =
app/home/player/lyrics/lyricsSurface.ts:53:      snap.cuesByRecordingId !== cuesByRecordingId ||
app/home/player/lyrics/lyricsSurface.ts:54:      snap.offsetByRecordingId !== offsetByRecordingId ||
app/home/player/lyrics/lyricsSurface.ts:55:      snap.globalOffsetMs !== globalOffsetMs;
app/home/player/lyrics/lyricsSurface.ts-56-
app/home/player/lyrics/lyricsSurface.ts-57-    if (!changed) return;
app/home/player/lyrics/lyricsSurface.ts-58-
app/home/player/lyrics/lyricsSurface.ts:59:    snap = { cuesByRecordingId, offsetByRecordingId, globalOffsetMs };
app/home/player/lyrics/lyricsSurface.ts-60-    emit();
app/home/player/lyrics/lyricsSurface.ts-61-  },
app/home/player/lyrics/lyricsSurface.ts-62-};
--
app/home/player/StageCore.tsx-3-
app/home/player/StageCore.tsx-4-import React from "react";
app/home/player/StageCore.tsx-5-import { usePlayerActions, usePlayerVisual } from "./PlayerState";
app/home/player/StageCore.tsx-6-import VisualizerCanvas from "./VisualizerCanvas";
app/home/player/StageCore.tsx-7-import LyricsOverlay from "./stage/LyricsOverlay";
app/home/player/StageCore.tsx-8-import type { LyricCue } from "@/lib/types";
app/home/player/StageCore.tsx-9-import { STAGE_TRANSPORT_FOOTER_PX } from "./StageTransportBar";
app/home/player/StageCore.tsx-10-import { mediaSurface } from "./mediaSurface";
app/home/player/StageCore.tsx-11-import { useLyricsSnapshot } from "./lyrics/useLyricsSurface";
app/home/player/StageCore.tsx-12-
app/home/player/StageCore.tsx-13-type CuesByRecordingId = Record<string, LyricCue[]>;
app/home/player/StageCore.tsx-14-type OffsetByRecordingId = Record<string, number>;
app/home/player/StageCore.tsx-15-
app/home/player/StageCore.tsx-16-function pickKeyWithCues(
app/home/player/StageCore.tsx:17:  cuesByRecordingId: CuesByRecordingId | undefined,
app/home/player/StageCore.tsx-18-  keys: Array<string | null | undefined>,
app/home/player/StageCore.tsx-19-) {
app/home/player/StageCore.tsx:20:  if (!cuesByRecordingId) return (keys.find(Boolean) as string | null) ?? null;
app/home/player/StageCore.tsx-21-  for (const k of keys) {
app/home/player/StageCore.tsx-22-    if (!k) continue;
app/home/player/StageCore.tsx:23:    const xs = cuesByRecordingId[k];
app/home/player/StageCore.tsx-24-    if (Array.isArray(xs) && xs.length) return k;
app/home/player/StageCore.tsx-25-  }
app/home/player/StageCore.tsx-26-  return (keys.find(Boolean) as string | null) ?? null;
app/home/player/StageCore.tsx-27-}
app/home/player/StageCore.tsx-28-
app/home/player/StageCore.tsx-29-function StageCore(props: {
app/home/player/StageCore.tsx-30-  variant: "inline" | "fullscreen";
app/home/player/StageCore.tsx:31:  cuesByRecordingId?: CuesByRecordingId;
app/home/player/StageCore.tsx:32:  offsetByRecordingId?: OffsetByRecordingId;
app/home/player/StageCore.tsx-33-  offsetMs?: number;
app/home/player/StageCore.tsx-34-  autoResumeOnSeek?: boolean;
app/home/player/StageCore.tsx-35-  lyricsMode?: "embedded" | "none";
app/home/player/StageCore.tsx-36-}) {
app/home/player/StageCore.tsx-37-  const {
app/home/player/StageCore.tsx-38-    variant,
app/home/player/StageCore.tsx:39:    cuesByRecordingId: cuesByRecordingIdProp,
app/home/player/StageCore.tsx:40:    offsetByRecordingId: offsetByRecordingIdProp,
app/home/player/StageCore.tsx:41:    offsetMs: globalOffsetMs = 0,
app/home/player/StageCore.tsx-42-    autoResumeOnSeek = false,
app/home/player/StageCore.tsx-43-    lyricsMode = "embedded",
app/home/player/StageCore.tsx-44-  } = props;
app/home/player/StageCore.tsx-45-
app/home/player/StageCore.tsx-46-  const player = usePlayerVisual();
app/home/player/StageCore.tsx-47-  const actions = usePlayerActions();
app/home/player/StageCore.tsx-48-  const snap = useLyricsSnapshot();
app/home/player/StageCore.tsx-49-
app/home/player/StageCore.tsx-50-  // ✅ prefer props when provided; otherwise use lyricsSurface
app/home/player/StageCore.tsx:51:  const cuesByRecordingId = cuesByRecordingIdProp ?? snap.cuesByRecordingId;
app/home/player/StageCore.tsx:52:  const offsetByRecordingId =
app/home/player/StageCore.tsx:53:    offsetByRecordingIdProp ?? snap.offsetByRecordingId;
app/home/player/StageCore.tsx-54-
app/home/player/StageCore.tsx-55-  // Register stage presence; fullscreen wins if it exists.
app/home/player/StageCore.tsx-56-  React.useEffect(() => mediaSurface.registerStage(variant), [variant]);
app/home/player/StageCore.tsx-57-
app/home/player/StageCore.tsx-58-  const [surfaceRecordingId, setSurfaceRecordingId] = React.useState<
app/home/player/StageCore.tsx-59-    string | null
app/home/player/StageCore.tsx-60-  >(() => mediaSurface.getRecordingId());
app/home/player/StageCore.tsx-61-
app/home/player/StageCore.tsx-62-  React.useEffect(() => {
app/home/player/StageCore.tsx-63-    const unsub = mediaSurface.subscribe((e) => {
app/home/player/StageCore.tsx-64-      if (e.type === "track") setSurfaceRecordingId(e.id);
app/home/player/StageCore.tsx-65-    });
app/home/player/StageCore.tsx-66-    return unsub;
app/home/player/StageCore.tsx-67-  }, []);
app/home/player/StageCore.tsx-68-
app/home/player/StageCore.tsx-69-  const playerRecordingId = player.current?.recordingId ?? null;
app/home/player/StageCore.tsx-70-  const playerDisplayId = player.current?.displayId ?? null;
app/home/player/StageCore.tsx-71-  const playerMuxId = player.current?.muxPlaybackId ?? null;
app/home/player/StageCore.tsx-72-
app/home/player/StageCore.tsx:73:  const recordingId = pickKeyWithCues(cuesByRecordingId, [
app/home/player/StageCore.tsx-74-    playerRecordingId,
app/home/player/StageCore.tsx-75-    surfaceRecordingId,
app/home/player/StageCore.tsx-76-    playerMuxId,
app/home/player/StageCore.tsx-77-  ]);
app/home/player/StageCore.tsx-78-
app/home/player/StageCore.tsx-79-  const displayId = playerDisplayId;
app/home/player/StageCore.tsx-80-
app/home/player/StageCore.tsx-81-  const cues: LyricCue[] | null = recordingId
app/home/player/StageCore.tsx:82:    ? Array.isArray(cuesByRecordingId?.[recordingId]) &&
app/home/player/StageCore.tsx:83:      cuesByRecordingId[recordingId].length
app/home/player/StageCore.tsx:84:      ? cuesByRecordingId[recordingId]
app/home/player/StageCore.tsx-85-      : null
app/home/player/StageCore.tsx-86-    : null;
app/home/player/StageCore.tsx-87-
app/home/player/StageCore.tsx-88-  const trackOffsetMs =
app/home/player/StageCore.tsx:89:    recordingId && typeof offsetByRecordingId?.[recordingId] === "number"
app/home/player/StageCore.tsx:90:      ? Number.isFinite(offsetByRecordingId[recordingId])
app/home/player/StageCore.tsx:91:        ? offsetByRecordingId[recordingId]
app/home/player/StageCore.tsx-92-        : 0
app/home/player/StageCore.tsx-93-      : 0;
app/home/player/StageCore.tsx-94-
app/home/player/StageCore.tsx:95:  const effectiveOffsetMs = trackOffsetMs + globalOffsetMs;
app/home/player/StageCore.tsx-96-  const resumeTrack = player.current ?? player.firstQueuedTrack;
app/home/player/StageCore.tsx-97-
app/home/player/StageCore.tsx-98-  const onSeek = React.useCallback(
app/home/player/StageCore.tsx-99-    (tMs: number) => {
app/home/player/StageCore.tsx-100-      const ms = Math.max(0, Math.floor(tMs));
app/home/player/StageCore.tsx-101-      actions.seek(ms);
app/home/player/StageCore.tsx-102-
app/home/player/StageCore.tsx-103-      if (autoResumeOnSeek) {
app/home/player/StageCore.tsx-104-        if (!resumeTrack) return;
app/home/player/StageCore.tsx-105-        actions.setIntent("play");
app/home/player/StageCore.tsx-106-        actions.play(resumeTrack);
app/home/player/StageCore.tsx-107-        window.dispatchEvent(new Event("af:play-intent"));
app/home/player/StageCore.tsx-108-      }
app/home/player/StageCore.tsx-109-    },
--
app/home/player/lyrics/ensureLyricsForTrack.ts-12-  return Object.prototype.hasOwnProperty.call(obj, key);
app/home/player/lyrics/ensureLyricsForTrack.ts-13-}
app/home/player/lyrics/ensureLyricsForTrack.ts-14-
app/home/player/lyrics/ensureLyricsForTrack.ts-15-/**
app/home/player/lyrics/ensureLyricsForTrack.ts-16- * Prime lyricsSurface from an album-provided bundle.
app/home/player/lyrics/ensureLyricsForTrack.ts-17- * - Merges without clobbering existing per-track entries (unless missing).
app/home/player/lyrics/ensureLyricsForTrack.ts-18- * - Ensures we still preserve "known no lyrics" as [] if already present.
app/home/player/lyrics/ensureLyricsForTrack.ts-19- * - No network; safe to call repeatedly (idempotent-ish).
app/home/player/lyrics/ensureLyricsForTrack.ts-20- */
app/home/player/lyrics/ensureLyricsForTrack.ts-21-export function primeLyricsFromAlbumBundle(
app/home/player/lyrics/ensureLyricsForTrack.ts-22-  bundle: AlbumLyricsBundle | null | undefined,
app/home/player/lyrics/ensureLyricsForTrack.ts-23-) {
app/home/player/lyrics/ensureLyricsForTrack.ts-24-  if (!bundle) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-25-
app/home/player/lyrics/ensureLyricsForTrack.ts:26:  const snap = lyricsSurface.getSnapshot();
app/home/player/lyrics/ensureLyricsForTrack.ts-27-
app/home/player/lyrics/ensureLyricsForTrack.ts:28:  const cuesIn = bundle.cuesByRecordingId ?? {};
app/home/player/lyrics/ensureLyricsForTrack.ts:29:  const offIn = bundle.offsetByRecordingId ?? {};
app/home/player/lyrics/ensureLyricsForTrack.ts-30-
app/home/player/lyrics/ensureLyricsForTrack.ts-31-  // Build merged maps but only add keys that are currently unknown.
app/home/player/lyrics/ensureLyricsForTrack.ts:32:  let cuesOut = snap.cuesByRecordingId;
app/home/player/lyrics/ensureLyricsForTrack.ts:33:  let offOut = snap.offsetByRecordingId;
app/home/player/lyrics/ensureLyricsForTrack.ts-34-
app/home/player/lyrics/ensureLyricsForTrack.ts-35-  for (const [recordingIdRaw, cuesRaw] of Object.entries(cuesIn)) {
app/home/player/lyrics/ensureLyricsForTrack.ts-36-    const id = (recordingIdRaw ?? "").trim();
app/home/player/lyrics/ensureLyricsForTrack.ts-37-    if (!id) continue;
app/home/player/lyrics/ensureLyricsForTrack.ts-38-
app/home/player/lyrics/ensureLyricsForTrack.ts-39-    if (hasOwn(cuesOut, id)) continue; // don't overwrite existing (including [] known-no-lyrics)
app/home/player/lyrics/ensureLyricsForTrack.ts-40-
app/home/player/lyrics/ensureLyricsForTrack.ts-41-    const cues = Array.isArray(cuesRaw) ? cuesRaw : [];
app/home/player/lyrics/ensureLyricsForTrack.ts-42-    cuesOut = { ...cuesOut, [id]: cues };
app/home/player/lyrics/ensureLyricsForTrack.ts-43-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-44-
app/home/player/lyrics/ensureLyricsForTrack.ts-45-  for (const [recordingIdRaw, offRaw] of Object.entries(offIn)) {
app/home/player/lyrics/ensureLyricsForTrack.ts-46-    const id = (recordingIdRaw ?? "").trim();
app/home/player/lyrics/ensureLyricsForTrack.ts-47-    if (!id) continue;
app/home/player/lyrics/ensureLyricsForTrack.ts-48-
app/home/player/lyrics/ensureLyricsForTrack.ts-49-    if (hasOwn(offOut, id)) continue; // don't overwrite existing
app/home/player/lyrics/ensureLyricsForTrack.ts-50-
app/home/player/lyrics/ensureLyricsForTrack.ts-51-    const n =
app/home/player/lyrics/ensureLyricsForTrack.ts-52-      typeof offRaw === "number" && Number.isFinite(offRaw)
app/home/player/lyrics/ensureLyricsForTrack.ts-53-        ? Math.floor(offRaw)
app/home/player/lyrics/ensureLyricsForTrack.ts-54-        : 0;
app/home/player/lyrics/ensureLyricsForTrack.ts-55-
app/home/player/lyrics/ensureLyricsForTrack.ts-56-    offOut = { ...offOut, [id]: n };
app/home/player/lyrics/ensureLyricsForTrack.ts-57-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-58-
app/home/player/lyrics/ensureLyricsForTrack.ts-59-  // Only write if something changed (cheap reference check).
app/home/player/lyrics/ensureLyricsForTrack.ts:60:  if (cuesOut === snap.cuesByRecordingId && offOut === snap.offsetByRecordingId) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-61-
app/home/player/lyrics/ensureLyricsForTrack.ts:62:  lyricsSurface.setMaps({
app/home/player/lyrics/ensureLyricsForTrack.ts:63:    cuesByrecordingId: cuesOut,
app/home/player/lyrics/ensureLyricsForTrack.ts:64:    offsetByrecordingId: offOut,
app/home/player/lyrics/ensureLyricsForTrack.ts:65:    globalOffsetMs: snap.globalOffsetMs,
app/home/player/lyrics/ensureLyricsForTrack.ts-66-  });
app/home/player/lyrics/ensureLyricsForTrack.ts-67-}
app/home/player/lyrics/ensureLyricsForTrack.ts-68-
app/home/player/lyrics/ensureLyricsForTrack.ts-69-/**
app/home/player/lyrics/ensureLyricsForTrack.ts-70- * Ensure lyrics (cues + offset) exist in lyricsSurface for recordingId.
app/home/player/lyrics/ensureLyricsForTrack.ts-71- * - Dedupes concurrent calls (module-level inFlight).
app/home/player/lyrics/ensureLyricsForTrack.ts-72- * - Caches "no lyrics" as [] to avoid refetch loops.
app/home/player/lyrics/ensureLyricsForTrack.ts-73- * - Safe to call repeatedly from any surface.
app/home/player/lyrics/ensureLyricsForTrack.ts-74- */
app/home/player/lyrics/ensureLyricsForTrack.ts-75-export async function ensureLyricsForTrack(
app/home/player/lyrics/ensureLyricsForTrack.ts-76-  recordingId: string,
app/home/player/lyrics/ensureLyricsForTrack.ts-77-  opts?: { signal?: AbortSignal },
app/home/player/lyrics/ensureLyricsForTrack.ts-78-): Promise<void> {
app/home/player/lyrics/ensureLyricsForTrack.ts-79-  const id = (recordingId ?? "").trim();
app/home/player/lyrics/ensureLyricsForTrack.ts-80-  if (!id) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-81-
app/home/player/lyrics/ensureLyricsForTrack.ts:82:  const snap = lyricsSurface.getSnapshot();
app/home/player/lyrics/ensureLyricsForTrack.ts:83:  const existing = snap.cuesByRecordingId[id];
app/home/player/lyrics/ensureLyricsForTrack.ts-84-
app/home/player/lyrics/ensureLyricsForTrack.ts:85:  const knownKey = hasOwn(snap.cuesByRecordingId, id);
app/home/player/lyrics/ensureLyricsForTrack.ts-86-  const hasCues = Array.isArray(existing) && existing.length > 0;
app/home/player/lyrics/ensureLyricsForTrack.ts-87-  const knownNoLyrics =
app/home/player/lyrics/ensureLyricsForTrack.ts-88-    knownKey && Array.isArray(existing) && existing.length === 0;
app/home/player/lyrics/ensureLyricsForTrack.ts-89-
app/home/player/lyrics/ensureLyricsForTrack.ts-90-  if (hasCues || knownNoLyrics) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-91-
app/home/player/lyrics/ensureLyricsForTrack.ts-92-  // Already fetching?
app/home/player/lyrics/ensureLyricsForTrack.ts-93-  if (inFlight.has(id)) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-94-
app/home/player/lyrics/ensureLyricsForTrack.ts-95-  const ac = new AbortController();
app/home/player/lyrics/ensureLyricsForTrack.ts-96-  inFlight.set(id, ac);
app/home/player/lyrics/ensureLyricsForTrack.ts-97-
app/home/player/lyrics/ensureLyricsForTrack.ts-98-  // Caller abort should NOT abort the shared in-flight request.
app/home/player/lyrics/ensureLyricsForTrack.ts-99-  // It only means "this caller no longer cares". The module-scope fetch may be
app/home/player/lyrics/ensureLyricsForTrack.ts-100-  // servicing other callers (PlayerState, StageInline, etc).
app/home/player/lyrics/ensureLyricsForTrack.ts-101-  const outerSignal = opts?.signal;
app/home/player/lyrics/ensureLyricsForTrack.ts-102-  if (outerSignal?.aborted) {
app/home/player/lyrics/ensureLyricsForTrack.ts-103-    inFlight.delete(id);
app/home/player/lyrics/ensureLyricsForTrack.ts-104-    return;
app/home/player/lyrics/ensureLyricsForTrack.ts-105-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-106-
app/home/player/lyrics/ensureLyricsForTrack.ts-107-  try {
app/home/player/lyrics/ensureLyricsForTrack.ts-108-    const r = await fetchLyricsByrecordingId(id, ac.signal);
app/home/player/lyrics/ensureLyricsForTrack.ts-109-    if (!r) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-110-    if (r.recordingId !== id) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-111-
app/home/player/lyrics/ensureLyricsForTrack.ts:112:    const prev = lyricsSurface.getSnapshot();
app/home/player/lyrics/ensureLyricsForTrack.ts-113-
app/home/player/lyrics/ensureLyricsForTrack.ts-114-    const nextCuesByrecordingId =
app/home/player/lyrics/ensureLyricsForTrack.ts-115-      Array.isArray(r.cues) && r.cues.length
app/home/player/lyrics/ensureLyricsForTrack.ts:116:        ? { ...prev.cuesByRecordingId, [id]: r.cues }
app/home/player/lyrics/ensureLyricsForTrack.ts:117:        : hasOwn(prev.cuesByRecordingId, id)
app/home/player/lyrics/ensureLyricsForTrack.ts:118:          ? prev.cuesByRecordingId
app/home/player/lyrics/ensureLyricsForTrack.ts:119:          : { ...prev.cuesByRecordingId, [id]: [] };
app/home/player/lyrics/ensureLyricsForTrack.ts-120-
app/home/player/lyrics/ensureLyricsForTrack.ts-121-    const nextOffsetByrecordingId =
app/home/player/lyrics/ensureLyricsForTrack.ts-122-      typeof r.offsetMs === "number" && Number.isFinite(r.offsetMs)
app/home/player/lyrics/ensureLyricsForTrack.ts:123:        ? { ...prev.offsetByRecordingId, [id]: r.offsetMs }
app/home/player/lyrics/ensureLyricsForTrack.ts:124:        : prev.offsetByRecordingId;
app/home/player/lyrics/ensureLyricsForTrack.ts-125-
app/home/player/lyrics/ensureLyricsForTrack.ts:126:    lyricsSurface.setMaps({
app/home/player/lyrics/ensureLyricsForTrack.ts:127:      cuesByrecordingId: nextCuesByrecordingId,
app/home/player/lyrics/ensureLyricsForTrack.ts:128:      offsetByrecordingId: nextOffsetByrecordingId,
app/home/player/lyrics/ensureLyricsForTrack.ts:129:      // preserve whatever globalOffsetMs currently is (lyricsSurface handles defaulting)
app/home/player/lyrics/ensureLyricsForTrack.ts:130:      globalOffsetMs: prev.globalOffsetMs,
app/home/player/lyrics/ensureLyricsForTrack.ts-131-    });
app/home/player/lyrics/ensureLyricsForTrack.ts-132-  } finally {
app/home/player/lyrics/ensureLyricsForTrack.ts-133-    inFlight.delete(id);
app/home/player/lyrics/ensureLyricsForTrack.ts-134-  }
app/home/player/lyrics/ensureLyricsForTrack.ts-135-}

===== NO-LYRICS / EXISTING-CACHE SEMANTICS =====

1-// web/app/home/player/lyrics/ensureLyricsForTrack.ts
2-"use client";
3-
4-import { lyricsSurface } from "./lyricsSurface";
5-import { fetchLyricsByrecordingId } from "./fetchLyricsByRecordingId";
6-import type { AlbumLyricsBundle } from "@/lib/types";
7-
8-// Module-scope in-flight map so *any* caller (any surface) dedupes fetches.
9-const inFlight = new Map<string, AbortController>();
10-
11:function hasOwn(obj: object, key: string): boolean {
12-  return Object.prototype.hasOwnProperty.call(obj, key);
13-}
14-
15-/**
16- * Prime lyricsSurface from an album-provided bundle.
17- * - Merges without clobbering existing per-track entries (unless missing).
18- * - Ensures we still preserve "known no lyrics" as [] if already present.
19- * - No network; safe to call repeatedly (idempotent-ish).
20- */
21-export function primeLyricsFromAlbumBundle(
22-  bundle: AlbumLyricsBundle | null | undefined,
23-) {
--
27-
28-  const cuesIn = bundle.cuesByRecordingId ?? {};
29-  const offIn = bundle.offsetByRecordingId ?? {};
30-
31-  // Build merged maps but only add keys that are currently unknown.
32-  let cuesOut = snap.cuesByRecordingId;
33-  let offOut = snap.offsetByRecordingId;
34-
35-  for (const [recordingIdRaw, cuesRaw] of Object.entries(cuesIn)) {
36-    const id = (recordingIdRaw ?? "").trim();
37-    if (!id) continue;
38-
39:    if (hasOwn(cuesOut, id)) continue; // don't overwrite existing (including [] known-no-lyrics)
40-
41:    const cues = Array.isArray(cuesRaw) ? cuesRaw : [];
42-    cuesOut = { ...cuesOut, [id]: cues };
43-  }
44-
45-  for (const [recordingIdRaw, offRaw] of Object.entries(offIn)) {
46-    const id = (recordingIdRaw ?? "").trim();
47-    if (!id) continue;
48-
49:    if (hasOwn(offOut, id)) continue; // don't overwrite existing
50-
51-    const n =
52-      typeof offRaw === "number" && Number.isFinite(offRaw)
53-        ? Math.floor(offRaw)
54-        : 0;
55-
56-    offOut = { ...offOut, [id]: n };
57-  }
58-
59-  // Only write if something changed (cheap reference check).
60-  if (cuesOut === snap.cuesByRecordingId && offOut === snap.offsetByRecordingId) return;
61-
--
73- * - Safe to call repeatedly from any surface.
74- */
75-export async function ensureLyricsForTrack(
76-  recordingId: string,
77-  opts?: { signal?: AbortSignal },
78-): Promise<void> {
79-  const id = (recordingId ?? "").trim();
80-  if (!id) return;
81-
82-  const snap = lyricsSurface.getSnapshot();
83-  const existing = snap.cuesByRecordingId[id];
84-
85:  const knownKey = hasOwn(snap.cuesByRecordingId, id);
86:  const hasCues = Array.isArray(existing) && existing.length > 0;
87:  const knownNoLyrics =
88:    knownKey && Array.isArray(existing) && existing.length === 0;
89-
90:  if (hasCues || knownNoLyrics) return;
91-
92-  // Already fetching?
93-  if (inFlight.has(id)) return;
94-
95-  const ac = new AbortController();
96-  inFlight.set(id, ac);
97-
98-  // Caller abort should NOT abort the shared in-flight request.
99-  // It only means "this caller no longer cares". The module-scope fetch may be
100-  // servicing other callers (PlayerState, StageInline, etc).
101-  const outerSignal = opts?.signal;
102-  if (outerSignal?.aborted) {
103-    inFlight.delete(id);
104-    return;
105-  }
106-
107-  try {
108-    const r = await fetchLyricsByrecordingId(id, ac.signal);
109-    if (!r) return;
110-    if (r.recordingId !== id) return;
111-
112-    const prev = lyricsSurface.getSnapshot();
113-
114-    const nextCuesByrecordingId =
115:      Array.isArray(r.cues) && r.cues.length
116-        ? { ...prev.cuesByRecordingId, [id]: r.cues }
117:        : hasOwn(prev.cuesByRecordingId, id)
118-          ? prev.cuesByRecordingId
119:          : { ...prev.cuesByRecordingId, [id]: [] };
120-
121-    const nextOffsetByrecordingId =
122-      typeof r.offsetMs === "number" && Number.isFinite(r.offsetMs)
123-        ? { ...prev.offsetByRecordingId, [id]: r.offsetMs }
124-        : prev.offsetByRecordingId;
125-
126-    lyricsSurface.setMaps({
127-      cuesByrecordingId: nextCuesByrecordingId,
128-      offsetByrecordingId: nextOffsetByrecordingId,
129-      // preserve whatever globalOffsetMs currently is (lyricsSurface handles defaulting)
130-      globalOffsetMs: prev.globalOffsetMs,
131-    });

===== CALLER ORDER / AWAITING BEHAVIOUR =====

app/home/player/StageInline.tsx-292-  const { height = 300 } = props;
app/home/player/StageInline.tsx-293-  const p = usePlayer();
app/home/player/StageInline.tsx-294-
app/home/player/StageInline.tsx-295-  // Lazy-load lyrics for the currently playing track when missing.
app/home/player/StageInline.tsx-296-  const currentRecordingId = p.current?.recordingId ?? null;
app/home/player/StageInline.tsx-297-
app/home/player/StageInline.tsx-298-  React.useEffect(() => {
app/home/player/StageInline.tsx-299-    if (!currentRecordingId) return;
app/home/player/StageInline.tsx-300-
app/home/player/StageInline.tsx-301-    const recordingId = currentRecordingId; // stable capture
app/home/player/StageInline.tsx-302-    const ac = new AbortController();
app/home/player/StageInline.tsx-303-
app/home/player/StageInline.tsx:304:    void ensureLyricsForTrack(recordingId, { signal: ac.signal });
app/home/player/StageInline.tsx-305-
app/home/player/StageInline.tsx-306-    return () => ac.abort();
app/home/player/StageInline.tsx-307-  }, [currentRecordingId]);
app/home/player/StageInline.tsx-308-
app/home/player/StageInline.tsx-309-  const isMobile = useIsMobile(640);
app/home/player/StageInline.tsx-310-  const isAdmin = useIsAdminBodyFlag();
app/home/player/StageInline.tsx-311-  const inlineHeight = isMobile
app/home/player/StageInline.tsx-312-    ? Math.max(140, Math.round(height * 0.5))
app/home/player/StageInline.tsx-313-    : height;
app/home/player/StageInline.tsx-314-
app/home/player/StageInline.tsx-315-  const [mounted, setMounted] = React.useState(false);
app/home/player/StageInline.tsx-316-  React.useEffect(() => setMounted(true), []);
--
app/home/player/PlayerState.tsx-1119-      visualPlayRequestPending,
app/home/player/PlayerState.tsx-1120-      visualCurrent,
app/home/player/PlayerState.tsx-1121-      firstQueuedTrack,
app/home/player/PlayerState.tsx-1122-    ],
app/home/player/PlayerState.tsx-1123-  );
app/home/player/PlayerState.tsx-1124-
app/home/player/PlayerState.tsx-1125-  const currentId = state.current?.recordingId ?? null;
app/home/player/PlayerState.tsx-1126-  const pendingId = state.pendingRecordingId ?? null;
app/home/player/PlayerState.tsx-1127-
app/home/player/PlayerState.tsx-1128-  React.useEffect(() => {
app/home/player/PlayerState.tsx-1129-    const id = (currentId ?? "").trim();
app/home/player/PlayerState.tsx-1130-    if (!id) return;
app/home/player/PlayerState.tsx:1131:    void ensureLyricsForTrack(id);
app/home/player/PlayerState.tsx-1132-  }, [currentId]);
app/home/player/PlayerState.tsx-1133-
app/home/player/PlayerState.tsx-1134-  React.useEffect(() => {
app/home/player/PlayerState.tsx-1135-    const id = (pendingId ?? "").trim();
app/home/player/PlayerState.tsx-1136-    if (!id) return;
app/home/player/PlayerState.tsx:1137:    void ensureLyricsForTrack(id);
app/home/player/PlayerState.tsx-1138-  }, [pendingId]);
app/home/player/PlayerState.tsx-1139-
app/home/player/PlayerState.tsx-1140-  return (
app/home/player/PlayerState.tsx-1141-    <PlayerActionsCtx.Provider value={actions}>
app/home/player/PlayerState.tsx-1142-      <PlayerVisualCtx.Provider value={visualState}>
app/home/player/PlayerState.tsx-1143-        <PlayerCtx.Provider value={api}>{props.children}</PlayerCtx.Provider>
app/home/player/PlayerState.tsx-1144-      </PlayerVisualCtx.Provider>
app/home/player/PlayerState.tsx-1145-    </PlayerActionsCtx.Provider>
app/home/player/PlayerState.tsx-1146-  );
app/home/player/PlayerState.tsx-1147-}
app/home/player/PlayerState.tsx-1148-
app/home/player/PlayerState.tsx-1149-export function usePlayer() {
--
app/home/player/lyrics/ensureLyricsForTrack.ts-63-    cuesByrecordingId: cuesOut,
app/home/player/lyrics/ensureLyricsForTrack.ts-64-    offsetByrecordingId: offOut,
app/home/player/lyrics/ensureLyricsForTrack.ts-65-    globalOffsetMs: snap.globalOffsetMs,
app/home/player/lyrics/ensureLyricsForTrack.ts-66-  });
app/home/player/lyrics/ensureLyricsForTrack.ts-67-}
app/home/player/lyrics/ensureLyricsForTrack.ts-68-
app/home/player/lyrics/ensureLyricsForTrack.ts-69-/**
app/home/player/lyrics/ensureLyricsForTrack.ts-70- * Ensure lyrics (cues + offset) exist in lyricsSurface for recordingId.
app/home/player/lyrics/ensureLyricsForTrack.ts-71- * - Dedupes concurrent calls (module-level inFlight).
app/home/player/lyrics/ensureLyricsForTrack.ts-72- * - Caches "no lyrics" as [] to avoid refetch loops.
app/home/player/lyrics/ensureLyricsForTrack.ts-73- * - Safe to call repeatedly from any surface.
app/home/player/lyrics/ensureLyricsForTrack.ts-74- */
app/home/player/lyrics/ensureLyricsForTrack.ts:75:export async function ensureLyricsForTrack(
app/home/player/lyrics/ensureLyricsForTrack.ts-76-  recordingId: string,
app/home/player/lyrics/ensureLyricsForTrack.ts-77-  opts?: { signal?: AbortSignal },
app/home/player/lyrics/ensureLyricsForTrack.ts-78-): Promise<void> {
app/home/player/lyrics/ensureLyricsForTrack.ts-79-  const id = (recordingId ?? "").trim();
app/home/player/lyrics/ensureLyricsForTrack.ts-80-  if (!id) return;
app/home/player/lyrics/ensureLyricsForTrack.ts-81-
app/home/player/lyrics/ensureLyricsForTrack.ts-82-  const snap = lyricsSurface.getSnapshot();
app/home/player/lyrics/ensureLyricsForTrack.ts-83-  const existing = snap.cuesByRecordingId[id];
app/home/player/lyrics/ensureLyricsForTrack.ts-84-
app/home/player/lyrics/ensureLyricsForTrack.ts-85-  const knownKey = hasOwn(snap.cuesByRecordingId, id);
app/home/player/lyrics/ensureLyricsForTrack.ts-86-  const hasCues = Array.isArray(existing) && existing.length > 0;
app/home/player/lyrics/ensureLyricsForTrack.ts-87-  const knownNoLyrics =

===== RELEVANT LYRICS TEST INVENTORY =====



===== TYPESCRIPT / PACKAGE / ESLINT CONFIG =====


----- package.json -----
{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.962.0",
    "@aws-sdk/s3-request-presigner": "^3.962.0",
    "@clerk/nextjs": "^6.36.5",
    "@mux/mux-node": "^12.8.1",
    "@portabletext/react": "^6.0.2",
    "@portabletext/types": "^4.0.1",
    "@react-email/components": "^1.0.6",
    "@react-email/render": "^2.0.4",
    "@sanity/client": "^7.13.2",
    "@sanity/image-url": "^1.2.0",
    "@sanity/vision": "^4.22.0",
    "@tiptap/extension-link": "^3.20.0",
    "@tiptap/extension-placeholder": "^3.20.0",
    "@tiptap/react": "^3.20.0",
    "@tiptap/starter-kit": "^3.20.0",
    "@vercel/postgres": "^0.10.0",
    "hls.js": "^1.6.15",
    "jose": "^6.1.3",
    "next": "16.1.1",
    "next-sanity": "^11.6.12",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "react-email": "^5.2.5",
    "resend": "^6.8.0",
    "sanity": "^4.22.0",
    "stripe": "^20.1.0",
    "styled-components": "^6.1.19",
    "svix": "^1.84.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.1",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}


----- tsconfig.json -----
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules"]
}


----- eslint.config.mjs -----
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;


===== CURRENT GIT STATUS =====

 M app/admin/playback/PlaybackTelemetryDashboardClient.tsx
 M app/admin/playback/dashboard/AudienceSplitCard.tsx
 M app/admin/playback/dashboard/DedupeTable.tsx
 M app/admin/playback/dashboard/PlaybackDashboardPrimitives.tsx
 M app/admin/playback/dashboard/QualifiedPlayTrendChart.tsx
 M app/admin/playback/dashboard/ShareLinkActivityTable.tsx
 M app/admin/playback/dashboard/TrackTable.tsx
 M app/admin/playback/dashboard/playbackTelemetryDashboardModel.ts
 M app/admin/playback/page.tsx
 M app/api/admin/campaigns/drain/route.ts
 M app/api/exegesis/comment/edit/route.ts
 M app/api/exegesis/report/route.ts
 M app/api/exegesis/thread/route.ts
 M app/api/exegesis/vote/route.ts
 M app/api/stripe/cancel-subscription/route.ts
 M app/api/stripe/create-checkout-session/route.ts
 M app/api/webhooks/stripe/route.ts
 M app/home/SessionChrome.tsx
 M app/home/usePlaybackReconciliation.ts
 M lib/shareTokens.ts
 M lib/stripeSubscriptions.ts
?? bjr-campaign-drain-patch-source.txt
?? bjr-exegesis-core-api-patch-source.txt
?? bjr-playback-dashboard-patch-source.txt
?? bjr-session-chrome-patch-source.txt
?? bjr-share-tokens-patch-source.txt
?? bjr-stripe-subscription-lifecycle-patch-source.txt
?? bjr-stripe-webhook-patch-source.txt
?? sonarqube-export-20260810-132041/

===== CURRENT GIT DIFF — PATCH TARGET =====



===== STAGED GIT DIFF — PATCH TARGET =====



===== CURRENT GIT DIFF — CALLER / DEPENDENCY CONTEXT =====



===== STAGED GIT DIFF — CALLER / DEPENDENCY CONTEXT =====



===== DIFF CHECK — ENSURE LYRICS SCOPE =====


