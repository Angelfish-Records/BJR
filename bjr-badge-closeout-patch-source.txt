BJR PATCH EVIDENCE BUNDLE — BADGE SUBSYSTEM CLOSE-OUT
PWD: /Users/brendan/Projects/Angelfish-Records/BJR/web
Sonar source: sonarqube-export-20260810-102734/sonarqube-assistant-bundle.json
Sonar analysis date: 2026-08-09T22:22:37+0000


===== ALL OPEN SONAR FINDINGS IN BADGE-NAMED FILES =====

app/admin/badges/BadgeDashboardClient.tsx:25  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/badges/BadgeDashboardClient.tsx:178  typescript:S6582  severity=MINOR  impact=LOW
  Prefer using an optional chain expression instead, as it's more concise and easier to read.

app/admin/badges/BadgeDashboardClient.tsx:241  typescript:S3776  severity=CRITICAL  impact=HIGH
  Refactor this function to reduce its Cognitive Complexity from 18 to the 15 allowed.

app/admin/badges/_components/BadgeCatalogueSection.tsx:22  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/badges/_components/BadgeCatalogueSection.tsx:230  typescript:S3358  severity=MAJOR  impact=MEDIUM
  Extract this nested ternary operation into an independent statement.

app/admin/badges/_components/BadgeQualificationFormSection.tsx:37  typescript:S6767  severity=MINOR  impact=LOW
  'selectedBadge' PropType is defined but prop is never used

app/admin/badges/_components/BadgeQualificationFormSection.tsx:38  typescript:S6767  severity=MINOR  impact=LOW
  'selectedMode' PropType is defined but prop is never used

app/admin/badges/_components/BadgeQualificationFormSection.tsx:99  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/badges/_components/BadgeQualificationFormSection.tsx:115  typescript:S3776  severity=CRITICAL  impact=HIGH
  Refactor this function to reduce its Cognitive Complexity from 24 to the 15 allowed.

app/admin/badges/_components/BadgeQualificationFormSection.tsx:115  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/badges/_components/PreviewResultsSection.tsx:46  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/badges/_components/RecordingPicker.tsx:21  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/admin/badges/page.tsx:7  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/api/admin/badges/award/route.ts:91  typescript:S3358  severity=MAJOR  impact=MEDIUM
  Extract this nested ternary operation into an independent statement.

app/api/admin/badges/preview/route.ts:50  typescript:S3776  severity=CRITICAL  impact=HIGH
  Refactor this function to reduce its Cognitive Complexity from 30 to the 15 allowed.

app/api/admin/badges/preview/route.ts:256  typescript:S3358  severity=MAJOR  impact=MEDIUM
  Extract this nested ternary operation into an independent statement.

app/home/badges/BadgeAwardOverlay.tsx:42  typescript:S6606  severity=MINOR  impact=LOW
  Prefer using nullish coalescing operator (`??=`) instead of an assignment expression, as it is simpler to read.

app/home/badges/BadgeAwardOverlay.tsx:67  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/home/badges/BadgeAwardOverlay.tsx:82  typescript:S6819  severity=MAJOR  impact=MEDIUM
  Use <input type="button">, <input type="image">, <input type="reset">, <input type="submit">, or <button> instead of the "button" role to ensure accessibility across all devices.

app/home/badges/BadgeAwardOverlay.tsx:82  typescript:S6825  severity=MAJOR  impact=MEDIUM
  aria-hidden="true" must not be set on focusable elements.

app/home/badges/BadgeAwardOverlay.tsx:107  typescript:S6819  severity=MAJOR  impact=MEDIUM
  Use <input type="button">, <input type="image">, <input type="reset">, <input type="submit">, or <button> instead of the "button" role to ensure accessibility across all devices.

app/home/badges/BadgeAwardOverlayProvider.tsx:53  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/home/badges/BadgeAwardRevealCard.tsx:14  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/home/modules/badges/BadgeCabinet.tsx:41  typescript:S3776  severity=CRITICAL  impact=HIGH
  Refactor this function to reduce its Cognitive Complexity from 29 to the 15 allowed.

app/home/modules/badges/BadgeCabinet.tsx:41  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/home/modules/badges/BadgeCabinetGrid.tsx:11  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/home/modules/badges/BadgeCabinetItem.tsx:16  typescript:S3776  severity=CRITICAL  impact=HIGH
  Refactor this function to reduce its Cognitive Complexity from 25 to the 15 allowed.

app/home/modules/badges/BadgeCabinetItem.tsx:16  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/home/modules/badges/BadgeCabinetItem.tsx:40  typescript:S6845  severity=MAJOR  impact=LOW,MEDIUM
  `tabIndex` should only be declared on interactive elements.

app/home/modules/badges/BadgeUnlockVisual.tsx:19  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/home/modules/badges/BadgeUnlockVisual.tsx:42  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/home/modules/badges/BadgeUnlockVisual.tsx:71  typescript:S3776  severity=CRITICAL  impact=HIGH
  Refactor this function to reduce its Cognitive Complexity from 33 to the 15 allowed.

app/home/modules/badges/BadgeUnlockVisual.tsx:71  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/home/modules/badges/BuildRevealMaskCanvas.tsx:36  typescript:S7758  severity=MINOR  impact=LOW
  Prefer `String#codePointAt()` over `String#charCodeAt()`.

app/home/modules/badges/BuildRevealMaskCanvas.tsx:205  typescript:S6759  severity=MINOR  impact=LOW
  Mark the props of the component as read-only.

app/home/modules/badges/badgeCabinetViewModel.ts:28  typescript:S3358  severity=MAJOR  impact=MEDIUM
  Extract this nested ternary operation into an independent statement.

app/home/modules/badges/badgeCabinetViewModel.ts:30  typescript:S6582  severity=MINOR  impact=LOW
  Prefer using an optional chain expression instead, as it's more concise and easier to read.

app/home/modules/badges/useFlipGridAnimation.ts:65  typescript:S6582  severity=MINOR  impact=LOW
  Prefer using an optional chain expression instead, as it's more concise and easier to read.

app/home/modules/badges/useFlipGridAnimation.ts:253  typescript:S6582  severity=MINOR  impact=LOW
  Prefer using an optional chain expression instead, as it's more concise and easier to read.

app/home/modules/badges/usePrefersReducedMotion.ts:27  typescript:S1874  severity=MINOR  impact=LOW
  The signature '(callback: ((this: MediaQueryList, ev: MediaQueryListEvent) => any) | null): void' of 'mediaQuery.addListener' is deprecated.

app/home/modules/badges/usePrefersReducedMotion.ts:28  typescript:S1874  severity=MINOR  impact=LOW
  The signature '(callback: ((this: MediaQueryList, ev: MediaQueryListEvent) => any) | null): void' of 'mediaQuery.removeListener' is deprecated.

lib/badgeAdmin.ts:278  typescript:S3358  severity=MAJOR  impact=MEDIUM
  Extract this nested ternary operation into an independent statement.

sanity/schemaTypes/badgeDefinition.ts:52  typescript:S6551  severity=MINOR  impact=LOW
  'parentObject(parent)?.autoQualificationMode ?? ""' will use Object's default stringification format ('[object Object]') when stringified.

TOTAL BADGE FINDINGS IN FRESH BASELINE: 43

By file:
    3  app/admin/badges/BadgeDashboardClient.tsx
    2  app/admin/badges/_components/BadgeCatalogueSection.tsx
    5  app/admin/badges/_components/BadgeQualificationFormSection.tsx
    1  app/admin/badges/_components/PreviewResultsSection.tsx
    1  app/admin/badges/_components/RecordingPicker.tsx
    1  app/admin/badges/page.tsx
    1  app/api/admin/badges/award/route.ts
    2  app/api/admin/badges/preview/route.ts
    5  app/home/badges/BadgeAwardOverlay.tsx
    1  app/home/badges/BadgeAwardOverlayProvider.tsx
    1  app/home/badges/BadgeAwardRevealCard.tsx
    2  app/home/modules/badges/BadgeCabinet.tsx
    1  app/home/modules/badges/BadgeCabinetGrid.tsx
    3  app/home/modules/badges/BadgeCabinetItem.tsx
    4  app/home/modules/badges/BadgeUnlockVisual.tsx
    2  app/home/modules/badges/BuildRevealMaskCanvas.tsx
    2  app/home/modules/badges/badgeCabinetViewModel.ts
    2  app/home/modules/badges/useFlipGridAnimation.ts
    2  app/home/modules/badges/usePrefersReducedMotion.ts
    1  lib/badgeAdmin.ts
    1  sanity/schemaTypes/badgeDefinition.ts

By rule:
    2  typescript:S1874
    5  typescript:S3358
    6  typescript:S3776
    1  typescript:S6551
    4  typescript:S6582
    1  typescript:S6606
   17  typescript:S6759
    2  typescript:S6767
    2  typescript:S6819
    1  typescript:S6825
    1  typescript:S6845
    1  typescript:S7758

By impact:
    6  HIGH
   29  LOW
    9  MEDIUM

===== CURRENT SHA-256 — ALL BADGE ISSUE OWNERS =====

1bd392e2fbe21a18ecd284ba0b71f518b2c4b2c0656eedf7396ba1f773ff0bc8  app/admin/badges/BadgeDashboardClient.tsx
85e5947a36c225e8e07054e7d85992866f6e5984115075e2e30a26109c7836b4  app/admin/badges/_components/BadgeCatalogueSection.tsx
fcd7157dd2147ffd1cc589a29333a7123cc5b33a6fd7b7e7a668c29c8bc7afe9  app/admin/badges/_components/BadgeQualificationFormSection.tsx
453fc237d0628d0a769b4ca0dd4cfa30ee8e57135e2caa492a3d3f129569cd9d  app/admin/badges/_components/PreviewResultsSection.tsx
ebbf9c17833d9e6749b4832fdd595d11006effdd728d8abe9d1d19ccbe265128  app/admin/badges/_components/RecordingPicker.tsx
5e30f9d9d1e3cb82ad63f91e2b307191e5118927057ce2f38942cdfe26297d3c  app/admin/badges/page.tsx
a18041861b494cfc8e653afdb21e4526156dee7dbdcc3fc8bc1692176a6b3cc6  app/api/admin/badges/award/route.ts
24b28b85c598e1f45b6f73a8dff0feb3277249ac8ccb0afebb03df7d5b1b25ee  app/api/admin/badges/preview/route.ts
b08f18e89c8a37a2455235d282da4488d73d07e59508b80ca4e9b84187ff4dd0  app/home/badges/BadgeAwardOverlay.tsx
78069a9ae38f0ea2005708681a1df95b0d7e8130734525b6f94c22f1ca683867  app/home/badges/BadgeAwardOverlayProvider.tsx
7d464fe2f67602f17d020e66c7b08e9f225f2b8cc236fcbccb3759cc18aeff61  app/home/badges/BadgeAwardRevealCard.tsx
d4ea83b2d174f9584decdca0cf051309b37b4e717315b1913db0c246a806209d  app/home/modules/badges/BadgeCabinet.tsx
35f8159824426d2d4f6fd2b53609d08dfd41da394f2724276794fcbc6c0a8bad  app/home/modules/badges/BadgeCabinetGrid.tsx
f12c9467b47fc40a49a7cc59070ed7825038af7271a99235631ba4747783ca30  app/home/modules/badges/BadgeCabinetItem.tsx
9ff4745b8c78dc193ac4f48c084c6bf47ba555b0d7b971758bb772ab21f16d26  app/home/modules/badges/BadgeUnlockVisual.tsx
52ac17f959ab714eaf9c59a92ced7d02a517ecd849baffc3afe55828534ce924  app/home/modules/badges/BuildRevealMaskCanvas.tsx
625d12e1e96bf981f63dbf5a4cd00bb6d9220e4ff3b51bd6bf312ba930ccde19  app/home/modules/badges/badgeCabinetViewModel.ts
2b0a898f875ee4d7115f371eced389c3deac3edb687d4390de84ab62ba60f3e6  app/home/modules/badges/useFlipGridAnimation.ts
89a7ca60842c306707ff7333b62e26ea5d687bcaa6e7a02db2cc280a9b63d031  app/home/modules/badges/usePrefersReducedMotion.ts
7c03d77375c2a6ab7d8af105a2adb4b0ea5315bfec34f9e07cb0ee9a6eb7ffea  lib/badgeAdmin.ts
d40c001e40e64154c4a460d9d9f9c903e580222d8fb6adbef0df8bfa1b9e1ec7  sanity/schemaTypes/badgeDefinition.ts

===== COMPLETE CURRENT SOURCE — ALL BADGE ISSUE OWNERS =====


----- app/admin/badges/BadgeDashboardClient.tsx -----
// web/app/admin/badges/BadgeDashboardClient.tsx
"use client";

import React from "react";
import { BADGE_PREVIEW_MODES } from "@/lib/badgePreviewModes";
import AdminPageFrame from "../AdminPageFrame";
import { BadgeCatalogueSection } from "./_components/BadgeCatalogueSection";
import { BadgeQualificationFormSection } from "./_components/BadgeQualificationFormSection";
import { PreviewResultsSection } from "./_components/PreviewResultsSection";
import type {
  AwardResponse,
  BadgeDefinitionOption,
  FormState,
  PreviewResponse,
  PreviewRow,
  Props,
  RecordingSearchResponse,
  RecordingSearchResult,
} from "./_lib/badgeDashboardTypes";
import {
  buildPreviewPayload,
  DEFAULT_FORM_STATE,
} from "./_lib/badgeDashboardUtils";

export default function BadgeDashboardClient({
  embed,
  badgeDefinitions,
}: Props) {
  const sortedBadges = React.useMemo(() => {
    return [...badgeDefinitions].sort((a, b) => {
      if (a.displayOrder !== b.displayOrder) {
        return a.displayOrder - b.displayOrder;
      }

      return a.title.localeCompare(b.title);
    });
  }, [badgeDefinitions]);

  const [form, setForm] = React.useState<FormState>(() => ({
    ...DEFAULT_FORM_STATE,
    entitlementKey: sortedBadges[0]?.entitlementKey ?? "",
  }));

  const [previewRows, setPreviewRows] = React.useState<PreviewRow[]>([]);
  const [previewCount, setPreviewCount] = React.useState<number>(0);
  const [previewError, setPreviewError] = React.useState<string | null>(null);
  const [previewLoading, setPreviewLoading] = React.useState(false);

  const [awardError, setAwardError] = React.useState<string | null>(null);
  const [awardMessage, setAwardMessage] = React.useState<string | null>(null);
  const [awardLoading, setAwardLoading] = React.useState(false);

  const [recordingQuery, setRecordingQuery] = React.useState("");
  const [recordingResults, setRecordingResults] = React.useState<
    RecordingSearchResult[]
  >([]);
  const [recordingSearchError, setRecordingSearchError] = React.useState<
    string | null
  >(null);
  const [recordingSearchLoading, setRecordingSearchLoading] =
    React.useState(false);
  const [selectedRecording, setSelectedRecording] =
    React.useState<RecordingSearchResult | null>(null);

  const selectedBadge = React.useMemo(() => {
    return (
      sortedBadges.find(
        (badge: BadgeDefinitionOption) =>
          badge.entitlementKey === form.entitlementKey,
      ) ?? null
    );
  }, [form.entitlementKey, sortedBadges]);

  const selectedMode = React.useMemo(() => {
    return BADGE_PREVIEW_MODES.find((mode) => mode.key === form.mode) ?? null;
  }, [form.mode]);

  const modeInputs = selectedMode?.inputRequirements ?? {
    minMinutes: false,
    minPlayCount: false,
    minCompletedCount: false,
    minProgressCount: false,
    minContributionCount: false,
    minVoteCount: false,
    joinedWindow: false,
    activeWindow: false,
    recordingId: false,
  };

  const modeFieldText = selectedMode?.fieldText ?? {
    minMinutesLabel: "Minimum minutes streamed",
    minMinutesHelp: null,
    minPlayCountLabel: "Minimum play count",
    minPlayCountHelp: null,
    minCompletedCountLabel: "Minimum complete count",
    minCompletedCountHelp: null,
    minProgressCountLabel: "Minimum progress count",
    minProgressCountHelp: null,
    minContributionCountLabel: "Minimum Exegesis contributions",
    minContributionCountHelp: null,
    minVoteCountLabel: "Minimum cumulative vote tally",
    minVoteCountHelp: null,
    joinedOnOrAfterLabel: "Joined on or after",
    joinedBeforeLabel: "Joined before",
    joinedWindowHelp: null,
    activeOnOrAfterLabel: "Active on or after",
    activeBeforeLabel: "Active before",
    activeWindowHelp: null,
    recordingIdLabel: "Recording ID",
    recordingIdPlaceholder: "recording UUID",
    recordingIdHelp: null,
  };

  const updateForm = React.useCallback(
    <K extends keyof FormState>(key: K, value: FormState[K]) => {
      setForm((current) => ({
        ...current,
        [key]: value,
      }));
    },
    [],
  );

  const runRecordingSearch = React.useCallback(async () => {
    const query = recordingQuery.trim();

    if (!query) {
      setRecordingResults([]);
      setRecordingSearchError(
        "Enter a track title, artist, album, or recording ID.",
      );
      return;
    }

    setRecordingSearchLoading(true);
    setRecordingSearchError(null);

    try {
      const params = new URLSearchParams({
        q: query,
        limit: "12",
      });

      const response = await fetch(
        `/api/admin/recordings/search?${params.toString()}`,
        {
          method: "GET",
        },
      );

      const json = (await response.json()) as RecordingSearchResponse;

      if (!response.ok || !json.ok) {
        throw new Error(json.error || "Unable to search recordings.");
      }

      const results = Array.isArray(json.results) ? json.results : [];
      setRecordingResults(results);

      if (results.length === 0) {
        setRecordingSearchError("No recordings matched that search.");
      }
    } catch (error) {
      const message =
        error instanceof Error ? error.message : "Unable to search recordings.";
      setRecordingResults([]);
      setRecordingSearchError(message);
    } finally {
      setRecordingSearchLoading(false);
    }
  }, [recordingQuery]);

  const selectRecording = React.useCallback(
    (recording: RecordingSearchResult) => {
      setSelectedRecording(recording);
      setRecordingQuery(
        [recording.title, recording.artist, recording.albumTitle]
          .filter((value): value is string => Boolean(value && value.trim()))
          .join(" • "),
      );
      setRecordingResults([]);
      setRecordingSearchError(null);
      updateForm("recordingId", recording.recordingId);
    },
    [updateForm],
  );

  const clearSelectedRecording = React.useCallback(() => {
    setSelectedRecording(null);
    setRecordingResults([]);
    setRecordingSearchError(null);
    setRecordingQuery("");
    updateForm("recordingId", "");
  }, [updateForm]);

  const runPreview = React.useCallback(async () => {
    if (modeInputs.recordingId && !form.recordingId.trim()) {
      setPreviewError(
        "Select a recording before previewing this badge cohort.",
      );
      setAwardError(null);
      setAwardMessage(null);
      return;
    }

    setPreviewLoading(true);
    setPreviewError(null);
    setAwardError(null);
    setAwardMessage(null);

    try {
      const response = await fetch("/api/admin/badges/preview", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify(buildPreviewPayload(form)),
      });

      const json = (await response.json()) as PreviewResponse;

      if (!response.ok || !json.ok) {
        throw new Error(json.error || "Unable to preview badge cohort.");
      }

      setPreviewRows(Array.isArray(json.rows) ? json.rows : []);
      setPreviewCount(typeof json.count === "number" ? json.count : 0);
    } catch (error) {
      const message =
        error instanceof Error
          ? error.message
          : "Unable to preview badge cohort.";
      setPreviewRows([]);
      setPreviewCount(0);
      setPreviewError(message);
    } finally {
      setPreviewLoading(false);
    }
  }, [form, modeInputs.recordingId]);

  const runAward = React.useCallback(async () => {
    if (!form.entitlementKey) {
      setAwardError("Choose a badge before awarding.");
      return;
    }

    if (previewRows.length === 0) {
      setAwardError("Preview a qualifying cohort before awarding.");
      return;
    }

    setAwardLoading(true);
    setAwardError(null);
    setAwardMessage(null);

    try {
      const response = await fetch("/api/admin/badges/award", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          entitlementKey: form.entitlementKey,
          memberIds: previewRows.map((row) => row.memberId),
          grantReason: form.grantReason.trim() || undefined,
          grantSource: "badge_admin_preview",
        }),
      });

      const json = (await response.json()) as AwardResponse;

      if (!response.ok || !json.ok || !json.result) {
        throw new Error(json.error || "Unable to award badge.");
      }

      const { entitlementKey, attempted, inserted, alreadyHeld } = json.result;

      if (inserted === attempted) {
        setAwardMessage(
          `Awarded ${entitlementKey} to ${inserted} member${inserted === 1 ? "" : "s"}.`,
        );
      } else if (inserted === 0 && alreadyHeld > 0) {
        setAwardMessage(
          `No new grants were created. ${alreadyHeld} member${alreadyHeld === 1 ? "" : "s"} already held ${entitlementKey}.`,
        );
      } else {
        setAwardMessage(
          `Processed ${attempted} member${attempted === 1 ? "" : "s"} for ${entitlementKey}: ${inserted} new grant${inserted === 1 ? "" : "s"}, ${alreadyHeld} already held.`,
        );
      }
    } catch (error) {
      const message =
        error instanceof Error ? error.message : "Unable to award badge.";
      setAwardError(message);
    } finally {
      setAwardLoading(false);
    }
  }, [form.entitlementKey, form.grantReason, previewRows]);

  React.useEffect(() => {
    if (!form.entitlementKey && sortedBadges[0]?.entitlementKey) {
      setForm((current) => ({
        ...current,
        entitlementKey: sortedBadges[0].entitlementKey,
      }));
    }
  }, [form.entitlementKey, sortedBadges]);

  React.useEffect(() => {
    if (!modeInputs.recordingId && form.recordingId) {
      setSelectedRecording(null);
      setRecordingResults([]);
      setRecordingSearchError(null);
      setRecordingQuery("");
      setForm((current) => ({
        ...current,
        recordingId: "",
      }));
    }
  }, [form.recordingId, modeInputs.recordingId]);

  return (
    <AdminPageFrame
      embed={embed}
      maxWidth={1360}
      title="Badge dashboard"
      subtitle="Preview and award entitlement-backed badges using live member and playback aggregates."
    >
      <div
        style={{
          display: "grid",
          gap: 16,
        }}
      >
        <BadgeCatalogueSection
          badges={sortedBadges}
          selectedEntitlementKey={form.entitlementKey}
          onSelectEntitlementKey={(entitlementKey) =>
            updateForm("entitlementKey", entitlementKey)
          }
        />

        <BadgeQualificationFormSection
          badges={sortedBadges}
          form={form}
          selectedBadge={selectedBadge}
          selectedMode={selectedMode}
          modeInputs={modeInputs}
          modeFieldText={modeFieldText}
          previewLoading={previewLoading}
          awardLoading={awardLoading}
          previewRowCount={previewRows.length}
          previewError={previewError}
          awardError={awardError}
          awardMessage={awardMessage}
          recordingQuery={recordingQuery}
          recordingResults={recordingResults}
          recordingSearchError={recordingSearchError}
          recordingSearchLoading={recordingSearchLoading}
          selectedRecording={selectedRecording}
          onFormChange={updateForm}
          onRecordingQueryChange={setRecordingQuery}
          onRunRecordingSearch={() => void runRecordingSearch()}
          onSelectRecording={selectRecording}
          onClearSelectedRecording={clearSelectedRecording}
          onRunPreview={() => void runPreview()}
          onRunAward={() => void runAward()}
        />

        <PreviewResultsSection rows={previewRows} count={previewCount} />
      </div>
    </AdminPageFrame>
  );
}


----- app/admin/badges/_components/BadgeCatalogueSection.tsx -----
// web/app/admin/badges/_components/BadgeCatalogueSection.tsx
"use client";

import Image from "next/image";
import React from "react";
import {
  BG_INSET,
  FONT_SIZE_UI,
  PANEL_BORDER,
  TEXT_MUTED,
  TEXT_PRIMARY,
} from "../../playback/dashboard/playbackTelemetryDashboardStyles";
import type { BadgeDefinitionOption } from "../_lib/badgeDashboardTypes";
import { getBadgeCardBorder } from "../_lib/badgeDashboardUtils";

type Props = {
  badges: BadgeDefinitionOption[];
  selectedEntitlementKey: string;
  onSelectEntitlementKey: (entitlementKey: string) => void;
};

export function BadgeCatalogueSection(props: Props) {
  const { badges, selectedEntitlementKey, onSelectEntitlementKey } = props;

  return (
    <section
      style={{
        border: PANEL_BORDER,
        borderRadius: 18,
        background: BG_INSET,
        padding: 16,
        display: "grid",
        gap: 14,
      }}
    >
      <div
        style={{
          display: "flex",
          justifyContent: "space-between",
          gap: 12,
          alignItems: "baseline",
          flexWrap: "wrap",
        }}
      >
        <div style={{ display: "grid", gap: 4 }}>
          <h2
            style={{
              margin: 0,
              fontSize: 18,
              lineHeight: 1.15,
              color: TEXT_PRIMARY,
            }}
          >
            Badge catalogue
          </h2>
          <p
            style={{
              margin: 0,
              fontSize: FONT_SIZE_UI,
              lineHeight: 1.5,
              color: TEXT_MUTED,
              maxWidth: 760,
            }}
          >
            Browse active badge definitions and select one to preview or award.
          </p>
        </div>

        <div
          style={{
            fontSize: FONT_SIZE_UI,
            lineHeight: 1.5,
            color: TEXT_MUTED,
          }}
        >
          {badges.length.toLocaleString()} active badge
          {badges.length === 1 ? "" : "s"}
        </div>
      </div>

      <div
        style={{
          display: "grid",
          gap: 10,
          gridTemplateColumns: "repeat(auto-fill, minmax(120px, 120px))",
          justifyContent: "flex-start",
        }}
      >
        {badges.length === 0 ? (
          <div
            style={{
              minHeight: 132,
              borderRadius: 16,
              border: "1px solid rgba(255,255,255,0.10)",
              background: "rgba(255,255,255,0.03)",
              display: "grid",
              placeItems: "center",
              padding: 16,
              fontSize: FONT_SIZE_UI,
              color: TEXT_MUTED,
            }}
          >
            No active badge definitions found.
          </div>
        ) : (
          badges.map((badge) => {
            const isSelected = badge.entitlementKey === selectedEntitlementKey;

            return (
              <button
                key={badge.entitlementKey}
                type="button"
                onClick={() => onSelectEntitlementKey(badge.entitlementKey)}
                title={badge.description || badge.title}
                style={{
                  appearance: "none",
                  width: 120,
                  minWidth: 120,
                  textAlign: "center",
                  color: "inherit",
                  cursor: "pointer",
                  border: getBadgeCardBorder(isSelected, badge.featured),
                  borderRadius: 16,
                  background: isSelected
                    ? "rgba(255,255,255,0.08)"
                    : "rgba(255,255,255,0.035)",
                  padding: 10,
                  display: "grid",
                  gap: 8,
                  alignContent: "start",
                  justifyItems: "center",
                  boxShadow: isSelected
                    ? "0 0 0 1px rgba(255,255,255,0.03) inset"
                    : "none",
                  transition:
                    "background 160ms ease, border-color 160ms ease, transform 160ms ease",
                }}
              >
                {badge.imageUrl ? (
                  <div
                    style={{
                      position: "relative",
                      width: 72,
                      height: 72,
                      overflow: "hidden",
                      boxShadow:
                        "inset 0 1px 0 rgba(255,255,255,0.05), 0 6px 18px rgba(0,0,0,0.18)",
                    }}
                  >
                    <Image
                      src={badge.imageUrl}
                      alt={badge.title}
                      fill
                      sizes="72px"
                      style={{
                        objectFit: "cover",
                        display: "block",
                      }}
                    />
                  </div>
                ) : (
                  <div
                    style={{
                      width: 72,
                      height: 72,
                      borderRadius: 999,
                      border: "1px dashed rgba(255,255,255,0.12)",
                      background: "rgba(255,255,255,0.02)",
                      display: "grid",
                      placeItems: "center",
                      fontSize: 18,
                      color: TEXT_MUTED,
                    }}
                  >
                    ✦
                  </div>
                )}

                <div
                  style={{
                    display: "grid",
                    gap: 4,
                    width: "100%",
                    minWidth: 0,
                  }}
                >
                  <strong
                    style={{
                      lineHeight: 1.25,
                      fontSize: 12,
                      color: TEXT_PRIMARY,
                      overflowWrap: "anywhere",
                    }}
                  >
                    {badge.title}
                  </strong>

                  <span
                    style={{
                      fontSize: 10,
                      lineHeight: 1.3,
                      color: TEXT_MUTED,
                      overflowWrap: "anywhere",
                    }}
                  >
                    {badge.entitlementKey}
                  </span>

                  <span
                    style={{
                      fontSize: 10,
                      lineHeight: 1.3,
                      color: TEXT_MUTED,
                    }}
                  >
                    #{badge.displayOrder}
                  </span>

                  <span
                    style={{
                      fontSize: 10,
                      lineHeight: 1.3,
                      color:
                        badge.awardMode === "automatic"
                          ? "rgba(186,244,202,0.9)"
                          : TEXT_MUTED,
                    }}
                  >
                    {badge.awardMode === "automatic"
                      ? badge.autoQualificationMode
                        ? `auto · ${badge.autoQualificationMode}`
                        : "auto"
                      : "manual"}
                  </span>
                </div>
              </button>
            );
          })
        )}
      </div>
    </section>
  );
}


----- app/admin/badges/_components/BadgeQualificationFormSection.tsx -----
// web/app/admin/badges/_components/BadgeQualificationFormSection.tsx
"use client";

import React from "react";
import {
  BADGE_PREVIEW_MODES,
  type BadgeQualificationMode,
} from "@/lib/badgePreviewModes";
import {
  BG_INSET,
  FONT_SIZE_UI,
  PANEL_BORDER,
  TEXT_MUTED,
  TEXT_PRIMARY,
} from "../../playback/dashboard/playbackTelemetryDashboardStyles";
import type {
  BadgeDefinitionOption,
  FormState,
  RecordingSearchResult,
  SelectedModeFieldText,
  SelectedModeInputRequirements,
} from "../_lib/badgeDashboardTypes";
import { RecordingPicker } from "./RecordingPicker";

type ModeDescriptor = {
  key: string;
  label: string;
  description: string;
  metricFamily: string;
  requiresRecording?: boolean;
  supportsDateWindow?: boolean;
};

type Props = {
  badges: BadgeDefinitionOption[];
  form: FormState;
  selectedBadge: BadgeDefinitionOption | null;
  selectedMode: ModeDescriptor | null;
  modeInputs: SelectedModeInputRequirements;
  modeFieldText: SelectedModeFieldText;
  previewLoading: boolean;
  awardLoading: boolean;
  previewRowCount: number;
  previewError: string | null;
  awardError: string | null;
  awardMessage: string | null;
  recordingQuery: string;
  recordingResults: RecordingSearchResult[];
  recordingSearchError: string | null;
  recordingSearchLoading: boolean;
  selectedRecording: RecordingSearchResult | null;
  onFormChange: <K extends keyof FormState>(
    key: K,
    value: FormState[K],
  ) => void;
  onRecordingQueryChange: (value: string) => void;
  onRunRecordingSearch: () => void;
  onSelectRecording: (recording: RecordingSearchResult) => void;
  onClearSelectedRecording: () => void;
  onRunPreview: () => void;
  onRunAward: () => void;
};

const labelTextStyle: React.CSSProperties = {
  fontSize: FONT_SIZE_UI,
  lineHeight: 1.4,
  color: TEXT_PRIMARY,
  fontWeight: 700,
};

const helpTextStyle: React.CSSProperties = {
  fontSize: 12,
  lineHeight: 1.45,
  color: TEXT_MUTED,
};

const controlStyle: React.CSSProperties = {
  width: "100%",
  height: 38,
  borderRadius: 12,
  border: "1px solid rgba(255,255,255,0.12)",
  background: "rgba(255,255,255,0.04)",
  color: TEXT_PRIMARY,
  padding: "0 12px",
  fontSize: FONT_SIZE_UI,
  outline: "none",
};

const actionButtonBaseStyle: React.CSSProperties = {
  height: 32,
  padding: "0 12px",
  borderRadius: 999,
  border: "1px solid rgba(255,255,255,0.14)",
  color: TEXT_PRIMARY,
  fontSize: FONT_SIZE_UI,
  fontWeight: 700,
};

function Field(props: {
  label: string;
  helpText?: string | null;
  children: React.ReactNode;
}) {
  const { label, helpText, children } = props;

  return (
    <label style={{ display: "grid", gap: 6 }}>
      <span style={labelTextStyle}>{label}</span>
      {children}
      {helpText ? <span style={helpTextStyle}>{helpText}</span> : null}
    </label>
  );
}

export function BadgeQualificationFormSection(props: Props) {
  const {
    badges,
    form,
    modeInputs,
    modeFieldText,
    previewLoading,
    awardLoading,
    previewRowCount,
    previewError,
    awardError,
    awardMessage,
    recordingQuery,
    recordingResults,
    recordingSearchError,
    recordingSearchLoading,
    selectedRecording,
    onFormChange,
    onRecordingQueryChange,
    onRunRecordingSearch,
    onSelectRecording,
    onClearSelectedRecording,
    onRunPreview,
    onRunAward,
  } = props;

  return (
    <section
      style={{
        border: PANEL_BORDER,
        borderRadius: 18,
        background: BG_INSET,
        padding: 16,
        display: "grid",
        gap: 16,
      }}
    >
      <div style={{ display: "grid", gap: 4 }}>
        <h2
          style={{
            margin: 0,
            fontSize: 18,
            lineHeight: 1.15,
            color: TEXT_PRIMARY,
          }}
        >
          Award badge
        </h2>
        <p
          style={{
            margin: 0,
            maxWidth: 820,
            fontSize: FONT_SIZE_UI,
            lineHeight: 1.5,
            color: TEXT_MUTED,
          }}
        >
          Choose a badge, define a qualifying cohort, preview matching members,
          and then execute a durable entitlement grant.
        </p>
      </div>

      <div
        style={{
          display: "grid",
          gap: 12,
          gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
        }}
      >
        <Field label="Badge">
          <select
            value={form.entitlementKey}
            onChange={(event) =>
              onFormChange("entitlementKey", event.target.value)
            }
            style={controlStyle}
          >
            {badges.map((badge) => (
              <option key={badge.entitlementKey} value={badge.entitlementKey}>
                {badge.title} ({badge.entitlementKey})
              </option>
            ))}
          </select>
        </Field>

        <Field label="Qualification mode">
          <select
            value={form.mode}
            onChange={(event) =>
              onFormChange("mode", event.target.value as BadgeQualificationMode)
            }
            style={controlStyle}
          >
            {BADGE_PREVIEW_MODES.map((mode) => (
              <option key={mode.key} value={mode.key}>
                {mode.label}
              </option>
            ))}
          </select>
        </Field>
      </div>

      {modeInputs.minMinutes ? (
        <Field
          label={modeFieldText.minMinutesLabel}
          helpText={modeFieldText.minMinutesHelp}
        >
          <input
            value={form.minMinutes}
            onChange={(event) => onFormChange("minMinutes", event.target.value)}
            inputMode="numeric"
            style={controlStyle}
          />
        </Field>
      ) : null}

      {modeInputs.minPlayCount && !modeInputs.activeWindow ? (
        <Field
          label={modeFieldText.minPlayCountLabel}
          helpText={modeFieldText.minPlayCountHelp}
        >
          <input
            value={form.minPlayCount}
            onChange={(event) =>
              onFormChange("minPlayCount", event.target.value)
            }
            inputMode="numeric"
            style={controlStyle}
          />
        </Field>
      ) : null}

      {modeInputs.minCompletedCount && !modeInputs.activeWindow ? (
        <Field
          label={modeFieldText.minCompletedCountLabel}
          helpText={modeFieldText.minCompletedCountHelp}
        >
          <input
            value={form.minCompletedCount}
            onChange={(event) =>
              onFormChange("minCompletedCount", event.target.value)
            }
            inputMode="numeric"
            style={controlStyle}
          />
        </Field>
      ) : null}

      {modeInputs.minContributionCount ? (
        <Field
          label={modeFieldText.minContributionCountLabel}
          helpText={modeFieldText.minContributionCountHelp}
        >
          <input
            value={form.minContributionCount}
            onChange={(event) =>
              onFormChange("minContributionCount", event.target.value)
            }
            inputMode="numeric"
            style={controlStyle}
          />
        </Field>
      ) : null}

      {modeInputs.minVoteCount ? (
        <Field
          label={modeFieldText.minVoteCountLabel}
          helpText={modeFieldText.minVoteCountHelp}
        >
          <input
            value={form.minVoteCount}
            onChange={(event) =>
              onFormChange("minVoteCount", event.target.value)
            }
            inputMode="numeric"
            style={controlStyle}
          />
        </Field>
      ) : null}

      {modeInputs.joinedWindow ? (
        <div style={{ display: "grid", gap: 12 }}>
          <div
            style={{
              display: "grid",
              gap: 12,
              gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
            }}
          >
            <Field label={modeFieldText.joinedOnOrAfterLabel}>
              <input
                type="datetime-local"
                value={form.joinedOnOrAfter}
                onChange={(event) =>
                  onFormChange("joinedOnOrAfter", event.target.value)
                }
                style={controlStyle}
              />
            </Field>

            <Field label={modeFieldText.joinedBeforeLabel}>
              <input
                type="datetime-local"
                value={form.joinedBefore}
                onChange={(event) =>
                  onFormChange("joinedBefore", event.target.value)
                }
                style={controlStyle}
              />
            </Field>
          </div>

          {modeFieldText.joinedWindowHelp ? (
            <span style={helpTextStyle}>{modeFieldText.joinedWindowHelp}</span>
          ) : null}
        </div>
      ) : null}

      {modeInputs.activeWindow ? (
        <div style={{ display: "grid", gap: 12 }}>
          <div
            style={{
              display: "grid",
              gap: 12,
              gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
            }}
          >
            <Field label={modeFieldText.activeOnOrAfterLabel}>
              <input
                type="datetime-local"
                value={form.activeOnOrAfter}
                onChange={(event) =>
                  onFormChange("activeOnOrAfter", event.target.value)
                }
                style={controlStyle}
              />
            </Field>

            <Field label={modeFieldText.activeBeforeLabel}>
              <input
                type="datetime-local"
                value={form.activeBefore}
                onChange={(event) =>
                  onFormChange("activeBefore", event.target.value)
                }
                style={controlStyle}
              />
            </Field>
          </div>

          <div
            style={{
              display: "grid",
              gap: 12,
              gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
            }}
          >
            <Field
              label={modeFieldText.minPlayCountLabel}
              helpText={modeFieldText.minPlayCountHelp}
            >
              <input
                value={form.minPlayCount}
                onChange={(event) =>
                  onFormChange("minPlayCount", event.target.value)
                }
                inputMode="numeric"
                style={controlStyle}
              />
            </Field>

            <Field
              label={modeFieldText.minProgressCountLabel}
              helpText={modeFieldText.minProgressCountHelp}
            >
              <input
                value={form.minProgressCount}
                onChange={(event) =>
                  onFormChange("minProgressCount", event.target.value)
                }
                inputMode="numeric"
                style={controlStyle}
              />
            </Field>

            <Field
              label={modeFieldText.minCompletedCountLabel}
              helpText={modeFieldText.minCompletedCountHelp}
            >
              <input
                value={form.minCompletedCount}
                onChange={(event) =>
                  onFormChange("minCompletedCount", event.target.value)
                }
                inputMode="numeric"
                style={controlStyle}
              />
            </Field>
          </div>

          {modeFieldText.activeWindowHelp ? (
            <span style={helpTextStyle}>{modeFieldText.activeWindowHelp}</span>
          ) : null}
        </div>
      ) : null}

      {modeInputs.recordingId ? (
        <RecordingPicker
          label={modeFieldText.recordingIdLabel}
          helpText={modeFieldText.recordingIdHelp}
          query={recordingQuery}
          results={recordingResults}
          error={recordingSearchError}
          loading={recordingSearchLoading}
          selectedRecording={selectedRecording}
          selectedRecordingId={form.recordingId}
          onQueryChange={onRecordingQueryChange}
          onRunSearch={onRunRecordingSearch}
          onSelectRecording={onSelectRecording}
          onClearSelectedRecording={onClearSelectedRecording}
        />
      ) : null}

      <Field label="Grant reason">
        <input
          value={form.grantReason}
          onChange={(event) => onFormChange("grantReason", event.target.value)}
          placeholder="Optional note stored with the grant"
          style={controlStyle}
        />
      </Field>
      <Field label="Preview limit">
        <input
          value={form.limit}
          onChange={(event) => onFormChange("limit", event.target.value)}
          inputMode="numeric"
          style={controlStyle}
        />
      </Field>

      <div style={{ display: "flex", gap: 12, flexWrap: "wrap" }}>
        <button
          type="button"
          onClick={onRunPreview}
          disabled={previewLoading}
          style={{
            ...actionButtonBaseStyle,
            background: "rgba(255,255,255,0.04)",
            cursor: previewLoading ? "default" : "pointer",
            opacity: previewLoading ? 0.72 : 1,
          }}
        >
          {previewLoading ? "Previewing…" : "Preview cohort"}
        </button>

        <button
          type="button"
          onClick={onRunAward}
          disabled={awardLoading || previewLoading || previewRowCount === 0}
          style={{
            ...actionButtonBaseStyle,
            background:
              awardLoading || previewLoading || previewRowCount === 0
                ? "rgba(255,255,255,0.04)"
                : "rgba(255,255,255,0.10)",
            cursor:
              awardLoading || previewLoading || previewRowCount === 0
                ? "default"
                : "pointer",
            opacity:
              awardLoading || previewLoading || previewRowCount === 0
                ? 0.72
                : 1,
          }}
        >
          {awardLoading ? "Awarding…" : "Award badge"}
        </button>
      </div>

      {previewError ? (
        <div
          style={{
            borderRadius: 12,
            border: "1px solid rgba(255,143,143,0.22)",
            background: "rgba(255,143,143,0.08)",
            padding: "10px 12px",
            color: "#ffb1b1",
            fontSize: FONT_SIZE_UI,
            lineHeight: 1.5,
          }}
        >
          {previewError}
        </div>
      ) : null}

      {awardError ? (
        <div
          style={{
            borderRadius: 12,
            border: "1px solid rgba(255,143,143,0.22)",
            background: "rgba(255,143,143,0.08)",
            padding: "10px 12px",
            color: "#ffb1b1",
            fontSize: FONT_SIZE_UI,
            lineHeight: 1.5,
          }}
        >
          {awardError}
        </div>
      ) : null}

      {awardMessage ? (
        <div
          style={{
            borderRadius: 12,
            border: "1px solid rgba(159,240,184,0.22)",
            background: "rgba(159,240,184,0.08)",
            padding: "10px 12px",
            color: "#baf4ca",
            fontSize: FONT_SIZE_UI,
            lineHeight: 1.5,
          }}
        >
          {awardMessage}
        </div>
      ) : null}
    </section>
  );
}


----- app/admin/badges/_components/PreviewResultsSection.tsx -----
// web/app/admin/badges/_components/PreviewResultsSection.tsx
"use client";

import React from "react";
import {
  BG_INSET,
  FONT_SIZE_UI,
  PANEL_BORDER,
  TEXT_MUTED,
  TEXT_PRIMARY,
} from "../../playback/dashboard/playbackTelemetryDashboardStyles";
import type { PreviewRow } from "../_lib/badgeDashboardTypes";
import { formatDateTime, formatMetric } from "../_lib/badgeDashboardUtils";

type Props = {
  rows: PreviewRow[];
  count: number;
};

const thStyle: React.CSSProperties = {
  textAlign: "left",
  padding: "10px 12px",
  fontSize: 12,
  lineHeight: 1.3,
  fontWeight: 700,
  color: TEXT_MUTED,
  borderBottom: "1px solid rgba(255,255,255,0.08)",
  whiteSpace: "nowrap",
};

const tdStyle: React.CSSProperties = {
  padding: "12px",
  borderTop: "1px solid rgba(255,255,255,0.08)",
  fontSize: FONT_SIZE_UI,
  lineHeight: 1.45,
  color: TEXT_PRIMARY,
  verticalAlign: "top",
};

const tdNumericStyle: React.CSSProperties = {
  ...tdStyle,
  textAlign: "right",
  fontVariantNumeric: "tabular-nums",
};

export function PreviewResultsSection(props: Props) {
  const { rows, count } = props;

  return (
    <section
      style={{
        border: PANEL_BORDER,
        borderRadius: 18,
        background: BG_INSET,
        padding: 16,
        display: "grid",
        gap: 12,
      }}
    >
      <div
        style={{
          display: "flex",
          justifyContent: "space-between",
          gap: 12,
          alignItems: "baseline",
          flexWrap: "wrap",
        }}
      >
        <div style={{ display: "grid", gap: 4 }}>
          <h2
            style={{
              margin: 0,
              fontSize: 18,
              lineHeight: 1.15,
              color: TEXT_PRIMARY,
            }}
          >
            Preview results
          </h2>
          <p
            style={{
              margin: 0,
              fontSize: FONT_SIZE_UI,
              lineHeight: 1.5,
              color: TEXT_MUTED,
            }}
          >
            Matching members resolved from the current badge qualification
            cohort.
          </p>
        </div>

        <div
          style={{
            fontSize: FONT_SIZE_UI,
            lineHeight: 1.5,
            color: TEXT_MUTED,
          }}
        >
          {count.toLocaleString()} matching member{count === 1 ? "" : "s"}
        </div>
      </div>

      <div
        style={{
          overflowX: "auto",
          borderRadius: 14,
          border: "1px solid rgba(255,255,255,0.08)",
          background: "rgba(255,255,255,0.025)",
        }}
      >
        <table
          style={{
            width: "100%",
            minWidth: 1180,
            borderCollapse: "separate",
            borderSpacing: 0,
          }}
        >
          <thead>
            <tr>
              <th style={thStyle}>Member</th>
              <th style={thStyle}>Member ID</th>
              <th style={thStyle}>Joined</th>
              <th style={{ ...thStyle, textAlign: "right" }}>Minutes</th>
              <th style={{ ...thStyle, textAlign: "right" }}>Plays</th>
              <th style={{ ...thStyle, textAlign: "right" }}>Completes</th>
              <th style={{ ...thStyle, textAlign: "right" }}>Window events</th>
              <th style={{ ...thStyle, textAlign: "right" }}>Exegesis</th>
              <th style={{ ...thStyle, textAlign: "right" }}>Votes</th>
              <th style={thStyle}>Public name</th>
              <th style={thStyle}>Recording</th>
            </tr>
          </thead>

          <tbody>
            {rows.length === 0 ? (
              <tr>
                <td
                  colSpan={11}
                  style={{
                    padding: "18px 12px",
                    fontSize: FONT_SIZE_UI,
                    lineHeight: 1.5,
                    color: TEXT_MUTED,
                  }}
                >
                  No preview results yet.
                </td>
              </tr>
            ) : (
              rows.map((row) => (
                <tr key={row.memberId}>
                  <td style={tdStyle}>
                    <div style={{ display: "grid", gap: 2 }}>
                      <span>{row.email || row.memberId}</span>
                    </div>
                  </td>

                  <td style={tdStyle}>
                    <span
                      style={{
                        fontSize: 12,
                        lineHeight: 1.45,
                        color: TEXT_MUTED,
                        wordBreak: "break-word",
                      }}
                    >
                      {row.memberId}
                    </span>
                  </td>

                  <td style={tdStyle}>{formatDateTime(row.joinedAt)}</td>

                  <td style={tdNumericStyle}>
                    {formatMetric(row.minutesStreamed)}
                  </td>

                  <td style={tdNumericStyle}>{formatMetric(row.playCount)}</td>

                  <td style={tdNumericStyle}>
                    {formatMetric(row.completedCount)}
                  </td>

                  <td style={tdNumericStyle}>
                    {formatMetric(row.matchedWindowEventCount)}
                  </td>

                  <td style={tdNumericStyle}>
                    {formatMetric(row.contributionCount)}
                  </td>

                  <td style={tdNumericStyle}>
                    {formatMetric(row.exegesisVoteCount)}
                  </td>

                  <td style={tdStyle}>
                    {row.publicNameUnlockedAt ? (
                      formatDateTime(row.publicNameUnlockedAt)
                    ) : (
                      <span style={{ color: TEXT_MUTED }}>—</span>
                    )}
                  </td>

                  <td style={tdStyle}>
                    {row.matchedRecordingId ? (
                      <div style={{ display: "grid", gap: 3 }}>
                        <span>
                          {row.matchedRecordingTitle || row.matchedRecordingId}
                        </span>
                        <span
                          style={{
                            fontSize: 12,
                            lineHeight: 1.45,
                            color: TEXT_MUTED,
                            wordBreak: "break-word",
                          }}
                        >
                          {row.matchedRecordingId}
                        </span>
                      </div>
                    ) : (
                      <span style={{ color: TEXT_MUTED }}>—</span>
                    )}
                  </td>
                </tr>
              ))
            )}
          </tbody>
        </table>
      </div>
    </section>
  );
}


----- app/admin/badges/_components/RecordingPicker.tsx -----
"use client";

import React from "react";
import type { RecordingSearchResult } from "../_lib/badgeDashboardTypes";

type Props = {
  label: string;
  helpText: string | null;
  query: string;
  results: RecordingSearchResult[];
  error: string | null;
  loading: boolean;
  selectedRecording: RecordingSearchResult | null;
  selectedRecordingId: string;
  onQueryChange: (value: string) => void;
  onRunSearch: () => void;
  onSelectRecording: (recording: RecordingSearchResult) => void;
  onClearSelectedRecording: () => void;
};

export function RecordingPicker(props: Props) {
  const {
    label,
    helpText,
    query,
    results,
    error,
    loading,
    selectedRecording,
    selectedRecordingId,
    onQueryChange,
    onRunSearch,
    onSelectRecording,
    onClearSelectedRecording,
  } = props;

  return (
    <div style={{ display: "grid", gap: 10 }}>
      <span>{label}</span>

      <div
        style={{
          display: "grid",
          gap: 10,
          gridTemplateColumns: "minmax(0, 1fr) auto",
          alignItems: "end",
        }}
      >
        <label style={{ display: "grid", gap: 6 }}>
          <span style={{ opacity: 0.78, fontSize: 12 }}>Search recording</span>
          <input
            value={query}
            onChange={(event) => onQueryChange(event.target.value)}
            placeholder="Track title, artist, album, or recording ID"
            onKeyDown={(event) => {
              if (event.key === "Enter") {
                event.preventDefault();
                onRunSearch();
              }
            }}
          />
        </label>

        <button type="button" onClick={onRunSearch} disabled={loading}>
          {loading ? "Searching…" : "Search"}
        </button>
      </div>

      {helpText ? (
        <span style={{ opacity: 0.62, fontSize: 12 }}>{helpText}</span>
      ) : null}

      {selectedRecording ? (
        <div
          style={{
            padding: 12,
            borderRadius: 12,
            background: "rgba(255,255,255,0.05)",
            border: "1px solid rgba(255,255,255,0.08)",
            display: "grid",
            gap: 6,
          }}
        >
          <div
            style={{
              display: "flex",
              justifyContent: "space-between",
              gap: 12,
              alignItems: "flex-start",
              flexWrap: "wrap",
            }}
          >
            <div style={{ display: "grid", gap: 4 }}>
              <strong>{selectedRecording.title}</strong>
              <span style={{ opacity: 0.75 }}>
                {selectedRecording.artist || "Unknown artist"}
                {selectedRecording.albumTitle
                  ? ` • ${selectedRecording.albumTitle}`
                  : ""}
              </span>
              <span style={{ opacity: 0.58, fontSize: 12 }}>
                {selectedRecording.recordingId}
              </span>
            </div>

            <button type="button" onClick={onClearSelectedRecording}>
              Clear
            </button>
          </div>
        </div>
      ) : null}

      {error ? <div style={{ color: "#ffb3b3", fontSize: 13 }}>{error}</div> : null}

      {!selectedRecording && results.length > 0 ? (
        <div
          style={{
            display: "grid",
            gap: 8,
          }}
        >
          {results.map((recording) => (
            <button
              key={recording.recordingId}
              type="button"
              onClick={() => onSelectRecording(recording)}
              style={{
                appearance: "none",
                textAlign: "left",
                color: "inherit",
                background: "rgba(255,255,255,0.03)",
                border: "1px solid rgba(255,255,255,0.08)",
                borderRadius: 12,
                padding: 12,
                cursor: "pointer",
                display: "grid",
                gap: 4,
              }}
            >
              <strong>{recording.title}</strong>
              <span style={{ opacity: 0.75 }}>
                {recording.artist || "Unknown artist"}
                {recording.albumTitle ? ` • ${recording.albumTitle}` : ""}
              </span>
              <span style={{ opacity: 0.55, fontSize: 12 }}>
                {recording.recordingId}
              </span>
            </button>
          ))}
        </div>
      ) : null}

      {selectedRecordingId ? (
        <span style={{ opacity: 0.55, fontSize: 12 }}>
          Selected recording ID: {selectedRecordingId}
        </span>
      ) : null}
    </div>
  );
}

----- app/admin/badges/page.tsx -----
// web/app/admin/badges/page.tsx
import "server-only";

import BadgeDashboardClient from "./BadgeDashboardClient";
import { getActiveBadgeDefinitions } from "@/lib/badges";

export default async function Page(props: {
  searchParams?: Promise<Record<string, string | string[] | undefined>>;
}) {
  const sp = (await props.searchParams) ?? {};
  const embed = typeof sp.embed === "string" && sp.embed === "1";

  const badgeDefinitions = await getActiveBadgeDefinitions();

  const badgeDefinitionOptions = badgeDefinitions.map((badge) => ({
    entitlementKey: badge.entitlementKey,
    title: badge.title,
    description: badge.description,
    displayOrder: badge.displayOrder,
    imageUrl: badge.imageUrl,
    featured: badge.featured,
    shareable: badge.shareable,
    undisclosed: badge.undisclosed,
    awardMode: badge.awardMode,
    autoAwardEnabled: badge.autoAward?.enabled ?? false,
    autoQualificationMode: badge.autoAward?.qualificationMode ?? null,
  }));

  return (
    <BadgeDashboardClient
      embed={embed}
      badgeDefinitions={badgeDefinitionOptions}
    />
  );
}


----- app/api/admin/badges/award/route.ts -----
// web/app/api/admin/badges/award/route.ts
import { NextResponse } from "next/server";
import { requireAdminMemberId } from "@/lib/adminAuth";
import { awardBadgeToMembers } from "@/lib/badgeAdmin";
import { areBadgesEnabled } from "@/lib/badgeFeature";

function isRecord(value: unknown): value is Record<string, unknown> {
  return typeof value === "object" && value !== null;
}

function asString(value: unknown): string | null {
  return typeof value === "string" && value.trim() ? value.trim() : null;
}

function asStringArray(value: unknown): string[] {
  if (!Array.isArray(value)) return [];

  const rows: string[] = [];
  for (const item of value) {
    if (typeof item !== "string") continue;
    const trimmed = item.trim();
    if (!trimmed) continue;
    rows.push(trimmed);
  }

  return rows;
}

export async function POST(request: Request) {
  try {
    const adminMemberId = await requireAdminMemberId();

    if (!areBadgesEnabled()) {
      return NextResponse.json(
        {
          ok: false,
          error: "Badges are currently disabled.",
          code: "BADGES_DISABLED",
        },
        { status: 409 },
      );
    }

    const body = (await request.json()) as unknown;
    if (!isRecord(body)) {
      throw new Error("Invalid request body.");
    }

    const entitlementKey = asString(body.entitlementKey);
    if (!entitlementKey) {
      throw new Error("entitlementKey is required.");
    }

    if (!entitlementKey.startsWith("badge_")) {
      throw new Error("entitlementKey must start with badge_.");
    }

    const memberIds = asStringArray(body.memberIds);
    if (memberIds.length === 0) {
      throw new Error("At least one memberId is required.");
    }

    const grantReason = asString(body.grantReason);
    const grantSource = asString(body.grantSource) ?? "badge_admin_preview";
    const grantSourceRef = asString(body.grantSourceRef);

    const result = await awardBadgeToMembers({
      entitlementKey,
      memberIds,
      grantedBy: adminMemberId,
      grantReason: grantReason ?? undefined,
      grantSource,
      grantSourceRef: grantSourceRef ?? undefined,
    });

    return NextResponse.json({
      ok: true,
      result,
      summary: {
        attempted: result.attempted,
        inserted: result.inserted,
        alreadyHeld: result.alreadyHeld,
        hasNewGrants: result.inserted > 0,
      },
    });
  } catch (error) {
    const message =
      error instanceof Error ? error.message : "Unable to award badge.";

    const status =
      message === "Unauthorized" ? 401 : message === "Forbidden" ? 403 : 400;

    return NextResponse.json(
      {
        ok: false,
        error: message,
      },
      { status },
    );
  }
}


----- app/api/admin/badges/preview/route.ts -----
// web/app/api/admin/badges/preview/route.ts
import { NextResponse } from "next/server";
import { requireAdminMemberId } from "@/lib/adminAuth";
import {
  previewBadgeQualification,
  type BadgePreviewInput,
} from "@/lib/badgeAdmin";
import {
  BADGE_PREVIEW_MODE_DESCRIPTORS,
  type BadgeQualificationMode,
} from "@/lib/badgePreviewModes";

function isRecord(value: unknown): value is Record<string, unknown> {
  return typeof value === "object" && value !== null;
}

function asString(value: unknown): string | null {
  return typeof value === "string" && value.trim() ? value.trim() : null;
}

function asOptionalString(value: unknown): string | null {
  if (value === null || value === undefined) return null;
  return asString(value);
}

function asPositiveNumber(value: unknown): number | null {
  if (typeof value === "number" && Number.isFinite(value) && value >= 0) {
    return value;
  }

  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    if (Number.isFinite(parsed) && parsed >= 0) return parsed;
  }

  return null;
}

function asOptionalLimit(value: unknown): number | undefined {
  const parsed = asPositiveNumber(value);
  return parsed === null ? undefined : Math.floor(parsed);
}

function isBadgeQualificationMode(
  value: string,
): value is BadgeQualificationMode {
  return value in BADGE_PREVIEW_MODE_DESCRIPTORS;
}

function parsePreviewInput(body: unknown): BadgePreviewInput {
  if (!isRecord(body)) {
    throw new Error("Invalid request body.");
  }

  const mode = asString(body.mode);
  if (!mode) {
    throw new Error("Mode is required.");
  }

  if (!isBadgeQualificationMode(mode)) {
    throw new Error("Unsupported badge preview mode.");
  }

  const limit = asOptionalLimit(body.limit);

  switch (mode) {
    case "minutes_streamed": {
      const minMinutes = asPositiveNumber(body.minMinutes);
      if (minMinutes === null) {
        throw new Error("minMinutes is required.");
      }

      return {
        mode,
        minMinutes,
        limit,
      };
    }

    case "play_count": {
      const minPlayCount = asPositiveNumber(body.minPlayCount);
      if (minPlayCount === null) {
        throw new Error("minPlayCount is required.");
      }

      return {
        mode,
        minPlayCount,
        limit,
      };
    }

    case "complete_count": {
      const minCompletedCount = asPositiveNumber(body.minCompletedCount);
      if (minCompletedCount === null) {
        throw new Error("minCompletedCount is required.");
      }

      return {
        mode,
        minCompletedCount,
        limit,
      };
    }

    case "joined_within_window": {
      const joinedOnOrAfter = asString(body.joinedOnOrAfter);
      if (!joinedOnOrAfter) {
        throw new Error("joinedOnOrAfter is required.");
      }

      return {
        mode,
        joinedOnOrAfter,
        joinedBefore: asOptionalString(body.joinedBefore),
        limit,
      };
    }

    case "active_within_window": {
      const activeOnOrAfter = asString(body.activeOnOrAfter);
      if (!activeOnOrAfter) {
        throw new Error("activeOnOrAfter is required.");
      }

      return {
        mode,
        activeOnOrAfter,
        activeBefore: asOptionalString(body.activeBefore),
        minPlayCount: asPositiveNumber(body.minPlayCount) ?? 0,
        minProgressCount: asPositiveNumber(body.minProgressCount) ?? 0,
        minCompleteCount: asPositiveNumber(body.minCompleteCount) ?? 0,
        limit,
      };
    }

    case "recording_minutes_streamed": {
      const recordingId = asString(body.recordingId);
      const minMinutes = asPositiveNumber(body.minMinutes);

      if (!recordingId) {
        throw new Error("recordingId is required.");
      }
      if (minMinutes === null) {
        throw new Error("minMinutes is required.");
      }

      return {
        mode,
        recordingId,
        minMinutes,
        limit,
      };
    }

    case "recording_play_count": {
      const recordingId = asString(body.recordingId);
      const minPlayCount = asPositiveNumber(body.minPlayCount);

      if (!recordingId) {
        throw new Error("recordingId is required.");
      }
      if (minPlayCount === null) {
        throw new Error("minPlayCount is required.");
      }

      return {
        mode,
        recordingId,
        minPlayCount,
        limit,
      };
    }

    case "recording_complete_count": {
      const recordingId = asString(body.recordingId);
      const minCompletedCount = asPositiveNumber(body.minCompletedCount);

      if (!recordingId) {
        throw new Error("recordingId is required.");
      }
      if (minCompletedCount === null) {
        throw new Error("minCompletedCount is required.");
      }

      return {
        mode,
        recordingId,
        minCompletedCount,
        limit,
      };
    }

    case "exegesis_contribution_count": {
      const minContributionCount = asPositiveNumber(body.minContributionCount);
      if (minContributionCount === null) {
        throw new Error("minContributionCount is required.");
      }

      return {
        mode,
        minContributionCount,
        limit,
      };
    }

    case "exegesis_vote_tally": {
      const minVoteCount = asPositiveNumber(body.minVoteCount);
      if (minVoteCount === null) {
        throw new Error("minVoteCount is required.");
      }

      return {
        mode,
        minVoteCount,
        limit,
      };
    }

    case "public_name_unlocked": {
      return {
        mode,
        limit,
      };
    }

    default: {
      const exhaustiveCheck: never = mode;
      throw new Error(
        `Unhandled badge preview mode: ${String(exhaustiveCheck)}`,
      );
    }
  }
}

export async function POST(request: Request) {
  try {
    await requireAdminMemberId();

    const body = (await request.json()) as unknown;
    const input = parsePreviewInput(body);
    const rows = await previewBadgeQualification(input);

    return NextResponse.json({
      ok: true,
      count: rows.length,
      rows,
    });
  } catch (error) {
    const message =
      error instanceof Error
        ? error.message
        : "Unable to preview badge cohort.";

    const status =
      message === "Unauthorized" ? 401 : message === "Forbidden" ? 403 : 400;

    return NextResponse.json(
      {
        ok: false,
        error: message,
      },
      { status },
    );
  }
}


----- app/home/badges/BadgeAwardOverlay.tsx -----
// web/app/home/badges/BadgeAwardOverlay.tsx
"use client";

import React from "react";
import { createPortal } from "react-dom";
import BadgeAwardRevealCard from "./BadgeAwardRevealCard";
import type { BadgeAwardNotice } from "./badgeAwardTypes";

type Props = {
  active: boolean;
  visible: boolean;
  badge: BadgeAwardNotice | null;
  onDismiss: () => void;
  allowAdminRibbonAbove?: boolean;
};

function BodyPortal(props: { children: React.ReactNode }) {
  const [mounted, setMounted] = React.useState(false);

  React.useEffect(() => {
    setMounted(true);
  }, []);

  if (!mounted) return null;
  if (typeof document === "undefined") return null;

  return createPortal(props.children, document.body);
}

function useAdminRibbonAboveOverlay(active: boolean) {
  const debugbarStyleRef = React.useRef<string | null>(null);

  React.useEffect(() => {
    const getEl = () =>
      typeof document !== "undefined"
        ? document.getElementById("af-admin-debugbar")
        : null;

    const el = getEl();
    if (!el) return;

    if (debugbarStyleRef.current == null) {
      debugbarStyleRef.current = el.getAttribute("style") ?? "";
    }

    if (active) {
      el.setAttribute(
        "style",
        `${debugbarStyleRef.current}; position: relative; z-index: 50000; pointer-events: auto;`,
      );
    } else {
      const orig = debugbarStyleRef.current ?? "";
      if (orig.trim()) el.setAttribute("style", orig);
      else el.removeAttribute("style");
    }

    return () => {
      const el2 = getEl();
      if (!el2) return;
      const orig = debugbarStyleRef.current ?? "";
      if (orig.trim()) el2.setAttribute("style", orig);
      else el2.removeAttribute("style");
    };
  }, [active]);
}

export default function BadgeAwardOverlay(props: Props) {
  const {
    active,
    visible,
    badge,
    onDismiss,
    allowAdminRibbonAbove = true,
  } = props;

  useAdminRibbonAboveOverlay(allowAdminRibbonAbove && active);

  if (!active || !badge) return null;

  return (
    <BodyPortal>
      <div
        aria-hidden="true"
        role="button"
        tabIndex={0}
        onClick={onDismiss}
        onKeyDown={(e) => {
          if (e.key === "Enter" || e.key === " ") {
            e.preventDefault();
            onDismiss();
          }
        }}
        style={{
          position: "fixed",
          inset: 0,
          zIndex: 21000,
          pointerEvents: "auto",
          backdropFilter: "blur(10px)",
          WebkitBackdropFilter: "blur(10px)",
          background: visible ? "rgba(0,0,0,0.24)" : "rgba(0,0,0,0.00)",
          opacity: visible ? 1 : 0,
          transition:
            "opacity 220ms ease, background-color 220ms ease, backdrop-filter 220ms ease",
        }}
      />

      <div
        role="button"
        tabIndex={0}
        aria-label="Dismiss badge award overlay"
        onClick={(e) => {
          if (e.target === e.currentTarget) onDismiss();
        }}
        onKeyDown={(e) => {
          if (e.target !== e.currentTarget) return;
          if (e.key === "Enter" || e.key === " ") {
            e.preventDefault();
            onDismiss();
          }
        }}
        style={{
          position: "fixed",
          inset: 0,
          zIndex: 31000,
          pointerEvents: "auto",
          display: "grid",
          placeItems: "center",
          padding: "min(8vh, 64px) 16px",
          opacity: visible ? 1 : 0,
          transform: visible ? "scale(1)" : "scale(0.985)",
          transition: "opacity 220ms ease, transform 220ms ease",
        }}
      >
        <div
          role="status"
          aria-live="polite"
          aria-atomic="true"
          style={{
            width: "100%",
            display: "grid",
            placeItems: "center",
          }}
        >
          <BadgeAwardRevealCard badge={badge} dismissHintVisible={visible} />
        </div>
      </div>
    </BodyPortal>
  );
}


----- app/home/badges/BadgeAwardOverlayProvider.tsx -----
// web/app/home/badges/BadgeAwardOverlayProvider.tsx
"use client";

import React from "react";
import BadgeAwardOverlay from "./BadgeAwardOverlay";
import type { BadgeAwardNotice } from "./badgeAwardTypes";

type BadgeAwardOverlayContextValue = {
  announceBadge: (badge: BadgeAwardNotice) => void;
  announceBadges: (badges: BadgeAwardNotice[]) => void;
  resetOverlayDebugState: () => void;
};

const BadgeAwardOverlayContext =
  React.createContext<BadgeAwardOverlayContextValue | null>(null);

const DISPLAY_MS = 5600;
const EXIT_MS = 220;
const RECENT_DEDUPE_WINDOW_MS = 10_000;

type ProviderProps = {
  children: React.ReactNode;
};

function dedupeIncomingBadges(
  badges: BadgeAwardNotice[],
  recentSeenRef: React.MutableRefObject<Map<string, number>>,
): BadgeAwardNotice[] {
  const now = Date.now();

  for (const [key, expiresAt] of recentSeenRef.current.entries()) {
    if (expiresAt <= now) {
      recentSeenRef.current.delete(key);
    }
  }

  const unique: BadgeAwardNotice[] = [];
  const seenThisBatch = new Set<string>();

  for (const badge of badges) {
    const key = badge.entitlementKey.trim();
    if (!key) continue;
    if (seenThisBatch.has(key)) continue;
    if (recentSeenRef.current.has(key)) continue;

    seenThisBatch.add(key);
    unique.push(badge);
  }

  return unique;
}

export function BadgeAwardOverlayProvider(props: ProviderProps) {
  const { children } = props;

  const [queue, setQueue] = React.useState<BadgeAwardNotice[]>([]);
  const [activeBadge, setActiveBadge] = React.useState<BadgeAwardNotice | null>(
    null,
  );
  const [visible, setVisible] = React.useState(false);

  const recentSeenRef = React.useRef(new Map<string, number>());
  const dismissTimeoutRef = React.useRef<number | null>(null);
  const clearActiveTimeoutRef = React.useRef<number | null>(null);

  const clearTimers = React.useCallback(() => {
    if (dismissTimeoutRef.current !== null) {
      window.clearTimeout(dismissTimeoutRef.current);
      dismissTimeoutRef.current = null;
    }

    if (clearActiveTimeoutRef.current !== null) {
      window.clearTimeout(clearActiveTimeoutRef.current);
      clearActiveTimeoutRef.current = null;
    }
  }, []);

  React.useEffect(() => {
    return () => {
      clearTimers();
    };
  }, [clearTimers]);

  const dismissActive = React.useCallback(() => {
    clearTimers();
    setVisible(false);

    clearActiveTimeoutRef.current = window.setTimeout(() => {
      setActiveBadge(null);
      clearActiveTimeoutRef.current = null;
    }, EXIT_MS);
  }, [clearTimers]);

  const announceBadges = React.useCallback((badges: BadgeAwardNotice[]) => {
    if (!Array.isArray(badges) || badges.length === 0) return;

    setQueue((current) => {
      const next = dedupeIncomingBadges(badges, recentSeenRef);
      if (next.length === 0) return current;
      return current.concat(next);
    });
  }, []);

  const announceBadge = React.useCallback(
    (badge: BadgeAwardNotice) => {
      announceBadges([badge]);
    },
    [announceBadges],
  );

  const resetOverlayDebugState = React.useCallback(() => {
    clearTimers();
    recentSeenRef.current.clear();
    setQueue([]);
    setVisible(false);
    setActiveBadge(null);
  }, [clearTimers]);

  React.useEffect(() => {
    if (activeBadge !== null) return;
    if (queue.length === 0) return;

    const [nextBadge, ...rest] = queue;
    setQueue(rest);
    setActiveBadge(nextBadge);
    setVisible(true);

    recentSeenRef.current.set(
      nextBadge.entitlementKey,
      Date.now() + RECENT_DEDUPE_WINDOW_MS,
    );

    dismissTimeoutRef.current = window.setTimeout(() => {
      setVisible(false);

      clearActiveTimeoutRef.current = window.setTimeout(() => {
        setActiveBadge(null);
        clearActiveTimeoutRef.current = null;
      }, EXIT_MS);

      dismissTimeoutRef.current = null;
    }, DISPLAY_MS);
  }, [activeBadge, queue]);

  const contextValue = React.useMemo<BadgeAwardOverlayContextValue>(
    () => ({
      announceBadge,
      announceBadges,
      resetOverlayDebugState,
    }),
    [announceBadge, announceBadges, resetOverlayDebugState],
  );

  return (
    <BadgeAwardOverlayContext.Provider value={contextValue}>
      {children}

      <BadgeAwardOverlay
        active={activeBadge !== null}
        visible={visible}
        badge={activeBadge}
        onDismiss={dismissActive}
      />
    </BadgeAwardOverlayContext.Provider>
  );
}

export function useBadgeAwardOverlay(): BadgeAwardOverlayContextValue {
  const value = React.useContext(BadgeAwardOverlayContext);

  if (!value) {
    throw new Error(
      "useBadgeAwardOverlay must be used within BadgeAwardOverlayProvider.",
    );
  }

  return value;
}


----- app/home/badges/BadgeAwardRevealCard.tsx -----
// web/app/home/badges/BadgeAwardRevealCard.tsx
"use client";

import React from "react";
import BadgeUnlockVisual from "../modules/badges/BadgeUnlockVisual";
import BadgeUnlockVisualStyles from "../modules/badges/BadgeUnlockVisualStyles";
import type { BadgeAwardNotice } from "./badgeAwardTypes";

type Props = {
  badge: BadgeAwardNotice;
  dismissHintVisible: boolean;
};

export default function BadgeAwardRevealCard(props: Props) {
  const { badge, dismissHintVisible } = props;

  return (
    <>
      <BadgeUnlockVisualStyles />

      <style jsx global>{`
        @keyframes badgeAwardOverlayCardIn {
          0% {
            opacity: 0;
            transform: translateY(18px) scale(0.94);
          }
          100% {
            opacity: 1;
            transform: translateY(0) scale(1);
          }
        }

        @media (prefers-reduced-motion: reduce) {
          .badge-award-overlay-card {
            animation: none !important;
          }
        }
      `}</style>

      <div
        className="badge-award-overlay-card"
        style={{
          width: "100%",
          maxWidth: 420,
          borderRadius: 28,
          border: "1px solid rgba(255,255,255,0.16)",
          background: "rgba(10,10,14,0.90)",
          backdropFilter: "blur(18px)",
          WebkitBackdropFilter: "blur(18px)",
          boxShadow: `
            0 28px 80px rgba(0,0,0,0.56),
            0 0 0 1px rgba(255,255,255,0.04),
            0 60px 160px rgba(0,0,0,0.74)
          `,
          padding: 22,
          display: "grid",
          gap: 18,
          justifyItems: "center",
          animation:
            "badgeAwardOverlayCardIn 340ms cubic-bezier(0.22, 1, 0.36, 1) both",
        }}
      >
        <div
          style={{
            fontSize: 11,
            letterSpacing: 0.36,
            textTransform: "uppercase",
            lineHeight: 1.2,
            opacity: 0.58,
          }}
        >
          Badge unlocked
        </div>

        <div
          className="portal-badge-unlock-host"
          data-badge-expanded="true"
          style={{
            position: "relative",
            width: 176,
            height: 176,
            display: "grid",
            placeItems: "center",
            overflow: "visible",
          }}
        >
          <div
            aria-hidden="true"
            style={{
              position: "absolute",
              inset: -18,
              borderRadius: "50%",
              background:
                "radial-gradient(circle, rgba(255,255,255,0.10) 0%, rgba(255,255,255,0.04) 34%, rgba(255,255,255,0.00) 72%)",
              filter: "blur(10px)",
              pointerEvents: "none",
            }}
          />

          <div
            style={{
              position: "relative",
              width: "100%",
              height: "100%",
              outline: "none",
              perspective: "900px",
              perspectiveOrigin: "50% 50%",
            }}
          >
            <BadgeUnlockVisual
              imageUrl={badge.imageUrl ?? null}
              label={badge.title}
              unlocked={true}
              isUnlocking={true}
              isNewlyUnlocked={true}
              variant="overlay"
            />
          </div>
        </div>

        <div
          style={{
            display: "grid",
            gap: 8,
            textAlign: "center",
            width: "100%",
          }}
        >
          <div
            style={{
              fontSize: 22,
              lineHeight: 1.15,
              fontWeight: 700,
              letterSpacing: -0.01,
            }}
          >
            {badge.title}
          </div>

          {badge.description ? (
            <div
              style={{
                fontSize: 14,
                lineHeight: 1.5,
                opacity: 0.72,
                maxWidth: 320,
                justifySelf: "center",
              }}
            >
              {badge.description}
            </div>
          ) : null}
        </div>

        <div
          style={{
            fontSize: 12,
            lineHeight: 1.35,
            opacity: dismissHintVisible ? 0.56 : 0,
            transition: "opacity 180ms ease",
          }}
        >
          Click anywhere to continue
        </div>
      </div>
    </>
  );
}

----- app/home/modules/badges/BadgeCabinet.tsx -----
// web/app/home/modules/badges/BadgeCabinet.tsx
"use client";

import React from "react";
import type { MemberDashboardBadge } from "@/lib/memberDashboard";
import type { BadgeAwardNotice } from "@/app/home/badges/badgeAwardTypes";
import { useBadgeAwardOverlay } from "@/app/home/badges/BadgeAwardOverlayProvider";
import BadgeCabinetGrid from "./BadgeCabinetGrid";
import BadgeCabinetItem from "./BadgeCabinetItem";
import BadgeCabinetStyles from "./BadgeCabinetStyles";
import BadgeUnlockVisualStyles from "./BadgeUnlockVisualStyles";
import { buildBadgeCabinetItems } from "./badgeCabinetViewModel";
import type { BadgeCabinetItemModel } from "./badgeCabinetTypes";
import { useBadgeCabinetUnlockSequence } from "./useBadgeCabinetUnlockSequence";
import { useFlipGridAnimation } from "./useFlipGridAnimation";
import { usePrefersReducedMotion } from "./usePrefersReducedMotion";

type Props = Readonly<{
  badges: MemberDashboardBadge[];
}>;

type DebugPanelProps = Readonly<{
  candidateItems: BadgeCabinetItemModel[];
  selectedKey: string | null;
  unlockedKey: string | null;
  onSelectKey: (key: string | null) => void;
  onReplaySelected: () => void;
  onReplayRandom: () => void;
  onCelebrateSelected: () => void;
  onCelebrateRandom: () => void;
  onReset: () => void;
}>;

const SHOW_DEBUG_PANEL = false;
const DEBUG_REPLAY_RESET_MS = 72;

function pickRandomItem<T>(items: T[]): T | null {
  if (items.length === 0) return null;
  const index = Math.floor(Math.random() * items.length);
  return items[index] ?? null;
}

function toBadgeAwardNotice(item: BadgeCabinetItemModel): BadgeAwardNotice {
  return {
    entitlementKey: item.key,
    title: item.label,
    description: item.description ?? null,
    imageUrl: item.imageUrl ?? null,
    shareable: item.shareable,
    unlockedAt: item.unlockedAt ?? "",
  };
}

function buildEffectiveBadges(
  badges: MemberDashboardBadge[],
  debugUnlockedKey: string | null,
): MemberDashboardBadge[] {
  if (!debugUnlockedKey) return badges;

  return badges.map((badge) => {
    if (badge.key !== debugUnlockedKey) return badge;

    return {
      ...badge,
      unlocked: true,
      unlockedAt: new Date().toISOString(),
    };
  });
}

function useAdminDebugFlag(): boolean {
  const [isAdminDebug, setIsAdminDebug] = React.useState(false);

  React.useEffect(() => {
    if (typeof document === "undefined") return;

    const syncAdminDebug = () => {
      setIsAdminDebug(document.body.dataset.afIsAdmin === "1");
    };

    syncAdminDebug();

    const observer = new MutationObserver(syncAdminDebug);
    observer.observe(document.body, {
      attributes: true,
      attributeFilter: ["data-af-is-admin"],
    });

    return () => {
      observer.disconnect();
    };
  }, []);

  return isAdminDebug;
}

function useDebugSelection(
  candidateItems: BadgeCabinetItemModel[],
): readonly [
  string | null,
  React.Dispatch<React.SetStateAction<string | null>>,
] {
  const [selectedKey, setSelectedKey] = React.useState<string | null>(null);

  React.useEffect(() => {
    if (candidateItems.length === 0) {
      setSelectedKey(null);
      return;
    }

    setSelectedKey((current) => {
      const currentStillAvailable =
        current !== null && candidateItems.some((item) => item.key === current);

      return currentStillAvailable ? current : (candidateItems[0]?.key ?? null);
    });
  }, [candidateItems]);

  return [selectedKey, setSelectedKey] as const;
}

function useDebugReplay(): Readonly<{
  debugUnlockedKey: string | null;
  replayDebugUnlock: (badgeKey: string | null) => void;
  clearDebugUnlock: () => void;
}> {
  const [debugUnlockedKey, setDebugUnlockedKey] = React.useState<string | null>(
    null,
  );
  const debugReplayTimeoutRef = React.useRef<number | null>(null);

  const clearReplayTimer = React.useCallback(() => {
    if (debugReplayTimeoutRef.current === null) return;

    window.clearTimeout(debugReplayTimeoutRef.current);
    debugReplayTimeoutRef.current = null;
  }, []);

  React.useEffect(() => {
    return clearReplayTimer;
  }, [clearReplayTimer]);

  const replayDebugUnlock = React.useCallback(
    (badgeKey: string | null) => {
      if (!badgeKey) return;

      clearReplayTimer();
      setDebugUnlockedKey(null);

      debugReplayTimeoutRef.current = window.setTimeout(() => {
        setDebugUnlockedKey(badgeKey);
        debugReplayTimeoutRef.current = null;
      }, DEBUG_REPLAY_RESET_MS);
    },
    [clearReplayTimer],
  );

  const clearDebugUnlock = React.useCallback(() => {
    clearReplayTimer();
    setDebugUnlockedKey(null);
  }, [clearReplayTimer]);

  return {
    debugUnlockedKey,
    replayDebugUnlock,
    clearDebugUnlock,
  };
}

function BadgeCabinetToggle(
  props: Readonly<{
    expanded: boolean;
    prefersReducedMotion: boolean;
    onToggle: () => void;
  }>,
) {
  const { expanded, prefersReducedMotion, onToggle } = props;

  return (
    <button
      type="button"
      onClick={onToggle}
      aria-expanded={expanded}
      style={{
        appearance: "none",
        border: "none",
        background: "transparent",
        padding: 0,
        margin: 0,
        display: "inline-flex",
        alignItems: "center",
        justifyContent: "flex-start",
        gap: 8,
        minWidth: 0,
        width: "fit-content",
        color: "inherit",
        cursor: "pointer",
      }}
    >
      <span
        style={{
          fontSize: 10,
          letterSpacing: 0.3,
          textTransform: "uppercase",
          lineHeight: 1.2,
          opacity: 0.5,
        }}
      >
        Badges
      </span>

      <span
        aria-hidden="true"
        style={{
          display: "inline-block",
          fontSize: 12,
          lineHeight: 1,
          opacity: 0.5,
          transform: expanded ? "rotate(90deg)" : "rotate(0deg)",
          transformOrigin: "50% 50%",
          transition: prefersReducedMotion
            ? undefined
            : "transform 220ms ease, opacity 180ms ease",
        }}
      >
        &gt;
      </span>
    </button>
  );
}

function DebugActionButton(
  props: Readonly<{
    label: string;
    disabled: boolean;
    onClick: () => void;
    transparent?: boolean;
    enabledOpacity?: number;
    disabledOpacity?: number;
  }>,
) {
  const {
    label,
    disabled,
    onClick,
    transparent = false,
    enabledOpacity = 0.9,
    disabledOpacity = 0.5,
  } = props;

  return (
    <button
      type="button"
      onClick={onClick}
      disabled={disabled}
      style={{
        appearance: "none",
        borderRadius: 999,
        border: "1px solid rgba(255,255,255,0.12)",
        background: transparent ? "transparent" : "rgba(255,255,255,0.05)",
        color: "inherit",
        padding: "7px 10px",
        fontSize: 12,
        lineHeight: 1.2,
        cursor: disabled ? "default" : "pointer",
        opacity: disabled ? disabledOpacity : enabledOpacity,
      }}
    >
      {label}
    </button>
  );
}

function BadgeCabinetDebugPanel(props: DebugPanelProps) {
  const {
    candidateItems,
    selectedKey,
    unlockedKey,
    onSelectKey,
    onReplaySelected,
    onReplayRandom,
    onCelebrateSelected,
    onCelebrateRandom,
    onReset,
  } = props;

  const hasCandidates = candidateItems.length > 0;

  return (
    <div
      style={{
        display: "grid",
        gap: 8,
        minWidth: 0,
        padding: "8px 10px",
        borderRadius: 12,
        border: "1px solid rgba(255,255,255,0.10)",
        background: "rgba(255,255,255,0.035)",
      }}
    >
      <div
        style={{
          fontSize: 10,
          letterSpacing: 0.3,
          textTransform: "uppercase",
          lineHeight: 1.2,
          opacity: 0.5,
        }}
      >
        Cabinet debug
      </div>

      <div
        style={{
          display: "grid",
          gridTemplateColumns: "minmax(0, 1fr)",
          gap: 8,
        }}
      >
        <select
          value={selectedKey ?? ""}
          onChange={(event) => {
            const nextKey = event.target.value.trim();
            onSelectKey(nextKey || null);
          }}
          disabled={!hasCandidates}
          style={{
            minWidth: 0,
            width: "100%",
            borderRadius: 10,
            border: "1px solid rgba(255,255,255,0.12)",
            background: "rgba(0,0,0,0.18)",
            color: "inherit",
            padding: "8px 10px",
            fontSize: 12,
            lineHeight: 1.3,
            opacity: hasCandidates ? 0.92 : 0.55,
          }}
        >
          {!hasCandidates ? (
            <option value="">No locked badges available</option>
          ) : null}

          {candidateItems.map((item) => (
            <option key={item.key} value={item.key}>
              {item.label}
            </option>
          ))}
        </select>

        <div
          style={{
            display: "flex",
            flexWrap: "wrap",
            gap: 8,
          }}
        >
          <DebugActionButton
            label="Replay selected"
            disabled={!selectedKey}
            onClick={onReplaySelected}
          />
          <DebugActionButton
            label="Replay random"
            disabled={!hasCandidates}
            onClick={onReplayRandom}
          />
        </div>

        <div
          style={{
            display: "flex",
            flexWrap: "wrap",
            gap: 8,
          }}
        >
          <DebugActionButton
            label="Celebrate selected"
            disabled={!selectedKey}
            onClick={onCelebrateSelected}
          />
          <DebugActionButton
            label="Celebrate random"
            disabled={!hasCandidates}
            onClick={onCelebrateRandom}
          />
          <DebugActionButton
            label="Reset"
            disabled={!unlockedKey}
            onClick={onReset}
            transparent
            enabledOpacity={0.78}
            disabledOpacity={0.45}
          />
        </div>
      </div>
    </div>
  );
}

export default function BadgeCabinet(props: Props) {
  const { badges } = props;

  const [expanded, setExpanded] = React.useState(false);
  const prefersReducedMotion = usePrefersReducedMotion();
  const isAdminDebug = useAdminDebugFlag();
  const { announceBadge, resetOverlayDebugState } = useBadgeAwardOverlay();

  const sourceItems = React.useMemo(
    () => buildBadgeCabinetItems(badges),
    [badges],
  );
  const debugCandidateItems = React.useMemo(
    () => sourceItems.filter((item) => !item.unlocked),
    [sourceItems],
  );
  const debugCandidateByKey = React.useMemo(
    () => new Map(debugCandidateItems.map((item) => [item.key, item])),
    [debugCandidateItems],
  );
  const [debugSelectedKey, setDebugSelectedKey] =
    useDebugSelection(debugCandidateItems);
  const { debugUnlockedKey, replayDebugUnlock, clearDebugUnlock } =
    useDebugReplay();

  const effectiveBadges = React.useMemo(
    () => buildEffectiveBadges(badges, debugUnlockedKey),
    [badges, debugUnlockedKey],
  );
  const items = React.useMemo(
    () => buildBadgeCabinetItems(effectiveBadges),
    [effectiveBadges],
  );

  const {
    displayItems,
    newlyUnlockedKeys,
    pendingUnlockKeys,
    unlockPhase,
    liveAnnouncement,
    isFlipSuspended,
    flipDurationMs,
    flipBaselineToken,
    resetUnlockSequence,
  } = useBadgeCabinetUnlockSequence({ items });

  const itemKeys = React.useMemo(
    () => displayItems.map((item) => item.key),
    [displayItems],
  );
  const displayLayoutToken = React.useMemo(
    () => displayItems.map((item) => item.key).join("|"),
    [displayItems],
  );
  const flipLayoutDependency = React.useMemo(
    () =>
      [
        expanded ? "expanded" : "collapsed",
        displayLayoutToken,
        unlockPhase,
      ].join(":"),
    [displayLayoutToken, expanded, unlockPhase],
  );

  const { registerItemRef } = useFlipGridAnimation({
    keys: itemKeys,
    disabled: prefersReducedMotion || isFlipSuspended,
    durationMs: flipDurationMs,
    layoutDependency: flipLayoutDependency,
    captureBaselineToken: flipBaselineToken,
  });

  const announceDebugOverlay = React.useCallback(
    (badgeKey: string | null) => {
      if (!badgeKey) return;

      const item = debugCandidateByKey.get(badgeKey);
      if (item) announceBadge(toBadgeAwardNotice(item));
    },
    [announceBadge, debugCandidateByKey],
  );

  const handleReplayRandom = React.useCallback(() => {
    const randomItem = pickRandomItem(debugCandidateItems);
    if (!randomItem) return;

    setDebugSelectedKey(randomItem.key);
    replayDebugUnlock(randomItem.key);
  }, [debugCandidateItems, replayDebugUnlock, setDebugSelectedKey]);

  const handleCelebrateRandom = React.useCallback(() => {
    const randomItem = pickRandomItem(debugCandidateItems);
    if (!randomItem) return;

    setDebugSelectedKey(randomItem.key);
    announceBadge(toBadgeAwardNotice(randomItem));
  }, [announceBadge, debugCandidateItems, setDebugSelectedKey]);

  const handleResetDebug = React.useCallback(() => {
    clearDebugUnlock();
    resetUnlockSequence();
    resetOverlayDebugState();
  }, [clearDebugUnlock, resetOverlayDebugState, resetUnlockSequence]);

  if (items.length === 0) return null;

  const showDebugPanel = isAdminDebug && SHOW_DEBUG_PANEL;

  return (
    <>
      <BadgeCabinetStyles />
      <BadgeUnlockVisualStyles />

      <div
        style={{
          display: "grid",
          gap: 10,
          minWidth: 0,
        }}
      >
        <div
          aria-live="polite"
          aria-atomic="true"
          className="portal-member-badge-live-region"
        >
          {liveAnnouncement}
        </div>

        <div
          style={{
            display: "grid",
            gap: 8,
            minWidth: 0,
          }}
        >
          <BadgeCabinetToggle
            expanded={expanded}
            prefersReducedMotion={prefersReducedMotion}
            onToggle={() => setExpanded((current) => !current)}
          />

          {showDebugPanel ? (
            <BadgeCabinetDebugPanel
              candidateItems={debugCandidateItems}
              selectedKey={debugSelectedKey}
              unlockedKey={debugUnlockedKey}
              onSelectKey={setDebugSelectedKey}
              onReplaySelected={() => replayDebugUnlock(debugSelectedKey)}
              onReplayRandom={handleReplayRandom}
              onCelebrateSelected={() => announceDebugOverlay(debugSelectedKey)}
              onCelebrateRandom={handleCelebrateRandom}
              onReset={handleResetDebug}
            />
          ) : null}
        </div>

        <BadgeCabinetGrid expanded={expanded}>
          {displayItems.map((item) => (
            <BadgeCabinetItem
              key={item.key}
              item={item}
              expanded={expanded}
              isNewlyUnlocked={newlyUnlockedKeys.has(item.key)}
              isUnlocking={pendingUnlockKeys.has(item.key)}
              itemRef={registerItemRef(item.key)}
            />
          ))}
        </BadgeCabinetGrid>
      </div>
    </>
  );
}


----- app/home/modules/badges/BadgeCabinetGrid.tsx -----
// web/app/home/modules/badges/BadgeCabinetGrid.tsx
"use client";

import React from "react";

type Props = {
  expanded: boolean;
  children: React.ReactNode;
};

export default function BadgeCabinetGrid(props: Props) {
  const { expanded, children } = props;

  return (
    <div
      className={`portal-member-badge-grid${
        expanded ? " portal-member-badges--expanded" : ""
      }`}
      data-expanded={expanded ? "true" : "false"}
    >
      {children}
    </div>
  );
}

----- app/home/modules/badges/BadgeCabinetItem.tsx -----
// web/app/home/modules/badges/BadgeCabinetItem.tsx
"use client";

import React from "react";
import type { BadgeCabinetItemModel } from "./badgeCabinetTypes";
import BadgeUnlockVisual from "./BadgeUnlockVisual";

type Props = Readonly<{
  item: BadgeCabinetItemModel;
  expanded: boolean;
  isNewlyUnlocked: boolean;
  isUnlocking: boolean;
  itemRef?: React.Ref<HTMLDivElement>;
}>;

function revealingClassName(
  baseClassName: string,
  revealingClassName: string,
  isMetaRevealing: boolean,
): string {
  return isMetaRevealing
    ? `${baseClassName} ${revealingClassName}`
    : baseClassName;
}

function BadgeVisual(
  props: Readonly<{
    item: BadgeCabinetItemModel;
    isNewlyUnlocked: boolean;
    isUnlocking: boolean;
  }>,
) {
  const { item, isNewlyUnlocked, isUnlocking } = props;
  const accessibleTitle = item.unlocked ? item.titleText : "Locked badge";

  return (
    <div
      role="img"
      aria-label={accessibleTitle}
      className="portal-member-badge-visual"
      style={{
        position: "relative",
        width: "100%",
        aspectRatio: "1 / 1",
        overflow: "visible",
        outline: "none",
        perspective: "900px",
        perspectiveOrigin: "50% 50%",
      }}
    >
      <BadgeUnlockVisual
        imageUrl={item.imageUrl}
        label={item.label}
        unlocked={item.unlocked}
        isUnlocking={isUnlocking}
        isNewlyUnlocked={isNewlyUnlocked}
        variant="cabinet"
      />
    </div>
  );
}

function BadgeMetaQuestionMark(
  props: Readonly<{
    isMetaRevealing: boolean;
  }>,
) {
  const { isMetaRevealing } = props;

  return (
    <div
      className={revealingClassName(
        "portal-member-badge-question-mark",
        "portal-member-badge-question-mark--dissolving",
        isMetaRevealing,
      )}
      aria-hidden="true"
      style={{
        fontSize: 10,
        lineHeight: 1.1,
        letterSpacing: 0.12,
        opacity: 0.3,
        fontWeight: 400,
        textAlign: "center",
      }}
    >
      —
    </div>
  );
}

function UnlockedBadgeMeta(
  props: Readonly<{
    item: BadgeCabinetItemModel;
    isMetaRevealing: boolean;
  }>,
) {
  const { item, isMetaRevealing } = props;

  return (
    <div
      className={revealingClassName(
        "portal-member-badge-meta-revealed",
        "portal-member-badge-meta-revealed--revealing",
        isMetaRevealing,
      )}
    >
      <div
        className={revealingClassName(
          "portal-member-badge-title",
          "portal-member-badge-title--revealing",
          isMetaRevealing,
        )}
        style={{
          fontSize: 10,
          letterSpacing: 0.7,
          textTransform: "uppercase",
          lineHeight: 1.2,
          fontWeight: 950,
          opacity: 0.7,
          overflowWrap: "anywhere",
        }}
      >
        {item.label}
      </div>

      {item.description ? (
        <div
          className={revealingClassName(
            "portal-member-badge-description",
            "portal-member-badge-description--revealing",
            isMetaRevealing,
          )}
          style={{
            marginTop: 4,
            fontSize: 9,
            lineHeight: 1.2,
            opacity: 0.58,
            overflowWrap: "anywhere",
          }}
        >
          {item.description}
        </div>
      ) : null}
    </div>
  );
}

function BadgeMeta(
  props: Readonly<{
    item: BadgeCabinetItemModel;
    isMetaRevealing: boolean;
  }>,
) {
  const { item, isMetaRevealing } = props;
  const showQuestionMark = !item.unlocked || isMetaRevealing;

  return (
    <div className="portal-member-badge-meta" aria-hidden="false">
      <div className="portal-member-badge-meta-inner">
        {showQuestionMark ? (
          <BadgeMetaQuestionMark isMetaRevealing={isMetaRevealing} />
        ) : null}

        {item.unlocked ? (
          <UnlockedBadgeMeta
            item={item}
            isMetaRevealing={isMetaRevealing}
          />
        ) : null}
      </div>
    </div>
  );
}

export default function BadgeCabinetItem(props: Props) {
  const { item, expanded, isNewlyUnlocked, isUnlocking, itemRef } = props;

  const isMetaRevealing =
    expanded && item.unlocked && (isUnlocking || isNewlyUnlocked);

  return (
    <div
      ref={itemRef}
      className="portal-badge-unlock-host portal-member-badge-wrap portal-member-badge-shell"
      data-badge-key={item.key}
      data-badge-partition={item.partition}
      data-badge-expanded={expanded ? "true" : "false"}
      data-badge-newly-unlocked={isNewlyUnlocked ? "true" : "false"}
      data-badge-unlocked={item.unlocked ? "true" : "false"}
      data-badge-meta-revealing={isMetaRevealing ? "true" : "false"}
      style={{
        position: "relative",
        minWidth: 0,
      }}
    >
      <BadgeVisual
        item={item}
        isNewlyUnlocked={isNewlyUnlocked}
        isUnlocking={isUnlocking}
      />

      {expanded ? (
        <BadgeMeta item={item} isMetaRevealing={isMetaRevealing} />
      ) : null}
    </div>
  );
}


----- app/home/modules/badges/BadgeUnlockVisual.tsx -----
// web/app/home/modules/badges/BadgeUnlockVisual.tsx
"use client";

import React from "react";
import BuildRevealMaskCanvas from "./BuildRevealMaskCanvas";

type Props = Readonly<{
  imageUrl: string | null;
  label: string;
  unlocked: boolean;
  isUnlocking: boolean;
  isNewlyUnlocked: boolean;
  variant: "cabinet" | "overlay";
}>;

const REVEAL_DELAY_MS = 1210;
const REVEAL_DURATION_MS = 600;

function BadgeFallbackArt(
  props: Readonly<{ unlocked: boolean; label: string }>,
) {
  const { unlocked, label } = props;

  return (
    <div
      aria-label={label}
      style={{
        position: "absolute",
        inset: 0,
        display: "grid",
        placeItems: "center",
        fontSize: 16,
        opacity: unlocked ? 0.82 : 0.34,
        filter: unlocked
          ? "drop-shadow(0 0 6px rgba(255,255,255,0.10))"
          : "grayscale(1) saturate(0) brightness(0.8)",
      }}
    >
      ✦
    </div>
  );
}

function BadgeArtImage(
  props: Readonly<{
    imageUrl: string;
    alt: string;
    className?: string;
    style?: React.CSSProperties;
  }>,
) {
  const { imageUrl, alt, className, style } = props;

  return (
    <img
      src={imageUrl}
      alt={alt}
      draggable="false"
      className={className}
      style={{
        position: "absolute",
        inset: 0,
        width: "100%",
        height: "100%",
        objectFit: "contain",
        display: "block",
        userSelect: "none",
        pointerEvents: "none",
        ...style,
      }}
    />
  );
}

function BadgeUnlockedAtmosphere() {
  return (
    <>
      <div
        className="portal-badge-idle-glow"
        style={{
          position: "absolute",
          inset: -6,
          borderRadius: "50%",
          background:
            "radial-gradient(circle, rgba(255,255,255,0.11) 0%, rgba(255,255,255,0.05) 32%, rgba(255,255,255,0.00) 74%)",
          filter: "blur(5px)",
          pointerEvents: "none",
          transition: "opacity 180ms ease, transform 180ms ease",
        }}
      />

      <div
        className="portal-badge-inner-aura"
        style={{
          position: "absolute",
          left: "50%",
          top: "50%",
          width: "58%",
          height: "58%",
          transform: "translate(-50%, -50%)",
          borderRadius: "50%",
          background:
            "radial-gradient(circle, rgba(255,255,255,0.16) 0%, rgba(255,255,255,0.05) 46%, rgba(255,255,255,0) 76%)",
          filter: "blur(5px)",
          pointerEvents: "none",
        }}
      />

      <div
        className="portal-badge-embers"
        style={{
          position: "absolute",
          left: "50%",
          bottom: "-5%",
          width: "74%",
          height: "94%",
          transform: "translateX(-50%)",
          overflow: "visible",
          pointerEvents: "none",
        }}
      >
        <div
          className="portal-badge-spark-a"
          style={{
            position: "absolute",
            left: "31%",
            bottom: "3%",
            width: "5.6%",
            height: "14.4%",
          }}
        />

        <div
          className="portal-badge-spark-b"
          style={{
            position: "absolute",
            left: "45.5%",
            bottom: "1%",
            width: "4.8%",
            height: "12.6%",
          }}
        />

        <div
          className="portal-badge-spark-c"
          style={{
            position: "absolute",
            left: "59.5%",
            bottom: "3.5%",
            width: "4.6%",
            height: "12.2%",
          }}
        />

        <div
          className="portal-badge-burst-a"
          style={{
            position: "absolute",
            left: "37.5%",
            bottom: "2%",
            width: "4.2%",
            height: "10.8%",
          }}
        />

        <div
          className="portal-badge-burst-b"
          style={{
            position: "absolute",
            left: "50.5%",
            bottom: "2.8%",
            width: "4.2%",
            height: "10.4%",
          }}
        />

        <div
          className="portal-badge-burst-c"
          style={{
            position: "absolute",
            left: "62.5%",
            bottom: "2.2%",
            width: "3.4%",
            height: "9.2%",
          }}
        />
      </div>
    </>
  );
}

function UnlockGlint(
  props: Readonly<{
    stage: 1 | 2 | 3;
  }>,
) {
  const { stage } = props;

  return (
    <div
      className={`portal-badge-quarter-glint portal-badge-quarter-glint--stage-${stage}`}
    />
  );
}

function SpinStages(
  props: Readonly<{
    isUnlocking: boolean;
    children: React.ReactNode;
  }>,
) {
  const { isUnlocking, children } = props;
  return (
    <div
      className={`portal-badge-spin-stage-1${
        isUnlocking ? " portal-badge-spin-stage-1--unlocking" : ""
      }`}
      style={{
        position: "absolute",
        inset: 0,
      }}
    >
      {isUnlocking ? <UnlockGlint stage={1} /> : null}

      <div
        className={`portal-badge-spin-stage-2${
          isUnlocking ? " portal-badge-spin-stage-2--unlocking" : ""
        }`}
        style={{
          position: "absolute",
          inset: 0,
        }}
      >
        {isUnlocking ? <UnlockGlint stage={2} /> : null}

        <div
          className={`portal-badge-spin-stage-3${
            isUnlocking ? " portal-badge-spin-stage-3--unlocking" : ""
          }`}
          style={{
            position: "absolute",
            inset: 0,
          }}
        >
          {isUnlocking ? <UnlockGlint stage={3} /> : null}
          {children}
        </div>
      </div>
    </div>
  );
}

function FinalBadgeArt(
  props: Readonly<{
    imageUrl: string;
    label: string;
    unlocked: boolean;
    isNewlyUnlocked: boolean;
  }>,
) {
  const { imageUrl, label, unlocked, isNewlyUnlocked } = props;

  return (
    <div
      className="portal-badge-final-art-shell"
      style={{
        position: "absolute",
        inset: 0,
      }}
    >
      <BadgeArtImage
        imageUrl={imageUrl}
        alt={label}
        className="portal-badge-final-art-image"
        style={{
          filter: unlocked
            ? "drop-shadow(0 0 6px rgba(255,255,255,0.1))"
            : "grayscale(1) saturate(0) brightness(0.60) contrast(0.85) blur(0.2px)",
          opacity: unlocked ? 1 : 0.35,
        }}
      />

      {isNewlyUnlocked ? (
        <div
          className="portal-badge-final-shimmer portal-badge-final-shimmer--celebrating"
          style={{
            position: "absolute",
            inset: 0,
            pointerEvents: "none",
            WebkitMaskImage: `url(${imageUrl})`,
            maskImage: `url(${imageUrl})`,
            WebkitMaskRepeat: "no-repeat",
            maskRepeat: "no-repeat",
            WebkitMaskPosition: "center",
            maskPosition: "center",
            WebkitMaskSize: "contain",
            maskSize: "contain",
          }}
        />
      ) : null}
    </div>
  );
}

function ImageBadgeArt(
  props: Readonly<{
    imageUrl: string;
    label: string;
    unlocked: boolean;
    isUnlocking: boolean;
    isNewlyUnlocked: boolean;
  }>,
) {
  const { imageUrl, label, unlocked, isUnlocking, isNewlyUnlocked } = props;
  const showGreyscaleBase = !unlocked || isUnlocking;

  return (
    <>
      {showGreyscaleBase ? (
        <BadgeArtImage
          imageUrl={imageUrl}
          alt=""
          className="portal-badge-art-base-greyscale"
          style={{
            opacity: isUnlocking ? 0.42 : 0.32,
            filter:
              "grayscale(1) saturate(0) brightness(0.96) contrast(0.94) blur(1.4px)",
            transform: "scale(1.02)",
          }}
        />
      ) : null}

      {isUnlocking ? (
        <>
          <BuildRevealMaskCanvas
            imageUrl={imageUrl}
            label={label}
            isActive={true}
            revealDelayMs={REVEAL_DELAY_MS}
            revealDurationMs={REVEAL_DURATION_MS}
            className="portal-badge-reveal-canvas-layer portal-badge-reveal-canvas-layer--unlocking"
          />

          <div
            className="portal-badge-unlock-energy-flare"
            style={{
              position: "absolute",
              inset: "-8%",
              borderRadius: "50%",
              pointerEvents: "none",
            }}
          />
        </>
      ) : null}

      {!isUnlocking ? (
        <FinalBadgeArt
          imageUrl={imageUrl}
          label={label}
          unlocked={unlocked}
          isNewlyUnlocked={isNewlyUnlocked}
        />
      ) : null}
    </>
  );
}

function UnlockEdgeSpines() {
  return (
    <>
      <div
        className="portal-badge-edge-spine portal-badge-edge-spine--left"
        style={{
          position: "absolute",
          left: "50%",
          top: "50%",
          width: "4.4%",
          height: "86%",
          borderRadius: 999,
          pointerEvents: "none",
        }}
      />

      <div
        className="portal-badge-edge-spine portal-badge-edge-spine--right"
        style={{
          position: "absolute",
          left: "50%",
          top: "50%",
          width: "4.4%",
          height: "86%",
          borderRadius: 999,
          pointerEvents: "none",
        }}
      />
    </>
  );
}

function CentreRadiance() {
  return (
    <div
      className="portal-badge-centre-radiance"
      style={{
        position: "absolute",
        left: "50%",
        top: "50%",
        width: "50%",
        height: "50%",
        transform: "translate(-50%, -50%)",
        borderRadius: "50%",
        background:
          "radial-gradient(circle, rgba(255,255,255,0.13) 0%, rgba(255,255,255,0.045) 42%, rgba(255,255,255,0.00) 76%)",
        filter: "blur(4px)",
        pointerEvents: "none",
      }}
    />
  );
}

function BadgeArtShell(
  props: Readonly<{
    imageUrl: string | null;
    label: string;
    unlocked: boolean;
    isUnlocking: boolean;
    isNewlyUnlocked: boolean;
  }>,
) {
  const { imageUrl, label, unlocked, isUnlocking, isNewlyUnlocked } = props;

  return (
    <div
      className={`portal-badge-art-shell${
        isUnlocking ? " portal-badge-art-shell--unlocking" : ""
      }`}
      style={{
        position: "absolute",
        inset: 0,
      }}
    >
      {imageUrl ? (
        <ImageBadgeArt
          imageUrl={imageUrl}
          label={label}
          unlocked={unlocked}
          isUnlocking={isUnlocking}
          isNewlyUnlocked={isNewlyUnlocked}
        />
      ) : (
        <BadgeFallbackArt unlocked={unlocked} label={label} />
      )}

      {isUnlocking ? <UnlockEdgeSpines /> : null}
      {unlocked ? <CentreRadiance /> : null}
    </div>
  );
}

function ImpactFlash() {
  return (
    <div
      className="portal-badge-impact-flash"
      style={{
        position: "absolute",
        inset: "-8%",
        borderRadius: "50%",
        pointerEvents: "none",
      }}
    />
  );
}

export default function BadgeUnlockVisual(props: Props) {
  const { imageUrl, label, unlocked, isUnlocking, isNewlyUnlocked, variant } =
    props;
  const showUnlockEvent = isUnlocking || isNewlyUnlocked;

  return (
    <div
      className={`portal-badge-unlock-visual-inner portal-badge-unlock-visual-inner--${variant}`}
      data-badge-variant={variant}
      data-badge-unlocking={isUnlocking ? "true" : "false"}
      data-badge-newly-unlocked={isNewlyUnlocked ? "true" : "false"}
      aria-hidden="true"
    >
      <div
        className={unlocked ? undefined : "portal-badge-core--locked"}
        style={{
          position: "absolute",
          inset: 0,
        }}
      >
        {unlocked ? <BadgeUnlockedAtmosphere /> : null}

        <SpinStages isUnlocking={isUnlocking}>
          <BadgeArtShell
            imageUrl={imageUrl}
            label={label}
            unlocked={unlocked}
            isUnlocking={isUnlocking}
            isNewlyUnlocked={isNewlyUnlocked}
          />
        </SpinStages>

        {showUnlockEvent ? <ImpactFlash /> : null}
      </div>
    </div>
  );
}


----- app/home/modules/badges/BuildRevealMaskCanvas.tsx -----
// web/app/home/modules/badges/BuildRevealMaskCanvas.tsx
"use client";

import React from "react";

type Props = {
  imageUrl: string;
  label: string;
  isActive: boolean;
  revealDelayMs?: number;
  revealDurationMs?: number;
  className?: string;
};

const CANVAS_SIZE = 192;
const TAU = Math.PI * 2;

function clamp(value: number, min: number, max: number): number {
  return Math.min(max, Math.max(min, value));
}

function easeOutCubic(value: number): number {
  const t = clamp(value, 0, 1);
  return 1 - Math.pow(1 - t, 3);
}

function easeInOutQuad(value: number): number {
  const t = clamp(value, 0, 1);
  return t < 0.5 ? 2 * t * t : 1 - Math.pow(-2 * t + 2, 2) / 2;
}

function hashString(value: string): number {
  let hash = 2166136261;

  for (let index = 0; index < value.length; index += 1) {
    hash ^= value.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }

  return Math.abs(hash >>> 0);
}

function drawRevealMaskFrame(
  context: CanvasRenderingContext2D,
  progress: number,
  seed: number,
): void {
  const size = CANVAS_SIZE;
  const centre = size / 2;

  context.clearRect(0, 0, size, size);

  if (progress <= 0) {
    return;
  }

  if (progress >= 0.999) {
    context.fillStyle = "white";
    context.fillRect(0, 0, size, size);
    return;
  }

  const softened = easeInOutQuad(progress);
  const stagedGrowth =
    progress < 0.58
      ? 0.68 * Math.pow(progress / 0.58, 1.45)
      : 0.68 + 0.32 * easeOutCubic((progress - 0.58) / 0.42);

  const maxRadius = size * 0.72;
  const baseRadius = size * 0.028 + maxRadius * stagedGrowth;
  const wavePhaseA = seed * 0.0007;
  const wavePhaseB = seed * 0.0011;
  const wavePhaseC = seed * 0.0017;
  const driftPhase = softened * 2.8;

  context.save();
  context.filter = "blur(4px)";
  context.fillStyle = "white";
  context.beginPath();

  const pointCount = 84;

  for (let index = 0; index <= pointCount; index += 1) {
    const angle = (index / pointCount) * TAU;

    const wobbleA = 0.16 * Math.sin(angle * 3 + wavePhaseA);
    const wobbleB = 0.1 * Math.sin(angle * 5 - wavePhaseB);
    const wobbleC = 0.065 * Math.cos(angle * 7 + wavePhaseC);
    const drift = 0.06 * Math.sin(angle * 2.6 + driftPhase);

    const directionBoost =
      0.08 *
      Math.max(
        Math.sin(angle - 0.55 + wavePhaseA),
        Math.sin(angle + 1.85 - wavePhaseB),
      );

    const radius = Math.max(
      size * 0.03,
      baseRadius * (1 + wobbleA + wobbleB + wobbleC + drift + directionBoost),
    );

    const x = centre + Math.cos(angle) * radius;
    const y = centre + Math.sin(angle) * radius;

    if (index === 0) {
      context.moveTo(x, y);
    } else {
      context.lineTo(x, y);
    }
  }

  context.closePath();
  context.fill();
  context.restore();

  const lobeDistance = baseRadius * 0.42;

  const lobeOffsets = [
    { angle: -0.95 + wavePhaseA, rx: 0.2, ry: 0.14, start: 0.3 },
    { angle: 0.5 - wavePhaseB, rx: 0.17, ry: 0.21, start: 0.42 },
    { angle: 2.05 + wavePhaseC, rx: 0.2, ry: 0.14, start: 0.54 },
    { angle: -2.3 + wavePhaseB, rx: 0.16, ry: 0.18, start: 0.64 },
  ] as const;

  context.save();
  context.filter = "blur(3.5px)";
  context.fillStyle = "white";

  for (const lobe of lobeOffsets) {
    const localProgress = clamp((progress - lobe.start) / 0.5, 0, 1);
    if (localProgress <= 0) continue;

    const lobeStrength = easeOutCubic(localProgress);
    const cx = centre + Math.cos(lobe.angle) * lobeDistance * lobeStrength;
    const cy = centre + Math.sin(lobe.angle) * lobeDistance * lobeStrength;
    const rx = size * lobe.rx * (0.35 + lobeStrength);
    const ry = size * lobe.ry * (0.35 + lobeStrength);

    context.beginPath();
    context.ellipse(cx, cy, rx, ry, lobe.angle, 0, TAU);
    context.fill();
  }

  context.restore();

  context.save();
  context.filter = "blur(1.8px)";
  context.fillStyle = "white";

  const coreRadius = size * (0.065 + progress * 0.045);
  context.beginPath();
  context.arc(centre, centre, coreRadius, 0, TAU);
  context.fill();

  context.restore();

  context.save();
  context.filter = "blur(0.9px)";
  context.fillStyle = "white";
  context.beginPath();

  for (let index = 0; index <= pointCount; index += 1) {
    const angle = (index / pointCount) * TAU;

    const wobbleA = 0.12 * Math.sin(angle * 3 + wavePhaseA);
    const wobbleB = 0.07 * Math.sin(angle * 5 - wavePhaseB);
    const wobbleC = 0.04 * Math.cos(angle * 7 + wavePhaseC);
    const drift = 0.04 * Math.sin(angle * 2.6 + driftPhase);

    const radius = Math.max(
      size * 0.03,
      baseRadius * (1 + wobbleA + wobbleB + wobbleC + drift),
    );

    const x = centre + Math.cos(angle) * radius;
    const y = centre + Math.sin(angle) * radius;

    if (index === 0) {
      context.moveTo(x, y);
    } else {
      context.lineTo(x, y);
    }
  }

  context.closePath();
  context.fill();
  context.restore();
}

function buildSolidMaskDataUrl(): string {
  const canvas = document.createElement("canvas");
  canvas.width = CANVAS_SIZE;
  canvas.height = CANVAS_SIZE;

  const context = canvas.getContext("2d");
  if (!context) return "";

  context.fillStyle = "white";
  context.fillRect(0, 0, CANVAS_SIZE, CANVAS_SIZE);

  return canvas.toDataURL("image/png");
}

export default function BuildRevealMaskCanvas(props: Props) {
  const {
    imageUrl,
    label,
    isActive,
    revealDelayMs = 1080,
    revealDurationMs = 920,
    className,
  } = props;

  const [maskDataUrl, setMaskDataUrl] = React.useState<string>("");
  const seed = React.useMemo(
    () => hashString(`${imageUrl}::${label}`),
    [imageUrl, label],
  );

  React.useEffect(() => {
    if (!isActive) {
      setMaskDataUrl("");
      return;
    }

    if (typeof window === "undefined") return;

    const reducedMotion = window.matchMedia(
      "(prefers-reduced-motion: reduce)",
    ).matches;

    if (reducedMotion) {
      setMaskDataUrl(buildSolidMaskDataUrl());
      return;
    }

    const canvas = document.createElement("canvas");
    canvas.width = CANVAS_SIZE;
    canvas.height = CANVAS_SIZE;

    const context = canvas.getContext("2d");
    if (!context) return;

    let rafId = 0;
    const startTime = performance.now();

    const renderFrame = (now: number) => {
      const elapsed = now - startTime;
      const rawProgress = (elapsed - revealDelayMs) / revealDurationMs;
      const progress = clamp(rawProgress, 0, 1);

      drawRevealMaskFrame(context, progress, seed);
      setMaskDataUrl(canvas.toDataURL("image/png"));

      if (progress < 1) {
        rafId = window.requestAnimationFrame(renderFrame);
      }
    };

    rafId = window.requestAnimationFrame(renderFrame);

    return () => {
      window.cancelAnimationFrame(rafId);
    };
  }, [isActive, label, imageUrl, revealDelayMs, revealDurationMs, seed]);

  if (!isActive || !maskDataUrl) return null;

  return (
    <div
      aria-hidden="true"
      className={className}
      style={{
        position: "absolute",
        inset: 0,
        pointerEvents: "none",
        backgroundImage: `url("${imageUrl}")`,
        backgroundRepeat: "no-repeat",
        backgroundPosition: "center",
        backgroundSize: "contain",
        WebkitMaskImage: `url("${maskDataUrl}")`,
        maskImage: `url("${maskDataUrl}")`,
        WebkitMaskRepeat: "no-repeat",
        maskRepeat: "no-repeat",
        WebkitMaskPosition: "center",
        maskPosition: "center",
        WebkitMaskSize: "contain",
        maskSize: "contain",
      }}
    />
  );
}


----- app/home/modules/badges/badgeCabinetViewModel.ts -----
// web/app/home/modules/badges/badgeCabinetViewModel.ts
import type { BadgeCabinetInputBadge, BadgeCabinetItemModel } from "./badgeCabinetTypes";

function formatUnlockedAt(value?: string | null): string | null {
  if (!value) return null;

  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return null;

  return date.toLocaleDateString(undefined, {
    year: "numeric",
    month: "short",
    day: "numeric",
  });
}

function getBadgeTitle(props: {
  label: string;
  description: string | null;
  unlocked: boolean;
  unlockedAt: string | null;
}): string {
  const { label, description, unlocked, unlockedAt } = props;

  return [
    label,
    description?.trim() || null,
    unlocked ? (unlockedAt ? `Unlocked ${unlockedAt}` : "Unlocked") : "Locked",
  ]
    .filter((value): value is string => Boolean(value && value.trim()))
    .join("\n");
}

function normalizeBadge(
  badge: BadgeCabinetInputBadge,
  fallbackEditorialOrder: number,
): BadgeCabinetItemModel | null {
  const key = typeof badge.key === "string" ? badge.key.trim() : "";
  const label = typeof badge.label === "string" ? badge.label.trim() : "";

  if (!key || !label) return null;

  const description =
    typeof badge.description === "string" && badge.description.trim()
      ? badge.description.trim()
      : null;

  const imageUrl =
    typeof badge.imageUrl === "string" && badge.imageUrl.trim()
      ? badge.imageUrl.trim()
      : null;

  const unlockedAt = badge.unlocked ? formatUnlockedAt(badge.unlockedAt) : null;

  const editorialOrder =
    typeof badge.editorialOrder === "number" && Number.isFinite(badge.editorialOrder)
      ? badge.editorialOrder
      : fallbackEditorialOrder;

  return {
    key,
    label,
    description,
    imageUrl,
    unlocked: badge.unlocked === true,
    unlockedAt,
    editorialOrder,
    partition: badge.unlocked ? "unlocked" : "locked",
    titleText: getBadgeTitle({
      label,
      description,
      unlocked: badge.unlocked === true,
      unlockedAt,
    }),
    shareable: badge.shareable === true,
    undisclosed: badge.undisclosed === true,
  };
}

export function buildBadgeCabinetItems(
  badges: BadgeCabinetInputBadge[],
): BadgeCabinetItemModel[] {
  const normalized = badges
    .map((badge, index) => normalizeBadge(badge, index))
    .filter((badge): badge is BadgeCabinetItemModel => badge !== null);

  normalized.sort((a, b) => {
    if (a.partition !== b.partition) {
      return a.partition === "unlocked" ? -1 : 1;
    }

    if (a.editorialOrder !== b.editorialOrder) {
      return a.editorialOrder - b.editorialOrder;
    }

    return a.label.localeCompare(b.label);
  });

  return normalized;
}

----- app/home/modules/badges/useFlipGridAnimation.ts -----
// web/app/home/modules/badges/useFlipGridAnimation.ts
"use client";

import React from "react";

type RegisterItemRef = (key: string) => (node: HTMLDivElement | null) => void;

type Options = {
  keys: string[];
  disabled?: boolean;
  durationMs?: number;
  easing?: string;
  layoutDependency?: string | number | boolean | null;
  captureBaselineToken?: string | number | boolean | null;
};

type LayoutRect = {
  left: number;
  top: number;
  width: number;
  height: number;
};

type RectMap = Map<string, LayoutRect>;

type CleanupFn = () => void;

function resetNodeTransform(node: HTMLDivElement): void {
  node.style.transform = "";
  node.style.transition = "";
  node.style.willChange = "";
}

function getRelativeRect(node: HTMLDivElement): LayoutRect {
  const nodeRect = node.getBoundingClientRect();
  const parent = node.parentElement;

  if (!parent) {
    return {
      left: nodeRect.left,
      top: nodeRect.top,
      width: nodeRect.width,
      height: nodeRect.height,
    };
  }

  const parentRect = parent.getBoundingClientRect();

  return {
    left: nodeRect.left - parentRect.left,
    top: nodeRect.top - parentRect.top,
    width: nodeRect.width,
    height: nodeRect.height,
  };
}

function snapshotRects(
  keys: string[],
  nodeByKey: Map<string, HTMLDivElement>,
): RectMap {
  const rects: RectMap = new Map();

  for (const key of keys) {
    const node = nodeByKey.get(key);
    if (!node || !node.isConnected) continue;
    rects.set(key, getRelativeRect(node));
  }

  return rects;
}

function isReasonableDelta(
  deltaX: number,
  deltaY: number,
  rect: LayoutRect,
): boolean {
  if (typeof window === "undefined") return true;

  const viewportX = window.innerWidth || 0;
  const viewportY = window.innerHeight || 0;

  const maxDeltaX = Math.max(viewportX * 1.25, rect.width * 8, 320);
  const maxDeltaY = Math.max(viewportY * 1.25, rect.height * 8, 320);

  return Math.abs(deltaX) <= maxDeltaX && Math.abs(deltaY) <= maxDeltaY;
}

export function useFlipGridAnimation(options: Options): {
  registerItemRef: RegisterItemRef;
} {
  const {
    keys,
    disabled = false,
    durationMs = 320,
    easing = "cubic-bezier(0.22, 1, 0.36, 1)",
    layoutDependency = null,
    captureBaselineToken = null,
  } = options;

  const nodeByKeyRef = React.useRef<Map<string, HTMLDivElement>>(new Map());
  const previousRectsRef = React.useRef<RectMap>(new Map());
  const cleanupByKeyRef = React.useRef<Map<string, CleanupFn>>(new Map());

  const playRafIdRef = React.useRef<number | null>(null);
  const settleRafIdRef = React.useRef<number | null>(null);
  const baselineRafARef = React.useRef<number | null>(null);
  const baselineRafBRef = React.useRef<number | null>(null);

  const hasStableBaselineRef = React.useRef(false);
  const lastCaptureBaselineTokenRef = React.useRef<
    string | number | boolean | null
  >(captureBaselineToken);

  const cancelScheduledAnimationFrames = React.useCallback((): void => {
    if (playRafIdRef.current !== null) {
      window.cancelAnimationFrame(playRafIdRef.current);
      playRafIdRef.current = null;
    }

    if (settleRafIdRef.current !== null) {
      window.cancelAnimationFrame(settleRafIdRef.current);
      settleRafIdRef.current = null;
    }

    if (baselineRafARef.current !== null) {
      window.cancelAnimationFrame(baselineRafARef.current);
      baselineRafARef.current = null;
    }

    if (baselineRafBRef.current !== null) {
      window.cancelAnimationFrame(baselineRafBRef.current);
      baselineRafBRef.current = null;
    }
  }, []);

  const clearActiveAnimations = React.useCallback((): void => {
    for (const cleanup of cleanupByKeyRef.current.values()) {
      cleanup();
    }
    cleanupByKeyRef.current.clear();
  }, []);

  const resetTrackedNodeStyles = React.useCallback(
    (trackedKeys: string[]): void => {
      for (const key of trackedKeys) {
        const node = nodeByKeyRef.current.get(key);
        if (!node) continue;
        resetNodeTransform(node);
      }
    },
    [],
  );

  const scheduleBaselineCapture = React.useCallback(
    (trackedKeys: string[]): void => {
      cancelScheduledAnimationFrames();

      baselineRafARef.current = window.requestAnimationFrame(() => {
        baselineRafARef.current = null;

        baselineRafBRef.current = window.requestAnimationFrame(() => {
          baselineRafBRef.current = null;

          resetTrackedNodeStyles(trackedKeys);
          previousRectsRef.current = snapshotRects(
            trackedKeys,
            nodeByKeyRef.current,
          );
          hasStableBaselineRef.current = true;
        });
      });
    },
    [cancelScheduledAnimationFrames, resetTrackedNodeStyles],
  );

  const registerItemRef = React.useCallback<RegisterItemRef>(
    (key: string) => (node: HTMLDivElement | null) => {
      const nodeByKey = nodeByKeyRef.current;
      const cleanupByKey = cleanupByKeyRef.current;
      const existingNode = nodeByKey.get(key);

      if (existingNode && existingNode !== node) {
        const cleanup = cleanupByKey.get(key);
        if (cleanup) {
          cleanup();
          cleanupByKey.delete(key);
        }
      }

      if (node) {
        nodeByKey.set(key, node);
        return;
      }

      nodeByKey.delete(key);

      const cleanup = cleanupByKey.get(key);
      if (cleanup) {
        cleanup();
        cleanupByKey.delete(key);
      }
    },
    [],
  );

  React.useEffect(() => {
    return () => {
      cancelScheduledAnimationFrames();
      clearActiveAnimations();
    };
  }, [cancelScheduledAnimationFrames, clearActiveAnimations]);

  React.useLayoutEffect(() => {
    cancelScheduledAnimationFrames();
    clearActiveAnimations();
    resetTrackedNodeStyles(keys);

    const captureBaselineChanged =
      captureBaselineToken !== lastCaptureBaselineTokenRef.current;

    lastCaptureBaselineTokenRef.current = captureBaselineToken;

    if (!hasStableBaselineRef.current) {
      scheduleBaselineCapture(keys);
      return;
    }

    if (captureBaselineChanged) {
      hasStableBaselineRef.current = false;
      scheduleBaselineCapture(keys);
      return;
    }

    const nextRects = snapshotRects(keys, nodeByKeyRef.current);

    if (disabled) {
      previousRectsRef.current = nextRects;
      return;
    }

    const animations: Array<{
      key: string;
      node: HTMLDivElement;
      deltaX: number;
      deltaY: number;
    }> = [];

    for (const key of keys) {
      const node = nodeByKeyRef.current.get(key);
      const previousRect = previousRectsRef.current.get(key);
      const nextRect = nextRects.get(key);

      if (!node || !node.isConnected || !previousRect || !nextRect) continue;

      const deltaX = previousRect.left - nextRect.left;
      const deltaY = previousRect.top - nextRect.top;

      if (Math.abs(deltaX) < 0.5 && Math.abs(deltaY) < 0.5) continue;
      if (!isReasonableDelta(deltaX, deltaY, nextRect)) continue;

      animations.push({
        key,
        node,
        deltaX,
        deltaY,
      });
    }

    if (animations.length === 0) {
      previousRectsRef.current = nextRects;
      return;
    }

    for (const animation of animations) {
      const { key, node, deltaX, deltaY } = animation;

      node.style.transition = "none";
      node.style.transform = `translate3d(${deltaX}px, ${deltaY}px, 0)`;
      node.style.willChange = "transform";

      let timeoutId: number | null = null;

      const cleanup = () => {
        node.removeEventListener("transitionend", handleTransitionEnd);

        if (timeoutId !== null) {
          window.clearTimeout(timeoutId);
          timeoutId = null;
        }

        resetNodeTransform(node);
        cleanupByKeyRef.current.delete(key);
      };

      const handleTransitionEnd = (event: TransitionEvent) => {
        if (event.target !== node) return;
        if (event.propertyName !== "transform") return;
        cleanup();
      };

      node.addEventListener("transitionend", handleTransitionEnd);

      timeoutId = window.setTimeout(() => {
        cleanup();
      }, durationMs + 120);

      cleanupByKeyRef.current.set(key, cleanup);
    }

    playRafIdRef.current = window.requestAnimationFrame(() => {
      playRafIdRef.current = null;

      settleRafIdRef.current = window.requestAnimationFrame(() => {
        settleRafIdRef.current = null;

        for (const animation of animations) {
          const { node } = animation;
          node.style.transition = `transform ${durationMs}ms ${easing}`;
          node.style.transform = "translate3d(0px, 0px, 0)";
        }
      });
    });

    previousRectsRef.current = nextRects;
  }, [
    keys,
    disabled,
    durationMs,
    easing,
    layoutDependency,
    captureBaselineToken,
    cancelScheduledAnimationFrames,
    clearActiveAnimations,
    resetTrackedNodeStyles,
    scheduleBaselineCapture,
  ]);

  return { registerItemRef };
}


----- app/home/modules/badges/usePrefersReducedMotion.ts -----
// web/app/home/modules/badges/usePrefersReducedMotion.ts
"use client";

import React from "react";

export function usePrefersReducedMotion(): boolean {
  const [prefersReducedMotion, setPrefersReducedMotion] = React.useState(false);

  React.useEffect(() => {
    if (typeof window === "undefined" || typeof window.matchMedia !== "function") {
      return;
    }

    const mediaQuery = window.matchMedia("(prefers-reduced-motion: reduce)");

    const update = () => {
      setPrefersReducedMotion(mediaQuery.matches);
    };

    update();

    if (typeof mediaQuery.addEventListener === "function") {
      mediaQuery.addEventListener("change", update);
      return () => mediaQuery.removeEventListener("change", update);
    }

    mediaQuery.addListener(update);
    return () => mediaQuery.removeListener(update);
  }, []);

  return prefersReducedMotion;
}

----- lib/badgeAdmin.ts -----
// web/lib/badgeAdmin.ts
import "server-only";

import { sql } from "@vercel/postgres";
import { getRecordingSummaryByRecordingId } from "@/lib/albums";
import { areBadgesEnabled } from "@/lib/badgeFeature";
import { grantEntitlement } from "@/lib/entitlementOps";

export type AwardBadgeToMembersInput = {
  entitlementKey: string;
  memberIds: string[];
  grantedBy: string;
  grantReason?: string | null;
  grantSource?: string | null;
  grantSourceRef?: string | null;
};

export type AwardBadgeToMembersResult = {
  entitlementKey: string;
  attempted: number;
  inserted: number;
  alreadyHeld: number;
};

function uniqueMemberIds(memberIds: string[]): string[] {
  const seen = new Set<string>();
  const ordered: string[] = [];

  for (const memberId of memberIds) {
    const normalized = memberId.trim();
    if (!normalized || seen.has(normalized)) continue;
    seen.add(normalized);
    ordered.push(normalized);
  }

  return ordered;
}

export async function awardBadgeToMembers(
  input: AwardBadgeToMembersInput,
): Promise<AwardBadgeToMembersResult> {
  if (!areBadgesEnabled()) {
    throw new Error("Badge system is disabled.");
  }

  const entitlementKey = input.entitlementKey.trim();
  const memberIds = uniqueMemberIds(input.memberIds);

  let inserted = 0;
  let alreadyHeld = 0;

  for (const memberId of memberIds) {
    const result = await grantEntitlement({
      memberId,
      entitlementKey,
      scopeId: null,
      grantedBy: input.grantedBy,
      grantReason: input.grantReason ?? undefined,
      grantSource: input.grantSource ?? "badge_rule",
      grantSourceRef: input.grantSourceRef ?? undefined,
    });

    if (result.status === "inserted") {
      inserted += 1;
      continue;
    }

    alreadyHeld += 1;
  }

  return {
    entitlementKey,
    attempted: memberIds.length,
    inserted,
    alreadyHeld,
  };
}

export type BadgePreviewMemberRow = {
  memberId: string;
  email: string | null;
  joinedAt: string | null;
  listenedMs: number | null;
  minutesStreamed: number | null;
  playCount: number | null;
  completedCount: number | null;
  matchedRecordingId: string | null;
  matchedRecordingTitle: string | null;
  matchedWindowEventCount: number | null;
  contributionCount: number | null;
  exegesisVoteCount: number | null;
  publicNameUnlockedAt: string | null;
};

export type MinutesStreamedPreviewInput = {
  mode: "minutes_streamed";
  minMinutes: number;
  limit?: number;
};

export type PlayCountPreviewInput = {
  mode: "play_count";
  minPlayCount: number;
  limit?: number;
};

export type CompleteCountPreviewInput = {
  mode: "complete_count";
  minCompletedCount: number;
  limit?: number;
};

export type JoinedWithinWindowPreviewInput = {
  mode: "joined_within_window";
  joinedOnOrAfter: string;
  joinedBefore?: string | null;
  limit?: number;
};

export type ActiveWithinWindowPreviewInput = {
  mode: "active_within_window";
  activeOnOrAfter: string;
  activeBefore?: string | null;
  minPlayCount?: number;
  minProgressCount?: number;
  minCompleteCount?: number;
  limit?: number;
};

export type RecordingMinutesStreamedPreviewInput = {
  mode: "recording_minutes_streamed";
  recordingId: string;
  minMinutes: number;
  limit?: number;
};

export type RecordingPlayCountPreviewInput = {
  mode: "recording_play_count";
  recordingId: string;
  minPlayCount: number;
  limit?: number;
};

export type RecordingCompleteCountPreviewInput = {
  mode: "recording_complete_count";
  recordingId: string;
  minCompletedCount: number;
  limit?: number;
};

export type ExegesisContributionCountPreviewInput = {
  mode: "exegesis_contribution_count";
  minContributionCount: number;
  limit?: number;
};

export type ExegesisVoteTallyPreviewInput = {
  mode: "exegesis_vote_tally";
  minVoteCount: number;
  limit?: number;
};

export type PublicNameUnlockedPreviewInput = {
  mode: "public_name_unlocked";
  limit?: number;
};

export type BadgePreviewInput =
  | MinutesStreamedPreviewInput
  | PlayCountPreviewInput
  | CompleteCountPreviewInput
  | JoinedWithinWindowPreviewInput
  | ActiveWithinWindowPreviewInput
  | RecordingMinutesStreamedPreviewInput
  | RecordingPlayCountPreviewInput
  | RecordingCompleteCountPreviewInput
  | ExegesisContributionCountPreviewInput
  | ExegesisVoteTallyPreviewInput
  | PublicNameUnlockedPreviewInput;

type BadgePreviewHandlerMap = {
  minutes_streamed: (
    input: MinutesStreamedPreviewInput,
  ) => Promise<BadgePreviewMemberRow[]>;
  play_count: (
    input: PlayCountPreviewInput,
  ) => Promise<BadgePreviewMemberRow[]>;
  complete_count: (
    input: CompleteCountPreviewInput,
  ) => Promise<BadgePreviewMemberRow[]>;
  joined_within_window: (
    input: JoinedWithinWindowPreviewInput,
  ) => Promise<BadgePreviewMemberRow[]>;
  active_within_window: (
    input: ActiveWithinWindowPreviewInput,
  ) => Promise<BadgePreviewMemberRow[]>;
  recording_minutes_streamed: (
    input: RecordingMinutesStreamedPreviewInput,
  ) => Promise<BadgePreviewMemberRow[]>;
  recording_play_count: (
    input: RecordingPlayCountPreviewInput,
  ) => Promise<BadgePreviewMemberRow[]>;
  recording_complete_count: (
    input: RecordingCompleteCountPreviewInput,
  ) => Promise<BadgePreviewMemberRow[]>;
  exegesis_contribution_count: (
    input: ExegesisContributionCountPreviewInput,
  ) => Promise<BadgePreviewMemberRow[]>;
  exegesis_vote_tally: (
    input: ExegesisVoteTallyPreviewInput,
  ) => Promise<BadgePreviewMemberRow[]>;
  public_name_unlocked: (
    input: PublicNameUnlockedPreviewInput,
  ) => Promise<BadgePreviewMemberRow[]>;
};

type MemberBaseRow = {
  id: string;
  email: string | null;
  created_at: string | null;
};

type AggregateRow = {
  member_id: string;
  email: string | null;
  created_at: string | null;
  listened_ms: string | number | null;
  play_count: string | number | null;
  completed_count: string | number | null;
  recording_id?: string | null;
  matched_window_event_count?: string | number | null;
  contribution_count?: string | number | null;
  vote_count?: string | number | null;
  public_name_unlocked_at?: string | null;
};

const DEFAULT_PREVIEW_LIMIT = 200;
const MAX_PREVIEW_LIMIT = 1000;
const TELEMETRY_PROGRESS_STEP_MS = 15_000;

const PLAYBACK_TELEMETRY_EVENT_TYPES = {
  PLAY: "playback_telemetry_play",
  PROGRESS: "playback_telemetry_progress",
  COMPLETE: "playback_telemetry_complete",
} as const;

function clampPreviewLimit(limit?: number): number {
  if (!Number.isFinite(limit)) return DEFAULT_PREVIEW_LIMIT;
  const rounded = Math.floor(limit ?? DEFAULT_PREVIEW_LIMIT);
  return Math.max(1, Math.min(MAX_PREVIEW_LIMIT, rounded));
}

function asNumber(value: string | number | null | undefined): number | null {
  if (typeof value === "number") {
    return Number.isFinite(value) ? value : null;
  }
  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    return Number.isFinite(parsed) ? parsed : null;
  }
  return null;
}

function mapAggregateRow(row: AggregateRow): BadgePreviewMemberRow {
  const listenedMs = asNumber(row.listened_ms);
  const playCount = asNumber(row.play_count);
  const completedCount = asNumber(row.completed_count);
  const matchedWindowEventCount = asNumber(row.matched_window_event_count);

  return {
    memberId: row.member_id,
    email: row.email,
    joinedAt: row.created_at,
    listenedMs,
    minutesStreamed:
      listenedMs !== null && listenedMs > 0
        ? Math.floor(listenedMs / 60_000)
        : listenedMs === 0
          ? 0
          : null,
    playCount,
    completedCount,
    matchedRecordingId: row.recording_id ?? null,
    matchedRecordingTitle: null,
    matchedWindowEventCount,
    contributionCount: asNumber(row.contribution_count),
    exegesisVoteCount: asNumber(row.vote_count),
    publicNameUnlockedAt: row.public_name_unlocked_at ?? null,
  };
}

function mapMemberBaseRow(row: MemberBaseRow): BadgePreviewMemberRow {
  return {
    memberId: row.id,
    email: row.email,
    joinedAt: row.created_at,
    listenedMs: null,
    minutesStreamed: null,
    playCount: null,
    completedCount: null,
    matchedRecordingId: null,
    matchedRecordingTitle: null,
    matchedWindowEventCount: null,
    contributionCount: null,
    exegesisVoteCount: null,
    publicNameUnlockedAt: null,
  };
}

const BADGE_PREVIEW_HANDLERS: BadgePreviewHandlerMap = {
  minutes_streamed: previewByMinutesStreamed,
  play_count: previewByPlayCount,
  complete_count: previewByCompleteCount,
  joined_within_window: previewByJoinedWindow,
  active_within_window: previewByActiveWithinWindow,
  recording_minutes_streamed: previewByRecordingMinutesStreamed,
  recording_play_count: previewByRecordingPlayCount,
  recording_complete_count: previewByRecordingCompleteCount,
  exegesis_contribution_count: previewByExegesisContributionCount,
  exegesis_vote_tally: previewByExegesisVoteTally,
  public_name_unlocked: previewByPublicNameUnlocked,
};

async function hydratePreviewRecordingTitles(
  rows: BadgePreviewMemberRow[],
): Promise<BadgePreviewMemberRow[]> {
  const distinctRecordingIds = Array.from(
    new Set(
      rows
        .map((row) => row.matchedRecordingId)
        .filter(
          (recordingId): recordingId is string =>
            typeof recordingId === "string" && recordingId.trim().length > 0,
        ),
    ),
  );

  if (distinctRecordingIds.length === 0) {
    return rows;
  }

  const titleByRecordingId = new Map<string, string | null>();

  await Promise.all(
    distinctRecordingIds.map(async (recordingId) => {
      const summary = await getRecordingSummaryByRecordingId(recordingId);
      titleByRecordingId.set(recordingId, summary?.title ?? recordingId);
    }),
  );

  return rows.map((row) => {
    if (!row.matchedRecordingId) return row;

    return {
      ...row,
      matchedRecordingTitle:
        titleByRecordingId.get(row.matchedRecordingId) ??
        row.matchedRecordingId,
    };
  });
}

export async function previewBadgeQualification(
  input: BadgePreviewInput,
): Promise<BadgePreviewMemberRow[]> {
  const handler = BADGE_PREVIEW_HANDLERS[input.mode] as (
    value: BadgePreviewInput,
  ) => Promise<BadgePreviewMemberRow[]>;

  const rows = await handler(input);
  return hydratePreviewRecordingTitles(rows);
}

async function previewByMinutesStreamed(
  input: MinutesStreamedPreviewInput,
): Promise<BadgePreviewMemberRow[]> {
  const minListenedMs = Math.max(0, Math.floor(input.minMinutes * 60_000));
  const limit = clampPreviewLimit(input.limit);

  const res = await sql<AggregateRow>`
    select
      mlt.member_id,
      m.email,
      
      m.created_at,
      mlt.listened_ms,
      mlt.play_count,
      mlt.completed_count
    from member_listen_totals mlt
    join members m on m.id = mlt.member_id
    where mlt.listened_ms >= ${minListenedMs}
    order by
      mlt.listened_ms desc,
      mlt.play_count desc,
      m.created_at asc nulls last
    limit ${limit}
  `;

  return res.rows.map(mapAggregateRow);
}

async function previewByPlayCount(
  input: PlayCountPreviewInput,
): Promise<BadgePreviewMemberRow[]> {
  const minPlayCount = Math.max(0, Math.floor(input.minPlayCount));
  const limit = clampPreviewLimit(input.limit);

  const res = await sql<AggregateRow>`
    select
      mlt.member_id,
      m.email,
      
      m.created_at,
      mlt.listened_ms,
      mlt.play_count,
      mlt.completed_count
    from member_listen_totals mlt
    join members m on m.id = mlt.member_id
    where mlt.play_count >= ${minPlayCount}
    order by
      mlt.play_count desc,
      mlt.listened_ms desc,
      m.created_at asc nulls last
    limit ${limit}
  `;

  return res.rows.map(mapAggregateRow);
}

async function previewByCompleteCount(
  input: CompleteCountPreviewInput,
): Promise<BadgePreviewMemberRow[]> {
  const minCompletedCount = Math.max(0, Math.floor(input.minCompletedCount));
  const limit = clampPreviewLimit(input.limit);

  const res = await sql<AggregateRow>`
    select
      mlt.member_id,
      m.email,
      
      m.created_at,
      mlt.listened_ms,
      mlt.play_count,
      mlt.completed_count
    from member_listen_totals mlt
    join members m on m.id = mlt.member_id
    where mlt.completed_count >= ${minCompletedCount}
    order by
      mlt.completed_count desc,
      mlt.listened_ms desc,
      m.created_at asc nulls last
    limit ${limit}
  `;

  return res.rows.map(mapAggregateRow);
}

async function previewByJoinedWindow(
  input: JoinedWithinWindowPreviewInput,
): Promise<BadgePreviewMemberRow[]> {
  const limit = clampPreviewLimit(input.limit);
  const joinedBefore = input.joinedBefore ?? null;

  const res = await sql<MemberBaseRow>`
    select
      m.id,
      m.email,
      
      m.created_at
    from members m
    where m.created_at >= ${input.joinedOnOrAfter}
      and (${joinedBefore}::timestamptz is null or m.created_at < ${joinedBefore}::timestamptz)
    order by
      m.created_at asc,
      m.id asc
    limit ${limit}
  `;

  return res.rows.map(mapMemberBaseRow);
}

async function previewByActiveWithinWindow(
  input: ActiveWithinWindowPreviewInput,
): Promise<BadgePreviewMemberRow[]> {
  const limit = clampPreviewLimit(input.limit);
  const activeBefore = input.activeBefore ?? null;
  const minPlayCount = Math.max(0, Math.floor(input.minPlayCount ?? 0));
  const minProgressCount = Math.max(0, Math.floor(input.minProgressCount ?? 0));
  const minCompleteCount = Math.max(0, Math.floor(input.minCompleteCount ?? 0));

  const res = await sql<AggregateRow>`
    with window_counts as (
      select
        d.member_id,
                count(*) filter (
          where d.event_type = ${PLAYBACK_TELEMETRY_EVENT_TYPES.PLAY}
        )::int as play_count,
        count(*) filter (
          where d.event_type = ${PLAYBACK_TELEMETRY_EVENT_TYPES.PROGRESS}
        )::int as listened_ms_steps,
        count(*) filter (
          where d.event_type = ${PLAYBACK_TELEMETRY_EVENT_TYPES.COMPLETE}
        )::int as completed_count
      from member_playback_telemetry_dedupe d
      where
        d.created_at >= ${input.activeOnOrAfter}::timestamptz
        and (
          ${activeBefore}::timestamptz is null
          or d.created_at < ${activeBefore}::timestamptz
        )
      group by d.member_id
    )
    select
      wc.member_id,
      m.email,
      
      m.created_at,
            (wc.listened_ms_steps * ${TELEMETRY_PROGRESS_STEP_MS})::bigint as listened_ms,
      wc.play_count,
      wc.completed_count,
      null::text as recording_id,
      (wc.play_count + wc.listened_ms_steps + wc.completed_count)::int as matched_window_event_count
    from window_counts wc
    join members m
      on m.id = wc.member_id
    where
      wc.play_count >= ${minPlayCount}
      and wc.listened_ms_steps >= ${minProgressCount}
      and wc.completed_count >= ${minCompleteCount}
      and (wc.play_count + wc.listened_ms_steps + wc.completed_count) > 0
    order by
      matched_window_event_count desc,
      wc.play_count desc,
      wc.listened_ms_steps desc,
      wc.completed_count desc,
      m.created_at asc nulls last
    limit ${limit}
  `;

  return res.rows.map(mapAggregateRow);
}

async function previewByRecordingMinutesStreamed(
  input: RecordingMinutesStreamedPreviewInput,
): Promise<BadgePreviewMemberRow[]> {
  const minListenedMs = Math.max(0, Math.floor(input.minMinutes * 60_000));
  const limit = clampPreviewLimit(input.limit);

  const res = await sql<AggregateRow>`
    select
      mts.member_id,
      m.email,
      
      m.created_at,
      mts.listened_ms,
      mts.play_count,
      mts.completed_count,
      mts.recording_id
    from member_track_listen_stats mts
    join members m on m.id = mts.member_id
    where mts.recording_id = ${input.recordingId}
      and mts.listened_ms >= ${minListenedMs}
    order by
      mts.listened_ms desc,
      mts.play_count desc,
      m.created_at asc nulls last
    limit ${limit}
  `;

  return res.rows.map(mapAggregateRow);
}

async function previewByRecordingPlayCount(
  input: RecordingPlayCountPreviewInput,
): Promise<BadgePreviewMemberRow[]> {
  const minPlayCount = Math.max(0, Math.floor(input.minPlayCount));
  const limit = clampPreviewLimit(input.limit);

  const res = await sql<AggregateRow>`
    select
      mts.member_id,
      m.email,
      
      m.created_at,
      mts.listened_ms,
      mts.play_count,
      mts.completed_count,
      mts.recording_id
    from member_track_listen_stats mts
    join members m on m.id = mts.member_id
    where mts.recording_id = ${input.recordingId}
      and mts.play_count >= ${minPlayCount}
    order by
      mts.play_count desc,
      mts.listened_ms desc,
      m.created_at asc nulls last
    limit ${limit}
  `;

  return res.rows.map(mapAggregateRow);
}

async function previewByRecordingCompleteCount(
  input: RecordingCompleteCountPreviewInput,
): Promise<BadgePreviewMemberRow[]> {
  const minCompletedCount = Math.max(0, Math.floor(input.minCompletedCount));
  const limit = clampPreviewLimit(input.limit);

  const res = await sql<AggregateRow>`
    select
      mts.member_id,
      m.email,
      
      m.created_at,
      mts.listened_ms,
      mts.play_count,
      mts.completed_count,
      mts.recording_id
    from member_track_listen_stats mts
    join members m on m.id = mts.member_id
    where mts.recording_id = ${input.recordingId}
      and mts.completed_count >= ${minCompletedCount}
    order by
      mts.completed_count desc,
      mts.listened_ms desc,
      m.created_at asc nulls last
    limit ${limit}
  `;

  return res.rows.map(mapAggregateRow);
}

async function previewByExegesisContributionCount(
  input: ExegesisContributionCountPreviewInput,
): Promise<BadgePreviewMemberRow[]> {
  const minContributionCount = Math.max(
    0,
    Math.floor(input.minContributionCount),
  );
  const limit = clampPreviewLimit(input.limit);

  const res = await sql<AggregateRow>`
    select
      ei.member_id,
      m.email,
      
      m.created_at,
      null::bigint as listened_ms,
      null::int as play_count,
      null::int as completed_count,
      null::text as recording_id,
      null::int as matched_window_event_count,
      ei.contribution_count::int as contribution_count,
      null::int as vote_count,
      ei.public_name_unlocked_at
    from exegesis_identity ei
    join members m
      on m.id = ei.member_id
    where ei.contribution_count >= ${minContributionCount}
    order by
      ei.contribution_count desc,
      ei.public_name_unlocked_at desc nulls last,
      m.created_at asc nulls last
    limit ${limit}
  `;

  return res.rows.map(mapAggregateRow);
}

async function previewByExegesisVoteTally(
  input: ExegesisVoteTallyPreviewInput,
): Promise<BadgePreviewMemberRow[]> {
  const minVoteCount = Math.max(0, Math.floor(input.minVoteCount));
  const limit = clampPreviewLimit(input.limit);

  const res = await sql<AggregateRow>`
    with vote_totals as (
      select
        c.created_by_member_id as member_id,
        coalesce(sum(c.vote_count), 0)::int as vote_count
      from exegesis_comment c
      where c.status = 'live'
      group by c.created_by_member_id
    )
    select
      vt.member_id,
      m.email,
      
      m.created_at,
      null::bigint as listened_ms,
      null::int as play_count,
      null::int as completed_count,
      null::text as recording_id,
      null::int as matched_window_event_count,
      coalesce(ei.contribution_count, 0)::int as contribution_count,
      vt.vote_count,
      ei.public_name_unlocked_at
    from vote_totals vt
    join members m
      on m.id = vt.member_id
    left join exegesis_identity ei
      on ei.member_id = vt.member_id
    where vt.vote_count >= ${minVoteCount}
    order by
      vt.vote_count desc,
      coalesce(ei.contribution_count, 0) desc,
      m.created_at asc nulls last
    limit ${limit}
  `;

  return res.rows.map(mapAggregateRow);
}

async function previewByPublicNameUnlocked(
  input: PublicNameUnlockedPreviewInput,
): Promise<BadgePreviewMemberRow[]> {
  const limit = clampPreviewLimit(input.limit);

  const res = await sql<AggregateRow>`
    select
      ei.member_id,
      m.email,
      
      m.created_at,
      null::bigint as listened_ms,
      null::int as play_count,
      null::int as completed_count,
      null::text as recording_id,
      null::int as matched_window_event_count,
      ei.contribution_count::int as contribution_count,
      coalesce(vt.vote_count, 0)::int as vote_count,
      ei.public_name_unlocked_at
    from exegesis_identity ei
    join members m
      on m.id = ei.member_id
    left join lateral (
      select coalesce(sum(c.vote_count), 0)::int as vote_count
      from exegesis_comment c
      where c.created_by_member_id = ei.member_id
        and c.status = 'live'
    ) vt on true
    where ei.public_name_unlocked_at is not null
    order by
      ei.public_name_unlocked_at asc,
      ei.contribution_count desc,
      m.created_at asc nulls last
    limit ${limit}
  `;

  return res.rows.map(mapAggregateRow);
}


----- sanity/schemaTypes/badgeDefinition.ts -----
// sanity/schemaTypes/badgeDefinition.ts
import { defineField, defineType } from "sanity";

const AUTO_QUALIFICATION_MODE_OPTIONS = [
  { title: "Total minutes streamed", value: "minutes_streamed" },
  { title: "Total play count", value: "play_count" },
  { title: "Total complete count", value: "complete_count" },
  { title: "Joined within date window", value: "joined_within_window" },
  { title: "Active within playback window", value: "active_within_window" },
  {
    title: "Recording-specific minutes streamed",
    value: "recording_minutes_streamed",
  },
  {
    title: "Recording-specific play count",
    value: "recording_play_count",
  },
  {
    title: "Recording-specific complete count",
    value: "recording_complete_count",
  },
  {
    title: "Exegesis contribution count",
    value: "exegesis_contribution_count",
  },
  {
    title: "Exegesis cumulative vote tally",
    value: "exegesis_vote_tally",
  },
  {
    title: "Public name unlocked",
    value: "public_name_unlocked",
  },
] as const;

type BadgeDefinitionParent = {
  awardMode?: unknown;
  autoQualificationMode?: unknown;
};

function parentObject(parent: unknown): BadgeDefinitionParent | null {
  return parent && typeof parent === "object"
    ? (parent as BadgeDefinitionParent)
    : null;
}

function isAutomaticBadge(parent: unknown): boolean {
  return parentObject(parent)?.awardMode === "automatic";
}

function autoQualificationMode(parent: unknown): string {
  return String(parentObject(parent)?.autoQualificationMode ?? "");
}

function hiddenUnlessAutomatic(props: { parent: unknown }): boolean {
  return !isAutomaticBadge(props.parent);
}

function hiddenUnlessMode(
  parent: unknown,
  modes: readonly string[],
): boolean {
  return !modes.includes(autoQualificationMode(parent));
}

export const badgeDefinition = defineType({
  name: "badgeDefinition",
  title: "Badge Definition",
  type: "document",
  fields: [
    defineField({
      name: "title",
      title: "Title",
      type: "string",
      validation: (rule) => rule.required().min(1),
    }),
    defineField({
      name: "keySuffix",
      title: "Badge key suffix",
      type: "string",
      description:
        "Enter only the suffix portion, for example early_supporter. The app will derive the entitlement key as badge_early_supporter.",
      validation: (rule) =>
        rule
          .required()
          .regex(/^[a-z0-9]+(?:_[a-z0-9]+)*$/, {
            name: "badge key suffix",
            invert: false,
          })
          .error(
            "Badge key suffixes must use lowercase letters, numbers, and underscores only.",
          ),
    }),
    defineField({
      name: "description",
      title: "Description",
      type: "text",
      rows: 3,
    }),
    defineField({
      name: "image",
      title: "Image",
      type: "image",
      options: {
        hotspot: true,
      },
    }),
    defineField({
      name: "displayOrder",
      title: "Display order",
      type: "number",
      initialValue: 100,
      validation: (rule) => rule.integer().min(0),
    }),
    defineField({
      name: "featured",
      title: "Featured",
      type: "boolean",
      initialValue: false,
    }),
    defineField({
      name: "shareable",
      title: "Shareable",
      type: "boolean",
      initialValue: false,
    }),
    defineField({
      name: "undisclosed",
      title: "Undisclosed until unlocked",
      type: "boolean",
      initialValue: false,
      description:
        "Hide this badge from members who have not unlocked it yet. Useful for exclusive, retired, or time-bound badges that should not appear as visible locked promises.",
    }),
    defineField({
      name: "active",
      title: "Active",
      type: "boolean",
      initialValue: true,
    }),
    defineField({
      name: "awardMode",
      title: "Award mode",
      type: "string",
      initialValue: "manual",
      options: {
        list: [
          { title: "Manual", value: "manual" },
          { title: "Automatic", value: "automatic" },
        ],
        layout: "radio",
      },
      validation: (rule) => rule.required(),
    }),
    defineField({
      name: "autoAwardEnabled",
      title: "Auto-award enabled",
      type: "boolean",
      initialValue: true,
      hidden: hiddenUnlessAutomatic,
    }),
    defineField({
      name: "autoQualificationMode",
      title: "Automatic qualification mode",
      type: "string",
      options: {
        list: AUTO_QUALIFICATION_MODE_OPTIONS.map((option) => ({
          title: option.title,
          value: option.value,
        })),
      },
      hidden: hiddenUnlessAutomatic,
      validation: (rule) =>
        rule.custom((value, context) => {
          if (
            isAutomaticBadge(context.parent) &&
            (typeof value !== "string" || !value.trim())
          ) {
            return "Automatic badges require an automatic qualification mode.";
          }

          return true;
        }),
    }),
    defineField({
      name: "recordingId",
      title: "Recording ID",
      type: "string",
      hidden: ({ parent }) =>
        hiddenUnlessMode(parent, [
          "recording_minutes_streamed",
          "recording_play_count",
          "recording_complete_count",
        ]),
    }),
    defineField({
      name: "minMinutes",
      title: "Minimum minutes",
      type: "number",
      validation: (rule) => rule.min(0),
      hidden: ({ parent }) =>
        hiddenUnlessMode(parent, [
          "minutes_streamed",
          "recording_minutes_streamed",
        ]),
    }),
    defineField({
      name: "minPlayCount",
      title: "Minimum play count",
      type: "number",
      validation: (rule) => rule.min(0),
      hidden: ({ parent }) =>
        hiddenUnlessMode(parent, [
          "play_count",
          "recording_play_count",
          "active_within_window",
        ]),
    }),
    defineField({
      name: "minCompletedCount",
      title: "Minimum complete count",
      type: "number",
      validation: (rule) => rule.min(0),
      hidden: ({ parent }) =>
        hiddenUnlessMode(parent, [
          "complete_count",
          "recording_complete_count",
          "active_within_window",
        ]),
    }),
    defineField({
      name: "minProgressCount",
      title: "Minimum progress count",
      type: "number",
      validation: (rule) => rule.min(0),
      hidden: ({ parent }) =>
        hiddenUnlessMode(parent, ["active_within_window"]),
    }),
    defineField({
      name: "minContributionCount",
      title: "Minimum contribution count",
      type: "number",
      validation: (rule) => rule.min(0),
      hidden: ({ parent }) =>
        hiddenUnlessMode(parent, ["exegesis_contribution_count"]),
    }),
    defineField({
      name: "minVoteCount",
      title: "Minimum vote count",
      type: "number",
      validation: (rule) => rule.min(0),
      hidden: ({ parent }) => hiddenUnlessMode(parent, ["exegesis_vote_tally"]),
    }),
    defineField({
      name: "joinedOnOrAfter",
      title: "Joined on or after",
      type: "datetime",
      hidden: ({ parent }) =>
        hiddenUnlessMode(parent, ["joined_within_window"]),
    }),
    defineField({
      name: "joinedBefore",
      title: "Joined before",
      type: "datetime",
      hidden: ({ parent }) =>
        hiddenUnlessMode(parent, ["joined_within_window"]),
    }),
    defineField({
      name: "activeOnOrAfter",
      title: "Active on or after",
      type: "datetime",
      hidden: ({ parent }) =>
        hiddenUnlessMode(parent, ["active_within_window"]),
    }),
    defineField({
      name: "activeBefore",
      title: "Active before",
      type: "datetime",
      hidden: ({ parent }) =>
        hiddenUnlessMode(parent, ["active_within_window"]),
    }),
    defineField({
      name: "internalNote",
      title: "Internal note",
      type: "text",
      rows: 4,
    }),
  ],
  orderings: [
    {
      title: "Display order",
      name: "displayOrderAsc",
      by: [
        { field: "displayOrder", direction: "asc" },
        { field: "title", direction: "asc" },
      ],
    },
  ],
  preview: {
    select: {
      title: "title",
      keySuffix: "keySuffix",
      media: "image",
      active: "active",
      undisclosed: "undisclosed",
    },
    prepare(selection) {
      const title =
        typeof selection.title === "string" && selection.title.trim()
          ? selection.title.trim()
          : "Untitled badge";

      const entitlementKey =
        typeof selection.keySuffix === "string" && selection.keySuffix.trim()
          ? `badge_${selection.keySuffix.trim()}`
          : "No entitlement key";

      const stateBits = [
        selection.active === false ? "inactive" : null,
        selection.undisclosed === true ? "undisclosed" : null,
      ].filter((value): value is string => Boolean(value));

      return {
        title,
        subtitle:
          stateBits.length > 0
            ? `${entitlementKey} · ${stateBits.join(" · ")}`
            : entitlementKey,
        media: selection.media,
      };
    },
  },
});

===== COMPLETE CURRENT SOURCE — BADGE SUPPORT / DIRECT IMPORTS =====


----- app/admin/AdminPageFrame.tsx -----
import React from "react";

type Props = {
  title: string;
  subtitle?: string;
  embed?: boolean;
  maxWidth?: number;
  headerActions?: React.ReactNode;
  children: React.ReactNode;
};

export default function AdminPageFrame(props: Props) {
  const {
    title,
    subtitle,
    embed = false,
    maxWidth = 1120,
    headerActions,
    children,
  } = props;

  const outerStyle: React.CSSProperties = {
    padding: embed ? "22px 24px 26px" : "28px 28px 32px",
    maxWidth: embed ? undefined : maxWidth,
    margin: embed ? undefined : "0 auto",
  };

  return (
    <div style={outerStyle}>
      <div
        style={{
          display: "grid",
          gap: 20,
        }}
      >
        <div
          style={{
            display: "grid",
            gap: 14,
            padding: "2px 2px 0",
          }}
        >
          <div
            style={{
              display: "flex",
              alignItems: "flex-end",
              justifyContent: "space-between",
              gap: 18,
              flexWrap: "wrap",
            }}
          >
            <div style={{ minWidth: 0 }}>
              <div
                style={{
                  fontSize: 11,
                  letterSpacing: "0.16em",
                  textTransform: "uppercase",
                  opacity: 0.5,
                  userSelect: "none",
                }}
              >
                Admin
              </div>
              <h1
                style={{
                  margin: "7px 0 0",
                  fontSize: 24,
                  lineHeight: 1.08,
                  fontWeight: 700,
                  color: "rgba(255,255,255,0.97)",
                  textWrap: "balance",
                }}
              >
                {title}
              </h1>
              {subtitle ? (
                <div
                  style={{
                    marginTop: 10,
                    fontSize: 13,
                    lineHeight: 1.58,
                    opacity: 0.72,
                    maxWidth: 820,
                  }}
                >
                  {subtitle}
                </div>
              ) : null}
            </div>

            {headerActions ? (
              <div
                style={{
                  display: "flex",
                  gap: 8,
                  alignItems: "center",
                  flexWrap: "wrap",
                  justifyContent: "flex-start",
                }}
              >
                {headerActions}
              </div>
            ) : null}
          </div>

          <div
            style={{
              height: 1,
              background:
                "linear-gradient(90deg, rgba(255,224,166,0.24), rgba(255,255,255,0.08) 35%, rgba(255,255,255,0))",
              opacity: 0.75,
            }}
          />
        </div>

        <div
          style={{
            display: "grid",
            gap: 14,
            minHeight: 0,
          }}
        >
          {children}
        </div>
      </div>

      {embed ? (
        <style>{`
          html, body {
            background: transparent !important;
          }

          body {
            color: rgba(255,255,255,0.94);
          }

          ::selection {
            background: rgba(255, 222, 160, 0.22);
          }

          ::-webkit-scrollbar {
            width: 12px;
            height: 12px;
          }

          ::-webkit-scrollbar-track {
            background: rgba(255,255,255,0.03);
          }

          ::-webkit-scrollbar-thumb {
            background: rgba(255,255,255,0.14);
            border-radius: 999px;
            border: 2px solid rgba(10,10,14,0.92);
          }

          ::-webkit-scrollbar-thumb:hover {
            background: rgba(255,255,255,0.22);
          }
        `}</style>
      ) : null}
    </div>
  );
}

----- app/admin/badges/_lib/badgeDashboardTypes.ts -----
// web/app/admin/badges/_lib/badgeDashboardTypes.ts
import type { BadgeQualificationMode } from "@/lib/badgePreviewModes";
import type { BadgeAwardMode } from "@/lib/badges";

export type BadgeDefinitionOption = {
  entitlementKey: string;
  title: string;
  description: string | null;
  displayOrder: number;
  imageUrl: string | null;
  featured: boolean;
  shareable: boolean;
  undisclosed: boolean;
  awardMode: BadgeAwardMode;
  autoAwardEnabled: boolean;
  autoQualificationMode: BadgeQualificationMode | null;
};

export type PreviewRow = {
  memberId: string;
  email: string | null;
  joinedAt: string | null;
  listenedMs: number | null;
  minutesStreamed: number | null;
  playCount: number | null;
  completedCount: number | null;
  matchedRecordingId: string | null;
  matchedRecordingTitle: string | null;
  matchedWindowEventCount: number | null;
  contributionCount: number | null;
  exegesisVoteCount: number | null;
  publicNameUnlockedAt: string | null;
};

export type PreviewResponse = {
  ok: boolean;
  count?: number;
  rows?: PreviewRow[];
  error?: string;
};

export type AwardResponse = {
  ok: boolean;
  result?: {
    entitlementKey: string;
    attempted: number;
    inserted: number;
    alreadyHeld: number;
  };
  summary?: {
    attempted: number;
    inserted: number;
    alreadyHeld: number;
    hasNewGrants: boolean;
  };
  error?: string;
};

export type Props = {
  embed: boolean;
  badgeDefinitions: BadgeDefinitionOption[];
};

export type RecordingSearchResult = {
  recordingId: string;
  title: string;
  artist?: string | null;
  albumSlug?: string | null;
  albumTitle?: string | null;
};

export type RecordingSearchResponse = {
  ok: boolean;
  results?: RecordingSearchResult[];
  error?: string;
};

export type FormState = {
  entitlementKey: string;
  mode: BadgeQualificationMode;
  minMinutes: string;
  minPlayCount: string;
  minCompletedCount: string;
  minProgressCount: string;
  minContributionCount: string;
  minVoteCount: string;
  joinedOnOrAfter: string;
  joinedBefore: string;
  activeOnOrAfter: string;
  activeBefore: string;
  recordingId: string;
  limit: string;
  grantReason: string;
};

export type SelectedModeInputRequirements = {
  minMinutes: boolean;
  minPlayCount: boolean;
  minCompletedCount: boolean;
  minProgressCount: boolean;
  minContributionCount: boolean;
  minVoteCount: boolean;
  joinedWindow: boolean;
  activeWindow: boolean;
  recordingId: boolean;
};

export type SelectedModeFieldText = {
  minMinutesLabel: string;
  minMinutesHelp: string | null;
  minPlayCountLabel: string;
  minPlayCountHelp: string | null;
  minCompletedCountLabel: string;
  minCompletedCountHelp: string | null;
  minProgressCountLabel: string;
  minProgressCountHelp: string | null;
  minContributionCountLabel: string;
  minContributionCountHelp: string | null;
  minVoteCountLabel: string;
  minVoteCountHelp: string | null;
  joinedOnOrAfterLabel: string;
  joinedBeforeLabel: string;
  joinedWindowHelp: string | null;
  activeOnOrAfterLabel: string;
  activeBeforeLabel: string;
  activeWindowHelp: string | null;
  recordingIdLabel: string;
  recordingIdPlaceholder: string;
  recordingIdHelp: string | null;
};


----- app/admin/badges/_lib/badgeDashboardUtils.ts -----
// web/app/admin/badges/_lib/badgeDashboardUtils.ts
import type { FormState } from "./badgeDashboardTypes";

export const DEFAULT_FORM_STATE: FormState = {
  entitlementKey: "",
  mode: "minutes_streamed",
  minMinutes: "500",
  minPlayCount: "10",
  minCompletedCount: "3",
  minProgressCount: "1",
  minContributionCount: "10",
  minVoteCount: "10",
  joinedOnOrAfter: "",
  joinedBefore: "",
  activeOnOrAfter: "",
  activeBefore: "",
  recordingId: "",
  limit: "200",
  grantReason: "",
};

export function formatDateTime(value: string | null): string {
  if (!value) return "—";

  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return value;

  return date.toLocaleString();
}

export function formatMetric(value: number | null): string {
  if (typeof value !== "number" || !Number.isFinite(value)) return "—";
  return value.toLocaleString();
}

export function getBadgeCardBorder(
  isSelected: boolean,
  isFeatured: boolean,
): string {
  if (isSelected) return "1px solid rgba(255,255,255,0.32)";
  if (isFeatured) return "1px solid rgba(255,255,255,0.18)";
  return "1px solid rgba(255,255,255,0.1)";
}

export function buildPreviewPayload(
  form: FormState,
): Record<string, string | number> {
  const limit = form.limit.trim() ? Number(form.limit) : 200;

  switch (form.mode) {
    case "minutes_streamed":
      return {
        mode: form.mode,
        minMinutes: Number(form.minMinutes || "0"),
        limit,
      };

    case "play_count":
      return {
        mode: form.mode,
        minPlayCount: Number(form.minPlayCount || "0"),
        limit,
      };

    case "complete_count":
      return {
        mode: form.mode,
        minCompletedCount: Number(form.minCompletedCount || "0"),
        limit,
      };

    case "joined_within_window": {
      const payload: Record<string, string | number> = {
        mode: form.mode,
        joinedOnOrAfter: form.joinedOnOrAfter,
        limit,
      };

      if (form.joinedBefore.trim()) {
        payload.joinedBefore = form.joinedBefore.trim();
      }

      return payload;
    }

    case "active_within_window": {
      const payload: Record<string, string | number> = {
        mode: form.mode,
        activeOnOrAfter: form.activeOnOrAfter,
        minPlayCount: Number(form.minPlayCount || "0"),
        minProgressCount: Number(form.minProgressCount || "0"),
        minCompleteCount: Number(form.minCompletedCount || "0"),
        limit,
      };

      if (form.activeBefore.trim()) {
        payload.activeBefore = form.activeBefore.trim();
      }

      return payload;
    }

    case "recording_minutes_streamed":
      return {
        mode: form.mode,
        recordingId: form.recordingId,
        minMinutes: Number(form.minMinutes || "0"),
        limit,
      };

    case "recording_play_count":
      return {
        mode: form.mode,
        recordingId: form.recordingId,
        minPlayCount: Number(form.minPlayCount || "0"),
        limit,
      };

    case "recording_complete_count":
      return {
        mode: form.mode,
        recordingId: form.recordingId,
        minCompletedCount: Number(form.minCompletedCount || "0"),
        limit,
      };

    case "exegesis_contribution_count":
      return {
        mode: form.mode,
        minContributionCount: Number(form.minContributionCount || "0"),
        limit,
      };

    case "exegesis_vote_tally":
      return {
        mode: form.mode,
        minVoteCount: Number(form.minVoteCount || "0"),
        limit,
      };

    case "public_name_unlocked":
      return {
        mode: form.mode,
        limit,
      };
  }
}


----- app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts -----
// web/app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts
export const PANEL_BORDER = "1px solid rgba(255,255,255,0.12)";
export const ROW_BORDER = "1px solid rgba(255,255,255,0.08)";
export const TEXT_PRIMARY = "rgba(255,255,255,0.92)";
export const TEXT_STRONG = "rgba(255,255,255,0.86)";
export const TEXT_MUTED = "rgba(255,255,255,0.68)";
export const TEXT_FAINT = "rgba(255,255,255,0.58)";
export const BG_PANEL = "rgba(255,255,255,0.04)";
export const BG_INSET = "rgba(255,255,255,0.02)";
export const BG_ACCENT = "rgba(255,255,255,0.08)";
export const BG_MEMBER = "rgba(255,255,255,0.78)";
export const BG_ANON = "rgba(255,255,255,0.22)";
export const FONT_SIZE_UI = 12;
export const FONT_SIZE_DEDUPE = 11;

----- app/api/admin/recordings/search/route.ts -----
import { NextResponse } from "next/server";
import { requireAdminMemberId } from "@/lib/adminAuth";
import { searchRecordingsForAdmin } from "@/lib/albums";

function asString(value: unknown): string | null {
  return typeof value === "string" && value.trim() ? value.trim() : null;
}

function asLimit(value: unknown): number | undefined {
  if (typeof value === "number" && Number.isFinite(value)) {
    return Math.max(1, Math.min(25, Math.floor(value)));
  }

  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    if (Number.isFinite(parsed)) {
      return Math.max(1, Math.min(25, Math.floor(parsed)));
    }
  }

  return undefined;
}

export async function GET(request: Request) {
  try {
    await requireAdminMemberId();

    const { searchParams } = new URL(request.url);
    const query = asString(searchParams.get("q"));
    const limit = asLimit(searchParams.get("limit"));

    if (!query) {
      return NextResponse.json({
        ok: true,
        query: "",
        results: [],
      });
    }

    const results = await searchRecordingsForAdmin({
      query,
      limit,
    });

    return NextResponse.json({
      ok: true,
      query,
      results,
    });
  } catch (error) {
    const message =
      error instanceof Error
        ? error.message
        : "Unable to search recordings.";

    const status =
      message === "Unauthorized" ? 401 : message === "Forbidden" ? 403 : 400;

    return NextResponse.json(
      {
        ok: false,
        error: message,
      },
      { status },
    );
  }
}

----- app/home/badges/badgeAwardTypes.ts -----
export type BadgeAwardNotice = {
  entitlementKey: string;
  title: string;
  description: string | null;
  imageUrl: string | null;
  shareable: boolean;
  unlockedAt: string;
};

function asTrimmedString(value: unknown): string | null {
  return typeof value === "string" && value.trim() ? value.trim() : null;
}

function asBoolean(value: unknown): boolean {
  return value === true;
}

function isRecord(value: unknown): value is Record<string, unknown> {
  return typeof value === "object" && value !== null;
}

export function normalizeBadgeAwardNotices(
  value: unknown,
): BadgeAwardNotice[] {
  if (!Array.isArray(value)) return [];

  const notices: BadgeAwardNotice[] = [];

  for (const item of value) {
    if (!isRecord(item)) continue;

    const entitlementKey = asTrimmedString(item.entitlementKey);
    const title = asTrimmedString(item.title);
    const unlockedAt = asTrimmedString(item.unlockedAt);

    if (!entitlementKey || !title || !unlockedAt) continue;

    notices.push({
      entitlementKey,
      title,
      description: asTrimmedString(item.description),
      imageUrl: asTrimmedString(item.imageUrl),
      shareable: asBoolean(item.shareable),
      unlockedAt,
    });
  }

  return notices;
}

----- app/home/modules/PortalMemberPanel.tsx -----
// web/app/home/modules/PortalMemberPanel.tsx
"use client";

import React from "react";
import type { PortalMemberSummary } from "@/lib/memberDashboard";
import BadgeCabinet from "./badges/BadgeCabinet";

type Props = {
  summary: PortalMemberSummary;
  title?: string;
  embedded?: boolean;
};

function MetricTable(props: {
  rows: Array<{
    label: string;
    value: React.ReactNode;
    muted?: boolean;
  }>;
}) {
  const { rows } = props;

  return (
    <div
      role="table"
      aria-label="Member summary metrics"
      style={{
        minWidth: 0,
        borderTop: "1px solid rgba(255,255,255,0.08)",
      }}
    >
      {rows.map((row) => (
        <div
          key={row.label}
          role="row"
          style={{
            display: "grid",
            gridTemplateColumns: "minmax(0, 140px) minmax(0, 1fr)",
            gap: 12,
            alignItems: "start",
            padding: "10px 0",
            borderBottom: "1px solid rgba(255,255,255,0.08)",
            minWidth: 0,
          }}
        >
          <div
            role="columnheader"
            style={{
              fontSize: 10,
              letterSpacing: 0.3,
              textTransform: "uppercase",
              lineHeight: 1.2,
              opacity: 0.5,
            }}
          >
            {row.label}
          </div>

          <div
            role="cell"
            style={{
              fontSize: 12,
              lineHeight: 1.4,
              opacity: row.muted ? 0.56 : 0.9,
              minWidth: 0,
              overflowWrap: "anywhere",
            }}
          >
            {row.value}
          </div>
        </div>
      ))}
    </div>
  );
}

export default function PortalMemberPanel(props: Props) {
  const { summary, embedded = false } = props;

  const displayName = summary.identity?.displayName?.trim() || "Anonymous";
  const contributionCount = summary.contributionCount;
  const minutesStreamed = summary.minutesStreamed;
  const favouriteTrack = summary.favouriteTrack;

  return (
    <div
      style={{
        borderRadius: embedded ? 0 : 18,
        border: embedded ? "none" : "1px solid rgba(255,255,255,0.10)",
        background: embedded ? "transparent" : "rgba(255,255,255,0.04)",
        padding: embedded ? 0 : 16,
        minWidth: 0,
      }}
    >
      <div
        style={{
          display: "grid",
          gap: 14,
          minWidth: 0,
        }}
      >
        <div style={{ minWidth: 0 }}>
          <div
            style={{
              marginTop: 8,
              marginBottom: 4,
              fontSize: 18,
              lineHeight: 1.3,
              letterSpacing: -0.02,
              opacity: 0.85,
              minWidth: 0,
              overflowWrap: "anywhere",
            }}
          >
            {displayName}
          </div>

          <div style={{ marginTop: 14, minWidth: 0 }}>
            <BadgeCabinet badges={summary.badges} />
          </div>
        </div>

        <MetricTable
          rows={[
            {
              label: "Minutes streamed",
              value: minutesStreamed ?? "—",
              muted: minutesStreamed == null,
            },
            {
              label: "Favourite track",
              value: favouriteTrack ? favouriteTrack.title : "—",
              muted: !favouriteTrack,
            },
            {
              label: "Exegesis posts",
              value: contributionCount ?? "—",
              muted: contributionCount == null,
            },
          ]}
        />
      </div>
    </div>
  );
}

----- app/home/modules/badges/BadgeCabinetStyles.tsx -----
// web/app/home/modules/badges/BadgeCabinetStyles.tsx
"use client";

import React from "react";

export default function BadgeCabinetStyles() {
  return (
    <style jsx global>{`
      :global(:root) {
        --portal-badge-columns-collapsed: 6;
        --portal-badge-columns-expanded: 3;
        --portal-badge-gap-collapsed: 14px;
        --portal-badge-gap-expanded: 18px;
        --portal-badge-art-scale-collapsed: 0.82;
        --portal-badge-art-scale-expanded: 1;
        --portal-badge-caption-offset: -3px;
        --portal-badge-caption-row-gap-collapsed: 10px;
        --portal-badge-caption-row-gap-expanded: 16px;
      }

      @media (max-width: 640px) {
        :global(:root) {
          --portal-badge-gap-collapsed: 10px;
          --portal-badge-gap-expanded: 12px;
          --portal-badge-art-scale-collapsed: 0.84;
          --portal-badge-caption-row-gap-collapsed: 8px;
          --portal-badge-caption-row-gap-expanded: 12px;
        }
      }

      @media (max-width: 420px) {
        :global(:root) {
          --portal-badge-gap-collapsed: 8px;
          --portal-badge-gap-expanded: 10px;
          --portal-badge-art-scale-collapsed: 0.86;
          --portal-badge-caption-row-gap-collapsed: 6px;
          --portal-badge-caption-row-gap-expanded: 10px;
        }
      }

      .portal-member-badge-grid {
        display: grid;
        width: 100%;
        min-width: 0;
        grid-template-columns: repeat(
          var(--portal-badge-columns-collapsed),
          minmax(0, 1fr)
        );
        column-gap: var(--portal-badge-gap-collapsed);
        row-gap: var(--portal-badge-caption-row-gap-collapsed);
        align-items: start;
      }

      .portal-member-badge-grid.portal-member-badges--expanded {
        grid-template-columns: repeat(
          var(--portal-badge-columns-expanded),
          minmax(0, 1fr)
        );
        column-gap: var(--portal-badge-gap-expanded);
        row-gap: var(--portal-badge-caption-row-gap-expanded);
      }

      .portal-member-badge-shell {
        display: grid;
        justify-items: center;
        align-self: start;
        gap: 0;
        min-width: 0;
        width: 100%;
        transform: translate(0px, 0px);
      }

      .portal-member-badge-meta {
        width: 100%;
        max-width: 100%;
        display: grid;
        grid-template-rows: 0fr;
        margin-top: 0;
        opacity: 0;
        transform: translateY(-4px);
        pointer-events: none;
        transition:
          grid-template-rows 260ms cubic-bezier(0.22, 1, 0.36, 1),
          opacity 180ms ease,
          transform 220ms ease,
          margin-top 220ms ease;
        transition-delay: 0ms, 0ms, 0ms, 0ms;
        text-align: center;
      }

      .portal-member-badge-grid.portal-member-badges--expanded
        .portal-member-badge-meta {
        grid-template-rows: 1fr;
        margin-top: 10px;
        opacity: 1;
        transform: translateY(0);
        pointer-events: auto;
        transition-delay: 0ms, 110ms, 110ms, 0ms;
      }

      .portal-member-badge-meta-inner {
        overflow: hidden;
        min-height: 0;
        opacity: 0;
        transform: translateY(var(--portal-badge-caption-offset));
        transition:
          opacity 180ms ease,
          transform 220ms ease;
        transition-delay: 0ms, 0ms;
      }

      .portal-member-badge-grid.portal-member-badges--expanded
        .portal-member-badge-meta-inner {
        opacity: 1;
        transform: translateY(0);
        transition-delay: 140ms, 140ms;
      }

      .portal-member-badge-live-region {
        position: absolute;
        width: 1px;
        height: 1px;
        padding: 0;
        margin: -1px;
        overflow: hidden;
        clip: rect(0, 0, 0, 0);
        white-space: nowrap;
        border: 0;
      }

      @media (prefers-reduced-motion: reduce) {
        .portal-member-badge-grid,
        .portal-member-badge-shell,
        .portal-member-badge-meta,
        .portal-member-badge-meta-inner {
          transition: none !important;
          transition-delay: 0ms !important;
        }

        .portal-member-badge-meta {
          grid-template-rows: none !important;
        }
      }
    `}</style>
  );
}

----- app/home/modules/badges/BadgeUnlockVisualStyles.tsx -----
// web/app/home/modules/badges/BadgeUnlockVisualStyles.tsx
"use client";

import React from "react";

export default function BadgeUnlockVisualStyles() {
  return (
    <style jsx global>{`
      @keyframes portalBadgeLockedPulse {
        0%,
        100% {
          transform: scale(1);
          opacity: 0.92;
        }
        50% {
          transform: scale(1.035);
          opacity: 1;
        }
      }

      @keyframes portalBadgeUnlockedIdleGlow {
        0%,
        100% {
          transform: scale(1);
          opacity: 0.16;
        }
        50% {
          transform: scale(1.028);
          opacity: 0.26;
        }
      }

      @keyframes portalBadgeInnerAuraPulse {
        0%,
        100% {
          transform: translate(-50%, -50%) scale(0.98);
          opacity: 0.34;
        }
        50% {
          transform: translate(-50%, -50%) scale(1.05);
          opacity: 0.5;
        }
      }

      @keyframes portalBadgeCentreRadiance {
        0%,
        100% {
          transform: translate(-50%, -50%) scale(0.96);
          opacity: 0.22;
        }
        50% {
          transform: translate(-50%, -50%) scale(1.04);
          opacity: 0.36;
        }
      }

      @keyframes portalBadgeEmberRiseA {
        0% {
          transform: translate3d(0, 5px, 0) rotate(-5deg) scale(0.56, 0.76);
          opacity: 0;
        }
        18% {
          transform: translate3d(-0.8px, -2px, 0) rotate(-8deg)
            scale(0.72, 0.98);
          opacity: 0.46;
        }
        44% {
          transform: translate3d(0.9px, -11px, 0) rotate(-4deg) scale(0.9, 1.16);
          opacity: 0.34;
        }
        72% {
          transform: translate3d(-0.7px, -21px, 0) rotate(1deg)
            scale(0.98, 1.28);
          opacity: 0.18;
        }
        100% {
          transform: translate3d(0.5px, -31px, 0) rotate(3deg) scale(0.9, 1.36);
          opacity: 0;
        }
      }

      @keyframes portalBadgeEmberRiseB {
        0% {
          transform: translate3d(0, 5px, 0) rotate(3deg) scale(0.54, 0.74);
          opacity: 0;
        }
        18% {
          transform: translate3d(0.7px, -2px, 0) rotate(6deg) scale(0.7, 0.96);
          opacity: 0.4;
        }
        46% {
          transform: translate3d(-0.8px, -12px, 0) rotate(3deg)
            scale(0.86, 1.12);
          opacity: 0.28;
        }
        74% {
          transform: translate3d(0.9px, -23px, 0) rotate(-1deg)
            scale(0.92, 1.24);
          opacity: 0.14;
        }
        100% {
          transform: translate3d(-0.6px, -33px, 0) rotate(-3deg)
            scale(0.84, 1.32);
          opacity: 0;
        }
      }

      @keyframes portalBadgeEmberRiseC {
        0% {
          transform: translate3d(0, 4px, 0) rotate(-3deg) scale(0.56, 0.74);
          opacity: 0;
        }
        20% {
          transform: translate3d(0.8px, -1px, 0) rotate(-6deg) scale(0.7, 0.94);
          opacity: 0.36;
        }
        48% {
          transform: translate3d(-0.9px, -10px, 0) rotate(-2deg)
            scale(0.84, 1.08);
          opacity: 0.24;
        }
        72% {
          transform: translate3d(0.7px, -19px, 0) rotate(2deg) scale(0.9, 1.2);
          opacity: 0.12;
        }
        100% {
          transform: translate3d(0.5px, -28px, 0) rotate(4deg) scale(0.82, 1.28);
          opacity: 0;
        }
      }

      @keyframes portalBadgeEmberBurstA {
        0% {
          transform: translate3d(0, 4px, 0) rotate(-6deg) scale(0.58, 0.8);
          opacity: 0;
        }
        16% {
          transform: translate3d(-1px, -5px, 0) rotate(-10deg) scale(0.84, 1.08);
          opacity: 0.7;
        }
        40% {
          transform: translate3d(1.2px, -18px, 0) rotate(-5deg)
            scale(0.98, 1.32);
          opacity: 0.42;
        }
        68% {
          transform: translate3d(-1.1px, -31px, 0) rotate(2deg)
            scale(0.94, 1.48);
          opacity: 0.2;
        }
        100% {
          transform: translate3d(0.8px, -44px, 0) rotate(5deg) scale(0.84, 1.62);
          opacity: 0;
        }
      }

      @keyframes portalBadgeEmberBurstB {
        0% {
          transform: translate3d(0, 4px, 0) rotate(4deg) scale(0.56, 0.78);
          opacity: 0;
        }
        16% {
          transform: translate3d(0.9px, -6px, 0) rotate(8deg) scale(0.8, 1.04);
          opacity: 0.58;
        }
        40% {
          transform: translate3d(-1.1px, -20px, 0) rotate(4deg)
            scale(0.92, 1.26);
          opacity: 0.36;
        }
        68% {
          transform: translate3d(1.3px, -34px, 0) rotate(-2deg) scale(0.9, 1.42);
          opacity: 0.16;
        }
        100% {
          transform: translate3d(-0.9px, -47px, 0) rotate(-5deg)
            scale(0.8, 1.56);
          opacity: 0;
        }
      }

      @keyframes portalBadgeEmberBurstC {
        0% {
          transform: translate3d(0, 4px, 0) rotate(-4deg) scale(0.56, 0.78);
          opacity: 0;
        }
        18% {
          transform: translate3d(-0.9px, -5px, 0) rotate(-8deg) scale(0.78, 1);
          opacity: 0.5;
        }
        42% {
          transform: translate3d(1px, -17px, 0) rotate(-4deg) scale(0.88, 1.2);
          opacity: 0.3;
        }
        72% {
          transform: translate3d(-1px, -29px, 0) rotate(2deg) scale(0.86, 1.36);
          opacity: 0.14;
        }
        100% {
          transform: translate3d(0.7px, -40px, 0) rotate(5deg) scale(0.78, 1.48);
          opacity: 0;
        }
      }

      @keyframes portalBadgeSpinHeavy {
        0% {
          transform: rotateY(0deg);
        }
        26% {
          transform: rotateY(78deg);
        }
        29.5% {
          transform: rotateY(108deg);
        }
        50% {
          transform: rotateY(180deg);
        }
        70.5% {
          transform: rotateY(252deg);
        }
        74% {
          transform: rotateY(282deg);
        }
        100% {
          transform: rotateY(360deg);
        }
      }
      @keyframes portalBadgeSpinFaster {
        0% {
          transform: rotateY(0deg);
        }
        27% {
          transform: rotateY(80deg);
        }
        30.5% {
          transform: rotateY(108deg);
        }
        50% {
          transform: rotateY(180deg);
        }
        69.5% {
          transform: rotateY(252deg);
        }
        73% {
          transform: rotateY(280deg);
        }
        100% {
          transform: rotateY(360deg);
        }
      }

      @keyframes portalBadgeSpinFastest {
        0% {
          transform: rotateY(0deg);
        }
        18% {
          transform: rotateY(102deg);
        }
        44% {
          transform: rotateY(246deg);
        }
        68% {
          transform: rotateY(328deg);
        }
        88.5% {
          transform: rotateY(353deg);
        }
        97.8% {
          transform: rotateY(359.1deg);
        }
        100% {
          transform: rotateY(360deg);
        }
      }

      @keyframes portalBadgeSpinSettle {
        0% {
          transform: translate3d(0, 0, 0) rotateZ(-1.6deg) scale(0.882);
          filter: brightness(0.97);
        }
        18% {
          transform: translate3d(0, 0, 0) rotateZ(-0.7deg) scale(0.952);
          filter: brightness(1);
        }
        52% {
          transform: translate3d(0, 0, 0) rotateZ(0.96deg) scale(1.058);
          filter: brightness(1.08);
        }
        72% {
          transform: translate3d(0, 0, 0) rotateZ(-0.32deg) scale(1.012);
          filter: brightness(1.03);
        }
        88% {
          transform: translate3d(0, 0, 0) rotateZ(0.14deg) scale(1.004);
          filter: brightness(1.01);
        }
        96% {
          transform: translate3d(0, 0, 0) rotateZ(0deg) scale(1);
          filter: brightness(1);
        }
        96.4% {
          transform: translate3d(-2px, 0.9px, 0) rotateZ(-0.58deg) scale(1.026);
          filter: brightness(1.12);
        }
        97.2% {
          transform: translate3d(2.3px, -1.15px, 0) rotateZ(0.5deg) scale(0.996);
          filter: brightness(1.08);
        }
        98% {
          transform: translate3d(-1.6px, 0.95px, 0) rotateZ(-0.34deg)
            scale(1.012);
          filter: brightness(1.05);
        }
        98.8% {
          transform: translate3d(1px, -0.6px, 0) rotateZ(0.22deg) scale(0.998);
          filter: brightness(1.03);
        }
        99.4% {
          transform: translate3d(-0.5px, 0.3px, 0) rotateZ(-0.1deg) scale(1.003);
          filter: brightness(1.015);
        }
        100% {
          transform: translate3d(0, 0, 0) rotateZ(0deg) scale(1);
          filter: brightness(1);
        }
      }

      @keyframes portalBadgeRevealLayerActivation {
        0% {
          filter: saturate(1) brightness(1);
        }
        45% {
          filter: saturate(1.03) brightness(1.01);
        }
        100% {
          filter: saturate(1.08) brightness(1.03);
        }
      }

      @keyframes portalBadgeUnlockEnergyFlare {
        0% {
          opacity: 0;
          transform: scale(0.62);
          filter: blur(10px);
        }
        12% {
          opacity: 0.18;
        }
        34% {
          opacity: 0.34;
          transform: scale(0.9);
          filter: blur(8px);
        }
        66% {
          opacity: 0.16;
          transform: scale(1.12);
          filter: blur(12px);
        }
        100% {
          opacity: 0;
          transform: scale(1.22);
          filter: blur(14px);
        }
      }

      @keyframes portalBadgeEdgeSpinePresence {
        0% {
          opacity: 0.42;
          filter: blur(0.7px) brightness(1);
        }
        18% {
          opacity: 0.64;
          filter: blur(0.42px) brightness(1.12);
        }
        44% {
          opacity: 0.9;
          filter: blur(0.16px) brightness(1.24);
        }
        70% {
          opacity: 0.82;
          filter: blur(0.24px) brightness(1.18);
        }
        100% {
          opacity: 0.48;
          filter: blur(0.62px) brightness(1.02);
        }
      }

      @keyframes portalBadgeQuarterGlintStage1 {
        0%,
        26.2%,
        28.6%,
        71.4%,
        73.8%,
        100% {
          opacity: 0;
          transform: translate(-50%, -50%) scaleX(0.48);
          filter: blur(1.2px);
        }
        27.4%,
        72.6% {
          opacity: 0.82;
          transform: translate(-50%, -50%) scaleX(1);
          filter: blur(0.24px);
        }
      }

      @keyframes portalBadgeQuarterGlintStage2 {
        0%,
        27.1%,
        29.4%,
        70.6%,
        72.9%,
        100% {
          opacity: 0;
          transform: translate(-50%, -50%) scaleX(0.48);
          filter: blur(1.05px);
        }
        28.25%,
        71.75% {
          opacity: 0.96;
          transform: translate(-50%, -50%) scaleX(1);
          filter: blur(0.18px);
        }
      }

      @keyframes portalBadgeQuarterGlintStage3 {
        0%,
        14.8%,
        17.0%,
        49.8%,
        52.2%,
        100% {
          opacity: 0;
          transform: translate(-50%, -50%) scaleX(0.46);
          filter: blur(0.95px);
        }
        15.9%,
        51.0% {
          opacity: 1;
          transform: translate(-50%, -50%) scaleX(1);
          filter: blur(0.12px);
        }
      }

      @keyframes portalBadgeImpactFlash {
        0%,
        95.8% {
          opacity: 0;
          transform: scale(0.9);
          filter: blur(12px);
        }
        96.8% {
          opacity: 0.28;
          transform: scale(0.985);
          filter: blur(8px);
        }
        97.6% {
          opacity: 1;
          transform: scale(1.02);
          filter: blur(3px);
        }
        98.8% {
          opacity: 0.42;
          transform: scale(1.01);
          filter: blur(6px);
        }
        100% {
          opacity: 0;
          transform: scale(1.06);
          filter: blur(12px);
        }
      }

      @keyframes portalBadgeFinalShimmerCelebrating {
        0%,
        62% {
          opacity: 0;
          transform: translate3d(-168%, 0, 0) rotate(24deg);
        }
        70% {
          opacity: 0.2;
        }
        78% {
          opacity: 1;
        }
        88% {
          opacity: 0.32;
          transform: translate3d(148%, 0, 0) rotate(24deg);
        }
        100% {
          opacity: 0;
          transform: translate3d(148%, 0, 0) rotate(24deg);
        }
      }

      @keyframes portalBadgeMetaQuestionDissolve {
        0% {
          opacity: 0.3;
          transform: translate3d(0, 0, 0) scale(1);
          filter: blur(0px);
        }
        100% {
          opacity: 0;
          transform: translate3d(0, -3px, 0) scale(0.96);
          filter: blur(1.2px);
        }
      }

      @keyframes portalBadgeMetaTitleReveal {
        0% {
          opacity: 0;
          transform: translate3d(0, 5px, 0);
          filter: blur(1.8px);
        }
        100% {
          opacity: 0.7;
          transform: translate3d(0, 0, 0);
          filter: blur(0px);
        }
      }

      @keyframes portalBadgeMetaDescriptionReveal {
        0% {
          opacity: 0;
          transform: translate3d(0, 6px, 0);
          filter: blur(2.2px);
        }
        100% {
          opacity: 0.58;
          transform: translate3d(0, 0, 0);
          filter: blur(0px);
        }
      }

      .portal-badge-unlock-visual {
        position: relative;
        width: 100%;
        aspect-ratio: 1 / 1;
        overflow: visible;
        outline: none;
        perspective: 1400px;
        perspective-origin: 50% 50%;
      }

      .portal-badge-unlock-visual-inner {
        position: absolute;
        inset: 0;
        transform: scale(var(--portal-badge-art-scale-collapsed));
        transform-origin: center center;
        transition: transform 260ms cubic-bezier(0.22, 1, 0.36, 1);
        will-change: transform;
      }

      .portal-badge-unlock-host[data-badge-expanded="true"]
        .portal-badge-unlock-visual-inner {
        transform: scale(var(--portal-badge-art-scale-expanded));
      }

      .portal-badge-spin-stage-1,
      .portal-badge-spin-stage-2,
      .portal-badge-spin-stage-3,
      .portal-badge-art-shell {
        transform-style: preserve-3d;
        transform-origin: 50% 50%;
        backface-visibility: hidden;
        -webkit-backface-visibility: hidden;
        will-change: transform;
      }

      .portal-badge-spin-stage-1--unlocking {
        animation: portalBadgeSpinHeavy 1480ms linear both;
      }

      .portal-badge-spin-stage-2--unlocking {
        animation: portalBadgeSpinFaster 900ms linear 1480ms both;
      }

      .portal-badge-spin-stage-3--unlocking {
        animation: portalBadgeSpinFastest 560ms linear 2380ms both;
      }

      .portal-badge-art-shell--unlocking {
        animation: portalBadgeSpinSettle 3060ms cubic-bezier(0.22, 1, 0.36, 1)
          both;
      }

      .portal-badge-art-shell img,
      .portal-badge-art-shell span,
      .portal-badge-art-shell div:not(.portal-badge-edge-spine) {
        backface-visibility: hidden;
        -webkit-backface-visibility: hidden;
        transform: translateZ(0.01px);
      }

      .portal-badge-art-base-greyscale {
        backface-visibility: hidden;
        -webkit-backface-visibility: hidden;
      }

      .portal-badge-reveal-canvas-layer {
        position: absolute;
        inset: 0;
        pointer-events: none;
        isolation: isolate;
        opacity: 1;
        filter: saturate(1) brightness(1);
      }

      .portal-badge-reveal-canvas-layer--unlocking {
        animation: portalBadgeRevealLayerActivation 600ms
          cubic-bezier(0.18, 0.88, 0.24, 1) 1210ms both;
      }

      .portal-badge-quarter-glint {
        position: absolute;
        left: 50%;
        top: 50%;
        width: 3.2%;
        height: 84%;
        border-radius: 999px;
        pointer-events: none;
        background: linear-gradient(
          90deg,
          rgba(255, 255, 255, 0) 0%,
          rgba(255, 255, 255, 0.86) 34%,
          rgba(255, 255, 255, 1) 50%,
          rgba(255, 255, 255, 0.86) 66%,
          rgba(255, 255, 255, 0) 100%
        );
        box-shadow:
          0 0 8px rgba(255, 255, 255, 0.34),
          0 0 16px rgba(255, 255, 255, 0.14);
        opacity: 0;
        z-index: 3;
      }

      .portal-badge-quarter-glint--stage-1 {
        animation: portalBadgeQuarterGlintStage1 1480ms linear both;
      }

      .portal-badge-quarter-glint--stage-2 {
        animation: portalBadgeQuarterGlintStage2 900ms linear 1480ms both;
      }

      .portal-badge-quarter-glint--stage-3 {
        animation: portalBadgeQuarterGlintStage3 560ms linear 2380ms both;
      }

      .portal-badge-edge-spine {
        transform-style: preserve-3d;
        transform-origin: 50% 50%;
        backface-visibility: hidden;
        -webkit-backface-visibility: hidden;
        background: linear-gradient(
          90deg,
          rgba(255, 255, 255, 0.18) 0%,
          rgba(255, 255, 255, 0.72) 18%,
          rgba(255, 255, 255, 1) 50%,
          rgba(255, 255, 255, 0.72) 82%,
          rgba(255, 255, 255, 0.18) 100%
        );
        box-shadow:
          0 0 4px rgba(255, 255, 255, 0.34),
          0 0 10px rgba(255, 255, 255, 0.18);
        opacity: 0.78;
      }

      .portal-badge-edge-spine--left {
        transform: translate(-50%, -50%) rotateY(90deg) translateZ(2.3px);
      }

      .portal-badge-edge-spine--right {
        transform: translate(-50%, -50%) rotateY(-90deg) translateZ(2.3px);
      }

      .portal-badge-edge-spine--left,
      .portal-badge-edge-spine--right {
        animation: portalBadgeEdgeSpinePresence 3060ms linear both;
      }

      .portal-badge-unlock-energy-flare {
        background:
          radial-gradient(
            circle at 50% 50%,
            rgba(255, 255, 255, 0.22) 0%,
            rgba(255, 255, 255, 0.08) 26%,
            rgba(255, 255, 255, 0) 62%
          ),
          radial-gradient(
            circle at 50% 50%,
            rgba(255, 255, 255, 0.08) 0%,
            rgba(255, 255, 255, 0) 74%
          );
        animation: portalBadgeUnlockEnergyFlare 920ms
          cubic-bezier(0.18, 0.88, 0.24, 1) 1120ms both;
      }

      .portal-badge-core--locked {
        animation: portalBadgeLockedPulse 2400ms ease-in-out infinite;
        transform-origin: center;
        will-change: transform, opacity;
      }

      .portal-badge-idle-glow {
        animation: portalBadgeUnlockedIdleGlow 3200ms ease-in-out infinite;
        will-change: transform, opacity;
      }

      .portal-badge-inner-aura {
        animation: portalBadgeInnerAuraPulse 3600ms ease-in-out infinite;
        will-change: transform, opacity;
      }

      .portal-badge-centre-radiance {
        animation: portalBadgeCentreRadiance 3400ms ease-in-out infinite;
        will-change: transform, opacity;
      }

      .portal-badge-unlock-host:hover .portal-badge-idle-glow,
      .portal-badge-unlock-host:focus-within .portal-badge-idle-glow {
        opacity: 0.42;
        transform: scale(1.06);
      }

      .portal-badge-embers {
        opacity: 0.34;
        transition:
          opacity 180ms ease,
          transform 180ms ease;
      }

      .portal-badge-unlock-host:hover .portal-badge-embers,
      .portal-badge-unlock-host:focus-within .portal-badge-embers {
        opacity: 0.92;
        transform: translateY(-1px);
      }

      .portal-badge-spark-a,
      .portal-badge-spark-b,
      .portal-badge-spark-c,
      .portal-badge-burst-a,
      .portal-badge-burst-b,
      .portal-badge-burst-c {
        opacity: 0;
        transform-origin: 50% 100%;
        clip-path: polygon(
          50% 0%,
          64% 10%,
          72% 24%,
          76% 42%,
          70% 60%,
          60% 76%,
          52% 90%,
          50% 100%,
          48% 90%,
          40% 76%,
          30% 60%,
          24% 42%,
          28% 24%,
          36% 10%
        );
        background: linear-gradient(
          180deg,
          rgba(255, 255, 255, 0.98) 0%,
          rgba(255, 255, 255, 0.8) 16%,
          rgba(255, 255, 255, 0.28) 54%,
          rgba(255, 255, 255, 0.06) 82%,
          rgba(255, 255, 255, 0) 100%
        );
        filter: blur(0.22px) drop-shadow(0 0 6px rgba(255, 255, 255, 0.22))
          drop-shadow(0 0 12px rgba(255, 255, 255, 0.1));
        will-change: transform, opacity, filter;
      }

      .portal-badge-spark-a::before,
      .portal-badge-spark-b::before,
      .portal-badge-spark-c::before,
      .portal-badge-burst-a::before,
      .portal-badge-burst-b::before,
      .portal-badge-burst-c::before {
        content: "";
        position: absolute;
        left: 50%;
        top: 8%;
        width: 22%;
        height: 76%;
        transform: translateX(-50%);
        clip-path: polygon(
          50% 0%,
          72% 14%,
          64% 62%,
          50% 100%,
          36% 62%,
          28% 14%
        );
        background: linear-gradient(
          180deg,
          rgba(255, 255, 255, 1) 0%,
          rgba(255, 255, 255, 0.8) 34%,
          rgba(255, 255, 255, 0.08) 100%
        );
        filter: blur(0.35px);
      }

      .portal-badge-spark-a::after,
      .portal-badge-spark-b::after,
      .portal-badge-spark-c::after,
      .portal-badge-burst-a::after,
      .portal-badge-burst-b::after,
      .portal-badge-burst-c::after {
        content: "";
        position: absolute;
        left: 50%;
        top: 16%;
        width: 78%;
        height: 64%;
        transform: translateX(-50%);
        clip-path: polygon(
          50% 0%,
          70% 16%,
          78% 42%,
          66% 70%,
          50% 100%,
          34% 70%,
          22% 42%,
          30% 16%
        );
        background: radial-gradient(
          ellipse at 50% 24%,
          rgba(255, 255, 255, 0.56) 0%,
          rgba(255, 255, 255, 0.18) 42%,
          rgba(255, 255, 255, 0) 78%
        );
        filter: blur(0.9px);
        opacity: 0.76;
      }

      .portal-badge-spark-a {
        animation: portalBadgeEmberRiseA 1300ms
          cubic-bezier(0.22, 0.61, 0.36, 1) infinite;
      }

      .portal-badge-spark-b {
        animation: portalBadgeEmberRiseB 1600ms
          cubic-bezier(0.19, 0.72, 0.32, 1) infinite 160ms;
      }

      .portal-badge-spark-c {
        animation: portalBadgeEmberRiseC 1450ms cubic-bezier(0.25, 0.68, 0.3, 1)
          infinite 320ms;
      }

      .portal-badge-spark-a,
      .portal-badge-burst-a {
        transform-origin: 50% 100%;
      }

      .portal-badge-spark-b,
      .portal-badge-burst-b {
        transform-origin: 50% 100%;
      }

      .portal-badge-spark-c,
      .portal-badge-burst-c {
        transform-origin: 50% 100%;
      }

      .portal-badge-unlock-host:hover .portal-badge-spark-a,
      .portal-badge-unlock-host:focus-within .portal-badge-spark-a {
        animation: portalBadgeEmberBurstA 950ms cubic-bezier(0.2, 0.72, 0.28, 1)
          infinite;
      }

      .portal-badge-unlock-host:hover .portal-badge-spark-b,
      .portal-badge-unlock-host:focus-within .portal-badge-spark-b {
        animation: portalBadgeEmberBurstB 1100ms
          cubic-bezier(0.18, 0.75, 0.3, 1) infinite 120ms;
      }

      .portal-badge-unlock-host:hover .portal-badge-spark-c,
      .portal-badge-unlock-host:focus-within .portal-badge-spark-c {
        animation: portalBadgeEmberBurstC 1000ms cubic-bezier(0.24, 0.7, 0.3, 1)
          infinite 220ms;
      }

      .portal-badge-unlock-host:hover .portal-badge-burst-a,
      .portal-badge-unlock-host:focus-within .portal-badge-burst-a {
        animation: portalBadgeEmberBurstA 820ms cubic-bezier(0.2, 0.74, 0.28, 1)
          infinite 40ms;
      }

      .portal-badge-unlock-host:hover .portal-badge-burst-b,
      .portal-badge-unlock-host:focus-within .portal-badge-burst-b {
        animation: portalBadgeEmberBurstB 900ms cubic-bezier(0.18, 0.76, 0.3, 1)
          infinite 180ms;
      }

      .portal-badge-unlock-host:hover .portal-badge-burst-c,
      .portal-badge-unlock-host:focus-within .portal-badge-burst-c {
        animation: portalBadgeEmberBurstC 860ms cubic-bezier(0.22, 0.72, 0.3, 1)
          infinite 300ms;
      }

      .portal-badge-final-art-shell {
        position: absolute;
        inset: 0;
      }

      .portal-badge-final-art-image {
        position: absolute;
        inset: 0;
      }

      .portal-badge-final-shimmer {
        overflow: hidden;
      }

      .portal-badge-final-shimmer::before {
        content: "";
        position: absolute;
        inset: -42% -62%;
        background: linear-gradient(
          90deg,
          rgba(255, 255, 255, 0) 0%,
          rgba(255, 255, 255, 0.04) 34%,
          rgba(255, 255, 255, 0.18) 42%,
          rgba(255, 255, 255, 0.98) 49%,
          rgba(255, 255, 255, 0.5) 53%,
          rgba(255, 255, 255, 0.12) 60%,
          rgba(255, 255, 255, 0) 72%
        );
        filter: blur(5px);
        mix-blend-mode: screen;
        opacity: 0;
        transform: translate3d(-168%, 0, 0) rotate(24deg);
      }

      .portal-badge-final-shimmer--celebrating::before {
        animation: portalBadgeFinalShimmerCelebrating 3000ms
          cubic-bezier(0.22, 1, 0.36, 1) 1 both;
      }

      .portal-member-badge-meta-inner {
        position: relative;
        min-height: 0;
      }

      .portal-badge-unlock-host[data-badge-expanded="true"]
        .portal-member-badge-meta-inner {
        min-height: 28px;
      }

      .portal-member-badge-question-mark {
        position: relative;
        z-index: 1;
        display: flex;
        align-items: flex-start;
        justify-content: center;
        min-height: 0;
        will-change: transform, opacity, filter;
      }

      .portal-badge-unlock-host[data-badge-expanded="true"]
        .portal-member-badge-question-mark {
        min-height: 28px;
      }

      .portal-member-badge-question-mark--dissolving {
        position: absolute;
        inset: 0;
        z-index: 1;
        display: flex;
        align-items: flex-start;
        justify-content: center;
        animation: portalBadgeMetaQuestionDissolve 300ms
          cubic-bezier(0.32, 1, 0.68, 1) 2640ms both;
      }

      .portal-member-badge-meta-revealed {
        position: relative;
        z-index: 2;
      }

      .portal-member-badge-meta-revealed--revealing {
        position: absolute;
        inset: 0;
        z-index: 2;
      }

      .portal-member-badge-title,
      .portal-member-badge-description {
        will-change: transform, opacity, filter;
      }

      .portal-member-badge-title--revealing {
        opacity: 0;
        animation: portalBadgeMetaTitleReveal 420ms
          cubic-bezier(0.22, 1, 0.36, 1) 2720ms both;
      }

      .portal-member-badge-description--revealing {
        opacity: 0;
        animation: portalBadgeMetaDescriptionReveal 480ms
          cubic-bezier(0.22, 1, 0.36, 1) 2790ms both;
      }

      @media (prefers-reduced-motion: reduce) {
        .portal-badge-core--locked,
        .portal-badge-idle-glow,
        .portal-badge-inner-aura,
        .portal-badge-centre-radiance,
        .portal-badge-spark-a,
        .portal-badge-spark-b,
        .portal-badge-spark-c,
        .portal-badge-burst-a,
        .portal-badge-burst-b,
        .portal-badge-burst-c,
        .portal-badge-spin-stage-1--unlocking,
        .portal-badge-spin-stage-2--unlocking,
        .portal-badge-spin-stage-3--unlocking,
        .portal-badge-art-shell--unlocking,
        .portal-badge-reveal-canvas-layer--unlocking,
        .portal-badge-unlock-energy-flare,
        .portal-badge-impact-flash,
        .portal-badge-impact-particle,
        .portal-badge-final-shimmer::before {
          animation: none !important;
        }

        .portal-badge-embers {
          opacity: 0 !important;
        }

        .portal-badge-unlock-visual-inner {
          transition: none !important;
        }

        .portal-badge-reveal-canvas-layer {
          opacity: 1 !important;
          filter: none !important;
        }

        .portal-member-badge-question-mark,
        .portal-member-badge-title,
        .portal-member-badge-description {
          animation: none !important;
          opacity: unset !important;
          transform: none !important;
          filter: none !important;
        }
      }
    `}</style>
  );
}


----- app/home/modules/badges/badgeCabinetTypes.ts -----
// web/app/home/modules/badges/badgeCabinetTypes.ts
import type { MemberDashboardBadge } from "@/lib/memberDashboard";

export type BadgeCabinetInputBadge = MemberDashboardBadge;

export type BadgeCabinetItemModel = {
  key: string;
  label: string;
  description: string | null;
  imageUrl: string | null;
  unlocked: boolean;
  unlockedAt: string | null;
  editorialOrder: number;
  partition: "unlocked" | "locked";
  titleText: string;
  shareable: boolean;
  undisclosed: boolean;
};

----- app/home/modules/badges/useBadgeCabinetUnlockSequence.ts -----
// web/app/home/modules/badges/useBadgeCabinetUnlockSequence.ts
"use client";

import React from "react";
import { flushSync } from "react-dom";
import type { BadgeCabinetItemModel } from "./badgeCabinetTypes";

const BADGE_UNLOCK_TIMELINE_MS = {
  /**
   * Visual ceremony timing.
   *
   * Keep these aligned with BadgeUnlockVisualStyles.tsx.
   *
   * Current intent:
   * - spin 1 is heavy and slow
   * - colour reveal should become most legible at the end of spin 1 and into spin 2
   * - spin 2 accelerates
   * - spin 3 is fastest and resolves into the hard stop
   */
  spinStage1: 1480,
  spinStage2: 900,
  spinStage2Delay: 1480,
  spinStage3: 560,
  spinStage3Delay: 2380,
  shellSettleTotal: 3060,

  /**
   * Cabinet movement timing after the visual ceremony is complete.
   */
  cabinetFlip: 1320,
  cabinetSettle: 640,

  /**
   * Default non-unlock FLIP duration.
   */
  defaultFlip: 420,
} as const;

function getUnlockVisualDurationMs(): number {
  return Math.max(
    BADGE_UNLOCK_TIMELINE_MS.shellSettleTotal,
    BADGE_UNLOCK_TIMELINE_MS.spinStage1,
    BADGE_UNLOCK_TIMELINE_MS.spinStage2Delay +
      BADGE_UNLOCK_TIMELINE_MS.spinStage2,
    BADGE_UNLOCK_TIMELINE_MS.spinStage3Delay +
      BADGE_UNLOCK_TIMELINE_MS.spinStage3,
  );
}

const UNLOCK_VISUAL_DURATION_MS = getUnlockVisualDurationMs();

function sortBadgeCabinetItems(
  items: BadgeCabinetItemModel[],
): BadgeCabinetItemModel[] {
  const nextItems = [...items];

  nextItems.sort((a, b) => {
    if (a.partition !== b.partition) {
      return a.partition === "unlocked" ? -1 : 1;
    }

    if (a.editorialOrder !== b.editorialOrder) {
      return a.editorialOrder - b.editorialOrder;
    }

    return a.label.localeCompare(b.label);
  });

  return nextItems;
}

function buildStagedUnlockItems(
  items: BadgeCabinetItemModel[],
  previousStableItems: BadgeCabinetItemModel[],
  unlockKeys: Set<string>,
): BadgeCabinetItemModel[] {
  if (unlockKeys.size === 0) return items;

  const previousByKey = new Map(
    previousStableItems.map((item) => [item.key, item]),
  );

  const stagedItems = items.map((item) => {
    if (!unlockKeys.has(item.key)) return item;

    const previousItem = previousByKey.get(item.key);
    if (!previousItem) return item;

    return {
      ...item,
      partition: previousItem.partition,
    };
  });

  return sortBadgeCabinetItems(stagedItems);
}

type UnlockableBadgeItem = BadgeCabinetItemModel & {
  cabinetRevealPending?: boolean;
};

type Options = {
  items: UnlockableBadgeItem[];
};

type Result = {
  displayItems: UnlockableBadgeItem[];
  newlyUnlockedKeys: Set<string>;
  pendingUnlockKeys: Set<string>;
  unlockPhase: "idle" | "reveal" | "move";
  liveAnnouncement: string;
  isFlipSuspended: boolean;
  flipDurationMs: number;
  flipBaselineToken: number;
  resetUnlockSequence: () => void;
};

export function useBadgeCabinetUnlockSequence(options: Options): Result {
  const { items } = options;

  const previousUnlockedKeysRef = React.useRef<Set<string> | null>(null);
  const previousStableItemsRef = React.useRef<UnlockableBadgeItem[]>(items);

  const unlockReleaseTimeoutRef = React.useRef<number | null>(null);
  const unlockCleanupTimeoutRef = React.useRef<number | null>(null);
  const unlockReleaseRafRef = React.useRef<number | null>(null);
  const unlockReleasePaintRafRef = React.useRef<number | null>(null);

  const [newlyUnlockedKeys, setNewlyUnlockedKeys] = React.useState<Set<string>>(
    () => new Set(),
  );
  const [pendingUnlockKeys, setPendingUnlockKeys] = React.useState<Set<string>>(
    () => new Set(),
  );
  const [unlockPhase, setUnlockPhase] = React.useState<
    "idle" | "reveal" | "move"
  >("idle");
  const [displayItemsOverride, setDisplayItemsOverride] = React.useState<
    UnlockableBadgeItem[] | null
  >(null);
  const [isFlipSuspended, setIsFlipSuspended] = React.useState(false);
  const [flipDurationMs, setFlipDurationMs] = React.useState<number>(
    BADGE_UNLOCK_TIMELINE_MS.defaultFlip,
  );
  const [flipBaselineToken, setFlipBaselineToken] = React.useState(0);
  const [liveAnnouncement, setLiveAnnouncement] = React.useState("");

  const clearSequenceTimers = React.useCallback(() => {
    if (unlockReleaseTimeoutRef.current !== null) {
      window.clearTimeout(unlockReleaseTimeoutRef.current);
      unlockReleaseTimeoutRef.current = null;
    }

    if (unlockCleanupTimeoutRef.current !== null) {
      window.clearTimeout(unlockCleanupTimeoutRef.current);
      unlockCleanupTimeoutRef.current = null;
    }

    if (unlockReleaseRafRef.current !== null) {
      window.cancelAnimationFrame(unlockReleaseRafRef.current);
      unlockReleaseRafRef.current = null;
    }

    if (unlockReleasePaintRafRef.current !== null) {
      window.cancelAnimationFrame(unlockReleasePaintRafRef.current);
      unlockReleasePaintRafRef.current = null;
    }
  }, []);

  const resetUnlockSequence = React.useCallback(() => {
    clearSequenceTimers();
    setNewlyUnlockedKeys(new Set());
    setPendingUnlockKeys(new Set());
    setUnlockPhase("idle");
    setDisplayItemsOverride(null);
    setIsFlipSuspended(false);
    setFlipDurationMs(BADGE_UNLOCK_TIMELINE_MS.defaultFlip);
    setLiveAnnouncement("");
  }, [clearSequenceTimers]);

  React.useEffect(() => {
    return () => {
      clearSequenceTimers();
    };
  }, [clearSequenceTimers]);

  React.useEffect(() => {
    if (displayItemsOverride !== null) return;
    if (unlockPhase !== "idle") return;
    previousStableItemsRef.current = items;
  }, [displayItemsOverride, items, unlockPhase]);

  React.useLayoutEffect(() => {
    const nextUnlockedKeys = new Set(
      items.filter((item) => item.unlocked).map((item) => item.key),
    );

    const previousUnlockedKeys = previousUnlockedKeysRef.current;

    const freshUnlocks =
      previousUnlockedKeys === null
        ? items.filter((item) => item.unlocked && item.cabinetRevealPending)
        : items.filter(
            (item) => item.unlocked && !previousUnlockedKeys.has(item.key),
          );

    previousUnlockedKeysRef.current = nextUnlockedKeys;

    if (freshUnlocks.length === 0) {
      return;
    }

    clearSequenceTimers();

    const freshUnlockKeySet = new Set(freshUnlocks.map((item) => item.key));
    const stagedItems = buildStagedUnlockItems(
      items,
      previousStableItemsRef.current,
      freshUnlockKeySet,
    );

    setIsFlipSuspended(true);
    setDisplayItemsOverride(stagedItems);
    setNewlyUnlockedKeys(freshUnlockKeySet);
    setPendingUnlockKeys(freshUnlockKeySet);
    setUnlockPhase("reveal");
    setFlipDurationMs(BADGE_UNLOCK_TIMELINE_MS.cabinetFlip);
    setLiveAnnouncement(
      freshUnlocks
        .map((item) => `New badge unlocked: ${item.label}`)
        .join(". "),
    );

    if (process.env.NODE_ENV !== "production") {
      console.debug("[useBadgeCabinetUnlockSequence] unlock started", {
        freshUnlockKeys: Array.from(freshUnlockKeySet),
        unlockVisualDurationMs: UNLOCK_VISUAL_DURATION_MS,
        cabinetFlipMs: BADGE_UNLOCK_TIMELINE_MS.cabinetFlip,
        cabinetSettleMs: BADGE_UNLOCK_TIMELINE_MS.cabinetSettle,
        timeline: BADGE_UNLOCK_TIMELINE_MS,
      });
    }

    unlockReleaseTimeoutRef.current = window.setTimeout(() => {
      unlockReleaseTimeoutRef.current = null;

      if (process.env.NODE_ENV !== "production") {
        console.debug(
          "[useBadgeCabinetUnlockSequence] unlock released to cabinet move",
        );
      }

      flushSync(() => {
        setUnlockPhase("move");
        setPendingUnlockKeys(new Set());
        setIsFlipSuspended(false);
        setFlipBaselineToken((current) => current + 1);
      });

      unlockReleaseRafRef.current = window.requestAnimationFrame(() => {
        unlockReleaseRafRef.current = null;

        unlockReleasePaintRafRef.current = window.requestAnimationFrame(() => {
          unlockReleasePaintRafRef.current = null;
          setDisplayItemsOverride(null);
        });
      });
    }, UNLOCK_VISUAL_DURATION_MS);

    unlockCleanupTimeoutRef.current = window.setTimeout(
      () => {
        setNewlyUnlockedKeys(new Set());
        setUnlockPhase("idle");
        setDisplayItemsOverride(null);
        setIsFlipSuspended(false);
        setFlipDurationMs(BADGE_UNLOCK_TIMELINE_MS.defaultFlip);
        setLiveAnnouncement("");
        unlockCleanupTimeoutRef.current = null;
      },
      UNLOCK_VISUAL_DURATION_MS +
        BADGE_UNLOCK_TIMELINE_MS.cabinetFlip +
        BADGE_UNLOCK_TIMELINE_MS.cabinetSettle,
    );
  }, [clearSequenceTimers, items]);

  const displayItems = React.useMemo(() => {
    return displayItemsOverride ?? items;
  }, [displayItemsOverride, items]);

  return {
    displayItems,
    newlyUnlockedKeys,
    pendingUnlockKeys,
    unlockPhase,
    liveAnnouncement,
    isFlipSuspended,
    flipDurationMs,
    flipBaselineToken,
    resetUnlockSequence,
  };
}


----- lib/adminAuth.ts -----
// web/lib/adminAuth.ts
import "server-only";
import { auth } from "@clerk/nextjs/server";
import { sql } from "@vercel/postgres";
import { checkAccess } from "@/lib/access";
import { ENTITLEMENTS } from "@/lib/vocab";

async function getMemberIdByClerkUserId(
  userId: string,
): Promise<string | null> {
  if (!userId) return null;
  const r = await sql<{ id: string }>`
    select id
    from members
    where clerk_user_id = ${userId}
    limit 1
  `;
  return (r.rows?.[0]?.id as string | undefined) ?? null;
}

export async function requireAdminMemberId(): Promise<string> {
  const { userId } = await auth();
  if (!userId) throw new Error("Unauthorized");

  const memberId = await getMemberIdByClerkUserId(userId);
  if (!memberId) throw new Error("Unauthorized");

  const decision = await checkAccess(
    memberId,
    { kind: "global", required: [ENTITLEMENTS.ADMIN] },
    { log: false },
  );
  if (!decision.allowed) throw new Error("Forbidden");

  return memberId;
}


----- lib/albums.ts -----
// web/lib/albums.ts
import { unstable_cache } from "next/cache";
import { client } from "@/sanity/lib/client";
import { urlFor } from "@/sanity/lib/image";
import type {
  AlbumInfo,
  PlayerTrack,
  TierName,
  LyricCue,
  AlbumPlayerBundle,
} from "@/lib/types";
import {
  makeAlbumPlayerBundle,
  normalizeLyricCuesFromSanity,
} from "@/lib/types";
import { getRecordingPlayCountsByRecordingIds } from "@/lib/recordingListenTotals";

export type AlbumDocTrack = {
  recordingId: string;
  displayId?: string;
  title?: string;
  artist?: string;
  durationMs?: number;
  muxPlaybackId?: string;
  visualTheme?: string;
  explicit?: boolean;
};

type AlbumDoc = {
  _id?: string;
  catalogueId?: string | null;
  slug?: string | null;
  title?: string;
  displayTitle?: string | null;
  artist?: string;
  year?: number;
  description?: string;
  artwork?: unknown;
  visualTheme?: string;
  publicPageVisible?: boolean;
  releaseAt?: string;
  embargoNote?: string;
  earlyAccessEnabled?: boolean;
  earlyAccessTiers?: string[];
  minTierToLoad?: string;
  platformLinks?: Array<{
    platform?: string;
    url?: string;
  }>;
  tracks?: AlbumDocTrack[];
};

type TrackLyricsDoc = {
  recordingId?: string;
  offsetMs?: number;
  cues?: Array<{ _key?: string; tMs?: number; text?: string; endMs?: number }>;
};

export async function getFeaturedAlbumSlugFromSanity(): Promise<{
  slug: string | null;
  fallbackSlug: string | null;
}> {
  const res = await getCachedFeaturedAlbumSlugDoc();

  return {
    slug:
      typeof res?.slug === "string" && res.slug.trim() ? res.slug.trim() : null,
    fallbackSlug:
      typeof res?.fallbackSlug === "string" && res.fallbackSlug.trim()
        ? res.fallbackSlug.trim()
        : null,
  };
}

export type AlbumBrowseItem = {
  id: string;
  slug: string;
  catalogueId?: string | null;
  title: string;
  artist?: string;
  year?: number;
  artwork?: unknown;
  artworkUrl?: string | null;

  // raw fields from GROQ
  publicPageVisible?: boolean;
  minTierToLoad?: string;

  policy?: {
    publicPageVisible: boolean;
    minTierToLoad?: string | null;
  };
};

function normStr(v: unknown): string | undefined {
  if (typeof v !== "string") return undefined;
  const s = v.trim();
  return s.length ? s : undefined;
}

function normTheme(v: unknown): string | undefined {
  const s = normStr(v);
  return s && s !== "" ? s : undefined;
}

function parseTierName(v: unknown): TierName | null {
  const s = normStr(v);
  if (!s) return null;
  if (s === "friend" || s === "patron" || s === "partner") return s;
  return null;
}

function parseTierNameArray(v: unknown): TierName[] {
  if (!Array.isArray(v)) return [];
  return v.map(parseTierName).filter((x): x is TierName => x !== null);
}

function slugifySeg(v: string): string {
  // safe for URLs and stable-ish: lowercase, hyphens, strip junk
  return v
    .toLowerCase()
    .trim()
    .replace(/['"]/g, "")
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/-+/g, "-")
    .replace(/^-|-$/g, "");
}

function fallbackDisplayId(args: {
  title?: string;
  trackNo: number; // 1-based
}): string {
  const base = slugifySeg(args.title ?? "");
  // always produce something
  return base
    ? `${String(args.trackNo).padStart(2, "0")}-${base}`
    : `track-${String(args.trackNo).padStart(2, "0")}`;
}

function uniqifyDisplayId(desired: string, used: Set<string>): string {
  let d = desired;
  let i = 2;
  while (used.has(d)) {
    d = `${desired}-${i}`;
    i++;
  }
  used.add(d);
  return d;
}

export type NormalizedAlbumTrack = {
  recordingId: string;
  displayId: string;
  title?: string;
  artist?: string;
  muxPlaybackId?: string;
  durationMs?: number;
  visualTheme?: string;
  explicit?: boolean;
  trackNo: number;
};

export function normalizeAlbumTracks(
  tracks: AlbumDocTrack[] | null | undefined,
  args?: { albumTheme?: string | undefined },
): NormalizedAlbumTrack[] {
  const albumTheme = normTheme(args?.albumTheme);
  if (!Array.isArray(tracks)) return [];

  const used = new Set<string>();

  return tracks
    .filter((t) => typeof t?.recordingId === "string" && t.recordingId.trim())
    .map((t, idx) => {
      const rawDur = t.durationMs;
      const dur =
        typeof rawDur === "number" && Number.isFinite(rawDur)
          ? rawDur
          : undefined;

      const trackNo = idx + 1;
      const recordingId = t.recordingId.trim();

      const wanted =
        normStr(t.displayId) ?? fallbackDisplayId({ title: t.title, trackNo });

      const displayId = uniqifyDisplayId(wanted, used);
      const trackTheme = normTheme(t.visualTheme);

      return {
        recordingId,
        displayId,
        title: normStr(t.title),
        artist: normStr(t.artist),
        muxPlaybackId: normStr(t.muxPlaybackId),
        durationMs: typeof dur === "number" && dur > 0 ? dur : undefined,
        visualTheme: trackTheme ?? albumTheme,
        explicit: t.explicit === true,
        trackNo,
      };
    });
}

type FeaturedAlbumSlugDoc = {
  slug?: string | null;
  fallbackSlug?: string | null;
};

const STATIC_CATALOGUE_REVALIDATE_SECONDS = 60 * 60;

const featuredAlbumSlugQuery = `
  *[_type == "siteFlags"]
    | order(_updatedAt desc)[0]{
      "slug": featuredAlbum->slug.current,
      "fallbackSlug": featuredAlbumFallbackSlug
    }
`;

const albumBySlugQuery = `
  *[_type == "album" && slug.current == $slug][0]{
    _id,
    catalogueId,
    "slug": slug.current,
    title,
    displayTitle,
    artist,
    year,
    description,
    artwork,
    visualTheme,
    publicPageVisible,
    releaseAt,
    embargoNote,
    earlyAccessEnabled,
    earlyAccessTiers,
    minTierToLoad,
    platformLinks[]{
      platform,
      url
    },
    "tracks": tracks[]{
      recordingId,
      displayId,
      title,
      artist,
      durationMs,
      muxPlaybackId,
      visualTheme,
      explicit
    }
  }
`;

const lyricsByRecordingIdsQuery = `
  *[_type == "lyrics" && recordingId in $recordingIds]{
    recordingId,
    offsetMs,
    cues[]{ _key, tMs, text, endMs }
  }
`;

const albumBrowseQuery = `
  *[_type=="album"]|order(year desc, _createdAt desc){
    "id": _id,
    "catalogueId": catalogueId,
    "slug": slug.current,
    title,
    artist,
    year,
    artwork,
    publicPageVisible,
    minTierToLoad
  }
`;

const getCachedFeaturedAlbumSlugDoc = unstable_cache(
  async (): Promise<FeaturedAlbumSlugDoc | null> => {
    return client.fetch<FeaturedAlbumSlugDoc | null>(featuredAlbumSlugQuery);
  },
  ["featured-album-slug-v1"],
  {
    revalidate: STATIC_CATALOGUE_REVALIDATE_SECONDS,
    tags: ["siteFlags"],
  },
);

const getCachedAlbumDocumentBySlug = unstable_cache(
  async (slug: string): Promise<AlbumDoc | null> => {
    return client.fetch<AlbumDoc | null>(albumBySlugQuery, { slug });
  },
  ["album-document-by-slug-v1"],
  {
    revalidate: STATIC_CATALOGUE_REVALIDATE_SECONDS,
    tags: ["albums"],
  },
);

const getCachedLyricsByRecordingIds = unstable_cache(
  async (recordingIds: string[]): Promise<TrackLyricsDoc[]> => {
    if (recordingIds.length === 0) return [];

    const docs = await client.fetch<TrackLyricsDoc[]>(
      lyricsByRecordingIdsQuery,
      { recordingIds },
    );

    return Array.isArray(docs) ? docs : [];
  },
  ["album-lyrics-by-recording-ids-v1"],
  {
    revalidate: STATIC_CATALOGUE_REVALIDATE_SECONDS,
    tags: ["lyrics"],
  },
);

const getCachedAlbumBrowseDocs = unstable_cache(
  async (): Promise<AlbumBrowseItem[]> => {
    const docs = await client.fetch<AlbumBrowseItem[]>(albumBrowseQuery);
    return Array.isArray(docs) ? docs : [];
  },
  ["album-browse-list-v1"],
  {
    revalidate: STATIC_CATALOGUE_REVALIDATE_SECONDS,
    tags: ["albums"],
  },
);

export type AlbumPlaybackAssetVerification = {
  ok: boolean;
  albumId: string;
  playbackId: string;
  trackId: string | null;
  matchedRecordingId: string | null;
};

export type AlbumPlaybackSessionAsset = {
  recordingId: string;
  displayId: string;
  playbackId: string;
  durationMs?: number;
};

function normalizeAlbumScopeId(raw: string): string {
  let s = (raw ?? "").trim();
  while (s.startsWith("alb:")) s = s.slice(4);
  return s.trim();
}

export async function getAlbumPlaybackAssetsForSession(params: {
  albumId: string;
}): Promise<{
  ok: boolean;
  albumId: string;
  albumScopeId: string;
  tracks: AlbumPlaybackSessionAsset[];
}> {
  const albumId = normalizeAlbumScopeId(params.albumId);

  if (!albumId) {
    return {
      ok: false,
      albumId,
      albumScopeId: "",
      tracks: [],
    };
  }

  const q = `
    *[
      _type == "album" &&
      (
        _id == $albumId ||
        catalogueId == $albumId
      )
    ][0]{
      _id,
      catalogueId,
      "tracks": tracks[
        defined(recordingId) &&
        defined(displayId) &&
        defined(muxPlaybackId)
      ]{
        recordingId,
        displayId,
        muxPlaybackId,
        durationMs
      }
    }
  `;

  const doc = await client.fetch<{
    _id?: string;
    catalogueId?: string | null;
    tracks?: Array<{
      recordingId?: string;
      displayId?: string;
      muxPlaybackId?: string;
      durationMs?: number;
    }>;
  } | null>(q, { albumId }, { next: { tags: ["albums"] } });

  const resolvedAlbumId = normStr(doc?.catalogueId) ?? normStr(doc?._id) ?? "";
  const albumScopeId = resolvedAlbumId ? `alb:${resolvedAlbumId}` : "";

  const tracks: AlbumPlaybackSessionAsset[] = [];

  if (Array.isArray(doc?.tracks)) {
    for (const t of doc.tracks) {
      const recordingId = normStr(t.recordingId);
      const displayId = normStr(t.displayId);
      const playbackId = normStr(t.muxPlaybackId);

      if (!recordingId || !displayId || !playbackId) continue;

      const durationMs =
        typeof t.durationMs === "number" &&
        Number.isFinite(t.durationMs) &&
        t.durationMs > 0
          ? Math.floor(t.durationMs)
          : undefined;

      tracks.push({
        recordingId,
        displayId,
        playbackId,
        ...(typeof durationMs === "number" ? { durationMs } : {}),
      });
    }
  }

  return {
    ok: Boolean(resolvedAlbumId && tracks.length > 0),
    albumId: resolvedAlbumId,
    albumScopeId,
    tracks,
  };
}

export async function verifyAlbumPlaybackAsset(params: {
  albumId: string;
  playbackId: string;
  trackId?: string | null;
}): Promise<AlbumPlaybackAssetVerification> {
  const albumId = normalizeAlbumScopeId(params.albumId);
  const playbackId = normStr(params.playbackId) ?? "";
  const trackId = normStr(params.trackId) ?? null;

  if (!albumId || !playbackId) {
    return {
      ok: false,
      albumId,
      playbackId,
      trackId,
      matchedRecordingId: null,
    };
  }

  const q = `
    *[
      _type == "album" &&
      (
        _id == $albumId ||
        catalogueId == $albumId
      )
    ][0]{
      "matches": tracks[
        defined(muxPlaybackId) &&
        muxPlaybackId == $playbackId
      ]{
        recordingId,
        displayId,
        muxPlaybackId
      }
    }
  `;

  const doc = await client.fetch<{
    matches?: Array<{
      recordingId?: string;
      displayId?: string;
      muxPlaybackId?: string;
    }>;
  } | null>(q, { albumId, playbackId }, { next: { tags: ["albums"] } });

  const matches = Array.isArray(doc?.matches) ? doc.matches : [];

  const matched = matches.find((t) => {
    const recordingId = normStr(t.recordingId);
    const displayId = normStr(t.displayId);
    const muxPlaybackId = normStr(t.muxPlaybackId);

    if (muxPlaybackId !== playbackId) return false;
    if (!trackId) return true;

    return recordingId === trackId || displayId === trackId;
  });

  return {
    ok: Boolean(matched),
    albumId,
    playbackId,
    trackId,
    matchedRecordingId: normStr(matched?.recordingId) ?? null,
  };
}

export async function getAlbumBySlug(slug: string): Promise<AlbumPlayerBundle> {
  const albumSlug = (slug ?? "").trim().toLowerCase();

  if (!albumSlug) {
    return makeAlbumPlayerBundle({
      albumSlug: "",
      album: null,
      tracks: [],
      albumLyrics: null,
    });
  }

  const doc = await getCachedAlbumDocumentBySlug(albumSlug);

  if (!doc?._id) {
    return makeAlbumPlayerBundle({
      albumSlug,
      album: null,
      tracks: [],
      albumLyrics: null,
    });
  }

  const albumCatalogueId = normStr(doc.catalogueId) ?? undefined;
  const albumTheme = normTheme(doc.visualTheme);

  // This remains outside the long-lived catalogue cache. An album must become
  // non-embargoed as its release timestamp passes, without a fresh publish.
  const releaseAt = doc.releaseAt ?? null;
  const releaseAtMs = releaseAt ? Date.parse(releaseAt) : NaN;
  const isEmbargoedByDate = Boolean(
    releaseAt && Number.isFinite(releaseAtMs) && releaseAtMs > Date.now(),
  );

  const embargoNote = normStr(doc.embargoNote) ?? null;

  const album: AlbumInfo = {
    id: doc._id,
    catalogueId: albumCatalogueId,
    title: doc.title ?? "Untitled",
    artist: normStr(doc.artist),
    year:
      typeof doc.year === "number" && Number.isFinite(doc.year)
        ? doc.year
        : undefined,
    description: normStr(doc.description),
    artworkUrl: doc.artwork
      ? urlFor(doc.artwork).width(900).height(900).quality(85).url()
      : null,
    platformLinks: Array.isArray(doc.platformLinks)
      ? doc.platformLinks
          .filter(
            (p): p is { platform: string; url: string } =>
              typeof p?.platform === "string" && typeof p?.url === "string",
          )
          .map((p) => ({
            platform: p.platform,
            url: p.url,
          }))
      : [],
    policy: {
      publicPageVisible: doc.publicPageVisible !== false,
      releaseAt: doc.releaseAt ?? null,
      earlyAccessEnabled: !!doc.earlyAccessEnabled,
      earlyAccessTiers: parseTierNameArray(doc.earlyAccessTiers),
      minTierToLoad: parseTierName(doc.minTierToLoad),
    },
    embargo: {
      embargoed: isEmbargoedByDate,
      releaseAt,
      note: embargoNote,
    },
  };

  const tracks: PlayerTrack[] = normalizeAlbumTracks(doc.tracks, {
    albumTheme,
  }).map((t) => ({
    recordingId: t.recordingId,
    displayId: t.displayId,
    title: t.title,
    artist: t.artist,
    muxPlaybackId: t.muxPlaybackId,
    durationMs: t.durationMs,
    visualTheme: t.visualTheme,
    explicit: t.explicit,
  }));

  const recordingIds = tracks
    .map((t) => t.recordingId)
    .filter((recordingId): recordingId is string => recordingId.length > 0);

  const lyricRecordingIds = Array.from(new Set(recordingIds)).sort((a, b) =>
    a.localeCompare(b),
  );

  const [playCountsByRecordingId, lyricDocs] = await Promise.all([
    getRecordingPlayCountsByRecordingIds(recordingIds),
    getCachedLyricsByRecordingIds(lyricRecordingIds),
  ]);

  const tracksWithPlayCounts: PlayerTrack[] = tracks.map((t) => ({
    ...t,
    playCount: playCountsByRecordingId[t.recordingId] ?? 0,
  }));

  const cuesByRecordingId: Record<string, LyricCue[]> = {};
  const offsetByRecordingId: Record<string, number> = {};

  for (const d of lyricDocs) {
    const id = (d.recordingId ?? "").trim();
    if (!id) continue;

    cuesByRecordingId[id] = normalizeLyricCuesFromSanity(d.cues);
    offsetByRecordingId[id] =
      typeof d.offsetMs === "number" && Number.isFinite(d.offsetMs)
        ? Math.floor(d.offsetMs)
        : 0;
  }

  return makeAlbumPlayerBundle({
    albumSlug: normStr(doc.slug) ?? albumSlug,
    album,
    tracks: tracksWithPlayCounts,
    albumLyrics: { cuesByRecordingId, offsetByRecordingId },
  });
}

export type AlbumCanonicalMetadata = {
  title: string | null;
  displayTitle: string | null;
  slug: string | null;
};

export type TrackCanonicalMetadata = {
  albumTitle: string | null;
  albumDisplayTitle: string | null;
  albumSlug: string | null;
  trackTitle: string | null;
  displayId: string | null;
};

export async function getAlbumCanonicalMetadataBySlug(
  slug: string,
): Promise<AlbumCanonicalMetadata | null> {
  const albumSlug = (slug ?? "").trim().toLowerCase();
  if (!albumSlug) return null;

  const doc = await getCachedAlbumDocumentBySlug(albumSlug);
  if (!doc?._id) return null;

  return {
    title: normStr(doc.title) ?? null,
    displayTitle: normStr(doc.displayTitle) ?? null,
    slug: normStr(doc.slug) ?? null,
  };
}

export async function getTrackCanonicalMetadataBySlugAndDisplayId(params: {
  slug: string;
  displayId: string;
}): Promise<TrackCanonicalMetadata | null> {
  const albumSlug = (params.slug ?? "").trim().toLowerCase();
  const requestedDisplayId = (params.displayId ?? "").trim();

  if (!albumSlug || !requestedDisplayId) return null;

  const doc = await getCachedAlbumDocumentBySlug(albumSlug);
  if (!doc?._id) return null;

  const matchingTrack =
    doc.tracks?.find(
      (track) => normStr(track.displayId) === requestedDisplayId,
    ) ?? null;

  return {
    albumTitle: normStr(doc.title) ?? null,
    albumDisplayTitle: normStr(doc.displayTitle) ?? null,
    albumSlug: normStr(doc.slug) ?? null,
    trackTitle: normStr(matchingTrack?.title) ?? null,
    displayId: normStr(matchingTrack?.displayId) ?? null,
  };
}

export async function listAlbumsForBrowse(): Promise<AlbumBrowseItem[]> {
  const items = await getCachedAlbumBrowseDocs();
  return items.map((a) => ({
    ...a,
    catalogueId: normStr(a.catalogueId) ?? null,
    artist: normStr(a.artist),
    title: a.title ?? "Untitled",
    artworkUrl: a.artwork
      ? urlFor(a.artwork).width(600).height(600).quality(80).url()
      : null,
    policy: {
      publicPageVisible: a.publicPageVisible !== false,
      minTierToLoad: parseTierName(a.minTierToLoad),
    },
  }));
}

export type RecordingSearchResult = {
  recordingId: string;
  title: string;
  artist?: string | null;
  albumSlug?: string | null;
  albumTitle?: string | null;
};

export async function searchRecordingsForAdmin(args: {
  query: string;
  limit?: number;
}): Promise<RecordingSearchResult[]> {
  const rawQuery = normStr(args.query);
  if (!rawQuery) return [];

  const limit = Math.max(1, Math.min(25, Math.floor(args.limit ?? 12)));

  const wildcard = `*${rawQuery}*`;
  const exactId = rawQuery;

  const q = `
    *[
      _type == "album" &&
      count(
        tracks[
          defined(recordingId) && (
            recordingId == $exactId ||
            recordingId match $wildcard ||
            title match $wildcard ||
            artist match $wildcard
          )
        ]
      ) > 0
    ] | order(title asc) {
      "albumSlug": slug.current,
      "albumTitle": title,
      "albumArtist": artist,
      "matches": tracks[
        defined(recordingId) && (
          recordingId == $exactId ||
          recordingId match $wildcard ||
          title match $wildcard ||
          artist match $wildcard
        )
      ]{
        recordingId,
        title,
        artist
      }
    }
  `;

  const docs = await client.fetch<
    Array<{
      albumSlug?: string;
      albumTitle?: string;
      albumArtist?: string;
      matches?: Array<{
        recordingId?: string;
        title?: string;
        artist?: string;
      }>;
    }>
  >(q, {
    exactId,
    wildcard,
  });

  const flattened: RecordingSearchResult[] = [];
  const seen = new Set<string>();

  for (const doc of Array.isArray(docs) ? docs : []) {
    const albumSlug = normStr(doc?.albumSlug) ?? null;
    const albumTitle = normStr(doc?.albumTitle) ?? null;
    const albumArtist = normStr(doc?.albumArtist) ?? null;

    for (const match of Array.isArray(doc?.matches) ? doc.matches : []) {
      const recordingId = normStr(match?.recordingId);
      const title = normStr(match?.title);

      if (!recordingId || !title || seen.has(recordingId)) continue;
      seen.add(recordingId);

      flattened.push({
        recordingId,
        title,
        artist: normStr(match?.artist) ?? albumArtist,
        albumSlug,
        albumTitle,
      });
    }
  }

  const normalizedQuery = rawQuery.toLowerCase();

  flattened.sort((a, b) => {
    const aId = a.recordingId.toLowerCase();
    const bId = b.recordingId.toLowerCase();
    const aTitle = a.title.toLowerCase();
    const bTitle = b.title.toLowerCase();

    const aExact = aId === normalizedQuery ? 1 : 0;
    const bExact = bId === normalizedQuery ? 1 : 0;
    if (aExact !== bExact) return bExact - aExact;

    const aTitleStarts = aTitle.startsWith(normalizedQuery) ? 1 : 0;
    const bTitleStarts = bTitle.startsWith(normalizedQuery) ? 1 : 0;
    if (aTitleStarts !== bTitleStarts) return bTitleStarts - aTitleStarts;

    const aTitleIncludes = aTitle.includes(normalizedQuery) ? 1 : 0;
    const bTitleIncludes = bTitle.includes(normalizedQuery) ? 1 : 0;
    if (aTitleIncludes !== bTitleIncludes) {
      return bTitleIncludes - aTitleIncludes;
    }

    return a.title.localeCompare(b.title);
  });

  return flattened.slice(0, limit);
}

export async function getRecordingSummaryByRecordingId(
  recordingId: string,
): Promise<RecordingSummary | null> {
  const id = normStr(recordingId);
  if (!id) return null;

  const q = `
        *[_type == "album" && count(tracks[recordingId == $recordingId]) > 0][0]{
      "albumCatalogueId": catalogueId,
      "albumSlug": slug.current,
      "albumTitle": title,
      "albumArtist": artist,
      "track": tracks[recordingId == $recordingId][0]{
        recordingId,
        title,
        artist
      }
    }
  `;

  const doc = await client.fetch<{
    albumCatalogueId?: string;
    albumSlug?: string;
    albumTitle?: string;
    albumArtist?: string;
    track?: {
      recordingId?: string;
      title?: string;
      artist?: string;
    };
  } | null>(q, { recordingId: id });

  const trackRecordingId = normStr(doc?.track?.recordingId);
  const trackTitle = normStr(doc?.track?.title);
  if (!trackRecordingId || !trackTitle) return null;

  return {
    recordingId: trackRecordingId,
    title: trackTitle,
    artist: normStr(doc?.track?.artist) ?? normStr(doc?.albumArtist) ?? null,
    albumCatalogueId: normStr(doc?.albumCatalogueId) ?? null,
    albumSlug: normStr(doc?.albumSlug) ?? null,
    albumTitle: normStr(doc?.albumTitle) ?? null,
  };
}

export type RecordingSummary = {
  recordingId: string;
  title: string;
  artist?: string | null;
  albumCatalogueId?: string | null;
  albumSlug?: string | null;
  albumTitle?: string | null;
};

export type AlbumEmailMeta = {
  slug: string;
  title: string;
  artist?: string;
  artworkUrl?: string | null;
};

export async function getAlbumEmailMetaBySlug(
  slug: string,
): Promise<AlbumEmailMeta | null> {
  const s = (slug ?? "").trim().toLowerCase();
  if (!s) return null;

  const q = `
    *[_type == "album" && slug.current == $slug][0]{
      "slug": slug.current,
      title,
      artist,
      artwork
    }
  `;

  const doc = await client.fetch<{
    slug?: string;
    title?: string;
    artist?: string;
    artwork?: unknown;
  } | null>(q, { slug: s });
  if (!doc?.slug) return null;

  return {
    slug: doc.slug,
    title: doc.title ?? "Untitled",
    artist: normStr(doc.artist),
    artworkUrl: doc.artwork
      ? urlFor(doc.artwork).width(900).height(900).quality(85).url()
      : null,
  };
}


----- lib/badgeAutoAward.ts -----
// web/lib/badgeAutoAward.ts
import "server-only";

import { sql } from "@vercel/postgres";
import {
  getAutoAwardBadgeDefinitions,
  type BadgeDefinition,
} from "@/lib/badges";
import { areBadgesEnabled } from "@/lib/badgeFeature";
import { grantEntitlement } from "@/lib/entitlementOps";

export type AutoBadgeTriggerKind =
  | "playback_aggregate_updated"
  | "exegesis_contribution_created"
  | "exegesis_vote_updated"
  | "public_name_unlocked";

export type NewlyAwardedBadge = {
  entitlementKey: string;
  title: string;
  description: string | null;
  imageUrl: string | null;
  shareable: boolean;
  unlockedAt: string;
};

export type RunAutoBadgeAwardsForMemberInput = {
  memberId: string;
  trigger: AutoBadgeTriggerKind;
  recordingId?: string | null;
  grantedBy?: string;
  correlationId?: string | null;
};

function asNumber(value: string | number | null | undefined): number {
  if (typeof value === "number") {
    return Number.isFinite(value) ? value : 0;
  }

  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    return Number.isFinite(parsed) ? parsed : 0;
  }

  return 0;
}

type PlaybackTelemetryBadgeEvent = "play" | "progress" | "complete";

type PlaybackAggregateSnapshot = {
  member: {
    listenedMs: number;
    playCount: number;
    completedCount: number;
  };
  recording: {
    listenedMs: number;
    playCount: number;
    completedCount: number;
  };
};

type PlaybackAggregateSnapshotRow = {
  member_listened_ms: string | number | null;
  member_play_count: string | number | null;
  member_completed_count: string | number | null;
  recording_listened_ms: string | number | null;
  recording_play_count: string | number | null;
  recording_completed_count: string | number | null;
};

function asNonNegativeInt(value: string | number | null | undefined): number {
  return Math.max(0, Math.floor(asNumber(value)));
}

function isPlaybackTelemetryBadgeRelevant(
  badge: BadgeDefinition,
  event: PlaybackTelemetryBadgeEvent,
  recordingId: string,
): boolean {
  const autoAward = badge.autoAward;
  const mode = autoAward?.qualificationMode;

  if (!autoAward || !mode) return false;

  switch (mode) {
    case "minutes_streamed":
      return event === "progress";

    case "play_count":
      return event === "play";

    case "complete_count":
      return event === "complete";

    case "recording_minutes_streamed":
      return (
        event === "progress" && autoAward.recordingId?.trim() === recordingId
      );

    case "recording_play_count":
      return event === "play" && autoAward.recordingId?.trim() === recordingId;

    case "recording_complete_count":
      return (
        event === "complete" && autoAward.recordingId?.trim() === recordingId
      );

    default:
      return false;
  }
}

function qualifiesPlaybackBadgeFromSnapshot(
  badge: BadgeDefinition,
  snapshot: PlaybackAggregateSnapshot,
): boolean {
  const autoAward = badge.autoAward;
  const mode = autoAward?.qualificationMode;

  if (!autoAward || !mode) return false;

  switch (mode) {
    case "minutes_streamed": {
      const minimumMs = Math.max(
        0,
        Math.floor(autoAward.minMinutes ?? 0) * 60_000,
      );

      return snapshot.member.listenedMs >= minimumMs;
    }

    case "play_count": {
      const minimum = Math.max(0, Math.floor(autoAward.minPlayCount ?? 0));

      return snapshot.member.playCount >= minimum;
    }

    case "complete_count": {
      const minimum = Math.max(0, Math.floor(autoAward.minCompletedCount ?? 0));

      return snapshot.member.completedCount >= minimum;
    }

    case "recording_minutes_streamed": {
      const minimumMs = Math.max(
        0,
        Math.floor(autoAward.minMinutes ?? 0) * 60_000,
      );

      return snapshot.recording.listenedMs >= minimumMs;
    }

    case "recording_play_count": {
      const minimum = Math.max(0, Math.floor(autoAward.minPlayCount ?? 0));

      return snapshot.recording.playCount >= minimum;
    }

    case "recording_complete_count": {
      const minimum = Math.max(0, Math.floor(autoAward.minCompletedCount ?? 0));

      return snapshot.recording.completedCount >= minimum;
    }

    default:
      return false;
  }
}

async function readPlaybackAggregateSnapshot(params: {
  memberId: string;
  recordingId: string;
}): Promise<PlaybackAggregateSnapshot> {
  const result = await sql<PlaybackAggregateSnapshotRow>`
    select
      coalesce(
        (
          select listened_ms
          from member_listen_totals
          where member_id = ${params.memberId}::uuid
          limit 1
        ),
        0
      ) as member_listened_ms,
      coalesce(
        (
          select play_count
          from member_listen_totals
          where member_id = ${params.memberId}::uuid
          limit 1
        ),
        0
      ) as member_play_count,
      coalesce(
        (
          select completed_count
          from member_listen_totals
          where member_id = ${params.memberId}::uuid
          limit 1
        ),
        0
      ) as member_completed_count,
      coalesce(
        (
          select listened_ms
          from member_track_listen_stats
          where member_id = ${params.memberId}::uuid
            and recording_id = ${params.recordingId}
          limit 1
        ),
        0
      ) as recording_listened_ms,
      coalesce(
        (
          select play_count
          from member_track_listen_stats
          where member_id = ${params.memberId}::uuid
            and recording_id = ${params.recordingId}
          limit 1
        ),
        0
      ) as recording_play_count,
      coalesce(
        (
          select completed_count
          from member_track_listen_stats
          where member_id = ${params.memberId}::uuid
            and recording_id = ${params.recordingId}
          limit 1
        ),
        0
      ) as recording_completed_count
  `;

  const row = result.rows[0];

  return {
    member: {
      listenedMs: asNonNegativeInt(row?.member_listened_ms),
      playCount: asNonNegativeInt(row?.member_play_count),
      completedCount: asNonNegativeInt(row?.member_completed_count),
    },
    recording: {
      listenedMs: asNonNegativeInt(row?.recording_listened_ms),
      playCount: asNonNegativeInt(row?.recording_play_count),
      completedCount: asNonNegativeInt(row?.recording_completed_count),
    },
  };
}

export async function runPlaybackAutoBadgeAwardsForMember(input: {
  memberId: string;
  recordingId: string;
  event: PlaybackTelemetryBadgeEvent;
  grantedBy?: string;
  correlationId?: string | null;
}): Promise<NewlyAwardedBadge[]> {
  if (!areBadgesEnabled()) {
    return [];
  }

  const recordingId = input.recordingId.trim();
  if (!recordingId) return [];

  const allAutoBadges = await getAutoAwardBadgeDefinitions();

  const relevantBadges = allAutoBadges.filter((badge) =>
    isPlaybackTelemetryBadgeRelevant(badge, input.event, recordingId),
  );

  if (relevantBadges.length === 0) {
    return [];
  }

  const snapshot = await readPlaybackAggregateSnapshot({
    memberId: input.memberId,
    recordingId,
  });

  const newlyAwarded: NewlyAwardedBadge[] = [];

  for (const badge of relevantBadges) {
    if (!qualifiesPlaybackBadgeFromSnapshot(badge, snapshot)) {
      continue;
    }

    const grantResult = await grantEntitlement({
      memberId: input.memberId,
      entitlementKey: badge.entitlementKey,
      scopeId: null,
      grantedBy: input.grantedBy ?? "system",
      grantReason: "Automatic badge award via playback_aggregate_updated.",
      grantSource: "badge_auto_rule",
      grantSourceRef: `${badge.entitlementKey}:playback_aggregate_updated`,
      correlationId: input.correlationId ?? null,
    });

    if (grantResult.status !== "inserted") {
      continue;
    }

    newlyAwarded.push({
      entitlementKey: badge.entitlementKey,
      title: badge.title,
      description: badge.description,
      imageUrl: badge.imageUrl,
      shareable: badge.shareable,
      unlockedAt: new Date().toISOString(),
    });
  }

  return newlyAwarded;
}

function isTriggerRelevant(
  badge: BadgeDefinition,
  trigger: AutoBadgeTriggerKind,
  recordingId?: string | null,
): boolean {
  const mode = badge.autoAward?.qualificationMode;

  if (!mode) return false;

  switch (mode) {
    case "minutes_streamed":
    case "play_count":
    case "complete_count":
      return trigger === "playback_aggregate_updated";

    case "recording_minutes_streamed":
    case "recording_play_count":
    case "recording_complete_count":
      return (
        trigger === "playback_aggregate_updated" &&
        typeof recordingId === "string" &&
        recordingId.trim().length > 0 &&
        badge.autoAward?.recordingId === recordingId.trim()
      );

    case "exegesis_contribution_count":
      return trigger === "exegesis_contribution_created";

    case "exegesis_vote_tally":
      return trigger === "exegesis_vote_updated";

    case "public_name_unlocked":
      return (
        trigger === "public_name_unlocked" ||
        trigger === "exegesis_contribution_created"
      );

    default:
      return false;
  }
}

async function qualifiesForBadge(
  badge: BadgeDefinition,
  memberId: string,
): Promise<boolean> {
  const autoAward = badge.autoAward;
  const mode = autoAward?.qualificationMode;

  if (!autoAward || !mode) return false;

  switch (mode) {
    case "minutes_streamed": {
      const minMinutes = Math.max(0, Math.floor(autoAward.minMinutes ?? 0));
      const minListenedMs = minMinutes * 60_000;

      const res = await sql<{ count: string | number }>`
        select count(*) as count
        from member_listen_totals
        where member_id = ${memberId}::uuid
          and listened_ms >= ${minListenedMs}
      `;

      return asNumber(res.rows[0]?.count) > 0;
    }

    case "play_count": {
      const minPlayCount = Math.max(0, Math.floor(autoAward.minPlayCount ?? 0));

      const res = await sql<{ count: string | number }>`
        select count(*) as count
        from member_listen_totals
        where member_id = ${memberId}::uuid
          and play_count >= ${minPlayCount}
      `;

      return asNumber(res.rows[0]?.count) > 0;
    }

    case "complete_count": {
      const minCompletedCount = Math.max(
        0,
        Math.floor(autoAward.minCompletedCount ?? 0),
      );

      const res = await sql<{ count: string | number }>`
        select count(*) as count
        from member_listen_totals
        where member_id = ${memberId}::uuid
          and completed_count >= ${minCompletedCount}
      `;

      return asNumber(res.rows[0]?.count) > 0;
    }

    case "recording_minutes_streamed": {
      const recordingId = autoAward.recordingId?.trim() ?? "";
      const minMinutes = Math.max(0, Math.floor(autoAward.minMinutes ?? 0));
      const minListenedMs = minMinutes * 60_000;

      if (!recordingId) return false;

      const res = await sql<{ count: string | number }>`
        select count(*) as count
        from member_track_listen_stats
        where member_id = ${memberId}::uuid
          and recording_id = ${recordingId}
          and listened_ms >= ${minListenedMs}
      `;

      return asNumber(res.rows[0]?.count) > 0;
    }

    case "recording_play_count": {
      const recordingId = autoAward.recordingId?.trim() ?? "";
      const minPlayCount = Math.max(0, Math.floor(autoAward.minPlayCount ?? 0));

      if (!recordingId) return false;

      const res = await sql<{ count: string | number }>`
        select count(*) as count
        from member_track_listen_stats
        where member_id = ${memberId}::uuid
          and recording_id = ${recordingId}
          and play_count >= ${minPlayCount}
      `;

      return asNumber(res.rows[0]?.count) > 0;
    }

    case "recording_complete_count": {
      const recordingId = autoAward.recordingId?.trim() ?? "";
      const minCompletedCount = Math.max(
        0,
        Math.floor(autoAward.minCompletedCount ?? 0),
      );

      if (!recordingId) return false;

      const res = await sql<{ count: string | number }>`
        select count(*) as count
        from member_track_listen_stats
        where member_id = ${memberId}::uuid
          and recording_id = ${recordingId}
          and completed_count >= ${minCompletedCount}
      `;

      return asNumber(res.rows[0]?.count) > 0;
    }

    case "exegesis_contribution_count": {
      const minContributionCount = Math.max(
        0,
        Math.floor(autoAward.minContributionCount ?? 0),
      );

      const res = await sql<{ count: string | number }>`
        select count(*) as count
        from exegesis_identity
        where member_id = ${memberId}::uuid
          and contribution_count >= ${minContributionCount}
      `;

      return asNumber(res.rows[0]?.count) > 0;
    }

    case "exegesis_vote_tally": {
      const minVoteCount = Math.max(0, Math.floor(autoAward.minVoteCount ?? 0));

      const res = await sql<{ vote_count: string | number }>`
        select coalesce(sum(vote_count), 0) as vote_count
        from exegesis_comment
        where created_by_member_id = ${memberId}::uuid
          and status = 'live'
      `;

      return asNumber(res.rows[0]?.vote_count) >= minVoteCount;
    }

    case "public_name_unlocked": {
      const res = await sql<{ count: string | number }>`
        select count(*) as count
        from exegesis_identity
        where member_id = ${memberId}::uuid
          and public_name_unlocked_at is not null
      `;

      return asNumber(res.rows[0]?.count) > 0;
    }

    case "joined_within_window":
    case "active_within_window":
      return false;
  }
}

export async function runAutoBadgeAwardsForMember(
  input: RunAutoBadgeAwardsForMemberInput,
): Promise<NewlyAwardedBadge[]> {
  if (!areBadgesEnabled()) {
    return [];
  }

  const allAutoBadges = await getAutoAwardBadgeDefinitions();

  const relevantBadges = allAutoBadges.filter((badge) =>
    isTriggerRelevant(badge, input.trigger, input.recordingId),
  );

  if (relevantBadges.length === 0) {
    return [];
  }

  const newlyAwarded: NewlyAwardedBadge[] = [];

  for (const badge of relevantBadges) {
    const qualifies = await qualifiesForBadge(badge, input.memberId);

    if (!qualifies) {
      continue;
    }

    const grantResult = await grantEntitlement({
      memberId: input.memberId,
      entitlementKey: badge.entitlementKey,
      scopeId: null,
      grantedBy: input.grantedBy ?? "system",
      grantReason: `Automatic badge award via ${input.trigger}.`,
      grantSource: "badge_auto_rule",
      grantSourceRef: `${badge.entitlementKey}:${input.trigger}`,
      correlationId: input.correlationId ?? null,
    });

    if (grantResult.status !== "inserted") {
      continue;
    }

    newlyAwarded.push({
      entitlementKey: badge.entitlementKey,
      title: badge.title,
      description: badge.description,
      imageUrl: badge.imageUrl,
      shareable: badge.shareable,
      unlockedAt: new Date().toISOString(),
    });
  }

  return newlyAwarded;
}


----- lib/badgeAwardAnnouncementServer.ts -----
// web/lib/badgeAwardAnnouncementServer.ts
import "server-only";

import { sql } from "@vercel/postgres";
import type { NewlyAwardedBadge } from "@/lib/badgeAutoAward";
import { areBadgesEnabled } from "@/lib/badgeFeature";

function normalizeEntitlementKeys(badges: NewlyAwardedBadge[]): string[] {
  const unique = new Set<string>();

  for (const badge of badges) {
    const entitlementKey =
      "entitlementKey" in badge && typeof badge.entitlementKey === "string"
        ? badge.entitlementKey.trim()
        : "";

    if (!entitlementKey) continue;
    unique.add(entitlementKey);
  }

  return Array.from(unique);
}

export async function markOverlayAnnouncedForAwardedBadges(params: {
  memberId: string;
  badges: NewlyAwardedBadge[];
}): Promise<void> {
  if (!areBadgesEnabled()) {
    return;
  }

  const { memberId, badges } = params;

  const entitlementKeys = normalizeEntitlementKeys(badges);
  if (entitlementKeys.length === 0) return;

  const entitlementKeysJson = JSON.stringify(entitlementKeys);

  await sql`
    with awarded_keys as (
      select jsonb_array_elements_text(${entitlementKeysJson}::jsonb) as entitlement_key
    )
    update entitlement_grants eg
    set overlay_announced_at = now()
    from awarded_keys ak
    where eg.member_id = ${memberId}::uuid
      and eg.entitlement_key = ak.entitlement_key
      and eg.scope_id is null
      and eg.revoked_at is null
      and (eg.expires_at is null or eg.expires_at > now())
      and eg.overlay_announced_at is null
  `;
}


----- lib/badgeFeature.ts -----
// web/lib/badgeFeature.ts
import "server-only";

/**
 * Badges are intentionally opt-in. An absent or malformed environment value
 * leaves the full badge system inactive.
 */
export function areBadgesEnabled(): boolean {
  return (process.env.BADGES_ENABLED ?? "").trim().toLowerCase() === "true";
}

----- lib/badgePreviewModes.ts -----
// web/lib/badgePreviewModes.ts
export type BadgeQualificationMode =
  | "minutes_streamed"
  | "play_count"
  | "complete_count"
  | "joined_within_window"
  | "active_within_window"
  | "recording_minutes_streamed"
  | "recording_play_count"
  | "recording_complete_count"
  | "exegesis_contribution_count"
  | "exegesis_vote_tally"
  | "public_name_unlocked";

export type BadgePreviewModeDescriptor = {
  key: BadgeQualificationMode;
  label: string;
  description: string;
  requiresRecording: boolean;
  supportsDateWindow: boolean;
  metricFamily:
    | "minutes"
    | "plays"
    | "completes"
    | "membership"
    | "activity"
    | "exegesis"
    | "identity";
  inputRequirements: {
    minMinutes: boolean;
    minPlayCount: boolean;
    minCompletedCount: boolean;
    minProgressCount: boolean;
    minContributionCount: boolean;
    minVoteCount: boolean;
    joinedWindow: boolean;
    activeWindow: boolean;
    recordingId: boolean;
  };
  fieldText: {
    minMinutesLabel: string;
    minMinutesHelp: string | null;
    minPlayCountLabel: string;
    minPlayCountHelp: string | null;
    minCompletedCountLabel: string;
    minCompletedCountHelp: string | null;
    minProgressCountLabel: string;
    minProgressCountHelp: string | null;
    minContributionCountLabel: string;
    minContributionCountHelp: string | null;
    minVoteCountLabel: string;
    minVoteCountHelp: string | null;
    joinedOnOrAfterLabel: string;
    joinedBeforeLabel: string;
    joinedWindowHelp: string | null;
    activeOnOrAfterLabel: string;
    activeBeforeLabel: string;
    activeWindowHelp: string | null;
    recordingIdLabel: string;
    recordingIdPlaceholder: string;
    recordingIdHelp: string | null;
  };
};

export const BADGE_PREVIEW_MODE_DESCRIPTORS: Record<
  BadgeQualificationMode,
  BadgePreviewModeDescriptor
> = {
  minutes_streamed: {
    key: "minutes_streamed",
    label: "Total minutes streamed",
    description: "Qualify members by all-time listening minutes.",
    requiresRecording: false,
    supportsDateWindow: false,
    metricFamily: "minutes",
    inputRequirements: {
      minMinutes: true,
      minPlayCount: false,
      minCompletedCount: false,
      minProgressCount: false,
      minContributionCount: false,
      minVoteCount: false,
      joinedWindow: false,
      activeWindow: false,
      recordingId: false,
    },
    fieldText: {
      minMinutesLabel: "Minimum minutes streamed",
      minMinutesHelp:
        "Qualify members whose all-time listening minutes meet or exceed this threshold.",
      minPlayCountLabel: "Minimum play count",
      minPlayCountHelp: null,
      minCompletedCountLabel: "Minimum complete count",
      minCompletedCountHelp: null,
      minProgressCountLabel: "Minimum progress count",
      minProgressCountHelp: null,
      minContributionCountLabel: "Minimum Exegesis contributions",
      minContributionCountHelp: null,
      minVoteCountLabel: "Minimum cumulative vote tally",
      minVoteCountHelp: null,
      joinedOnOrAfterLabel: "Joined on or after",
      joinedBeforeLabel: "Joined before",
      joinedWindowHelp: null,
      activeOnOrAfterLabel: "Active on or after",
      activeBeforeLabel: "Active before",
      activeWindowHelp: null,
      recordingIdLabel: "Recording ID",
      recordingIdPlaceholder: "recording UUID",
      recordingIdHelp: null,
    },
  },
  play_count: {
    key: "play_count",
    label: "Total play count",
    description: "Qualify members by all-time credited play count.",
    requiresRecording: false,
    supportsDateWindow: false,
    metricFamily: "plays",
    inputRequirements: {
      minMinutes: false,
      minPlayCount: true,
      minCompletedCount: false,
      minProgressCount: false,
      minContributionCount: false,
      minVoteCount: false,
      joinedWindow: false,
      activeWindow: false,
      recordingId: false,
    },
    fieldText: {
      minMinutesLabel: "Minimum minutes streamed",
      minMinutesHelp: null,
      minPlayCountLabel: "Minimum play count",
      minPlayCountHelp:
        "Qualify members whose all-time credited plays meet or exceed this threshold.",
      minCompletedCountLabel: "Minimum complete count",
      minCompletedCountHelp: null,
      minProgressCountLabel: "Minimum progress count",
      minProgressCountHelp: null,
      minContributionCountLabel: "Minimum Exegesis contributions",
      minContributionCountHelp: null,
      minVoteCountLabel: "Minimum cumulative vote tally",
      minVoteCountHelp: null,
      joinedOnOrAfterLabel: "Joined on or after",
      joinedBeforeLabel: "Joined before",
      joinedWindowHelp: null,
      activeOnOrAfterLabel: "Active on or after",
      activeBeforeLabel: "Active before",
      activeWindowHelp: null,
      recordingIdLabel: "Recording ID",
      recordingIdPlaceholder: "recording UUID",
      recordingIdHelp: null,
    },
  },
  complete_count: {
    key: "complete_count",
    label: "Total complete count",
    description: "Qualify members by all-time completion count.",
    requiresRecording: false,
    supportsDateWindow: false,
    metricFamily: "completes",
    inputRequirements: {
      minMinutes: false,
      minPlayCount: false,
      minCompletedCount: true,
      minProgressCount: false,
      minContributionCount: false,
      minVoteCount: false,
      joinedWindow: false,
      activeWindow: false,
      recordingId: false,
    },
    fieldText: {
      minMinutesLabel: "Minimum minutes streamed",
      minMinutesHelp: null,
      minPlayCountLabel: "Minimum play count",
      minPlayCountHelp: null,
      minCompletedCountLabel: "Minimum complete count",
      minCompletedCountHelp:
        "Qualify members whose all-time completion count meets or exceeds this threshold.",
      minProgressCountLabel: "Minimum progress count",
      minProgressCountHelp: null,
      minContributionCountLabel: "Minimum Exegesis contributions",
      minContributionCountHelp: null,
      minVoteCountLabel: "Minimum cumulative vote tally",
      minVoteCountHelp: null,
      joinedOnOrAfterLabel: "Joined on or after",
      joinedBeforeLabel: "Joined before",
      joinedWindowHelp: null,
      activeOnOrAfterLabel: "Active on or after",
      activeBeforeLabel: "Active before",
      activeWindowHelp: null,
      recordingIdLabel: "Recording ID",
      recordingIdPlaceholder: "recording UUID",
      recordingIdHelp: null,
    },
  },
  joined_within_window: {
    key: "joined_within_window",
    label: "Joined within date window",
    description: "Qualify members by membership creation time.",
    requiresRecording: false,
    supportsDateWindow: true,
    metricFamily: "membership",
    inputRequirements: {
      minMinutes: false,
      minPlayCount: false,
      minCompletedCount: false,
      minProgressCount: false,
      minContributionCount: false,
      minVoteCount: false,
      joinedWindow: true,
      activeWindow: false,
      recordingId: false,
    },
    fieldText: {
      minMinutesLabel: "Minimum minutes streamed",
      minMinutesHelp: null,
      minPlayCountLabel: "Minimum play count",
      minPlayCountHelp: null,
      minCompletedCountLabel: "Minimum complete count",
      minCompletedCountHelp: null,
      minProgressCountLabel: "Minimum progress count",
      minProgressCountHelp: null,
      minContributionCountLabel: "Minimum Exegesis contributions",
      minContributionCountHelp: null,
      minVoteCountLabel: "Minimum cumulative vote tally",
      minVoteCountHelp: null,
      joinedOnOrAfterLabel: "Joined on or after",
      joinedBeforeLabel: "Joined before",
      joinedWindowHelp:
        "Qualify members whose account creation timestamp falls within this date window.",
      activeOnOrAfterLabel: "Active on or after",
      activeBeforeLabel: "Active before",
      activeWindowHelp: null,
      recordingIdLabel: "Recording ID",
      recordingIdPlaceholder: "recording UUID",
      recordingIdHelp: null,
    },
  },
  active_within_window: {
    key: "active_within_window",
    label: "Active within playback window",
    description:
      "Qualify members by member telemetry activity inside a time window.",
    requiresRecording: false,
    supportsDateWindow: true,
    metricFamily: "activity",
    inputRequirements: {
      minMinutes: false,
      minPlayCount: true,
      minCompletedCount: true,
      minProgressCount: true,
      minContributionCount: false,
      minVoteCount: false,
      joinedWindow: false,
      activeWindow: true,
      recordingId: false,
    },
    fieldText: {
      minMinutesLabel: "Minimum minutes streamed",
      minMinutesHelp: null,
      minPlayCountLabel: "Minimum play count in window",
      minPlayCountHelp:
        "Count qualifying play events within the selected playback window.",
      minCompletedCountLabel: "Minimum complete count in window",
      minCompletedCountHelp:
        "Count qualifying completion events within the selected playback window.",
      minProgressCountLabel: "Minimum progress count in window",
      minProgressCountHelp:
        "Progress count reflects credited telemetry progress events, not literal minutes.",
      minContributionCountLabel: "Minimum Exegesis contributions",
      minContributionCountHelp: null,
      minVoteCountLabel: "Minimum cumulative vote tally",
      minVoteCountHelp: null,
      joinedOnOrAfterLabel: "Joined on or after",
      joinedBeforeLabel: "Joined before",
      joinedWindowHelp: null,
      activeOnOrAfterLabel: "Active on or after",
      activeBeforeLabel: "Active before",
      activeWindowHelp:
        "Qualify members whose telemetry activity falls within this playback time window.",
      recordingIdLabel: "Recording ID",
      recordingIdPlaceholder: "recording UUID",
      recordingIdHelp: null,
    },
  },
  recording_minutes_streamed: {
    key: "recording_minutes_streamed",
    label: "Recording-specific minutes streamed",
    description: "Qualify members by minutes streamed on a specific recording.",
    requiresRecording: true,
    supportsDateWindow: false,
    metricFamily: "minutes",
    inputRequirements: {
      minMinutes: true,
      minPlayCount: false,
      minCompletedCount: false,
      minProgressCount: false,
      minContributionCount: false,
      minVoteCount: false,
      joinedWindow: false,
      activeWindow: false,
      recordingId: true,
    },
    fieldText: {
      minMinutesLabel: "Minimum minutes streamed on recording",
      minMinutesHelp:
        "Qualify members whose listening minutes on the selected recording meet or exceed this threshold.",
      minPlayCountLabel: "Minimum play count",
      minPlayCountHelp: null,
      minCompletedCountLabel: "Minimum complete count",
      minCompletedCountHelp: null,
      minProgressCountLabel: "Minimum progress count",
      minProgressCountHelp: null,
      minContributionCountLabel: "Minimum Exegesis contributions",
      minContributionCountHelp: null,
      minVoteCountLabel: "Minimum cumulative vote tally",
      minVoteCountHelp: null,
      joinedOnOrAfterLabel: "Joined on or after",
      joinedBeforeLabel: "Joined before",
      joinedWindowHelp: null,
      activeOnOrAfterLabel: "Active on or after",
      activeBeforeLabel: "Active before",
      activeWindowHelp: null,
      recordingIdLabel: "Recording ID",
      recordingIdPlaceholder: "recording UUID",
      recordingIdHelp:
        "Use the canonical recording UUID for the work being qualified.",
    },
  },
  recording_play_count: {
    key: "recording_play_count",
    label: "Recording-specific play count",
    description: "Qualify members by play count on a specific recording.",
    requiresRecording: true,
    supportsDateWindow: false,
    metricFamily: "plays",
    inputRequirements: {
      minMinutes: false,
      minPlayCount: true,
      minCompletedCount: false,
      minProgressCount: false,
      minContributionCount: false,
      minVoteCount: false,
      joinedWindow: false,
      activeWindow: false,
      recordingId: true,
    },
    fieldText: {
      minMinutesLabel: "Minimum minutes streamed",
      minMinutesHelp: null,
      minPlayCountLabel: "Minimum play count on recording",
      minPlayCountHelp:
        "Qualify members whose credited plays on the selected recording meet or exceed this threshold.",
      minCompletedCountLabel: "Minimum complete count",
      minCompletedCountHelp: null,
      minProgressCountLabel: "Minimum progress count",
      minProgressCountHelp: null,
      minContributionCountLabel: "Minimum Exegesis contributions",
      minContributionCountHelp: null,
      minVoteCountLabel: "Minimum cumulative vote tally",
      minVoteCountHelp: null,
      joinedOnOrAfterLabel: "Joined on or after",
      joinedBeforeLabel: "Joined before",
      joinedWindowHelp: null,
      activeOnOrAfterLabel: "Active on or after",
      activeBeforeLabel: "Active before",
      activeWindowHelp: null,
      recordingIdLabel: "Recording ID",
      recordingIdPlaceholder: "recording UUID",
      recordingIdHelp:
        "Use the canonical recording UUID for the work being qualified.",
    },
  },
  recording_complete_count: {
    key: "recording_complete_count",
    label: "Recording-specific complete count",
    description: "Qualify members by completion count on a specific recording.",
    requiresRecording: true,
    supportsDateWindow: false,
    metricFamily: "completes",
    inputRequirements: {
      minMinutes: false,
      minPlayCount: false,
      minCompletedCount: true,
      minProgressCount: false,
      minContributionCount: false,
      minVoteCount: false,
      joinedWindow: false,
      activeWindow: false,
      recordingId: true,
    },
    fieldText: {
      minMinutesLabel: "Minimum minutes streamed",
      minMinutesHelp: null,
      minPlayCountLabel: "Minimum play count",
      minPlayCountHelp: null,
      minCompletedCountLabel: "Minimum complete count on recording",
      minCompletedCountHelp:
        "Qualify members whose completion count on the selected recording meets or exceeds this threshold.",
      minProgressCountLabel: "Minimum progress count",
      minProgressCountHelp: null,
      minContributionCountLabel: "Minimum Exegesis contributions",
      minContributionCountHelp: null,
      minVoteCountLabel: "Minimum cumulative vote tally",
      minVoteCountHelp: null,
      joinedOnOrAfterLabel: "Joined on or after",
      joinedBeforeLabel: "Joined before",
      joinedWindowHelp: null,
      activeOnOrAfterLabel: "Active on or after",
      activeBeforeLabel: "Active before",
      activeWindowHelp: null,
      recordingIdLabel: "Recording ID",
      recordingIdPlaceholder: "recording UUID",
      recordingIdHelp:
        "Use the canonical recording UUID for the work being qualified.",
    },
  },
  exegesis_contribution_count: {
    key: "exegesis_contribution_count",
    label: "Exegesis contribution count",
    description:
      "Qualify members by their cumulative Exegesis contribution count.",
    requiresRecording: false,
    supportsDateWindow: false,
    metricFamily: "exegesis",
    inputRequirements: {
      minMinutes: false,
      minPlayCount: false,
      minCompletedCount: false,
      minProgressCount: false,
      minContributionCount: true,
      minVoteCount: false,
      joinedWindow: false,
      activeWindow: false,
      recordingId: false,
    },
    fieldText: {
      minMinutesLabel: "Minimum minutes streamed",
      minMinutesHelp: null,
      minPlayCountLabel: "Minimum play count",
      minPlayCountHelp: null,
      minCompletedCountLabel: "Minimum complete count",
      minCompletedCountHelp: null,
      minProgressCountLabel: "Minimum progress count",
      minProgressCountHelp: null,
      minContributionCountLabel: "Minimum Exegesis contributions",
      minContributionCountHelp:
        "Qualify members whose total Exegesis contribution count meets or exceeds this threshold.",
      minVoteCountLabel: "Minimum cumulative vote tally",
      minVoteCountHelp: null,
      joinedOnOrAfterLabel: "Joined on or after",
      joinedBeforeLabel: "Joined before",
      joinedWindowHelp: null,
      activeOnOrAfterLabel: "Active on or after",
      activeBeforeLabel: "Active before",
      activeWindowHelp: null,
      recordingIdLabel: "Recording ID",
      recordingIdPlaceholder: "recording UUID",
      recordingIdHelp: null,
    },
  },
  exegesis_vote_tally: {
    key: "exegesis_vote_tally",
    label: "Exegesis cumulative vote tally",
    description:
      "Qualify members by the cumulative votes received across their Exegesis contributions.",
    requiresRecording: false,
    supportsDateWindow: false,
    metricFamily: "exegesis",
    inputRequirements: {
      minMinutes: false,
      minPlayCount: false,
      minCompletedCount: false,
      minProgressCount: false,
      minContributionCount: false,
      minVoteCount: true,
      joinedWindow: false,
      activeWindow: false,
      recordingId: false,
    },
    fieldText: {
      minMinutesLabel: "Minimum minutes streamed",
      minMinutesHelp: null,
      minPlayCountLabel: "Minimum play count",
      minPlayCountHelp: null,
      minCompletedCountLabel: "Minimum complete count",
      minCompletedCountHelp: null,
      minProgressCountLabel: "Minimum progress count",
      minProgressCountHelp: null,
      minContributionCountLabel: "Minimum Exegesis contributions",
      minContributionCountHelp: null,
      minVoteCountLabel: "Minimum cumulative vote tally",
      minVoteCountHelp:
        "Qualify members whose total votes received across live Exegesis comments meet or exceed this threshold.",
      joinedOnOrAfterLabel: "Joined on or after",
      joinedBeforeLabel: "Joined before",
      joinedWindowHelp: null,
      activeOnOrAfterLabel: "Active on or after",
      activeBeforeLabel: "Active before",
      activeWindowHelp: null,
      recordingIdLabel: "Recording ID",
      recordingIdPlaceholder: "recording UUID",
      recordingIdHelp: null,
    },
  },
  public_name_unlocked: {
    key: "public_name_unlocked",
    label: "Public name unlocked",
    description:
      "Qualify members whose public-name unlock state has been reached through Exegesis.",
    requiresRecording: false,
    supportsDateWindow: false,
    metricFamily: "identity",
    inputRequirements: {
      minMinutes: false,
      minPlayCount: false,
      minCompletedCount: false,
      minProgressCount: false,
      minContributionCount: false,
      minVoteCount: false,
      joinedWindow: false,
      activeWindow: false,
      recordingId: false,
    },
    fieldText: {
      minMinutesLabel: "Minimum minutes streamed",
      minMinutesHelp: null,
      minPlayCountLabel: "Minimum play count",
      minPlayCountHelp: null,
      minCompletedCountLabel: "Minimum complete count",
      minCompletedCountHelp: null,
      minProgressCountLabel: "Minimum progress count",
      minProgressCountHelp: null,
      minContributionCountLabel: "Minimum Exegesis contributions",
      minContributionCountHelp: null,
      minVoteCountLabel: "Minimum cumulative vote tally",
      minVoteCountHelp: null,
      joinedOnOrAfterLabel: "Joined on or after",
      joinedBeforeLabel: "Joined before",
      joinedWindowHelp: null,
      activeOnOrAfterLabel: "Active on or after",
      activeBeforeLabel: "Active before",
      activeWindowHelp: null,
      recordingIdLabel: "Recording ID",
      recordingIdPlaceholder: "recording UUID",
      recordingIdHelp: null,
    },
  },
};

export const BADGE_PREVIEW_MODES: BadgePreviewModeDescriptor[] = [
  BADGE_PREVIEW_MODE_DESCRIPTORS.minutes_streamed,
  BADGE_PREVIEW_MODE_DESCRIPTORS.play_count,
  BADGE_PREVIEW_MODE_DESCRIPTORS.complete_count,
  BADGE_PREVIEW_MODE_DESCRIPTORS.joined_within_window,
  BADGE_PREVIEW_MODE_DESCRIPTORS.active_within_window,
  BADGE_PREVIEW_MODE_DESCRIPTORS.recording_minutes_streamed,
  BADGE_PREVIEW_MODE_DESCRIPTORS.recording_play_count,
  BADGE_PREVIEW_MODE_DESCRIPTORS.recording_complete_count,
  BADGE_PREVIEW_MODE_DESCRIPTORS.exegesis_contribution_count,
  BADGE_PREVIEW_MODE_DESCRIPTORS.exegesis_vote_tally,
  BADGE_PREVIEW_MODE_DESCRIPTORS.public_name_unlocked,
];

export function getBadgePreviewModeDescriptor(
  mode: BadgeQualificationMode,
): BadgePreviewModeDescriptor {
  return BADGE_PREVIEW_MODE_DESCRIPTORS[mode];
}

export function isRecordingScopedBadgePreviewMode(
  mode: BadgeQualificationMode,
): boolean {
  return BADGE_PREVIEW_MODE_DESCRIPTORS[mode].requiresRecording;
}


----- lib/badges.ts -----
// web/lib/badges.ts
import "server-only";

import { unstable_cache } from "next/cache";
import { cache } from "react";
import { client } from "@/sanity/lib/client";
import { areBadgesEnabled } from "@/lib/badgeFeature";
import type { BadgeQualificationMode } from "@/lib/badgePreviewModes";

export type BadgeAwardMode = "manual" | "automatic";

export type BadgeAutoAwardConfig = {
  enabled: boolean;
  qualificationMode: BadgeQualificationMode | null;
  minMinutes: number | null;
  minPlayCount: number | null;
  minCompletedCount: number | null;
  minProgressCount: number | null;
  minContributionCount: number | null;
  minVoteCount: number | null;
  joinedOnOrAfter: string | null;
  joinedBefore: string | null;
  activeOnOrAfter: string | null;
  activeBefore: string | null;
  recordingId: string | null;
};

export type BadgeDefinition = {
  _id: string;
  title: string;
  entitlementKey: string;
  description: string | null;
  imageUrl: string | null;
  displayOrder: number;
  featured: boolean;
  shareable: boolean;
  undisclosed: boolean;
  active: boolean;
  awardMode: BadgeAwardMode;
  autoAward: BadgeAutoAwardConfig | null;
};

type BadgeDefinitionQueryRow = {
  _id: string;
  title?: string | null;
  keySuffix?: string | null;
  description?: string | null;
  imageUrl?: string | null;
  displayOrder?: number | null;
  featured?: boolean | null;
  shareable?: boolean | null;
  undisclosed?: boolean | null;
  active?: boolean | null;
  awardMode?: string | null;
  autoAwardEnabled?: boolean | null;
  autoQualificationMode?: string | null;
  minMinutes?: number | null;
  minPlayCount?: number | null;
  minCompletedCount?: number | null;
  minProgressCount?: number | null;
  minContributionCount?: number | null;
  minVoteCount?: number | null;
  joinedOnOrAfter?: string | null;
  joinedBefore?: string | null;
  activeOnOrAfter?: string | null;
  activeBefore?: string | null;
  recordingId?: string | null;
};

const BADGE_DEFINITIONS_QUERY = `
   *[_type == "badgeDefinition" && coalesce(active, true) == true]{
    _id,
    title,
    keySuffix,
    description,
    "imageUrl": image.asset->url,
    displayOrder,
    featured,
    shareable,
    undisclosed,
    active,
    awardMode,
    autoAwardEnabled,
    autoQualificationMode,
    minMinutes,
    minPlayCount,
    minCompletedCount,
    minProgressCount,
    minContributionCount,
    minVoteCount,
    joinedOnOrAfter,
    joinedBefore,
    activeOnOrAfter,
    activeBefore,
    recordingId
  }
`;

function asFiniteNumber(value: unknown): number | null {
  return typeof value === "number" && Number.isFinite(value) ? value : null;
}

function asTrimmedString(value: unknown): string | null {
  return typeof value === "string" && value.trim() ? value.trim() : null;
}

function asQualificationMode(value: unknown): BadgeQualificationMode | null {
  const raw = asTrimmedString(value);

  switch (raw) {
    case "minutes_streamed":
    case "play_count":
    case "complete_count":
    case "joined_within_window":
    case "active_within_window":
    case "recording_minutes_streamed":
    case "recording_play_count":
    case "recording_complete_count":
    case "exegesis_contribution_count":
    case "exegesis_vote_tally":
    case "public_name_unlocked":
      return raw;
    default:
      return null;
  }
}

function coerceBadgeDefinition(
  row: BadgeDefinitionQueryRow,
): BadgeDefinition | null {
  const keySuffix =
    typeof row.keySuffix === "string" ? row.keySuffix.trim() : "";
  const title = typeof row.title === "string" ? row.title.trim() : "";

  if (!keySuffix || !title) return null;
  if (!/^[a-z0-9]+(?:_[a-z0-9]+)*$/.test(keySuffix)) return null;

  const entitlementKey = `badge_${keySuffix}`;
  const awardMode: BadgeAwardMode =
    row.awardMode === "automatic" ? "automatic" : "manual";

  const qualificationMode = asQualificationMode(row.autoQualificationMode);

  const autoAward: BadgeAutoAwardConfig | null =
    awardMode === "automatic"
      ? {
          enabled: row.autoAwardEnabled !== false,
          qualificationMode,
          minMinutes: asFiniteNumber(row.minMinutes),
          minPlayCount: asFiniteNumber(row.minPlayCount),
          minCompletedCount: asFiniteNumber(row.minCompletedCount),
          minProgressCount: asFiniteNumber(row.minProgressCount),
          minContributionCount: asFiniteNumber(row.minContributionCount),
          minVoteCount: asFiniteNumber(row.minVoteCount),
          joinedOnOrAfter: asTrimmedString(row.joinedOnOrAfter),
          joinedBefore: asTrimmedString(row.joinedBefore),
          activeOnOrAfter: asTrimmedString(row.activeOnOrAfter),
          activeBefore: asTrimmedString(row.activeBefore),
          recordingId: asTrimmedString(row.recordingId),
        }
      : null;

  return {
    _id: row._id,
    title,
    entitlementKey,
    description:
      typeof row.description === "string" && row.description.trim()
        ? row.description.trim()
        : null,
    imageUrl:
      typeof row.imageUrl === "string" && row.imageUrl.trim()
        ? row.imageUrl.trim()
        : null,
    displayOrder:
      typeof row.displayOrder === "number" && Number.isFinite(row.displayOrder)
        ? row.displayOrder
        : 100,
    featured: row.featured === true,
    shareable: row.shareable === true,
    undisclosed: row.undisclosed === true,
    active: row.active !== false,
    awardMode,
    autoAward,
  };
}

const BADGE_DEFINITIONS_REVALIDATE_SECONDS = 5 * 60;

const getCachedActiveBadgeDefinitions = unstable_cache(
  async (): Promise<BadgeDefinition[]> => {
    const result = await client.fetch(BADGE_DEFINITIONS_QUERY);

    const rows = Array.isArray(result)
      ? (result as BadgeDefinitionQueryRow[])
      : [];

    return rows
      .map((row: BadgeDefinitionQueryRow) => coerceBadgeDefinition(row))
      .filter((row): row is BadgeDefinition => row !== null);
  },
  ["active-badge-definitions-v1"],
  {
    revalidate: BADGE_DEFINITIONS_REVALIDATE_SECONDS,
    tags: ["badgeDefinitions"],
  },
);

export const getActiveBadgeDefinitions = cache(
  async (): Promise<BadgeDefinition[]> => {
    if (!areBadgesEnabled()) {
      return [];
    }

    return getCachedActiveBadgeDefinitions();
  },
);

export const getActiveBadgeDefinitionsByEntitlementKey = cache(
  async (): Promise<Map<string, BadgeDefinition>> => {
    const definitions = await getActiveBadgeDefinitions();
    return new Map(
      definitions.map((definition) => [definition.entitlementKey, definition]),
    );
  },
);

export const getAutoAwardBadgeDefinitions = cache(
  async (): Promise<BadgeDefinition[]> => {
    const definitions = await getActiveBadgeDefinitions();

    return definitions.filter(
      (definition) =>
        definition.awardMode === "automatic" &&
        definition.autoAward !== null &&
        definition.autoAward.enabled &&
        definition.autoAward.qualificationMode !== null,
    );
  },
);


----- lib/entitlementOps.ts -----
// web/lib/entitlementOps.ts
import "server-only";
import { sql } from "@vercel/postgres";
import { EVENT_SOURCES, type EventSource } from "./vocab";
import { logEntitlementGranted, logEntitlementRevoked } from "./events";

const uuidOk = (v: string) =>
  /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(
    v,
  );

type GrantParams = {
  memberId: string;
  entitlementKey: string;
  scopeId?: string | null;
  scopeMeta?: Record<string, unknown>;
  grantedBy?: string;
  grantReason?: string;
  grantSource?: string;
  grantSourceRef?: string | null;
  expiresAt?: Date | null;
  correlationId?: string | null;
  eventSource?: EventSource | string;
};

export type GrantEntitlementResult =
  | { status: "inserted" }
  | { status: "already_present" };

export type RevokeEntitlementResult =
  | { status: "revoked" }
  | { status: "not_found" };

async function ensureEntitlementType(
  entitlementKey: string,
  scopeId: string | null,
) {
  const key = entitlementKey?.trim();
  if (!key) return;

  // Contract:
  // - scope_id NULL => "global" entitlements (this includes catalogue-wide rights)
  // - scope_id NOT NULL => "scoped" entitlements (album-scoped, etc)
  const scope = scopeId ? "scoped" : "global";

  await sql`
    insert into entitlement_types (key, description, scope)
    select ${key}, 'auto-registered', ${scope}
    where not exists (
      select 1 from entitlement_types et where et.key = ${key}
    )
  `;
}

export async function grantEntitlement(
  params: GrantParams,
): Promise<GrantEntitlementResult> {
  if (!uuidOk(params.memberId)) throw new Error("Invalid memberId");

  const {
    memberId,
    entitlementKey,
    scopeId = null,
    scopeMeta = {},
    grantedBy = "system",
    grantReason = null,
    grantSource = "unknown",
    grantSourceRef = null,
    expiresAt = null,
    correlationId = null,
    eventSource = EVENT_SOURCES.SERVER,
  } = params;

  await ensureEntitlementType(entitlementKey, scopeId);

  const inserted = await sql`
    insert into entitlement_grants (
      member_id,
      entitlement_key,
      scope_id,
      scope_meta,
      granted_by,
      grant_reason,
      grant_source,
      grant_source_ref,
      expires_at
    )
    select
      ${memberId}::uuid,
      ${entitlementKey},
      ${scopeId},
      ${JSON.stringify(scopeMeta)}::jsonb,
      ${grantedBy},
      ${grantReason},
      ${grantSource},
      ${grantSourceRef},
      ${expiresAt ? expiresAt.toISOString() : null}::timestamptz
    where not exists (
      select 1
      from entitlement_grants eg
      where eg.member_id = ${memberId}::uuid
        and eg.entitlement_key = ${entitlementKey}
        and coalesce(eg.scope_id,'') = coalesce(${scopeId ?? ""},'')
        and eg.revoked_at is null
        and (eg.expires_at is null or eg.expires_at > now())
    )
    returning 1
  `;

  if (!inserted.rowCount) {
    return { status: "already_present" };
  }

  await logEntitlementGranted({
    memberId,
    entitlementKey,
    scopeId,
    source: eventSource,
    correlationId,
    payload: {
      granted_by: grantedBy,
      grant_source: grantSource,
      grant_reason: grantReason,
      grant_source_ref: grantSourceRef,
      expires_at: expiresAt ? expiresAt.toISOString() : null,
    },
  });

  return { status: "inserted" };
}

export async function revokeEntitlement(params: {
  memberId: string;
  entitlementKey: string;
  scopeId?: string | null;
  grantSource?: string | null;
  grantSourceRef?: string | null;
  revokedBy?: string;
  revokeReason?: string | null;
  correlationId?: string | null;
  eventSource?: EventSource | string;
}): Promise<RevokeEntitlementResult> {
  if (!uuidOk(params.memberId)) throw new Error("Invalid memberId");

  const {
    memberId,
    entitlementKey,
    scopeId = null,
    grantSource = null,
    grantSourceRef = null,
    revokedBy = "system",
    revokeReason = null,
    correlationId = null,
    eventSource = EVENT_SOURCES.SERVER,
  } = params;

  const revoked = await sql`
    update entitlement_grants
    set revoked_at = now(),
        revoked_by = ${revokedBy},
        revoke_reason = ${revokeReason}
    where member_id = ${memberId}::uuid
      and entitlement_key = ${entitlementKey}
      and coalesce(scope_id,'') = coalesce(${scopeId ?? ""},'')
      and (${grantSource}::text is null or grant_source = ${grantSource})
      and (
        ${grantSourceRef}::text is null
        or grant_source_ref = ${grantSourceRef}
      )
      and revoked_at is null
      and (expires_at is null or expires_at > now())
    returning 1
  `;

  if (!revoked.rowCount) {
    return { status: "not_found" };
  }

  await logEntitlementRevoked({
    memberId,
    entitlementKey,
    scopeId,
    source: eventSource,
    correlationId,
    payload: {
      revoked_by: revokedBy,
      revoke_reason: revokeReason,
      grant_source: grantSource,
      grant_source_ref: grantSourceRef,
    },
  });

  return { status: "revoked" };
}


----- lib/memberDashboard.ts -----
// web/lib/memberDashboard.ts
import type { ResolvedDisplayIdentity } from "@/lib/memberIdentity";

export type MemberDashboardBadge = {
  key: string;
  label: string;
  description?: string | null;
  imageUrl?: string | null;
  shareable?: boolean;
  undisclosed?: boolean;
  unlocked: boolean;
  unlockedAt?: string | null;
  editorialOrder?: number | null;
  cabinetRevealPending?: boolean;
};

export type PortalMemberFavouriteTrack = {
  recordingId: string;
  title: string;
  artist?: string | null;
  minutes?: number | null;
};

export type PortalMemberSummary = {
  identity: ResolvedDisplayIdentity | null;

  /**
   * All-time member signal.
   *
   * Transitional semantic:
   * currently sourced from Exegesis contributions only.
   *
   * The field name remains broader so it can later widen into a
   * cross-surface participation aggregate without changing the
   * member-panel contract.
   */
  contributionCount: number | null;

  /**
   * All-time listening minutes once playback telemetry exists.
   */
  minutesStreamed: number | null;

  /**
   * All-time statistically inferred listening preference.
   */
  favouriteTrack: PortalMemberFavouriteTrack | null;

  /**
   * Unified badge cabinet projection.
   * Ordered badge definitions are merged with member possession state.
   */
  badges: MemberDashboardBadge[];
};

export function emptyPortalMemberSummary(): PortalMemberSummary {
  return {
    identity: null,
    contributionCount: null,
    minutesStreamed: null,
    favouriteTrack: null,
    badges: [],
  };
}


===== BADGE IMPORT / EXPORT / TYPE SURFACE =====

app/admin/badges/_components/PreviewResultsSection.tsx:1:// web/app/admin/badges/_components/PreviewResultsSection.tsx
app/admin/badges/_components/PreviewResultsSection.tsx:4:import React from "react";
app/admin/badges/_components/PreviewResultsSection.tsx:5:import {
app/admin/badges/_components/PreviewResultsSection.tsx:12:import type { PreviewRow } from "../_lib/badgeDashboardTypes";
app/admin/badges/_components/PreviewResultsSection.tsx:13:import { formatDateTime, formatMetric } from "../_lib/badgeDashboardUtils";
app/admin/badges/_components/PreviewResultsSection.tsx:15:type Props = {
app/admin/badges/_components/PreviewResultsSection.tsx:20:const thStyle: React.CSSProperties = {
app/admin/badges/_components/PreviewResultsSection.tsx:31:const tdStyle: React.CSSProperties = {
app/admin/badges/_components/PreviewResultsSection.tsx:40:const tdNumericStyle: React.CSSProperties = {
app/admin/badges/_components/PreviewResultsSection.tsx:46:export function PreviewResultsSection(props: Props) {
app/admin/badges/_components/PreviewResultsSection.tsx:88:            Matching members resolved from the current badge qualification
app/admin/badges/BadgeDashboardClient.tsx:1:// web/app/admin/badges/BadgeDashboardClient.tsx
app/admin/badges/BadgeDashboardClient.tsx:4:import React from "react";
app/admin/badges/BadgeDashboardClient.tsx:5:import { BADGE_PREVIEW_MODES } from "@/lib/badgePreviewModes";
app/admin/badges/BadgeDashboardClient.tsx:6:import AdminPageFrame from "../AdminPageFrame";
app/admin/badges/BadgeDashboardClient.tsx:7:import { BadgeCatalogueSection } from "./_components/BadgeCatalogueSection";
app/admin/badges/BadgeDashboardClient.tsx:8:import { BadgeQualificationFormSection } from "./_components/BadgeQualificationFormSection";
app/admin/badges/BadgeDashboardClient.tsx:9:import { PreviewResultsSection } from "./_components/PreviewResultsSection";
app/admin/badges/BadgeDashboardClient.tsx:10:import type {
app/admin/badges/BadgeDashboardClient.tsx:12:  BadgeDefinitionOption,
app/admin/badges/BadgeDashboardClient.tsx:19:} from "./_lib/badgeDashboardTypes";
app/admin/badges/BadgeDashboardClient.tsx:20:import {
app/admin/badges/BadgeDashboardClient.tsx:23:} from "./_lib/badgeDashboardUtils";
app/admin/badges/BadgeDashboardClient.tsx:25:export default function BadgeDashboardClient({
app/admin/badges/BadgeDashboardClient.tsx:27:  badgeDefinitions,
app/admin/badges/BadgeDashboardClient.tsx:29:  const sortedBadges = React.useMemo(() => {
app/admin/badges/BadgeDashboardClient.tsx:30:    return [...badgeDefinitions].sort((a, b) => {
app/admin/badges/BadgeDashboardClient.tsx:37:  }, [badgeDefinitions]);
app/admin/badges/BadgeDashboardClient.tsx:41:    entitlementKey: sortedBadges[0]?.entitlementKey ?? "",
app/admin/badges/BadgeDashboardClient.tsx:65:  const selectedBadge = React.useMemo(() => {
app/admin/badges/BadgeDashboardClient.tsx:67:      sortedBadges.find(
app/admin/badges/BadgeDashboardClient.tsx:68:        (badge: BadgeDefinitionOption) =>
app/admin/badges/BadgeDashboardClient.tsx:69:          badge.entitlementKey === form.entitlementKey,
app/admin/badges/BadgeDashboardClient.tsx:72:  }, [form.entitlementKey, sortedBadges]);
app/admin/badges/BadgeDashboardClient.tsx:87:    recordingId: false,
app/admin/badges/BadgeDashboardClient.tsx:109:    recordingIdLabel: "Recording ID",
app/admin/badges/BadgeDashboardClient.tsx:110:    recordingIdPlaceholder: "recording UUID",
app/admin/badges/BadgeDashboardClient.tsx:111:    recordingIdHelp: null,
app/admin/badges/BadgeDashboardClient.tsx:183:      updateForm("recordingId", recording.recordingId);
app/admin/badges/BadgeDashboardClient.tsx:193:    updateForm("recordingId", "");
app/admin/badges/BadgeDashboardClient.tsx:197:    if (modeInputs.recordingId && !form.recordingId.trim()) {
app/admin/badges/BadgeDashboardClient.tsx:199:        "Select a recording before previewing this badge cohort.",
app/admin/badges/BadgeDashboardClient.tsx:212:      const response = await fetch("/api/admin/badges/preview", {
app/admin/badges/BadgeDashboardClient.tsx:223:        throw new Error(json.error || "Unable to preview badge cohort.");
app/admin/badges/BadgeDashboardClient.tsx:232:          : "Unable to preview badge cohort.";
app/admin/badges/BadgeDashboardClient.tsx:239:  }, [form, modeInputs.recordingId]);
app/admin/badges/BadgeDashboardClient.tsx:242:    if (!form.entitlementKey) {
app/admin/badges/BadgeDashboardClient.tsx:243:      setAwardError("Choose a badge before awarding.");
app/admin/badges/BadgeDashboardClient.tsx:257:      const response = await fetch("/api/admin/badges/award", {
app/admin/badges/BadgeDashboardClient.tsx:263:          entitlementKey: form.entitlementKey,
app/admin/badges/BadgeDashboardClient.tsx:266:          grantSource: "badge_admin_preview",
app/admin/badges/BadgeDashboardClient.tsx:273:        throw new Error(json.error || "Unable to award badge.");
app/admin/badges/BadgeDashboardClient.tsx:276:      const { entitlementKey, attempted, inserted, alreadyHeld } = json.result;
app/admin/badges/BadgeDashboardClient.tsx:280:          `Awarded ${entitlementKey} to ${inserted} member${inserted === 1 ? "" : "s"}.`,
app/admin/badges/BadgeDashboardClient.tsx:284:          `No new grants were created. ${alreadyHeld} member${alreadyHeld === 1 ? "" : "s"} already held ${entitlementKey}.`,
app/admin/badges/BadgeDashboardClient.tsx:288:          `Processed ${attempted} member${attempted === 1 ? "" : "s"} for ${entitlementKey}: ${inserted} new grant${inserted === 1 ? "" : "s"}, ${alreadyHeld} already held.`,
app/admin/badges/BadgeDashboardClient.tsx:293:        error instanceof Error ? error.message : "Unable to award badge.";
app/admin/badges/BadgeDashboardClient.tsx:298:  }, [form.entitlementKey, form.grantReason, previewRows]);
app/admin/badges/BadgeDashboardClient.tsx:301:    if (!form.entitlementKey && sortedBadges[0]?.entitlementKey) {
app/admin/badges/BadgeDashboardClient.tsx:304:        entitlementKey: sortedBadges[0].entitlementKey,
app/admin/badges/BadgeDashboardClient.tsx:307:  }, [form.entitlementKey, sortedBadges]);
app/admin/badges/BadgeDashboardClient.tsx:310:    if (!modeInputs.recordingId && form.recordingId) {
app/admin/badges/BadgeDashboardClient.tsx:317:        recordingId: "",
app/admin/badges/BadgeDashboardClient.tsx:320:  }, [form.recordingId, modeInputs.recordingId]);
app/admin/badges/BadgeDashboardClient.tsx:326:      title="Badge dashboard"
app/admin/badges/BadgeDashboardClient.tsx:327:      subtitle="Preview and award entitlement-backed badges using live member and playback aggregates."
app/admin/badges/BadgeDashboardClient.tsx:335:        <BadgeCatalogueSection
app/admin/badges/BadgeDashboardClient.tsx:336:          badges={sortedBadges}
app/admin/badges/BadgeDashboardClient.tsx:337:          selectedEntitlementKey={form.entitlementKey}
app/admin/badges/BadgeDashboardClient.tsx:338:          onSelectEntitlementKey={(entitlementKey) =>
app/admin/badges/BadgeDashboardClient.tsx:339:            updateForm("entitlementKey", entitlementKey)
app/admin/badges/BadgeDashboardClient.tsx:343:        <BadgeQualificationFormSection
app/admin/badges/BadgeDashboardClient.tsx:344:          badges={sortedBadges}
app/admin/badges/BadgeDashboardClient.tsx:346:          selectedBadge={selectedBadge}
app/admin/AdminPageFrame.tsx:1:import React from "react";
app/admin/AdminPageFrame.tsx:3:type Props = {
app/admin/AdminPageFrame.tsx:12:export default function AdminPageFrame(props: Props) {
app/admin/badges/_components/RecordingPicker.tsx:3:import React from "react";
app/admin/badges/_components/RecordingPicker.tsx:4:import type { RecordingSearchResult } from "../_lib/badgeDashboardTypes";
app/admin/badges/_components/RecordingPicker.tsx:6:type Props = {
app/admin/badges/_components/RecordingPicker.tsx:21:export function RecordingPicker(props: Props) {
app/admin/badges/_components/RecordingPicker.tsx:102:                {selectedRecording.recordingId}
app/admin/badges/_components/RecordingPicker.tsx:124:              key={recording.recordingId}
app/admin/badges/_components/RecordingPicker.tsx:146:                {recording.recordingId}
app/admin/badges/_lib/badgeDashboardUtils.ts:1:// web/app/admin/badges/_lib/badgeDashboardUtils.ts
app/admin/badges/_lib/badgeDashboardUtils.ts:2:import type { FormState } from "./badgeDashboardTypes";
app/admin/badges/_lib/badgeDashboardUtils.ts:4:export const DEFAULT_FORM_STATE: FormState = {
app/admin/badges/_lib/badgeDashboardUtils.ts:5:  entitlementKey: "",
app/admin/badges/_lib/badgeDashboardUtils.ts:17:  recordingId: "",
app/admin/badges/_lib/badgeDashboardUtils.ts:22:export function formatDateTime(value: string | null): string {
app/admin/badges/_lib/badgeDashboardUtils.ts:31:export function formatMetric(value: number | null): string {
app/admin/badges/_lib/badgeDashboardUtils.ts:36:export function getBadgeCardBorder(
app/admin/badges/_lib/badgeDashboardUtils.ts:45:export function buildPreviewPayload(
app/admin/badges/_lib/badgeDashboardUtils.ts:106:        recordingId: form.recordingId,
app/admin/badges/_lib/badgeDashboardUtils.ts:114:        recordingId: form.recordingId,
app/admin/badges/_lib/badgeDashboardUtils.ts:122:        recordingId: form.recordingId,
app/api/admin/recordings/search/route.ts:1:import { NextResponse } from "next/server";
app/api/admin/recordings/search/route.ts:2:import { requireAdminMemberId } from "@/lib/adminAuth";
app/api/admin/recordings/search/route.ts:3:import { searchRecordingsForAdmin } from "@/lib/albums";
app/api/admin/recordings/search/route.ts:5:function asString(value: unknown): string | null {
app/api/admin/recordings/search/route.ts:9:function asLimit(value: unknown): number | undefined {
app/api/admin/recordings/search/route.ts:24:export async function GET(request: Request) {
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:2:export const PANEL_BORDER = "1px solid rgba(255,255,255,0.12)";
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:3:export const ROW_BORDER = "1px solid rgba(255,255,255,0.08)";
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:4:export const TEXT_PRIMARY = "rgba(255,255,255,0.92)";
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:5:export const TEXT_STRONG = "rgba(255,255,255,0.86)";
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:6:export const TEXT_MUTED = "rgba(255,255,255,0.68)";
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:7:export const TEXT_FAINT = "rgba(255,255,255,0.58)";
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:8:export const BG_PANEL = "rgba(255,255,255,0.04)";
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:9:export const BG_INSET = "rgba(255,255,255,0.02)";
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:10:export const BG_ACCENT = "rgba(255,255,255,0.08)";
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:11:export const BG_MEMBER = "rgba(255,255,255,0.78)";
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:12:export const BG_ANON = "rgba(255,255,255,0.22)";
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:13:export const FONT_SIZE_UI = 12;
app/admin/playback/dashboard/playbackTelemetryDashboardStyles.ts:14:export const FONT_SIZE_DEDUPE = 11;
app/admin/badges/_components/BadgeCatalogueSection.tsx:1:// web/app/admin/badges/_components/BadgeCatalogueSection.tsx
app/admin/badges/_components/BadgeCatalogueSection.tsx:4:import Image from "next/image";
app/admin/badges/_components/BadgeCatalogueSection.tsx:5:import React from "react";
app/admin/badges/_components/BadgeCatalogueSection.tsx:6:import {
app/admin/badges/_components/BadgeCatalogueSection.tsx:13:import type { BadgeDefinitionOption } from "../_lib/badgeDashboardTypes";
app/admin/badges/_components/BadgeCatalogueSection.tsx:14:import { getBadgeCardBorder } from "../_lib/badgeDashboardUtils";
app/admin/badges/_components/BadgeCatalogueSection.tsx:16:type Props = {
app/admin/badges/_components/BadgeCatalogueSection.tsx:17:  badges: BadgeDefinitionOption[];
app/admin/badges/_components/BadgeCatalogueSection.tsx:19:  onSelectEntitlementKey: (entitlementKey: string) => void;
app/admin/badges/_components/BadgeCatalogueSection.tsx:22:export function BadgeCatalogueSection(props: Props) {
app/admin/badges/_components/BadgeCatalogueSection.tsx:23:  const { badges, selectedEntitlementKey, onSelectEntitlementKey } = props;
app/admin/badges/_components/BadgeCatalogueSection.tsx:54:            Badge catalogue
app/admin/badges/_components/BadgeCatalogueSection.tsx:65:            Browse active badge definitions and select one to preview or award.
app/admin/badges/_components/BadgeCatalogueSection.tsx:76:          {badges.length.toLocaleString()} active badge
app/admin/badges/_components/BadgeCatalogueSection.tsx:77:          {badges.length === 1 ? "" : "s"}
app/admin/badges/_components/BadgeCatalogueSection.tsx:89:        {badges.length === 0 ? (
app/admin/badges/_components/BadgeCatalogueSection.tsx:103:            No active badge definitions found.
app/admin/badges/_components/BadgeCatalogueSection.tsx:106:          badges.map((badge) => {
app/admin/badges/_components/BadgeCatalogueSection.tsx:107:            const isSelected = badge.entitlementKey === selectedEntitlementKey;
app/admin/badges/_components/BadgeCatalogueSection.tsx:111:                key={badge.entitlementKey}
app/admin/badges/_components/BadgeCatalogueSection.tsx:113:                onClick={() => onSelectEntitlementKey(badge.entitlementKey)}
app/admin/badges/_components/BadgeCatalogueSection.tsx:114:                title={badge.description || badge.title}
app/admin/badges/_components/BadgeCatalogueSection.tsx:122:                  border: getBadgeCardBorder(isSelected, badge.featured),
app/admin/badges/_components/BadgeCatalogueSection.tsx:139:                {badge.imageUrl ? (
app/admin/badges/_components/BadgeCatalogueSection.tsx:151:                      src={badge.imageUrl}
app/admin/badges/_components/BadgeCatalogueSection.tsx:152:                      alt={badge.title}
app/admin/badges/_components/BadgeCatalogueSection.tsx:195:                    {badge.title}
app/admin/badges/_components/BadgeCatalogueSection.tsx:206:                    {badge.entitlementKey}
app/admin/badges/_components/BadgeCatalogueSection.tsx:216:                    #{badge.displayOrder}
app/admin/badges/_components/BadgeCatalogueSection.tsx:224:                        badge.awardMode === "automatic"
app/admin/badges/_components/BadgeCatalogueSection.tsx:229:                    {badge.awardMode === "automatic"
app/admin/badges/_components/BadgeCatalogueSection.tsx:230:                      ? badge.autoQualificationMode
app/admin/badges/_components/BadgeCatalogueSection.tsx:231:                        ? `auto · ${badge.autoQualificationMode}`
app/admin/badges/_lib/badgeDashboardTypes.ts:1:// web/app/admin/badges/_lib/badgeDashboardTypes.ts
app/admin/badges/_lib/badgeDashboardTypes.ts:2:import type { BadgeQualificationMode } from "@/lib/badgePreviewModes";
app/admin/badges/_lib/badgeDashboardTypes.ts:3:import type { BadgeAwardMode } from "@/lib/badges";
app/admin/badges/_lib/badgeDashboardTypes.ts:5:export type BadgeDefinitionOption = {
app/admin/badges/_lib/badgeDashboardTypes.ts:6:  entitlementKey: string;
app/admin/badges/_lib/badgeDashboardTypes.ts:14:  awardMode: BadgeAwardMode;
app/admin/badges/_lib/badgeDashboardTypes.ts:16:  autoQualificationMode: BadgeQualificationMode | null;
app/admin/badges/_lib/badgeDashboardTypes.ts:19:export type PreviewRow = {
app/admin/badges/_lib/badgeDashboardTypes.ts:35:export type PreviewResponse = {
app/admin/badges/_lib/badgeDashboardTypes.ts:42:export type AwardResponse = {
app/admin/badges/_lib/badgeDashboardTypes.ts:45:    entitlementKey: string;
app/admin/badges/_lib/badgeDashboardTypes.ts:59:export type Props = {
app/admin/badges/_lib/badgeDashboardTypes.ts:61:  badgeDefinitions: BadgeDefinitionOption[];
app/admin/badges/_lib/badgeDashboardTypes.ts:64:export type RecordingSearchResult = {
app/admin/badges/_lib/badgeDashboardTypes.ts:65:  recordingId: string;
app/admin/badges/_lib/badgeDashboardTypes.ts:72:export type RecordingSearchResponse = {
app/admin/badges/_lib/badgeDashboardTypes.ts:78:export type FormState = {
app/admin/badges/_lib/badgeDashboardTypes.ts:79:  entitlementKey: string;
app/admin/badges/_lib/badgeDashboardTypes.ts:80:  mode: BadgeQualificationMode;
app/admin/badges/_lib/badgeDashboardTypes.ts:91:  recordingId: string;
app/admin/badges/_lib/badgeDashboardTypes.ts:96:export type SelectedModeInputRequirements = {
app/admin/badges/_lib/badgeDashboardTypes.ts:105:  recordingId: boolean;
app/admin/badges/_lib/badgeDashboardTypes.ts:108:export type SelectedModeFieldText = {
app/admin/badges/_lib/badgeDashboardTypes.ts:127:  recordingIdLabel: string;
app/admin/badges/_lib/badgeDashboardTypes.ts:128:  recordingIdPlaceholder: string;
app/admin/badges/_lib/badgeDashboardTypes.ts:129:  recordingIdHelp: string | null;
app/home/badges/BadgeAwardRevealCard.tsx:1:// web/app/home/badges/BadgeAwardRevealCard.tsx
app/home/badges/BadgeAwardRevealCard.tsx:4:import React from "react";
app/home/badges/BadgeAwardRevealCard.tsx:5:import BadgeUnlockVisual from "../modules/badges/BadgeUnlockVisual";
app/home/badges/BadgeAwardRevealCard.tsx:6:import BadgeUnlockVisualStyles from "../modules/badges/BadgeUnlockVisualStyles";
app/home/badges/BadgeAwardRevealCard.tsx:7:import type { BadgeAwardNotice } from "./badgeAwardTypes";
app/home/badges/BadgeAwardRevealCard.tsx:9:type Props = {
app/home/badges/BadgeAwardRevealCard.tsx:10:  badge: BadgeAwardNotice;
app/home/badges/BadgeAwardRevealCard.tsx:14:export default function BadgeAwardRevealCard(props: Props) {
app/home/badges/BadgeAwardRevealCard.tsx:15:  const { badge, dismissHintVisible } = props;
app/home/badges/BadgeAwardRevealCard.tsx:19:      <BadgeUnlockVisualStyles />
app/home/badges/BadgeAwardRevealCard.tsx:22:        @keyframes badgeAwardOverlayCardIn {
app/home/badges/BadgeAwardRevealCard.tsx:34:          .badge-award-overlay-card {
app/home/badges/BadgeAwardRevealCard.tsx:41:        className="badge-award-overlay-card"
app/home/badges/BadgeAwardRevealCard.tsx:60:            "badgeAwardOverlayCardIn 340ms cubic-bezier(0.22, 1, 0.36, 1) both",
app/home/badges/BadgeAwardRevealCard.tsx:72:          Badge unlocked
app/home/badges/BadgeAwardRevealCard.tsx:76:          className="portal-badge-unlock-host"
app/home/badges/BadgeAwardRevealCard.tsx:77:          data-badge-expanded="true"
app/home/badges/BadgeAwardRevealCard.tsx:110:            <BadgeUnlockVisual
app/home/badges/BadgeAwardRevealCard.tsx:111:              imageUrl={badge.imageUrl ?? null}
app/home/badges/BadgeAwardRevealCard.tsx:112:              label={badge.title}
app/home/badges/BadgeAwardRevealCard.tsx:137:            {badge.title}
app/home/badges/BadgeAwardRevealCard.tsx:140:          {badge.description ? (
app/home/badges/BadgeAwardRevealCard.tsx:150:              {badge.description}
app/home/modules/badges/BadgeCabinetItem.tsx:1:// web/app/home/modules/badges/BadgeCabinetItem.tsx
app/home/modules/badges/BadgeCabinetItem.tsx:4:import React from "react";
app/home/modules/badges/BadgeCabinetItem.tsx:5:import type { BadgeCabinetItemModel } from "./badgeCabinetTypes";
app/home/modules/badges/BadgeCabinetItem.tsx:6:import BadgeUnlockVisual from "./BadgeUnlockVisual";
app/home/modules/badges/BadgeCabinetItem.tsx:8:type Props = Readonly<{
app/home/modules/badges/BadgeCabinetItem.tsx:9:  item: BadgeCabinetItemModel;
app/home/modules/badges/BadgeCabinetItem.tsx:16:function revealingClassName(
app/home/modules/badges/BadgeCabinetItem.tsx:26:function BadgeVisual(
app/home/modules/badges/BadgeCabinetItem.tsx:28:    item: BadgeCabinetItemModel;
app/home/modules/badges/BadgeCabinetItem.tsx:34:  const accessibleTitle = item.unlocked ? item.titleText : "Locked badge";
app/home/modules/badges/BadgeCabinetItem.tsx:40:      className="portal-member-badge-visual"
app/home/modules/badges/BadgeCabinetItem.tsx:51:      <BadgeUnlockVisual
app/home/modules/badges/BadgeCabinetItem.tsx:63:function BadgeMetaQuestionMark(
app/home/modules/badges/BadgeCabinetItem.tsx:73:        "portal-member-badge-question-mark",
app/home/modules/badges/BadgeCabinetItem.tsx:74:        "portal-member-badge-question-mark--dissolving",
app/home/modules/badges/BadgeCabinetItem.tsx:92:function UnlockedBadgeMeta(
app/home/modules/badges/BadgeCabinetItem.tsx:94:    item: BadgeCabinetItemModel;
app/home/modules/badges/BadgeCabinetItem.tsx:103:        "portal-member-badge-meta-revealed",
app/home/modules/badges/BadgeCabinetItem.tsx:104:        "portal-member-badge-meta-revealed--revealing",
app/home/modules/badges/BadgeCabinetItem.tsx:110:          "portal-member-badge-title",
app/home/modules/badges/BadgeCabinetItem.tsx:111:          "portal-member-badge-title--revealing",
app/home/modules/badges/BadgeCabinetItem.tsx:130:            "portal-member-badge-description",
app/home/modules/badges/BadgeCabinetItem.tsx:131:            "portal-member-badge-description--revealing",
app/home/modules/badges/BadgeCabinetItem.tsx:149:function BadgeMeta(
app/home/modules/badges/BadgeCabinetItem.tsx:151:    item: BadgeCabinetItemModel;
app/home/modules/badges/BadgeCabinetItem.tsx:159:    <div className="portal-member-badge-meta" aria-hidden="false">
app/home/modules/badges/BadgeCabinetItem.tsx:160:      <div className="portal-member-badge-meta-inner">
app/home/modules/badges/BadgeCabinetItem.tsx:162:          <BadgeMetaQuestionMark isMetaRevealing={isMetaRevealing} />
app/home/modules/badges/BadgeCabinetItem.tsx:166:          <UnlockedBadgeMeta
app/home/modules/badges/BadgeCabinetItem.tsx:176:export default function BadgeCabinetItem(props: Props) {
app/home/modules/badges/BadgeCabinetItem.tsx:185:      className="portal-badge-unlock-host portal-member-badge-wrap portal-member-badge-shell"
app/home/modules/badges/BadgeCabinetItem.tsx:186:      data-badge-key={item.key}
app/home/modules/badges/BadgeCabinetItem.tsx:187:      data-badge-partition={item.partition}
app/home/modules/badges/BadgeCabinetItem.tsx:188:      data-badge-expanded={expanded ? "true" : "false"}
app/home/modules/badges/BadgeCabinetItem.tsx:189:      data-badge-newly-unlocked={isNewlyUnlocked ? "true" : "false"}
app/home/modules/badges/BadgeCabinetItem.tsx:190:      data-badge-unlocked={item.unlocked ? "true" : "false"}
app/home/modules/badges/BadgeCabinetItem.tsx:191:      data-badge-meta-revealing={isMetaRevealing ? "true" : "false"}
app/home/modules/badges/BadgeCabinetItem.tsx:197:      <BadgeVisual
app/home/modules/badges/BadgeCabinetItem.tsx:204:        <BadgeMeta item={item} isMetaRevealing={isMetaRevealing} />
app/api/admin/badges/award/route.ts:1:// web/app/api/admin/badges/award/route.ts
app/api/admin/badges/award/route.ts:2:import { NextResponse } from "next/server";
app/api/admin/badges/award/route.ts:3:import { requireAdminMemberId } from "@/lib/adminAuth";
app/api/admin/badges/award/route.ts:4:import { awardBadgeToMembers } from "@/lib/badgeAdmin";
app/api/admin/badges/award/route.ts:5:import { areBadgesEnabled } from "@/lib/badgeFeature";
app/api/admin/badges/award/route.ts:7:function isRecord(value: unknown): value is Record<string, unknown> {
app/api/admin/badges/award/route.ts:11:function asString(value: unknown): string | null {
app/api/admin/badges/award/route.ts:15:function asStringArray(value: unknown): string[] {
app/api/admin/badges/award/route.ts:29:export async function POST(request: Request) {
app/api/admin/badges/award/route.ts:33:    if (!areBadgesEnabled()) {
app/api/admin/badges/award/route.ts:37:          error: "Badges are currently disabled.",
app/api/admin/badges/award/route.ts:49:    const entitlementKey = asString(body.entitlementKey);
app/api/admin/badges/award/route.ts:50:    if (!entitlementKey) {
app/api/admin/badges/award/route.ts:51:      throw new Error("entitlementKey is required.");
app/api/admin/badges/award/route.ts:54:    if (!entitlementKey.startsWith("badge_")) {
app/api/admin/badges/award/route.ts:55:      throw new Error("entitlementKey must start with badge_.");
app/api/admin/badges/award/route.ts:64:    const grantSource = asString(body.grantSource) ?? "badge_admin_preview";
app/api/admin/badges/award/route.ts:67:    const result = await awardBadgeToMembers({
app/api/admin/badges/award/route.ts:68:      entitlementKey,
app/api/admin/badges/award/route.ts:88:      error instanceof Error ? error.message : "Unable to award badge.";
app/admin/badges/page.tsx:1:// web/app/admin/badges/page.tsx
app/admin/badges/page.tsx:2:import "server-only";
app/admin/badges/page.tsx:4:import BadgeDashboardClient from "./BadgeDashboardClient";
app/admin/badges/page.tsx:5:import { getActiveBadgeDefinitions } from "@/lib/badges";
app/admin/badges/page.tsx:7:export default async function Page(props: {
app/admin/badges/page.tsx:13:  const badgeDefinitions = await getActiveBadgeDefinitions();
app/admin/badges/page.tsx:15:  const badgeDefinitionOptions = badgeDefinitions.map((badge) => ({
app/admin/badges/page.tsx:16:    entitlementKey: badge.entitlementKey,
app/admin/badges/page.tsx:17:    title: badge.title,
app/admin/badges/page.tsx:18:    description: badge.description,
app/admin/badges/page.tsx:19:    displayOrder: badge.displayOrder,
app/admin/badges/page.tsx:20:    imageUrl: badge.imageUrl,
app/admin/badges/page.tsx:21:    featured: badge.featured,
app/admin/badges/page.tsx:22:    shareable: badge.shareable,
app/admin/badges/page.tsx:23:    undisclosed: badge.undisclosed,
app/admin/badges/page.tsx:24:    awardMode: badge.awardMode,
app/admin/badges/page.tsx:25:    autoAwardEnabled: badge.autoAward?.enabled ?? false,
app/admin/badges/page.tsx:26:    autoQualificationMode: badge.autoAward?.qualificationMode ?? null,
app/admin/badges/page.tsx:30:    <BadgeDashboardClient
app/admin/badges/page.tsx:32:      badgeDefinitions={badgeDefinitionOptions}
app/home/modules/PortalMemberPanel.tsx:4:import React from "react";
app/home/modules/PortalMemberPanel.tsx:5:import type { PortalMemberSummary } from "@/lib/memberDashboard";
app/home/modules/PortalMemberPanel.tsx:6:import BadgeCabinet from "./badges/BadgeCabinet";
app/home/modules/PortalMemberPanel.tsx:8:type Props = {
app/home/modules/PortalMemberPanel.tsx:14:function MetricTable(props: {
app/home/modules/PortalMemberPanel.tsx:77:export default function PortalMemberPanel(props: Props) {
app/home/modules/PortalMemberPanel.tsx:119:            <BadgeCabinet badges={summary.badges} />
app/admin/badges/_components/BadgeQualificationFormSection.tsx:1:// web/app/admin/badges/_components/BadgeQualificationFormSection.tsx
app/admin/badges/_components/BadgeQualificationFormSection.tsx:4:import React from "react";
app/admin/badges/_components/BadgeQualificationFormSection.tsx:5:import {
app/admin/badges/_components/BadgeQualificationFormSection.tsx:7:  type BadgeQualificationMode,
app/admin/badges/_components/BadgeQualificationFormSection.tsx:8:} from "@/lib/badgePreviewModes";
app/admin/badges/_components/BadgeQualificationFormSection.tsx:9:import {
app/admin/badges/_components/BadgeQualificationFormSection.tsx:16:import type {
app/admin/badges/_components/BadgeQualificationFormSection.tsx:17:  BadgeDefinitionOption,
app/admin/badges/_components/BadgeQualificationFormSection.tsx:22:} from "../_lib/badgeDashboardTypes";
app/admin/badges/_components/BadgeQualificationFormSection.tsx:23:import { RecordingPicker } from "./RecordingPicker";
app/admin/badges/_components/BadgeQualificationFormSection.tsx:25:type ModeDescriptor = {
app/admin/badges/_components/BadgeQualificationFormSection.tsx:34:type Props = {
app/admin/badges/_components/BadgeQualificationFormSection.tsx:35:  badges: BadgeDefinitionOption[];
app/admin/badges/_components/BadgeQualificationFormSection.tsx:37:  selectedBadge: BadgeDefinitionOption | null;
app/admin/badges/_components/BadgeQualificationFormSection.tsx:64:const labelTextStyle: React.CSSProperties = {
app/admin/badges/_components/BadgeQualificationFormSection.tsx:71:const helpTextStyle: React.CSSProperties = {
app/admin/badges/_components/BadgeQualificationFormSection.tsx:77:const controlStyle: React.CSSProperties = {
app/admin/badges/_components/BadgeQualificationFormSection.tsx:89:const actionButtonBaseStyle: React.CSSProperties = {
app/admin/badges/_components/BadgeQualificationFormSection.tsx:99:function Field(props: {
app/admin/badges/_components/BadgeQualificationFormSection.tsx:115:export function BadgeQualificationFormSection(props: Props) {
app/admin/badges/_components/BadgeQualificationFormSection.tsx:117:    badges,
app/admin/badges/_components/BadgeQualificationFormSection.tsx:161:          Award badge
app/admin/badges/_components/BadgeQualificationFormSection.tsx:172:          Choose a badge, define a qualifying cohort, preview matching members,
app/admin/badges/_components/BadgeQualificationFormSection.tsx:184:        <Field label="Badge">
app/admin/badges/_components/BadgeQualificationFormSection.tsx:186:            value={form.entitlementKey}
app/admin/badges/_components/BadgeQualificationFormSection.tsx:188:              onFormChange("entitlementKey", event.target.value)
app/admin/badges/_components/BadgeQualificationFormSection.tsx:192:            {badges.map((badge) => (
app/admin/badges/_components/BadgeQualificationFormSection.tsx:193:              <option key={badge.entitlementKey} value={badge.entitlementKey}>
app/admin/badges/_components/BadgeQualificationFormSection.tsx:194:                {badge.title} ({badge.entitlementKey})
app/admin/badges/_components/BadgeQualificationFormSection.tsx:204:              onFormChange("mode", event.target.value as BadgeQualificationMode)
app/admin/badges/_components/BadgeQualificationFormSection.tsx:421:      {modeInputs.recordingId ? (
app/admin/badges/_components/BadgeQualificationFormSection.tsx:423:          label={modeFieldText.recordingIdLabel}
app/admin/badges/_components/BadgeQualificationFormSection.tsx:424:          helpText={modeFieldText.recordingIdHelp}
app/admin/badges/_components/BadgeQualificationFormSection.tsx:430:          selectedRecordingId={form.recordingId}
app/admin/badges/_components/BadgeQualificationFormSection.tsx:490:          {awardLoading ? "Awarding…" : "Award badge"}
app/home/modules/badges/useFlipGridAnimation.ts:1:// web/app/home/modules/badges/useFlipGridAnimation.ts
app/home/modules/badges/useFlipGridAnimation.ts:4:import React from "react";
app/home/modules/badges/useFlipGridAnimation.ts:6:type RegisterItemRef = (key: string) => (node: HTMLDivElement | null) => void;
app/home/modules/badges/useFlipGridAnimation.ts:8:type Options = {
app/home/modules/badges/useFlipGridAnimation.ts:17:type LayoutRect = {
app/home/modules/badges/useFlipGridAnimation.ts:24:type RectMap = Map<string, LayoutRect>;
app/home/modules/badges/useFlipGridAnimation.ts:26:type CleanupFn = () => void;
app/home/modules/badges/useFlipGridAnimation.ts:28:function resetNodeTransform(node: HTMLDivElement): void {
app/home/modules/badges/useFlipGridAnimation.ts:34:function getRelativeRect(node: HTMLDivElement): LayoutRect {
app/home/modules/badges/useFlipGridAnimation.ts:57:function snapshotRects(
app/home/modules/badges/useFlipGridAnimation.ts:72:function isReasonableDelta(
app/home/modules/badges/useFlipGridAnimation.ts:88:export function useFlipGridAnimation(options: Options): {
app/home/modules/badges/BadgeCabinet.tsx:1:// web/app/home/modules/badges/BadgeCabinet.tsx
app/home/modules/badges/BadgeCabinet.tsx:4:import React from "react";
app/home/modules/badges/BadgeCabinet.tsx:5:import type { MemberDashboardBadge } from "@/lib/memberDashboard";
app/home/modules/badges/BadgeCabinet.tsx:6:import type { BadgeAwardNotice } from "@/app/home/badges/badgeAwardTypes";
app/home/modules/badges/BadgeCabinet.tsx:7:import { useBadgeAwardOverlay } from "@/app/home/badges/BadgeAwardOverlayProvider";
app/home/modules/badges/BadgeCabinet.tsx:8:import BadgeCabinetGrid from "./BadgeCabinetGrid";
app/home/modules/badges/BadgeCabinet.tsx:9:import BadgeCabinetItem from "./BadgeCabinetItem";
app/home/modules/badges/BadgeCabinet.tsx:10:import BadgeCabinetStyles from "./BadgeCabinetStyles";
app/home/modules/badges/BadgeCabinet.tsx:11:import BadgeUnlockVisualStyles from "./BadgeUnlockVisualStyles";
app/home/modules/badges/BadgeCabinet.tsx:12:import { buildBadgeCabinetItems } from "./badgeCabinetViewModel";
app/home/modules/badges/BadgeCabinet.tsx:13:import type { BadgeCabinetItemModel } from "./badgeCabinetTypes";
app/home/modules/badges/BadgeCabinet.tsx:14:import { useBadgeCabinetUnlockSequence } from "./useBadgeCabinetUnlockSequence";
app/home/modules/badges/BadgeCabinet.tsx:15:import { useFlipGridAnimation } from "./useFlipGridAnimation";
app/home/modules/badges/BadgeCabinet.tsx:16:import { usePrefersReducedMotion } from "./usePrefersReducedMotion";
app/home/modules/badges/BadgeCabinet.tsx:18:type Props = Readonly<{
app/home/modules/badges/BadgeCabinet.tsx:19:  badges: MemberDashboardBadge[];
app/home/modules/badges/BadgeCabinet.tsx:22:type DebugPanelProps = Readonly<{
app/home/modules/badges/BadgeCabinet.tsx:23:  candidateItems: BadgeCabinetItemModel[];
app/home/modules/badges/BadgeCabinet.tsx:34:const SHOW_DEBUG_PANEL = false;
app/home/modules/badges/BadgeCabinet.tsx:35:const DEBUG_REPLAY_RESET_MS = 72;
app/home/modules/badges/BadgeCabinet.tsx:37:function pickRandomItem<T>(items: T[]): T | null {
app/home/modules/badges/BadgeCabinet.tsx:43:function toBadgeAwardNotice(item: BadgeCabinetItemModel): BadgeAwardNotice {
app/home/modules/badges/BadgeCabinet.tsx:45:    entitlementKey: item.key,
app/home/modules/badges/BadgeCabinet.tsx:54:function buildEffectiveBadges(
app/home/modules/badges/BadgeCabinet.tsx:55:  badges: MemberDashboardBadge[],
app/home/modules/badges/BadgeCabinet.tsx:57:): MemberDashboardBadge[] {
app/home/modules/badges/BadgeCabinet.tsx:58:  if (!debugUnlockedKey) return badges;
app/home/modules/badges/BadgeCabinet.tsx:60:  return badges.map((badge) => {
app/home/modules/badges/BadgeCabinet.tsx:61:    if (badge.key !== debugUnlockedKey) return badge;
app/home/modules/badges/BadgeCabinet.tsx:64:      ...badge,
app/home/modules/badges/BadgeCabinet.tsx:71:function useAdminDebugFlag(): boolean {
app/home/modules/badges/BadgeCabinet.tsx:97:function useDebugSelection(
app/home/modules/badges/BadgeCabinet.tsx:98:  candidateItems: BadgeCabinetItemModel[],
app/home/modules/badges/BadgeCabinet.tsx:122:function useDebugReplay(): Readonly<{
app/home/modules/badges/BadgeCabinet.tsx:124:  replayDebugUnlock: (badgeKey: string | null) => void;
app/home/modules/badges/BadgeCabinet.tsx:144:    (badgeKey: string | null) => {
app/home/modules/badges/BadgeCabinet.tsx:145:      if (!badgeKey) return;
app/home/modules/badges/BadgeCabinet.tsx:151:        setDebugUnlockedKey(badgeKey);
app/home/modules/badges/BadgeCabinet.tsx:170:function BadgeCabinetToggle(
app/home/modules/badges/BadgeCabinet.tsx:209:        Badges
app/home/modules/badges/BadgeCabinet.tsx:232:function DebugActionButton(
app/home/modules/badges/BadgeCabinet.tsx:274:function BadgeCabinetDebugPanel(props: DebugPanelProps) {
app/home/modules/badges/BadgeCabinet.tsx:341:            <option value="">No locked badges available</option>
app/home/modules/badges/BadgeCabinet.tsx:401:export default function BadgeCabinet(props: Props) {
app/home/modules/badges/BadgeCabinet.tsx:402:  const { badges } = props;
app/home/modules/badges/BadgeCabinet.tsx:407:  const { announceBadge, resetOverlayDebugState } = useBadgeAwardOverlay();
app/home/modules/badges/BadgeCabinet.tsx:410:    () => buildBadgeCabinetItems(badges),
app/home/modules/badges/BadgeCabinet.tsx:411:    [badges],
app/home/modules/badges/BadgeCabinet.tsx:426:  const effectiveBadges = React.useMemo(
app/home/modules/badges/BadgeCabinet.tsx:427:    () => buildEffectiveBadges(badges, debugUnlockedKey),
app/home/modules/badges/BadgeCabinet.tsx:428:    [badges, debugUnlockedKey],
app/home/modules/badges/BadgeCabinet.tsx:431:    () => buildBadgeCabinetItems(effectiveBadges),
app/home/modules/badges/BadgeCabinet.tsx:432:    [effectiveBadges],
app/home/modules/badges/BadgeCabinet.tsx:445:  } = useBadgeCabinetUnlockSequence({ items });
app/home/modules/badges/BadgeCabinet.tsx:474:    (badgeKey: string | null) => {
app/home/modules/badges/BadgeCabinet.tsx:475:      if (!badgeKey) return;
app/home/modules/badges/BadgeCabinet.tsx:477:      const item = debugCandidateByKey.get(badgeKey);
app/home/modules/badges/BadgeCabinet.tsx:478:      if (item) announceBadge(toBadgeAwardNotice(item));
app/home/modules/badges/BadgeCabinet.tsx:480:    [announceBadge, debugCandidateByKey],
app/home/modules/badges/BadgeCabinet.tsx:496:    announceBadge(toBadgeAwardNotice(randomItem));
app/home/modules/badges/BadgeCabinet.tsx:497:  }, [announceBadge, debugCandidateItems, setDebugSelectedKey]);
app/home/modules/badges/BadgeCabinet.tsx:511:      <BadgeCabinetStyles />
app/home/modules/badges/BadgeCabinet.tsx:512:      <BadgeUnlockVisualStyles />
app/home/modules/badges/BadgeCabinet.tsx:524:          className="portal-member-badge-live-region"
app/home/modules/badges/BadgeCabinet.tsx:536:          <BadgeCabinetToggle
app/home/modules/badges/BadgeCabinet.tsx:543:            <BadgeCabinetDebugPanel
app/home/modules/badges/BadgeCabinet.tsx:557:        <BadgeCabinetGrid expanded={expanded}>
app/home/modules/badges/BadgeCabinet.tsx:559:            <BadgeCabinetItem
app/home/modules/badges/BadgeCabinet.tsx:568:        </BadgeCabinetGrid>
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1:// web/app/home/modules/badges/BadgeUnlockVisualStyles.tsx
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:4:import React from "react";
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:6:export default function BadgeUnlockVisualStyles() {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:9:      @keyframes portalBadgeLockedPulse {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:21:      @keyframes portalBadgeUnlockedIdleGlow {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:33:      @keyframes portalBadgeInnerAuraPulse {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:45:      @keyframes portalBadgeCentreRadiance {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:57:      @keyframes portalBadgeEmberRiseA {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:82:      @keyframes portalBadgeEmberRiseB {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:108:      @keyframes portalBadgeEmberRiseC {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:132:      @keyframes portalBadgeEmberBurstA {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:157:      @keyframes portalBadgeEmberBurstB {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:182:      @keyframes portalBadgeEmberBurstC {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:205:      @keyframes portalBadgeSpinHeavy {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:228:      @keyframes portalBadgeSpinFaster {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:252:      @keyframes portalBadgeSpinFastest {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:276:      @keyframes portalBadgeSpinSettle {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:328:      @keyframes portalBadgeRevealLayerActivation {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:340:      @keyframes portalBadgeUnlockEnergyFlare {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:366:      @keyframes portalBadgeEdgeSpinePresence {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:389:      @keyframes portalBadgeQuarterGlintStage1 {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:408:      @keyframes portalBadgeQuarterGlintStage2 {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:427:      @keyframes portalBadgeQuarterGlintStage3 {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:446:      @keyframes portalBadgeImpactFlash {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:475:      @keyframes portalBadgeFinalShimmerCelebrating {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:497:      @keyframes portalBadgeMetaQuestionDissolve {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:510:      @keyframes portalBadgeMetaTitleReveal {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:523:      @keyframes portalBadgeMetaDescriptionReveal {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:536:      .portal-badge-unlock-visual {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:546:      .portal-badge-unlock-visual-inner {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:549:        transform: scale(var(--portal-badge-art-scale-collapsed));
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:555:      .portal-badge-unlock-host[data-badge-expanded="true"]
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:556:        .portal-badge-unlock-visual-inner {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:557:        transform: scale(var(--portal-badge-art-scale-expanded));
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:560:      .portal-badge-spin-stage-1,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:561:      .portal-badge-spin-stage-2,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:562:      .portal-badge-spin-stage-3,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:563:      .portal-badge-art-shell {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:571:      .portal-badge-spin-stage-1--unlocking {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:572:        animation: portalBadgeSpinHeavy 1480ms linear both;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:575:      .portal-badge-spin-stage-2--unlocking {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:576:        animation: portalBadgeSpinFaster 900ms linear 1480ms both;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:579:      .portal-badge-spin-stage-3--unlocking {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:580:        animation: portalBadgeSpinFastest 560ms linear 2380ms both;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:583:      .portal-badge-art-shell--unlocking {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:584:        animation: portalBadgeSpinSettle 3060ms cubic-bezier(0.22, 1, 0.36, 1)
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:588:      .portal-badge-art-shell img,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:589:      .portal-badge-art-shell span,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:590:      .portal-badge-art-shell div:not(.portal-badge-edge-spine) {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:596:      .portal-badge-art-base-greyscale {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:601:      .portal-badge-reveal-canvas-layer {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:610:      .portal-badge-reveal-canvas-layer--unlocking {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:611:        animation: portalBadgeRevealLayerActivation 600ms
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:615:      .portal-badge-quarter-glint {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:638:      .portal-badge-quarter-glint--stage-1 {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:639:        animation: portalBadgeQuarterGlintStage1 1480ms linear both;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:642:      .portal-badge-quarter-glint--stage-2 {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:643:        animation: portalBadgeQuarterGlintStage2 900ms linear 1480ms both;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:646:      .portal-badge-quarter-glint--stage-3 {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:647:        animation: portalBadgeQuarterGlintStage3 560ms linear 2380ms both;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:650:      .portal-badge-edge-spine {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:669:      .portal-badge-edge-spine--left {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:673:      .portal-badge-edge-spine--right {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:677:      .portal-badge-edge-spine--left,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:678:      .portal-badge-edge-spine--right {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:679:        animation: portalBadgeEdgeSpinePresence 3060ms linear both;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:682:      .portal-badge-unlock-energy-flare {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:695:        animation: portalBadgeUnlockEnergyFlare 920ms
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:699:      .portal-badge-core--locked {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:700:        animation: portalBadgeLockedPulse 2400ms ease-in-out infinite;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:705:      .portal-badge-idle-glow {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:706:        animation: portalBadgeUnlockedIdleGlow 3200ms ease-in-out infinite;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:710:      .portal-badge-inner-aura {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:711:        animation: portalBadgeInnerAuraPulse 3600ms ease-in-out infinite;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:715:      .portal-badge-centre-radiance {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:716:        animation: portalBadgeCentreRadiance 3400ms ease-in-out infinite;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:720:      .portal-badge-unlock-host:hover .portal-badge-idle-glow,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:721:      .portal-badge-unlock-host:focus-within .portal-badge-idle-glow {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:726:      .portal-badge-embers {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:733:      .portal-badge-unlock-host:hover .portal-badge-embers,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:734:      .portal-badge-unlock-host:focus-within .portal-badge-embers {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:739:      .portal-badge-spark-a,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:740:      .portal-badge-spark-b,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:741:      .portal-badge-spark-c,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:742:      .portal-badge-burst-a,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:743:      .portal-badge-burst-b,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:744:      .portal-badge-burst-c {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:776:      .portal-badge-spark-a::before,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:777:      .portal-badge-spark-b::before,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:778:      .portal-badge-spark-c::before,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:779:      .portal-badge-burst-a::before,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:780:      .portal-badge-burst-b::before,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:781:      .portal-badge-burst-c::before {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:806:      .portal-badge-spark-a::after,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:807:      .portal-badge-spark-b::after,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:808:      .portal-badge-spark-c::after,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:809:      .portal-badge-burst-a::after,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:810:      .portal-badge-burst-b::after,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:811:      .portal-badge-burst-c::after {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:839:      .portal-badge-spark-a {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:840:        animation: portalBadgeEmberRiseA 1300ms
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:844:      .portal-badge-spark-b {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:845:        animation: portalBadgeEmberRiseB 1600ms
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:849:      .portal-badge-spark-c {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:850:        animation: portalBadgeEmberRiseC 1450ms cubic-bezier(0.25, 0.68, 0.3, 1)
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:854:      .portal-badge-spark-a,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:855:      .portal-badge-burst-a {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:859:      .portal-badge-spark-b,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:860:      .portal-badge-burst-b {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:864:      .portal-badge-spark-c,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:865:      .portal-badge-burst-c {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:869:      .portal-badge-unlock-host:hover .portal-badge-spark-a,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:870:      .portal-badge-unlock-host:focus-within .portal-badge-spark-a {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:871:        animation: portalBadgeEmberBurstA 950ms cubic-bezier(0.2, 0.72, 0.28, 1)
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:875:      .portal-badge-unlock-host:hover .portal-badge-spark-b,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:876:      .portal-badge-unlock-host:focus-within .portal-badge-spark-b {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:877:        animation: portalBadgeEmberBurstB 1100ms
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:881:      .portal-badge-unlock-host:hover .portal-badge-spark-c,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:882:      .portal-badge-unlock-host:focus-within .portal-badge-spark-c {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:883:        animation: portalBadgeEmberBurstC 1000ms cubic-bezier(0.24, 0.7, 0.3, 1)
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:887:      .portal-badge-unlock-host:hover .portal-badge-burst-a,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:888:      .portal-badge-unlock-host:focus-within .portal-badge-burst-a {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:889:        animation: portalBadgeEmberBurstA 820ms cubic-bezier(0.2, 0.74, 0.28, 1)
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:893:      .portal-badge-unlock-host:hover .portal-badge-burst-b,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:894:      .portal-badge-unlock-host:focus-within .portal-badge-burst-b {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:895:        animation: portalBadgeEmberBurstB 900ms cubic-bezier(0.18, 0.76, 0.3, 1)
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:899:      .portal-badge-unlock-host:hover .portal-badge-burst-c,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:900:      .portal-badge-unlock-host:focus-within .portal-badge-burst-c {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:901:        animation: portalBadgeEmberBurstC 860ms cubic-bezier(0.22, 0.72, 0.3, 1)
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:905:      .portal-badge-final-art-shell {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:910:      .portal-badge-final-art-image {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:915:      .portal-badge-final-shimmer {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:919:      .portal-badge-final-shimmer::before {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:939:      .portal-badge-final-shimmer--celebrating::before {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:940:        animation: portalBadgeFinalShimmerCelebrating 3000ms
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:944:      .portal-member-badge-meta-inner {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:949:      .portal-badge-unlock-host[data-badge-expanded="true"]
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:950:        .portal-member-badge-meta-inner {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:954:      .portal-member-badge-question-mark {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:964:      .portal-badge-unlock-host[data-badge-expanded="true"]
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:965:        .portal-member-badge-question-mark {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:969:      .portal-member-badge-question-mark--dissolving {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:976:        animation: portalBadgeMetaQuestionDissolve 300ms
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:980:      .portal-member-badge-meta-revealed {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:985:      .portal-member-badge-meta-revealed--revealing {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:991:      .portal-member-badge-title,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:992:      .portal-member-badge-description {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:996:      .portal-member-badge-title--revealing {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:998:        animation: portalBadgeMetaTitleReveal 420ms
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1002:      .portal-member-badge-description--revealing {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1004:        animation: portalBadgeMetaDescriptionReveal 480ms
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1009:        .portal-badge-core--locked,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1010:        .portal-badge-idle-glow,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1011:        .portal-badge-inner-aura,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1012:        .portal-badge-centre-radiance,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1013:        .portal-badge-spark-a,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1014:        .portal-badge-spark-b,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1015:        .portal-badge-spark-c,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1016:        .portal-badge-burst-a,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1017:        .portal-badge-burst-b,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1018:        .portal-badge-burst-c,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1019:        .portal-badge-spin-stage-1--unlocking,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1020:        .portal-badge-spin-stage-2--unlocking,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1021:        .portal-badge-spin-stage-3--unlocking,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1022:        .portal-badge-art-shell--unlocking,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1023:        .portal-badge-reveal-canvas-layer--unlocking,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1024:        .portal-badge-unlock-energy-flare,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1025:        .portal-badge-impact-flash,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1026:        .portal-badge-impact-particle,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1027:        .portal-badge-final-shimmer::before {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1031:        .portal-badge-embers {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1035:        .portal-badge-unlock-visual-inner {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1039:        .portal-badge-reveal-canvas-layer {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1044:        .portal-member-badge-question-mark,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1045:        .portal-member-badge-title,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:1046:        .portal-member-badge-description {
lib/badgePreviewModes.ts:1:// web/lib/badgePreviewModes.ts
lib/badgePreviewModes.ts:2:export type BadgeQualificationMode =
lib/badgePreviewModes.ts:15:export type BadgePreviewModeDescriptor = {
lib/badgePreviewModes.ts:16:  key: BadgeQualificationMode;
lib/badgePreviewModes.ts:38:    recordingId: boolean;
lib/badgePreviewModes.ts:59:    recordingIdLabel: string;
lib/badgePreviewModes.ts:60:    recordingIdPlaceholder: string;
lib/badgePreviewModes.ts:61:    recordingIdHelp: string | null;
lib/badgePreviewModes.ts:65:export const BADGE_PREVIEW_MODE_DESCRIPTORS: Record<
lib/badgePreviewModes.ts:66:  BadgeQualificationMode,
lib/badgePreviewModes.ts:67:  BadgePreviewModeDescriptor
lib/badgePreviewModes.ts:85:      recordingId: false,
lib/badgePreviewModes.ts:107:      recordingIdLabel: "Recording ID",
lib/badgePreviewModes.ts:108:      recordingIdPlaceholder: "recording UUID",
lib/badgePreviewModes.ts:109:      recordingIdHelp: null,
lib/badgePreviewModes.ts:128:      recordingId: false,
lib/badgePreviewModes.ts:150:      recordingIdLabel: "Recording ID",
lib/badgePreviewModes.ts:151:      recordingIdPlaceholder: "recording UUID",
lib/badgePreviewModes.ts:152:      recordingIdHelp: null,
lib/badgePreviewModes.ts:171:      recordingId: false,
lib/badgePreviewModes.ts:193:      recordingIdLabel: "Recording ID",
lib/badgePreviewModes.ts:194:      recordingIdPlaceholder: "recording UUID",
lib/badgePreviewModes.ts:195:      recordingIdHelp: null,
lib/badgePreviewModes.ts:214:      recordingId: false,
lib/badgePreviewModes.ts:236:      recordingIdLabel: "Recording ID",
lib/badgePreviewModes.ts:237:      recordingIdPlaceholder: "recording UUID",
lib/badgePreviewModes.ts:238:      recordingIdHelp: null,
lib/badgePreviewModes.ts:258:      recordingId: false,
lib/badgePreviewModes.ts:283:      recordingIdLabel: "Recording ID",
lib/badgePreviewModes.ts:284:      recordingIdPlaceholder: "recording UUID",
lib/badgePreviewModes.ts:285:      recordingIdHelp: null,
lib/badgePreviewModes.ts:304:      recordingId: true,
lib/badgePreviewModes.ts:326:      recordingIdLabel: "Recording ID",
lib/badgePreviewModes.ts:327:      recordingIdPlaceholder: "recording UUID",
lib/badgePreviewModes.ts:328:      recordingIdHelp:
lib/badgePreviewModes.ts:348:      recordingId: true,
lib/badgePreviewModes.ts:370:      recordingIdLabel: "Recording ID",
lib/badgePreviewModes.ts:371:      recordingIdPlaceholder: "recording UUID",
lib/badgePreviewModes.ts:372:      recordingIdHelp:
lib/badgePreviewModes.ts:392:      recordingId: true,
lib/badgePreviewModes.ts:414:      recordingIdLabel: "Recording ID",
lib/badgePreviewModes.ts:415:      recordingIdPlaceholder: "recording UUID",
lib/badgePreviewModes.ts:416:      recordingIdHelp:
lib/badgePreviewModes.ts:437:      recordingId: false,
lib/badgePreviewModes.ts:459:      recordingIdLabel: "Recording ID",
lib/badgePreviewModes.ts:460:      recordingIdPlaceholder: "recording UUID",
lib/badgePreviewModes.ts:461:      recordingIdHelp: null,
lib/badgePreviewModes.ts:481:      recordingId: false,
lib/badgePreviewModes.ts:503:      recordingIdLabel: "Recording ID",
lib/badgePreviewModes.ts:504:      recordingIdPlaceholder: "recording UUID",
lib/badgePreviewModes.ts:505:      recordingIdHelp: null,
lib/badgePreviewModes.ts:525:      recordingId: false,
lib/badgePreviewModes.ts:546:      recordingIdLabel: "Recording ID",
lib/badgePreviewModes.ts:547:      recordingIdPlaceholder: "recording UUID",
lib/badgePreviewModes.ts:548:      recordingIdHelp: null,
lib/badgePreviewModes.ts:553:export const BADGE_PREVIEW_MODES: BadgePreviewModeDescriptor[] = [
lib/badgePreviewModes.ts:567:export function getBadgePreviewModeDescriptor(
lib/badgePreviewModes.ts:568:  mode: BadgeQualificationMode,
lib/badgePreviewModes.ts:569:): BadgePreviewModeDescriptor {
lib/badgePreviewModes.ts:573:export function isRecordingScopedBadgePreviewMode(
lib/badgePreviewModes.ts:574:  mode: BadgeQualificationMode,
app/home/badges/BadgeAwardOverlay.tsx:1:// web/app/home/badges/BadgeAwardOverlay.tsx
app/home/badges/BadgeAwardOverlay.tsx:4:import React from "react";
app/home/badges/BadgeAwardOverlay.tsx:5:import { createPortal } from "react-dom";
app/home/badges/BadgeAwardOverlay.tsx:6:import BadgeAwardRevealCard from "./BadgeAwardRevealCard";
app/home/badges/BadgeAwardOverlay.tsx:7:import type { BadgeAwardNotice } from "./badgeAwardTypes";
app/home/badges/BadgeAwardOverlay.tsx:9:type Props = {
app/home/badges/BadgeAwardOverlay.tsx:12:  badge: BadgeAwardNotice | null;
app/home/badges/BadgeAwardOverlay.tsx:17:function BodyPortal(props: { children: React.ReactNode }) {
app/home/badges/BadgeAwardOverlay.tsx:30:function useAdminRibbonAboveOverlay(active: boolean) {
app/home/badges/BadgeAwardOverlay.tsx:67:export default function BadgeAwardOverlay(props: Props) {
app/home/badges/BadgeAwardOverlay.tsx:71:    badge,
app/home/badges/BadgeAwardOverlay.tsx:78:  if (!active || !badge) return null;
app/home/badges/BadgeAwardOverlay.tsx:110:        aria-label="Dismiss badge award overlay"
app/home/badges/BadgeAwardOverlay.tsx:144:          <BadgeAwardRevealCard badge={badge} dismissHintVisible={visible} />
app/home/badges/badgeAwardTypes.ts:1:export type BadgeAwardNotice = {
app/home/badges/badgeAwardTypes.ts:2:  entitlementKey: string;
app/home/badges/badgeAwardTypes.ts:10:function asTrimmedString(value: unknown): string | null {
app/home/badges/badgeAwardTypes.ts:14:function asBoolean(value: unknown): boolean {
app/home/badges/badgeAwardTypes.ts:18:function isRecord(value: unknown): value is Record<string, unknown> {
app/home/badges/badgeAwardTypes.ts:22:export function normalizeBadgeAwardNotices(
app/home/badges/badgeAwardTypes.ts:24:): BadgeAwardNotice[] {
app/home/badges/badgeAwardTypes.ts:27:  const notices: BadgeAwardNotice[] = [];
app/home/badges/badgeAwardTypes.ts:32:    const entitlementKey = asTrimmedString(item.entitlementKey);
app/home/badges/badgeAwardTypes.ts:36:    if (!entitlementKey || !title || !unlockedAt) continue;
app/home/badges/badgeAwardTypes.ts:39:      entitlementKey,
app/home/modules/badges/badgeCabinetViewModel.ts:1:// web/app/home/modules/badges/badgeCabinetViewModel.ts
app/home/modules/badges/badgeCabinetViewModel.ts:2:import type { BadgeCabinetInputBadge, BadgeCabinetItemModel } from "./badgeCabinetTypes";
app/home/modules/badges/badgeCabinetViewModel.ts:4:function formatUnlockedAt(value?: string | null): string | null {
app/home/modules/badges/badgeCabinetViewModel.ts:17:function getBadgeTitle(props: {
app/home/modules/badges/badgeCabinetViewModel.ts:34:function normalizeBadge(
app/home/modules/badges/badgeCabinetViewModel.ts:35:  badge: BadgeCabinetInputBadge,
app/home/modules/badges/badgeCabinetViewModel.ts:37:): BadgeCabinetItemModel | null {
app/home/modules/badges/badgeCabinetViewModel.ts:38:  const key = typeof badge.key === "string" ? badge.key.trim() : "";
app/home/modules/badges/badgeCabinetViewModel.ts:39:  const label = typeof badge.label === "string" ? badge.label.trim() : "";
app/home/modules/badges/badgeCabinetViewModel.ts:44:    typeof badge.description === "string" && badge.description.trim()
app/home/modules/badges/badgeCabinetViewModel.ts:45:      ? badge.description.trim()
app/home/modules/badges/badgeCabinetViewModel.ts:49:    typeof badge.imageUrl === "string" && badge.imageUrl.trim()
app/home/modules/badges/badgeCabinetViewModel.ts:50:      ? badge.imageUrl.trim()
app/home/modules/badges/badgeCabinetViewModel.ts:53:  const unlockedAt = badge.unlocked ? formatUnlockedAt(badge.unlockedAt) : null;
app/home/modules/badges/badgeCabinetViewModel.ts:56:    typeof badge.editorialOrder === "number" && Number.isFinite(badge.editorialOrder)
app/home/modules/badges/badgeCabinetViewModel.ts:57:      ? badge.editorialOrder
app/home/modules/badges/badgeCabinetViewModel.ts:65:    unlocked: badge.unlocked === true,
app/home/modules/badges/badgeCabinetViewModel.ts:68:    partition: badge.unlocked ? "unlocked" : "locked",
app/home/modules/badges/badgeCabinetViewModel.ts:69:    titleText: getBadgeTitle({
app/home/modules/badges/badgeCabinetViewModel.ts:72:      unlocked: badge.unlocked === true,
app/home/modules/badges/badgeCabinetViewModel.ts:75:    shareable: badge.shareable === true,
app/home/modules/badges/badgeCabinetViewModel.ts:76:    undisclosed: badge.undisclosed === true,
app/home/modules/badges/badgeCabinetViewModel.ts:80:export function buildBadgeCabinetItems(
app/home/modules/badges/badgeCabinetViewModel.ts:81:  badges: BadgeCabinetInputBadge[],
app/home/modules/badges/badgeCabinetViewModel.ts:82:): BadgeCabinetItemModel[] {
app/home/modules/badges/badgeCabinetViewModel.ts:83:  const normalized = badges
app/home/modules/badges/badgeCabinetViewModel.ts:84:    .map((badge, index) => normalizeBadge(badge, index))
app/home/modules/badges/badgeCabinetViewModel.ts:85:    .filter((badge): badge is BadgeCabinetItemModel => badge !== null);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:1:// web/app/home/modules/badges/useBadgeCabinetUnlockSequence.ts
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:4:import React from "react";
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:5:import { flushSync } from "react-dom";
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:6:import type { BadgeCabinetItemModel } from "./badgeCabinetTypes";
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:8:const BADGE_UNLOCK_TIMELINE_MS = {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:12:   * Keep these aligned with BadgeUnlockVisualStyles.tsx.
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:39:function getUnlockVisualDurationMs(): number {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:50:const UNLOCK_VISUAL_DURATION_MS = getUnlockVisualDurationMs();
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:52:function sortBadgeCabinetItems(
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:53:  items: BadgeCabinetItemModel[],
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:54:): BadgeCabinetItemModel[] {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:72:function buildStagedUnlockItems(
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:73:  items: BadgeCabinetItemModel[],
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:74:  previousStableItems: BadgeCabinetItemModel[],
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:76:): BadgeCabinetItemModel[] {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:95:  return sortBadgeCabinetItems(stagedItems);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:98:type UnlockableBadgeItem = BadgeCabinetItemModel & {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:102:type Options = {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:103:  items: UnlockableBadgeItem[];
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:106:type Result = {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:107:  displayItems: UnlockableBadgeItem[];
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:118:export function useBadgeCabinetUnlockSequence(options: Options): Result {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:122:  const previousStableItemsRef = React.useRef<UnlockableBadgeItem[]>(items);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:139:    UnlockableBadgeItem[] | null
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:230:        .map((item) => `New badge unlocked: ${item.label}`)
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:235:      console.debug("[useBadgeCabinetUnlockSequence] unlock started", {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:249:          "[useBadgeCabinetUnlockSequence] unlock released to cabinet move",
app/home/badges/BadgeAwardOverlayProvider.tsx:1:// web/app/home/badges/BadgeAwardOverlayProvider.tsx
app/home/badges/BadgeAwardOverlayProvider.tsx:4:import React from "react";
app/home/badges/BadgeAwardOverlayProvider.tsx:5:import BadgeAwardOverlay from "./BadgeAwardOverlay";
app/home/badges/BadgeAwardOverlayProvider.tsx:6:import type { BadgeAwardNotice } from "./badgeAwardTypes";
app/home/badges/BadgeAwardOverlayProvider.tsx:8:type BadgeAwardOverlayContextValue = {
app/home/badges/BadgeAwardOverlayProvider.tsx:9:  announceBadge: (badge: BadgeAwardNotice) => void;
app/home/badges/BadgeAwardOverlayProvider.tsx:10:  announceBadges: (badges: BadgeAwardNotice[]) => void;
app/home/badges/BadgeAwardOverlayProvider.tsx:14:const BadgeAwardOverlayContext =
app/home/badges/BadgeAwardOverlayProvider.tsx:15:  React.createContext<BadgeAwardOverlayContextValue | null>(null);
app/home/badges/BadgeAwardOverlayProvider.tsx:17:const DISPLAY_MS = 5600;
app/home/badges/BadgeAwardOverlayProvider.tsx:18:const EXIT_MS = 220;
app/home/badges/BadgeAwardOverlayProvider.tsx:19:const RECENT_DEDUPE_WINDOW_MS = 10_000;
app/home/badges/BadgeAwardOverlayProvider.tsx:21:type ProviderProps = {
app/home/badges/BadgeAwardOverlayProvider.tsx:25:function dedupeIncomingBadges(
app/home/badges/BadgeAwardOverlayProvider.tsx:26:  badges: BadgeAwardNotice[],
app/home/badges/BadgeAwardOverlayProvider.tsx:28:): BadgeAwardNotice[] {
app/home/badges/BadgeAwardOverlayProvider.tsx:37:  const unique: BadgeAwardNotice[] = [];
app/home/badges/BadgeAwardOverlayProvider.tsx:40:  for (const badge of badges) {
app/home/badges/BadgeAwardOverlayProvider.tsx:41:    const key = badge.entitlementKey.trim();
app/home/badges/BadgeAwardOverlayProvider.tsx:47:    unique.push(badge);
app/home/badges/BadgeAwardOverlayProvider.tsx:53:export function BadgeAwardOverlayProvider(props: ProviderProps) {
app/home/badges/BadgeAwardOverlayProvider.tsx:56:  const [queue, setQueue] = React.useState<BadgeAwardNotice[]>([]);
app/home/badges/BadgeAwardOverlayProvider.tsx:57:  const [activeBadge, setActiveBadge] = React.useState<BadgeAwardNotice | null>(
app/home/badges/BadgeAwardOverlayProvider.tsx:89:      setActiveBadge(null);
app/home/badges/BadgeAwardOverlayProvider.tsx:94:  const announceBadges = React.useCallback((badges: BadgeAwardNotice[]) => {
app/home/badges/BadgeAwardOverlayProvider.tsx:95:    if (!Array.isArray(badges) || badges.length === 0) return;
app/home/badges/BadgeAwardOverlayProvider.tsx:98:      const next = dedupeIncomingBadges(badges, recentSeenRef);
app/home/badges/BadgeAwardOverlayProvider.tsx:104:  const announceBadge = React.useCallback(
app/home/badges/BadgeAwardOverlayProvider.tsx:105:    (badge: BadgeAwardNotice) => {
app/home/badges/BadgeAwardOverlayProvider.tsx:106:      announceBadges([badge]);
app/home/badges/BadgeAwardOverlayProvider.tsx:108:    [announceBadges],
app/home/badges/BadgeAwardOverlayProvider.tsx:116:    setActiveBadge(null);
app/home/badges/BadgeAwardOverlayProvider.tsx:120:    if (activeBadge !== null) return;
app/home/badges/BadgeAwardOverlayProvider.tsx:123:    const [nextBadge, ...rest] = queue;
app/home/badges/BadgeAwardOverlayProvider.tsx:125:    setActiveBadge(nextBadge);
app/home/badges/BadgeAwardOverlayProvider.tsx:129:      nextBadge.entitlementKey,
app/home/badges/BadgeAwardOverlayProvider.tsx:137:        setActiveBadge(null);
app/home/badges/BadgeAwardOverlayProvider.tsx:143:  }, [activeBadge, queue]);
app/home/badges/BadgeAwardOverlayProvider.tsx:145:  const contextValue = React.useMemo<BadgeAwardOverlayContextValue>(
app/home/badges/BadgeAwardOverlayProvider.tsx:147:      announceBadge,
app/home/badges/BadgeAwardOverlayProvider.tsx:148:      announceBadges,
app/home/badges/BadgeAwardOverlayProvider.tsx:151:    [announceBadge, announceBadges, resetOverlayDebugState],
app/home/badges/BadgeAwardOverlayProvider.tsx:155:    <BadgeAwardOverlayContext.Provider value={contextValue}>
app/home/badges/BadgeAwardOverlayProvider.tsx:158:      <BadgeAwardOverlay
app/home/badges/BadgeAwardOverlayProvider.tsx:159:        active={activeBadge !== null}
app/home/badges/BadgeAwardOverlayProvider.tsx:161:        badge={activeBadge}
app/home/badges/BadgeAwardOverlayProvider.tsx:164:    </BadgeAwardOverlayContext.Provider>
app/home/badges/BadgeAwardOverlayProvider.tsx:168:export function useBadgeAwardOverlay(): BadgeAwardOverlayContextValue {
app/home/badges/BadgeAwardOverlayProvider.tsx:169:  const value = React.useContext(BadgeAwardOverlayContext);
app/home/badges/BadgeAwardOverlayProvider.tsx:173:      "useBadgeAwardOverlay must be used within BadgeAwardOverlayProvider.",
lib/albums.ts:2:import { unstable_cache } from "next/cache";
lib/albums.ts:3:import { client } from "@/sanity/lib/client";
lib/albums.ts:4:import { urlFor } from "@/sanity/lib/image";
lib/albums.ts:5:import type {
lib/albums.ts:12:import {
lib/albums.ts:16:import { getRecordingPlayCountsByRecordingIds } from "@/lib/recordingListenTotals";
lib/albums.ts:18:export type AlbumDocTrack = {
lib/albums.ts:19:  recordingId: string;
lib/albums.ts:29:type AlbumDoc = {
lib/albums.ts:53:type TrackLyricsDoc = {
lib/albums.ts:54:  recordingId?: string;
lib/albums.ts:59:export async function getFeaturedAlbumSlugFromSanity(): Promise<{
lib/albums.ts:75:export type AlbumBrowseItem = {
lib/albums.ts:95:function normStr(v: unknown): string | undefined {
lib/albums.ts:101:function normTheme(v: unknown): string | undefined {
lib/albums.ts:106:function parseTierName(v: unknown): TierName | null {
lib/albums.ts:113:function parseTierNameArray(v: unknown): TierName[] {
lib/albums.ts:118:function slugifySeg(v: string): string {
lib/albums.ts:129:function fallbackDisplayId(args: {
lib/albums.ts:140:function uniqifyDisplayId(desired: string, used: Set<string>): string {
lib/albums.ts:151:export type NormalizedAlbumTrack = {
lib/albums.ts:152:  recordingId: string;
lib/albums.ts:163:export function normalizeAlbumTracks(
lib/albums.ts:173:    .filter((t) => typeof t?.recordingId === "string" && t.recordingId.trim())
lib/albums.ts:182:      const recordingId = t.recordingId.trim();
lib/albums.ts:191:        recordingId,
lib/albums.ts:204:type FeaturedAlbumSlugDoc = {
lib/albums.ts:209:const STATIC_CATALOGUE_REVALIDATE_SECONDS = 60 * 60;
lib/albums.ts:211:const featuredAlbumSlugQuery = `
lib/albums.ts:219:const albumBySlugQuery = `
lib/albums.ts:242:      recordingId,
lib/albums.ts:254:const lyricsByRecordingIdsQuery = `
lib/albums.ts:255:  *[_type == "lyrics" && recordingId in $recordingIds]{
lib/albums.ts:256:    recordingId,
lib/albums.ts:262:const albumBrowseQuery = `
lib/albums.ts:276:const getCachedFeaturedAlbumSlugDoc = unstable_cache(
lib/albums.ts:287:const getCachedAlbumDocumentBySlug = unstable_cache(
lib/albums.ts:298:const getCachedLyricsByRecordingIds = unstable_cache(
lib/albums.ts:299:  async (recordingIds: string[]): Promise<TrackLyricsDoc[]> => {
lib/albums.ts:300:    if (recordingIds.length === 0) return [];
lib/albums.ts:304:      { recordingIds },
lib/albums.ts:316:const getCachedAlbumBrowseDocs = unstable_cache(
lib/albums.ts:328:export type AlbumPlaybackAssetVerification = {
lib/albums.ts:336:export type AlbumPlaybackSessionAsset = {
lib/albums.ts:337:  recordingId: string;
lib/albums.ts:343:function normalizeAlbumScopeId(raw: string): string {
lib/albums.ts:349:export async function getAlbumPlaybackAssetsForSession(params: {
lib/albums.ts:379:        defined(recordingId) &&
lib/albums.ts:383:        recordingId,
lib/albums.ts:395:      recordingId?: string;
lib/albums.ts:409:      const recordingId = normStr(t.recordingId);
lib/albums.ts:413:      if (!recordingId || !displayId || !playbackId) continue;
lib/albums.ts:423:        recordingId,
lib/albums.ts:439:export async function verifyAlbumPlaybackAsset(params: {
lib/albums.ts:470:        recordingId,
lib/albums.ts:479:      recordingId?: string;
lib/albums.ts:488:    const recordingId = normStr(t.recordingId);
lib/albums.ts:495:    return recordingId === trackId || displayId === trackId;
lib/albums.ts:503:    matchedRecordingId: normStr(matched?.recordingId) ?? null,
lib/albums.ts:507:export async function getAlbumBySlug(slug: string): Promise<AlbumPlayerBundle> {
lib/albums.ts:584:    recordingId: t.recordingId,
lib/albums.ts:594:  const recordingIds = tracks
lib/albums.ts:595:    .map((t) => t.recordingId)
lib/albums.ts:596:    .filter((recordingId): recordingId is string => recordingId.length > 0);
lib/albums.ts:598:  const lyricRecordingIds = Array.from(new Set(recordingIds)).sort((a, b) =>
lib/albums.ts:603:    getRecordingPlayCountsByRecordingIds(recordingIds),
lib/albums.ts:609:    playCount: playCountsByRecordingId[t.recordingId] ?? 0,
lib/albums.ts:616:    const id = (d.recordingId ?? "").trim();
lib/albums.ts:634:export type AlbumCanonicalMetadata = {
lib/albums.ts:640:export type TrackCanonicalMetadata = {
lib/albums.ts:648:export async function getAlbumCanonicalMetadataBySlug(
lib/albums.ts:664:export async function getTrackCanonicalMetadataBySlugAndDisplayId(params: {
lib/albums.ts:690:export async function listAlbumsForBrowse(): Promise<AlbumBrowseItem[]> {
lib/albums.ts:707:export type RecordingSearchResult = {
lib/albums.ts:708:  recordingId: string;
lib/albums.ts:715:export async function searchRecordingsForAdmin(args: {
lib/albums.ts:732:          defined(recordingId) && (
lib/albums.ts:733:            recordingId == $exactId ||
lib/albums.ts:734:            recordingId match $wildcard ||
lib/albums.ts:745:        defined(recordingId) && (
lib/albums.ts:746:          recordingId == $exactId ||
lib/albums.ts:747:          recordingId match $wildcard ||
lib/albums.ts:752:        recordingId,
lib/albums.ts:765:        recordingId?: string;
lib/albums.ts:784:      const recordingId = normStr(match?.recordingId);
lib/albums.ts:787:      if (!recordingId || !title || seen.has(recordingId)) continue;
lib/albums.ts:788:      seen.add(recordingId);
lib/albums.ts:791:        recordingId,
lib/albums.ts:803:    const aId = a.recordingId.toLowerCase();
lib/albums.ts:804:    const bId = b.recordingId.toLowerCase();
lib/albums.ts:828:export async function getRecordingSummaryByRecordingId(
lib/albums.ts:829:  recordingId: string,
lib/albums.ts:831:  const id = normStr(recordingId);
lib/albums.ts:835:        *[_type == "album" && count(tracks[recordingId == $recordingId]) > 0][0]{
lib/albums.ts:840:      "track": tracks[recordingId == $recordingId][0]{
lib/albums.ts:841:        recordingId,
lib/albums.ts:854:      recordingId?: string;
lib/albums.ts:858:  } | null>(q, { recordingId: id });
lib/albums.ts:860:  const trackRecordingId = normStr(doc?.track?.recordingId);
lib/albums.ts:865:    recordingId: trackRecordingId,
lib/albums.ts:874:export type RecordingSummary = {
lib/albums.ts:875:  recordingId: string;
lib/albums.ts:883:export type AlbumEmailMeta = {
lib/albums.ts:890:export async function getAlbumEmailMetaBySlug(
app/home/modules/badges/badgeCabinetTypes.ts:1:// web/app/home/modules/badges/badgeCabinetTypes.ts
app/home/modules/badges/badgeCabinetTypes.ts:2:import type { MemberDashboardBadge } from "@/lib/memberDashboard";
app/home/modules/badges/badgeCabinetTypes.ts:4:export type BadgeCabinetInputBadge = MemberDashboardBadge;
app/home/modules/badges/badgeCabinetTypes.ts:6:export type BadgeCabinetItemModel = {
app/home/modules/badges/BuildRevealMaskCanvas.tsx:1:// web/app/home/modules/badges/BuildRevealMaskCanvas.tsx
app/home/modules/badges/BuildRevealMaskCanvas.tsx:4:import React from "react";
app/home/modules/badges/BuildRevealMaskCanvas.tsx:6:type Props = {
app/home/modules/badges/BuildRevealMaskCanvas.tsx:15:const CANVAS_SIZE = 192;
app/home/modules/badges/BuildRevealMaskCanvas.tsx:16:const TAU = Math.PI * 2;
app/home/modules/badges/BuildRevealMaskCanvas.tsx:18:function clamp(value: number, min: number, max: number): number {
app/home/modules/badges/BuildRevealMaskCanvas.tsx:22:function easeOutCubic(value: number): number {
app/home/modules/badges/BuildRevealMaskCanvas.tsx:27:function easeInOutQuad(value: number): number {
app/home/modules/badges/BuildRevealMaskCanvas.tsx:32:function hashString(value: string): number {
app/home/modules/badges/BuildRevealMaskCanvas.tsx:43:function drawRevealMaskFrame(
app/home/modules/badges/BuildRevealMaskCanvas.tsx:191:function buildSolidMaskDataUrl(): string {
app/home/modules/badges/BuildRevealMaskCanvas.tsx:205:export default function BuildRevealMaskCanvas(props: Props) {
app/api/admin/badges/preview/route.ts:1:// web/app/api/admin/badges/preview/route.ts
app/api/admin/badges/preview/route.ts:2:import { NextResponse } from "next/server";
app/api/admin/badges/preview/route.ts:3:import { requireAdminMemberId } from "@/lib/adminAuth";
app/api/admin/badges/preview/route.ts:4:import {
app/api/admin/badges/preview/route.ts:5:  previewBadgeQualification,
app/api/admin/badges/preview/route.ts:6:  type BadgePreviewInput,
app/api/admin/badges/preview/route.ts:7:} from "@/lib/badgeAdmin";
app/api/admin/badges/preview/route.ts:8:import {
app/api/admin/badges/preview/route.ts:10:  type BadgeQualificationMode,
app/api/admin/badges/preview/route.ts:11:} from "@/lib/badgePreviewModes";
app/api/admin/badges/preview/route.ts:13:function isRecord(value: unknown): value is Record<string, unknown> {
app/api/admin/badges/preview/route.ts:17:function asString(value: unknown): string | null {
app/api/admin/badges/preview/route.ts:21:function asOptionalString(value: unknown): string | null {
app/api/admin/badges/preview/route.ts:26:function asPositiveNumber(value: unknown): number | null {
app/api/admin/badges/preview/route.ts:39:function asOptionalLimit(value: unknown): number | undefined {
app/api/admin/badges/preview/route.ts:44:function isBadgeQualificationMode(
app/api/admin/badges/preview/route.ts:46:): value is BadgeQualificationMode {
app/api/admin/badges/preview/route.ts:50:function parsePreviewInput(body: unknown): BadgePreviewInput {
app/api/admin/badges/preview/route.ts:60:  if (!isBadgeQualificationMode(mode)) {
app/api/admin/badges/preview/route.ts:61:    throw new Error("Unsupported badge preview mode.");
app/api/admin/badges/preview/route.ts:138:      const recordingId = asString(body.recordingId);
app/api/admin/badges/preview/route.ts:141:      if (!recordingId) {
app/api/admin/badges/preview/route.ts:142:        throw new Error("recordingId is required.");
app/api/admin/badges/preview/route.ts:150:        recordingId,
app/api/admin/badges/preview/route.ts:157:      const recordingId = asString(body.recordingId);
app/api/admin/badges/preview/route.ts:160:      if (!recordingId) {
app/api/admin/badges/preview/route.ts:161:        throw new Error("recordingId is required.");
app/api/admin/badges/preview/route.ts:169:        recordingId,
app/api/admin/badges/preview/route.ts:176:      const recordingId = asString(body.recordingId);
app/api/admin/badges/preview/route.ts:179:      if (!recordingId) {
app/api/admin/badges/preview/route.ts:180:        throw new Error("recordingId is required.");
app/api/admin/badges/preview/route.ts:188:        recordingId,
app/api/admin/badges/preview/route.ts:230:        `Unhandled badge preview mode: ${String(exhaustiveCheck)}`,
app/api/admin/badges/preview/route.ts:236:export async function POST(request: Request) {
app/api/admin/badges/preview/route.ts:242:    const rows = await previewBadgeQualification(input);
app/api/admin/badges/preview/route.ts:253:        : "Unable to preview badge cohort.";
lib/badgeFeature.ts:1:// web/lib/badgeFeature.ts
lib/badgeFeature.ts:2:import "server-only";
lib/badgeFeature.ts:5: * Badges are intentionally opt-in. An absent or malformed environment value
lib/badgeFeature.ts:6: * leaves the full badge system inactive.
lib/badgeFeature.ts:8:export function areBadgesEnabled(): boolean {
lib/memberDashboard.ts:2:import type { ResolvedDisplayIdentity } from "@/lib/memberIdentity";
lib/memberDashboard.ts:4:export type MemberDashboardBadge = {
lib/memberDashboard.ts:17:export type PortalMemberFavouriteTrack = {
lib/memberDashboard.ts:18:  recordingId: string;
lib/memberDashboard.ts:24:export type PortalMemberSummary = {
lib/memberDashboard.ts:50:   * Unified badge cabinet projection.
lib/memberDashboard.ts:51:   * Ordered badge definitions are merged with member possession state.
lib/memberDashboard.ts:53:  badges: MemberDashboardBadge[];
lib/memberDashboard.ts:56:export function emptyPortalMemberSummary(): PortalMemberSummary {
lib/memberDashboard.ts:62:    badges: [],
lib/badgeAdmin.ts:1:// web/lib/badgeAdmin.ts
lib/badgeAdmin.ts:2:import "server-only";
lib/badgeAdmin.ts:4:import { sql } from "@vercel/postgres";
lib/badgeAdmin.ts:5:import { getRecordingSummaryByRecordingId } from "@/lib/albums";
lib/badgeAdmin.ts:6:import { areBadgesEnabled } from "@/lib/badgeFeature";
lib/badgeAdmin.ts:7:import { grantEntitlement } from "@/lib/entitlementOps";
lib/badgeAdmin.ts:9:export type AwardBadgeToMembersInput = {
lib/badgeAdmin.ts:10:  entitlementKey: string;
lib/badgeAdmin.ts:18:export type AwardBadgeToMembersResult = {
lib/badgeAdmin.ts:19:  entitlementKey: string;
lib/badgeAdmin.ts:25:function uniqueMemberIds(memberIds: string[]): string[] {
lib/badgeAdmin.ts:39:export async function awardBadgeToMembers(
lib/badgeAdmin.ts:40:  input: AwardBadgeToMembersInput,
lib/badgeAdmin.ts:41:): Promise<AwardBadgeToMembersResult> {
lib/badgeAdmin.ts:42:  if (!areBadgesEnabled()) {
lib/badgeAdmin.ts:43:    throw new Error("Badge system is disabled.");
lib/badgeAdmin.ts:46:  const entitlementKey = input.entitlementKey.trim();
lib/badgeAdmin.ts:53:    const result = await grantEntitlement({
lib/badgeAdmin.ts:55:      entitlementKey,
lib/badgeAdmin.ts:59:      grantSource: input.grantSource ?? "badge_rule",
lib/badgeAdmin.ts:72:    entitlementKey,
lib/badgeAdmin.ts:79:export type BadgePreviewMemberRow = {
lib/badgeAdmin.ts:95:export type MinutesStreamedPreviewInput = {
lib/badgeAdmin.ts:101:export type PlayCountPreviewInput = {
lib/badgeAdmin.ts:107:export type CompleteCountPreviewInput = {
lib/badgeAdmin.ts:113:export type JoinedWithinWindowPreviewInput = {
lib/badgeAdmin.ts:120:export type ActiveWithinWindowPreviewInput = {
lib/badgeAdmin.ts:130:export type RecordingMinutesStreamedPreviewInput = {
lib/badgeAdmin.ts:132:  recordingId: string;
lib/badgeAdmin.ts:137:export type RecordingPlayCountPreviewInput = {
lib/badgeAdmin.ts:139:  recordingId: string;
lib/badgeAdmin.ts:144:export type RecordingCompleteCountPreviewInput = {
lib/badgeAdmin.ts:146:  recordingId: string;
lib/badgeAdmin.ts:151:export type ExegesisContributionCountPreviewInput = {
lib/badgeAdmin.ts:157:export type ExegesisVoteTallyPreviewInput = {
lib/badgeAdmin.ts:163:export type PublicNameUnlockedPreviewInput = {
lib/badgeAdmin.ts:168:export type BadgePreviewInput =
lib/badgeAdmin.ts:181:type BadgePreviewHandlerMap = {
lib/badgeAdmin.ts:184:  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts:187:  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts:190:  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts:193:  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts:196:  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts:199:  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts:202:  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts:205:  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts:208:  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts:211:  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts:214:  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts:217:type MemberBaseRow = {
lib/badgeAdmin.ts:223:type AggregateRow = {
lib/badgeAdmin.ts:237:const DEFAULT_PREVIEW_LIMIT = 200;
lib/badgeAdmin.ts:238:const MAX_PREVIEW_LIMIT = 1000;
lib/badgeAdmin.ts:239:const TELEMETRY_PROGRESS_STEP_MS = 15_000;
lib/badgeAdmin.ts:241:const PLAYBACK_TELEMETRY_EVENT_TYPES = {
lib/badgeAdmin.ts:247:function clampPreviewLimit(limit?: number): number {
lib/badgeAdmin.ts:253:function asNumber(value: string | number | null | undefined): number | null {
lib/badgeAdmin.ts:264:function mapAggregateRow(row: AggregateRow): BadgePreviewMemberRow {
lib/badgeAdmin.ts:292:function mapMemberBaseRow(row: MemberBaseRow): BadgePreviewMemberRow {
lib/badgeAdmin.ts:310:const BADGE_PREVIEW_HANDLERS: BadgePreviewHandlerMap = {
lib/badgeAdmin.ts:325:  rows: BadgePreviewMemberRow[],
lib/badgeAdmin.ts:326:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts:332:          (recordingId): recordingId is string =>
lib/badgeAdmin.ts:333:            typeof recordingId === "string" && recordingId.trim().length > 0,
lib/badgeAdmin.ts:345:    distinctRecordingIds.map(async (recordingId) => {
lib/badgeAdmin.ts:346:      const summary = await getRecordingSummaryByRecordingId(recordingId);
lib/badgeAdmin.ts:347:      titleByRecordingId.set(recordingId, summary?.title ?? recordingId);
lib/badgeAdmin.ts:363:export async function previewBadgeQualification(
lib/badgeAdmin.ts:364:  input: BadgePreviewInput,
lib/badgeAdmin.ts:365:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts:367:    value: BadgePreviewInput,
lib/badgeAdmin.ts:368:  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts:376:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts:404:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts:432:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts:460:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts:484:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts:545:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts:561:    where mts.recording_id = ${input.recordingId}
lib/badgeAdmin.ts:575:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts:591:    where mts.recording_id = ${input.recordingId}
lib/badgeAdmin.ts:605:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts:621:    where mts.recording_id = ${input.recordingId}
lib/badgeAdmin.ts:635:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts:672:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts:716:): Promise<BadgePreviewMemberRow[]> {
lib/badgeAutoAward.ts:1:// web/lib/badgeAutoAward.ts
lib/badgeAutoAward.ts:2:import "server-only";
lib/badgeAutoAward.ts:4:import { sql } from "@vercel/postgres";
lib/badgeAutoAward.ts:5:import {
lib/badgeAutoAward.ts:6:  getAutoAwardBadgeDefinitions,
lib/badgeAutoAward.ts:7:  type BadgeDefinition,
lib/badgeAutoAward.ts:8:} from "@/lib/badges";
lib/badgeAutoAward.ts:9:import { areBadgesEnabled } from "@/lib/badgeFeature";
lib/badgeAutoAward.ts:10:import { grantEntitlement } from "@/lib/entitlementOps";
lib/badgeAutoAward.ts:12:export type AutoBadgeTriggerKind =
lib/badgeAutoAward.ts:18:export type NewlyAwardedBadge = {
lib/badgeAutoAward.ts:19:  entitlementKey: string;
lib/badgeAutoAward.ts:27:export type RunAutoBadgeAwardsForMemberInput = {
lib/badgeAutoAward.ts:29:  trigger: AutoBadgeTriggerKind;
lib/badgeAutoAward.ts:30:  recordingId?: string | null;
lib/badgeAutoAward.ts:35:function asNumber(value: string | number | null | undefined): number {
lib/badgeAutoAward.ts:48:type PlaybackTelemetryBadgeEvent = "play" | "progress" | "complete";
lib/badgeAutoAward.ts:50:type PlaybackAggregateSnapshot = {
lib/badgeAutoAward.ts:63:type PlaybackAggregateSnapshotRow = {
lib/badgeAutoAward.ts:72:function asNonNegativeInt(value: string | number | null | undefined): number {
lib/badgeAutoAward.ts:76:function isPlaybackTelemetryBadgeRelevant(
lib/badgeAutoAward.ts:77:  badge: BadgeDefinition,
lib/badgeAutoAward.ts:78:  event: PlaybackTelemetryBadgeEvent,
lib/badgeAutoAward.ts:79:  recordingId: string,
lib/badgeAutoAward.ts:81:  const autoAward = badge.autoAward;
lib/badgeAutoAward.ts:82:  const mode = autoAward?.qualificationMode;
lib/badgeAutoAward.ts:98:        event === "progress" && autoAward.recordingId?.trim() === recordingId
lib/badgeAutoAward.ts:102:      return event === "play" && autoAward.recordingId?.trim() === recordingId;
lib/badgeAutoAward.ts:106:        event === "complete" && autoAward.recordingId?.trim() === recordingId
lib/badgeAutoAward.ts:114:function qualifiesPlaybackBadgeFromSnapshot(
lib/badgeAutoAward.ts:115:  badge: BadgeDefinition,
lib/badgeAutoAward.ts:118:  const autoAward = badge.autoAward;
lib/badgeAutoAward.ts:119:  const mode = autoAward?.qualificationMode;
lib/badgeAutoAward.ts:173:  recordingId: string;
lib/badgeAutoAward.ts:209:            and recording_id = ${params.recordingId}
lib/badgeAutoAward.ts:219:            and recording_id = ${params.recordingId}
lib/badgeAutoAward.ts:229:            and recording_id = ${params.recordingId}
lib/badgeAutoAward.ts:252:export async function runPlaybackAutoBadgeAwardsForMember(input: {
lib/badgeAutoAward.ts:254:  recordingId: string;
lib/badgeAutoAward.ts:255:  event: PlaybackTelemetryBadgeEvent;
lib/badgeAutoAward.ts:258:}): Promise<NewlyAwardedBadge[]> {
lib/badgeAutoAward.ts:259:  if (!areBadgesEnabled()) {
lib/badgeAutoAward.ts:263:  const recordingId = input.recordingId.trim();
lib/badgeAutoAward.ts:264:  if (!recordingId) return [];
lib/badgeAutoAward.ts:266:  const allAutoBadges = await getAutoAwardBadgeDefinitions();
lib/badgeAutoAward.ts:268:  const relevantBadges = allAutoBadges.filter((badge) =>
lib/badgeAutoAward.ts:269:    isPlaybackTelemetryBadgeRelevant(badge, input.event, recordingId),
lib/badgeAutoAward.ts:272:  if (relevantBadges.length === 0) {
lib/badgeAutoAward.ts:278:    recordingId,
lib/badgeAutoAward.ts:281:  const newlyAwarded: NewlyAwardedBadge[] = [];
lib/badgeAutoAward.ts:283:  for (const badge of relevantBadges) {
lib/badgeAutoAward.ts:284:    if (!qualifiesPlaybackBadgeFromSnapshot(badge, snapshot)) {
lib/badgeAutoAward.ts:288:    const grantResult = await grantEntitlement({
lib/badgeAutoAward.ts:290:      entitlementKey: badge.entitlementKey,
lib/badgeAutoAward.ts:293:      grantReason: "Automatic badge award via playback_aggregate_updated.",
lib/badgeAutoAward.ts:294:      grantSource: "badge_auto_rule",
lib/badgeAutoAward.ts:295:      grantSourceRef: `${badge.entitlementKey}:playback_aggregate_updated`,
lib/badgeAutoAward.ts:304:      entitlementKey: badge.entitlementKey,
lib/badgeAutoAward.ts:305:      title: badge.title,
lib/badgeAutoAward.ts:306:      description: badge.description,
lib/badgeAutoAward.ts:307:      imageUrl: badge.imageUrl,
lib/badgeAutoAward.ts:308:      shareable: badge.shareable,
lib/badgeAutoAward.ts:316:function isTriggerRelevant(
lib/badgeAutoAward.ts:317:  badge: BadgeDefinition,
lib/badgeAutoAward.ts:318:  trigger: AutoBadgeTriggerKind,
lib/badgeAutoAward.ts:319:  recordingId?: string | null,
lib/badgeAutoAward.ts:321:  const mode = badge.autoAward?.qualificationMode;
lib/badgeAutoAward.ts:336:        typeof recordingId === "string" &&
lib/badgeAutoAward.ts:337:        recordingId.trim().length > 0 &&
lib/badgeAutoAward.ts:338:        badge.autoAward?.recordingId === recordingId.trim()
lib/badgeAutoAward.ts:358:async function qualifiesForBadge(
lib/badgeAutoAward.ts:359:  badge: BadgeDefinition,
lib/badgeAutoAward.ts:362:  const autoAward = badge.autoAward;
lib/badgeAutoAward.ts:363:  const mode = autoAward?.qualificationMode;
lib/badgeAutoAward.ts:412:      const recordingId = autoAward.recordingId?.trim() ?? "";
lib/badgeAutoAward.ts:416:      if (!recordingId) return false;
lib/badgeAutoAward.ts:422:          and recording_id = ${recordingId}
lib/badgeAutoAward.ts:430:      const recordingId = autoAward.recordingId?.trim() ?? "";
lib/badgeAutoAward.ts:433:      if (!recordingId) return false;
lib/badgeAutoAward.ts:439:          and recording_id = ${recordingId}
lib/badgeAutoAward.ts:447:      const recordingId = autoAward.recordingId?.trim() ?? "";
lib/badgeAutoAward.ts:453:      if (!recordingId) return false;
lib/badgeAutoAward.ts:459:          and recording_id = ${recordingId}
lib/badgeAutoAward.ts:512:export async function runAutoBadgeAwardsForMember(
lib/badgeAutoAward.ts:513:  input: RunAutoBadgeAwardsForMemberInput,
lib/badgeAutoAward.ts:514:): Promise<NewlyAwardedBadge[]> {
lib/badgeAutoAward.ts:515:  if (!areBadgesEnabled()) {
lib/badgeAutoAward.ts:519:  const allAutoBadges = await getAutoAwardBadgeDefinitions();
lib/badgeAutoAward.ts:521:  const relevantBadges = allAutoBadges.filter((badge) =>
lib/badgeAutoAward.ts:522:    isTriggerRelevant(badge, input.trigger, input.recordingId),
lib/badgeAutoAward.ts:525:  if (relevantBadges.length === 0) {
lib/badgeAutoAward.ts:529:  const newlyAwarded: NewlyAwardedBadge[] = [];
lib/badgeAutoAward.ts:531:  for (const badge of relevantBadges) {
lib/badgeAutoAward.ts:532:    const qualifies = await qualifiesForBadge(badge, input.memberId);
lib/badgeAutoAward.ts:538:    const grantResult = await grantEntitlement({
lib/badgeAutoAward.ts:540:      entitlementKey: badge.entitlementKey,
lib/badgeAutoAward.ts:543:      grantReason: `Automatic badge award via ${input.trigger}.`,
lib/badgeAutoAward.ts:544:      grantSource: "badge_auto_rule",
lib/badgeAutoAward.ts:545:      grantSourceRef: `${badge.entitlementKey}:${input.trigger}`,
lib/badgeAutoAward.ts:554:      entitlementKey: badge.entitlementKey,
lib/badgeAutoAward.ts:555:      title: badge.title,
lib/badgeAutoAward.ts:556:      description: badge.description,
lib/badgeAutoAward.ts:557:      imageUrl: badge.imageUrl,
lib/badgeAutoAward.ts:558:      shareable: badge.shareable,
sanity/schemaTypes/badgeDefinition.ts:1:// sanity/schemaTypes/badgeDefinition.ts
sanity/schemaTypes/badgeDefinition.ts:2:import { defineField, defineType } from "sanity";
sanity/schemaTypes/badgeDefinition.ts:4:const AUTO_QUALIFICATION_MODE_OPTIONS = [
sanity/schemaTypes/badgeDefinition.ts:36:type BadgeDefinitionParent = {
sanity/schemaTypes/badgeDefinition.ts:41:function parentObject(parent: unknown): BadgeDefinitionParent | null {
sanity/schemaTypes/badgeDefinition.ts:43:    ? (parent as BadgeDefinitionParent)
sanity/schemaTypes/badgeDefinition.ts:47:function isAutomaticBadge(parent: unknown): boolean {
sanity/schemaTypes/badgeDefinition.ts:51:function autoQualificationMode(parent: unknown): string {
sanity/schemaTypes/badgeDefinition.ts:55:function hiddenUnlessAutomatic(props: { parent: unknown }): boolean {
sanity/schemaTypes/badgeDefinition.ts:56:  return !isAutomaticBadge(props.parent);
sanity/schemaTypes/badgeDefinition.ts:59:function hiddenUnlessMode(
sanity/schemaTypes/badgeDefinition.ts:66:export const badgeDefinition = defineType({
sanity/schemaTypes/badgeDefinition.ts:67:  name: "badgeDefinition",
sanity/schemaTypes/badgeDefinition.ts:68:  title: "Badge Definition",
sanity/schemaTypes/badgeDefinition.ts:79:      title: "Badge key suffix",
sanity/schemaTypes/badgeDefinition.ts:82:        "Enter only the suffix portion, for example early_supporter. The app will derive the entitlement key as badge_early_supporter.",
sanity/schemaTypes/badgeDefinition.ts:87:            name: "badge key suffix",
sanity/schemaTypes/badgeDefinition.ts:91:            "Badge key suffixes must use lowercase letters, numbers, and underscores only.",
sanity/schemaTypes/badgeDefinition.ts:133:        "Hide this badge from members who have not unlocked it yet. Useful for exclusive, retired, or time-bound badges that should not appear as visible locked promises.",
sanity/schemaTypes/badgeDefinition.ts:176:            isAutomaticBadge(context.parent) &&
sanity/schemaTypes/badgeDefinition.ts:179:            return "Automatic badges require an automatic qualification mode.";
sanity/schemaTypes/badgeDefinition.ts:186:      name: "recordingId",
sanity/schemaTypes/badgeDefinition.ts:311:          : "Untitled badge";
sanity/schemaTypes/badgeDefinition.ts:313:      const entitlementKey =
sanity/schemaTypes/badgeDefinition.ts:315:          ? `badge_${selection.keySuffix.trim()}`
sanity/schemaTypes/badgeDefinition.ts:327:            ? `${entitlementKey} · ${stateBits.join(" · ")}`
sanity/schemaTypes/badgeDefinition.ts:328:            : entitlementKey,
lib/entitlementOps.ts:2:import "server-only";
lib/entitlementOps.ts:3:import { sql } from "@vercel/postgres";
lib/entitlementOps.ts:4:import { EVENT_SOURCES, type EventSource } from "./vocab";
lib/entitlementOps.ts:5:import { logEntitlementGranted, logEntitlementRevoked } from "./events";
lib/entitlementOps.ts:7:const uuidOk = (v: string) =>
lib/entitlementOps.ts:12:type GrantParams = {
lib/entitlementOps.ts:14:  entitlementKey: string;
lib/entitlementOps.ts:26:export type GrantEntitlementResult =
lib/entitlementOps.ts:30:export type RevokeEntitlementResult =
lib/entitlementOps.ts:35:  entitlementKey: string,
lib/entitlementOps.ts:38:  const key = entitlementKey?.trim();
lib/entitlementOps.ts:55:export async function grantEntitlement(
lib/entitlementOps.ts:62:    entitlementKey,
lib/entitlementOps.ts:74:  await ensureEntitlementType(entitlementKey, scopeId);
lib/entitlementOps.ts:90:      ${entitlementKey},
lib/entitlementOps.ts:102:        and eg.entitlement_key = ${entitlementKey}
lib/entitlementOps.ts:116:    entitlementKey,
lib/entitlementOps.ts:132:export async function revokeEntitlement(params: {
lib/entitlementOps.ts:134:  entitlementKey: string;
lib/entitlementOps.ts:147:    entitlementKey,
lib/entitlementOps.ts:163:      and entitlement_key = ${entitlementKey}
lib/entitlementOps.ts:181:    entitlementKey,
lib/adminAuth.ts:2:import "server-only";
lib/adminAuth.ts:3:import { auth } from "@clerk/nextjs/server";
lib/adminAuth.ts:4:import { sql } from "@vercel/postgres";
lib/adminAuth.ts:5:import { checkAccess } from "@/lib/access";
lib/adminAuth.ts:6:import { ENTITLEMENTS } from "@/lib/vocab";
lib/adminAuth.ts:21:export async function requireAdminMemberId(): Promise<string> {
app/home/modules/badges/usePrefersReducedMotion.ts:1:// web/app/home/modules/badges/usePrefersReducedMotion.ts
app/home/modules/badges/usePrefersReducedMotion.ts:4:import React from "react";
app/home/modules/badges/usePrefersReducedMotion.ts:6:export function usePrefersReducedMotion(): boolean {
app/home/modules/badges/BadgeCabinetGrid.tsx:1:// web/app/home/modules/badges/BadgeCabinetGrid.tsx
app/home/modules/badges/BadgeCabinetGrid.tsx:4:import React from "react";
app/home/modules/badges/BadgeCabinetGrid.tsx:6:type Props = {
app/home/modules/badges/BadgeCabinetGrid.tsx:11:export default function BadgeCabinetGrid(props: Props) {
app/home/modules/badges/BadgeCabinetGrid.tsx:16:      className={`portal-member-badge-grid${
app/home/modules/badges/BadgeCabinetGrid.tsx:17:        expanded ? " portal-member-badges--expanded" : ""
lib/badgeAwardAnnouncementServer.ts:1:// web/lib/badgeAwardAnnouncementServer.ts
lib/badgeAwardAnnouncementServer.ts:2:import "server-only";
lib/badgeAwardAnnouncementServer.ts:4:import { sql } from "@vercel/postgres";
lib/badgeAwardAnnouncementServer.ts:5:import type { NewlyAwardedBadge } from "@/lib/badgeAutoAward";
lib/badgeAwardAnnouncementServer.ts:6:import { areBadgesEnabled } from "@/lib/badgeFeature";
lib/badgeAwardAnnouncementServer.ts:8:function normalizeEntitlementKeys(badges: NewlyAwardedBadge[]): string[] {
lib/badgeAwardAnnouncementServer.ts:11:  for (const badge of badges) {
lib/badgeAwardAnnouncementServer.ts:12:    const entitlementKey =
lib/badgeAwardAnnouncementServer.ts:13:      "entitlementKey" in badge && typeof badge.entitlementKey === "string"
lib/badgeAwardAnnouncementServer.ts:14:        ? badge.entitlementKey.trim()
lib/badgeAwardAnnouncementServer.ts:17:    if (!entitlementKey) continue;
lib/badgeAwardAnnouncementServer.ts:18:    unique.add(entitlementKey);
lib/badgeAwardAnnouncementServer.ts:24:export async function markOverlayAnnouncedForAwardedBadges(params: {
lib/badgeAwardAnnouncementServer.ts:26:  badges: NewlyAwardedBadge[];
lib/badgeAwardAnnouncementServer.ts:28:  if (!areBadgesEnabled()) {
lib/badgeAwardAnnouncementServer.ts:32:  const { memberId, badges } = params;
lib/badgeAwardAnnouncementServer.ts:34:  const entitlementKeys = normalizeEntitlementKeys(badges);
lib/badgeAwardAnnouncementServer.ts:35:  if (entitlementKeys.length === 0) return;
lib/badgeAwardAnnouncementServer.ts:37:  const entitlementKeysJson = JSON.stringify(entitlementKeys);
lib/badgeAwardAnnouncementServer.ts:41:      select jsonb_array_elements_text(${entitlementKeysJson}::jsonb) as entitlement_key
app/home/modules/badges/BadgeCabinetStyles.tsx:1:// web/app/home/modules/badges/BadgeCabinetStyles.tsx
app/home/modules/badges/BadgeCabinetStyles.tsx:4:import React from "react";
app/home/modules/badges/BadgeCabinetStyles.tsx:6:export default function BadgeCabinetStyles() {
app/home/modules/badges/BadgeCabinetStyles.tsx:10:        --portal-badge-columns-collapsed: 6;
app/home/modules/badges/BadgeCabinetStyles.tsx:11:        --portal-badge-columns-expanded: 3;
app/home/modules/badges/BadgeCabinetStyles.tsx:12:        --portal-badge-gap-collapsed: 14px;
app/home/modules/badges/BadgeCabinetStyles.tsx:13:        --portal-badge-gap-expanded: 18px;
app/home/modules/badges/BadgeCabinetStyles.tsx:14:        --portal-badge-art-scale-collapsed: 0.82;
app/home/modules/badges/BadgeCabinetStyles.tsx:15:        --portal-badge-art-scale-expanded: 1;
app/home/modules/badges/BadgeCabinetStyles.tsx:16:        --portal-badge-caption-offset: -3px;
app/home/modules/badges/BadgeCabinetStyles.tsx:17:        --portal-badge-caption-row-gap-collapsed: 10px;
app/home/modules/badges/BadgeCabinetStyles.tsx:18:        --portal-badge-caption-row-gap-expanded: 16px;
app/home/modules/badges/BadgeCabinetStyles.tsx:23:          --portal-badge-gap-collapsed: 10px;
app/home/modules/badges/BadgeCabinetStyles.tsx:24:          --portal-badge-gap-expanded: 12px;
app/home/modules/badges/BadgeCabinetStyles.tsx:25:          --portal-badge-art-scale-collapsed: 0.84;
app/home/modules/badges/BadgeCabinetStyles.tsx:26:          --portal-badge-caption-row-gap-collapsed: 8px;
app/home/modules/badges/BadgeCabinetStyles.tsx:27:          --portal-badge-caption-row-gap-expanded: 12px;
app/home/modules/badges/BadgeCabinetStyles.tsx:33:          --portal-badge-gap-collapsed: 8px;
app/home/modules/badges/BadgeCabinetStyles.tsx:34:          --portal-badge-gap-expanded: 10px;
app/home/modules/badges/BadgeCabinetStyles.tsx:35:          --portal-badge-art-scale-collapsed: 0.86;
app/home/modules/badges/BadgeCabinetStyles.tsx:36:          --portal-badge-caption-row-gap-collapsed: 6px;
app/home/modules/badges/BadgeCabinetStyles.tsx:37:          --portal-badge-caption-row-gap-expanded: 10px;
app/home/modules/badges/BadgeCabinetStyles.tsx:41:      .portal-member-badge-grid {
app/home/modules/badges/BadgeCabinetStyles.tsx:46:          var(--portal-badge-columns-collapsed),
app/home/modules/badges/BadgeCabinetStyles.tsx:49:        column-gap: var(--portal-badge-gap-collapsed);
app/home/modules/badges/BadgeCabinetStyles.tsx:50:        row-gap: var(--portal-badge-caption-row-gap-collapsed);
app/home/modules/badges/BadgeCabinetStyles.tsx:54:      .portal-member-badge-grid.portal-member-badges--expanded {
app/home/modules/badges/BadgeCabinetStyles.tsx:56:          var(--portal-badge-columns-expanded),
app/home/modules/badges/BadgeCabinetStyles.tsx:59:        column-gap: var(--portal-badge-gap-expanded);
app/home/modules/badges/BadgeCabinetStyles.tsx:60:        row-gap: var(--portal-badge-caption-row-gap-expanded);
app/home/modules/badges/BadgeCabinetStyles.tsx:63:      .portal-member-badge-shell {
app/home/modules/badges/BadgeCabinetStyles.tsx:73:      .portal-member-badge-meta {
app/home/modules/badges/BadgeCabinetStyles.tsx:91:      .portal-member-badge-grid.portal-member-badges--expanded
app/home/modules/badges/BadgeCabinetStyles.tsx:92:        .portal-member-badge-meta {
app/home/modules/badges/BadgeCabinetStyles.tsx:101:      .portal-member-badge-meta-inner {
app/home/modules/badges/BadgeCabinetStyles.tsx:105:        transform: translateY(var(--portal-badge-caption-offset));
app/home/modules/badges/BadgeCabinetStyles.tsx:112:      .portal-member-badge-grid.portal-member-badges--expanded
app/home/modules/badges/BadgeCabinetStyles.tsx:113:        .portal-member-badge-meta-inner {
app/home/modules/badges/BadgeCabinetStyles.tsx:119:      .portal-member-badge-live-region {
app/home/modules/badges/BadgeCabinetStyles.tsx:132:        .portal-member-badge-grid,
app/home/modules/badges/BadgeCabinetStyles.tsx:133:        .portal-member-badge-shell,
app/home/modules/badges/BadgeCabinetStyles.tsx:134:        .portal-member-badge-meta,
app/home/modules/badges/BadgeCabinetStyles.tsx:135:        .portal-member-badge-meta-inner {
app/home/modules/badges/BadgeCabinetStyles.tsx:140:        .portal-member-badge-meta {
lib/badges.ts:1:// web/lib/badges.ts
lib/badges.ts:2:import "server-only";
lib/badges.ts:4:import { unstable_cache } from "next/cache";
lib/badges.ts:5:import { cache } from "react";
lib/badges.ts:6:import { client } from "@/sanity/lib/client";
lib/badges.ts:7:import { areBadgesEnabled } from "@/lib/badgeFeature";
lib/badges.ts:8:import type { BadgeQualificationMode } from "@/lib/badgePreviewModes";
lib/badges.ts:10:export type BadgeAwardMode = "manual" | "automatic";
lib/badges.ts:12:export type BadgeAutoAwardConfig = {
lib/badges.ts:14:  qualificationMode: BadgeQualificationMode | null;
lib/badges.ts:25:  recordingId: string | null;
lib/badges.ts:28:export type BadgeDefinition = {
lib/badges.ts:31:  entitlementKey: string;
lib/badges.ts:39:  awardMode: BadgeAwardMode;
lib/badges.ts:40:  autoAward: BadgeAutoAwardConfig | null;
lib/badges.ts:43:type BadgeDefinitionQueryRow = {
lib/badges.ts:67:  recordingId?: string | null;
lib/badges.ts:70:const BADGE_DEFINITIONS_QUERY = `
lib/badges.ts:71:   *[_type == "badgeDefinition" && coalesce(active, true) == true]{
lib/badges.ts:95:    recordingId
lib/badges.ts:99:function asFiniteNumber(value: unknown): number | null {
lib/badges.ts:103:function asTrimmedString(value: unknown): string | null {
lib/badges.ts:107:function asQualificationMode(value: unknown): BadgeQualificationMode | null {
lib/badges.ts:128:function coerceBadgeDefinition(
lib/badges.ts:129:  row: BadgeDefinitionQueryRow,
lib/badges.ts:130:): BadgeDefinition | null {
lib/badges.ts:138:  const entitlementKey = `badge_${keySuffix}`;
lib/badges.ts:139:  const awardMode: BadgeAwardMode =
lib/badges.ts:142:  const qualificationMode = asQualificationMode(row.autoQualificationMode);
lib/badges.ts:144:  const autoAward: BadgeAutoAwardConfig | null =
lib/badges.ts:148:          qualificationMode,
lib/badges.ts:159:          recordingId: asTrimmedString(row.recordingId),
lib/badges.ts:166:    entitlementKey,
lib/badges.ts:188:const BADGE_DEFINITIONS_REVALIDATE_SECONDS = 5 * 60;
lib/badges.ts:190:const getCachedActiveBadgeDefinitions = unstable_cache(
lib/badges.ts:191:  async (): Promise<BadgeDefinition[]> => {
lib/badges.ts:195:      ? (result as BadgeDefinitionQueryRow[])
lib/badges.ts:199:      .map((row: BadgeDefinitionQueryRow) => coerceBadgeDefinition(row))
lib/badges.ts:200:      .filter((row): row is BadgeDefinition => row !== null);
lib/badges.ts:202:  ["active-badge-definitions-v1"],
lib/badges.ts:205:    tags: ["badgeDefinitions"],
lib/badges.ts:209:export const getActiveBadgeDefinitions = cache(
lib/badges.ts:210:  async (): Promise<BadgeDefinition[]> => {
lib/badges.ts:211:    if (!areBadgesEnabled()) {
lib/badges.ts:215:    return getCachedActiveBadgeDefinitions();
lib/badges.ts:219:export const getActiveBadgeDefinitionsByEntitlementKey = cache(
lib/badges.ts:220:  async (): Promise<Map<string, BadgeDefinition>> => {
lib/badges.ts:221:    const definitions = await getActiveBadgeDefinitions();
lib/badges.ts:223:      definitions.map((definition) => [definition.entitlementKey, definition]),
lib/badges.ts:228:export const getAutoAwardBadgeDefinitions = cache(
lib/badges.ts:229:  async (): Promise<BadgeDefinition[]> => {
lib/badges.ts:230:    const definitions = await getActiveBadgeDefinitions();
lib/badges.ts:237:        definition.autoAward.qualificationMode !== null,
app/home/modules/badges/BadgeUnlockVisual.tsx:1:// web/app/home/modules/badges/BadgeUnlockVisual.tsx
app/home/modules/badges/BadgeUnlockVisual.tsx:4:import React from "react";
app/home/modules/badges/BadgeUnlockVisual.tsx:5:import BuildRevealMaskCanvas from "./BuildRevealMaskCanvas";
app/home/modules/badges/BadgeUnlockVisual.tsx:7:type Props = Readonly<{
app/home/modules/badges/BadgeUnlockVisual.tsx:16:const REVEAL_DELAY_MS = 1210;
app/home/modules/badges/BadgeUnlockVisual.tsx:17:const REVEAL_DURATION_MS = 600;
app/home/modules/badges/BadgeUnlockVisual.tsx:19:function BadgeFallbackArt(
app/home/modules/badges/BadgeUnlockVisual.tsx:44:function BadgeArtImage(
app/home/modules/badges/BadgeUnlockVisual.tsx:75:function BadgeUnlockedAtmosphere() {
app/home/modules/badges/BadgeUnlockVisual.tsx:79:        className="portal-badge-idle-glow"
app/home/modules/badges/BadgeUnlockVisual.tsx:93:        className="portal-badge-inner-aura"
app/home/modules/badges/BadgeUnlockVisual.tsx:110:        className="portal-badge-embers"
app/home/modules/badges/BadgeUnlockVisual.tsx:123:          className="portal-badge-spark-a"
app/home/modules/badges/BadgeUnlockVisual.tsx:134:          className="portal-badge-spark-b"
app/home/modules/badges/BadgeUnlockVisual.tsx:145:          className="portal-badge-spark-c"
app/home/modules/badges/BadgeUnlockVisual.tsx:156:          className="portal-badge-burst-a"
app/home/modules/badges/BadgeUnlockVisual.tsx:167:          className="portal-badge-burst-b"
app/home/modules/badges/BadgeUnlockVisual.tsx:178:          className="portal-badge-burst-c"
app/home/modules/badges/BadgeUnlockVisual.tsx:192:function UnlockGlint(
app/home/modules/badges/BadgeUnlockVisual.tsx:201:      className={`portal-badge-quarter-glint portal-badge-quarter-glint--stage-${stage}`}
app/home/modules/badges/BadgeUnlockVisual.tsx:206:function SpinStages(
app/home/modules/badges/BadgeUnlockVisual.tsx:215:      className={`portal-badge-spin-stage-1${
app/home/modules/badges/BadgeUnlockVisual.tsx:216:        isUnlocking ? " portal-badge-spin-stage-1--unlocking" : ""
app/home/modules/badges/BadgeUnlockVisual.tsx:226:        className={`portal-badge-spin-stage-2${
app/home/modules/badges/BadgeUnlockVisual.tsx:227:          isUnlocking ? " portal-badge-spin-stage-2--unlocking" : ""
app/home/modules/badges/BadgeUnlockVisual.tsx:237:          className={`portal-badge-spin-stage-3${
app/home/modules/badges/BadgeUnlockVisual.tsx:238:            isUnlocking ? " portal-badge-spin-stage-3--unlocking" : ""
app/home/modules/badges/BadgeUnlockVisual.tsx:253:function FinalBadgeArt(
app/home/modules/badges/BadgeUnlockVisual.tsx:265:      className="portal-badge-final-art-shell"
app/home/modules/badges/BadgeUnlockVisual.tsx:271:      <BadgeArtImage
app/home/modules/badges/BadgeUnlockVisual.tsx:274:        className="portal-badge-final-art-image"
app/home/modules/badges/BadgeUnlockVisual.tsx:285:          className="portal-badge-final-shimmer portal-badge-final-shimmer--celebrating"
app/home/modules/badges/BadgeUnlockVisual.tsx:305:function ImageBadgeArt(
app/home/modules/badges/BadgeUnlockVisual.tsx:320:        <BadgeArtImage
app/home/modules/badges/BadgeUnlockVisual.tsx:323:          className="portal-badge-art-base-greyscale"
app/home/modules/badges/BadgeUnlockVisual.tsx:341:            className="portal-badge-reveal-canvas-layer portal-badge-reveal-canvas-layer--unlocking"
app/home/modules/badges/BadgeUnlockVisual.tsx:345:            className="portal-badge-unlock-energy-flare"
app/home/modules/badges/BadgeUnlockVisual.tsx:357:        <FinalBadgeArt
app/home/modules/badges/BadgeUnlockVisual.tsx:368:function UnlockEdgeSpines() {
app/home/modules/badges/BadgeUnlockVisual.tsx:372:        className="portal-badge-edge-spine portal-badge-edge-spine--left"
app/home/modules/badges/BadgeUnlockVisual.tsx:385:        className="portal-badge-edge-spine portal-badge-edge-spine--right"
app/home/modules/badges/BadgeUnlockVisual.tsx:400:function CentreRadiance() {
app/home/modules/badges/BadgeUnlockVisual.tsx:403:      className="portal-badge-centre-radiance"
app/home/modules/badges/BadgeUnlockVisual.tsx:421:function BadgeArtShell(
app/home/modules/badges/BadgeUnlockVisual.tsx:434:      className={`portal-badge-art-shell${
app/home/modules/badges/BadgeUnlockVisual.tsx:435:        isUnlocking ? " portal-badge-art-shell--unlocking" : ""
app/home/modules/badges/BadgeUnlockVisual.tsx:443:        <ImageBadgeArt
app/home/modules/badges/BadgeUnlockVisual.tsx:451:        <BadgeFallbackArt unlocked={unlocked} label={label} />
app/home/modules/badges/BadgeUnlockVisual.tsx:460:function ImpactFlash() {
app/home/modules/badges/BadgeUnlockVisual.tsx:463:      className="portal-badge-impact-flash"
app/home/modules/badges/BadgeUnlockVisual.tsx:474:export default function BadgeUnlockVisual(props: Props) {
app/home/modules/badges/BadgeUnlockVisual.tsx:481:      className={`portal-badge-unlock-visual-inner portal-badge-unlock-visual-inner--${variant}`}
app/home/modules/badges/BadgeUnlockVisual.tsx:482:      data-badge-variant={variant}
app/home/modules/badges/BadgeUnlockVisual.tsx:483:      data-badge-unlocking={isUnlocking ? "true" : "false"}
app/home/modules/badges/BadgeUnlockVisual.tsx:484:      data-badge-newly-unlocked={isNewlyUnlocked ? "true" : "false"}
app/home/modules/badges/BadgeUnlockVisual.tsx:488:        className={unlocked ? undefined : "portal-badge-core--locked"}
app/home/modules/badges/BadgeUnlockVisual.tsx:494:        {unlocked ? <BadgeUnlockedAtmosphere /> : null}
app/home/modules/badges/BadgeUnlockVisual.tsx:497:          <BadgeArtShell

===== EXTERNAL BADGE CALLERS / INTEGRATION POINTS =====

lib/badgeAdmin.ts-356-      matchedRecordingTitle:
lib/badgeAdmin.ts-357-        titleByRecordingId.get(row.matchedRecordingId) ??
lib/badgeAdmin.ts-358-        row.matchedRecordingId,
lib/badgeAdmin.ts-359-    };
lib/badgeAdmin.ts-360-  });
lib/badgeAdmin.ts-361-}
lib/badgeAdmin.ts-362-
lib/badgeAdmin.ts:363:export async function previewBadgeQualification(
lib/badgeAdmin.ts-364-  input: BadgePreviewInput,
lib/badgeAdmin.ts-365-): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts-366-  const handler = BADGE_PREVIEW_HANDLERS[input.mode] as (
lib/badgeAdmin.ts-367-    value: BadgePreviewInput,
lib/badgeAdmin.ts-368-  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts-369-
lib/badgeAdmin.ts-370-  const rows = await handler(input);
--
lib/memberDashboardServer.ts-155-  ]);
lib/memberDashboardServer.ts-156-
lib/memberDashboardServer.ts-157-  const ownedBadgeByKey = new Map<
lib/memberDashboardServer.ts-158-    string,
lib/memberDashboardServer.ts-159-    {
lib/memberDashboardServer.ts-160-      grantId: string;
lib/memberDashboardServer.ts-161-      grantedAt: string | null;
lib/memberDashboardServer.ts:162:      cabinetRevealPending: boolean;
lib/memberDashboardServer.ts-163-    }
lib/memberDashboardServer.ts-164-  >();
lib/memberDashboardServer.ts-165-
lib/memberDashboardServer.ts-166-  for (const grant of activeBadgeGrants) {
lib/memberDashboardServer.ts-167-    const existing = ownedBadgeByKey.get(grant.entitlement_key);
lib/memberDashboardServer.ts-168-
lib/memberDashboardServer.ts-169-    const candidate = {
lib/memberDashboardServer.ts-170-      grantId: grant.id,
lib/memberDashboardServer.ts-171-      grantedAt: grant.granted_at,
lib/memberDashboardServer.ts:172:      cabinetRevealPending: grant.cabinet_revealed_at == null,
lib/memberDashboardServer.ts-173-    };
lib/memberDashboardServer.ts-174-
lib/memberDashboardServer.ts-175-    if (!existing) {
lib/memberDashboardServer.ts-176-      ownedBadgeByKey.set(grant.entitlement_key, candidate);
lib/memberDashboardServer.ts-177-      continue;
lib/memberDashboardServer.ts-178-    }
lib/memberDashboardServer.ts-179-
--
lib/memberDashboardServer.ts-206-      description: definition.description ?? undefined,
lib/memberDashboardServer.ts-207-      imageUrl: definition.imageUrl ?? undefined,
lib/memberDashboardServer.ts-208-      shareable: definition.shareable,
lib/memberDashboardServer.ts-209-      undisclosed: definition.undisclosed,
lib/memberDashboardServer.ts-210-      unlocked,
lib/memberDashboardServer.ts-211-      unlockedAt: owned?.grantedAt ?? null,
lib/memberDashboardServer.ts-212-      editorialOrder: definition.displayOrder,
lib/memberDashboardServer.ts:213:      cabinetRevealPending: owned?.cabinetRevealPending ?? false,
lib/memberDashboardServer.ts-214-    });
lib/memberDashboardServer.ts-215-  }
lib/memberDashboardServer.ts-216-
lib/memberDashboardServer.ts-217-  const pendingGrantIds = Array.from(ownedBadgeByKey.values())
lib/memberDashboardServer.ts:218:    .filter((grant) => grant.cabinetRevealPending)
lib/memberDashboardServer.ts-219-    .map((grant) => grant.grantId);
lib/memberDashboardServer.ts-220-
lib/memberDashboardServer.ts-221-  if (pendingGrantIds.length > 0) {
lib/memberDashboardServer.ts-222-    await consumeCabinetRevealGrants(pendingGrantIds);
lib/memberDashboardServer.ts-223-  }
lib/memberDashboardServer.ts-224-
lib/memberDashboardServer.ts-225-  badges.sort((a, b) => {
--
lib/memberDashboardServer.ts-236-    description: badge.description ?? undefined,
lib/memberDashboardServer.ts-237-    imageUrl: badge.imageUrl ?? undefined,
lib/memberDashboardServer.ts-238-    shareable: badge.shareable,
lib/memberDashboardServer.ts-239-    undisclosed: badge.undisclosed,
lib/memberDashboardServer.ts-240-    unlocked: badge.unlocked,
lib/memberDashboardServer.ts-241-    unlockedAt: badge.unlockedAt,
lib/memberDashboardServer.ts-242-    editorialOrder: badge.editorialOrder,
lib/memberDashboardServer.ts:243:    cabinetRevealPending: badge.cabinetRevealPending ?? false,
lib/memberDashboardServer.ts-244-  }));
lib/memberDashboardServer.ts-245-}
lib/memberDashboardServer.ts-246-
lib/memberDashboardServer.ts-247-export async function buildPortalMemberSummary(
lib/memberDashboardServer.ts-248-  memberId: string,
lib/memberDashboardServer.ts-249-): Promise<PortalMemberSummary> {
lib/memberDashboardServer.ts-250-  const identityState = await buildMemberIdentityState(memberId);
--
lib/badgeAutoAward.ts-287-
lib/badgeAutoAward.ts-288-    const grantResult = await grantEntitlement({
lib/badgeAutoAward.ts-289-      memberId: input.memberId,
lib/badgeAutoAward.ts-290-      entitlementKey: badge.entitlementKey,
lib/badgeAutoAward.ts-291-      scopeId: null,
lib/badgeAutoAward.ts-292-      grantedBy: input.grantedBy ?? "system",
lib/badgeAutoAward.ts-293-      grantReason: "Automatic badge award via playback_aggregate_updated.",
lib/badgeAutoAward.ts:294:      grantSource: "badge_auto_rule",
lib/badgeAutoAward.ts-295-      grantSourceRef: `${badge.entitlementKey}:playback_aggregate_updated`,
lib/badgeAutoAward.ts-296-      correlationId: input.correlationId ?? null,
lib/badgeAutoAward.ts-297-    });
lib/badgeAutoAward.ts-298-
lib/badgeAutoAward.ts-299-    if (grantResult.status !== "inserted") {
lib/badgeAutoAward.ts-300-      continue;
lib/badgeAutoAward.ts-301-    }
--
lib/badgeAutoAward.ts-505-
lib/badgeAutoAward.ts-506-    case "joined_within_window":
lib/badgeAutoAward.ts-507-    case "active_within_window":
lib/badgeAutoAward.ts-508-      return false;
lib/badgeAutoAward.ts-509-  }
lib/badgeAutoAward.ts-510-}
lib/badgeAutoAward.ts-511-
lib/badgeAutoAward.ts:512:export async function runAutoBadgeAwardsForMember(
lib/badgeAutoAward.ts-513-  input: RunAutoBadgeAwardsForMemberInput,
lib/badgeAutoAward.ts-514-): Promise<NewlyAwardedBadge[]> {
lib/badgeAutoAward.ts-515-  if (!areBadgesEnabled()) {
lib/badgeAutoAward.ts-516-    return [];
lib/badgeAutoAward.ts-517-  }
lib/badgeAutoAward.ts-518-
lib/badgeAutoAward.ts-519-  const allAutoBadges = await getAutoAwardBadgeDefinitions();
--
lib/badgeAutoAward.ts-537-
lib/badgeAutoAward.ts-538-    const grantResult = await grantEntitlement({
lib/badgeAutoAward.ts-539-      memberId: input.memberId,
lib/badgeAutoAward.ts-540-      entitlementKey: badge.entitlementKey,
lib/badgeAutoAward.ts-541-      scopeId: null,
lib/badgeAutoAward.ts-542-      grantedBy: input.grantedBy ?? "system",
lib/badgeAutoAward.ts-543-      grantReason: `Automatic badge award via ${input.trigger}.`,
lib/badgeAutoAward.ts:544:      grantSource: "badge_auto_rule",
lib/badgeAutoAward.ts-545-      grantSourceRef: `${badge.entitlementKey}:${input.trigger}`,
lib/badgeAutoAward.ts-546-      correlationId: input.correlationId ?? null,
lib/badgeAutoAward.ts-547-    });
lib/badgeAutoAward.ts-548-
lib/badgeAutoAward.ts-549-    if (grantResult.status !== "inserted") {
lib/badgeAutoAward.ts-550-      continue;
lib/badgeAutoAward.ts-551-    }
--
lib/memberDashboard.ts-7-  description?: string | null;
lib/memberDashboard.ts-8-  imageUrl?: string | null;
lib/memberDashboard.ts-9-  shareable?: boolean;
lib/memberDashboard.ts-10-  undisclosed?: boolean;
lib/memberDashboard.ts-11-  unlocked: boolean;
lib/memberDashboard.ts-12-  unlockedAt?: string | null;
lib/memberDashboard.ts-13-  editorialOrder?: number | null;
lib/memberDashboard.ts:14:  cabinetRevealPending?: boolean;
lib/memberDashboard.ts-15-};
lib/memberDashboard.ts-16-
lib/memberDashboard.ts-17-export type PortalMemberFavouriteTrack = {
lib/memberDashboard.ts-18-  recordingId: string;
lib/memberDashboard.ts-19-  title: string;
lib/memberDashboard.ts-20-  artist?: string | null;
lib/memberDashboard.ts-21-  minutes?: number | null;
--
app/api/exegesis/comment/route.ts-6-
app/api/exegesis/comment/route.ts-7-import type { IdentityDTO } from "@/lib/exegesisIdentityDto";
app/api/exegesis/comment/route.ts-8-
app/api/exegesis/comment/route.ts-9-import { hasAnyEntitlement } from "@/lib/entitlements";
app/api/exegesis/comment/route.ts-10-import { ensureMemberIdentity } from "@/lib/memberIdentityServer";
app/api/exegesis/comment/route.ts-11-import { ENTITLEMENTS } from "@/lib/vocab";
app/api/exegesis/comment/route.ts-12-import {
app/api/exegesis/comment/route.ts:13:  runAutoBadgeAwardsForMember,
app/api/exegesis/comment/route.ts-14-  type NewlyAwardedBadge,
app/api/exegesis/comment/route.ts-15-} from "@/lib/badgeAutoAward";
app/api/exegesis/comment/route.ts-16-import { markOverlayAnnouncedForAwardedBadges } from "@/lib/badgeAwardAnnouncementServer";
app/api/exegesis/comment/route.ts-17-
app/api/exegesis/comment/route.ts-18-import { resolveGroupKeyForAnchor } from "@/lib/exegesis/resolveGroupKey";
app/api/exegesis/comment/route.ts-19-import { validateAndSanitizeTipTapDoc } from "@/lib/exegesis/richText";
app/api/exegesis/comment/route.ts-20-import { correlationIdFromRequest, gateError, jsonOk } from "@/app/api/_gate";
--
app/api/exegesis/comment/route.ts-32-type ApiOk = {
app/api/exegesis/comment/route.ts-33-  ok: true;
app/api/exegesis/comment/route.ts-34-  recordingId: string;
app/api/exegesis/comment/route.ts-35-  groupKey: string;
app/api/exegesis/comment/route.ts-36-  comment: CommentDTO;
app/api/exegesis/comment/route.ts-37-  meta: ThreadMetaDTO;
app/api/exegesis/comment/route.ts-38-  identities: Record<string, IdentityDTO>;
app/api/exegesis/comment/route.ts:39:  newlyAwardedBadges: NewlyAwardedBadge[];
app/api/exegesis/comment/route.ts-40-};
app/api/exegesis/comment/route.ts-41-
app/api/exegesis/comment/route.ts-42-type ApiErr = ExegesisApiErr;
app/api/exegesis/comment/route.ts-43-
app/api/exegesis/comment/route.ts-44-type CommentDTO = {
app/api/exegesis/comment/route.ts-45-  id: string;
app/api/exegesis/comment/route.ts-46-  recordingId: string;
--
app/api/exegesis/comment/route.ts-809-  recordingId: string;
app/api/exegesis/comment/route.ts-810-  publicNameUnlockedAt: string | null;
app/api/exegesis/comment/route.ts-811-  correlationId: string;
app/api/exegesis/comment/route.ts-812-}): Promise<NewlyAwardedBadge[]> {
app/api/exegesis/comment/route.ts-813-  let badges: NewlyAwardedBadge[];
app/api/exegesis/comment/route.ts-814-
app/api/exegesis/comment/route.ts-815-  try {
app/api/exegesis/comment/route.ts:816:    badges = await runAutoBadgeAwardsForMember({
app/api/exegesis/comment/route.ts-817-      memberId: params.memberId,
app/api/exegesis/comment/route.ts-818-      trigger:
app/api/exegesis/comment/route.ts-819-        params.publicNameUnlockedAt !== null
app/api/exegesis/comment/route.ts-820-          ? "public_name_unlocked"
app/api/exegesis/comment/route.ts-821-          : "exegesis_contribution_created",
app/api/exegesis/comment/route.ts-822-      recordingId: params.recordingId,
app/api/exegesis/comment/route.ts-823-      grantedBy: "system",
--
app/api/exegesis/comment/route.ts-895-    });
app/api/exegesis/comment/route.ts-896-
app/api/exegesis/comment/route.ts-897-    const insert = resolveInsertResult(req, correlationId, insertRow);
app/api/exegesis/comment/route.ts-898-    if (!insert.ok) return insert.response;
app/api/exegesis/comment/route.ts-899-
app/api/exegesis/comment/route.ts-900-    const { row } = insert;
app/api/exegesis/comment/route.ts-901-    const identities = buildIdentityMap(row, authority.identity);
app/api/exegesis/comment/route.ts:902:    const newlyAwardedBadges = await runPostCommitBadgeEffects({
app/api/exegesis/comment/route.ts-903-      memberId: authority.memberId,
app/api/exegesis/comment/route.ts-904-      recordingId: command.value.recordingId,
app/api/exegesis/comment/route.ts-905-      publicNameUnlockedAt: row.ident_public_name_unlocked_at,
app/api/exegesis/comment/route.ts-906-      correlationId,
app/api/exegesis/comment/route.ts-907-    });
app/api/exegesis/comment/route.ts-908-
app/api/exegesis/comment/route.ts-909-    return jsonOk<ApiOk>(
app/api/exegesis/comment/route.ts-910-      {
app/api/exegesis/comment/route.ts-911-        ok: true,
app/api/exegesis/comment/route.ts-912-        recordingId: command.value.recordingId,
app/api/exegesis/comment/route.ts-913-        groupKey: command.value.groupKey,
app/api/exegesis/comment/route.ts-914-        comment: buildCommentDto(row),
app/api/exegesis/comment/route.ts-915-        meta: buildThreadMetaDto(row),
app/api/exegesis/comment/route.ts-916-        identities,
app/api/exegesis/comment/route.ts:917:        newlyAwardedBadges,
app/api/exegesis/comment/route.ts-918-      },
app/api/exegesis/comment/route.ts-919-      { correlationId },
app/api/exegesis/comment/route.ts-920-    );
app/api/exegesis/comment/route.ts-921-  } catch (error: unknown) {
app/api/exegesis/comment/route.ts-922-    console.error("[exegesis/comment] POST failed", error);
app/api/exegesis/comment/route.ts-923-
app/api/exegesis/comment/route.ts-924-    return jsonErr(correlationId, 500, {
--
app/api/exegesis/vote/route.ts-11-import {
app/api/exegesis/vote/route.ts-12-  correlationIdFromRequest,
app/api/exegesis/vote/route.ts-13-  gateError,
app/api/exegesis/vote/route.ts-14-  jsonOk,
app/api/exegesis/vote/route.ts-15-  withCorrelationId,
app/api/exegesis/vote/route.ts-16-} from "@/app/api/_gate";
app/api/exegesis/vote/route.ts-17-import {
app/api/exegesis/vote/route.ts:18:  runAutoBadgeAwardsForMember,
app/api/exegesis/vote/route.ts-19-  type NewlyAwardedBadge,
app/api/exegesis/vote/route.ts-20-} from "@/lib/badgeAutoAward";
app/api/exegesis/vote/route.ts-21-import { markOverlayAnnouncedForAwardedBadges } from "@/lib/badgeAwardAnnouncementServer";
app/api/exegesis/vote/route.ts-22-
app/api/exegesis/vote/route.ts-23-export const runtime = "nodejs";
app/api/exegesis/vote/route.ts-24-
app/api/exegesis/vote/route.ts-25-type ApiOk = {
app/api/exegesis/vote/route.ts-26-  ok: true;
app/api/exegesis/vote/route.ts-27-  commentId: string;
app/api/exegesis/vote/route.ts-28-  viewerHasVoted: boolean;
app/api/exegesis/vote/route.ts-29-  voteCount: number;
app/api/exegesis/vote/route.ts:30:  newlyAwardedBadges: NewlyAwardedBadge[];
app/api/exegesis/vote/route.ts-31-};
app/api/exegesis/vote/route.ts-32-
app/api/exegesis/vote/route.ts-33-type ApiErr = { ok: false; error: string; gate?: GatePayload };
app/api/exegesis/vote/route.ts-34-
app/api/exegesis/vote/route.ts-35-function jsonErr(correlationId: string, status: number, body: ApiErr) {
app/api/exegesis/vote/route.ts-36-  return withCorrelationId(NextResponse.json(body, { status }), correlationId);
app/api/exegesis/vote/route.ts-37-}
--
app/api/exegesis/vote/route.ts-265-
app/api/exegesis/vote/route.ts-266-      return jsonErr(correlationId, 400, {
app/api/exegesis/vote/route.ts-267-        ok: false,
app/api/exegesis/vote/route.ts-268-        error: "Cannot vote on this comment.",
app/api/exegesis/vote/route.ts-269-      });
app/api/exegesis/vote/route.ts-270-    }
app/api/exegesis/vote/route.ts-271-
app/api/exegesis/vote/route.ts:272:    const newlyAwardedBadges =
app/api/exegesis/vote/route.ts-273-      row.author_member_id && isUuid(row.author_member_id)
app/api/exegesis/vote/route.ts:274:        ? await runAutoBadgeAwardsForMember({
app/api/exegesis/vote/route.ts-275-            memberId: row.author_member_id,
app/api/exegesis/vote/route.ts-276-            trigger: "exegesis_vote_updated",
app/api/exegesis/vote/route.ts-277-            grantedBy: "system",
app/api/exegesis/vote/route.ts-278-            correlationId,
app/api/exegesis/vote/route.ts-279-          })
app/api/exegesis/vote/route.ts-280-        : [];
app/api/exegesis/vote/route.ts-281-
app/api/exegesis/vote/route.ts-282-    if (row.author_member_id && isUuid(row.author_member_id)) {
app/api/exegesis/vote/route.ts-283-      await markOverlayAnnouncedForAwardedBadges({
app/api/exegesis/vote/route.ts-284-        memberId: row.author_member_id,
app/api/exegesis/vote/route.ts:285:        badges: newlyAwardedBadges,
app/api/exegesis/vote/route.ts-286-      });
app/api/exegesis/vote/route.ts-287-    }
app/api/exegesis/vote/route.ts-288-
app/api/exegesis/vote/route.ts-289-    return jsonOk<ApiOk>(
app/api/exegesis/vote/route.ts-290-      {
app/api/exegesis/vote/route.ts-291-        ok: true,
app/api/exegesis/vote/route.ts-292-        commentId,
app/api/exegesis/vote/route.ts-293-        viewerHasVoted: row.viewer_has_voted,
app/api/exegesis/vote/route.ts-294-        voteCount: Number(row.vote_count ?? 0),
app/api/exegesis/vote/route.ts:295:        newlyAwardedBadges,
app/api/exegesis/vote/route.ts-296-      },
app/api/exegesis/vote/route.ts-297-      { correlationId },
app/api/exegesis/vote/route.ts-298-    );
app/api/exegesis/vote/route.ts-299-  } catch (e: unknown) {
app/api/exegesis/vote/route.ts-300-    const msg =
app/api/exegesis/vote/route.ts-301-      e instanceof Error
app/api/exegesis/vote/route.ts-302-        ? norm(e.message)
--
app/api/playback/telemetry/route.ts-643-      persistAnonId(res, anonId);
app/api/playback/telemetry/route.ts-644-    }
app/api/playback/telemetry/route.ts-645-
app/api/playback/telemetry/route.ts-646-    res.headers.set("x-correlation-id", correlationId);
app/api/playback/telemetry/route.ts-647-    return res;
app/api/playback/telemetry/route.ts-648-  }
app/api/playback/telemetry/route.ts-649-
app/api/playback/telemetry/route.ts:650:  let newlyAwardedBadges: NewlyAwardedBadge[] = [];
app/api/playback/telemetry/route.ts-651-
app/api/playback/telemetry/route.ts-652-  if (event === "play") {
app/api/playback/telemetry/route.ts-653-    await Promise.all([
app/api/playback/telemetry/route.ts-654-      memberId
app/api/playback/telemetry/route.ts-655-        ? upsertPlaybackPlay({
app/api/playback/telemetry/route.ts-656-            memberId,
app/api/playback/telemetry/route.ts-657-            recordingId,
--
app/api/playback/telemetry/route.ts-661-      upsertRecordingPlaybackPlay({
app/api/playback/telemetry/route.ts-662-        recordingId,
app/api/playback/telemetry/route.ts-663-        occurredAtIso,
app/api/playback/telemetry/route.ts-664-      }),
app/api/playback/telemetry/route.ts-665-    ]);
app/api/playback/telemetry/route.ts-666-
app/api/playback/telemetry/route.ts-667-    if (memberId) {
app/api/playback/telemetry/route.ts:668:      newlyAwardedBadges = await runPlaybackAutoBadgeAwardsForMember({
app/api/playback/telemetry/route.ts-669-        memberId,
app/api/playback/telemetry/route.ts-670-        recordingId,
app/api/playback/telemetry/route.ts-671-        event: "play",
app/api/playback/telemetry/route.ts-672-        grantedBy: "system",
app/api/playback/telemetry/route.ts-673-        correlationId,
app/api/playback/telemetry/route.ts-674-      });
app/api/playback/telemetry/route.ts-675-
--
app/api/playback/telemetry/route.ts-701-        recordingId,
app/api/playback/telemetry/route.ts-702-        listenedMs,
app/api/playback/telemetry/route.ts-703-        occurredAtIso,
app/api/playback/telemetry/route.ts-704-      }),
app/api/playback/telemetry/route.ts-705-    ]);
app/api/playback/telemetry/route.ts-706-
app/api/playback/telemetry/route.ts-707-    if (memberId) {
app/api/playback/telemetry/route.ts:708:      newlyAwardedBadges = await runPlaybackAutoBadgeAwardsForMember({
app/api/playback/telemetry/route.ts-709-        memberId,
app/api/playback/telemetry/route.ts-710-        recordingId,
app/api/playback/telemetry/route.ts-711-        event: "progress",
app/api/playback/telemetry/route.ts-712-        grantedBy: "system",
app/api/playback/telemetry/route.ts-713-        correlationId,
app/api/playback/telemetry/route.ts-714-      });
app/api/playback/telemetry/route.ts-715-
--
app/api/playback/telemetry/route.ts-740-      upsertRecordingPlaybackComplete({
app/api/playback/telemetry/route.ts-741-        recordingId,
app/api/playback/telemetry/route.ts-742-        occurredAtIso,
app/api/playback/telemetry/route.ts-743-      }),
app/api/playback/telemetry/route.ts-744-    ]);
app/api/playback/telemetry/route.ts-745-
app/api/playback/telemetry/route.ts-746-    if (memberId) {
app/api/playback/telemetry/route.ts:747:      newlyAwardedBadges = await runPlaybackAutoBadgeAwardsForMember({
app/api/playback/telemetry/route.ts-748-        memberId,
app/api/playback/telemetry/route.ts-749-        recordingId,
app/api/playback/telemetry/route.ts-750-        event: "complete",
app/api/playback/telemetry/route.ts-751-        grantedBy: "system",
app/api/playback/telemetry/route.ts-752-        correlationId,
app/api/playback/telemetry/route.ts-753-      });
app/api/playback/telemetry/route.ts-754-
--
app/api/playback/telemetry/route.ts-764-          duration_ms: durationMs,
app/api/playback/telemetry/route.ts-765-          clerk_user_id: userId,
app/api/playback/telemetry/route.ts-766-        },
app/api/playback/telemetry/route.ts-767-      });
app/api/playback/telemetry/route.ts-768-    }
app/api/playback/telemetry/route.ts-769-  }
app/api/playback/telemetry/route.ts-770-
app/api/playback/telemetry/route.ts:771:  if (memberId && newlyAwardedBadges.length > 0) {
app/api/playback/telemetry/route.ts-772-    await markOverlayAnnouncedForAwardedBadges({
app/api/playback/telemetry/route.ts-773-      memberId,
app/api/playback/telemetry/route.ts:774:      badges: newlyAwardedBadges,
app/api/playback/telemetry/route.ts-775-    });
app/api/playback/telemetry/route.ts-776-  }
app/api/playback/telemetry/route.ts-777-
app/api/playback/telemetry/route.ts:778:  const res = NextResponse.json({ ok: true, newlyAwardedBadges });
app/api/playback/telemetry/route.ts-779-
app/api/playback/telemetry/route.ts-780-  if (isNewAnonId) {
app/api/playback/telemetry/route.ts-781-    persistAnonId(res, anonId);
app/api/playback/telemetry/route.ts-782-  }
app/api/playback/telemetry/route.ts-783-
app/api/playback/telemetry/route.ts-784-  res.headers.set("x-correlation-id", correlationId);
app/api/playback/telemetry/route.ts-785-  return res;
--
app/(site)/SiteProviders.tsx-2-"use client";
app/(site)/SiteProviders.tsx-3-
app/(site)/SiteProviders.tsx-4-import React from "react";
app/(site)/SiteProviders.tsx-5-import PlayerHost from "./PlayerHost";
app/(site)/SiteProviders.tsx-6-import { MembershipModalProvider } from "@/app/home/MembershipModalProvider";
app/(site)/SiteProviders.tsx-7-import { GateBrokerProvider } from "@/app/home/gating/GateBroker";
app/(site)/SiteProviders.tsx-8-import AdminRibbonBootstrap from "@/app/home/AdminRibbonBootstrap";
app/(site)/SiteProviders.tsx:9:import { BadgeAwardOverlayProvider } from "@/app/home/badges/BadgeAwardOverlayProvider";
app/(site)/SiteProviders.tsx-10-
app/(site)/SiteProviders.tsx-11-export default function SiteProviders(props: { children: React.ReactNode }) {
app/(site)/SiteProviders.tsx-12-  return (
app/(site)/SiteProviders.tsx-13-    <GateBrokerProvider>
app/(site)/SiteProviders.tsx:14:      <BadgeAwardOverlayProvider>
app/(site)/SiteProviders.tsx-15-        <MembershipModalProvider>
app/(site)/SiteProviders.tsx-16-          <PlayerHost>
app/(site)/SiteProviders.tsx-17-            <AdminRibbonBootstrap />
app/(site)/SiteProviders.tsx-18-            {props.children}
app/(site)/SiteProviders.tsx-19-          </PlayerHost>
app/(site)/SiteProviders.tsx-20-        </MembershipModalProvider>
app/(site)/SiteProviders.tsx:21:      </BadgeAwardOverlayProvider>
app/(site)/SiteProviders.tsx-22-    </GateBrokerProvider>
app/(site)/SiteProviders.tsx-23-  );
app/(site)/SiteProviders.tsx-24-}
--
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-23-} from "../exegesisTypes";
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-24-import type { ExegesisSelectedLine } from "./useExegesisHashState";
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-25-import {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-26-  reorderRootsPinnedFirst,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-27-  rootRecentTs,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-28-  rootTopScore,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-29-} from "../exegesisUi";
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:30:import { useBadgeAwardOverlay } from "@/app/home/badges/BadgeAwardOverlayProvider";
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:31:import { normalizeBadgeAwardNotices } from "@/app/home/badges/badgeAwardTypes";
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-32-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-33-type ThreadGatePayload = NonNullable<ThreadApiErr["gate"]>;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-34-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-35-async function fetchVoteRecoveryThread(
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-36-  params: Readonly<{
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-37-    recordingId: string;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-38-    selected: ExegesisSelectedLine | null;
--
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-130-    setClaimBusy,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-131-  } = props;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-132-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-133-  const [thread, setThread] = React.useState<ThreadApiOk | null>(null);
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-134-  const [threadErr, setThreadErr] = React.useState<string>("");
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-135-  const [threadLoading, setThreadLoading] = React.useState<boolean>(false);
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-136-  const [threadLoadedKey, setThreadLoadedKey] = React.useState<string>("");
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:137:  const { announceBadges } = useBadgeAwardOverlay();
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-138-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-139-  const isAnon = authLoaded ? !userId : false;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-140-  const viewerKey = userId ?? "anon";
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-141-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-142-  const threadWantedCoreKey = React.useMemo(() => {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-143-    const lk = (selected?.lineKey ?? "").trim();
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-144-    const gk = (selected?.groupKey ?? "").trim();
--
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-199-    ? `${thread.recordingId}::${thread.groupKey}::${thread.meta?.commentCount ?? 0}`
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-200-    : "";
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-201-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-202-  const prevUserIdRef = React.useRef<string | null>(null);
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-203-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-204-  const announceAwardPayload = React.useCallback(
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-205-    (payload: unknown) => {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:206:      const badges = normalizeBadgeAwardNotices(payload);
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-207-      if (badges.length === 0) return;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-208-      announceBadges(badges);
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-209-    },
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-210-    [announceBadges],
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-211-  );
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-212-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-213-  React.useEffect(() => {
--
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-554-          tMs: selected.tMs,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-555-          lineTextSnapshot: selected.lineText,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-556-          lyricsVersion: lyricsVersion ?? null,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-557-        }),
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-558-      });
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-559-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-560-      const j = (await r.json()) as
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:561:        | (CommentPostOk & { newlyAwardedBadges?: unknown })
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-562-        | {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-563-            ok: false;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-564-            error: string;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-565-            gate?: ThreadGatePayload;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:566:            newlyAwardedBadges?: unknown;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-567-          };
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-568-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-569-      if (!j.ok) {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-570-        if (j.gate) {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-571-          const res = gateResultFromPayload({
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-572-            payload: j.gate,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-573-            attempt: { verb: "postComment", domain: "exegesis" },
--
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-584-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-585-        setThreadErr(j.error || "Failed to post comment.");
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-586-        return;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-587-      }
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-588-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-589-      setDraft("");
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-590-      setDraftDoc(null);
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:591:      announceAwardPayload(j.newlyAwardedBadges);
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-592-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-593-      pendingScrollCommentIdRef.current = j.comment.id;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-594-      setHash({
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-595-        lineKey: selected.lineKey,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-596-        commentId: j.comment.id,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-597-        rootId: focusedRootId || undefined,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-598-      });
--
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-703-          tMs: selected.tMs,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-704-          lineTextSnapshot: selected.lineText,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-705-          lyricsVersion: lyricsVersion ?? null,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-706-        }),
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-707-      });
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-708-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-709-      const j = (await r.json()) as
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:710:        | (CommentPostOk & { newlyAwardedBadges?: unknown })
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-711-        | {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-712-            ok: false;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-713-            error: string;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-714-            gate?: ThreadGatePayload;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:715:            newlyAwardedBadges?: unknown;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-716-          };
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-717-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-718-      if (!j.ok) {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-719-        if (j.gate) {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-720-          const res = gateResultFromPayload({
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-721-            payload: j.gate,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-722-            attempt: { verb: "postComment", domain: "exegesis" },
--
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-742-          posting: false,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-743-          open: false,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-744-          plain: "",
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-745-          doc: null,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-746-          err: "",
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-747-        },
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-748-      }));
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:749:      announceAwardPayload(j.newlyAwardedBadges);
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-750-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-751-      pendingScrollCommentIdRef.current = j.comment.id;
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-752-      setHash({
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-753-        lineKey: selected.lineKey,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-754-        commentId: j.comment.id,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-755-        rootId: focusedRootId || undefined,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-756-      });
--
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-836-    const r = await fetch("/api/exegesis/vote", {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-837-      method: "POST",
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-838-      headers: { "content-type": "application/json" },
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-839-      body: JSON.stringify({ commentId }),
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-840-    });
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-841-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-842-    const j = (await r.json()) as
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:843:      | (VoteOk & { newlyAwardedBadges?: unknown })
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:844:      | (VoteErr & { newlyAwardedBadges?: unknown });
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-845-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-846-    if (!j.ok) {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-847-      if (j.gate) {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-848-        const res = gateResultFromPayload({
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-849-          payload: j.gate,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-850-          attempt: { verb: "vote", domain: "exegesis" },
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-851-          isSignedIn: Boolean(userId),
--
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-883-            ? { ...c, viewerHasVoted: j.viewerHasVoted, voteCount: j.voteCount }
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-884-            : c,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-885-        ),
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-886-      }));
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-887-      return { ...prev, roots };
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-888-    });
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-889-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts:890:    announceAwardPayload(j.newlyAwardedBadges);
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-891-  }
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-892-
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-893-  return {
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-894-    thread,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-895-    setThread,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-896-    threadErr,
app/(site)/exegesis/[displayId]/hooks/useExegesisThread.ts-897-    setThreadErr,
--
app/home/modules/PortalMemberPanel.tsx-1-// web/app/home/modules/PortalMemberPanel.tsx
app/home/modules/PortalMemberPanel.tsx-2-"use client";
app/home/modules/PortalMemberPanel.tsx-3-
app/home/modules/PortalMemberPanel.tsx-4-import React from "react";
app/home/modules/PortalMemberPanel.tsx-5-import type { PortalMemberSummary } from "@/lib/memberDashboard";
app/home/modules/PortalMemberPanel.tsx:6:import BadgeCabinet from "./badges/BadgeCabinet";
app/home/modules/PortalMemberPanel.tsx-7-
app/home/modules/PortalMemberPanel.tsx-8-type Props = {
app/home/modules/PortalMemberPanel.tsx-9-  summary: PortalMemberSummary;
app/home/modules/PortalMemberPanel.tsx-10-  title?: string;
app/home/modules/PortalMemberPanel.tsx-11-  embedded?: boolean;
app/home/modules/PortalMemberPanel.tsx-12-};
app/home/modules/PortalMemberPanel.tsx-13-
--
app/home/modules/PortalMemberPanel.tsx-112-              overflowWrap: "anywhere",
app/home/modules/PortalMemberPanel.tsx-113-            }}
app/home/modules/PortalMemberPanel.tsx-114-          >
app/home/modules/PortalMemberPanel.tsx-115-            {displayName}
app/home/modules/PortalMemberPanel.tsx-116-          </div>
app/home/modules/PortalMemberPanel.tsx-117-
app/home/modules/PortalMemberPanel.tsx-118-          <div style={{ marginTop: 14, minWidth: 0 }}>
app/home/modules/PortalMemberPanel.tsx:119:            <BadgeCabinet badges={summary.badges} />
app/home/modules/PortalMemberPanel.tsx-120-          </div>
app/home/modules/PortalMemberPanel.tsx-121-        </div>
app/home/modules/PortalMemberPanel.tsx-122-
app/home/modules/PortalMemberPanel.tsx-123-        <MetricTable
app/home/modules/PortalMemberPanel.tsx-124-          rows={[
app/home/modules/PortalMemberPanel.tsx-125-            {
app/home/modules/PortalMemberPanel.tsx-126-              label: "Minutes streamed",
--
app/home/player/AudioEngine.tsx-13-  GateDomain,
app/home/player/AudioEngine.tsx-14-  GateAction,
app/home/player/AudioEngine.tsx-15-  GateCodeRaw,
app/home/player/AudioEngine.tsx-16-} from "@/app/home/gating/gateTypes";
app/home/player/AudioEngine.tsx-17-import { normalizeGateCodeRaw } from "@/app/home/gating/gateTypes";
app/home/player/AudioEngine.tsx-18-import { gateResultFromPayload } from "@/app/home/gating/fromPayload";
app/home/player/AudioEngine.tsx-19-import { useGateBroker } from "@/app/home/gating/GateBroker";
app/home/player/AudioEngine.tsx:20:import { useBadgeAwardOverlay } from "@/app/home/badges/BadgeAwardOverlayProvider";
app/home/player/AudioEngine.tsx:21:import { normalizeBadgeAwardNotices } from "@/app/home/badges/badgeAwardTypes";
app/home/player/AudioEngine.tsx-22-
app/home/player/AudioEngine.tsx-23-type TokenResponse =
app/home/player/AudioEngine.tsx-24-  | { ok: true; token: string; expiresAt: string | number }
app/home/player/AudioEngine.tsx-25-  | { ok: false; error: string; gate?: GatePayload };
app/home/player/AudioEngine.tsx-26-
app/home/player/AudioEngine.tsx-27-type AlbumSessionToken = {
app/home/player/AudioEngine.tsx-28-  recordingId: string;
--
app/home/player/AudioEngine.tsx-581-  const p = usePlayer();
app/home/player/AudioEngine.tsx-582-  const { isLoaded: isUserLoaded, isSignedIn } = useUser();
app/home/player/AudioEngine.tsx-583-
app/home/player/AudioEngine.tsx-584-  const audioARef = React.useRef<HTMLAudioElement | null>(null);
app/home/player/AudioEngine.tsx-585-  const audioBRef = React.useRef<HTMLAudioElement | null>(null);
app/home/player/AudioEngine.tsx-586-
app/home/player/AudioEngine.tsx-587-  const { reportGate, clearGate } = useGateBroker();
app/home/player/AudioEngine.tsx:588:  const { announceBadges } = useBadgeAwardOverlay();
app/home/player/AudioEngine.tsx-589-
app/home/player/AudioEngine.tsx-590-  const hlsByDeckRef = React.useRef<Record<DeckId, Hls | null>>({
app/home/player/AudioEngine.tsx-591-    a: null,
app/home/player/AudioEngine.tsx-592-    b: null,
app/home/player/AudioEngine.tsx-593-  });
app/home/player/AudioEngine.tsx-594-  const metaByDeckRef = React.useRef<Record<DeckId, DeckMeta | null>>({
app/home/player/AudioEngine.tsx-595-    a: null,
--
app/home/player/AudioEngine.tsx-2714-          try {
app/home/player/AudioEngine.tsx-2715-            json = await response.json();
app/home/player/AudioEngine.tsx-2716-          } catch {
app/home/player/AudioEngine.tsx-2717-            json = null;
app/home/player/AudioEngine.tsx-2718-          }
app/home/player/AudioEngine.tsx-2719-
app/home/player/AudioEngine.tsx-2720-          const badges =
app/home/player/AudioEngine.tsx:2721:            json && typeof json === "object" && "newlyAwardedBadges" in json
app/home/player/AudioEngine.tsx:2722:              ? normalizeBadgeAwardNotices(
app/home/player/AudioEngine.tsx:2723:                  (json as { newlyAwardedBadges?: unknown }).newlyAwardedBadges,
app/home/player/AudioEngine.tsx-2724-                )
app/home/player/AudioEngine.tsx-2725-              : [];
app/home/player/AudioEngine.tsx-2726-
app/home/player/AudioEngine.tsx-2727-          if (badges.length > 0) {
app/home/player/AudioEngine.tsx-2728-            announceBadges(badges);
app/home/player/AudioEngine.tsx-2729-          }
app/home/player/AudioEngine.tsx-2730-

===== PLAYBACK → BADGE AUTO-AWARD BOUNDARY — SNIPPETS ONLY =====

5-import { auth } from "@clerk/nextjs/server";
6-import { sql } from "@vercel/postgres";
7-import {
8-  logPlaybackTelemetryComplete,
9-  logPlaybackTelemetryPlay,
10-  logPlaybackTelemetryProgress,
11-  newCorrelationId,
12-} from "@/lib/events";
13-import { EVENT_SOURCES, EVENT_TYPES } from "@/lib/vocab";
14-import {
15:  runPlaybackAutoBadgeAwardsForMember,
16:  type NewlyAwardedBadge,
17:} from "@/lib/badgeAutoAward";
18:import { markOverlayAnnouncedForAwardedBadges } from "@/lib/badgeAwardAnnouncementServer";
19-import { ensureAnonId, persistAnonId } from "@/lib/anon";
20-import {
21-  recordShareTokenPlaybackEvent,
22-  resolveShareTokenPlaybackContext,
23-} from "@/lib/shareTokenPlaybackContext";
24-
25-type PlaybackTelemetryEvent = "play" | "progress" | "complete";
26-
27-type PlaybackTelemetryRequest = {
28-  event?: PlaybackTelemetryEvent;
--
640-    const res = NextResponse.json({ ok: true, deduped: true });
641-
642-    if (isNewAnonId) {
643-      persistAnonId(res, anonId);
644-    }
645-
646-    res.headers.set("x-correlation-id", correlationId);
647-    return res;
648-  }
649-
650:  let newlyAwardedBadges: NewlyAwardedBadge[] = [];
651-
652-  if (event === "play") {
653-    await Promise.all([
654-      memberId
655-        ? upsertPlaybackPlay({
656-            memberId,
657-            recordingId,
658-            occurredAtIso,
659-          })
660-        : Promise.resolve(),
661-      upsertRecordingPlaybackPlay({
662-        recordingId,
663-        occurredAtIso,
664-      }),
665-    ]);
666-
667-    if (memberId) {
668:      newlyAwardedBadges = await runPlaybackAutoBadgeAwardsForMember({
669-        memberId,
670-        recordingId,
671-        event: "play",
672-        grantedBy: "system",
673-        correlationId,
674-      });
675-
676-      await logPlaybackTelemetryPlay({
677-        memberId,
678-        source: EVENT_SOURCES.SERVER,
--
698-          })
699-        : Promise.resolve(),
700-      upsertRecordingPlaybackProgress({
701-        recordingId,
702-        listenedMs,
703-        occurredAtIso,
704-      }),
705-    ]);
706-
707-    if (memberId) {
708:      newlyAwardedBadges = await runPlaybackAutoBadgeAwardsForMember({
709-        memberId,
710-        recordingId,
711-        event: "progress",
712-        grantedBy: "system",
713-        correlationId,
714-      });
715-
716-      await logPlaybackTelemetryProgress({
717-        memberId,
718-        source: EVENT_SOURCES.SERVER,
--
737-            occurredAtIso,
738-          })
739-        : Promise.resolve(),
740-      upsertRecordingPlaybackComplete({
741-        recordingId,
742-        occurredAtIso,
743-      }),
744-    ]);
745-
746-    if (memberId) {
747:      newlyAwardedBadges = await runPlaybackAutoBadgeAwardsForMember({
748-        memberId,
749-        recordingId,
750-        event: "complete",
751-        grantedBy: "system",
752-        correlationId,
753-      });
754-
755-      await logPlaybackTelemetryComplete({
756-        memberId,
757-        source: EVENT_SOURCES.SERVER,
--
761-          playback_id: playbackId,
762-          milestone_key: milestoneKey,
763-          progress_ms: progressMs,
764-          duration_ms: durationMs,
765-          clerk_user_id: userId,
766-        },
767-      });
768-    }
769-  }
770-
771:  if (memberId && newlyAwardedBadges.length > 0) {
772:    await markOverlayAnnouncedForAwardedBadges({
773-      memberId,
774:      badges: newlyAwardedBadges,
775-    });
776-  }
777-
778:  const res = NextResponse.json({ ok: true, newlyAwardedBadges });
779-
780-  if (isNewAnonId) {
781-    persistAnonId(res, anonId);
782-  }
783-
784-  res.headers.set("x-correlation-id", correlationId);
785-  return res;
786-}

===== ADMIN BADGE ENDPOINT / CLIENT CONTRACT REFERENCES =====

lib/badgePreviewModes.ts-1-// web/lib/badgePreviewModes.ts
lib/badgePreviewModes.ts:2:export type BadgeQualificationMode =
lib/badgePreviewModes.ts-3-  | "minutes_streamed"
lib/badgePreviewModes.ts-4-  | "play_count"
lib/badgePreviewModes.ts-5-  | "complete_count"
lib/badgePreviewModes.ts-6-  | "joined_within_window"
lib/badgePreviewModes.ts-7-  | "active_within_window"
lib/badgePreviewModes.ts-8-  | "recording_minutes_streamed"
--
lib/badgePreviewModes.ts-10-  | "recording_complete_count"
lib/badgePreviewModes.ts-11-  | "exegesis_contribution_count"
lib/badgePreviewModes.ts-12-  | "exegesis_vote_tally"
lib/badgePreviewModes.ts-13-  | "public_name_unlocked";
lib/badgePreviewModes.ts-14-
lib/badgePreviewModes.ts-15-export type BadgePreviewModeDescriptor = {
lib/badgePreviewModes.ts:16:  key: BadgeQualificationMode;
lib/badgePreviewModes.ts-17-  label: string;
lib/badgePreviewModes.ts-18-  description: string;
lib/badgePreviewModes.ts-19-  requiresRecording: boolean;
lib/badgePreviewModes.ts-20-  supportsDateWindow: boolean;
lib/badgePreviewModes.ts-21-  metricFamily:
lib/badgePreviewModes.ts-22-    | "minutes"
--
lib/badgePreviewModes.ts-60-    recordingIdPlaceholder: string;
lib/badgePreviewModes.ts-61-    recordingIdHelp: string | null;
lib/badgePreviewModes.ts-62-  };
lib/badgePreviewModes.ts-63-};
lib/badgePreviewModes.ts-64-
lib/badgePreviewModes.ts-65-export const BADGE_PREVIEW_MODE_DESCRIPTORS: Record<
lib/badgePreviewModes.ts:66:  BadgeQualificationMode,
lib/badgePreviewModes.ts-67-  BadgePreviewModeDescriptor
lib/badgePreviewModes.ts-68-> = {
lib/badgePreviewModes.ts-69-  minutes_streamed: {
lib/badgePreviewModes.ts-70-    key: "minutes_streamed",
lib/badgePreviewModes.ts-71-    label: "Total minutes streamed",
lib/badgePreviewModes.ts-72-    description: "Qualify members by all-time listening minutes.",
--
lib/badgePreviewModes.ts-562-  BADGE_PREVIEW_MODE_DESCRIPTORS.exegesis_contribution_count,
lib/badgePreviewModes.ts-563-  BADGE_PREVIEW_MODE_DESCRIPTORS.exegesis_vote_tally,
lib/badgePreviewModes.ts-564-  BADGE_PREVIEW_MODE_DESCRIPTORS.public_name_unlocked,
lib/badgePreviewModes.ts-565-];
lib/badgePreviewModes.ts-566-
lib/badgePreviewModes.ts-567-export function getBadgePreviewModeDescriptor(
lib/badgePreviewModes.ts:568:  mode: BadgeQualificationMode,
lib/badgePreviewModes.ts-569-): BadgePreviewModeDescriptor {
lib/badgePreviewModes.ts-570-  return BADGE_PREVIEW_MODE_DESCRIPTORS[mode];
lib/badgePreviewModes.ts-571-}
lib/badgePreviewModes.ts-572-
lib/badgePreviewModes.ts-573-export function isRecordingScopedBadgePreviewMode(
lib/badgePreviewModes.ts:574:  mode: BadgeQualificationMode,
lib/badgePreviewModes.ts-575-): boolean {
lib/badgePreviewModes.ts-576-  return BADGE_PREVIEW_MODE_DESCRIPTORS[mode].requiresRecording;
lib/badgePreviewModes.ts-577-}
--
lib/badgeAdmin.ts-5-import { getRecordingSummaryByRecordingId } from "@/lib/albums";
lib/badgeAdmin.ts-6-import { areBadgesEnabled } from "@/lib/badgeFeature";
lib/badgeAdmin.ts-7-import { grantEntitlement } from "@/lib/entitlementOps";
lib/badgeAdmin.ts-8-
lib/badgeAdmin.ts-9-export type AwardBadgeToMembersInput = {
lib/badgeAdmin.ts-10-  entitlementKey: string;
lib/badgeAdmin.ts:11:  memberIds: string[];
lib/badgeAdmin.ts-12-  grantedBy: string;
lib/badgeAdmin.ts:13:  grantReason?: string | null;
lib/badgeAdmin.ts:14:  grantSource?: string | null;
lib/badgeAdmin.ts:15:  grantSourceRef?: string | null;
lib/badgeAdmin.ts-16-};
lib/badgeAdmin.ts-17-
lib/badgeAdmin.ts-18-export type AwardBadgeToMembersResult = {
lib/badgeAdmin.ts-19-  entitlementKey: string;
lib/badgeAdmin.ts-20-  attempted: number;
lib/badgeAdmin.ts-21-  inserted: number;
lib/badgeAdmin.ts-22-  alreadyHeld: number;
lib/badgeAdmin.ts-23-};
lib/badgeAdmin.ts-24-
lib/badgeAdmin.ts:25:function uniqueMemberIds(memberIds: string[]): string[] {
lib/badgeAdmin.ts-26-  const seen = new Set<string>();
lib/badgeAdmin.ts-27-  const ordered: string[] = [];
lib/badgeAdmin.ts-28-
lib/badgeAdmin.ts:29:  for (const memberId of memberIds) {
lib/badgeAdmin.ts-30-    const normalized = memberId.trim();
lib/badgeAdmin.ts-31-    if (!normalized || seen.has(normalized)) continue;
lib/badgeAdmin.ts-32-    seen.add(normalized);
lib/badgeAdmin.ts-33-    ordered.push(normalized);
lib/badgeAdmin.ts-34-  }
lib/badgeAdmin.ts-35-
--
lib/badgeAdmin.ts-41-): Promise<AwardBadgeToMembersResult> {
lib/badgeAdmin.ts-42-  if (!areBadgesEnabled()) {
lib/badgeAdmin.ts-43-    throw new Error("Badge system is disabled.");
lib/badgeAdmin.ts-44-  }
lib/badgeAdmin.ts-45-
lib/badgeAdmin.ts-46-  const entitlementKey = input.entitlementKey.trim();
lib/badgeAdmin.ts:47:  const memberIds = uniqueMemberIds(input.memberIds);
lib/badgeAdmin.ts-48-
lib/badgeAdmin.ts-49-  let inserted = 0;
lib/badgeAdmin.ts-50-  let alreadyHeld = 0;
lib/badgeAdmin.ts-51-
lib/badgeAdmin.ts:52:  for (const memberId of memberIds) {
lib/badgeAdmin.ts-53-    const result = await grantEntitlement({
lib/badgeAdmin.ts-54-      memberId,
lib/badgeAdmin.ts-55-      entitlementKey,
lib/badgeAdmin.ts-56-      scopeId: null,
lib/badgeAdmin.ts-57-      grantedBy: input.grantedBy,
lib/badgeAdmin.ts:58:      grantReason: input.grantReason ?? undefined,
lib/badgeAdmin.ts:59:      grantSource: input.grantSource ?? "badge_rule",
lib/badgeAdmin.ts:60:      grantSourceRef: input.grantSourceRef ?? undefined,
lib/badgeAdmin.ts-61-    });
lib/badgeAdmin.ts-62-
lib/badgeAdmin.ts-63-    if (result.status === "inserted") {
lib/badgeAdmin.ts-64-      inserted += 1;
lib/badgeAdmin.ts-65-      continue;
lib/badgeAdmin.ts-66-    }
lib/badgeAdmin.ts-67-
lib/badgeAdmin.ts-68-    alreadyHeld += 1;
lib/badgeAdmin.ts-69-  }
lib/badgeAdmin.ts-70-
lib/badgeAdmin.ts-71-  return {
lib/badgeAdmin.ts-72-    entitlementKey,
lib/badgeAdmin.ts:73:    attempted: memberIds.length,
lib/badgeAdmin.ts-74-    inserted,
lib/badgeAdmin.ts-75-    alreadyHeld,
lib/badgeAdmin.ts-76-  };
lib/badgeAdmin.ts-77-}
lib/badgeAdmin.ts-78-
lib/badgeAdmin.ts-79-export type BadgePreviewMemberRow = {
--
lib/badgeAdmin.ts-162-
lib/badgeAdmin.ts-163-export type PublicNameUnlockedPreviewInput = {
lib/badgeAdmin.ts-164-  mode: "public_name_unlocked";
lib/badgeAdmin.ts-165-  limit?: number;
lib/badgeAdmin.ts-166-};
lib/badgeAdmin.ts-167-
lib/badgeAdmin.ts:168:export type BadgePreviewInput =
lib/badgeAdmin.ts-169-  | MinutesStreamedPreviewInput
lib/badgeAdmin.ts-170-  | PlayCountPreviewInput
lib/badgeAdmin.ts-171-  | CompleteCountPreviewInput
lib/badgeAdmin.ts-172-  | JoinedWithinWindowPreviewInput
lib/badgeAdmin.ts-173-  | ActiveWithinWindowPreviewInput
lib/badgeAdmin.ts-174-  | RecordingMinutesStreamedPreviewInput
--
lib/badgeAdmin.ts-358-        row.matchedRecordingId,
lib/badgeAdmin.ts-359-    };
lib/badgeAdmin.ts-360-  });
lib/badgeAdmin.ts-361-}
lib/badgeAdmin.ts-362-
lib/badgeAdmin.ts-363-export async function previewBadgeQualification(
lib/badgeAdmin.ts:364:  input: BadgePreviewInput,
lib/badgeAdmin.ts-365-): Promise<BadgePreviewMemberRow[]> {
lib/badgeAdmin.ts-366-  const handler = BADGE_PREVIEW_HANDLERS[input.mode] as (
lib/badgeAdmin.ts:367:    value: BadgePreviewInput,
lib/badgeAdmin.ts-368-  ) => Promise<BadgePreviewMemberRow[]>;
lib/badgeAdmin.ts-369-
lib/badgeAdmin.ts-370-  const rows = await handler(input);
lib/badgeAdmin.ts-371-  return hydratePreviewRecordingTitles(rows);
lib/badgeAdmin.ts-372-}
lib/badgeAdmin.ts-373-
--
app/admin/badges/BadgeDashboardClient.tsx-5-import { BADGE_PREVIEW_MODES } from "@/lib/badgePreviewModes";
app/admin/badges/BadgeDashboardClient.tsx-6-import AdminPageFrame from "../AdminPageFrame";
app/admin/badges/BadgeDashboardClient.tsx-7-import { BadgeCatalogueSection } from "./_components/BadgeCatalogueSection";
app/admin/badges/BadgeDashboardClient.tsx-8-import { BadgeQualificationFormSection } from "./_components/BadgeQualificationFormSection";
app/admin/badges/BadgeDashboardClient.tsx-9-import { PreviewResultsSection } from "./_components/PreviewResultsSection";
app/admin/badges/BadgeDashboardClient.tsx-10-import type {
app/admin/badges/BadgeDashboardClient.tsx:11:  AwardResponse,
app/admin/badges/BadgeDashboardClient.tsx-12-  BadgeDefinitionOption,
app/admin/badges/BadgeDashboardClient.tsx-13-  FormState,
app/admin/badges/BadgeDashboardClient.tsx:14:  PreviewResponse,
app/admin/badges/BadgeDashboardClient.tsx-15-  PreviewRow,
app/admin/badges/BadgeDashboardClient.tsx-16-  Props,
app/admin/badges/BadgeDashboardClient.tsx-17-  RecordingSearchResponse,
app/admin/badges/BadgeDashboardClient.tsx-18-  RecordingSearchResult,
app/admin/badges/BadgeDashboardClient.tsx-19-} from "./_lib/badgeDashboardTypes";
app/admin/badges/BadgeDashboardClient.tsx-20-import {
app/admin/badges/BadgeDashboardClient.tsx:21:  buildPreviewPayload,
app/admin/badges/BadgeDashboardClient.tsx-22-  DEFAULT_FORM_STATE,
app/admin/badges/BadgeDashboardClient.tsx-23-} from "./_lib/badgeDashboardUtils";
app/admin/badges/BadgeDashboardClient.tsx-24-
app/admin/badges/BadgeDashboardClient.tsx-25-export default function BadgeDashboardClient({
app/admin/badges/BadgeDashboardClient.tsx-26-  embed,
app/admin/badges/BadgeDashboardClient.tsx-27-  badgeDefinitions,
--
app/admin/badges/BadgeDashboardClient.tsx-139-      const params = new URLSearchParams({
app/admin/badges/BadgeDashboardClient.tsx-140-        q: query,
app/admin/badges/BadgeDashboardClient.tsx-141-        limit: "12",
app/admin/badges/BadgeDashboardClient.tsx-142-      });
app/admin/badges/BadgeDashboardClient.tsx-143-
app/admin/badges/BadgeDashboardClient.tsx-144-      const response = await fetch(
app/admin/badges/BadgeDashboardClient.tsx:145:        `/api/admin/recordings/search?${params.toString()}`,
app/admin/badges/BadgeDashboardClient.tsx-146-        {
app/admin/badges/BadgeDashboardClient.tsx-147-          method: "GET",
app/admin/badges/BadgeDashboardClient.tsx-148-        },
app/admin/badges/BadgeDashboardClient.tsx-149-      );
app/admin/badges/BadgeDashboardClient.tsx-150-
app/admin/badges/BadgeDashboardClient.tsx-151-      const json = (await response.json()) as RecordingSearchResponse;
--
app/admin/badges/BadgeDashboardClient.tsx-206-    setPreviewLoading(true);
app/admin/badges/BadgeDashboardClient.tsx-207-    setPreviewError(null);
app/admin/badges/BadgeDashboardClient.tsx-208-    setAwardError(null);
app/admin/badges/BadgeDashboardClient.tsx-209-    setAwardMessage(null);
app/admin/badges/BadgeDashboardClient.tsx-210-
app/admin/badges/BadgeDashboardClient.tsx-211-    try {
app/admin/badges/BadgeDashboardClient.tsx:212:      const response = await fetch("/api/admin/badges/preview", {
app/admin/badges/BadgeDashboardClient.tsx-213-        method: "POST",
app/admin/badges/BadgeDashboardClient.tsx-214-        headers: {
app/admin/badges/BadgeDashboardClient.tsx-215-          "Content-Type": "application/json",
app/admin/badges/BadgeDashboardClient.tsx-216-        },
app/admin/badges/BadgeDashboardClient.tsx:217:        body: JSON.stringify(buildPreviewPayload(form)),
app/admin/badges/BadgeDashboardClient.tsx-218-      });
app/admin/badges/BadgeDashboardClient.tsx-219-
app/admin/badges/BadgeDashboardClient.tsx:220:      const json = (await response.json()) as PreviewResponse;
app/admin/badges/BadgeDashboardClient.tsx-221-
app/admin/badges/BadgeDashboardClient.tsx-222-      if (!response.ok || !json.ok) {
app/admin/badges/BadgeDashboardClient.tsx-223-        throw new Error(json.error || "Unable to preview badge cohort.");
app/admin/badges/BadgeDashboardClient.tsx-224-      }
app/admin/badges/BadgeDashboardClient.tsx-225-
app/admin/badges/BadgeDashboardClient.tsx-226-      setPreviewRows(Array.isArray(json.rows) ? json.rows : []);
--
app/admin/badges/BadgeDashboardClient.tsx-251-
app/admin/badges/BadgeDashboardClient.tsx-252-    setAwardLoading(true);
app/admin/badges/BadgeDashboardClient.tsx-253-    setAwardError(null);
app/admin/badges/BadgeDashboardClient.tsx-254-    setAwardMessage(null);
app/admin/badges/BadgeDashboardClient.tsx-255-
app/admin/badges/BadgeDashboardClient.tsx-256-    try {
app/admin/badges/BadgeDashboardClient.tsx:257:      const response = await fetch("/api/admin/badges/award", {
app/admin/badges/BadgeDashboardClient.tsx-258-        method: "POST",
app/admin/badges/BadgeDashboardClient.tsx-259-        headers: {
app/admin/badges/BadgeDashboardClient.tsx-260-          "Content-Type": "application/json",
app/admin/badges/BadgeDashboardClient.tsx-261-        },
app/admin/badges/BadgeDashboardClient.tsx-262-        body: JSON.stringify({
app/admin/badges/BadgeDashboardClient.tsx-263-          entitlementKey: form.entitlementKey,
app/admin/badges/BadgeDashboardClient.tsx:264:          memberIds: previewRows.map((row) => row.memberId),
app/admin/badges/BadgeDashboardClient.tsx:265:          grantReason: form.grantReason.trim() || undefined,
app/admin/badges/BadgeDashboardClient.tsx:266:          grantSource: "badge_admin_preview",
app/admin/badges/BadgeDashboardClient.tsx-267-        }),
app/admin/badges/BadgeDashboardClient.tsx-268-      });
app/admin/badges/BadgeDashboardClient.tsx-269-
app/admin/badges/BadgeDashboardClient.tsx:270:      const json = (await response.json()) as AwardResponse;
app/admin/badges/BadgeDashboardClient.tsx-271-
app/admin/badges/BadgeDashboardClient.tsx-272-      if (!response.ok || !json.ok || !json.result) {
app/admin/badges/BadgeDashboardClient.tsx-273-        throw new Error(json.error || "Unable to award badge.");
app/admin/badges/BadgeDashboardClient.tsx-274-      }
app/admin/badges/BadgeDashboardClient.tsx-275-
app/admin/badges/BadgeDashboardClient.tsx-276-      const { entitlementKey, attempted, inserted, alreadyHeld } = json.result;
--
app/admin/badges/BadgeDashboardClient.tsx-292-      const message =
app/admin/badges/BadgeDashboardClient.tsx-293-        error instanceof Error ? error.message : "Unable to award badge.";
app/admin/badges/BadgeDashboardClient.tsx-294-      setAwardError(message);
app/admin/badges/BadgeDashboardClient.tsx-295-    } finally {
app/admin/badges/BadgeDashboardClient.tsx-296-      setAwardLoading(false);
app/admin/badges/BadgeDashboardClient.tsx-297-    }
app/admin/badges/BadgeDashboardClient.tsx:298:  }, [form.entitlementKey, form.grantReason, previewRows]);
app/admin/badges/BadgeDashboardClient.tsx-299-
app/admin/badges/BadgeDashboardClient.tsx-300-  React.useEffect(() => {
app/admin/badges/BadgeDashboardClient.tsx-301-    if (!form.entitlementKey && sortedBadges[0]?.entitlementKey) {
app/admin/badges/BadgeDashboardClient.tsx-302-      setForm((current) => ({
app/admin/badges/BadgeDashboardClient.tsx-303-        ...current,
app/admin/badges/BadgeDashboardClient.tsx-304-        entitlementKey: sortedBadges[0].entitlementKey,
--
app/api/admin/badges/preview/route.ts:1:// web/app/api/admin/badges/preview/route.ts
app/api/admin/badges/preview/route.ts-2-import { NextResponse } from "next/server";
app/api/admin/badges/preview/route.ts-3-import { requireAdminMemberId } from "@/lib/adminAuth";
app/api/admin/badges/preview/route.ts-4-import {
app/api/admin/badges/preview/route.ts-5-  previewBadgeQualification,
app/api/admin/badges/preview/route.ts:6:  type BadgePreviewInput,
app/api/admin/badges/preview/route.ts-7-} from "@/lib/badgeAdmin";
app/api/admin/badges/preview/route.ts-8-import {
app/api/admin/badges/preview/route.ts-9-  BADGE_PREVIEW_MODE_DESCRIPTORS,
app/api/admin/badges/preview/route.ts:10:  type BadgeQualificationMode,
app/api/admin/badges/preview/route.ts-11-} from "@/lib/badgePreviewModes";
app/api/admin/badges/preview/route.ts-12-
app/api/admin/badges/preview/route.ts-13-function isRecord(value: unknown): value is Record<string, unknown> {
app/api/admin/badges/preview/route.ts-14-  return typeof value === "object" && value !== null;
app/api/admin/badges/preview/route.ts-15-}
app/api/admin/badges/preview/route.ts-16-
--
app/api/admin/badges/preview/route.ts-38-
app/api/admin/badges/preview/route.ts-39-function asOptionalLimit(value: unknown): number | undefined {
app/api/admin/badges/preview/route.ts-40-  const parsed = asPositiveNumber(value);
app/api/admin/badges/preview/route.ts-41-  return parsed === null ? undefined : Math.floor(parsed);
app/api/admin/badges/preview/route.ts-42-}
app/api/admin/badges/preview/route.ts-43-
app/api/admin/badges/preview/route.ts:44:function isBadgeQualificationMode(
app/api/admin/badges/preview/route.ts-45-  value: string,
app/api/admin/badges/preview/route.ts:46:): value is BadgeQualificationMode {
app/api/admin/badges/preview/route.ts-47-  return value in BADGE_PREVIEW_MODE_DESCRIPTORS;
app/api/admin/badges/preview/route.ts-48-}
app/api/admin/badges/preview/route.ts-49-
app/api/admin/badges/preview/route.ts:50:function parsePreviewInput(body: unknown): BadgePreviewInput {
app/api/admin/badges/preview/route.ts-51-  if (!isRecord(body)) {
app/api/admin/badges/preview/route.ts-52-    throw new Error("Invalid request body.");
app/api/admin/badges/preview/route.ts-53-  }
app/api/admin/badges/preview/route.ts-54-
app/api/admin/badges/preview/route.ts-55-  const mode = asString(body.mode);
app/api/admin/badges/preview/route.ts-56-  if (!mode) {
app/api/admin/badges/preview/route.ts-57-    throw new Error("Mode is required.");
app/api/admin/badges/preview/route.ts-58-  }
app/api/admin/badges/preview/route.ts-59-
app/api/admin/badges/preview/route.ts:60:  if (!isBadgeQualificationMode(mode)) {
app/api/admin/badges/preview/route.ts-61-    throw new Error("Unsupported badge preview mode.");
app/api/admin/badges/preview/route.ts-62-  }
app/api/admin/badges/preview/route.ts-63-
app/api/admin/badges/preview/route.ts-64-  const limit = asOptionalLimit(body.limit);
app/api/admin/badges/preview/route.ts-65-
app/api/admin/badges/preview/route.ts-66-  switch (mode) {
--
app/api/admin/badges/award/route.ts:1:// web/app/api/admin/badges/award/route.ts
app/api/admin/badges/award/route.ts-2-import { NextResponse } from "next/server";
app/api/admin/badges/award/route.ts-3-import { requireAdminMemberId } from "@/lib/adminAuth";
app/api/admin/badges/award/route.ts-4-import { awardBadgeToMembers } from "@/lib/badgeAdmin";
app/api/admin/badges/award/route.ts-5-import { areBadgesEnabled } from "@/lib/badgeFeature";
app/api/admin/badges/award/route.ts-6-
app/api/admin/badges/award/route.ts-7-function isRecord(value: unknown): value is Record<string, unknown> {
--
app/api/admin/badges/award/route.ts-52-    }
app/api/admin/badges/award/route.ts-53-
app/api/admin/badges/award/route.ts-54-    if (!entitlementKey.startsWith("badge_")) {
app/api/admin/badges/award/route.ts-55-      throw new Error("entitlementKey must start with badge_.");
app/api/admin/badges/award/route.ts-56-    }
app/api/admin/badges/award/route.ts-57-
app/api/admin/badges/award/route.ts:58:    const memberIds = asStringArray(body.memberIds);
app/api/admin/badges/award/route.ts:59:    if (memberIds.length === 0) {
app/api/admin/badges/award/route.ts-60-      throw new Error("At least one memberId is required.");
app/api/admin/badges/award/route.ts-61-    }
app/api/admin/badges/award/route.ts-62-
app/api/admin/badges/award/route.ts:63:    const grantReason = asString(body.grantReason);
app/api/admin/badges/award/route.ts:64:    const grantSource = asString(body.grantSource) ?? "badge_admin_preview";
app/api/admin/badges/award/route.ts:65:    const grantSourceRef = asString(body.grantSourceRef);
app/api/admin/badges/award/route.ts-66-
app/api/admin/badges/award/route.ts-67-    const result = await awardBadgeToMembers({
app/api/admin/badges/award/route.ts-68-      entitlementKey,
app/api/admin/badges/award/route.ts:69:      memberIds,
app/api/admin/badges/award/route.ts-70-      grantedBy: adminMemberId,
app/api/admin/badges/award/route.ts:71:      grantReason: grantReason ?? undefined,
app/api/admin/badges/award/route.ts:72:      grantSource,
app/api/admin/badges/award/route.ts:73:      grantSourceRef: grantSourceRef ?? undefined,
app/api/admin/badges/award/route.ts-74-    });
app/api/admin/badges/award/route.ts-75-
app/api/admin/badges/award/route.ts-76-    return NextResponse.json({
app/api/admin/badges/award/route.ts-77-      ok: true,
app/api/admin/badges/award/route.ts-78-      result,
app/api/admin/badges/award/route.ts-79-      summary: {
--
app/admin/badges/_components/BadgeQualificationFormSection.tsx-1-// web/app/admin/badges/_components/BadgeQualificationFormSection.tsx
app/admin/badges/_components/BadgeQualificationFormSection.tsx-2-"use client";
app/admin/badges/_components/BadgeQualificationFormSection.tsx-3-
app/admin/badges/_components/BadgeQualificationFormSection.tsx-4-import React from "react";
app/admin/badges/_components/BadgeQualificationFormSection.tsx-5-import {
app/admin/badges/_components/BadgeQualificationFormSection.tsx-6-  BADGE_PREVIEW_MODES,
app/admin/badges/_components/BadgeQualificationFormSection.tsx:7:  type BadgeQualificationMode,
app/admin/badges/_components/BadgeQualificationFormSection.tsx-8-} from "@/lib/badgePreviewModes";
app/admin/badges/_components/BadgeQualificationFormSection.tsx-9-import {
app/admin/badges/_components/BadgeQualificationFormSection.tsx-10-  BG_INSET,
app/admin/badges/_components/BadgeQualificationFormSection.tsx-11-  FONT_SIZE_UI,
app/admin/badges/_components/BadgeQualificationFormSection.tsx-12-  PANEL_BORDER,
app/admin/badges/_components/BadgeQualificationFormSection.tsx-13-  TEXT_MUTED,
--
app/admin/badges/_components/BadgeQualificationFormSection.tsx-198-        </Field>
app/admin/badges/_components/BadgeQualificationFormSection.tsx-199-
app/admin/badges/_components/BadgeQualificationFormSection.tsx-200-        <Field label="Qualification mode">
app/admin/badges/_components/BadgeQualificationFormSection.tsx-201-          <select
app/admin/badges/_components/BadgeQualificationFormSection.tsx-202-            value={form.mode}
app/admin/badges/_components/BadgeQualificationFormSection.tsx-203-            onChange={(event) =>
app/admin/badges/_components/BadgeQualificationFormSection.tsx:204:              onFormChange("mode", event.target.value as BadgeQualificationMode)
app/admin/badges/_components/BadgeQualificationFormSection.tsx-205-            }
app/admin/badges/_components/BadgeQualificationFormSection.tsx-206-            style={controlStyle}
app/admin/badges/_components/BadgeQualificationFormSection.tsx-207-          >
app/admin/badges/_components/BadgeQualificationFormSection.tsx-208-            {BADGE_PREVIEW_MODES.map((mode) => (
app/admin/badges/_components/BadgeQualificationFormSection.tsx-209-              <option key={mode.key} value={mode.key}>
app/admin/badges/_components/BadgeQualificationFormSection.tsx-210-                {mode.label}
--
app/admin/badges/_components/BadgeQualificationFormSection.tsx-434-          onClearSelectedRecording={onClearSelectedRecording}
app/admin/badges/_components/BadgeQualificationFormSection.tsx-435-        />
app/admin/badges/_components/BadgeQualificationFormSection.tsx-436-      ) : null}
app/admin/badges/_components/BadgeQualificationFormSection.tsx-437-
app/admin/badges/_components/BadgeQualificationFormSection.tsx-438-      <Field label="Grant reason">
app/admin/badges/_components/BadgeQualificationFormSection.tsx-439-        <input
app/admin/badges/_components/BadgeQualificationFormSection.tsx:440:          value={form.grantReason}
app/admin/badges/_components/BadgeQualificationFormSection.tsx:441:          onChange={(event) => onFormChange("grantReason", event.target.value)}
app/admin/badges/_components/BadgeQualificationFormSection.tsx-442-          placeholder="Optional note stored with the grant"
app/admin/badges/_components/BadgeQualificationFormSection.tsx-443-          style={controlStyle}
app/admin/badges/_components/BadgeQualificationFormSection.tsx-444-        />
app/admin/badges/_components/BadgeQualificationFormSection.tsx-445-      </Field>
app/admin/badges/_components/BadgeQualificationFormSection.tsx-446-      <Field label="Preview limit">
app/admin/badges/_components/BadgeQualificationFormSection.tsx-447-        <input
--
app/admin/badges/_lib/badgeDashboardTypes.ts-1-// web/app/admin/badges/_lib/badgeDashboardTypes.ts
app/admin/badges/_lib/badgeDashboardTypes.ts:2:import type { BadgeQualificationMode } from "@/lib/badgePreviewModes";
app/admin/badges/_lib/badgeDashboardTypes.ts-3-import type { BadgeAwardMode } from "@/lib/badges";
app/admin/badges/_lib/badgeDashboardTypes.ts-4-
app/admin/badges/_lib/badgeDashboardTypes.ts-5-export type BadgeDefinitionOption = {
app/admin/badges/_lib/badgeDashboardTypes.ts-6-  entitlementKey: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-7-  title: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-8-  description: string | null;
--
app/admin/badges/_lib/badgeDashboardTypes.ts-10-  imageUrl: string | null;
app/admin/badges/_lib/badgeDashboardTypes.ts-11-  featured: boolean;
app/admin/badges/_lib/badgeDashboardTypes.ts-12-  shareable: boolean;
app/admin/badges/_lib/badgeDashboardTypes.ts-13-  undisclosed: boolean;
app/admin/badges/_lib/badgeDashboardTypes.ts-14-  awardMode: BadgeAwardMode;
app/admin/badges/_lib/badgeDashboardTypes.ts-15-  autoAwardEnabled: boolean;
app/admin/badges/_lib/badgeDashboardTypes.ts:16:  autoQualificationMode: BadgeQualificationMode | null;
app/admin/badges/_lib/badgeDashboardTypes.ts-17-};
app/admin/badges/_lib/badgeDashboardTypes.ts-18-
app/admin/badges/_lib/badgeDashboardTypes.ts-19-export type PreviewRow = {
app/admin/badges/_lib/badgeDashboardTypes.ts-20-  memberId: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-21-  email: string | null;
app/admin/badges/_lib/badgeDashboardTypes.ts-22-  joinedAt: string | null;
--
app/admin/badges/_lib/badgeDashboardTypes.ts-29-  matchedWindowEventCount: number | null;
app/admin/badges/_lib/badgeDashboardTypes.ts-30-  contributionCount: number | null;
app/admin/badges/_lib/badgeDashboardTypes.ts-31-  exegesisVoteCount: number | null;
app/admin/badges/_lib/badgeDashboardTypes.ts-32-  publicNameUnlockedAt: string | null;
app/admin/badges/_lib/badgeDashboardTypes.ts-33-};
app/admin/badges/_lib/badgeDashboardTypes.ts-34-
app/admin/badges/_lib/badgeDashboardTypes.ts:35:export type PreviewResponse = {
app/admin/badges/_lib/badgeDashboardTypes.ts-36-  ok: boolean;
app/admin/badges/_lib/badgeDashboardTypes.ts-37-  count?: number;
app/admin/badges/_lib/badgeDashboardTypes.ts-38-  rows?: PreviewRow[];
app/admin/badges/_lib/badgeDashboardTypes.ts-39-  error?: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-40-};
app/admin/badges/_lib/badgeDashboardTypes.ts-41-
app/admin/badges/_lib/badgeDashboardTypes.ts:42:export type AwardResponse = {
app/admin/badges/_lib/badgeDashboardTypes.ts-43-  ok: boolean;
app/admin/badges/_lib/badgeDashboardTypes.ts-44-  result?: {
app/admin/badges/_lib/badgeDashboardTypes.ts-45-    entitlementKey: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-46-    attempted: number;
app/admin/badges/_lib/badgeDashboardTypes.ts-47-    inserted: number;
app/admin/badges/_lib/badgeDashboardTypes.ts-48-    alreadyHeld: number;
--
app/admin/badges/_lib/badgeDashboardTypes.ts-74-  results?: RecordingSearchResult[];
app/admin/badges/_lib/badgeDashboardTypes.ts-75-  error?: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-76-};
app/admin/badges/_lib/badgeDashboardTypes.ts-77-
app/admin/badges/_lib/badgeDashboardTypes.ts-78-export type FormState = {
app/admin/badges/_lib/badgeDashboardTypes.ts-79-  entitlementKey: string;
app/admin/badges/_lib/badgeDashboardTypes.ts:80:  mode: BadgeQualificationMode;
app/admin/badges/_lib/badgeDashboardTypes.ts-81-  minMinutes: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-82-  minPlayCount: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-83-  minCompletedCount: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-84-  minProgressCount: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-85-  minContributionCount: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-86-  minVoteCount: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-87-  joinedOnOrAfter: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-88-  joinedBefore: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-89-  activeOnOrAfter: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-90-  activeBefore: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-91-  recordingId: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-92-  limit: string;
app/admin/badges/_lib/badgeDashboardTypes.ts:93:  grantReason: string;
app/admin/badges/_lib/badgeDashboardTypes.ts-94-};
app/admin/badges/_lib/badgeDashboardTypes.ts-95-
app/admin/badges/_lib/badgeDashboardTypes.ts-96-export type SelectedModeInputRequirements = {
app/admin/badges/_lib/badgeDashboardTypes.ts-97-  minMinutes: boolean;
app/admin/badges/_lib/badgeDashboardTypes.ts-98-  minPlayCount: boolean;
app/admin/badges/_lib/badgeDashboardTypes.ts-99-  minCompletedCount: boolean;
--
app/admin/badges/_lib/badgeDashboardUtils.ts-13-  joinedOnOrAfter: "",
app/admin/badges/_lib/badgeDashboardUtils.ts-14-  joinedBefore: "",
app/admin/badges/_lib/badgeDashboardUtils.ts-15-  activeOnOrAfter: "",
app/admin/badges/_lib/badgeDashboardUtils.ts-16-  activeBefore: "",
app/admin/badges/_lib/badgeDashboardUtils.ts-17-  recordingId: "",
app/admin/badges/_lib/badgeDashboardUtils.ts-18-  limit: "200",
app/admin/badges/_lib/badgeDashboardUtils.ts:19:  grantReason: "",
app/admin/badges/_lib/badgeDashboardUtils.ts-20-};
app/admin/badges/_lib/badgeDashboardUtils.ts-21-
app/admin/badges/_lib/badgeDashboardUtils.ts-22-export function formatDateTime(value: string | null): string {
app/admin/badges/_lib/badgeDashboardUtils.ts-23-  if (!value) return "—";
app/admin/badges/_lib/badgeDashboardUtils.ts-24-
app/admin/badges/_lib/badgeDashboardUtils.ts-25-  const date = new Date(value);
--
app/admin/badges/_lib/badgeDashboardUtils.ts-39-): string {
app/admin/badges/_lib/badgeDashboardUtils.ts-40-  if (isSelected) return "1px solid rgba(255,255,255,0.32)";
app/admin/badges/_lib/badgeDashboardUtils.ts-41-  if (isFeatured) return "1px solid rgba(255,255,255,0.18)";
app/admin/badges/_lib/badgeDashboardUtils.ts-42-  return "1px solid rgba(255,255,255,0.1)";
app/admin/badges/_lib/badgeDashboardUtils.ts-43-}
app/admin/badges/_lib/badgeDashboardUtils.ts-44-
app/admin/badges/_lib/badgeDashboardUtils.ts:45:export function buildPreviewPayload(
app/admin/badges/_lib/badgeDashboardUtils.ts-46-  form: FormState,
app/admin/badges/_lib/badgeDashboardUtils.ts-47-): Record<string, string | number> {
app/admin/badges/_lib/badgeDashboardUtils.ts-48-  const limit = form.limit.trim() ? Number(form.limit) : 200;
app/admin/badges/_lib/badgeDashboardUtils.ts-49-
app/admin/badges/_lib/badgeDashboardUtils.ts-50-  switch (form.mode) {
app/admin/badges/_lib/badgeDashboardUtils.ts-51-    case "minutes_streamed":

===== RUNTIME BADGE ANIMATION / ACCESSIBILITY CONTRACT REFERENCES =====

app/home/badges/BadgeAwardOverlay.tsx-78-  if (!active || !badge) return null;
app/home/badges/BadgeAwardOverlay.tsx-79-
app/home/badges/BadgeAwardOverlay.tsx-80-  return (
app/home/badges/BadgeAwardOverlay.tsx-81-    <BodyPortal>
app/home/badges/BadgeAwardOverlay.tsx-82-      <div
app/home/badges/BadgeAwardOverlay.tsx:83:        aria-hidden="true"
app/home/badges/BadgeAwardOverlay.tsx:84:        role="button"
app/home/badges/BadgeAwardOverlay.tsx:85:        tabIndex={0}
app/home/badges/BadgeAwardOverlay.tsx-86-        onClick={onDismiss}
app/home/badges/BadgeAwardOverlay.tsx-87-        onKeyDown={(e) => {
app/home/badges/BadgeAwardOverlay.tsx-88-          if (e.key === "Enter" || e.key === " ") {
app/home/badges/BadgeAwardOverlay.tsx-89-            e.preventDefault();
app/home/badges/BadgeAwardOverlay.tsx-90-            onDismiss();
--
app/home/badges/BadgeAwardOverlay.tsx-103-            "opacity 220ms ease, background-color 220ms ease, backdrop-filter 220ms ease",
app/home/badges/BadgeAwardOverlay.tsx-104-        }}
app/home/badges/BadgeAwardOverlay.tsx-105-      />
app/home/badges/BadgeAwardOverlay.tsx-106-
app/home/badges/BadgeAwardOverlay.tsx-107-      <div
app/home/badges/BadgeAwardOverlay.tsx:108:        role="button"
app/home/badges/BadgeAwardOverlay.tsx:109:        tabIndex={0}
app/home/badges/BadgeAwardOverlay.tsx:110:        aria-label="Dismiss badge award overlay"
app/home/badges/BadgeAwardOverlay.tsx-111-        onClick={(e) => {
app/home/badges/BadgeAwardOverlay.tsx-112-          if (e.target === e.currentTarget) onDismiss();
app/home/badges/BadgeAwardOverlay.tsx-113-        }}
app/home/badges/BadgeAwardOverlay.tsx-114-        onKeyDown={(e) => {
app/home/badges/BadgeAwardOverlay.tsx-115-          if (e.target !== e.currentTarget) return;
--
app/home/badges/BadgeAwardOverlay.tsx-130-          transform: visible ? "scale(1)" : "scale(0.985)",
app/home/badges/BadgeAwardOverlay.tsx-131-          transition: "opacity 220ms ease, transform 220ms ease",
app/home/badges/BadgeAwardOverlay.tsx-132-        }}
app/home/badges/BadgeAwardOverlay.tsx-133-      >
app/home/badges/BadgeAwardOverlay.tsx-134-        <div
app/home/badges/BadgeAwardOverlay.tsx:135:          role="status"
app/home/badges/BadgeAwardOverlay.tsx:136:          aria-live="polite"
app/home/badges/BadgeAwardOverlay.tsx:137:          aria-atomic="true"
app/home/badges/BadgeAwardOverlay.tsx-138-          style={{
app/home/badges/BadgeAwardOverlay.tsx-139-            width: "100%",
app/home/badges/BadgeAwardOverlay.tsx-140-            display: "grid",
app/home/badges/BadgeAwardOverlay.tsx-141-            placeItems: "center",
app/home/badges/BadgeAwardOverlay.tsx-142-          }}
--
app/home/badges/BadgeAwardRevealCard.tsx-83-            placeItems: "center",
app/home/badges/BadgeAwardRevealCard.tsx-84-            overflow: "visible",
app/home/badges/BadgeAwardRevealCard.tsx-85-          }}
app/home/badges/BadgeAwardRevealCard.tsx-86-        >
app/home/badges/BadgeAwardRevealCard.tsx-87-          <div
app/home/badges/BadgeAwardRevealCard.tsx:88:            aria-hidden="true"
app/home/badges/BadgeAwardRevealCard.tsx-89-            style={{
app/home/badges/BadgeAwardRevealCard.tsx-90-              position: "absolute",
app/home/badges/BadgeAwardRevealCard.tsx-91-              inset: -18,
app/home/badges/BadgeAwardRevealCard.tsx-92-              borderRadius: "50%",
app/home/badges/BadgeAwardRevealCard.tsx-93-              background:
--
app/home/modules/badges/BadgeCabinet.tsx-168-}
app/home/modules/badges/BadgeCabinet.tsx-169-
app/home/modules/badges/BadgeCabinet.tsx-170-function BadgeCabinetToggle(
app/home/modules/badges/BadgeCabinet.tsx-171-  props: Readonly<{
app/home/modules/badges/BadgeCabinet.tsx-172-    expanded: boolean;
app/home/modules/badges/BadgeCabinet.tsx:173:    prefersReducedMotion: boolean;
app/home/modules/badges/BadgeCabinet.tsx-174-    onToggle: () => void;
app/home/modules/badges/BadgeCabinet.tsx-175-  }>,
app/home/modules/badges/BadgeCabinet.tsx-176-) {
app/home/modules/badges/BadgeCabinet.tsx:177:  const { expanded, prefersReducedMotion, onToggle } = props;
app/home/modules/badges/BadgeCabinet.tsx-178-
app/home/modules/badges/BadgeCabinet.tsx-179-  return (
app/home/modules/badges/BadgeCabinet.tsx-180-    <button
app/home/modules/badges/BadgeCabinet.tsx-181-      type="button"
app/home/modules/badges/BadgeCabinet.tsx-182-      onClick={onToggle}
app/home/modules/badges/BadgeCabinet.tsx:183:      aria-expanded={expanded}
app/home/modules/badges/BadgeCabinet.tsx-184-      style={{
app/home/modules/badges/BadgeCabinet.tsx-185-        appearance: "none",
app/home/modules/badges/BadgeCabinet.tsx-186-        border: "none",
app/home/modules/badges/BadgeCabinet.tsx-187-        background: "transparent",
app/home/modules/badges/BadgeCabinet.tsx-188-        padding: 0,
--
app/home/modules/badges/BadgeCabinet.tsx-208-      >
app/home/modules/badges/BadgeCabinet.tsx-209-        Badges
app/home/modules/badges/BadgeCabinet.tsx-210-      </span>
app/home/modules/badges/BadgeCabinet.tsx-211-
app/home/modules/badges/BadgeCabinet.tsx-212-      <span
app/home/modules/badges/BadgeCabinet.tsx:213:        aria-hidden="true"
app/home/modules/badges/BadgeCabinet.tsx-214-        style={{
app/home/modules/badges/BadgeCabinet.tsx-215-          display: "inline-block",
app/home/modules/badges/BadgeCabinet.tsx-216-          fontSize: 12,
app/home/modules/badges/BadgeCabinet.tsx-217-          lineHeight: 1,
app/home/modules/badges/BadgeCabinet.tsx-218-          opacity: 0.5,
app/home/modules/badges/BadgeCabinet.tsx-219-          transform: expanded ? "rotate(90deg)" : "rotate(0deg)",
app/home/modules/badges/BadgeCabinet.tsx-220-          transformOrigin: "50% 50%",
app/home/modules/badges/BadgeCabinet.tsx:221:          transition: prefersReducedMotion
app/home/modules/badges/BadgeCabinet.tsx-222-            ? undefined
app/home/modules/badges/BadgeCabinet.tsx-223-            : "transform 220ms ease, opacity 180ms ease",
app/home/modules/badges/BadgeCabinet.tsx-224-        }}
app/home/modules/badges/BadgeCabinet.tsx-225-      >
app/home/modules/badges/BadgeCabinet.tsx-226-        &gt;
--
app/home/modules/badges/BadgeCabinet.tsx-400-
app/home/modules/badges/BadgeCabinet.tsx-401-export default function BadgeCabinet(props: Props) {
app/home/modules/badges/BadgeCabinet.tsx-402-  const { badges } = props;
app/home/modules/badges/BadgeCabinet.tsx-403-
app/home/modules/badges/BadgeCabinet.tsx-404-  const [expanded, setExpanded] = React.useState(false);
app/home/modules/badges/BadgeCabinet.tsx:405:  const prefersReducedMotion = usePrefersReducedMotion();
app/home/modules/badges/BadgeCabinet.tsx-406-  const isAdminDebug = useAdminDebugFlag();
app/home/modules/badges/BadgeCabinet.tsx-407-  const { announceBadge, resetOverlayDebugState } = useBadgeAwardOverlay();
app/home/modules/badges/BadgeCabinet.tsx-408-
app/home/modules/badges/BadgeCabinet.tsx-409-  const sourceItems = React.useMemo(
app/home/modules/badges/BadgeCabinet.tsx-410-    () => buildBadgeCabinetItems(badges),
--
app/home/modules/badges/BadgeCabinet.tsx-434-
app/home/modules/badges/BadgeCabinet.tsx-435-  const {
app/home/modules/badges/BadgeCabinet.tsx-436-    displayItems,
app/home/modules/badges/BadgeCabinet.tsx-437-    newlyUnlockedKeys,
app/home/modules/badges/BadgeCabinet.tsx-438-    pendingUnlockKeys,
app/home/modules/badges/BadgeCabinet.tsx:439:    unlockPhase,
app/home/modules/badges/BadgeCabinet.tsx-440-    liveAnnouncement,
app/home/modules/badges/BadgeCabinet.tsx:441:    isFlipSuspended,
app/home/modules/badges/BadgeCabinet.tsx:442:    flipDurationMs,
app/home/modules/badges/BadgeCabinet.tsx-443-    flipBaselineToken,
app/home/modules/badges/BadgeCabinet.tsx-444-    resetUnlockSequence,
app/home/modules/badges/BadgeCabinet.tsx-445-  } = useBadgeCabinetUnlockSequence({ items });
app/home/modules/badges/BadgeCabinet.tsx-446-
app/home/modules/badges/BadgeCabinet.tsx-447-  const itemKeys = React.useMemo(
--
app/home/modules/badges/BadgeCabinet.tsx-455-  const flipLayoutDependency = React.useMemo(
app/home/modules/badges/BadgeCabinet.tsx-456-    () =>
app/home/modules/badges/BadgeCabinet.tsx-457-      [
app/home/modules/badges/BadgeCabinet.tsx-458-        expanded ? "expanded" : "collapsed",
app/home/modules/badges/BadgeCabinet.tsx-459-        displayLayoutToken,
app/home/modules/badges/BadgeCabinet.tsx:460:        unlockPhase,
app/home/modules/badges/BadgeCabinet.tsx-461-      ].join(":"),
app/home/modules/badges/BadgeCabinet.tsx:462:    [displayLayoutToken, expanded, unlockPhase],
app/home/modules/badges/BadgeCabinet.tsx-463-  );
app/home/modules/badges/BadgeCabinet.tsx-464-
app/home/modules/badges/BadgeCabinet.tsx-465-  const { registerItemRef } = useFlipGridAnimation({
app/home/modules/badges/BadgeCabinet.tsx-466-    keys: itemKeys,
app/home/modules/badges/BadgeCabinet.tsx:467:    disabled: prefersReducedMotion || isFlipSuspended,
app/home/modules/badges/BadgeCabinet.tsx:468:    durationMs: flipDurationMs,
app/home/modules/badges/BadgeCabinet.tsx-469-    layoutDependency: flipLayoutDependency,
app/home/modules/badges/BadgeCabinet.tsx:470:    captureBaselineToken: flipBaselineToken,
app/home/modules/badges/BadgeCabinet.tsx-471-  });
app/home/modules/badges/BadgeCabinet.tsx-472-
app/home/modules/badges/BadgeCabinet.tsx-473-  const announceDebugOverlay = React.useCallback(
app/home/modules/badges/BadgeCabinet.tsx-474-    (badgeKey: string | null) => {
app/home/modules/badges/BadgeCabinet.tsx-475-      if (!badgeKey) return;
--
app/home/modules/badges/BadgeCabinet.tsx-517-          gap: 10,
app/home/modules/badges/BadgeCabinet.tsx-518-          minWidth: 0,
app/home/modules/badges/BadgeCabinet.tsx-519-        }}
app/home/modules/badges/BadgeCabinet.tsx-520-      >
app/home/modules/badges/BadgeCabinet.tsx-521-        <div
app/home/modules/badges/BadgeCabinet.tsx:522:          aria-live="polite"
app/home/modules/badges/BadgeCabinet.tsx:523:          aria-atomic="true"
app/home/modules/badges/BadgeCabinet.tsx-524-          className="portal-member-badge-live-region"
app/home/modules/badges/BadgeCabinet.tsx-525-        >
app/home/modules/badges/BadgeCabinet.tsx-526-          {liveAnnouncement}
app/home/modules/badges/BadgeCabinet.tsx-527-        </div>
app/home/modules/badges/BadgeCabinet.tsx-528-
--
app/home/modules/badges/BadgeCabinet.tsx-533-            minWidth: 0,
app/home/modules/badges/BadgeCabinet.tsx-534-          }}
app/home/modules/badges/BadgeCabinet.tsx-535-        >
app/home/modules/badges/BadgeCabinet.tsx-536-          <BadgeCabinetToggle
app/home/modules/badges/BadgeCabinet.tsx-537-            expanded={expanded}
app/home/modules/badges/BadgeCabinet.tsx:538:            prefersReducedMotion={prefersReducedMotion}
app/home/modules/badges/BadgeCabinet.tsx-539-            onToggle={() => setExpanded((current) => !current)}
app/home/modules/badges/BadgeCabinet.tsx-540-          />
app/home/modules/badges/BadgeCabinet.tsx-541-
app/home/modules/badges/BadgeCabinet.tsx-542-          {showDebugPanel ? (
app/home/modules/badges/BadgeCabinet.tsx-543-            <BadgeCabinetDebugPanel
--
app/home/modules/badges/BadgeCabinetItem.tsx-33-  const { item, isNewlyUnlocked, isUnlocking } = props;
app/home/modules/badges/BadgeCabinetItem.tsx-34-  const accessibleTitle = item.unlocked ? item.titleText : "Locked badge";
app/home/modules/badges/BadgeCabinetItem.tsx-35-
app/home/modules/badges/BadgeCabinetItem.tsx-36-  return (
app/home/modules/badges/BadgeCabinetItem.tsx-37-    <div
app/home/modules/badges/BadgeCabinetItem.tsx:38:      role="img"
app/home/modules/badges/BadgeCabinetItem.tsx:39:      aria-label={accessibleTitle}
app/home/modules/badges/BadgeCabinetItem.tsx-40-      className="portal-member-badge-visual"
app/home/modules/badges/BadgeCabinetItem.tsx-41-      style={{
app/home/modules/badges/BadgeCabinetItem.tsx-42-        position: "relative",
app/home/modules/badges/BadgeCabinetItem.tsx-43-        width: "100%",
app/home/modules/badges/BadgeCabinetItem.tsx-44-        aspectRatio: "1 / 1",
--
app/home/modules/badges/BadgeCabinetItem.tsx-72-      className={revealingClassName(
app/home/modules/badges/BadgeCabinetItem.tsx-73-        "portal-member-badge-question-mark",
app/home/modules/badges/BadgeCabinetItem.tsx-74-        "portal-member-badge-question-mark--dissolving",
app/home/modules/badges/BadgeCabinetItem.tsx-75-        isMetaRevealing,
app/home/modules/badges/BadgeCabinetItem.tsx-76-      )}
app/home/modules/badges/BadgeCabinetItem.tsx:77:      aria-hidden="true"
app/home/modules/badges/BadgeCabinetItem.tsx-78-      style={{
app/home/modules/badges/BadgeCabinetItem.tsx-79-        fontSize: 10,
app/home/modules/badges/BadgeCabinetItem.tsx-80-        lineHeight: 1.1,
app/home/modules/badges/BadgeCabinetItem.tsx-81-        letterSpacing: 0.12,
app/home/modules/badges/BadgeCabinetItem.tsx-82-        opacity: 0.3,
--
app/home/modules/badges/BadgeCabinetItem.tsx-154-) {
app/home/modules/badges/BadgeCabinetItem.tsx-155-  const { item, isMetaRevealing } = props;
app/home/modules/badges/BadgeCabinetItem.tsx-156-  const showQuestionMark = !item.unlocked || isMetaRevealing;
app/home/modules/badges/BadgeCabinetItem.tsx-157-
app/home/modules/badges/BadgeCabinetItem.tsx-158-  return (
app/home/modules/badges/BadgeCabinetItem.tsx:159:    <div className="portal-member-badge-meta" aria-hidden="false">
app/home/modules/badges/BadgeCabinetItem.tsx-160-      <div className="portal-member-badge-meta-inner">
app/home/modules/badges/BadgeCabinetItem.tsx-161-        {showQuestionMark ? (
app/home/modules/badges/BadgeCabinetItem.tsx-162-          <BadgeMetaQuestionMark isMetaRevealing={isMetaRevealing} />
app/home/modules/badges/BadgeCabinetItem.tsx-163-        ) : null}
app/home/modules/badges/BadgeCabinetItem.tsx-164-
--
app/home/modules/badges/BadgeUnlockVisual.tsx-21-) {
app/home/modules/badges/BadgeUnlockVisual.tsx-22-  const { unlocked, label } = props;
app/home/modules/badges/BadgeUnlockVisual.tsx-23-
app/home/modules/badges/BadgeUnlockVisual.tsx-24-  return (
app/home/modules/badges/BadgeUnlockVisual.tsx-25-    <div
app/home/modules/badges/BadgeUnlockVisual.tsx:26:      aria-label={label}
app/home/modules/badges/BadgeUnlockVisual.tsx-27-      style={{
app/home/modules/badges/BadgeUnlockVisual.tsx-28-        position: "absolute",
app/home/modules/badges/BadgeUnlockVisual.tsx-29-        inset: 0,
app/home/modules/badges/BadgeUnlockVisual.tsx-30-        display: "grid",
app/home/modules/badges/BadgeUnlockVisual.tsx-31-        placeItems: "center",
--
app/home/modules/badges/BadgeUnlockVisual.tsx-480-    <div
app/home/modules/badges/BadgeUnlockVisual.tsx-481-      className={`portal-badge-unlock-visual-inner portal-badge-unlock-visual-inner--${variant}`}
app/home/modules/badges/BadgeUnlockVisual.tsx-482-      data-badge-variant={variant}
app/home/modules/badges/BadgeUnlockVisual.tsx-483-      data-badge-unlocking={isUnlocking ? "true" : "false"}
app/home/modules/badges/BadgeUnlockVisual.tsx-484-      data-badge-newly-unlocked={isNewlyUnlocked ? "true" : "false"}
app/home/modules/badges/BadgeUnlockVisual.tsx:485:      aria-hidden="true"
app/home/modules/badges/BadgeUnlockVisual.tsx-486-    >
app/home/modules/badges/BadgeUnlockVisual.tsx-487-      <div
app/home/modules/badges/BadgeUnlockVisual.tsx-488-        className={unlocked ? undefined : "portal-badge-core--locked"}
app/home/modules/badges/BadgeUnlockVisual.tsx-489-        style={{
app/home/modules/badges/BadgeUnlockVisual.tsx-490-          position: "absolute",
--
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-3-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-4-import React from "react";
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-5-import { flushSync } from "react-dom";
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-6-import type { BadgeCabinetItemModel } from "./badgeCabinetTypes";
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-7-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:8:const BADGE_UNLOCK_TIMELINE_MS = {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-9-  /**
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-10-   * Visual ceremony timing.
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-11-   *
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-12-   * Keep these aligned with BadgeUnlockVisualStyles.tsx.
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-13-   *
--
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-36-  defaultFlip: 420,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-37-} as const;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-38-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-39-function getUnlockVisualDurationMs(): number {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-40-  return Math.max(
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:41:    BADGE_UNLOCK_TIMELINE_MS.shellSettleTotal,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:42:    BADGE_UNLOCK_TIMELINE_MS.spinStage1,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:43:    BADGE_UNLOCK_TIMELINE_MS.spinStage2Delay +
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:44:      BADGE_UNLOCK_TIMELINE_MS.spinStage2,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:45:    BADGE_UNLOCK_TIMELINE_MS.spinStage3Delay +
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:46:      BADGE_UNLOCK_TIMELINE_MS.spinStage3,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-47-  );
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-48-}
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-49-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:50:const UNLOCK_VISUAL_DURATION_MS = getUnlockVisualDurationMs();
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-51-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-52-function sortBadgeCabinetItems(
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-53-  items: BadgeCabinetItemModel[],
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-54-): BadgeCabinetItemModel[] {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-55-  const nextItems = [...items];
--
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-105-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-106-type Result = {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-107-  displayItems: UnlockableBadgeItem[];
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-108-  newlyUnlockedKeys: Set<string>;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-109-  pendingUnlockKeys: Set<string>;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:110:  unlockPhase: "idle" | "reveal" | "move";
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-111-  liveAnnouncement: string;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:112:  isFlipSuspended: boolean;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:113:  flipDurationMs: number;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-114-  flipBaselineToken: number;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-115-  resetUnlockSequence: () => void;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-116-};
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-117-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-118-export function useBadgeCabinetUnlockSequence(options: Options): Result {
--
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-130-    () => new Set(),
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-131-  );
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-132-  const [pendingUnlockKeys, setPendingUnlockKeys] = React.useState<Set<string>>(
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-133-    () => new Set(),
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-134-  );
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:135:  const [unlockPhase, setUnlockPhase] = React.useState<
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-136-    "idle" | "reveal" | "move"
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-137-  >("idle");
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-138-  const [displayItemsOverride, setDisplayItemsOverride] = React.useState<
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-139-    UnlockableBadgeItem[] | null
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-140-  >(null);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:141:  const [isFlipSuspended, setIsFlipSuspended] = React.useState(false);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:142:  const [flipDurationMs, setFlipDurationMs] = React.useState<number>(
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:143:    BADGE_UNLOCK_TIMELINE_MS.defaultFlip,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-144-  );
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-145-  const [flipBaselineToken, setFlipBaselineToken] = React.useState(0);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-146-  const [liveAnnouncement, setLiveAnnouncement] = React.useState("");
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-147-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-148-  const clearSequenceTimers = React.useCallback(() => {
--
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-172-    setNewlyUnlockedKeys(new Set());
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-173-    setPendingUnlockKeys(new Set());
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-174-    setUnlockPhase("idle");
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-175-    setDisplayItemsOverride(null);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-176-    setIsFlipSuspended(false);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:177:    setFlipDurationMs(BADGE_UNLOCK_TIMELINE_MS.defaultFlip);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-178-    setLiveAnnouncement("");
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-179-  }, [clearSequenceTimers]);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-180-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-181-  React.useEffect(() => {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-182-    return () => {
--
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-184-    };
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-185-  }, [clearSequenceTimers]);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-186-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-187-  React.useEffect(() => {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-188-    if (displayItemsOverride !== null) return;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:189:    if (unlockPhase !== "idle") return;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-190-    previousStableItemsRef.current = items;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:191:  }, [displayItemsOverride, items, unlockPhase]);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-192-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-193-  React.useLayoutEffect(() => {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-194-    const nextUnlockedKeys = new Set(
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-195-      items.filter((item) => item.unlocked).map((item) => item.key),
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-196-    );
--
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-222-    setIsFlipSuspended(true);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-223-    setDisplayItemsOverride(stagedItems);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-224-    setNewlyUnlockedKeys(freshUnlockKeySet);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-225-    setPendingUnlockKeys(freshUnlockKeySet);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-226-    setUnlockPhase("reveal");
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:227:    setFlipDurationMs(BADGE_UNLOCK_TIMELINE_MS.cabinetFlip);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-228-    setLiveAnnouncement(
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-229-      freshUnlocks
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-230-        .map((item) => `New badge unlocked: ${item.label}`)
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-231-        .join(". "),
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-232-    );
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-233-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-234-    if (process.env.NODE_ENV !== "production") {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-235-      console.debug("[useBadgeCabinetUnlockSequence] unlock started", {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-236-        freshUnlockKeys: Array.from(freshUnlockKeySet),
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:237:        unlockVisualDurationMs: UNLOCK_VISUAL_DURATION_MS,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:238:        cabinetFlipMs: BADGE_UNLOCK_TIMELINE_MS.cabinetFlip,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:239:        cabinetSettleMs: BADGE_UNLOCK_TIMELINE_MS.cabinetSettle,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:240:        timeline: BADGE_UNLOCK_TIMELINE_MS,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-241-      });
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-242-    }
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-243-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-244-    unlockReleaseTimeoutRef.current = window.setTimeout(() => {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-245-      unlockReleaseTimeoutRef.current = null;
--
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-263-        unlockReleasePaintRafRef.current = window.requestAnimationFrame(() => {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-264-          unlockReleasePaintRafRef.current = null;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-265-          setDisplayItemsOverride(null);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-266-        });
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-267-      });
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:268:    }, UNLOCK_VISUAL_DURATION_MS);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-269-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-270-    unlockCleanupTimeoutRef.current = window.setTimeout(
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-271-      () => {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-272-        setNewlyUnlockedKeys(new Set());
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-273-        setUnlockPhase("idle");
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-274-        setDisplayItemsOverride(null);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-275-        setIsFlipSuspended(false);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:276:        setFlipDurationMs(BADGE_UNLOCK_TIMELINE_MS.defaultFlip);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-277-        setLiveAnnouncement("");
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-278-        unlockCleanupTimeoutRef.current = null;
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-279-      },
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:280:      UNLOCK_VISUAL_DURATION_MS +
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:281:        BADGE_UNLOCK_TIMELINE_MS.cabinetFlip +
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:282:        BADGE_UNLOCK_TIMELINE_MS.cabinetSettle,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-283-    );
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-284-  }, [clearSequenceTimers, items]);
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-285-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-286-  const displayItems = React.useMemo(() => {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-287-    return displayItemsOverride ?? items;
--
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-289-
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-290-  return {
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-291-    displayItems,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-292-    newlyUnlockedKeys,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-293-    pendingUnlockKeys,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:294:    unlockPhase,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-295-    liveAnnouncement,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:296:    isFlipSuspended,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts:297:    flipDurationMs,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-298-    flipBaselineToken,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-299-    resetUnlockSequence,
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-300-  };
app/home/modules/badges/useBadgeCabinetUnlockSequence.ts-301-}
--
app/home/modules/badges/usePrefersReducedMotion.ts-2-"use client";
app/home/modules/badges/usePrefersReducedMotion.ts-3-
app/home/modules/badges/usePrefersReducedMotion.ts-4-import React from "react";
app/home/modules/badges/usePrefersReducedMotion.ts-5-
app/home/modules/badges/usePrefersReducedMotion.ts-6-export function usePrefersReducedMotion(): boolean {
app/home/modules/badges/usePrefersReducedMotion.ts:7:  const [prefersReducedMotion, setPrefersReducedMotion] = React.useState(false);
app/home/modules/badges/usePrefersReducedMotion.ts-8-
app/home/modules/badges/usePrefersReducedMotion.ts-9-  React.useEffect(() => {
app/home/modules/badges/usePrefersReducedMotion.ts-10-    if (typeof window === "undefined" || typeof window.matchMedia !== "function") {
app/home/modules/badges/usePrefersReducedMotion.ts-11-      return;
app/home/modules/badges/usePrefersReducedMotion.ts-12-    }
--
app/home/modules/badges/usePrefersReducedMotion.ts-26-
app/home/modules/badges/usePrefersReducedMotion.ts-27-    mediaQuery.addListener(update);
app/home/modules/badges/usePrefersReducedMotion.ts-28-    return () => mediaQuery.removeListener(update);
app/home/modules/badges/usePrefersReducedMotion.ts-29-  }, []);
app/home/modules/badges/usePrefersReducedMotion.ts-30-
app/home/modules/badges/usePrefersReducedMotion.ts:31:  return prefersReducedMotion;
app/home/modules/badges/usePrefersReducedMotion.ts-32-}
--
app/home/modules/badges/useFlipGridAnimation.ts-9-  keys: string[];
app/home/modules/badges/useFlipGridAnimation.ts-10-  disabled?: boolean;
app/home/modules/badges/useFlipGridAnimation.ts-11-  durationMs?: number;
app/home/modules/badges/useFlipGridAnimation.ts-12-  easing?: string;
app/home/modules/badges/useFlipGridAnimation.ts-13-  layoutDependency?: string | number | boolean | null;
app/home/modules/badges/useFlipGridAnimation.ts:14:  captureBaselineToken?: string | number | boolean | null;
app/home/modules/badges/useFlipGridAnimation.ts-15-};
app/home/modules/badges/useFlipGridAnimation.ts-16-
app/home/modules/badges/useFlipGridAnimation.ts-17-type LayoutRect = {
app/home/modules/badges/useFlipGridAnimation.ts-18-  left: number;
app/home/modules/badges/useFlipGridAnimation.ts-19-  top: number;
--
app/home/modules/badges/useFlipGridAnimation.ts-92-    keys,
app/home/modules/badges/useFlipGridAnimation.ts-93-    disabled = false,
app/home/modules/badges/useFlipGridAnimation.ts-94-    durationMs = 320,
app/home/modules/badges/useFlipGridAnimation.ts-95-    easing = "cubic-bezier(0.22, 1, 0.36, 1)",
app/home/modules/badges/useFlipGridAnimation.ts-96-    layoutDependency = null,
app/home/modules/badges/useFlipGridAnimation.ts:97:    captureBaselineToken = null,
app/home/modules/badges/useFlipGridAnimation.ts-98-  } = options;
app/home/modules/badges/useFlipGridAnimation.ts-99-
app/home/modules/badges/useFlipGridAnimation.ts-100-  const nodeByKeyRef = React.useRef<Map<string, HTMLDivElement>>(new Map());
app/home/modules/badges/useFlipGridAnimation.ts-101-  const previousRectsRef = React.useRef<RectMap>(new Map());
app/home/modules/badges/useFlipGridAnimation.ts-102-  const cleanupByKeyRef = React.useRef<Map<string, CleanupFn>>(new Map());
--
app/home/modules/badges/useFlipGridAnimation.ts-107-  const baselineRafBRef = React.useRef<number | null>(null);
app/home/modules/badges/useFlipGridAnimation.ts-108-
app/home/modules/badges/useFlipGridAnimation.ts-109-  const hasStableBaselineRef = React.useRef(false);
app/home/modules/badges/useFlipGridAnimation.ts-110-  const lastCaptureBaselineTokenRef = React.useRef<
app/home/modules/badges/useFlipGridAnimation.ts-111-    string | number | boolean | null
app/home/modules/badges/useFlipGridAnimation.ts:112:  >(captureBaselineToken);
app/home/modules/badges/useFlipGridAnimation.ts-113-
app/home/modules/badges/useFlipGridAnimation.ts-114-  const cancelScheduledAnimationFrames = React.useCallback((): void => {
app/home/modules/badges/useFlipGridAnimation.ts-115-    if (playRafIdRef.current !== null) {
app/home/modules/badges/useFlipGridAnimation.ts-116-      window.cancelAnimationFrame(playRafIdRef.current);
app/home/modules/badges/useFlipGridAnimation.ts-117-      playRafIdRef.current = null;
--
app/home/modules/badges/useFlipGridAnimation.ts-214-    cancelScheduledAnimationFrames();
app/home/modules/badges/useFlipGridAnimation.ts-215-    clearActiveAnimations();
app/home/modules/badges/useFlipGridAnimation.ts-216-    resetTrackedNodeStyles(keys);
app/home/modules/badges/useFlipGridAnimation.ts-217-
app/home/modules/badges/useFlipGridAnimation.ts-218-    const captureBaselineChanged =
app/home/modules/badges/useFlipGridAnimation.ts:219:      captureBaselineToken !== lastCaptureBaselineTokenRef.current;
app/home/modules/badges/useFlipGridAnimation.ts-220-
app/home/modules/badges/useFlipGridAnimation.ts:221:    lastCaptureBaselineTokenRef.current = captureBaselineToken;
app/home/modules/badges/useFlipGridAnimation.ts-222-
app/home/modules/badges/useFlipGridAnimation.ts-223-    if (!hasStableBaselineRef.current) {
app/home/modules/badges/useFlipGridAnimation.ts-224-      scheduleBaselineCapture(keys);
app/home/modules/badges/useFlipGridAnimation.ts-225-      return;
app/home/modules/badges/useFlipGridAnimation.ts-226-    }
--
app/home/modules/badges/useFlipGridAnimation.ts-279-      node.style.willChange = "transform";
app/home/modules/badges/useFlipGridAnimation.ts-280-
app/home/modules/badges/useFlipGridAnimation.ts-281-      let timeoutId: number | null = null;
app/home/modules/badges/useFlipGridAnimation.ts-282-
app/home/modules/badges/useFlipGridAnimation.ts-283-      const cleanup = () => {
app/home/modules/badges/useFlipGridAnimation.ts:284:        node.removeEventListener("transitionend", handleTransitionEnd);
app/home/modules/badges/useFlipGridAnimation.ts-285-
app/home/modules/badges/useFlipGridAnimation.ts-286-        if (timeoutId !== null) {
app/home/modules/badges/useFlipGridAnimation.ts-287-          window.clearTimeout(timeoutId);
app/home/modules/badges/useFlipGridAnimation.ts-288-          timeoutId = null;
app/home/modules/badges/useFlipGridAnimation.ts-289-        }
--
app/home/modules/badges/useFlipGridAnimation.ts-296-        if (event.target !== node) return;
app/home/modules/badges/useFlipGridAnimation.ts-297-        if (event.propertyName !== "transform") return;
app/home/modules/badges/useFlipGridAnimation.ts-298-        cleanup();
app/home/modules/badges/useFlipGridAnimation.ts-299-      };
app/home/modules/badges/useFlipGridAnimation.ts-300-
app/home/modules/badges/useFlipGridAnimation.ts:301:      node.addEventListener("transitionend", handleTransitionEnd);
app/home/modules/badges/useFlipGridAnimation.ts-302-
app/home/modules/badges/useFlipGridAnimation.ts-303-      timeoutId = window.setTimeout(() => {
app/home/modules/badges/useFlipGridAnimation.ts-304-        cleanup();
app/home/modules/badges/useFlipGridAnimation.ts-305-      }, durationMs + 120);
app/home/modules/badges/useFlipGridAnimation.ts-306-
--
app/home/modules/badges/useFlipGridAnimation.ts-326-    keys,
app/home/modules/badges/useFlipGridAnimation.ts-327-    disabled,
app/home/modules/badges/useFlipGridAnimation.ts-328-    durationMs,
app/home/modules/badges/useFlipGridAnimation.ts-329-    easing,
app/home/modules/badges/useFlipGridAnimation.ts-330-    layoutDependency,
app/home/modules/badges/useFlipGridAnimation.ts:331:    captureBaselineToken,
app/home/modules/badges/useFlipGridAnimation.ts-332-    cancelScheduledAnimationFrames,
app/home/modules/badges/useFlipGridAnimation.ts-333-    clearActiveAnimations,
app/home/modules/badges/useFlipGridAnimation.ts-334-    resetTrackedNodeStyles,
app/home/modules/badges/useFlipGridAnimation.ts-335-    scheduleBaselineCapture,
app/home/modules/badges/useFlipGridAnimation.ts-336-  ]);
--
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-716-        animation: portalBadgeCentreRadiance 3400ms ease-in-out infinite;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-717-        will-change: transform, opacity;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-718-      }
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-719-
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-720-      .portal-badge-unlock-host:hover .portal-badge-idle-glow,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:721:      .portal-badge-unlock-host:focus-within .portal-badge-idle-glow {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-722-        opacity: 0.42;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-723-        transform: scale(1.06);
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-724-      }
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-725-
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-726-      .portal-badge-embers {
--
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-729-          opacity 180ms ease,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-730-          transform 180ms ease;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-731-      }
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-732-
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-733-      .portal-badge-unlock-host:hover .portal-badge-embers,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:734:      .portal-badge-unlock-host:focus-within .portal-badge-embers {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-735-        opacity: 0.92;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-736-        transform: translateY(-1px);
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-737-      }
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-738-
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-739-      .portal-badge-spark-a,
--
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-865-      .portal-badge-burst-c {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-866-        transform-origin: 50% 100%;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-867-      }
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-868-
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-869-      .portal-badge-unlock-host:hover .portal-badge-spark-a,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:870:      .portal-badge-unlock-host:focus-within .portal-badge-spark-a {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-871-        animation: portalBadgeEmberBurstA 950ms cubic-bezier(0.2, 0.72, 0.28, 1)
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-872-          infinite;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-873-      }
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-874-
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-875-      .portal-badge-unlock-host:hover .portal-badge-spark-b,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:876:      .portal-badge-unlock-host:focus-within .portal-badge-spark-b {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-877-        animation: portalBadgeEmberBurstB 1100ms
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-878-          cubic-bezier(0.18, 0.75, 0.3, 1) infinite 120ms;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-879-      }
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-880-
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-881-      .portal-badge-unlock-host:hover .portal-badge-spark-c,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:882:      .portal-badge-unlock-host:focus-within .portal-badge-spark-c {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-883-        animation: portalBadgeEmberBurstC 1000ms cubic-bezier(0.24, 0.7, 0.3, 1)
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-884-          infinite 220ms;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-885-      }
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-886-
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-887-      .portal-badge-unlock-host:hover .portal-badge-burst-a,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:888:      .portal-badge-unlock-host:focus-within .portal-badge-burst-a {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-889-        animation: portalBadgeEmberBurstA 820ms cubic-bezier(0.2, 0.74, 0.28, 1)
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-890-          infinite 40ms;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-891-      }
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-892-
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-893-      .portal-badge-unlock-host:hover .portal-badge-burst-b,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:894:      .portal-badge-unlock-host:focus-within .portal-badge-burst-b {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-895-        animation: portalBadgeEmberBurstB 900ms cubic-bezier(0.18, 0.76, 0.3, 1)
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-896-          infinite 180ms;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-897-      }
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-898-
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-899-      .portal-badge-unlock-host:hover .portal-badge-burst-c,
app/home/modules/badges/BadgeUnlockVisualStyles.tsx:900:      .portal-badge-unlock-host:focus-within .portal-badge-burst-c {
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-901-        animation: portalBadgeEmberBurstC 860ms cubic-bezier(0.22, 0.72, 0.3, 1)
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-902-          infinite 300ms;
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-903-      }
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-904-
app/home/modules/badges/BadgeUnlockVisualStyles.tsx-905-      .portal-badge-final-art-shell {
--
app/home/modules/badges/BuildRevealMaskCanvas.tsx-267-
app/home/modules/badges/BuildRevealMaskCanvas.tsx-268-  if (!isActive || !maskDataUrl) return null;
app/home/modules/badges/BuildRevealMaskCanvas.tsx-269-
app/home/modules/badges/BuildRevealMaskCanvas.tsx-270-  return (
app/home/modules/badges/BuildRevealMaskCanvas.tsx-271-    <div
app/home/modules/badges/BuildRevealMaskCanvas.tsx:272:      aria-hidden="true"
app/home/modules/badges/BuildRevealMaskCanvas.tsx-273-      className={className}
app/home/modules/badges/BuildRevealMaskCanvas.tsx-274-      style={{
app/home/modules/badges/BuildRevealMaskCanvas.tsx-275-        position: "absolute",
app/home/modules/badges/BuildRevealMaskCanvas.tsx-276-        inset: 0,
app/home/modules/badges/BuildRevealMaskCanvas.tsx-277-        pointerEvents: "none",

===== TYPESCRIPT / ESLINT / PACKAGE CONFIG =====


----- tsconfig.json -----
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules"]
}


----- package.json -----
{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.962.0",
    "@aws-sdk/s3-request-presigner": "^3.962.0",
    "@clerk/nextjs": "^6.36.5",
    "@mux/mux-node": "^12.8.1",
    "@portabletext/react": "^6.0.2",
    "@portabletext/types": "^4.0.1",
    "@react-email/components": "^1.0.6",
    "@react-email/render": "^2.0.4",
    "@sanity/client": "^7.13.2",
    "@sanity/image-url": "^1.2.0",
    "@sanity/vision": "^4.22.0",
    "@tiptap/extension-link": "^3.20.0",
    "@tiptap/extension-placeholder": "^3.20.0",
    "@tiptap/react": "^3.20.0",
    "@tiptap/starter-kit": "^3.20.0",
    "@vercel/postgres": "^0.10.0",
    "hls.js": "^1.6.15",
    "jose": "^6.1.3",
    "next": "16.1.1",
    "next-sanity": "^11.6.12",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "react-email": "^5.2.5",
    "resend": "^6.8.0",
    "sanity": "^4.22.0",
    "stripe": "^20.1.0",
    "styled-components": "^6.1.19",
    "svix": "^1.84.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.1",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}


----- eslint.config.mjs -----
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;


===== CURRENT GIT STATUS =====

 M app/home/modules/MailbagFeedbackForm.tsx
 M app/home/modules/badges/BadgeCabinet.tsx
 M app/home/modules/badges/BadgeCabinetItem.tsx
 M app/home/modules/badges/BadgeUnlockVisual.tsx
 D bjr-sonar-exegesis-discourse-panel-patch-source.txt
 D bjr-sonar-use-exegesis-thread-patch-source.txt
 D bjr-sonar-use-portal-artist-posts-controller-patch-source.txt
 M lib/exegesis/richText.ts
 M lib/share.ts
?? bjr-badge-runtime-patch-source-20260810-120528.txt
?? bjr-mailbag-feedback-patch-source-20260810-115733.txt
?? bjr-richtext-patch-source-20260810-112553.txt
?? bjr-share-patch-source-20260810-110410.txt
?? sonarqube-export-20260810-102734/

===== CURRENT GIT DIFF — BADGE SUBSYSTEM =====

diff --git a/app/home/modules/badges/BadgeCabinet.tsx b/app/home/modules/badges/BadgeCabinet.tsx
index e9fdd64..60dddc5 100644
--- a/app/home/modules/badges/BadgeCabinet.tsx
+++ b/app/home/modules/badges/BadgeCabinet.tsx
@@ -15,9 +15,22 @@ import { useBadgeCabinetUnlockSequence } from "./useBadgeCabinetUnlockSequence";
 import { useFlipGridAnimation } from "./useFlipGridAnimation";
 import { usePrefersReducedMotion } from "./usePrefersReducedMotion";
 
-type Props = {
+type Props = Readonly<{
   badges: MemberDashboardBadge[];
-};
+}>;
+
+type DebugPanelProps = Readonly<{
+  candidateItems: BadgeCabinetItemModel[];
+  selectedKey: string | null;
+  unlockedKey: string | null;
+  onSelectKey: (key: string | null) => void;
+  onReplaySelected: () => void;
+  onReplayRandom: () => void;
+  onCelebrateSelected: () => void;
+  onCelebrateRandom: () => void;
+  onReset: () => void;
+}>;
+
 const SHOW_DEBUG_PANEL = false;
 const DEBUG_REPLAY_RESET_MS = 72;
 
@@ -38,50 +51,382 @@ function toBadgeAwardNotice(item: BadgeCabinetItemModel): BadgeAwardNotice {
   };
 }
 
-export default function BadgeCabinet(props: Props) {
-  const { badges } = props;
+function buildEffectiveBadges(
+  badges: MemberDashboardBadge[],
+  debugUnlockedKey: string | null,
+): MemberDashboardBadge[] {
+  if (!debugUnlockedKey) return badges;
 
-  const [expanded, setExpanded] = React.useState(false);
+  return badges.map((badge) => {
+    if (badge.key !== debugUnlockedKey) return badge;
+
+    return {
+      ...badge,
+      unlocked: true,
+      unlockedAt: new Date().toISOString(),
+    };
+  });
+}
+
+function useAdminDebugFlag(): boolean {
   const [isAdminDebug, setIsAdminDebug] = React.useState(false);
+
+  React.useEffect(() => {
+    if (typeof document === "undefined") return;
+
+    const syncAdminDebug = () => {
+      setIsAdminDebug(document.body.dataset.afIsAdmin === "1");
+    };
+
+    syncAdminDebug();
+
+    const observer = new MutationObserver(syncAdminDebug);
+    observer.observe(document.body, {
+      attributes: true,
+      attributeFilter: ["data-af-is-admin"],
+    });
+
+    return () => {
+      observer.disconnect();
+    };
+  }, []);
+
+  return isAdminDebug;
+}
+
+function useDebugSelection(
+  candidateItems: BadgeCabinetItemModel[],
+): readonly [
+  string | null,
+  React.Dispatch<React.SetStateAction<string | null>>,
+] {
+  const [selectedKey, setSelectedKey] = React.useState<string | null>(null);
+
+  React.useEffect(() => {
+    if (candidateItems.length === 0) {
+      setSelectedKey(null);
+      return;
+    }
+
+    setSelectedKey((current) => {
+      const currentStillAvailable =
+        current !== null && candidateItems.some((item) => item.key === current);
+
+      return currentStillAvailable ? current : (candidateItems[0]?.key ?? null);
+    });
+  }, [candidateItems]);
+
+  return [selectedKey, setSelectedKey] as const;
+}
+
+function useDebugReplay(): Readonly<{
+  debugUnlockedKey: string | null;
+  replayDebugUnlock: (badgeKey: string | null) => void;
+  clearDebugUnlock: () => void;
+}> {
   const [debugUnlockedKey, setDebugUnlockedKey] = React.useState<string | null>(
     null,
   );
-  const [debugSelectedKey, setDebugSelectedKey] = React.useState<string | null>(
-    null,
+  const debugReplayTimeoutRef = React.useRef<number | null>(null);
+
+  const clearReplayTimer = React.useCallback(() => {
+    if (debugReplayTimeoutRef.current === null) return;
+
+    window.clearTimeout(debugReplayTimeoutRef.current);
+    debugReplayTimeoutRef.current = null;
+  }, []);
+
+  React.useEffect(() => {
+    return clearReplayTimer;
+  }, [clearReplayTimer]);
+
+  const replayDebugUnlock = React.useCallback(
+    (badgeKey: string | null) => {
+      if (!badgeKey) return;
+
+      clearReplayTimer();
+      setDebugUnlockedKey(null);
+
+      debugReplayTimeoutRef.current = window.setTimeout(() => {
+        setDebugUnlockedKey(badgeKey);
+        debugReplayTimeoutRef.current = null;
+      }, DEBUG_REPLAY_RESET_MS);
+    },
+    [clearReplayTimer],
+  );
+
+  const clearDebugUnlock = React.useCallback(() => {
+    clearReplayTimer();
+    setDebugUnlockedKey(null);
+  }, [clearReplayTimer]);
+
+  return {
+    debugUnlockedKey,
+    replayDebugUnlock,
+    clearDebugUnlock,
+  };
+}
+
+function BadgeCabinetToggle(
+  props: Readonly<{
+    expanded: boolean;
+    prefersReducedMotion: boolean;
+    onToggle: () => void;
+  }>,
+) {
+  const { expanded, prefersReducedMotion, onToggle } = props;
+
+  return (
+    <button
+      type="button"
+      onClick={onToggle}
+      aria-expanded={expanded}
+      style={{
+        appearance: "none",
+        border: "none",
+        background: "transparent",
+        padding: 0,
+        margin: 0,
+        display: "inline-flex",
+        alignItems: "center",
+        justifyContent: "flex-start",
+        gap: 8,
+        minWidth: 0,
+        width: "fit-content",
+        color: "inherit",
+        cursor: "pointer",
+      }}
+    >
+      <span
+        style={{
+          fontSize: 10,
+          letterSpacing: 0.3,
+          textTransform: "uppercase",
+          lineHeight: 1.2,
+          opacity: 0.5,
+        }}
+      >
+        Badges
+      </span>
+
+      <span
+        aria-hidden="true"
+        style={{
+          display: "inline-block",
+          fontSize: 12,
+          lineHeight: 1,
+          opacity: 0.5,
+          transform: expanded ? "rotate(90deg)" : "rotate(0deg)",
+          transformOrigin: "50% 50%",
+          transition: prefersReducedMotion
+            ? undefined
+            : "transform 220ms ease, opacity 180ms ease",
+        }}
+      >
+        &gt;
+      </span>
+    </button>
+  );
+}
+
+function DebugActionButton(
+  props: Readonly<{
+    label: string;
+    disabled: boolean;
+    onClick: () => void;
+    transparent?: boolean;
+    enabledOpacity?: number;
+    disabledOpacity?: number;
+  }>,
+) {
+  const {
+    label,
+    disabled,
+    onClick,
+    transparent = false,
+    enabledOpacity = 0.9,
+    disabledOpacity = 0.5,
+  } = props;
+
+  return (
+    <button
+      type="button"
+      onClick={onClick}
+      disabled={disabled}
+      style={{
+        appearance: "none",
+        borderRadius: 999,
+        border: "1px solid rgba(255,255,255,0.12)",
+        background: transparent ? "transparent" : "rgba(255,255,255,0.05)",
+        color: "inherit",
+        padding: "7px 10px",
+        fontSize: 12,
+        lineHeight: 1.2,
+        cursor: disabled ? "default" : "pointer",
+        opacity: disabled ? disabledOpacity : enabledOpacity,
+      }}
+    >
+      {label}
+    </button>
   );
+}
+
+function BadgeCabinetDebugPanel(props: DebugPanelProps) {
+  const {
+    candidateItems,
+    selectedKey,
+    unlockedKey,
+    onSelectKey,
+    onReplaySelected,
+    onReplayRandom,
+    onCelebrateSelected,
+    onCelebrateRandom,
+    onReset,
+  } = props;
+
+  const hasCandidates = candidateItems.length > 0;
 
+  return (
+    <div
+      style={{
+        display: "grid",
+        gap: 8,
+        minWidth: 0,
+        padding: "8px 10px",
+        borderRadius: 12,
+        border: "1px solid rgba(255,255,255,0.10)",
+        background: "rgba(255,255,255,0.035)",
+      }}
+    >
+      <div
+        style={{
+          fontSize: 10,
+          letterSpacing: 0.3,
+          textTransform: "uppercase",
+          lineHeight: 1.2,
+          opacity: 0.5,
+        }}
+      >
+        Cabinet debug
+      </div>
+
+      <div
+        style={{
+          display: "grid",
+          gridTemplateColumns: "minmax(0, 1fr)",
+          gap: 8,
+        }}
+      >
+        <select
+          value={selectedKey ?? ""}
+          onChange={(event) => {
+            const nextKey = event.target.value.trim();
+            onSelectKey(nextKey || null);
+          }}
+          disabled={!hasCandidates}
+          style={{
+            minWidth: 0,
+            width: "100%",
+            borderRadius: 10,
+            border: "1px solid rgba(255,255,255,0.12)",
+            background: "rgba(0,0,0,0.18)",
+            color: "inherit",
+            padding: "8px 10px",
+            fontSize: 12,
+            lineHeight: 1.3,
+            opacity: hasCandidates ? 0.92 : 0.55,
+          }}
+        >
+          {!hasCandidates ? (
+            <option value="">No locked badges available</option>
+          ) : null}
+
+          {candidateItems.map((item) => (
+            <option key={item.key} value={item.key}>
+              {item.label}
+            </option>
+          ))}
+        </select>
+
+        <div
+          style={{
+            display: "flex",
+            flexWrap: "wrap",
+            gap: 8,
+          }}
+        >
+          <DebugActionButton
+            label="Replay selected"
+            disabled={!selectedKey}
+            onClick={onReplaySelected}
+          />
+          <DebugActionButton
+            label="Replay random"
+            disabled={!hasCandidates}
+            onClick={onReplayRandom}
+          />
+        </div>
+
+        <div
+          style={{
+            display: "flex",
+            flexWrap: "wrap",
+            gap: 8,
+          }}
+        >
+          <DebugActionButton
+            label="Celebrate selected"
+            disabled={!selectedKey}
+            onClick={onCelebrateSelected}
+          />
+          <DebugActionButton
+            label="Celebrate random"
+            disabled={!hasCandidates}
+            onClick={onCelebrateRandom}
+          />
+          <DebugActionButton
+            label="Reset"
+            disabled={!unlockedKey}
+            onClick={onReset}
+            transparent
+            enabledOpacity={0.78}
+            disabledOpacity={0.45}
+          />
+        </div>
+      </div>
+    </div>
+  );
+}
+
+export default function BadgeCabinet(props: Props) {
+  const { badges } = props;
+
+  const [expanded, setExpanded] = React.useState(false);
   const prefersReducedMotion = usePrefersReducedMotion();
+  const isAdminDebug = useAdminDebugFlag();
   const { announceBadge, resetOverlayDebugState } = useBadgeAwardOverlay();
-  const debugReplayTimeoutRef = React.useRef<number | null>(null);
 
   const sourceItems = React.useMemo(
     () => buildBadgeCabinetItems(badges),
     [badges],
   );
-
   const debugCandidateItems = React.useMemo(
     () => sourceItems.filter((item) => !item.unlocked),
     [sourceItems],
   );
-
-  const debugCandidateByKey = React.useMemo(() => {
-    return new Map(debugCandidateItems.map((item) => [item.key, item]));
-  }, [debugCandidateItems]);
-
-  const effectiveBadges = React.useMemo(() => {
-    if (!debugUnlockedKey) return badges;
-
-    return badges.map((badge) => {
-      if (badge.key !== debugUnlockedKey) return badge;
-
-      return {
-        ...badge,
-        unlocked: true,
-        unlockedAt: new Date().toISOString(),
-      };
-    });
-  }, [badges, debugUnlockedKey]);
-
+  const debugCandidateByKey = React.useMemo(
+    () => new Map(debugCandidateItems.map((item) => [item.key, item])),
+    [debugCandidateItems],
+  );
+  const [debugSelectedKey, setDebugSelectedKey] =
+    useDebugSelection(debugCandidateItems);
+  const { debugUnlockedKey, replayDebugUnlock, clearDebugUnlock } =
+    useDebugReplay();
+
+  const effectiveBadges = React.useMemo(
+    () => buildEffectiveBadges(badges, debugUnlockedKey),
+    [badges, debugUnlockedKey],
+  );
   const items = React.useMemo(
     () => buildBadgeCabinetItems(effectiveBadges),
     [effectiveBadges],
@@ -97,20 +442,16 @@ export default function BadgeCabinet(props: Props) {
     flipDurationMs,
     flipBaselineToken,
     resetUnlockSequence,
-  } = useBadgeCabinetUnlockSequence({
-    items,
-  });
+  } = useBadgeCabinetUnlockSequence({ items });
 
   const itemKeys = React.useMemo(
     () => displayItems.map((item) => item.key),
     [displayItems],
   );
-
   const displayLayoutToken = React.useMemo(
     () => displayItems.map((item) => item.key).join("|"),
     [displayItems],
   );
-
   const flipLayoutDependency = React.useMemo(
     () =>
       [
@@ -129,93 +470,23 @@ export default function BadgeCabinet(props: Props) {
     captureBaselineToken: flipBaselineToken,
   });
 
-  React.useEffect(() => {
-    if (typeof document === "undefined") return;
-
-    const syncAdminDebug = () => {
-      setIsAdminDebug(document.body.dataset.afIsAdmin === "1");
-    };
-
-    syncAdminDebug();
-
-    const observer = new MutationObserver(syncAdminDebug);
-    observer.observe(document.body, {
-      attributes: true,
-      attributeFilter: ["data-af-is-admin"],
-    });
-
-    return () => {
-      observer.disconnect();
-    };
-  }, []);
-
-  React.useEffect(() => {
-    return () => {
-      if (debugReplayTimeoutRef.current !== null) {
-        window.clearTimeout(debugReplayTimeoutRef.current);
-        debugReplayTimeoutRef.current = null;
-      }
-    };
-  }, []);
-
-  React.useEffect(() => {
-    if (debugCandidateItems.length === 0) {
-      setDebugSelectedKey(null);
-      return;
-    }
-
-    setDebugSelectedKey((current) => {
-      if (current && debugCandidateItems.some((item) => item.key === current)) {
-        return current;
-      }
-
-      return debugCandidateItems[0]?.key ?? null;
-    });
-  }, [debugCandidateItems]);
-
-  const replayDebugUnlock = React.useCallback((badgeKey: string | null) => {
-    if (!badgeKey) return;
-
-    if (debugReplayTimeoutRef.current !== null) {
-      window.clearTimeout(debugReplayTimeoutRef.current);
-      debugReplayTimeoutRef.current = null;
-    }
-
-    setDebugUnlockedKey(null);
-
-    debugReplayTimeoutRef.current = window.setTimeout(() => {
-      setDebugUnlockedKey(badgeKey);
-      debugReplayTimeoutRef.current = null;
-    }, DEBUG_REPLAY_RESET_MS);
-  }, []);
-
   const announceDebugOverlay = React.useCallback(
     (badgeKey: string | null) => {
       if (!badgeKey) return;
 
       const item = debugCandidateByKey.get(badgeKey);
-      if (!item) return;
-
-      announceBadge(toBadgeAwardNotice(item));
+      if (item) announceBadge(toBadgeAwardNotice(item));
     },
     [announceBadge, debugCandidateByKey],
   );
 
-  const handleReplaySelected = React.useCallback(() => {
-    replayDebugUnlock(debugSelectedKey);
-  }, [debugSelectedKey, replayDebugUnlock]);
-
   const handleReplayRandom = React.useCallback(() => {
     const randomItem = pickRandomItem(debugCandidateItems);
     if (!randomItem) return;
 
     setDebugSelectedKey(randomItem.key);
     replayDebugUnlock(randomItem.key);
-  }, [debugCandidateItems, replayDebugUnlock]);
-
-  const handleCelebrateSelected = React.useCallback(() => {
-    announceDebugOverlay(debugSelectedKey);
-  }, [announceDebugOverlay, debugSelectedKey]);
+  }, [debugCandidateItems, replayDebugUnlock, setDebugSelectedKey]);
 
   const handleCelebrateRandom = React.useCallback(() => {
     const randomItem = pickRandomItem(debugCandidateItems);
@@ -223,21 +494,18 @@ export default function BadgeCabinet(props: Props) {
 
     setDebugSelectedKey(randomItem.key);
     announceBadge(toBadgeAwardNotice(randomItem));
-  }, [announceBadge, debugCandidateItems]);
+  }, [announceBadge, debugCandidateItems, setDebugSelectedKey]);
 
   const handleResetDebug = React.useCallback(() => {
-    if (debugReplayTimeoutRef.current !== null) {
-      window.clearTimeout(debugReplayTimeoutRef.current);
-      debugReplayTimeoutRef.current = null;
-    }
-
-    setDebugUnlockedKey(null);
+    clearDebugUnlock();
     resetUnlockSequence();
     resetOverlayDebugState();
-  }, [resetOverlayDebugState, resetUnlockSequence]);
+  }, [clearDebugUnlock, resetOverlayDebugState, resetUnlockSequence]);
 
   if (items.length === 0) return null;
 
+  const showDebugPanel = isAdminDebug && SHOW_DEBUG_PANEL;
+
   return (
     <>
       <BadgeCabinetStyles />
@@ -265,237 +533,24 @@ export default function BadgeCabinet(props: Props) {
             minWidth: 0,
           }}
         >
-          <button
-            type="button"
-            onClick={() => setExpanded((current) => !current)}
-            aria-expanded={expanded}
-            style={{
-              appearance: "none",
-              border: "none",
-              background: "transparent",
-              padding: 0,
-              margin: 0,
-              display: "inline-flex",
-              alignItems: "center",
-              justifyContent: "flex-start",
-              gap: 8,
-              minWidth: 0,
-              width: "fit-content",
-              color: "inherit",
-              cursor: "pointer",
-            }}
-          >
-            <span
-              style={{
-                fontSize: 10,
-                letterSpacing: 0.3,
-                textTransform: "uppercase",
-                lineHeight: 1.2,
-                opacity: 0.5,
-              }}
-            >
-              Badges
-            </span>
-
-            <span
-              aria-hidden="true"
-              style={{
-                display: "inline-block",
-                fontSize: 12,
-                lineHeight: 1,
-                opacity: 0.5,
-                transform: expanded ? "rotate(90deg)" : "rotate(0deg)",
-                transformOrigin: "50% 50%",
-                transition: prefersReducedMotion
-                  ? undefined
-                  : "transform 220ms ease, opacity 180ms ease",
-              }}
-            >
-              &gt;
-            </span>
-          </button>
-
-          {isAdminDebug && SHOW_DEBUG_PANEL ? (
-            <div
-              style={{
-                display: "grid",
-                gap: 8,
-                minWidth: 0,
-                padding: "8px 10px",
-                borderRadius: 12,
-                border: "1px solid rgba(255,255,255,0.10)",
-                background: "rgba(255,255,255,0.035)",
-              }}
-            >
-              <div
-                style={{
-                  fontSize: 10,
-                  letterSpacing: 0.3,
-                  textTransform: "uppercase",
-                  lineHeight: 1.2,
-                  opacity: 0.5,
-                }}
-              >
-                Cabinet debug
-              </div>
-
-              <div
-                style={{
-                  display: "grid",
-                  gridTemplateColumns: "minmax(0, 1fr)",
-                  gap: 8,
-                }}
-              >
-                <select
-                  value={debugSelectedKey ?? ""}
-                  onChange={(event) => {
-                    const nextKey = event.target.value.trim();
-                    setDebugSelectedKey(nextKey || null);
-                  }}
-                  disabled={debugCandidateItems.length === 0}
-                  style={{
-                    minWidth: 0,
-                    width: "100%",
-                    borderRadius: 10,
-                    border: "1px solid rgba(255,255,255,0.12)",
-                    background: "rgba(0,0,0,0.18)",
-                    color: "inherit",
-                    padding: "8px 10px",
-                    fontSize: 12,
-                    lineHeight: 1.3,
-                    opacity: debugCandidateItems.length === 0 ? 0.55 : 0.92,
-                  }}
-                >
-                  {debugCandidateItems.length === 0 ? (
-                    <option value="">No locked badges available</option>
-                  ) : null}
-
-                  {debugCandidateItems.map((item) => (
-                    <option key={item.key} value={item.key}>
-                      {item.label}
-                    </option>
-                  ))}
-                </select>
-
-                <div
-                  style={{
-                    display: "flex",
-                    flexWrap: "wrap",
-                    gap: 8,
-                  }}
-                >
-                  <button
-                    type="button"
-                    onClick={handleReplaySelected}
-                    disabled={!debugSelectedKey}
-                    style={{
-                      appearance: "none",
-                      borderRadius: 999,
-                      border: "1px solid rgba(255,255,255,0.12)",
-                      background: "rgba(255,255,255,0.05)",
-                      color: "inherit",
-                      padding: "7px 10px",
-                      fontSize: 12,
-                      lineHeight: 1.2,
-                      cursor: debugSelectedKey ? "pointer" : "default",
-                      opacity: debugSelectedKey ? 0.9 : 0.5,
-                    }}
-                  >
-                    Replay selected
-                  </button>
-
-                  <button
-                    type="button"
-                    onClick={handleReplayRandom}
-                    disabled={debugCandidateItems.length === 0}
-                    style={{
-                      appearance: "none",
-                      borderRadius: 999,
-                      border: "1px solid rgba(255,255,255,0.12)",
-                      background: "rgba(255,255,255,0.05)",
-                      color: "inherit",
-                      padding: "7px 10px",
-                      fontSize: 12,
-                      lineHeight: 1.2,
-                      cursor:
-                        debugCandidateItems.length > 0 ? "pointer" : "default",
-                      opacity: debugCandidateItems.length > 0 ? 0.9 : 0.5,
-                    }}
-                  >
-                    Replay random
-                  </button>
-                </div>
-
-                <div
-                  style={{
-                    display: "flex",
-                    flexWrap: "wrap",
-                    gap: 8,
-                  }}
-                >
-                  <button
-                    type="button"
-                    onClick={handleCelebrateSelected}
-                    disabled={!debugSelectedKey}
-                    style={{
-                      appearance: "none",
-                      borderRadius: 999,
-                      border: "1px solid rgba(255,255,255,0.12)",
-                      background: "rgba(255,255,255,0.05)",
-                      color: "inherit",
-                      padding: "7px 10px",
-                      fontSize: 12,
-                      lineHeight: 1.2,
-                      cursor: debugSelectedKey ? "pointer" : "default",
-                      opacity: debugSelectedKey ? 0.9 : 0.5,
-                    }}
-                  >
-                    Celebrate selected
-                  </button>
-
-                  <button
-                    type="button"
-                    onClick={handleCelebrateRandom}
-                    disabled={debugCandidateItems.length === 0}
-                    style={{
-                      appearance: "none",
-                      borderRadius: 999,
-                      border: "1px solid rgba(255,255,255,0.12)",
-                      background: "rgba(255,255,255,0.05)",
-                      color: "inherit",
-                      padding: "7px 10px",
-                      fontSize: 12,
-                      lineHeight: 1.2,
-                      cursor:
-                        debugCandidateItems.length > 0 ? "pointer" : "default",
-                      opacity: debugCandidateItems.length > 0 ? 0.9 : 0.5,
-                    }}
-                  >
-                    Celebrate random
-                  </button>
-
-                  <button
-                    type="button"
-                    onClick={handleResetDebug}
-                    disabled={!debugUnlockedKey}
-                    style={{
-                      appearance: "none",
-                      borderRadius: 999,
-                      border: "1px solid rgba(255,255,255,0.12)",
-                      background: "transparent",
-                      color: "inherit",
-                      padding: "7px 10px",
-                      fontSize: 12,
-                      lineHeight: 1.2,
-                      cursor: debugUnlockedKey ? "pointer" : "default",
-                      opacity: debugUnlockedKey ? 0.78 : 0.45,
-                    }}
-                  >
-                    Reset
-                  </button>
-                </div>
-              </div>
-            </div>
+          <BadgeCabinetToggle
+            expanded={expanded}
+            prefersReducedMotion={prefersReducedMotion}
+            onToggle={() => setExpanded((current) => !current)}
+          />
+
+          {showDebugPanel ? (
+            <BadgeCabinetDebugPanel
+              candidateItems={debugCandidateItems}
+              selectedKey={debugSelectedKey}
+              unlockedKey={debugUnlockedKey}
+              onSelectKey={setDebugSelectedKey}
+              onReplaySelected={() => replayDebugUnlock(debugSelectedKey)}
+              onReplayRandom={handleReplayRandom}
+              onCelebrateSelected={() => announceDebugOverlay(debugSelectedKey)}
+              onCelebrateRandom={handleCelebrateRandom}
+              onReset={handleResetDebug}
+            />
           ) : null}
         </div>
 
diff --git a/app/home/modules/badges/BadgeCabinetItem.tsx b/app/home/modules/badges/BadgeCabinetItem.tsx
index 2a6f100..35f66ca 100644
--- a/app/home/modules/badges/BadgeCabinetItem.tsx
+++ b/app/home/modules/badges/BadgeCabinetItem.tsx
@@ -5,21 +5,179 @@ import React from "react";
 import type { BadgeCabinetItemModel } from "./badgeCabinetTypes";
 import BadgeUnlockVisual from "./BadgeUnlockVisual";
 
-type Props = {
+type Props = Readonly<{
   item: BadgeCabinetItemModel;
   expanded: boolean;
   isNewlyUnlocked: boolean;
   isUnlocking: boolean;
   itemRef?: React.Ref<HTMLDivElement>;
-};
+}>;
+
+function revealingClassName(
+  baseClassName: string,
+  revealingClassName: string,
+  isMetaRevealing: boolean,
+): string {
+  return isMetaRevealing
+    ? `${baseClassName} ${revealingClassName}`
+    : baseClassName;
+}
+
+function BadgeVisual(
+  props: Readonly<{
+    item: BadgeCabinetItemModel;
+    isNewlyUnlocked: boolean;
+    isUnlocking: boolean;
+  }>,
+) {
+  const { item, isNewlyUnlocked, isUnlocking } = props;
+  const accessibleTitle = item.unlocked ? item.titleText : "Locked badge";
+
+  return (
+    <div
+      role="img"
+      aria-label={accessibleTitle}
+      className="portal-member-badge-visual"
+      style={{
+        position: "relative",
+        width: "100%",
+        aspectRatio: "1 / 1",
+        overflow: "visible",
+        outline: "none",
+        perspective: "900px",
+        perspectiveOrigin: "50% 50%",
+      }}
+    >
+      <BadgeUnlockVisual
+        imageUrl={item.imageUrl}
+        label={item.label}
+        unlocked={item.unlocked}
+        isUnlocking={isUnlocking}
+        isNewlyUnlocked={isNewlyUnlocked}
+        variant="cabinet"
+      />
+    </div>
+  );
+}
+
+function BadgeMetaQuestionMark(
+  props: Readonly<{
+    isMetaRevealing: boolean;
+  }>,
+) {
+  const { isMetaRevealing } = props;
+
+  return (
+    <div
+      className={revealingClassName(
+        "portal-member-badge-question-mark",
+        "portal-member-badge-question-mark--dissolving",
+        isMetaRevealing,
+      )}
+      aria-hidden="true"
+      style={{
+        fontSize: 10,
+        lineHeight: 1.1,
+        letterSpacing: 0.12,
+        opacity: 0.3,
+        fontWeight: 400,
+        textAlign: "center",
+      }}
+    >
+      —
+    </div>
+  );
+}
+
+function UnlockedBadgeMeta(
+  props: Readonly<{
+    item: BadgeCabinetItemModel;
+    isMetaRevealing: boolean;
+  }>,
+) {
+  const { item, isMetaRevealing } = props;
+
+  return (
+    <div
+      className={revealingClassName(
+        "portal-member-badge-meta-revealed",
+        "portal-member-badge-meta-revealed--revealing",
+        isMetaRevealing,
+      )}
+    >
+      <div
+        className={revealingClassName(
+          "portal-member-badge-title",
+          "portal-member-badge-title--revealing",
+          isMetaRevealing,
+        )}
+        style={{
+          fontSize: 10,
+          letterSpacing: 0.7,
+          textTransform: "uppercase",
+          lineHeight: 1.2,
+          fontWeight: 950,
+          opacity: 0.7,
+          overflowWrap: "anywhere",
+        }}
+      >
+        {item.label}
+      </div>
+
+      {item.description ? (
+        <div
+          className={revealingClassName(
+            "portal-member-badge-description",
+            "portal-member-badge-description--revealing",
+            isMetaRevealing,
+          )}
+          style={{
+            marginTop: 4,
+            fontSize: 9,
+            lineHeight: 1.2,
+            opacity: 0.58,
+            overflowWrap: "anywhere",
+          }}
+        >
+          {item.description}
+        </div>
+      ) : null}
+    </div>
+  );
+}
+
+function BadgeMeta(
+  props: Readonly<{
+    item: BadgeCabinetItemModel;
+    isMetaRevealing: boolean;
+  }>,
+) {
+  const { item, isMetaRevealing } = props;
+  const showQuestionMark = !item.unlocked || isMetaRevealing;
+
+  return (
+    <div className="portal-member-badge-meta" aria-hidden="false">
+      <div className="portal-member-badge-meta-inner">
+        {showQuestionMark ? (
+          <BadgeMetaQuestionMark isMetaRevealing={isMetaRevealing} />
+        ) : null}
+
+        {item.unlocked ? (
+          <UnlockedBadgeMeta
+            item={item}
+            isMetaRevealing={isMetaRevealing}
+          />
+        ) : null}
+      </div>
+    </div>
+  );
+}
 
 export default function BadgeCabinetItem(props: Props) {
   const { item, expanded, isNewlyUnlocked, isUnlocking, itemRef } = props;
 
   const isMetaRevealing =
     expanded && item.unlocked && (isUnlocking || isNewlyUnlocked);
-  const accessibleTitle = item.unlocked ? item.titleText : "Locked badge";
-  const shouldRenderMeta = expanded;
 
   return (
     <div
@@ -36,103 +194,14 @@ export default function BadgeCabinetItem(props: Props) {
         minWidth: 0,
       }}
     >
-      <div
-        tabIndex={0}
-        aria-label={accessibleTitle}
-        className="portal-member-badge-visual"
-        style={{
-          position: "relative",
-          width: "100%",
-          aspectRatio: "1 / 1",
-          overflow: "visible",
-          outline: "none",
-          perspective: "900px",
-          perspectiveOrigin: "50% 50%",
-        }}
-      >
-        <BadgeUnlockVisual
-          imageUrl={item.imageUrl}
-          label={item.label}
-          unlocked={item.unlocked}
-          isUnlocking={isUnlocking}
-          isNewlyUnlocked={isNewlyUnlocked}
-          variant="cabinet"
-        />
-      </div>
+      <BadgeVisual
+        item={item}
+        isNewlyUnlocked={isNewlyUnlocked}
+        isUnlocking={isUnlocking}
+      />
 
-      {shouldRenderMeta ? (
-        <div className="portal-member-badge-meta" aria-hidden="false">
-          <div className="portal-member-badge-meta-inner">
-            {(!item.unlocked || isMetaRevealing) && (
-              <div
-                className={
-                  isMetaRevealing
-                    ? "portal-member-badge-question-mark portal-member-badge-question-mark--dissolving"
-                    : "portal-member-badge-question-mark"
-                }
-                aria-hidden="true"
-                style={{
-                  fontSize: 10,
-                  lineHeight: 1.1,
-                  letterSpacing: 0.12,
-                  opacity: 0.3,
-                  fontWeight: 400,
-                  textAlign: "center",
-                }}
-              >
-                —
-              </div>
-            )}
-
-            {item.unlocked ? (
-              <div
-                className={
-                  isMetaRevealing
-                    ? "portal-member-badge-meta-revealed portal-member-badge-meta-revealed--revealing"
-                    : "portal-member-badge-meta-revealed"
-                }
-              >
-                <div
-                  className={
-                    isMetaRevealing
-                      ? "portal-member-badge-title portal-member-badge-title--revealing"
-                      : "portal-member-badge-title"
-                  }
-                  style={{
-                    fontSize: 10,
-                    letterSpacing: 0.7,
-                    textTransform: "uppercase",
-                    lineHeight: 1.2,
-                    fontWeight: 950,
-                    opacity: 0.7,
-                    overflowWrap: "anywhere",
-                  }}
-                >
-                  {item.label}
-                </div>
-
-                {item.description ? (
-                  <div
-                    className={
-                      isMetaRevealing
-                        ? "portal-member-badge-description portal-member-badge-description--revealing"
-                        : "portal-member-badge-description"
-                    }
-                    style={{
-                      marginTop: 4,
-                      fontSize: 9,
-                      lineHeight: 1.2,
-                      opacity: 0.58,
-                      overflowWrap: "anywhere",
-                    }}
-                  >
-                    {item.description}
-                  </div>
-                ) : null}
-              </div>
-            ) : null}
-          </div>
-        </div>
+      {expanded ? (
+        <BadgeMeta item={item} isMetaRevealing={isMetaRevealing} />
       ) : null}
     </div>
   );
diff --git a/app/home/modules/badges/BadgeUnlockVisual.tsx b/app/home/modules/badges/BadgeUnlockVisual.tsx
index ebe3337..52bb573 100644
--- a/app/home/modules/badges/BadgeUnlockVisual.tsx
+++ b/app/home/modules/badges/BadgeUnlockVisual.tsx
@@ -4,19 +4,21 @@
 import React from "react";
 import BuildRevealMaskCanvas from "./BuildRevealMaskCanvas";
 
-type Props = {
+type Props = Readonly<{
   imageUrl: string | null;
   label: string;
   unlocked: boolean;
   isUnlocking: boolean;
   isNewlyUnlocked: boolean;
   variant: "cabinet" | "overlay";
-};
+}>;
 
 const REVEAL_DELAY_MS = 1210;
 const REVEAL_DURATION_MS = 600;
 
-function BadgeFallbackArt(props: { unlocked: boolean; label: string }) {
+function BadgeFallbackArt(
+  props: Readonly<{ unlocked: boolean; label: string }>,
+) {
   const { unlocked, label } = props;
 
   return (
@@ -39,12 +41,14 @@ function BadgeFallbackArt(props: { unlocked: boolean; label: string }) {
   );
 }
 
-function BadgeArtImage(props: {
-  imageUrl: string;
-  alt: string;
-  className?: string;
-  style?: React.CSSProperties;
-}) {
+function BadgeArtImage(
+  props: Readonly<{
+    imageUrl: string;
+    alt: string;
+    className?: string;
+    style?: React.CSSProperties;
+  }>,
+) {
   const { imageUrl, alt, className, style } = props;
 
   return (
@@ -68,330 +72,277 @@ function BadgeArtImage(props: {
   );
 }
 
-export default function BadgeUnlockVisual(props: Props) {
-  const { imageUrl, label, unlocked, isUnlocking, isNewlyUnlocked, variant } =
-    props;
+function BadgeUnlockedAtmosphere() {
+  return (
+    <>
+      <div
+        className="portal-badge-idle-glow"
+        style={{
+          position: "absolute",
+          inset: -6,
+          borderRadius: "50%",
+          background:
+            "radial-gradient(circle, rgba(255,255,255,0.11) 0%, rgba(255,255,255,0.05) 32%, rgba(255,255,255,0.00) 74%)",
+          filter: "blur(5px)",
+          pointerEvents: "none",
+          transition: "opacity 180ms ease, transform 180ms ease",
+        }}
+      />
 
-  const showUnlockedAtmosphere = unlocked;
-  const showUnlockEvent = isUnlocking || isNewlyUnlocked;
+      <div
+        className="portal-badge-inner-aura"
+        style={{
+          position: "absolute",
+          left: "50%",
+          top: "50%",
+          width: "58%",
+          height: "58%",
+          transform: "translate(-50%, -50%)",
+          borderRadius: "50%",
+          background:
+            "radial-gradient(circle, rgba(255,255,255,0.16) 0%, rgba(255,255,255,0.05) 46%, rgba(255,255,255,0) 76%)",
+          filter: "blur(5px)",
+          pointerEvents: "none",
+        }}
+      />
+
+      <div
+        className="portal-badge-embers"
+        style={{
+          position: "absolute",
+          left: "50%",
+          bottom: "-5%",
+          width: "74%",
+          height: "94%",
+          transform: "translateX(-50%)",
+          overflow: "visible",
+          pointerEvents: "none",
+        }}
+      >
+        <div
+          className="portal-badge-spark-a"
+          style={{
+            position: "absolute",
+            left: "31%",
+            bottom: "3%",
+            width: "5.6%",
+            height: "14.4%",
+          }}
+        />
+
+        <div
+          className="portal-badge-spark-b"
+          style={{
+            position: "absolute",
+            left: "45.5%",
+            bottom: "1%",
+            width: "4.8%",
+            height: "12.6%",
+          }}
+        />
+
+        <div
+          className="portal-badge-spark-c"
+          style={{
+            position: "absolute",
+            left: "59.5%",
+            bottom: "3.5%",
+            width: "4.6%",
+            height: "12.2%",
+          }}
+        />
+
+        <div
+          className="portal-badge-burst-a"
+          style={{
+            position: "absolute",
+            left: "37.5%",
+            bottom: "2%",
+            width: "4.2%",
+            height: "10.8%",
+          }}
+        />
+
+        <div
+          className="portal-badge-burst-b"
+          style={{
+            position: "absolute",
+            left: "50.5%",
+            bottom: "2.8%",
+            width: "4.2%",
+            height: "10.4%",
+          }}
+        />
+
+        <div
+          className="portal-badge-burst-c"
+          style={{
+            position: "absolute",
+            left: "62.5%",
+            bottom: "2.2%",
+            width: "3.4%",
+            height: "9.2%",
+          }}
+        />
+      </div>
+    </>
+  );
+}
+
+function UnlockGlint(
+  props: Readonly<{
+    stage: 1 | 2 | 3;
+  }>,
+) {
+  const { stage } = props;
 
   return (
     <div
-      className={`portal-badge-unlock-visual-inner portal-badge-unlock-visual-inner--${variant}`}
-      data-badge-variant={variant}
-      data-badge-unlocking={isUnlocking ? "true" : "false"}
-      data-badge-newly-unlocked={isNewlyUnlocked ? "true" : "false"}
-      aria-hidden="true"
+      className={`portal-badge-quarter-glint portal-badge-quarter-glint--stage-${stage}`}
+    />
+  );
+}
+
+function SpinStages(
+  props: Readonly<{
+    isUnlocking: boolean;
+    children: React.ReactNode;
+  }>,
+) {
+  const { isUnlocking, children } = props;
+  return (
+    <div
+      className={`portal-badge-spin-stage-1${
+        isUnlocking ? " portal-badge-spin-stage-1--unlocking" : ""
+      }`}
+      style={{
+        position: "absolute",
+        inset: 0,
+      }}
     >
+      {isUnlocking ? <UnlockGlint stage={1} /> : null}
+
       <div
-        className={unlocked ? undefined : "portal-badge-core--locked"}
+        className={`portal-badge-spin-stage-2${
+          isUnlocking ? " portal-badge-spin-stage-2--unlocking" : ""
+        }`}
         style={{
           position: "absolute",
           inset: 0,
         }}
       >
-        {showUnlockedAtmosphere ? (
-          <>
-            <div
-              className="portal-badge-idle-glow"
-              style={{
-                position: "absolute",
-                inset: -6,
-                borderRadius: "50%",
-                background:
-                  "radial-gradient(circle, rgba(255,255,255,0.11) 0%, rgba(255,255,255,0.05) 32%, rgba(255,255,255,0.00) 74%)",
-                filter: "blur(5px)",
-                pointerEvents: "none",
-                transition: "opacity 180ms ease, transform 180ms ease",
-              }}
-            />
-
-            <div
-              className="portal-badge-inner-aura"
-              style={{
-                position: "absolute",
-                left: "50%",
-                top: "50%",
-                width: "58%",
-                height: "58%",
-                transform: "translate(-50%, -50%)",
-                borderRadius: "50%",
-                background:
-                  "radial-gradient(circle, rgba(255,255,255,0.16) 0%, rgba(255,255,255,0.05) 46%, rgba(255,255,255,0) 76%)",
-                filter: "blur(5px)",
-                pointerEvents: "none",
-              }}
-            />
-
-            <div
-              className="portal-badge-embers"
-              style={{
-                position: "absolute",
-                left: "50%",
-                bottom: "-5%",
-                width: "74%",
-                height: "94%",
-                transform: "translateX(-50%)",
-                overflow: "visible",
-                pointerEvents: "none",
-              }}
-            >
-              <div
-                className="portal-badge-spark-a"
-                style={{
-                  position: "absolute",
-                  left: "31%",
-                  bottom: "3%",
-                  width: "5.6%",
-                  height: "14.4%",
-                }}
-              />
-
-              <div
-                className="portal-badge-spark-b"
-                style={{
-                  position: "absolute",
-                  left: "45.5%",
-                  bottom: "1%",
-                  width: "4.8%",
-                  height: "12.6%",
-                }}
-              />
-
-              <div
-                className="portal-badge-spark-c"
-                style={{
-                  position: "absolute",
-                  left: "59.5%",
-                  bottom: "3.5%",
-                  width: "4.6%",
-                  height: "12.2%",
-                }}
-              />
-
-              <div
-                className="portal-badge-burst-a"
-                style={{
-                  position: "absolute",
-                  left: "37.5%",
-                  bottom: "2%",
-                  width: "4.2%",
-                  height: "10.8%",
-                }}
-              />
-
-              <div
-                className="portal-badge-burst-b"
-                style={{
-                  position: "absolute",
-                  left: "50.5%",
-                  bottom: "2.8%",
-                  width: "4.2%",
-                  height: "10.4%",
-                }}
-              />
-
-              <div
-                className="portal-badge-burst-c"
-                style={{
-                  position: "absolute",
-                  left: "62.5%",
-                  bottom: "2.2%",
-                  width: "3.4%",
-                  height: "9.2%",
-                }}
-              />
-            </div>
-          </>
-        ) : null}
+        {isUnlocking ? <UnlockGlint stage={2} /> : null}
 
         <div
-          className={`portal-badge-spin-stage-1${
-            isUnlocking ? " portal-badge-spin-stage-1--unlocking" : ""
+          className={`portal-badge-spin-stage-3${
+            isUnlocking ? " portal-badge-spin-stage-3--unlocking" : ""
           }`}
           style={{
             position: "absolute",
             inset: 0,
           }}
         >
-          {isUnlocking ? (
-            <div className="portal-badge-quarter-glint portal-badge-quarter-glint--stage-1" />
-          ) : null}
-
-          <div
-            className={`portal-badge-spin-stage-2${
-              isUnlocking ? " portal-badge-spin-stage-2--unlocking" : ""
-            }`}
-            style={{
-              position: "absolute",
-              inset: 0,
-            }}
-          >
-            {isUnlocking ? (
-              <div className="portal-badge-quarter-glint portal-badge-quarter-glint--stage-2" />
-            ) : null}
-
-            <div
-              className={`portal-badge-spin-stage-3${
-                isUnlocking ? " portal-badge-spin-stage-3--unlocking" : ""
-              }`}
-              style={{
-                position: "absolute",
-                inset: 0,
-              }}
-            >
-              {isUnlocking ? (
-                <div className="portal-badge-quarter-glint portal-badge-quarter-glint--stage-3" />
-              ) : null}
-              <div
-                className={`portal-badge-art-shell${
-                  isUnlocking ? " portal-badge-art-shell--unlocking" : ""
-                }`}
-                style={{
-                  position: "absolute",
-                  inset: 0,
-                }}
-              >
-                {imageUrl ? (
-                  <>
-                    {!unlocked || isUnlocking ? (
-                      <BadgeArtImage
-                        imageUrl={imageUrl}
-                        alt=""
-                        className="portal-badge-art-base-greyscale"
-                        style={{
-                          opacity: isUnlocking ? 0.42 : 0.32,
-                          filter:
-                            "grayscale(1) saturate(0) brightness(0.96) contrast(0.94) blur(1.4px)",
-                          transform: "scale(1.02)",
-                        }}
-                      />
-                    ) : null}
-
-                    {isUnlocking ? (
-                      <>
-                        <BuildRevealMaskCanvas
-                          imageUrl={imageUrl}
-                          label={label}
-                          isActive={true}
-                          revealDelayMs={REVEAL_DELAY_MS}
-                          revealDurationMs={REVEAL_DURATION_MS}
-                          className="portal-badge-reveal-canvas-layer portal-badge-reveal-canvas-layer--unlocking"
-                        />
-
-                        <div
-                          className="portal-badge-unlock-energy-flare"
-                          style={{
-                            position: "absolute",
-                            inset: "-8%",
-                            borderRadius: "50%",
-                            pointerEvents: "none",
-                          }}
-                        />
-                      </>
-                    ) : null}
-
-                    {!isUnlocking ? (
-                      <div
-                        className="portal-badge-final-art-shell"
-                        style={{
-                          position: "absolute",
-                          inset: 0,
-                        }}
-                      >
-                        <BadgeArtImage
-                          imageUrl={imageUrl}
-                          alt={label}
-                          className="portal-badge-final-art-image"
-                          style={{
-                            filter: unlocked
-                              ? "drop-shadow(0 0 6px rgba(255,255,255,0.1))"
-                              : "grayscale(1) saturate(0) brightness(0.60) contrast(0.85) blur(0.2px)",
-                            opacity: unlocked ? 1 : 0.35,
-                          }}
-                        />
-
-                        {isNewlyUnlocked ? (
-                          <div
-                            className="portal-badge-final-shimmer portal-badge-final-shimmer--celebrating"
-                            style={{
-                              position: "absolute",
-                              inset: 0,
-                              pointerEvents: "none",
-                              WebkitMaskImage: `url(${imageUrl})`,
-                              maskImage: `url(${imageUrl})`,
-                              WebkitMaskRepeat: "no-repeat",
-                              maskRepeat: "no-repeat",
-                              WebkitMaskPosition: "center",
-                              maskPosition: "center",
-                              WebkitMaskSize: "contain",
-                              maskSize: "contain",
-                            }}
-                          />
-                        ) : null}
-                      </div>
-                    ) : null}
-                  </>
-                ) : (
-                  <BadgeFallbackArt unlocked={unlocked} label={label} />
-                )}
-
-                {isUnlocking ? (
-                  <>
-                    <div
-                      className="portal-badge-edge-spine portal-badge-edge-spine--left"
-                      style={{
-                        position: "absolute",
-                        left: "50%",
-                        top: "50%",
-                        width: "4.4%",
-                        height: "86%",
-                        borderRadius: 999,
-                        pointerEvents: "none",
-                      }}
-                    />
-
-                    <div
-                      className="portal-badge-edge-spine portal-badge-edge-spine--right"
-                      style={{
-                        position: "absolute",
-                        left: "50%",
-                        top: "50%",
-                        width: "4.4%",
-                        height: "86%",
-                        borderRadius: 999,
-                        pointerEvents: "none",
-                      }}
-                    />
-                  </>
-                ) : null}
-
-                {unlocked ? (
-                  <div
-                    className="portal-badge-centre-radiance"
-                    style={{
-                      position: "absolute",
-                      left: "50%",
-                      top: "50%",
-                      width: "50%",
-                      height: "50%",
-                      transform: "translate(-50%, -50%)",
-                      borderRadius: "50%",
-                      background:
-                        "radial-gradient(circle, rgba(255,255,255,0.13) 0%, rgba(255,255,255,0.045) 42%, rgba(255,255,255,0.00) 76%)",
-                      filter: "blur(4px)",
-                      pointerEvents: "none",
-                    }}
-                  />
-                ) : null}
-              </div>
-            </div>
-          </div>
+          {isUnlocking ? <UnlockGlint stage={3} /> : null}
+          {children}
         </div>
+      </div>
+    </div>
+  );
+}
+
+function FinalBadgeArt(
+  props: Readonly<{
+    imageUrl: string;
+    label: string;
+    unlocked: boolean;
+    isNewlyUnlocked: boolean;
+  }>,
+) {
+  const { imageUrl, label, unlocked, isNewlyUnlocked } = props;
+
+  return (
+    <div
+      className="portal-badge-final-art-shell"
+      style={{
+        position: "absolute",
+        inset: 0,
+      }}
+    >
+      <BadgeArtImage
+        imageUrl={imageUrl}
+        alt={label}
+        className="portal-badge-final-art-image"
+        style={{
+          filter: unlocked
+            ? "drop-shadow(0 0 6px rgba(255,255,255,0.1))"
+            : "grayscale(1) saturate(0) brightness(0.60) contrast(0.85) blur(0.2px)",
+          opacity: unlocked ? 1 : 0.35,
+        }}
+      />
+
+      {isNewlyUnlocked ? (
+        <div
+          className="portal-badge-final-shimmer portal-badge-final-shimmer--celebrating"
+          style={{
+            position: "absolute",
+            inset: 0,
+            pointerEvents: "none",
+            WebkitMaskImage: `url(${imageUrl})`,
+            maskImage: `url(${imageUrl})`,
+            WebkitMaskRepeat: "no-repeat",
+            maskRepeat: "no-repeat",
+            WebkitMaskPosition: "center",
+            maskPosition: "center",
+            WebkitMaskSize: "contain",
+            maskSize: "contain",
+          }}
+        />
+      ) : null}
+    </div>
+  );
+}
+
+function ImageBadgeArt(
+  props: Readonly<{
+    imageUrl: string;
+    label: string;
+    unlocked: boolean;
+    isUnlocking: boolean;
+    isNewlyUnlocked: boolean;
+  }>,
+) {
+  const { imageUrl, label, unlocked, isUnlocking, isNewlyUnlocked } = props;
+  const showGreyscaleBase = !unlocked || isUnlocking;
+
+  return (
+    <>
+      {showGreyscaleBase ? (
+        <BadgeArtImage
+          imageUrl={imageUrl}
+          alt=""
+          className="portal-badge-art-base-greyscale"
+          style={{
+            opacity: isUnlocking ? 0.42 : 0.32,
+            filter:
+              "grayscale(1) saturate(0) brightness(0.96) contrast(0.94) blur(1.4px)",
+            transform: "scale(1.02)",
+          }}
+        />
+      ) : null}
+
+      {isUnlocking ? (
+        <>
+          <BuildRevealMaskCanvas
+            imageUrl={imageUrl}
+            label={label}
+            isActive={true}
+            revealDelayMs={REVEAL_DELAY_MS}
+            revealDurationMs={REVEAL_DURATION_MS}
+            className="portal-badge-reveal-canvas-layer portal-badge-reveal-canvas-layer--unlocking"
+          />
 
-        {showUnlockEvent ? (
           <div
-            className="portal-badge-impact-flash"
+            className="portal-badge-unlock-energy-flare"
             style={{
               position: "absolute",
               inset: "-8%",
@@ -399,7 +350,160 @@ export default function BadgeUnlockVisual(props: Props) {
               pointerEvents: "none",
             }}
           />
-        ) : null}
+        </>
+      ) : null}
+
+      {!isUnlocking ? (
+        <FinalBadgeArt
+          imageUrl={imageUrl}
+          label={label}
+          unlocked={unlocked}
+          isNewlyUnlocked={isNewlyUnlocked}
+        />
+      ) : null}
+    </>
+  );
+}
+
+function UnlockEdgeSpines() {
+  return (
+    <>
+      <div
+        className="portal-badge-edge-spine portal-badge-edge-spine--left"
+        style={{
+          position: "absolute",
+          left: "50%",
+          top: "50%",
+          width: "4.4%",
+          height: "86%",
+          borderRadius: 999,
+          pointerEvents: "none",
+        }}
+      />
+
+      <div
+        className="portal-badge-edge-spine portal-badge-edge-spine--right"
+        style={{
+          position: "absolute",
+          left: "50%",
+          top: "50%",
+          width: "4.4%",
+          height: "86%",
+          borderRadius: 999,
+          pointerEvents: "none",
+        }}
+      />
+    </>
+  );
+}
+
+function CentreRadiance() {
+  return (
+    <div
+      className="portal-badge-centre-radiance"
+      style={{
+        position: "absolute",
+        left: "50%",
+        top: "50%",
+        width: "50%",
+        height: "50%",
+        transform: "translate(-50%, -50%)",
+        borderRadius: "50%",
+        background:
+          "radial-gradient(circle, rgba(255,255,255,0.13) 0%, rgba(255,255,255,0.045) 42%, rgba(255,255,255,0.00) 76%)",
+        filter: "blur(4px)",
+        pointerEvents: "none",
+      }}
+    />
+  );
+}
+
+function BadgeArtShell(
+  props: Readonly<{
+    imageUrl: string | null;
+    label: string;
+    unlocked: boolean;
+    isUnlocking: boolean;
+    isNewlyUnlocked: boolean;
+  }>,
+) {
+  const { imageUrl, label, unlocked, isUnlocking, isNewlyUnlocked } = props;
+
+  return (
+    <div
+      className={`portal-badge-art-shell${
+        isUnlocking ? " portal-badge-art-shell--unlocking" : ""
+      }`}
+      style={{
+        position: "absolute",
+        inset: 0,
+      }}
+    >
+      {imageUrl ? (
+        <ImageBadgeArt
+          imageUrl={imageUrl}
+          label={label}
+          unlocked={unlocked}
+          isUnlocking={isUnlocking}
+          isNewlyUnlocked={isNewlyUnlocked}
+        />
+      ) : (
+        <BadgeFallbackArt unlocked={unlocked} label={label} />
+      )}
+
+      {isUnlocking ? <UnlockEdgeSpines /> : null}
+      {unlocked ? <CentreRadiance /> : null}
+    </div>
+  );
+}
+
+function ImpactFlash() {
+  return (
+    <div
+      className="portal-badge-impact-flash"
+      style={{
+        position: "absolute",
+        inset: "-8%",
+        borderRadius: "50%",
+        pointerEvents: "none",
+      }}
+    />
+  );
+}
+
+export default function BadgeUnlockVisual(props: Props) {
+  const { imageUrl, label, unlocked, isUnlocking, isNewlyUnlocked, variant } =
+    props;
+  const showUnlockEvent = isUnlocking || isNewlyUnlocked;
+
+  return (
+    <div
+      className={`portal-badge-unlock-visual-inner portal-badge-unlock-visual-inner--${variant}`}
+      data-badge-variant={variant}
+      data-badge-unlocking={isUnlocking ? "true" : "false"}
+      data-badge-newly-unlocked={isNewlyUnlocked ? "true" : "false"}
+      aria-hidden="true"
+    >
+      <div
+        className={unlocked ? undefined : "portal-badge-core--locked"}
+        style={{
+          position: "absolute",
+          inset: 0,
+        }}
+      >
+        {unlocked ? <BadgeUnlockedAtmosphere /> : null}
+
+        <SpinStages isUnlocking={isUnlocking}>
+          <BadgeArtShell
+            imageUrl={imageUrl}
+            label={label}
+            unlocked={unlocked}
+            isUnlocking={isUnlocking}
+            isNewlyUnlocked={isNewlyUnlocked}
+          />
+        </SpinStages>
+
+        {showUnlockEvent ? <ImpactFlash /> : null}
       </div>
     </div>
   );

===== STAGED GIT DIFF — BADGE SUBSYSTEM =====



===== DIFF STAT — BADGE SUBSYSTEM =====

 app/home/modules/badges/BadgeCabinet.tsx      | 743 ++++++++++++++------------
 app/home/modules/badges/BadgeCabinetItem.tsx  | 269 ++++++----
 app/home/modules/badges/BadgeUnlockVisual.tsx | 728 ++++++++++++++-----------
 3 files changed, 984 insertions(+), 756 deletions(-)
